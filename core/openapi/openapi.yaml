openapi: 3.0.3
info:
  contact:
    email: hello@confighub.com
    name: ConfigHub Support
    url: https://confighub.com/
  description: ConfigHub API
  license:
    name: N/A
    url: https://confighub.com/TBD
  termsOfService: TBD
  title: ConfigHub API
  version: 0.1.0
servers:
- description: production
  url: https://hub.confighub.com/api
security:
- bearerAuth: []
paths:
  /_space:
    delete:
      description: Delete multiple spaces selected by query parameters
      operationId: BulkDeleteSpaces
      parameters:
      - description: |-
          The specified string is an expression for the purpose of filtering
          the list of Spaces returned. The expression syntax was inspired by SQL.
          It supports conjunctions using `AND` of relational expressions of the form *attribute*
          *operator* *attribute_or_literal*. The attribute names are case-sensitive and PascalCase,
          as in the JSON encoding.
          Strings support the following operators: `<`, `>`, `<=`, `>=`, `=`, `!=`, `LIKE`, `NOT LIKE`, `ILIKE`, `~~`, `!~~`, `~`, `~*`, `!~`, `!~*`, `IN`, `NOT IN`.
          String pattern operators: `LIKE` and `~~` for pattern matching with `%` and `_` wildcards,
          `ILIKE` for case-insensitive pattern matching, `NOT LIKE` and `!~~` for negated pattern matching.
          String regex operators: `~` for regex matching, `~*` for case-insensitive regex,
          `!~` and `!~*` for regex not matching (case-sensitive and insensitive).
          Integers support the following operators: `<`, `>`, `<=`, `>=`, `=`, `!=`, `IN`, `NOT IN`.
          UUIDs and boolean attributes support equality and inequality only.
          UUID and time literals must be quoted as string literals.
          String literals are quoted with single quotes, such as `'string'`.
          Time literals use the same form as when serialized as JSON,
          such as: `CreatedAt > '2025-02-18T23:16:34'`.
          Integer and boolean literals are also supported for attributes of those types.
          Arrays support the `?` operator to to match any element of the array,
          as in `ApprovedBy ? '7c61626f-ddbe-41af-93f6-b69f4ab6d308'`.
          Arrays can perform LEN() to check for length, as in `LEN(ApprovedBy) > 0`.
          Map support the dot notation to specify a particular map key, as in `Labels.tier = 'Backend'`.
          Maps support `IS NULL` and `IS NOT NULL` with dot notation to check for key absence or presence,
          as in `Labels.tier IS NULL` (key doesn't exist) or `Labels.tier IS NOT NULL` (key exists).
          Comparison results can be tested with `IS TRUE`, `IS FALSE`, `IS NOT TRUE`, and `IS NOT FALSE`.
          These are useful for nullable columns: `MergeSourceID = '<uuid>' IS NOT FALSE` matches rows where MergeSourceID equals the value OR is NULL.
          The `IN` and `NOT IN` operators accept a comma-separated list of values in parentheses,
          such as `Slug IN ('slugone', 'slugtwo')` or `Labels.environment IN ('prod', 'staging')`.
          Conjunctions are supported using the `AND` operator.
          An example conjunction is:
          `CreatedAt >= '2025-01-07' AND Slug = 'test' AND Labels.mykey = 'myvalue'`.

          Supported attributes for filtering on Space: AttributeFilterID, AttributeHash, AttributeIDs, CreatedAt, DeleteGates, DisplayName, Labels, OrganizationID, Permissions, Slug, SpaceID, TriggerFilterID, TriggerHash, TriggerIDs, UpdatedAt.

          The whole string must be query-encoded.
        in: query
        name: where
        required: false
        schema:
          type: string
      - description: |-
          UUID of a Filter entity to apply to the Space list.

          The Filter must be in the same Organization as the user credentials.

          The Filter's From field must match the entity type being filtered (Space).

          For Space-resident entities, if the Filter has a FromSpaceID, it must match the operation's SpaceID.

          The Filter's Where clause will be combined with any explicit 'where' parameter using AND logic.

          If both 'filter' and 'where' parameters are specified, they are combined with AND logic.
        in: query
        name: filter
        required: false
        schema:
          type: string
      - description: |-
          Free text search that approximately matches the specified string against string fields and map keys/values.

          The search is case-insensitive and uses pattern matching to find entities containing the text.

          Searchable string fields include attributes like Slug, DisplayName, and string-typed custom fields.

          For map fields (like Labels and Annotations), the search matches both map keys and values.

          The search uses OR logic across all searchable fields, so matching any field will return the entity.

          If both 'where' and 'contains' parameters are specified, they are combined with AND logic.

          Searchable fields for Space include string and map-type attributes from the queryable attributes list.

          The whole string must be query-encoded.
        in: query
        name: contains
        required: false
        schema:
          type: string
      - description: |-
          Include clause for expanding related entities in the response for Space.
          The attribute names are case-sensitive, PascalCase, and
          expected in a comma-separated list format as in the JSON encoding.

          Supported attributes for Space are AttributeFilterID, AttributeIDs, OrganizationID, TriggerFilterID, TriggerIDs.

          The whole string must be query-encoded.
        in: query
        name: include
        required: false
        schema:
          type: string
      - description: Valid values are true and false. False is the default if unspecified.
          If true, recursively delete all entities within the deleted space(s) so
          long as none have delete gates.
        in: query
        name: recursive
        required: false
        schema:
          type: string
      - description: Valid values are true and false. False is the default if unspecified.
          If true, recursively delete all entities within the deleted space(s) regardless
          whether any have delete gates.
        in: query
        name: recursive_force
        required: false
        schema:
          type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                items:
                  $ref: '#/components/schemas/DeleteResponse'
                type: array
          description: OK
        "207":
          content:
            application/json:
              schema:
                items:
                  $ref: '#/components/schemas/DeleteResponse'
                type: array
          description: 'Multi-Status: Mixed success and failure results'
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Space request is invalid (Bad Request).
        "401":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Unauthorized access.
        "403":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Forbidden access.
        "404":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Space not found.
        "409":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Space data conflict. Data has changed since last read.
        "422":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Unable to delete entity
        "500":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Something went wrong while processing Space.
        default:
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Unexpected error.
      summary: Bulk delete multiple spaces
      tags:
      - Space
    patch:
      description: Apply JSON merge patch to multiple spaces selected by query parameters
      operationId: BulkPatchSpaces
      parameters:
      - description: |-
          The specified string is an expression for the purpose of filtering
          the list of Spaces returned. The expression syntax was inspired by SQL.
          It supports conjunctions using `AND` of relational expressions of the form *attribute*
          *operator* *attribute_or_literal*. The attribute names are case-sensitive and PascalCase,
          as in the JSON encoding.
          Strings support the following operators: `<`, `>`, `<=`, `>=`, `=`, `!=`, `LIKE`, `NOT LIKE`, `ILIKE`, `~~`, `!~~`, `~`, `~*`, `!~`, `!~*`, `IN`, `NOT IN`.
          String pattern operators: `LIKE` and `~~` for pattern matching with `%` and `_` wildcards,
          `ILIKE` for case-insensitive pattern matching, `NOT LIKE` and `!~~` for negated pattern matching.
          String regex operators: `~` for regex matching, `~*` for case-insensitive regex,
          `!~` and `!~*` for regex not matching (case-sensitive and insensitive).
          Integers support the following operators: `<`, `>`, `<=`, `>=`, `=`, `!=`, `IN`, `NOT IN`.
          UUIDs and boolean attributes support equality and inequality only.
          UUID and time literals must be quoted as string literals.
          String literals are quoted with single quotes, such as `'string'`.
          Time literals use the same form as when serialized as JSON,
          such as: `CreatedAt > '2025-02-18T23:16:34'`.
          Integer and boolean literals are also supported for attributes of those types.
          Arrays support the `?` operator to to match any element of the array,
          as in `ApprovedBy ? '7c61626f-ddbe-41af-93f6-b69f4ab6d308'`.
          Arrays can perform LEN() to check for length, as in `LEN(ApprovedBy) > 0`.
          Map support the dot notation to specify a particular map key, as in `Labels.tier = 'Backend'`.
          Maps support `IS NULL` and `IS NOT NULL` with dot notation to check for key absence or presence,
          as in `Labels.tier IS NULL` (key doesn't exist) or `Labels.tier IS NOT NULL` (key exists).
          Comparison results can be tested with `IS TRUE`, `IS FALSE`, `IS NOT TRUE`, and `IS NOT FALSE`.
          These are useful for nullable columns: `MergeSourceID = '<uuid>' IS NOT FALSE` matches rows where MergeSourceID equals the value OR is NULL.
          The `IN` and `NOT IN` operators accept a comma-separated list of values in parentheses,
          such as `Slug IN ('slugone', 'slugtwo')` or `Labels.environment IN ('prod', 'staging')`.
          Conjunctions are supported using the `AND` operator.
          An example conjunction is:
          `CreatedAt >= '2025-01-07' AND Slug = 'test' AND Labels.mykey = 'myvalue'`.

          Supported attributes for filtering on Space: AttributeFilterID, AttributeHash, AttributeIDs, CreatedAt, DeleteGates, DisplayName, Labels, OrganizationID, Permissions, Slug, SpaceID, TriggerFilterID, TriggerHash, TriggerIDs, UpdatedAt.

          The whole string must be query-encoded.
        in: query
        name: where
        required: false
        schema:
          type: string
      - description: |-
          UUID of a Filter entity to apply to the Space list.

          The Filter must be in the same Organization as the user credentials.

          The Filter's From field must match the entity type being filtered (Space).

          For Space-resident entities, if the Filter has a FromSpaceID, it must match the operation's SpaceID.

          The Filter's Where clause will be combined with any explicit 'where' parameter using AND logic.

          If both 'filter' and 'where' parameters are specified, they are combined with AND logic.
        in: query
        name: filter
        required: false
        schema:
          type: string
      - description: |-
          Free text search that approximately matches the specified string against string fields and map keys/values.

          The search is case-insensitive and uses pattern matching to find entities containing the text.

          Searchable string fields include attributes like Slug, DisplayName, and string-typed custom fields.

          For map fields (like Labels and Annotations), the search matches both map keys and values.

          The search uses OR logic across all searchable fields, so matching any field will return the entity.

          If both 'where' and 'contains' parameters are specified, they are combined with AND logic.

          Searchable fields for Space include string and map-type attributes from the queryable attributes list.

          The whole string must be query-encoded.
        in: query
        name: contains
        required: false
        schema:
          type: string
      - description: |-
          Include clause for expanding related entities in the response for Space.
          The attribute names are case-sensitive, PascalCase, and
          expected in a comma-separated list format as in the JSON encoding.

          Supported attributes for Space are AttributeFilterID, AttributeIDs, OrganizationID, TriggerFilterID, TriggerIDs.

          The whole string must be query-encoded.
        in: query
        name: include
        required: false
        schema:
          type: string
      - description: If true, re-list the Triggers matching WhereTrigger and/or TriggerFilterID
          even if these fields have not changed
        in: query
        name: refresh_triggers
        required: false
        schema:
          type: boolean
      requestBody:
        content:
          application/merge-patch+json:
            schema:
              description: JSON Merge Patch schema for Space. All fields are optional
                and nullable. Readonly fields are ignored.
              properties:
                Annotations:
                  additionalProperties:
                    nullable: true
                    type: string
                  description: An optional map of Annotation key/value pairs for tools
                    to attach information to entities.
                  nullable: true
                  type: object
                AttributeFilterID:
                  description: ""
                  format: uuid
                  nullable: true
                  type: string
                DeleteGates:
                  additionalProperties:
                    nullable: true
                    type: boolean
                  description: An optional set of gates that, if any is present, will
                    block deletion
                  nullable: true
                  type: object
                DisplayName:
                  description: Friendly name for the entity.
                  nullable: true
                  type: string
                Labels:
                  additionalProperties:
                    nullable: true
                    type: string
                  description: An optional map of Label key/value pairs to specify
                    identifying attributes of entities for the purpose of grouping
                    and filtering them.
                  nullable: true
                  type: object
                Permissions:
                  additionalProperties:
                    nullable: true
                    type: object
                  description: ""
                  nullable: true
                  type: object
                Slug:
                  description: Unique URL-safe identifier for the entity.
                  nullable: true
                  type: string
                TriggerFilterID:
                  description: ""
                  format: uuid
                  nullable: true
                  type: string
                Version:
                  description: An entity-specific sequence number used for optimistic
                    concurrency control. The value read must be sent in calls to Update.
                  nullable: true
                  type: integer
                WhereAttribute:
                  description: ""
                  nullable: true
                  type: string
                WhereTrigger:
                  description: ""
                  nullable: true
                  type: string
              type: object
      responses:
        "200":
          content:
            application/json:
              schema:
                items:
                  $ref: '#/components/schemas/SpaceCreateOrUpdateResponse'
                type: array
          description: OK
        "207":
          content:
            application/json:
              schema:
                items:
                  $ref: '#/components/schemas/SpaceCreateOrUpdateResponse'
                type: array
          description: 'Multi-Status: Mixed success and failure results'
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Space request is invalid (Bad Request).
        "401":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Unauthorized access.
        "403":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Forbidden access.
        "404":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Space not found.
        "409":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Space data conflict. Data has changed since last read.
        "500":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Something went wrong while processing Space.
        default:
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Unexpected error.
      summary: Bulk patch multiple spaces
      tags:
      - Space
    post:
      description: Clone multiple spaces selected by query parameters with optional
        name prefixes
      operationId: BulkCreateSpaces
      parameters:
      - description: |-
          The specified string is an expression for the purpose of filtering
          the list of Spaces returned. The expression syntax was inspired by SQL.
          It supports conjunctions using `AND` of relational expressions of the form *attribute*
          *operator* *attribute_or_literal*. The attribute names are case-sensitive and PascalCase,
          as in the JSON encoding.
          Strings support the following operators: `<`, `>`, `<=`, `>=`, `=`, `!=`, `LIKE`, `NOT LIKE`, `ILIKE`, `~~`, `!~~`, `~`, `~*`, `!~`, `!~*`, `IN`, `NOT IN`.
          String pattern operators: `LIKE` and `~~` for pattern matching with `%` and `_` wildcards,
          `ILIKE` for case-insensitive pattern matching, `NOT LIKE` and `!~~` for negated pattern matching.
          String regex operators: `~` for regex matching, `~*` for case-insensitive regex,
          `!~` and `!~*` for regex not matching (case-sensitive and insensitive).
          Integers support the following operators: `<`, `>`, `<=`, `>=`, `=`, `!=`, `IN`, `NOT IN`.
          UUIDs and boolean attributes support equality and inequality only.
          UUID and time literals must be quoted as string literals.
          String literals are quoted with single quotes, such as `'string'`.
          Time literals use the same form as when serialized as JSON,
          such as: `CreatedAt > '2025-02-18T23:16:34'`.
          Integer and boolean literals are also supported for attributes of those types.
          Arrays support the `?` operator to to match any element of the array,
          as in `ApprovedBy ? '7c61626f-ddbe-41af-93f6-b69f4ab6d308'`.
          Arrays can perform LEN() to check for length, as in `LEN(ApprovedBy) > 0`.
          Map support the dot notation to specify a particular map key, as in `Labels.tier = 'Backend'`.
          Maps support `IS NULL` and `IS NOT NULL` with dot notation to check for key absence or presence,
          as in `Labels.tier IS NULL` (key doesn't exist) or `Labels.tier IS NOT NULL` (key exists).
          Comparison results can be tested with `IS TRUE`, `IS FALSE`, `IS NOT TRUE`, and `IS NOT FALSE`.
          These are useful for nullable columns: `MergeSourceID = '<uuid>' IS NOT FALSE` matches rows where MergeSourceID equals the value OR is NULL.
          The `IN` and `NOT IN` operators accept a comma-separated list of values in parentheses,
          such as `Slug IN ('slugone', 'slugtwo')` or `Labels.environment IN ('prod', 'staging')`.
          Conjunctions are supported using the `AND` operator.
          An example conjunction is:
          `CreatedAt >= '2025-01-07' AND Slug = 'test' AND Labels.mykey = 'myvalue'`.

          Supported attributes for filtering on Space: AttributeFilterID, AttributeHash, AttributeIDs, CreatedAt, DeleteGates, DisplayName, Labels, OrganizationID, Permissions, Slug, SpaceID, TriggerFilterID, TriggerHash, TriggerIDs, UpdatedAt.

          The whole string must be query-encoded.
        in: query
        name: where
        required: false
        schema:
          type: string
      - description: |-
          UUID of a Filter entity to apply to the Space list.

          The Filter must be in the same Organization as the user credentials.

          The Filter's From field must match the entity type being filtered (Space).

          For Space-resident entities, if the Filter has a FromSpaceID, it must match the operation's SpaceID.

          The Filter's Where clause will be combined with any explicit 'where' parameter using AND logic.

          If both 'filter' and 'where' parameters are specified, they are combined with AND logic.
        in: query
        name: filter
        required: false
        schema:
          type: string
      - description: |-
          Free text search that approximately matches the specified string against string fields and map keys/values.

          The search is case-insensitive and uses pattern matching to find entities containing the text.

          Searchable string fields include attributes like Slug, DisplayName, and string-typed custom fields.

          For map fields (like Labels and Annotations), the search matches both map keys and values.

          The search uses OR logic across all searchable fields, so matching any field will return the entity.

          If both 'where' and 'contains' parameters are specified, they are combined with AND logic.

          Searchable fields for Space include string and map-type attributes from the queryable attributes list.

          The whole string must be query-encoded.
        in: query
        name: contains
        required: false
        schema:
          type: string
      - description: |-
          Include clause for expanding related entities in the response for Space.
          The attribute names are case-sensitive, PascalCase, and
          expected in a comma-separated list format as in the JSON encoding.

          Supported attributes for Space are AttributeFilterID, AttributeIDs, OrganizationID, TriggerFilterID, TriggerIDs.

          The whole string must be query-encoded.
        in: query
        name: include
        required: false
        schema:
          type: string
      - description: Comma-separated list of prefixes to apply to cloned Space names
        in: query
        name: name_prefixes
        required: false
        schema:
          type: string
      - description: Comma-separated list of labels with multiple values for cloned
          Space labels, in the format of key1=value1|value2,key2=value1|value2|value3
        in: query
        name: variant_labels
        required: false
        schema:
          type: string
      - description: 'A string for clone names, use the prefix ''template:'' for a
          Go-template with .SourceEntitySlug to access the original entity''s slug
          and .Labels to access variant labels, example: ''template:{{.SourceEntitySlug}}-{{.Labels.env}}'''
        in: query
        name: name_pattern
        required: false
        schema:
          type: string
      - description: Allowed values are true and false. Default is false. When true,
          reports success when an entity already exists and returns the existing entity
        in: query
        name: allow_exists
        required: false
        schema:
          type: string
      requestBody:
        content:
          application/merge-patch+json:
            schema:
              description: JSON Merge Patch schema for Space. All fields are optional
                and nullable. Readonly fields are ignored.
              properties:
                Annotations:
                  additionalProperties:
                    nullable: true
                    type: string
                  description: An optional map of Annotation key/value pairs for tools
                    to attach information to entities.
                  nullable: true
                  type: object
                AttributeFilterID:
                  description: ""
                  format: uuid
                  nullable: true
                  type: string
                DeleteGates:
                  additionalProperties:
                    nullable: true
                    type: boolean
                  description: An optional set of gates that, if any is present, will
                    block deletion
                  nullable: true
                  type: object
                DisplayName:
                  description: Friendly name for the entity.
                  nullable: true
                  type: string
                Labels:
                  additionalProperties:
                    nullable: true
                    type: string
                  description: An optional map of Label key/value pairs to specify
                    identifying attributes of entities for the purpose of grouping
                    and filtering them.
                  nullable: true
                  type: object
                Permissions:
                  additionalProperties:
                    nullable: true
                    type: object
                  description: ""
                  nullable: true
                  type: object
                Slug:
                  description: Unique URL-safe identifier for the entity.
                  nullable: true
                  type: string
                TriggerFilterID:
                  description: ""
                  format: uuid
                  nullable: true
                  type: string
                Version:
                  description: An entity-specific sequence number used for optimistic
                    concurrency control. The value read must be sent in calls to Update.
                  nullable: true
                  type: integer
                WhereAttribute:
                  description: ""
                  nullable: true
                  type: string
                WhereTrigger:
                  description: ""
                  nullable: true
                  type: string
              type: object
      responses:
        "200":
          content:
            application/json:
              schema:
                items:
                  $ref: '#/components/schemas/SpaceCreateOrUpdateResponse'
                type: array
          description: OK
        "207":
          content:
            application/json:
              schema:
                items:
                  $ref: '#/components/schemas/SpaceCreateOrUpdateResponse'
                type: array
          description: 'Multi-Status: Mixed success and failure results'
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Space request is invalid (Bad Request).
        "401":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Unauthorized access.
        "403":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Forbidden access.
        "404":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Space not found.
        "409":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Space data conflict. Data has changed since last read.
        "500":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Something went wrong while processing Space.
        default:
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Unexpected error.
      summary: Bulk create (clone) multiple spaces
      tags:
      - Space
  /attribute:
    delete:
      description: Delete multiple attributes selected by query parameters
      operationId: BulkDeleteAttributes
      parameters:
      - description: |-
          The specified string is an expression for the purpose of filtering
          the list of Attributes returned. The expression syntax was inspired by SQL.
          It supports conjunctions using `AND` of relational expressions of the form *attribute*
          *operator* *attribute_or_literal*. The attribute names are case-sensitive and PascalCase,
          as in the JSON encoding.
          Strings support the following operators: `<`, `>`, `<=`, `>=`, `=`, `!=`, `LIKE`, `NOT LIKE`, `ILIKE`, `~~`, `!~~`, `~`, `~*`, `!~`, `!~*`, `IN`, `NOT IN`.
          String pattern operators: `LIKE` and `~~` for pattern matching with `%` and `_` wildcards,
          `ILIKE` for case-insensitive pattern matching, `NOT LIKE` and `!~~` for negated pattern matching.
          String regex operators: `~` for regex matching, `~*` for case-insensitive regex,
          `!~` and `!~*` for regex not matching (case-sensitive and insensitive).
          Integers support the following operators: `<`, `>`, `<=`, `>=`, `=`, `!=`, `IN`, `NOT IN`.
          UUIDs and boolean attributes support equality and inequality only.
          UUID and time literals must be quoted as string literals.
          String literals are quoted with single quotes, such as `'string'`.
          Time literals use the same form as when serialized as JSON,
          such as: `CreatedAt > '2025-02-18T23:16:34'`.
          Integer and boolean literals are also supported for attributes of those types.
          Arrays support the `?` operator to to match any element of the array,
          as in `ApprovedBy ? '7c61626f-ddbe-41af-93f6-b69f4ab6d308'`.
          Arrays can perform LEN() to check for length, as in `LEN(ApprovedBy) > 0`.
          Map support the dot notation to specify a particular map key, as in `Labels.tier = 'Backend'`.
          Maps support `IS NULL` and `IS NOT NULL` with dot notation to check for key absence or presence,
          as in `Labels.tier IS NULL` (key doesn't exist) or `Labels.tier IS NOT NULL` (key exists).
          Comparison results can be tested with `IS TRUE`, `IS FALSE`, `IS NOT TRUE`, and `IS NOT FALSE`.
          These are useful for nullable columns: `MergeSourceID = '<uuid>' IS NOT FALSE` matches rows where MergeSourceID equals the value OR is NULL.
          The `IN` and `NOT IN` operators accept a comma-separated list of values in parentheses,
          such as `Slug IN ('slugone', 'slugtwo')` or `Labels.environment IN ('prod', 'staging')`.
          Conjunctions are supported using the `AND` operator.
          An example conjunction is:
          `CreatedAt >= '2025-01-07' AND Slug = 'test' AND Labels.mykey = 'myvalue'`.

          Supported attributes for filtering on Attribute: AttributeID, CreatedAt, DataType, DeleteGates, DisplayName, Hash, Labels, OrganizationID, Slug, SpaceID, ToolchainType, UpdatedAt.

          The whole string must be query-encoded.
        in: query
        name: where
        required: false
        schema:
          type: string
      - description: |-
          UUID of a Filter entity to apply to the Attribute list.

          The Filter must be in the same Organization as the user credentials.

          The Filter's From field must match the entity type being filtered (Attribute).

          For Space-resident entities, if the Filter has a FromSpaceID, it must match the operation's SpaceID.

          The Filter's Where clause will be combined with any explicit 'where' parameter using AND logic.

          If both 'filter' and 'where' parameters are specified, they are combined with AND logic.
        in: query
        name: filter
        required: false
        schema:
          type: string
      - description: |-
          Free text search that approximately matches the specified string against string fields and map keys/values.

          The search is case-insensitive and uses pattern matching to find entities containing the text.

          Searchable string fields include attributes like Slug, DisplayName, and string-typed custom fields.

          For map fields (like Labels and Annotations), the search matches both map keys and values.

          The search uses OR logic across all searchable fields, so matching any field will return the entity.

          If both 'where' and 'contains' parameters are specified, they are combined with AND logic.

          Searchable fields for Attribute include string and map-type attributes from the queryable attributes list.

          The whole string must be query-encoded.
        in: query
        name: contains
        required: false
        schema:
          type: string
      - description: |-
          Include clause for expanding related entities in the response for Attribute.
          The attribute names are case-sensitive, PascalCase, and
          expected in a comma-separated list format as in the JSON encoding.

          Supported attributes for Attribute are OrganizationID, SpaceID.

          The whole string must be query-encoded.
        in: query
        name: include
        required: false
        schema:
          type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                items:
                  $ref: '#/components/schemas/DeleteResponse'
                type: array
          description: OK
        "207":
          content:
            application/json:
              schema:
                items:
                  $ref: '#/components/schemas/DeleteResponse'
                type: array
          description: 'Multi-Status: Mixed success and failure results'
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Attribute request is invalid (Bad Request).
        "401":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Unauthorized access.
        "403":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Forbidden access.
        "404":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Attribute not found.
        "409":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Attribute data conflict. Data has changed since last read.
        "422":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Unable to delete entity
        "500":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Something went wrong while processing Attribute.
        default:
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Unexpected error.
      summary: Bulk delete multiple attributes
      tags:
      - Attribute
    get:
      description: Retrieves a list of Attributes across spaces in the Organization
      operationId: ListAllAttributes
      parameters:
      - description: |-
          The specified string is an expression for the purpose of filtering
          the list of Attributes returned. The expression syntax was inspired by SQL.
          It supports conjunctions using `AND` of relational expressions of the form *attribute*
          *operator* *attribute_or_literal*. The attribute names are case-sensitive and PascalCase,
          as in the JSON encoding.
          Strings support the following operators: `<`, `>`, `<=`, `>=`, `=`, `!=`, `LIKE`, `NOT LIKE`, `ILIKE`, `~~`, `!~~`, `~`, `~*`, `!~`, `!~*`, `IN`, `NOT IN`.
          String pattern operators: `LIKE` and `~~` for pattern matching with `%` and `_` wildcards,
          `ILIKE` for case-insensitive pattern matching, `NOT LIKE` and `!~~` for negated pattern matching.
          String regex operators: `~` for regex matching, `~*` for case-insensitive regex,
          `!~` and `!~*` for regex not matching (case-sensitive and insensitive).
          Integers support the following operators: `<`, `>`, `<=`, `>=`, `=`, `!=`, `IN`, `NOT IN`.
          UUIDs and boolean attributes support equality and inequality only.
          UUID and time literals must be quoted as string literals.
          String literals are quoted with single quotes, such as `'string'`.
          Time literals use the same form as when serialized as JSON,
          such as: `CreatedAt > '2025-02-18T23:16:34'`.
          Integer and boolean literals are also supported for attributes of those types.
          Arrays support the `?` operator to to match any element of the array,
          as in `ApprovedBy ? '7c61626f-ddbe-41af-93f6-b69f4ab6d308'`.
          Arrays can perform LEN() to check for length, as in `LEN(ApprovedBy) > 0`.
          Map support the dot notation to specify a particular map key, as in `Labels.tier = 'Backend'`.
          Maps support `IS NULL` and `IS NOT NULL` with dot notation to check for key absence or presence,
          as in `Labels.tier IS NULL` (key doesn't exist) or `Labels.tier IS NOT NULL` (key exists).
          Comparison results can be tested with `IS TRUE`, `IS FALSE`, `IS NOT TRUE`, and `IS NOT FALSE`.
          These are useful for nullable columns: `MergeSourceID = '<uuid>' IS NOT FALSE` matches rows where MergeSourceID equals the value OR is NULL.
          The `IN` and `NOT IN` operators accept a comma-separated list of values in parentheses,
          such as `Slug IN ('slugone', 'slugtwo')` or `Labels.environment IN ('prod', 'staging')`.
          Conjunctions are supported using the `AND` operator.
          An example conjunction is:
          `CreatedAt >= '2025-01-07' AND Slug = 'test' AND Labels.mykey = 'myvalue'`.

          Supported attributes for filtering on Attribute: AttributeID, CreatedAt, DataType, DeleteGates, DisplayName, Hash, Labels, OrganizationID, Slug, SpaceID, ToolchainType, UpdatedAt.

          The whole string must be query-encoded.
        in: query
        name: where
        required: false
        schema:
          type: string
      - description: |-
          UUID of a Filter entity to apply to the Attribute list.

          The Filter must be in the same Organization as the user credentials.

          The Filter's From field must match the entity type being filtered (Attribute).

          For Space-resident entities, if the Filter has a FromSpaceID, it must match the operation's SpaceID.

          The Filter's Where clause will be combined with any explicit 'where' parameter using AND logic.

          If both 'filter' and 'where' parameters are specified, they are combined with AND logic.
        in: query
        name: filter
        required: false
        schema:
          type: string
      - description: |-
          Free text search that approximately matches the specified string against string fields and map keys/values.

          The search is case-insensitive and uses pattern matching to find entities containing the text.

          Searchable string fields include attributes like Slug, DisplayName, and string-typed custom fields.

          For map fields (like Labels and Annotations), the search matches both map keys and values.

          The search uses OR logic across all searchable fields, so matching any field will return the entity.

          If both 'where' and 'contains' parameters are specified, they are combined with AND logic.

          Searchable fields for Attribute include string and map-type attributes from the queryable attributes list.

          The whole string must be query-encoded.
        in: query
        name: contains
        required: false
        schema:
          type: string
      - description: |-
          Include clause for expanding related entities in the response for Attribute.
          The attribute names are case-sensitive, PascalCase, and
          expected in a comma-separated list format as in the JSON encoding.

          Supported attributes for Attribute are OrganizationID, SpaceID.

          The whole string must be query-encoded.
        in: query
        name: include
        required: false
        schema:
          type: string
      - description: |-
          Select clause for specifying which fields to include in the response for Attribute.
          The attribute names are case-sensitive, PascalCase, and
          expected in a comma-separated list format as in the JSON encoding.
          If not specified, all fields are returned.
          Entity and parent IDs (like OrganizationID, SpaceID, AttributeID) and Slug are always returned regardless of the select parameter.
          Fields used in where and contains filters are also automatically included.
          Example: 'DisplayName,CreatedAt,Labels' will return only those fields plus the required ID and Slug fields.
          The whole string must be query-encoded.
        in: query
        name: select
        required: false
        schema:
          type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                items:
                  $ref: '#/components/schemas/ExtendedAttribute'
                type: array
          description: OK
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Attribute request is invalid (Bad Request).
        "401":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Unauthorized access.
        "403":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Forbidden access.
        "404":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Attribute not found.
        "500":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Something went wrong while processing Attribute.
        default:
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Unexpected error.
      summary: List of Attributes across spaces
      tags:
      - Attribute
    patch:
      description: Apply JSON merge patch to multiple attributes selected by query
        parameters
      operationId: BulkPatchAttributes
      parameters:
      - description: |-
          The specified string is an expression for the purpose of filtering
          the list of Attributes returned. The expression syntax was inspired by SQL.
          It supports conjunctions using `AND` of relational expressions of the form *attribute*
          *operator* *attribute_or_literal*. The attribute names are case-sensitive and PascalCase,
          as in the JSON encoding.
          Strings support the following operators: `<`, `>`, `<=`, `>=`, `=`, `!=`, `LIKE`, `NOT LIKE`, `ILIKE`, `~~`, `!~~`, `~`, `~*`, `!~`, `!~*`, `IN`, `NOT IN`.
          String pattern operators: `LIKE` and `~~` for pattern matching with `%` and `_` wildcards,
          `ILIKE` for case-insensitive pattern matching, `NOT LIKE` and `!~~` for negated pattern matching.
          String regex operators: `~` for regex matching, `~*` for case-insensitive regex,
          `!~` and `!~*` for regex not matching (case-sensitive and insensitive).
          Integers support the following operators: `<`, `>`, `<=`, `>=`, `=`, `!=`, `IN`, `NOT IN`.
          UUIDs and boolean attributes support equality and inequality only.
          UUID and time literals must be quoted as string literals.
          String literals are quoted with single quotes, such as `'string'`.
          Time literals use the same form as when serialized as JSON,
          such as: `CreatedAt > '2025-02-18T23:16:34'`.
          Integer and boolean literals are also supported for attributes of those types.
          Arrays support the `?` operator to to match any element of the array,
          as in `ApprovedBy ? '7c61626f-ddbe-41af-93f6-b69f4ab6d308'`.
          Arrays can perform LEN() to check for length, as in `LEN(ApprovedBy) > 0`.
          Map support the dot notation to specify a particular map key, as in `Labels.tier = 'Backend'`.
          Maps support `IS NULL` and `IS NOT NULL` with dot notation to check for key absence or presence,
          as in `Labels.tier IS NULL` (key doesn't exist) or `Labels.tier IS NOT NULL` (key exists).
          Comparison results can be tested with `IS TRUE`, `IS FALSE`, `IS NOT TRUE`, and `IS NOT FALSE`.
          These are useful for nullable columns: `MergeSourceID = '<uuid>' IS NOT FALSE` matches rows where MergeSourceID equals the value OR is NULL.
          The `IN` and `NOT IN` operators accept a comma-separated list of values in parentheses,
          such as `Slug IN ('slugone', 'slugtwo')` or `Labels.environment IN ('prod', 'staging')`.
          Conjunctions are supported using the `AND` operator.
          An example conjunction is:
          `CreatedAt >= '2025-01-07' AND Slug = 'test' AND Labels.mykey = 'myvalue'`.

          Supported attributes for filtering on Attribute: AttributeID, CreatedAt, DataType, DeleteGates, DisplayName, Hash, Labels, OrganizationID, Slug, SpaceID, ToolchainType, UpdatedAt.

          The whole string must be query-encoded.
        in: query
        name: where
        required: false
        schema:
          type: string
      - description: |-
          UUID of a Filter entity to apply to the Attribute list.

          The Filter must be in the same Organization as the user credentials.

          The Filter's From field must match the entity type being filtered (Attribute).

          For Space-resident entities, if the Filter has a FromSpaceID, it must match the operation's SpaceID.

          The Filter's Where clause will be combined with any explicit 'where' parameter using AND logic.

          If both 'filter' and 'where' parameters are specified, they are combined with AND logic.
        in: query
        name: filter
        required: false
        schema:
          type: string
      - description: |-
          Free text search that approximately matches the specified string against string fields and map keys/values.

          The search is case-insensitive and uses pattern matching to find entities containing the text.

          Searchable string fields include attributes like Slug, DisplayName, and string-typed custom fields.

          For map fields (like Labels and Annotations), the search matches both map keys and values.

          The search uses OR logic across all searchable fields, so matching any field will return the entity.

          If both 'where' and 'contains' parameters are specified, they are combined with AND logic.

          Searchable fields for Attribute include string and map-type attributes from the queryable attributes list.

          The whole string must be query-encoded.
        in: query
        name: contains
        required: false
        schema:
          type: string
      - description: |-
          Include clause for expanding related entities in the response for Attribute.
          The attribute names are case-sensitive, PascalCase, and
          expected in a comma-separated list format as in the JSON encoding.

          Supported attributes for Attribute are OrganizationID, SpaceID.

          The whole string must be query-encoded.
        in: query
        name: include
        required: false
        schema:
          type: string
      requestBody:
        content:
          application/merge-patch+json:
            schema:
              description: JSON Merge Patch schema for Attribute. All fields are optional
                and nullable. Readonly fields are ignored.
              properties:
                Annotations:
                  additionalProperties:
                    nullable: true
                    type: string
                  description: An optional map of Annotation key/value pairs for tools
                    to attach information to entities.
                  nullable: true
                  type: object
                DataType:
                  description: ""
                  nullable: true
                  type: string
                DeleteGates:
                  additionalProperties:
                    nullable: true
                    type: boolean
                  description: An optional set of gates that, if any is present, will
                    block deletion
                  nullable: true
                  type: object
                Description:
                  description: ""
                  nullable: true
                  type: string
                DisplayName:
                  description: Friendly name for the entity.
                  nullable: true
                  type: string
                Labels:
                  additionalProperties:
                    nullable: true
                    type: string
                  description: An optional map of Label key/value pairs to specify
                    identifying attributes of entities for the purpose of grouping
                    and filtering them.
                  nullable: true
                  type: object
                Parameters:
                  description: ""
                  items:
                    nullable: true
                    type: object
                  nullable: true
                  type: array
                ResourceTypePaths:
                  description: ""
                  items:
                    nullable: true
                    type: object
                  nullable: true
                  type: array
                Slug:
                  description: Unique URL-safe identifier for the entity.
                  nullable: true
                  type: string
                ToolchainType:
                  description: ""
                  nullable: true
                  type: string
                Version:
                  description: An entity-specific sequence number used for optimistic
                    concurrency control. The value read must be sent in calls to Update.
                  nullable: true
                  type: integer
              type: object
      responses:
        "200":
          content:
            application/json:
              schema:
                items:
                  $ref: '#/components/schemas/AttributeCreateOrUpdateResponse'
                type: array
          description: OK
        "207":
          content:
            application/json:
              schema:
                items:
                  $ref: '#/components/schemas/AttributeCreateOrUpdateResponse'
                type: array
          description: 'Multi-Status: Mixed success and failure results'
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Attribute request is invalid (Bad Request).
        "401":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Unauthorized access.
        "403":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Forbidden access.
        "404":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Attribute not found.
        "409":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Attribute data conflict. Data has changed since last read.
        "500":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Something went wrong while processing Attribute.
        default:
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Unexpected error.
      summary: Bulk patch multiple attributes
      tags:
      - Attribute
    post:
      description: Clone multiple attributes selected by query parameters with optional
        name prefixes and destination spaces
      operationId: BulkCreateAttributes
      parameters:
      - description: |-
          The specified string is an expression for the purpose of filtering
          the list of Attributes returned. The expression syntax was inspired by SQL.
          It supports conjunctions using `AND` of relational expressions of the form *attribute*
          *operator* *attribute_or_literal*. The attribute names are case-sensitive and PascalCase,
          as in the JSON encoding.
          Strings support the following operators: `<`, `>`, `<=`, `>=`, `=`, `!=`, `LIKE`, `NOT LIKE`, `ILIKE`, `~~`, `!~~`, `~`, `~*`, `!~`, `!~*`, `IN`, `NOT IN`.
          String pattern operators: `LIKE` and `~~` for pattern matching with `%` and `_` wildcards,
          `ILIKE` for case-insensitive pattern matching, `NOT LIKE` and `!~~` for negated pattern matching.
          String regex operators: `~` for regex matching, `~*` for case-insensitive regex,
          `!~` and `!~*` for regex not matching (case-sensitive and insensitive).
          Integers support the following operators: `<`, `>`, `<=`, `>=`, `=`, `!=`, `IN`, `NOT IN`.
          UUIDs and boolean attributes support equality and inequality only.
          UUID and time literals must be quoted as string literals.
          String literals are quoted with single quotes, such as `'string'`.
          Time literals use the same form as when serialized as JSON,
          such as: `CreatedAt > '2025-02-18T23:16:34'`.
          Integer and boolean literals are also supported for attributes of those types.
          Arrays support the `?` operator to to match any element of the array,
          as in `ApprovedBy ? '7c61626f-ddbe-41af-93f6-b69f4ab6d308'`.
          Arrays can perform LEN() to check for length, as in `LEN(ApprovedBy) > 0`.
          Map support the dot notation to specify a particular map key, as in `Labels.tier = 'Backend'`.
          Maps support `IS NULL` and `IS NOT NULL` with dot notation to check for key absence or presence,
          as in `Labels.tier IS NULL` (key doesn't exist) or `Labels.tier IS NOT NULL` (key exists).
          Comparison results can be tested with `IS TRUE`, `IS FALSE`, `IS NOT TRUE`, and `IS NOT FALSE`.
          These are useful for nullable columns: `MergeSourceID = '<uuid>' IS NOT FALSE` matches rows where MergeSourceID equals the value OR is NULL.
          The `IN` and `NOT IN` operators accept a comma-separated list of values in parentheses,
          such as `Slug IN ('slugone', 'slugtwo')` or `Labels.environment IN ('prod', 'staging')`.
          Conjunctions are supported using the `AND` operator.
          An example conjunction is:
          `CreatedAt >= '2025-01-07' AND Slug = 'test' AND Labels.mykey = 'myvalue'`.

          Supported attributes for filtering on Attribute: AttributeID, CreatedAt, DataType, DeleteGates, DisplayName, Hash, Labels, OrganizationID, Slug, SpaceID, ToolchainType, UpdatedAt.

          The whole string must be query-encoded.
        in: query
        name: where
        required: false
        schema:
          type: string
      - description: |-
          UUID of a Filter entity to apply to the Attribute list.

          The Filter must be in the same Organization as the user credentials.

          The Filter's From field must match the entity type being filtered (Attribute).

          For Space-resident entities, if the Filter has a FromSpaceID, it must match the operation's SpaceID.

          The Filter's Where clause will be combined with any explicit 'where' parameter using AND logic.

          If both 'filter' and 'where' parameters are specified, they are combined with AND logic.
        in: query
        name: filter
        required: false
        schema:
          type: string
      - description: |-
          Free text search that approximately matches the specified string against string fields and map keys/values.

          The search is case-insensitive and uses pattern matching to find entities containing the text.

          Searchable string fields include attributes like Slug, DisplayName, and string-typed custom fields.

          For map fields (like Labels and Annotations), the search matches both map keys and values.

          The search uses OR logic across all searchable fields, so matching any field will return the entity.

          If both 'where' and 'contains' parameters are specified, they are combined with AND logic.

          Searchable fields for Attribute include string and map-type attributes from the queryable attributes list.

          The whole string must be query-encoded.
        in: query
        name: contains
        required: false
        schema:
          type: string
      - description: |-
          Include clause for expanding related entities in the response for Attribute.
          The attribute names are case-sensitive, PascalCase, and
          expected in a comma-separated list format as in the JSON encoding.

          Supported attributes for Attribute are OrganizationID, SpaceID.

          The whole string must be query-encoded.
        in: query
        name: include
        required: false
        schema:
          type: string
      - description: Comma-separated list of prefixes to apply to cloned Attribute
          names
        in: query
        name: name_prefixes
        required: false
        schema:
          type: string
      - description: |-
          The specified string is an expression for the purpose of filtering
          the list of Spaces returned. The expression syntax was inspired by SQL.
          It supports conjunctions using `AND` of relational expressions of the form *attribute*
          *operator* *attribute_or_literal*. The attribute names are case-sensitive and PascalCase,
          as in the JSON encoding.
          Strings support the following operators: `<`, `>`, `<=`, `>=`, `=`, `!=`, `LIKE`, `NOT LIKE`, `ILIKE`, `~~`, `!~~`, `~`, `~*`, `!~`, `!~*`, `IN`, `NOT IN`.
          String pattern operators: `LIKE` and `~~` for pattern matching with `%` and `_` wildcards,
          `ILIKE` for case-insensitive pattern matching, `NOT LIKE` and `!~~` for negated pattern matching.
          String regex operators: `~` for regex matching, `~*` for case-insensitive regex,
          `!~` and `!~*` for regex not matching (case-sensitive and insensitive).
          Integers support the following operators: `<`, `>`, `<=`, `>=`, `=`, `!=`, `IN`, `NOT IN`.
          UUIDs and boolean attributes support equality and inequality only.
          UUID and time literals must be quoted as string literals.
          String literals are quoted with single quotes, such as `'string'`.
          Time literals use the same form as when serialized as JSON,
          such as: `CreatedAt > '2025-02-18T23:16:34'`.
          Integer and boolean literals are also supported for attributes of those types.
          Arrays support the `?` operator to to match any element of the array,
          as in `ApprovedBy ? '7c61626f-ddbe-41af-93f6-b69f4ab6d308'`.
          Arrays can perform LEN() to check for length, as in `LEN(ApprovedBy) > 0`.
          Map support the dot notation to specify a particular map key, as in `Labels.tier = 'Backend'`.
          Maps support `IS NULL` and `IS NOT NULL` with dot notation to check for key absence or presence,
          as in `Labels.tier IS NULL` (key doesn't exist) or `Labels.tier IS NOT NULL` (key exists).
          Comparison results can be tested with `IS TRUE`, `IS FALSE`, `IS NOT TRUE`, and `IS NOT FALSE`.
          These are useful for nullable columns: `MergeSourceID = '<uuid>' IS NOT FALSE` matches rows where MergeSourceID equals the value OR is NULL.
          The `IN` and `NOT IN` operators accept a comma-separated list of values in parentheses,
          such as `Slug IN ('slugone', 'slugtwo')` or `Labels.environment IN ('prod', 'staging')`.
          Conjunctions are supported using the `AND` operator.
          An example conjunction is:
          `CreatedAt >= '2025-01-07' AND Slug = 'test' AND Labels.mykey = 'myvalue'`.

          Supported attributes for filtering on Space: AttributeFilterID, AttributeHash, AttributeIDs, CreatedAt, DeleteGates, DisplayName, Labels, OrganizationID, Permissions, Slug, SpaceID, TriggerFilterID, TriggerHash, TriggerIDs, UpdatedAt.

          Where expression to select destination spaces for cloning attributes

          The whole string must be query-encoded.
        in: query
        name: where_space
        required: false
        schema:
          type: string
      - description: |-
          UUID of a Filter entity to apply to the Space list.

          The Filter must be in the same Organization as the user credentials.

          The Filter's From field must match the entity type being filtered (Space).

          For Space-resident entities, if the Filter has a FromSpaceID, it must match the operation's SpaceID.

          The Filter's Where clause will be combined with any explicit 'where' parameter using AND logic.

          If both 'filter' and 'where' parameters are specified, they are combined with AND logic.
        in: query
        name: filter_space
        required: false
        schema:
          type: string
      - description: Allowed values are true and false. Default is false. When true,
          reports success when an entity already exists and returns the existing entity
        in: query
        name: allow_exists
        required: false
        schema:
          type: string
      requestBody:
        content:
          application/merge-patch+json:
            schema:
              description: JSON Merge Patch schema for Attribute. All fields are optional
                and nullable. Readonly fields are ignored.
              properties:
                Annotations:
                  additionalProperties:
                    nullable: true
                    type: string
                  description: An optional map of Annotation key/value pairs for tools
                    to attach information to entities.
                  nullable: true
                  type: object
                DataType:
                  description: ""
                  nullable: true
                  type: string
                DeleteGates:
                  additionalProperties:
                    nullable: true
                    type: boolean
                  description: An optional set of gates that, if any is present, will
                    block deletion
                  nullable: true
                  type: object
                Description:
                  description: ""
                  nullable: true
                  type: string
                DisplayName:
                  description: Friendly name for the entity.
                  nullable: true
                  type: string
                Labels:
                  additionalProperties:
                    nullable: true
                    type: string
                  description: An optional map of Label key/value pairs to specify
                    identifying attributes of entities for the purpose of grouping
                    and filtering them.
                  nullable: true
                  type: object
                Parameters:
                  description: ""
                  items:
                    nullable: true
                    type: object
                  nullable: true
                  type: array
                ResourceTypePaths:
                  description: ""
                  items:
                    nullable: true
                    type: object
                  nullable: true
                  type: array
                Slug:
                  description: Unique URL-safe identifier for the entity.
                  nullable: true
                  type: string
                ToolchainType:
                  description: ""
                  nullable: true
                  type: string
                Version:
                  description: An entity-specific sequence number used for optimistic
                    concurrency control. The value read must be sent in calls to Update.
                  nullable: true
                  type: integer
              type: object
      responses:
        "200":
          content:
            application/json:
              schema:
                items:
                  $ref: '#/components/schemas/AttributeCreateOrUpdateResponse'
                type: array
          description: OK
        "207":
          content:
            application/json:
              schema:
                items:
                  $ref: '#/components/schemas/AttributeCreateOrUpdateResponse'
                type: array
          description: Multi-Status (partial success)
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Attribute request is invalid (Bad Request).
        "401":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Unauthorized access.
        "403":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Forbidden access.
        "404":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Attribute not found.
        "409":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Attribute data conflict. Data has changed since last read.
        "500":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Something went wrong while processing Attribute.
        default:
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Unexpected error.
      summary: Bulk create (clone) multiple attributes
      tags:
      - Attribute
  /bridge_worker:
    delete:
      description: Delete multiple bridge workers selected by query parameters
      operationId: BulkDeleteBridgeWorkers
      parameters:
      - description: |-
          The specified string is an expression for the purpose of filtering
          the list of BridgeWorkers returned. The expression syntax was inspired by SQL.
          It supports conjunctions using `AND` of relational expressions of the form *attribute*
          *operator* *attribute_or_literal*. The attribute names are case-sensitive and PascalCase,
          as in the JSON encoding.
          Strings support the following operators: `<`, `>`, `<=`, `>=`, `=`, `!=`, `LIKE`, `NOT LIKE`, `ILIKE`, `~~`, `!~~`, `~`, `~*`, `!~`, `!~*`, `IN`, `NOT IN`.
          String pattern operators: `LIKE` and `~~` for pattern matching with `%` and `_` wildcards,
          `ILIKE` for case-insensitive pattern matching, `NOT LIKE` and `!~~` for negated pattern matching.
          String regex operators: `~` for regex matching, `~*` for case-insensitive regex,
          `!~` and `!~*` for regex not matching (case-sensitive and insensitive).
          Integers support the following operators: `<`, `>`, `<=`, `>=`, `=`, `!=`, `IN`, `NOT IN`.
          UUIDs and boolean attributes support equality and inequality only.
          UUID and time literals must be quoted as string literals.
          String literals are quoted with single quotes, such as `'string'`.
          Time literals use the same form as when serialized as JSON,
          such as: `CreatedAt > '2025-02-18T23:16:34'`.
          Integer and boolean literals are also supported for attributes of those types.
          Arrays support the `?` operator to to match any element of the array,
          as in `ApprovedBy ? '7c61626f-ddbe-41af-93f6-b69f4ab6d308'`.
          Arrays can perform LEN() to check for length, as in `LEN(ApprovedBy) > 0`.
          Map support the dot notation to specify a particular map key, as in `Labels.tier = 'Backend'`.
          Maps support `IS NULL` and `IS NOT NULL` with dot notation to check for key absence or presence,
          as in `Labels.tier IS NULL` (key doesn't exist) or `Labels.tier IS NOT NULL` (key exists).
          Comparison results can be tested with `IS TRUE`, `IS FALSE`, `IS NOT TRUE`, and `IS NOT FALSE`.
          These are useful for nullable columns: `MergeSourceID = '<uuid>' IS NOT FALSE` matches rows where MergeSourceID equals the value OR is NULL.
          The `IN` and `NOT IN` operators accept a comma-separated list of values in parentheses,
          such as `Slug IN ('slugone', 'slugtwo')` or `Labels.environment IN ('prod', 'staging')`.
          Conjunctions are supported using the `AND` operator.
          An example conjunction is:
          `CreatedAt >= '2025-01-07' AND Slug = 'test' AND Labels.mykey = 'myvalue'`.

          Supported attributes for filtering on BridgeWorker: BridgeWorkerID, Condition, CreatedAt, DisplayName, IPAddress, Labels, LastMessage, LastSeenAt, OrgRole, OrganizationID, Permissions, Slug, SpaceID, UpdatedAt, UserID.

          The whole string must be query-encoded.
        in: query
        name: where
        required: false
        schema:
          type: string
      - description: |-
          UUID of a Filter entity to apply to the BridgeWorker list.

          The Filter must be in the same Organization as the user credentials.

          The Filter's From field must match the entity type being filtered (BridgeWorker).

          For Space-resident entities, if the Filter has a FromSpaceID, it must match the operation's SpaceID.

          The Filter's Where clause will be combined with any explicit 'where' parameter using AND logic.

          If both 'filter' and 'where' parameters are specified, they are combined with AND logic.
        in: query
        name: filter
        required: false
        schema:
          type: string
      - description: |-
          Free text search that approximately matches the specified string against string fields and map keys/values.

          The search is case-insensitive and uses pattern matching to find entities containing the text.

          Searchable string fields include attributes like Slug, DisplayName, and string-typed custom fields.

          For map fields (like Labels and Annotations), the search matches both map keys and values.

          The search uses OR logic across all searchable fields, so matching any field will return the entity.

          If both 'where' and 'contains' parameters are specified, they are combined with AND logic.

          Searchable fields for BridgeWorker include string and map-type attributes from the queryable attributes list.

          The whole string must be query-encoded.
        in: query
        name: contains
        required: false
        schema:
          type: string
      - description: |-
          Include clause for expanding related entities in the response for BridgeWorker.
          The attribute names are case-sensitive, PascalCase, and
          expected in a comma-separated list format as in the JSON encoding.

          Supported attributes for BridgeWorker are OrganizationID, SpaceID.

          The whole string must be query-encoded.
        in: query
        name: include
        required: false
        schema:
          type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                items:
                  $ref: '#/components/schemas/DeleteResponse'
                type: array
          description: OK
        "207":
          content:
            application/json:
              schema:
                items:
                  $ref: '#/components/schemas/DeleteResponse'
                type: array
          description: 'Multi-Status: Mixed success and failure results'
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: BridgeWorker request is invalid (Bad Request).
        "401":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Unauthorized access.
        "403":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Forbidden access.
        "404":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: BridgeWorker not found.
        "409":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: BridgeWorker data conflict. Data has changed since last read.
        "422":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: BridgeWorker could not be deleted.
        "500":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Something went wrong while processing BridgeWorker.
        default:
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Unexpected error.
      summary: Bulk delete multiple bridge workers
      tags:
      - BridgeWorker
    get:
      description: List all BridgeWorker across spaces
      operationId: ListAllBridgeWorkers
      parameters:
      - description: |-
          The specified string is an expression for the purpose of filtering
          the list of BridgeWorkers returned. The expression syntax was inspired by SQL.
          It supports conjunctions using `AND` of relational expressions of the form *attribute*
          *operator* *attribute_or_literal*. The attribute names are case-sensitive and PascalCase,
          as in the JSON encoding.
          Strings support the following operators: `<`, `>`, `<=`, `>=`, `=`, `!=`, `LIKE`, `NOT LIKE`, `ILIKE`, `~~`, `!~~`, `~`, `~*`, `!~`, `!~*`, `IN`, `NOT IN`.
          String pattern operators: `LIKE` and `~~` for pattern matching with `%` and `_` wildcards,
          `ILIKE` for case-insensitive pattern matching, `NOT LIKE` and `!~~` for negated pattern matching.
          String regex operators: `~` for regex matching, `~*` for case-insensitive regex,
          `!~` and `!~*` for regex not matching (case-sensitive and insensitive).
          Integers support the following operators: `<`, `>`, `<=`, `>=`, `=`, `!=`, `IN`, `NOT IN`.
          UUIDs and boolean attributes support equality and inequality only.
          UUID and time literals must be quoted as string literals.
          String literals are quoted with single quotes, such as `'string'`.
          Time literals use the same form as when serialized as JSON,
          such as: `CreatedAt > '2025-02-18T23:16:34'`.
          Integer and boolean literals are also supported for attributes of those types.
          Arrays support the `?` operator to to match any element of the array,
          as in `ApprovedBy ? '7c61626f-ddbe-41af-93f6-b69f4ab6d308'`.
          Arrays can perform LEN() to check for length, as in `LEN(ApprovedBy) > 0`.
          Map support the dot notation to specify a particular map key, as in `Labels.tier = 'Backend'`.
          Maps support `IS NULL` and `IS NOT NULL` with dot notation to check for key absence or presence,
          as in `Labels.tier IS NULL` (key doesn't exist) or `Labels.tier IS NOT NULL` (key exists).
          Comparison results can be tested with `IS TRUE`, `IS FALSE`, `IS NOT TRUE`, and `IS NOT FALSE`.
          These are useful for nullable columns: `MergeSourceID = '<uuid>' IS NOT FALSE` matches rows where MergeSourceID equals the value OR is NULL.
          The `IN` and `NOT IN` operators accept a comma-separated list of values in parentheses,
          such as `Slug IN ('slugone', 'slugtwo')` or `Labels.environment IN ('prod', 'staging')`.
          Conjunctions are supported using the `AND` operator.
          An example conjunction is:
          `CreatedAt >= '2025-01-07' AND Slug = 'test' AND Labels.mykey = 'myvalue'`.

          Supported attributes for filtering on BridgeWorker: BridgeWorkerID, Condition, CreatedAt, DisplayName, IPAddress, Labels, LastMessage, LastSeenAt, OrgRole, OrganizationID, Permissions, Slug, SpaceID, UpdatedAt, UserID.

          The whole string must be query-encoded.
        in: query
        name: where
        required: false
        schema:
          type: string
      - description: |-
          Free text search that approximately matches the specified string against string fields and map keys/values.

          The search is case-insensitive and uses pattern matching to find entities containing the text.

          Searchable string fields include attributes like Slug, DisplayName, and string-typed custom fields.

          For map fields (like Labels and Annotations), the search matches both map keys and values.

          The search uses OR logic across all searchable fields, so matching any field will return the entity.

          If both 'where' and 'contains' parameters are specified, they are combined with AND logic.

          Searchable fields for BridgeWorker include string and map-type attributes from the queryable attributes list.

          The whole string must be query-encoded.
        in: query
        name: contains
        required: false
        schema:
          type: string
      - description: |-
          Include clause for expanding related entities in the response for BridgeWorker.
          The attribute names are case-sensitive, PascalCase, and
          expected in a comma-separated list format as in the JSON encoding.

          Supported attributes for BridgeWorker are OrganizationID, SpaceID.

          The whole string must be query-encoded.
        in: query
        name: include
        required: false
        schema:
          type: string
      - description: |-
          Select clause for specifying which fields to include in the response for BridgeWorker.
          The attribute names are case-sensitive, PascalCase, and
          expected in a comma-separated list format as in the JSON encoding.
          If not specified, all fields are returned.
          Entity and parent IDs (like OrganizationID, SpaceID, BridgeWorkerID) and Slug are always returned regardless of the select parameter.
          Fields used in where and contains filters are also automatically included.
          Example: 'DisplayName,CreatedAt,Labels' will return only those fields plus the required ID and Slug fields.
          The whole string must be query-encoded.
        in: query
        name: select
        required: false
        schema:
          type: string
      - description: Include summary information in the response
        in: query
        name: summary
        required: false
        schema:
          type: boolean
      responses:
        "200":
          content:
            application/json:
              schema:
                items:
                  $ref: '#/components/schemas/ExtendedBridgeWorker'
                type: array
          description: OK
      summary: List all BridgeWorker across spaces
      tags:
      - BridgeWorker
    patch:
      description: Apply JSON merge patch to multiple bridge workers selected by query
        parameters
      operationId: BulkPatchBridgeWorkers
      parameters:
      - description: |-
          The specified string is an expression for the purpose of filtering
          the list of BridgeWorkers returned. The expression syntax was inspired by SQL.
          It supports conjunctions using `AND` of relational expressions of the form *attribute*
          *operator* *attribute_or_literal*. The attribute names are case-sensitive and PascalCase,
          as in the JSON encoding.
          Strings support the following operators: `<`, `>`, `<=`, `>=`, `=`, `!=`, `LIKE`, `NOT LIKE`, `ILIKE`, `~~`, `!~~`, `~`, `~*`, `!~`, `!~*`, `IN`, `NOT IN`.
          String pattern operators: `LIKE` and `~~` for pattern matching with `%` and `_` wildcards,
          `ILIKE` for case-insensitive pattern matching, `NOT LIKE` and `!~~` for negated pattern matching.
          String regex operators: `~` for regex matching, `~*` for case-insensitive regex,
          `!~` and `!~*` for regex not matching (case-sensitive and insensitive).
          Integers support the following operators: `<`, `>`, `<=`, `>=`, `=`, `!=`, `IN`, `NOT IN`.
          UUIDs and boolean attributes support equality and inequality only.
          UUID and time literals must be quoted as string literals.
          String literals are quoted with single quotes, such as `'string'`.
          Time literals use the same form as when serialized as JSON,
          such as: `CreatedAt > '2025-02-18T23:16:34'`.
          Integer and boolean literals are also supported for attributes of those types.
          Arrays support the `?` operator to to match any element of the array,
          as in `ApprovedBy ? '7c61626f-ddbe-41af-93f6-b69f4ab6d308'`.
          Arrays can perform LEN() to check for length, as in `LEN(ApprovedBy) > 0`.
          Map support the dot notation to specify a particular map key, as in `Labels.tier = 'Backend'`.
          Maps support `IS NULL` and `IS NOT NULL` with dot notation to check for key absence or presence,
          as in `Labels.tier IS NULL` (key doesn't exist) or `Labels.tier IS NOT NULL` (key exists).
          Comparison results can be tested with `IS TRUE`, `IS FALSE`, `IS NOT TRUE`, and `IS NOT FALSE`.
          These are useful for nullable columns: `MergeSourceID = '<uuid>' IS NOT FALSE` matches rows where MergeSourceID equals the value OR is NULL.
          The `IN` and `NOT IN` operators accept a comma-separated list of values in parentheses,
          such as `Slug IN ('slugone', 'slugtwo')` or `Labels.environment IN ('prod', 'staging')`.
          Conjunctions are supported using the `AND` operator.
          An example conjunction is:
          `CreatedAt >= '2025-01-07' AND Slug = 'test' AND Labels.mykey = 'myvalue'`.

          Supported attributes for filtering on BridgeWorker: BridgeWorkerID, Condition, CreatedAt, DisplayName, IPAddress, Labels, LastMessage, LastSeenAt, OrgRole, OrganizationID, Permissions, Slug, SpaceID, UpdatedAt, UserID.

          The whole string must be query-encoded.
        in: query
        name: where
        required: false
        schema:
          type: string
      - description: |-
          UUID of a Filter entity to apply to the BridgeWorker list.

          The Filter must be in the same Organization as the user credentials.

          The Filter's From field must match the entity type being filtered (BridgeWorker).

          For Space-resident entities, if the Filter has a FromSpaceID, it must match the operation's SpaceID.

          The Filter's Where clause will be combined with any explicit 'where' parameter using AND logic.

          If both 'filter' and 'where' parameters are specified, they are combined with AND logic.
        in: query
        name: filter
        required: false
        schema:
          type: string
      - description: |-
          Free text search that approximately matches the specified string against string fields and map keys/values.

          The search is case-insensitive and uses pattern matching to find entities containing the text.

          Searchable string fields include attributes like Slug, DisplayName, and string-typed custom fields.

          For map fields (like Labels and Annotations), the search matches both map keys and values.

          The search uses OR logic across all searchable fields, so matching any field will return the entity.

          If both 'where' and 'contains' parameters are specified, they are combined with AND logic.

          Searchable fields for BridgeWorker include string and map-type attributes from the queryable attributes list.

          The whole string must be query-encoded.
        in: query
        name: contains
        required: false
        schema:
          type: string
      - description: |-
          Include clause for expanding related entities in the response for BridgeWorker.
          The attribute names are case-sensitive, PascalCase, and
          expected in a comma-separated list format as in the JSON encoding.

          Supported attributes for BridgeWorker are OrganizationID, SpaceID.

          The whole string must be query-encoded.
        in: query
        name: include
        required: false
        schema:
          type: string
      requestBody:
        content:
          application/merge-patch+json:
            schema:
              description: JSON Merge Patch schema for BridgeWorker. All fields are
                optional and nullable. Readonly fields are ignored.
              properties:
                Annotations:
                  additionalProperties:
                    nullable: true
                    type: string
                  description: An optional map of Annotation key/value pairs for tools
                    to attach information to entities.
                  nullable: true
                  type: object
                Condition:
                  description: ""
                  nullable: true
                  type: string
                DeleteGates:
                  additionalProperties:
                    nullable: true
                    type: boolean
                  description: An optional set of gates that, if any is present, will
                    block deletion
                  nullable: true
                  type: object
                DisplayName:
                  description: Friendly name for the entity.
                  nullable: true
                  type: string
                Labels:
                  additionalProperties:
                    nullable: true
                    type: string
                  description: An optional map of Label key/value pairs to specify
                    identifying attributes of entities for the purpose of grouping
                    and filtering them.
                  nullable: true
                  type: object
                OrgRole:
                  description: ""
                  nullable: true
                  type: string
                Permissions:
                  additionalProperties:
                    nullable: true
                    type: object
                  description: ""
                  nullable: true
                  type: object
                ProvidedInfo:
                  description: ""
                  nullable: true
                  type: object
                Slug:
                  description: Unique URL-safe identifier for the entity.
                  nullable: true
                  type: string
                Version:
                  description: An entity-specific sequence number used for optimistic
                    concurrency control. The value read must be sent in calls to Update.
                  nullable: true
                  type: integer
              type: object
      responses:
        "200":
          content:
            application/json:
              schema:
                items:
                  $ref: '#/components/schemas/BridgeWorkerCreateOrUpdateResponse'
                type: array
          description: OK
        "207":
          content:
            application/json:
              schema:
                items:
                  $ref: '#/components/schemas/BridgeWorkerCreateOrUpdateResponse'
                type: array
          description: 'Multi-Status: Mixed success and failure results'
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: BridgeWorker request is invalid (Bad Request).
        "401":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Unauthorized access.
        "403":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Forbidden access.
        "404":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: BridgeWorker not found.
        "409":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: BridgeWorker data conflict. Data has changed since last read.
        "500":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Something went wrong while processing BridgeWorker.
        default:
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Unexpected error.
      summary: Bulk patch multiple bridge workers
      tags:
      - BridgeWorker
  /bridge_worker/{bridge_worker_id}/action_result:
    post:
      description: Records the status/result of an action executed by a bridge worker
      operationId: CreateActionResult
      parameters:
      - description: Unique identifier for a bridge_worker_id
        in: path
        name: bridge_worker_id
        required: true
        schema:
          format: uuid
          type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ActionResult'
      responses:
        "200":
          content:
            application/json:
              schema:
                type: string
          description: OK
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: BridgeWorker request is invalid (Bad Request).
        "401":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Unauthorized access.
        "403":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Forbidden access.
        "404":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: BridgeWorker not found.
        "409":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: BridgeWorker data conflict. Data has changed since last read.
        "500":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Something went wrong while processing BridgeWorker.
        default:
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Unexpected error.
      summary: Create action result
      tags:
      - BridgeWorker
  /bridge_worker/{bridge_worker_id}/me:
    get:
      description: Gets the current BridgeWorker from context
      operationId: GetSelf
      parameters:
      - description: Unique identifier for a bridge_worker_id
        in: path
        name: bridge_worker_id
        required: true
        schema:
          format: uuid
          type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BridgeWorker'
          description: |-
            BridgeWorker represents a bridge worker in ConfigHub.
            A bridge worker is a worker program that connects ConfigHub to external systems and targets.
            It acts as a bridge between ConfigHub and the infrastructure where configurations need
            to be applied. Bridge workers are responsible for executing configuration changes on
            remote targets and reporting status back to ConfigHub.
            When starting a bridge worker program, both the BridgeWorkerID and Secret are
            required for authentication with the ConfigHub server. These credentials allow the
            bridge worker to establish a secure connection and receive configuration actions.
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: BridgeWorker request is invalid (Bad Request).
        "401":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Unauthorized access.
        "403":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Forbidden access.
        "404":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: BridgeWorker not found.
        "500":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Something went wrong while processing BridgeWorker.
        default:
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Unexpected error.
      summary: Gets the current BridgeWorker from context
      tags:
      - BridgeWorker
  /bridge_worker/{bridge_worker_id}/queued_operation:
    get:
      description: List QueuedOperations
      operationId: ListQueuedOperations
      parameters:
      - description: Unique identifier for a bridge_worker_id
        in: path
        name: bridge_worker_id
        required: true
        schema:
          format: uuid
          type: string
      - description: |-
          The specified string is an expression for the purpose of filtering
          the list of QueuedOperations returned. The expression syntax was inspired by SQL.
          It supports conjunctions using `AND` of relational expressions of the form *attribute*
          *operator* *attribute_or_literal*. The attribute names are case-sensitive and PascalCase,
          as in the JSON encoding.
          Strings support the following operators: `<`, `>`, `<=`, `>=`, `=`, `!=`, `LIKE`, `NOT LIKE`, `ILIKE`, `~~`, `!~~`, `~`, `~*`, `!~`, `!~*`, `IN`, `NOT IN`.
          String pattern operators: `LIKE` and `~~` for pattern matching with `%` and `_` wildcards,
          `ILIKE` for case-insensitive pattern matching, `NOT LIKE` and `!~~` for negated pattern matching.
          String regex operators: `~` for regex matching, `~*` for case-insensitive regex,
          `!~` and `!~*` for regex not matching (case-sensitive and insensitive).
          Integers support the following operators: `<`, `>`, `<=`, `>=`, `=`, `!=`, `IN`, `NOT IN`.
          UUIDs and boolean attributes support equality and inequality only.
          UUID and time literals must be quoted as string literals.
          String literals are quoted with single quotes, such as `'string'`.
          Time literals use the same form as when serialized as JSON,
          such as: `CreatedAt > '2025-02-18T23:16:34'`.
          Integer and boolean literals are also supported for attributes of those types.
          Arrays support the `?` operator to to match any element of the array,
          as in `ApprovedBy ? '7c61626f-ddbe-41af-93f6-b69f4ab6d308'`.
          Arrays can perform LEN() to check for length, as in `LEN(ApprovedBy) > 0`.
          Map support the dot notation to specify a particular map key, as in `Labels.tier = 'Backend'`.
          Maps support `IS NULL` and `IS NOT NULL` with dot notation to check for key absence or presence,
          as in `Labels.tier IS NULL` (key doesn't exist) or `Labels.tier IS NOT NULL` (key exists).
          Comparison results can be tested with `IS TRUE`, `IS FALSE`, `IS NOT TRUE`, and `IS NOT FALSE`.
          These are useful for nullable columns: `MergeSourceID = '<uuid>' IS NOT FALSE` matches rows where MergeSourceID equals the value OR is NULL.
          The `IN` and `NOT IN` operators accept a comma-separated list of values in parentheses,
          such as `Slug IN ('slugone', 'slugtwo')` or `Labels.environment IN ('prod', 'staging')`.
          Conjunctions are supported using the `AND` operator.
          An example conjunction is:
          `CreatedAt >= '2025-01-07' AND Slug = 'test' AND Labels.mykey = 'myvalue'`.

          Supported attributes for filtering on QueuedOperation: Action, BridgeWorkerID, CreatedAt, DriftReconciliationMode, DryRun, OrganizationID, QueuedOperationID, RevisionNum, SpaceID, Status, TargetID, UnitActionNum, UnitID.

          The whole string must be query-encoded.
        in: query
        name: where
        required: false
        schema:
          type: string
      - description: |-
          UUID of a Filter entity to apply to the QueuedOperation list.

          The Filter must be in the same Organization as the user credentials.

          The Filter's From field must match the entity type being filtered (QueuedOperation).

          For Space-resident entities, if the Filter has a FromSpaceID, it must match the operation's SpaceID.

          The Filter's Where clause will be combined with any explicit 'where' parameter using AND logic.

          If both 'filter' and 'where' parameters are specified, they are combined with AND logic.
        in: query
        name: filter
        required: false
        schema:
          type: string
      - description: |-
          Free text search that approximately matches the specified string against string fields and map keys/values.

          The search is case-insensitive and uses pattern matching to find entities containing the text.

          Searchable string fields include attributes like Slug, DisplayName, and string-typed custom fields.

          For map fields (like Labels and Annotations), the search matches both map keys and values.

          The search uses OR logic across all searchable fields, so matching any field will return the entity.

          If both 'where' and 'contains' parameters are specified, they are combined with AND logic.

          Searchable fields for QueuedOperation include string and map-type attributes from the queryable attributes list.

          The whole string must be query-encoded.
        in: query
        name: contains
        required: false
        schema:
          type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                items:
                  $ref: '#/components/schemas/QueuedOperation'
                type: array
          description: OK
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: QueuedOperation request is invalid (Bad Request).
        "401":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Unauthorized access.
        "403":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Forbidden access.
        "404":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: QueuedOperation not found.
        "500":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Something went wrong while processing QueuedOperation.
        default:
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Unexpected error.
      summary: List QueuedOperations
      tags:
      - QueuedOperation
  /bridge_worker/{bridge_worker_id}/queued_operation/{queued_operation_id}:
    get:
      description: Get QueuedOperation
      operationId: GetQueuedOperation
      parameters:
      - description: Unique identifier for a bridge_worker_id
        in: path
        name: bridge_worker_id
        required: true
        schema:
          format: uuid
          type: string
      - description: Unique identifier for a queued_operation_id
        in: path
        name: queued_operation_id
        required: true
        schema:
          format: uuid
          type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/QueuedOperation'
          description: "UnitAction is a record of an action to be performed by a Bridge
            Worker. They are queued and sent to the worker in creation order. \nIf
            the worker is temporarily disconnected the queued actions will be sent
            when the worker reconnects or responds.\nIf there are links between units
            applied or destroyed in a single API call, they will be sent to the appropriate\nworker(s)
            in the appropriate order (reverse or forword topological order). One or
            more UnitEvents will correspond\nto each UnitAction."
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: QueuedOperation request is invalid (Bad Request).
        "401":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Unauthorized access.
        "403":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Forbidden access.
        "404":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: QueuedOperation not found.
        "500":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Something went wrong while processing QueuedOperation.
        default:
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Unexpected error.
      summary: Get QueuedOperation
      tags:
      - QueuedOperation
  /bridge_worker/{bridge_worker_id}/stream:
    post:
      description: Establishes an SSE connection with a bridge worker for real-time
        event streaming
      operationId: StreamBridgeWorker
      parameters:
      - description: Unique identifier for a bridge_worker_id
        in: path
        name: bridge_worker_id
        required: true
        schema:
          format: uuid
          type: string
      responses:
        "200":
          content:
            text/event-stream:
              schema:
                $ref: '#/components/schemas/EventMessage'
          description: OK
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: BridgeWorker request is invalid (Bad Request).
        "401":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Unauthorized access.
        "403":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Forbidden access.
        "404":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: BridgeWorker not found.
        "409":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: BridgeWorker data conflict. Data has changed since last read.
        "500":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Something went wrong while processing BridgeWorker.
        default:
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Unexpected error.
      summary: Stream events to bridge worker
      tags:
      - BridgeWorker
  /bridge_worker/{bridge_worker_id}/user_action_result:
    post:
      description: Records the status/result of an action executed by a bridge worker
        using a worker user identity
      operationId: UserCreateActionResult
      parameters:
      - description: Unique identifier for a bridge_worker_id
        in: path
        name: bridge_worker_id
        required: true
        schema:
          format: uuid
          type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ActionResult'
      responses:
        "200":
          content:
            application/json:
              schema:
                type: string
          description: OK
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: BridgeWorker request is invalid (Bad Request).
        "401":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Unauthorized access.
        "403":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Forbidden access.
        "404":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: BridgeWorker not found.
        "409":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: BridgeWorker data conflict. Data has changed since last read.
        "500":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Something went wrong while processing BridgeWorker.
        default:
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Unexpected error.
      summary: Create action result
      tags:
      - BridgeWorker
  /change_set:
    delete:
      description: Delete multiple changesets selected by query parameters
      operationId: BulkDeleteChangeSets
      parameters:
      - description: |-
          The specified string is an expression for the purpose of filtering
          the list of ChangeSets returned. The expression syntax was inspired by SQL.
          It supports conjunctions using `AND` of relational expressions of the form *attribute*
          *operator* *attribute_or_literal*. The attribute names are case-sensitive and PascalCase,
          as in the JSON encoding.
          Strings support the following operators: `<`, `>`, `<=`, `>=`, `=`, `!=`, `LIKE`, `NOT LIKE`, `ILIKE`, `~~`, `!~~`, `~`, `~*`, `!~`, `!~*`, `IN`, `NOT IN`.
          String pattern operators: `LIKE` and `~~` for pattern matching with `%` and `_` wildcards,
          `ILIKE` for case-insensitive pattern matching, `NOT LIKE` and `!~~` for negated pattern matching.
          String regex operators: `~` for regex matching, `~*` for case-insensitive regex,
          `!~` and `!~*` for regex not matching (case-sensitive and insensitive).
          Integers support the following operators: `<`, `>`, `<=`, `>=`, `=`, `!=`, `IN`, `NOT IN`.
          UUIDs and boolean attributes support equality and inequality only.
          UUID and time literals must be quoted as string literals.
          String literals are quoted with single quotes, such as `'string'`.
          Time literals use the same form as when serialized as JSON,
          such as: `CreatedAt > '2025-02-18T23:16:34'`.
          Integer and boolean literals are also supported for attributes of those types.
          Arrays support the `?` operator to to match any element of the array,
          as in `ApprovedBy ? '7c61626f-ddbe-41af-93f6-b69f4ab6d308'`.
          Arrays can perform LEN() to check for length, as in `LEN(ApprovedBy) > 0`.
          Map support the dot notation to specify a particular map key, as in `Labels.tier = 'Backend'`.
          Maps support `IS NULL` and `IS NOT NULL` with dot notation to check for key absence or presence,
          as in `Labels.tier IS NULL` (key doesn't exist) or `Labels.tier IS NOT NULL` (key exists).
          Comparison results can be tested with `IS TRUE`, `IS FALSE`, `IS NOT TRUE`, and `IS NOT FALSE`.
          These are useful for nullable columns: `MergeSourceID = '<uuid>' IS NOT FALSE` matches rows where MergeSourceID equals the value OR is NULL.
          The `IN` and `NOT IN` operators accept a comma-separated list of values in parentheses,
          such as `Slug IN ('slugone', 'slugtwo')` or `Labels.environment IN ('prod', 'staging')`.
          Conjunctions are supported using the `AND` operator.
          An example conjunction is:
          `CreatedAt >= '2025-01-07' AND Slug = 'test' AND Labels.mykey = 'myvalue'`.

          Supported attributes for filtering on ChangeSet: ChangeSetID, CreatedAt, DeleteGates, Description, DisplayName, EndTagID, Labels, OrganizationID, Slug, SpaceID, StartTagID, State, UpdatedAt.

          The whole string must be query-encoded.
        in: query
        name: where
        required: false
        schema:
          type: string
      - description: |-
          UUID of a Filter entity to apply to the ChangeSet list.

          The Filter must be in the same Organization as the user credentials.

          The Filter's From field must match the entity type being filtered (ChangeSet).

          For Space-resident entities, if the Filter has a FromSpaceID, it must match the operation's SpaceID.

          The Filter's Where clause will be combined with any explicit 'where' parameter using AND logic.

          If both 'filter' and 'where' parameters are specified, they are combined with AND logic.
        in: query
        name: filter
        required: false
        schema:
          type: string
      - description: |-
          Free text search that approximately matches the specified string against string fields and map keys/values.

          The search is case-insensitive and uses pattern matching to find entities containing the text.

          Searchable string fields include attributes like Slug, DisplayName, and string-typed custom fields.

          For map fields (like Labels and Annotations), the search matches both map keys and values.

          The search uses OR logic across all searchable fields, so matching any field will return the entity.

          If both 'where' and 'contains' parameters are specified, they are combined with AND logic.

          Searchable fields for ChangeSet include string and map-type attributes from the queryable attributes list.

          The whole string must be query-encoded.
        in: query
        name: contains
        required: false
        schema:
          type: string
      - description: |-
          Include clause for expanding related entities in the response for ChangeSet.
          The attribute names are case-sensitive, PascalCase, and
          expected in a comma-separated list format as in the JSON encoding.

          Supported attributes for ChangeSet are EndTagID, OrganizationID, SpaceID, StartTagID.

          The whole string must be query-encoded.
        in: query
        name: include
        required: false
        schema:
          type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                items:
                  $ref: '#/components/schemas/DeleteResponse'
                type: array
          description: OK
        "207":
          content:
            application/json:
              schema:
                items:
                  $ref: '#/components/schemas/DeleteResponse'
                type: array
          description: 'Multi-Status: Mixed success and failure results'
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: ChangeSet request is invalid (Bad Request).
        "401":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Unauthorized access.
        "403":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Forbidden access.
        "404":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: ChangeSet not found.
        "409":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: ChangeSet data conflict. Data has changed since last read.
        "422":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Unable to delete entity
        "500":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Something went wrong while processing ChangeSet.
        default:
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Unexpected error.
      summary: Bulk delete multiple changesets
      tags:
      - ChangeSet
    get:
      description: Retrieves a list of ChangeSets across spaces in the Organization
      operationId: ListAllChangeSets
      parameters:
      - description: |-
          The specified string is an expression for the purpose of filtering
          the list of ChangeSets returned. The expression syntax was inspired by SQL.
          It supports conjunctions using `AND` of relational expressions of the form *attribute*
          *operator* *attribute_or_literal*. The attribute names are case-sensitive and PascalCase,
          as in the JSON encoding.
          Strings support the following operators: `<`, `>`, `<=`, `>=`, `=`, `!=`, `LIKE`, `NOT LIKE`, `ILIKE`, `~~`, `!~~`, `~`, `~*`, `!~`, `!~*`, `IN`, `NOT IN`.
          String pattern operators: `LIKE` and `~~` for pattern matching with `%` and `_` wildcards,
          `ILIKE` for case-insensitive pattern matching, `NOT LIKE` and `!~~` for negated pattern matching.
          String regex operators: `~` for regex matching, `~*` for case-insensitive regex,
          `!~` and `!~*` for regex not matching (case-sensitive and insensitive).
          Integers support the following operators: `<`, `>`, `<=`, `>=`, `=`, `!=`, `IN`, `NOT IN`.
          UUIDs and boolean attributes support equality and inequality only.
          UUID and time literals must be quoted as string literals.
          String literals are quoted with single quotes, such as `'string'`.
          Time literals use the same form as when serialized as JSON,
          such as: `CreatedAt > '2025-02-18T23:16:34'`.
          Integer and boolean literals are also supported for attributes of those types.
          Arrays support the `?` operator to to match any element of the array,
          as in `ApprovedBy ? '7c61626f-ddbe-41af-93f6-b69f4ab6d308'`.
          Arrays can perform LEN() to check for length, as in `LEN(ApprovedBy) > 0`.
          Map support the dot notation to specify a particular map key, as in `Labels.tier = 'Backend'`.
          Maps support `IS NULL` and `IS NOT NULL` with dot notation to check for key absence or presence,
          as in `Labels.tier IS NULL` (key doesn't exist) or `Labels.tier IS NOT NULL` (key exists).
          Comparison results can be tested with `IS TRUE`, `IS FALSE`, `IS NOT TRUE`, and `IS NOT FALSE`.
          These are useful for nullable columns: `MergeSourceID = '<uuid>' IS NOT FALSE` matches rows where MergeSourceID equals the value OR is NULL.
          The `IN` and `NOT IN` operators accept a comma-separated list of values in parentheses,
          such as `Slug IN ('slugone', 'slugtwo')` or `Labels.environment IN ('prod', 'staging')`.
          Conjunctions are supported using the `AND` operator.
          An example conjunction is:
          `CreatedAt >= '2025-01-07' AND Slug = 'test' AND Labels.mykey = 'myvalue'`.

          Supported attributes for filtering on ChangeSet: ChangeSetID, CreatedAt, DeleteGates, Description, DisplayName, EndTagID, Labels, OrganizationID, Slug, SpaceID, StartTagID, State, UpdatedAt.

          The whole string must be query-encoded.
        in: query
        name: where
        required: false
        schema:
          type: string
      - description: |-
          UUID of a Filter entity to apply to the ChangeSet list.

          The Filter must be in the same Organization as the user credentials.

          The Filter's From field must match the entity type being filtered (ChangeSet).

          For Space-resident entities, if the Filter has a FromSpaceID, it must match the operation's SpaceID.

          The Filter's Where clause will be combined with any explicit 'where' parameter using AND logic.

          If both 'filter' and 'where' parameters are specified, they are combined with AND logic.
        in: query
        name: filter
        required: false
        schema:
          type: string
      - description: |-
          Free text search that approximately matches the specified string against string fields and map keys/values.

          The search is case-insensitive and uses pattern matching to find entities containing the text.

          Searchable string fields include attributes like Slug, DisplayName, and string-typed custom fields.

          For map fields (like Labels and Annotations), the search matches both map keys and values.

          The search uses OR logic across all searchable fields, so matching any field will return the entity.

          If both 'where' and 'contains' parameters are specified, they are combined with AND logic.

          Searchable fields for ChangeSet include string and map-type attributes from the queryable attributes list.

          The whole string must be query-encoded.
        in: query
        name: contains
        required: false
        schema:
          type: string
      - description: |-
          Include clause for expanding related entities in the response for ChangeSet.
          The attribute names are case-sensitive, PascalCase, and
          expected in a comma-separated list format as in the JSON encoding.

          Supported attributes for ChangeSet are EndTagID, OrganizationID, SpaceID, StartTagID.

          The whole string must be query-encoded.
        in: query
        name: include
        required: false
        schema:
          type: string
      - description: |-
          Select clause for specifying which fields to include in the response for ChangeSet.
          The attribute names are case-sensitive, PascalCase, and
          expected in a comma-separated list format as in the JSON encoding.
          If not specified, all fields are returned.
          Entity and parent IDs (like OrganizationID, SpaceID, ChangeSetID) and Slug are always returned regardless of the select parameter.
          Fields used in where and contains filters are also automatically included.
          Example: 'DisplayName,CreatedAt,Labels' will return only those fields plus the required ID and Slug fields.
          The whole string must be query-encoded.
        in: query
        name: select
        required: false
        schema:
          type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                items:
                  $ref: '#/components/schemas/ExtendedChangeSet'
                type: array
          description: OK
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: ChangeSet request is invalid (Bad Request).
        "401":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Unauthorized access.
        "403":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Forbidden access.
        "404":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: ChangeSet not found.
        "500":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Something went wrong while processing ChangeSet.
        default:
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Unexpected error.
      summary: List of ChangeSets across spaces
      tags:
      - ChangeSet
    patch:
      description: Apply JSON merge patch to multiple changesets selected by query
        parameters
      operationId: BulkPatchChangeSets
      parameters:
      - description: |-
          The specified string is an expression for the purpose of filtering
          the list of ChangeSets returned. The expression syntax was inspired by SQL.
          It supports conjunctions using `AND` of relational expressions of the form *attribute*
          *operator* *attribute_or_literal*. The attribute names are case-sensitive and PascalCase,
          as in the JSON encoding.
          Strings support the following operators: `<`, `>`, `<=`, `>=`, `=`, `!=`, `LIKE`, `NOT LIKE`, `ILIKE`, `~~`, `!~~`, `~`, `~*`, `!~`, `!~*`, `IN`, `NOT IN`.
          String pattern operators: `LIKE` and `~~` for pattern matching with `%` and `_` wildcards,
          `ILIKE` for case-insensitive pattern matching, `NOT LIKE` and `!~~` for negated pattern matching.
          String regex operators: `~` for regex matching, `~*` for case-insensitive regex,
          `!~` and `!~*` for regex not matching (case-sensitive and insensitive).
          Integers support the following operators: `<`, `>`, `<=`, `>=`, `=`, `!=`, `IN`, `NOT IN`.
          UUIDs and boolean attributes support equality and inequality only.
          UUID and time literals must be quoted as string literals.
          String literals are quoted with single quotes, such as `'string'`.
          Time literals use the same form as when serialized as JSON,
          such as: `CreatedAt > '2025-02-18T23:16:34'`.
          Integer and boolean literals are also supported for attributes of those types.
          Arrays support the `?` operator to to match any element of the array,
          as in `ApprovedBy ? '7c61626f-ddbe-41af-93f6-b69f4ab6d308'`.
          Arrays can perform LEN() to check for length, as in `LEN(ApprovedBy) > 0`.
          Map support the dot notation to specify a particular map key, as in `Labels.tier = 'Backend'`.
          Maps support `IS NULL` and `IS NOT NULL` with dot notation to check for key absence or presence,
          as in `Labels.tier IS NULL` (key doesn't exist) or `Labels.tier IS NOT NULL` (key exists).
          Comparison results can be tested with `IS TRUE`, `IS FALSE`, `IS NOT TRUE`, and `IS NOT FALSE`.
          These are useful for nullable columns: `MergeSourceID = '<uuid>' IS NOT FALSE` matches rows where MergeSourceID equals the value OR is NULL.
          The `IN` and `NOT IN` operators accept a comma-separated list of values in parentheses,
          such as `Slug IN ('slugone', 'slugtwo')` or `Labels.environment IN ('prod', 'staging')`.
          Conjunctions are supported using the `AND` operator.
          An example conjunction is:
          `CreatedAt >= '2025-01-07' AND Slug = 'test' AND Labels.mykey = 'myvalue'`.

          Supported attributes for filtering on ChangeSet: ChangeSetID, CreatedAt, DeleteGates, Description, DisplayName, EndTagID, Labels, OrganizationID, Slug, SpaceID, StartTagID, State, UpdatedAt.

          The whole string must be query-encoded.
        in: query
        name: where
        required: false
        schema:
          type: string
      - description: |-
          UUID of a Filter entity to apply to the ChangeSet list.

          The Filter must be in the same Organization as the user credentials.

          The Filter's From field must match the entity type being filtered (ChangeSet).

          For Space-resident entities, if the Filter has a FromSpaceID, it must match the operation's SpaceID.

          The Filter's Where clause will be combined with any explicit 'where' parameter using AND logic.

          If both 'filter' and 'where' parameters are specified, they are combined with AND logic.
        in: query
        name: filter
        required: false
        schema:
          type: string
      - description: |-
          Free text search that approximately matches the specified string against string fields and map keys/values.

          The search is case-insensitive and uses pattern matching to find entities containing the text.

          Searchable string fields include attributes like Slug, DisplayName, and string-typed custom fields.

          For map fields (like Labels and Annotations), the search matches both map keys and values.

          The search uses OR logic across all searchable fields, so matching any field will return the entity.

          If both 'where' and 'contains' parameters are specified, they are combined with AND logic.

          Searchable fields for ChangeSet include string and map-type attributes from the queryable attributes list.

          The whole string must be query-encoded.
        in: query
        name: contains
        required: false
        schema:
          type: string
      - description: |-
          Include clause for expanding related entities in the response for ChangeSet.
          The attribute names are case-sensitive, PascalCase, and
          expected in a comma-separated list format as in the JSON encoding.

          Supported attributes for ChangeSet are EndTagID, OrganizationID, SpaceID, StartTagID.

          The whole string must be query-encoded.
        in: query
        name: include
        required: false
        schema:
          type: string
      requestBody:
        content:
          application/merge-patch+json:
            schema:
              description: JSON Merge Patch schema for ChangeSet. All fields are optional
                and nullable. Readonly fields are ignored.
              properties:
                Annotations:
                  additionalProperties:
                    nullable: true
                    type: string
                  description: An optional map of Annotation key/value pairs for tools
                    to attach information to entities.
                  nullable: true
                  type: object
                DeleteGates:
                  additionalProperties:
                    nullable: true
                    type: boolean
                  description: An optional set of gates that, if any is present, will
                    block deletion
                  nullable: true
                  type: object
                Description:
                  description: ""
                  nullable: true
                  type: string
                DisplayName:
                  description: Friendly name for the entity.
                  nullable: true
                  type: string
                Labels:
                  additionalProperties:
                    nullable: true
                    type: string
                  description: An optional map of Label key/value pairs to specify
                    identifying attributes of entities for the purpose of grouping
                    and filtering them.
                  nullable: true
                  type: object
                Slug:
                  description: Unique URL-safe identifier for the entity.
                  nullable: true
                  type: string
                Version:
                  description: An entity-specific sequence number used for optimistic
                    concurrency control. The value read must be sent in calls to Update.
                  nullable: true
                  type: integer
              type: object
      responses:
        "200":
          content:
            application/json:
              schema:
                items:
                  $ref: '#/components/schemas/ChangeSetCreateOrUpdateResponse'
                type: array
          description: OK
        "207":
          content:
            application/json:
              schema:
                items:
                  $ref: '#/components/schemas/ChangeSetCreateOrUpdateResponse'
                type: array
          description: 'Multi-Status: Mixed success and failure results'
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: ChangeSet request is invalid (Bad Request).
        "401":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Unauthorized access.
        "403":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Forbidden access.
        "404":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: ChangeSet not found.
        "409":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: ChangeSet data conflict. Data has changed since last read.
        "500":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Something went wrong while processing ChangeSet.
        default:
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Unexpected error.
      summary: Bulk patch multiple changesets
      tags:
      - ChangeSet
    post:
      description: Clone multiple changesets selected by query parameters with optional
        name prefixes and destination spaces
      operationId: BulkCreateChangeSets
      parameters:
      - description: |-
          The specified string is an expression for the purpose of filtering
          the list of ChangeSets returned. The expression syntax was inspired by SQL.
          It supports conjunctions using `AND` of relational expressions of the form *attribute*
          *operator* *attribute_or_literal*. The attribute names are case-sensitive and PascalCase,
          as in the JSON encoding.
          Strings support the following operators: `<`, `>`, `<=`, `>=`, `=`, `!=`, `LIKE`, `NOT LIKE`, `ILIKE`, `~~`, `!~~`, `~`, `~*`, `!~`, `!~*`, `IN`, `NOT IN`.
          String pattern operators: `LIKE` and `~~` for pattern matching with `%` and `_` wildcards,
          `ILIKE` for case-insensitive pattern matching, `NOT LIKE` and `!~~` for negated pattern matching.
          String regex operators: `~` for regex matching, `~*` for case-insensitive regex,
          `!~` and `!~*` for regex not matching (case-sensitive and insensitive).
          Integers support the following operators: `<`, `>`, `<=`, `>=`, `=`, `!=`, `IN`, `NOT IN`.
          UUIDs and boolean attributes support equality and inequality only.
          UUID and time literals must be quoted as string literals.
          String literals are quoted with single quotes, such as `'string'`.
          Time literals use the same form as when serialized as JSON,
          such as: `CreatedAt > '2025-02-18T23:16:34'`.
          Integer and boolean literals are also supported for attributes of those types.
          Arrays support the `?` operator to to match any element of the array,
          as in `ApprovedBy ? '7c61626f-ddbe-41af-93f6-b69f4ab6d308'`.
          Arrays can perform LEN() to check for length, as in `LEN(ApprovedBy) > 0`.
          Map support the dot notation to specify a particular map key, as in `Labels.tier = 'Backend'`.
          Maps support `IS NULL` and `IS NOT NULL` with dot notation to check for key absence or presence,
          as in `Labels.tier IS NULL` (key doesn't exist) or `Labels.tier IS NOT NULL` (key exists).
          Comparison results can be tested with `IS TRUE`, `IS FALSE`, `IS NOT TRUE`, and `IS NOT FALSE`.
          These are useful for nullable columns: `MergeSourceID = '<uuid>' IS NOT FALSE` matches rows where MergeSourceID equals the value OR is NULL.
          The `IN` and `NOT IN` operators accept a comma-separated list of values in parentheses,
          such as `Slug IN ('slugone', 'slugtwo')` or `Labels.environment IN ('prod', 'staging')`.
          Conjunctions are supported using the `AND` operator.
          An example conjunction is:
          `CreatedAt >= '2025-01-07' AND Slug = 'test' AND Labels.mykey = 'myvalue'`.

          Supported attributes for filtering on ChangeSet: ChangeSetID, CreatedAt, DeleteGates, Description, DisplayName, EndTagID, Labels, OrganizationID, Slug, SpaceID, StartTagID, State, UpdatedAt.

          The whole string must be query-encoded.
        in: query
        name: where
        required: false
        schema:
          type: string
      - description: |-
          UUID of a Filter entity to apply to the ChangeSet list.

          The Filter must be in the same Organization as the user credentials.

          The Filter's From field must match the entity type being filtered (ChangeSet).

          For Space-resident entities, if the Filter has a FromSpaceID, it must match the operation's SpaceID.

          The Filter's Where clause will be combined with any explicit 'where' parameter using AND logic.

          If both 'filter' and 'where' parameters are specified, they are combined with AND logic.
        in: query
        name: filter
        required: false
        schema:
          type: string
      - description: |-
          Free text search that approximately matches the specified string against string fields and map keys/values.

          The search is case-insensitive and uses pattern matching to find entities containing the text.

          Searchable string fields include attributes like Slug, DisplayName, and string-typed custom fields.

          For map fields (like Labels and Annotations), the search matches both map keys and values.

          The search uses OR logic across all searchable fields, so matching any field will return the entity.

          If both 'where' and 'contains' parameters are specified, they are combined with AND logic.

          Searchable fields for ChangeSet include string and map-type attributes from the queryable attributes list.

          The whole string must be query-encoded.
        in: query
        name: contains
        required: false
        schema:
          type: string
      - description: |-
          Include clause for expanding related entities in the response for ChangeSet.
          The attribute names are case-sensitive, PascalCase, and
          expected in a comma-separated list format as in the JSON encoding.

          Supported attributes for ChangeSet are EndTagID, OrganizationID, SpaceID, StartTagID.

          The whole string must be query-encoded.
        in: query
        name: include
        required: false
        schema:
          type: string
      - description: Comma-separated list of prefixes to apply to cloned ChangeSet
          names
        in: query
        name: name_prefixes
        required: false
        schema:
          type: string
      - description: Comma-separated list of labels with multiple values for cloned
          ChangeSet labels, in the format of key1=value1|value2,key2=value1|value2|value3
        in: query
        name: variant_labels
        required: false
        schema:
          type: string
      - description: 'A string for clone names, use the prefix ''template:'' for a
          Go-template with .SourceEntitySlug to access the original entity''s slug
          and .Labels to access variant labels, example: ''template:{{.SourceEntitySlug}}-{{.Labels.env}}'''
        in: query
        name: name_pattern
        required: false
        schema:
          type: string
      - description: |-
          The specified string is an expression for the purpose of filtering
          the list of Spaces returned. The expression syntax was inspired by SQL.
          It supports conjunctions using `AND` of relational expressions of the form *attribute*
          *operator* *attribute_or_literal*. The attribute names are case-sensitive and PascalCase,
          as in the JSON encoding.
          Strings support the following operators: `<`, `>`, `<=`, `>=`, `=`, `!=`, `LIKE`, `NOT LIKE`, `ILIKE`, `~~`, `!~~`, `~`, `~*`, `!~`, `!~*`, `IN`, `NOT IN`.
          String pattern operators: `LIKE` and `~~` for pattern matching with `%` and `_` wildcards,
          `ILIKE` for case-insensitive pattern matching, `NOT LIKE` and `!~~` for negated pattern matching.
          String regex operators: `~` for regex matching, `~*` for case-insensitive regex,
          `!~` and `!~*` for regex not matching (case-sensitive and insensitive).
          Integers support the following operators: `<`, `>`, `<=`, `>=`, `=`, `!=`, `IN`, `NOT IN`.
          UUIDs and boolean attributes support equality and inequality only.
          UUID and time literals must be quoted as string literals.
          String literals are quoted with single quotes, such as `'string'`.
          Time literals use the same form as when serialized as JSON,
          such as: `CreatedAt > '2025-02-18T23:16:34'`.
          Integer and boolean literals are also supported for attributes of those types.
          Arrays support the `?` operator to to match any element of the array,
          as in `ApprovedBy ? '7c61626f-ddbe-41af-93f6-b69f4ab6d308'`.
          Arrays can perform LEN() to check for length, as in `LEN(ApprovedBy) > 0`.
          Map support the dot notation to specify a particular map key, as in `Labels.tier = 'Backend'`.
          Maps support `IS NULL` and `IS NOT NULL` with dot notation to check for key absence or presence,
          as in `Labels.tier IS NULL` (key doesn't exist) or `Labels.tier IS NOT NULL` (key exists).
          Comparison results can be tested with `IS TRUE`, `IS FALSE`, `IS NOT TRUE`, and `IS NOT FALSE`.
          These are useful for nullable columns: `MergeSourceID = '<uuid>' IS NOT FALSE` matches rows where MergeSourceID equals the value OR is NULL.
          The `IN` and `NOT IN` operators accept a comma-separated list of values in parentheses,
          such as `Slug IN ('slugone', 'slugtwo')` or `Labels.environment IN ('prod', 'staging')`.
          Conjunctions are supported using the `AND` operator.
          An example conjunction is:
          `CreatedAt >= '2025-01-07' AND Slug = 'test' AND Labels.mykey = 'myvalue'`.

          Supported attributes for filtering on Space: AttributeFilterID, AttributeHash, AttributeIDs, CreatedAt, DeleteGates, DisplayName, Labels, OrganizationID, Permissions, Slug, SpaceID, TriggerFilterID, TriggerHash, TriggerIDs, UpdatedAt.

          Where expression to select destination spaces for cloning changesets

          The whole string must be query-encoded.
        in: query
        name: where_space
        required: false
        schema:
          type: string
      - description: |-
          UUID of a Filter entity to apply to the Space list.

          The Filter must be in the same Organization as the user credentials.

          The Filter's From field must match the entity type being filtered (Space).

          For Space-resident entities, if the Filter has a FromSpaceID, it must match the operation's SpaceID.

          The Filter's Where clause will be combined with any explicit 'where' parameter using AND logic.

          If both 'filter' and 'where' parameters are specified, they are combined with AND logic.
        in: query
        name: filter_space
        required: false
        schema:
          type: string
      - description: Allowed values are true and false. Default is false. When true,
          reports success when an entity already exists and returns the existing entity
        in: query
        name: allow_exists
        required: false
        schema:
          type: string
      requestBody:
        content:
          application/merge-patch+json:
            schema:
              description: JSON Merge Patch schema for ChangeSet. All fields are optional
                and nullable. Readonly fields are ignored.
              properties:
                Annotations:
                  additionalProperties:
                    nullable: true
                    type: string
                  description: An optional map of Annotation key/value pairs for tools
                    to attach information to entities.
                  nullable: true
                  type: object
                DeleteGates:
                  additionalProperties:
                    nullable: true
                    type: boolean
                  description: An optional set of gates that, if any is present, will
                    block deletion
                  nullable: true
                  type: object
                Description:
                  description: ""
                  nullable: true
                  type: string
                DisplayName:
                  description: Friendly name for the entity.
                  nullable: true
                  type: string
                Labels:
                  additionalProperties:
                    nullable: true
                    type: string
                  description: An optional map of Label key/value pairs to specify
                    identifying attributes of entities for the purpose of grouping
                    and filtering them.
                  nullable: true
                  type: object
                Slug:
                  description: Unique URL-safe identifier for the entity.
                  nullable: true
                  type: string
                Version:
                  description: An entity-specific sequence number used for optimistic
                    concurrency control. The value read must be sent in calls to Update.
                  nullable: true
                  type: integer
              type: object
      responses:
        "200":
          content:
            application/json:
              schema:
                items:
                  $ref: '#/components/schemas/ChangeSetCreateOrUpdateResponse'
                type: array
          description: OK
        "207":
          content:
            application/json:
              schema:
                items:
                  $ref: '#/components/schemas/ChangeSetCreateOrUpdateResponse'
                type: array
          description: Multi-Status (partial success)
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: ChangeSet request is invalid (Bad Request).
        "401":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Unauthorized access.
        "403":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Forbidden access.
        "404":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: ChangeSet not found.
        "409":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: ChangeSet data conflict. Data has changed since last read.
        "500":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Something went wrong while processing ChangeSet.
        default:
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Unexpected error.
      summary: Bulk create (clone) multiple changesets
      tags:
      - ChangeSet
  /filter:
    delete:
      description: Delete multiple filters selected by query parameters
      operationId: BulkDeleteFilters
      parameters:
      - description: |-
          The specified string is an expression for the purpose of filtering
          the list of Filters returned. The expression syntax was inspired by SQL.
          It supports conjunctions using `AND` of relational expressions of the form *attribute*
          *operator* *attribute_or_literal*. The attribute names are case-sensitive and PascalCase,
          as in the JSON encoding.
          Strings support the following operators: `<`, `>`, `<=`, `>=`, `=`, `!=`, `LIKE`, `NOT LIKE`, `ILIKE`, `~~`, `!~~`, `~`, `~*`, `!~`, `!~*`, `IN`, `NOT IN`.
          String pattern operators: `LIKE` and `~~` for pattern matching with `%` and `_` wildcards,
          `ILIKE` for case-insensitive pattern matching, `NOT LIKE` and `!~~` for negated pattern matching.
          String regex operators: `~` for regex matching, `~*` for case-insensitive regex,
          `!~` and `!~*` for regex not matching (case-sensitive and insensitive).
          Integers support the following operators: `<`, `>`, `<=`, `>=`, `=`, `!=`, `IN`, `NOT IN`.
          UUIDs and boolean attributes support equality and inequality only.
          UUID and time literals must be quoted as string literals.
          String literals are quoted with single quotes, such as `'string'`.
          Time literals use the same form as when serialized as JSON,
          such as: `CreatedAt > '2025-02-18T23:16:34'`.
          Integer and boolean literals are also supported for attributes of those types.
          Arrays support the `?` operator to to match any element of the array,
          as in `ApprovedBy ? '7c61626f-ddbe-41af-93f6-b69f4ab6d308'`.
          Arrays can perform LEN() to check for length, as in `LEN(ApprovedBy) > 0`.
          Map support the dot notation to specify a particular map key, as in `Labels.tier = 'Backend'`.
          Maps support `IS NULL` and `IS NOT NULL` with dot notation to check for key absence or presence,
          as in `Labels.tier IS NULL` (key doesn't exist) or `Labels.tier IS NOT NULL` (key exists).
          Comparison results can be tested with `IS TRUE`, `IS FALSE`, `IS NOT TRUE`, and `IS NOT FALSE`.
          These are useful for nullable columns: `MergeSourceID = '<uuid>' IS NOT FALSE` matches rows where MergeSourceID equals the value OR is NULL.
          The `IN` and `NOT IN` operators accept a comma-separated list of values in parentheses,
          such as `Slug IN ('slugone', 'slugtwo')` or `Labels.environment IN ('prod', 'staging')`.
          Conjunctions are supported using the `AND` operator.
          An example conjunction is:
          `CreatedAt >= '2025-01-07' AND Slug = 'test' AND Labels.mykey = 'myvalue'`.

          Supported attributes for filtering on Filter: CreatedAt, DeleteGates, DisplayName, FilterID, From, FromSpaceID, Hash, Labels, OrganizationID, ResourceType, Slug, SpaceID, UpdatedAt, Where, WhereData.

          The whole string must be query-encoded.
        in: query
        name: where
        required: false
        schema:
          type: string
      - description: |-
          UUID of a Filter entity to apply to the Filter list.

          The Filter must be in the same Organization as the user credentials.

          The Filter's From field must match the entity type being filtered (Filter).

          For Space-resident entities, if the Filter has a FromSpaceID, it must match the operation's SpaceID.

          The Filter's Where clause will be combined with any explicit 'where' parameter using AND logic.

          If both 'filter' and 'where' parameters are specified, they are combined with AND logic.
        in: query
        name: filter
        required: false
        schema:
          type: string
      - description: |-
          Free text search that approximately matches the specified string against string fields and map keys/values.

          The search is case-insensitive and uses pattern matching to find entities containing the text.

          Searchable string fields include attributes like Slug, DisplayName, and string-typed custom fields.

          For map fields (like Labels and Annotations), the search matches both map keys and values.

          The search uses OR logic across all searchable fields, so matching any field will return the entity.

          If both 'where' and 'contains' parameters are specified, they are combined with AND logic.

          Searchable fields for Filter include string and map-type attributes from the queryable attributes list.

          The whole string must be query-encoded.
        in: query
        name: contains
        required: false
        schema:
          type: string
      - description: |-
          Include clause for expanding related entities in the response for Filter.
          The attribute names are case-sensitive, PascalCase, and
          expected in a comma-separated list format as in the JSON encoding.

          Supported attributes for Filter are FromSpaceID, OrganizationID, SpaceID.

          The whole string must be query-encoded.
        in: query
        name: include
        required: false
        schema:
          type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                items:
                  $ref: '#/components/schemas/DeleteResponse'
                type: array
          description: OK
        "207":
          content:
            application/json:
              schema:
                items:
                  $ref: '#/components/schemas/DeleteResponse'
                type: array
          description: 'Multi-Status: Mixed success and failure results'
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Filter request is invalid (Bad Request).
        "401":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Unauthorized access.
        "403":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Forbidden access.
        "404":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Filter not found.
        "409":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Filter data conflict. Data has changed since last read.
        "422":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Unable to delete entity
        "500":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Something went wrong while processing Filter.
        default:
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Unexpected error.
      summary: Bulk delete multiple filters
      tags:
      - Filter
    get:
      description: Retrieves a list of Filters across spaces in the Organization
      operationId: ListAllFilters
      parameters:
      - description: |-
          The specified string is an expression for the purpose of filtering
          the list of Filters returned. The expression syntax was inspired by SQL.
          It supports conjunctions using `AND` of relational expressions of the form *attribute*
          *operator* *attribute_or_literal*. The attribute names are case-sensitive and PascalCase,
          as in the JSON encoding.
          Strings support the following operators: `<`, `>`, `<=`, `>=`, `=`, `!=`, `LIKE`, `NOT LIKE`, `ILIKE`, `~~`, `!~~`, `~`, `~*`, `!~`, `!~*`, `IN`, `NOT IN`.
          String pattern operators: `LIKE` and `~~` for pattern matching with `%` and `_` wildcards,
          `ILIKE` for case-insensitive pattern matching, `NOT LIKE` and `!~~` for negated pattern matching.
          String regex operators: `~` for regex matching, `~*` for case-insensitive regex,
          `!~` and `!~*` for regex not matching (case-sensitive and insensitive).
          Integers support the following operators: `<`, `>`, `<=`, `>=`, `=`, `!=`, `IN`, `NOT IN`.
          UUIDs and boolean attributes support equality and inequality only.
          UUID and time literals must be quoted as string literals.
          String literals are quoted with single quotes, such as `'string'`.
          Time literals use the same form as when serialized as JSON,
          such as: `CreatedAt > '2025-02-18T23:16:34'`.
          Integer and boolean literals are also supported for attributes of those types.
          Arrays support the `?` operator to to match any element of the array,
          as in `ApprovedBy ? '7c61626f-ddbe-41af-93f6-b69f4ab6d308'`.
          Arrays can perform LEN() to check for length, as in `LEN(ApprovedBy) > 0`.
          Map support the dot notation to specify a particular map key, as in `Labels.tier = 'Backend'`.
          Maps support `IS NULL` and `IS NOT NULL` with dot notation to check for key absence or presence,
          as in `Labels.tier IS NULL` (key doesn't exist) or `Labels.tier IS NOT NULL` (key exists).
          Comparison results can be tested with `IS TRUE`, `IS FALSE`, `IS NOT TRUE`, and `IS NOT FALSE`.
          These are useful for nullable columns: `MergeSourceID = '<uuid>' IS NOT FALSE` matches rows where MergeSourceID equals the value OR is NULL.
          The `IN` and `NOT IN` operators accept a comma-separated list of values in parentheses,
          such as `Slug IN ('slugone', 'slugtwo')` or `Labels.environment IN ('prod', 'staging')`.
          Conjunctions are supported using the `AND` operator.
          An example conjunction is:
          `CreatedAt >= '2025-01-07' AND Slug = 'test' AND Labels.mykey = 'myvalue'`.

          Supported attributes for filtering on Filter: CreatedAt, DeleteGates, DisplayName, FilterID, From, FromSpaceID, Hash, Labels, OrganizationID, ResourceType, Slug, SpaceID, UpdatedAt, Where, WhereData.

          The whole string must be query-encoded.
        in: query
        name: where
        required: false
        schema:
          type: string
      - description: |-
          UUID of a Filter entity to apply to the Filter list.

          The Filter must be in the same Organization as the user credentials.

          The Filter's From field must match the entity type being filtered (Filter).

          For Space-resident entities, if the Filter has a FromSpaceID, it must match the operation's SpaceID.

          The Filter's Where clause will be combined with any explicit 'where' parameter using AND logic.

          If both 'filter' and 'where' parameters are specified, they are combined with AND logic.
        in: query
        name: filter
        required: false
        schema:
          type: string
      - description: |-
          Free text search that approximately matches the specified string against string fields and map keys/values.

          The search is case-insensitive and uses pattern matching to find entities containing the text.

          Searchable string fields include attributes like Slug, DisplayName, and string-typed custom fields.

          For map fields (like Labels and Annotations), the search matches both map keys and values.

          The search uses OR logic across all searchable fields, so matching any field will return the entity.

          If both 'where' and 'contains' parameters are specified, they are combined with AND logic.

          Searchable fields for Filter include string and map-type attributes from the queryable attributes list.

          The whole string must be query-encoded.
        in: query
        name: contains
        required: false
        schema:
          type: string
      - description: |-
          Include clause for expanding related entities in the response for Filter.
          The attribute names are case-sensitive, PascalCase, and
          expected in a comma-separated list format as in the JSON encoding.

          Supported attributes for Filter are FromSpaceID, OrganizationID, SpaceID.

          The whole string must be query-encoded.
        in: query
        name: include
        required: false
        schema:
          type: string
      - description: |-
          Select clause for specifying which fields to include in the response for Filter.
          The attribute names are case-sensitive, PascalCase, and
          expected in a comma-separated list format as in the JSON encoding.
          If not specified, all fields are returned.
          Entity and parent IDs (like OrganizationID, SpaceID, FilterID) and Slug are always returned regardless of the select parameter.
          Fields used in where and contains filters are also automatically included.
          Example: 'DisplayName,CreatedAt,Labels' will return only those fields plus the required ID and Slug fields.
          The whole string must be query-encoded.
        in: query
        name: select
        required: false
        schema:
          type: string
      - description: Entity type to filter for (e.g., Unit, Space). Must be specified
          together with 'id' parameter.
        in: query
        name: entity
        required: false
        schema:
          type: string
      - description: Entity ID to filter for. Must be specified together with 'entity'
          parameter.
        in: query
        name: id
        required: false
        schema:
          type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                items:
                  $ref: '#/components/schemas/ExtendedFilter'
                type: array
          description: OK
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Filter request is invalid (Bad Request).
        "401":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Unauthorized access.
        "403":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Forbidden access.
        "404":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Filter not found.
        "500":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Something went wrong while processing Filter.
        default:
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Unexpected error.
      summary: List of Filters across spaces
      tags:
      - Filter
    patch:
      description: Apply JSON merge patch to multiple filters selected by query parameters
      operationId: BulkPatchFilters
      parameters:
      - description: |-
          The specified string is an expression for the purpose of filtering
          the list of Filters returned. The expression syntax was inspired by SQL.
          It supports conjunctions using `AND` of relational expressions of the form *attribute*
          *operator* *attribute_or_literal*. The attribute names are case-sensitive and PascalCase,
          as in the JSON encoding.
          Strings support the following operators: `<`, `>`, `<=`, `>=`, `=`, `!=`, `LIKE`, `NOT LIKE`, `ILIKE`, `~~`, `!~~`, `~`, `~*`, `!~`, `!~*`, `IN`, `NOT IN`.
          String pattern operators: `LIKE` and `~~` for pattern matching with `%` and `_` wildcards,
          `ILIKE` for case-insensitive pattern matching, `NOT LIKE` and `!~~` for negated pattern matching.
          String regex operators: `~` for regex matching, `~*` for case-insensitive regex,
          `!~` and `!~*` for regex not matching (case-sensitive and insensitive).
          Integers support the following operators: `<`, `>`, `<=`, `>=`, `=`, `!=`, `IN`, `NOT IN`.
          UUIDs and boolean attributes support equality and inequality only.
          UUID and time literals must be quoted as string literals.
          String literals are quoted with single quotes, such as `'string'`.
          Time literals use the same form as when serialized as JSON,
          such as: `CreatedAt > '2025-02-18T23:16:34'`.
          Integer and boolean literals are also supported for attributes of those types.
          Arrays support the `?` operator to to match any element of the array,
          as in `ApprovedBy ? '7c61626f-ddbe-41af-93f6-b69f4ab6d308'`.
          Arrays can perform LEN() to check for length, as in `LEN(ApprovedBy) > 0`.
          Map support the dot notation to specify a particular map key, as in `Labels.tier = 'Backend'`.
          Maps support `IS NULL` and `IS NOT NULL` with dot notation to check for key absence or presence,
          as in `Labels.tier IS NULL` (key doesn't exist) or `Labels.tier IS NOT NULL` (key exists).
          Comparison results can be tested with `IS TRUE`, `IS FALSE`, `IS NOT TRUE`, and `IS NOT FALSE`.
          These are useful for nullable columns: `MergeSourceID = '<uuid>' IS NOT FALSE` matches rows where MergeSourceID equals the value OR is NULL.
          The `IN` and `NOT IN` operators accept a comma-separated list of values in parentheses,
          such as `Slug IN ('slugone', 'slugtwo')` or `Labels.environment IN ('prod', 'staging')`.
          Conjunctions are supported using the `AND` operator.
          An example conjunction is:
          `CreatedAt >= '2025-01-07' AND Slug = 'test' AND Labels.mykey = 'myvalue'`.

          Supported attributes for filtering on Filter: CreatedAt, DeleteGates, DisplayName, FilterID, From, FromSpaceID, Hash, Labels, OrganizationID, ResourceType, Slug, SpaceID, UpdatedAt, Where, WhereData.

          The whole string must be query-encoded.
        in: query
        name: where
        required: false
        schema:
          type: string
      - description: |-
          UUID of a Filter entity to apply to the Filter list.

          The Filter must be in the same Organization as the user credentials.

          The Filter's From field must match the entity type being filtered (Filter).

          For Space-resident entities, if the Filter has a FromSpaceID, it must match the operation's SpaceID.

          The Filter's Where clause will be combined with any explicit 'where' parameter using AND logic.

          If both 'filter' and 'where' parameters are specified, they are combined with AND logic.
        in: query
        name: filter
        required: false
        schema:
          type: string
      - description: |-
          Free text search that approximately matches the specified string against string fields and map keys/values.

          The search is case-insensitive and uses pattern matching to find entities containing the text.

          Searchable string fields include attributes like Slug, DisplayName, and string-typed custom fields.

          For map fields (like Labels and Annotations), the search matches both map keys and values.

          The search uses OR logic across all searchable fields, so matching any field will return the entity.

          If both 'where' and 'contains' parameters are specified, they are combined with AND logic.

          Searchable fields for Filter include string and map-type attributes from the queryable attributes list.

          The whole string must be query-encoded.
        in: query
        name: contains
        required: false
        schema:
          type: string
      - description: |-
          Include clause for expanding related entities in the response for Filter.
          The attribute names are case-sensitive, PascalCase, and
          expected in a comma-separated list format as in the JSON encoding.

          Supported attributes for Filter are FromSpaceID, OrganizationID, SpaceID.

          The whole string must be query-encoded.
        in: query
        name: include
        required: false
        schema:
          type: string
      requestBody:
        content:
          application/merge-patch+json:
            schema:
              description: JSON Merge Patch schema for Filter. All fields are optional
                and nullable. Readonly fields are ignored.
              properties:
                Annotations:
                  additionalProperties:
                    nullable: true
                    type: string
                  description: An optional map of Annotation key/value pairs for tools
                    to attach information to entities.
                  nullable: true
                  type: object
                DeleteGates:
                  additionalProperties:
                    nullable: true
                    type: boolean
                  description: An optional set of gates that, if any is present, will
                    block deletion
                  nullable: true
                  type: object
                DisplayName:
                  description: Friendly name for the entity.
                  nullable: true
                  type: string
                From:
                  description: ""
                  nullable: true
                  type: string
                FromSpaceID:
                  description: ""
                  format: uuid
                  nullable: true
                  type: string
                Labels:
                  additionalProperties:
                    nullable: true
                    type: string
                  description: An optional map of Label key/value pairs to specify
                    identifying attributes of entities for the purpose of grouping
                    and filtering them.
                  nullable: true
                  type: object
                ResourceType:
                  description: ""
                  nullable: true
                  type: string
                Slug:
                  description: Unique URL-safe identifier for the entity.
                  nullable: true
                  type: string
                Version:
                  description: An entity-specific sequence number used for optimistic
                    concurrency control. The value read must be sent in calls to Update.
                  nullable: true
                  type: integer
                Where:
                  description: ""
                  nullable: true
                  type: string
                WhereData:
                  description: ""
                  nullable: true
                  type: string
              type: object
      responses:
        "200":
          content:
            application/json:
              schema:
                items:
                  $ref: '#/components/schemas/FilterCreateOrUpdateResponse'
                type: array
          description: OK
        "207":
          content:
            application/json:
              schema:
                items:
                  $ref: '#/components/schemas/FilterCreateOrUpdateResponse'
                type: array
          description: 'Multi-Status: Mixed success and failure results'
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Filter request is invalid (Bad Request).
        "401":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Unauthorized access.
        "403":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Forbidden access.
        "404":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Filter not found.
        "409":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Filter data conflict. Data has changed since last read.
        "500":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Something went wrong while processing Filter.
        default:
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Unexpected error.
      summary: Bulk patch multiple filters
      tags:
      - Filter
    post:
      description: Clone multiple filters selected by query parameters with optional
        name prefixes and destination spaces
      operationId: BulkCreateFilters
      parameters:
      - description: |-
          The specified string is an expression for the purpose of filtering
          the list of Filters returned. The expression syntax was inspired by SQL.
          It supports conjunctions using `AND` of relational expressions of the form *attribute*
          *operator* *attribute_or_literal*. The attribute names are case-sensitive and PascalCase,
          as in the JSON encoding.
          Strings support the following operators: `<`, `>`, `<=`, `>=`, `=`, `!=`, `LIKE`, `NOT LIKE`, `ILIKE`, `~~`, `!~~`, `~`, `~*`, `!~`, `!~*`, `IN`, `NOT IN`.
          String pattern operators: `LIKE` and `~~` for pattern matching with `%` and `_` wildcards,
          `ILIKE` for case-insensitive pattern matching, `NOT LIKE` and `!~~` for negated pattern matching.
          String regex operators: `~` for regex matching, `~*` for case-insensitive regex,
          `!~` and `!~*` for regex not matching (case-sensitive and insensitive).
          Integers support the following operators: `<`, `>`, `<=`, `>=`, `=`, `!=`, `IN`, `NOT IN`.
          UUIDs and boolean attributes support equality and inequality only.
          UUID and time literals must be quoted as string literals.
          String literals are quoted with single quotes, such as `'string'`.
          Time literals use the same form as when serialized as JSON,
          such as: `CreatedAt > '2025-02-18T23:16:34'`.
          Integer and boolean literals are also supported for attributes of those types.
          Arrays support the `?` operator to to match any element of the array,
          as in `ApprovedBy ? '7c61626f-ddbe-41af-93f6-b69f4ab6d308'`.
          Arrays can perform LEN() to check for length, as in `LEN(ApprovedBy) > 0`.
          Map support the dot notation to specify a particular map key, as in `Labels.tier = 'Backend'`.
          Maps support `IS NULL` and `IS NOT NULL` with dot notation to check for key absence or presence,
          as in `Labels.tier IS NULL` (key doesn't exist) or `Labels.tier IS NOT NULL` (key exists).
          Comparison results can be tested with `IS TRUE`, `IS FALSE`, `IS NOT TRUE`, and `IS NOT FALSE`.
          These are useful for nullable columns: `MergeSourceID = '<uuid>' IS NOT FALSE` matches rows where MergeSourceID equals the value OR is NULL.
          The `IN` and `NOT IN` operators accept a comma-separated list of values in parentheses,
          such as `Slug IN ('slugone', 'slugtwo')` or `Labels.environment IN ('prod', 'staging')`.
          Conjunctions are supported using the `AND` operator.
          An example conjunction is:
          `CreatedAt >= '2025-01-07' AND Slug = 'test' AND Labels.mykey = 'myvalue'`.

          Supported attributes for filtering on Filter: CreatedAt, DeleteGates, DisplayName, FilterID, From, FromSpaceID, Hash, Labels, OrganizationID, ResourceType, Slug, SpaceID, UpdatedAt, Where, WhereData.

          The whole string must be query-encoded.
        in: query
        name: where
        required: false
        schema:
          type: string
      - description: |-
          UUID of a Filter entity to apply to the Filter list.

          The Filter must be in the same Organization as the user credentials.

          The Filter's From field must match the entity type being filtered (Filter).

          For Space-resident entities, if the Filter has a FromSpaceID, it must match the operation's SpaceID.

          The Filter's Where clause will be combined with any explicit 'where' parameter using AND logic.

          If both 'filter' and 'where' parameters are specified, they are combined with AND logic.
        in: query
        name: filter
        required: false
        schema:
          type: string
      - description: |-
          Free text search that approximately matches the specified string against string fields and map keys/values.

          The search is case-insensitive and uses pattern matching to find entities containing the text.

          Searchable string fields include attributes like Slug, DisplayName, and string-typed custom fields.

          For map fields (like Labels and Annotations), the search matches both map keys and values.

          The search uses OR logic across all searchable fields, so matching any field will return the entity.

          If both 'where' and 'contains' parameters are specified, they are combined with AND logic.

          Searchable fields for Filter include string and map-type attributes from the queryable attributes list.

          The whole string must be query-encoded.
        in: query
        name: contains
        required: false
        schema:
          type: string
      - description: |-
          Include clause for expanding related entities in the response for Filter.
          The attribute names are case-sensitive, PascalCase, and
          expected in a comma-separated list format as in the JSON encoding.

          Supported attributes for Filter are FromSpaceID, OrganizationID, SpaceID.

          The whole string must be query-encoded.
        in: query
        name: include
        required: false
        schema:
          type: string
      - description: Comma-separated list of prefixes to apply to cloned Filter names
        in: query
        name: name_prefixes
        required: false
        schema:
          type: string
      - description: Comma-separated list of labels with multiple values for cloned
          Filter labels, in the format of key1=value1|value2,key2=value1|value2|value3
        in: query
        name: variant_labels
        required: false
        schema:
          type: string
      - description: A Go-template string for clone name, use .SourceEntity to access
          the original entity and .Labels to access variant labels
        in: query
        name: name_pattern
        required: false
        schema:
          type: string
      - description: |-
          The specified string is an expression for the purpose of filtering
          the list of Spaces returned. The expression syntax was inspired by SQL.
          It supports conjunctions using `AND` of relational expressions of the form *attribute*
          *operator* *attribute_or_literal*. The attribute names are case-sensitive and PascalCase,
          as in the JSON encoding.
          Strings support the following operators: `<`, `>`, `<=`, `>=`, `=`, `!=`, `LIKE`, `NOT LIKE`, `ILIKE`, `~~`, `!~~`, `~`, `~*`, `!~`, `!~*`, `IN`, `NOT IN`.
          String pattern operators: `LIKE` and `~~` for pattern matching with `%` and `_` wildcards,
          `ILIKE` for case-insensitive pattern matching, `NOT LIKE` and `!~~` for negated pattern matching.
          String regex operators: `~` for regex matching, `~*` for case-insensitive regex,
          `!~` and `!~*` for regex not matching (case-sensitive and insensitive).
          Integers support the following operators: `<`, `>`, `<=`, `>=`, `=`, `!=`, `IN`, `NOT IN`.
          UUIDs and boolean attributes support equality and inequality only.
          UUID and time literals must be quoted as string literals.
          String literals are quoted with single quotes, such as `'string'`.
          Time literals use the same form as when serialized as JSON,
          such as: `CreatedAt > '2025-02-18T23:16:34'`.
          Integer and boolean literals are also supported for attributes of those types.
          Arrays support the `?` operator to to match any element of the array,
          as in `ApprovedBy ? '7c61626f-ddbe-41af-93f6-b69f4ab6d308'`.
          Arrays can perform LEN() to check for length, as in `LEN(ApprovedBy) > 0`.
          Map support the dot notation to specify a particular map key, as in `Labels.tier = 'Backend'`.
          Maps support `IS NULL` and `IS NOT NULL` with dot notation to check for key absence or presence,
          as in `Labels.tier IS NULL` (key doesn't exist) or `Labels.tier IS NOT NULL` (key exists).
          Comparison results can be tested with `IS TRUE`, `IS FALSE`, `IS NOT TRUE`, and `IS NOT FALSE`.
          These are useful for nullable columns: `MergeSourceID = '<uuid>' IS NOT FALSE` matches rows where MergeSourceID equals the value OR is NULL.
          The `IN` and `NOT IN` operators accept a comma-separated list of values in parentheses,
          such as `Slug IN ('slugone', 'slugtwo')` or `Labels.environment IN ('prod', 'staging')`.
          Conjunctions are supported using the `AND` operator.
          An example conjunction is:
          `CreatedAt >= '2025-01-07' AND Slug = 'test' AND Labels.mykey = 'myvalue'`.

          Supported attributes for filtering on Space: AttributeFilterID, AttributeHash, AttributeIDs, CreatedAt, DeleteGates, DisplayName, Labels, OrganizationID, Permissions, Slug, SpaceID, TriggerFilterID, TriggerHash, TriggerIDs, UpdatedAt.

          Where expression to select destination spaces for cloning filters

          The whole string must be query-encoded.
        in: query
        name: where_space
        required: false
        schema:
          type: string
      - description: |-
          UUID of a Filter entity to apply to the Space list.

          The Filter must be in the same Organization as the user credentials.

          The Filter's From field must match the entity type being filtered (Space).

          For Space-resident entities, if the Filter has a FromSpaceID, it must match the operation's SpaceID.

          The Filter's Where clause will be combined with any explicit 'where' parameter using AND logic.

          If both 'filter' and 'where' parameters are specified, they are combined with AND logic.
        in: query
        name: filter_space
        required: false
        schema:
          type: string
      - description: Allowed values are true and false. Default is false. When true,
          reports success when an entity already exists and returns the existing entity
        in: query
        name: allow_exists
        required: false
        schema:
          type: string
      requestBody:
        content:
          application/merge-patch+json:
            schema:
              description: JSON Merge Patch schema for Filter. All fields are optional
                and nullable. Readonly fields are ignored.
              properties:
                Annotations:
                  additionalProperties:
                    nullable: true
                    type: string
                  description: An optional map of Annotation key/value pairs for tools
                    to attach information to entities.
                  nullable: true
                  type: object
                DeleteGates:
                  additionalProperties:
                    nullable: true
                    type: boolean
                  description: An optional set of gates that, if any is present, will
                    block deletion
                  nullable: true
                  type: object
                DisplayName:
                  description: Friendly name for the entity.
                  nullable: true
                  type: string
                From:
                  description: ""
                  nullable: true
                  type: string
                FromSpaceID:
                  description: ""
                  format: uuid
                  nullable: true
                  type: string
                Labels:
                  additionalProperties:
                    nullable: true
                    type: string
                  description: An optional map of Label key/value pairs to specify
                    identifying attributes of entities for the purpose of grouping
                    and filtering them.
                  nullable: true
                  type: object
                ResourceType:
                  description: ""
                  nullable: true
                  type: string
                Slug:
                  description: Unique URL-safe identifier for the entity.
                  nullable: true
                  type: string
                Version:
                  description: An entity-specific sequence number used for optimistic
                    concurrency control. The value read must be sent in calls to Update.
                  nullable: true
                  type: integer
                Where:
                  description: ""
                  nullable: true
                  type: string
                WhereData:
                  description: ""
                  nullable: true
                  type: string
              type: object
      responses:
        "200":
          content:
            application/json:
              schema:
                items:
                  $ref: '#/components/schemas/FilterCreateOrUpdateResponse'
                type: array
          description: OK
        "207":
          content:
            application/json:
              schema:
                items:
                  $ref: '#/components/schemas/FilterCreateOrUpdateResponse'
                type: array
          description: Multi-Status (partial success)
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Filter request is invalid (Bad Request).
        "401":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Unauthorized access.
        "403":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Forbidden access.
        "404":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Filter not found.
        "409":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Filter data conflict. Data has changed since last read.
        "500":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Something went wrong while processing Filter.
        default:
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Unexpected error.
      summary: Bulk create (clone) multiple filters
      tags:
      - Filter
  /function:
    get:
      description: List retrieves a map of Functions across the Organization
      operationId: ListOrgFunctions
      parameters:
      - description: Space ID or slug whose executor to use for listing or invoking
          builtin functions
        in: query
        name: executor_space
        required: false
        schema:
          type: string
      - description: |-
          The specified string is an expression for the purpose of filtering
          the list of FunctionSignatures returned. The expression syntax was inspired by SQL.
          It supports conjunctions using `AND` of relational expressions of the form *attribute*
          *operator* *attribute_or_literal*. The attribute names are case-sensitive and PascalCase,
          as in the JSON encoding.
          Strings support the following operators: `<`, `>`, `<=`, `>=`, `=`, `!=`, `LIKE`, `NOT LIKE`, `ILIKE`, `~~`, `!~~`, `~`, `~*`, `!~`, `!~*`, `IN`, `NOT IN`.
          String pattern operators: `LIKE` and `~~` for pattern matching with `%` and `_` wildcards,
          `ILIKE` for case-insensitive pattern matching, `NOT LIKE` and `!~~` for negated pattern matching.
          String regex operators: `~` for regex matching, `~*` for case-insensitive regex,
          `!~` and `!~*` for regex not matching (case-sensitive and insensitive).
          Integers support the following operators: `<`, `>`, `<=`, `>=`, `=`, `!=`, `IN`, `NOT IN`.
          UUIDs and boolean attributes support equality and inequality only.
          UUID and time literals must be quoted as string literals.
          String literals are quoted with single quotes, such as `'string'`.
          Time literals use the same form as when serialized as JSON,
          such as: `CreatedAt > '2025-02-18T23:16:34'`.
          Integer and boolean literals are also supported for attributes of those types.
          Arrays support the `?` operator to to match any element of the array,
          as in `ApprovedBy ? '7c61626f-ddbe-41af-93f6-b69f4ab6d308'`.
          Arrays can perform LEN() to check for length, as in `LEN(ApprovedBy) > 0`.
          Map support the dot notation to specify a particular map key, as in `Labels.tier = 'Backend'`.
          Maps support `IS NULL` and `IS NOT NULL` with dot notation to check for key absence or presence,
          as in `Labels.tier IS NULL` (key doesn't exist) or `Labels.tier IS NOT NULL` (key exists).
          Comparison results can be tested with `IS TRUE`, `IS FALSE`, `IS NOT TRUE`, and `IS NOT FALSE`.
          These are useful for nullable columns: `MergeSourceID = '<uuid>' IS NOT FALSE` matches rows where MergeSourceID equals the value OR is NULL.
          The `IN` and `NOT IN` operators accept a comma-separated list of values in parentheses,
          such as `Slug IN ('slugone', 'slugtwo')` or `Labels.environment IN ('prod', 'staging')`.
          Conjunctions are supported using the `AND` operator.
          An example conjunction is:
          `CreatedAt >= '2025-01-07' AND Slug = 'test' AND Labels.mykey = 'myvalue'`.

          Supported attributes for filtering on FunctionSignature: AttributeName, Description, FunctionName, FunctionType, Hermetic, Idempotent, Mutating, OutputInfo.Description, OutputInfo.OutputType, OutputInfo.ResultName, RequiredParameters, ToolchainType, Validating, VarArgs.

          The whole string must be query-encoded.
        in: query
        name: where
        required: false
        schema:
          type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                additionalProperties:
                  additionalProperties:
                    $ref: '#/components/schemas/FunctionSignature'
                  type: object
                type: object
          description: OK
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Function request is invalid (Bad Request).
        "401":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Unauthorized access.
        "403":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Forbidden access.
        "404":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Function not found.
        "500":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Something went wrong while processing Function.
        default:
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Unexpected error.
      summary: List retrieves a map of Functions across the Organization
      tags:
      - Function
  /function/invoke:
    post:
      description: Invoke Functions across Units in the Organization
      operationId: InvokeFunctionsOnOrg
      parameters:
      - description: Space ID or slug whose executor to use for listing or invoking
          builtin functions
        in: query
        name: executor_space
        required: false
        schema:
          type: string
      - description: 'Dry run mode: when true, skip updating configuration data even
          if it changed'
        in: query
        name: dry_run
        required: false
        schema:
          type: string
      - description: Must match ChangeSetID of affected Units unless in dry run mode;
          not valid when invoked on Revisions
        in: query
        name: change_set_id
        required: false
        schema:
          format: uuid
          type: string
      - description: User-defined category for the Mutation. Must be alphanumeric,
          at most 64 characters. The prefix 'ConfigHub' is reserved.
        in: query
        name: subgroup
        required: false
        schema:
          type: string
      - description: 'Source of additional configuration data to pass to functions
          that need it (e.g., vet-immutable). Supports named revision specifiers:
          LiveRevisionNum, LastAppliedRevisionNum, PreviousLiveRevisionNum, HeadRevisionNum.
          Can be prefixed with ''Before:'' (e.g., Before:HeadRevisionNum). May be
          repeated for multiple sources.'
        in: query
        name: other_data_source
        required: false
        schema:
          type: string
      - description: |-
          The specified string is an expression for the purpose of filtering
          the list of Units returned. The expression syntax was inspired by SQL.
          It supports conjunctions using `AND` of relational expressions of the form *attribute*
          *operator* *attribute_or_literal*. The attribute names are case-sensitive and PascalCase,
          as in the JSON encoding.
          Strings support the following operators: `<`, `>`, `<=`, `>=`, `=`, `!=`, `LIKE`, `NOT LIKE`, `ILIKE`, `~~`, `!~~`, `~`, `~*`, `!~`, `!~*`, `IN`, `NOT IN`.
          String pattern operators: `LIKE` and `~~` for pattern matching with `%` and `_` wildcards,
          `ILIKE` for case-insensitive pattern matching, `NOT LIKE` and `!~~` for negated pattern matching.
          String regex operators: `~` for regex matching, `~*` for case-insensitive regex,
          `!~` and `!~*` for regex not matching (case-sensitive and insensitive).
          Integers support the following operators: `<`, `>`, `<=`, `>=`, `=`, `!=`, `IN`, `NOT IN`.
          UUIDs and boolean attributes support equality and inequality only.
          UUID and time literals must be quoted as string literals.
          String literals are quoted with single quotes, such as `'string'`.
          Time literals use the same form as when serialized as JSON,
          such as: `CreatedAt > '2025-02-18T23:16:34'`.
          Integer and boolean literals are also supported for attributes of those types.
          Arrays support the `?` operator to to match any element of the array,
          as in `ApprovedBy ? '7c61626f-ddbe-41af-93f6-b69f4ab6d308'`.
          Arrays can perform LEN() to check for length, as in `LEN(ApprovedBy) > 0`.
          Map support the dot notation to specify a particular map key, as in `Labels.tier = 'Backend'`.
          Maps support `IS NULL` and `IS NOT NULL` with dot notation to check for key absence or presence,
          as in `Labels.tier IS NULL` (key doesn't exist) or `Labels.tier IS NOT NULL` (key exists).
          Comparison results can be tested with `IS TRUE`, `IS FALSE`, `IS NOT TRUE`, and `IS NOT FALSE`.
          These are useful for nullable columns: `MergeSourceID = '<uuid>' IS NOT FALSE` matches rows where MergeSourceID equals the value OR is NULL.
          The `IN` and `NOT IN` operators accept a comma-separated list of values in parentheses,
          such as `Slug IN ('slugone', 'slugtwo')` or `Labels.environment IN ('prod', 'staging')`.
          Conjunctions are supported using the `AND` operator.
          An example conjunction is:
          `CreatedAt >= '2025-01-07' AND Slug = 'test' AND Labels.mykey = 'myvalue'`.

          Supported attributes for filtering on Unit: ApplyGates, ApplyWarnings, ApprovedBy, BridgeWorkerID, ChangeSetID, CreatedAt, DataHash, DeleteGates, DestroyGates, DisplayName, DriftReconciliationMode, FromLinkID, HeadRevisionNum, HeadUnitActionNum, HeadUnitEventNum, Labels, LastActionAt, LastAppliedRevisionNum, LastChangeDescription, LiveRevisionNum, OrganizationID, PreviousLiveRevisionNum, ProviderType, Slug, SpaceID, TargetID, TargetOptions, ToolchainType, UnitID, UpdatedAt, UpstreamOrganizationID, UpstreamRevisionNum, UpstreamSpaceID, UpstreamUnitID, Values.

          Finding all units created by cloning can be done using the expression `UpstreamRevisionNum > 0`. Clones of a specific unit can be found by additionally filtering based on `UpstreamUnitID`. Unapplied units can be found using `LiveRevisionNum = 0`. Units with unapplied changes can be found with `HeadRevisionNum > LiveRevisionNum`.

          The whole string must be query-encoded.
        in: query
        name: where
        required: false
        schema:
          type: string
      - description: |-
          UUID of a Filter entity to apply to the Unit list.

          The Filter must be in the same Organization as the user credentials.

          The Filter's From field must match the entity type being filtered (Unit).

          For Space-resident entities, if the Filter has a FromSpaceID, it must match the operation's SpaceID.

          The Filter's Where clause will be combined with any explicit 'where' parameter using AND logic.

          If both 'filter' and 'where' parameters are specified, they are combined with AND logic.
        in: query
        name: filter
        required: false
        schema:
          type: string
      - description: 'Resource type: Resource type to match for the desired ToolchainType,
          for example apps/v1/Deployment'
        in: query
        name: resource_type
        required: false
        schema:
          type: string
      - description: 'Where data: The specified string is an expression for the purpose
          of evaluating whether the configuration data matches the filter. It supports
          conjunctions using `AND` of relational expressions of the form *path* *operator*
          *literal*. The path specifications are dot-separated, for both map fields
          and array indices, as in `spec.template.spec.containers.0.image = ''ghcr.io/headlamp-k8s/headlamp:latest''
          AND spec.replicas > 1`. Path expressions support `*` for wildcard array
          or map segments and `?key=value` syntax for associative matches of array
          elements containing objects with a `key` attribute. Strings support the
          following operators: `<`, `>`, `<=`, `>=`, `=`, `!=`, `LIKE`, `ILIKE`, `~~`,
          `!~~`, `~`, `!~`, `~*`, `!~*`, `IN`, `NOT IN`. String pattern operators:
          `LIKE` and `~~` for pattern matching with `%` and `_` wildcards, `ILIKE`
          for case-insensitive pattern matching, `!~~` for NOT LIKE. String regex
          operators: `~` for regex matching, `~*` for case-insensitive regex, `!~`
          and `!~*` for regex not matching (case-sensitive and insensitive). Integers
          support the following operators: `<`, `>`, `<=`, `>=`, `=`, `!=`, `IN`,
          `NOT IN`. Boolean values support equality and inequality only. The `IN`
          and `NOT IN` operators accept a comma-separated list of values in parentheses,
          such as `spec.template.spec.containers.0.image#reference IN ('':latest'',
          '':arm64-latest'')`. The syntax `.|` requires the preceding path to exist;
          otherwise the relation `!=` will always return true regardless what it is
          compared with. String literals are quoted with single quotes, such as `''string''`.
          Integer and boolean literals are also supported for attributes of those
          types. The whole string must be query-encoded.'
        in: query
        name: where_data
        required: false
        schema:
          type: string
      - description: 'Where expression to match Triggers. Matched triggers are invoked
          on each unit to filter by validation results. Use with triggers_passed to
          control whether passing or failing units are returned (default: failing).'
        in: query
        name: where_trigger
        required: false
        schema:
          type: string
      - description: Filter UUID (with From=Trigger). The filter's matching triggers
          are invoked on units to filter by validation results. Can be combined with
          where_trigger.
        in: query
        name: trigger_filter
        required: false
        schema:
          type: string
      - description: When true, return units that pass trigger validation; when false
          (default), return units that fail. Only applies when where_trigger or trigger_filter
          is specified.
        in: query
        name: triggers_passed
        required: false
        schema:
          type: boolean
      - description: View slug or UUID. Applies the View's column definitions to extract
          values for each unit. If the View has a FilterID, its filter is ANDed with
          other filters. The View must have Of=Unit or a Filter with From=Unit.
        in: query
        name: view
        required: false
        schema:
          type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/FunctionInvocationsRequest'
      responses:
        "200":
          content:
            application/json:
              schema:
                items:
                  $ref: '#/components/schemas/FunctionInvocationsResponse'
                type: array
          description: OK
        "207":
          content:
            application/json:
              schema:
                items:
                  $ref: '#/components/schemas/FunctionInvocationsResponse'
                type: array
          description: 'Multi-Status: Mixed success and failure results'
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Function request is invalid (Bad Request).
        "401":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Unauthorized access.
        "403":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Forbidden access.
        "404":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Function not found.
        "409":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Function data conflict. Data has changed since last read.
        "422":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Unable to process configuration data
        "429":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Bridge worker is overloaded with outstanding function invocations
        "500":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Something went wrong while processing Function.
        "503":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Bridge worker is unavailable; one or more function sources
            were not executed
        "504":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Timed out waiting for the bridge worker to return a function
            result
        default:
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Unexpected error.
      summary: Invoke Functions across Org
      tags:
      - Function
  /info:
    get:
      description: Get info about this api server
      operationId: apiInfo
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApiInfo'
          description: Information provided to clients by the server.
      summary: Get info about this api server
      tags:
      - Meta
  /invocation:
    delete:
      description: Delete multiple invocations selected by query parameters
      operationId: BulkDeleteInvocations
      parameters:
      - description: |-
          The specified string is an expression for the purpose of filtering
          the list of Invocations returned. The expression syntax was inspired by SQL.
          It supports conjunctions using `AND` of relational expressions of the form *attribute*
          *operator* *attribute_or_literal*. The attribute names are case-sensitive and PascalCase,
          as in the JSON encoding.
          Strings support the following operators: `<`, `>`, `<=`, `>=`, `=`, `!=`, `LIKE`, `NOT LIKE`, `ILIKE`, `~~`, `!~~`, `~`, `~*`, `!~`, `!~*`, `IN`, `NOT IN`.
          String pattern operators: `LIKE` and `~~` for pattern matching with `%` and `_` wildcards,
          `ILIKE` for case-insensitive pattern matching, `NOT LIKE` and `!~~` for negated pattern matching.
          String regex operators: `~` for regex matching, `~*` for case-insensitive regex,
          `!~` and `!~*` for regex not matching (case-sensitive and insensitive).
          Integers support the following operators: `<`, `>`, `<=`, `>=`, `=`, `!=`, `IN`, `NOT IN`.
          UUIDs and boolean attributes support equality and inequality only.
          UUID and time literals must be quoted as string literals.
          String literals are quoted with single quotes, such as `'string'`.
          Time literals use the same form as when serialized as JSON,
          such as: `CreatedAt > '2025-02-18T23:16:34'`.
          Integer and boolean literals are also supported for attributes of those types.
          Arrays support the `?` operator to to match any element of the array,
          as in `ApprovedBy ? '7c61626f-ddbe-41af-93f6-b69f4ab6d308'`.
          Arrays can perform LEN() to check for length, as in `LEN(ApprovedBy) > 0`.
          Map support the dot notation to specify a particular map key, as in `Labels.tier = 'Backend'`.
          Maps support `IS NULL` and `IS NOT NULL` with dot notation to check for key absence or presence,
          as in `Labels.tier IS NULL` (key doesn't exist) or `Labels.tier IS NOT NULL` (key exists).
          Comparison results can be tested with `IS TRUE`, `IS FALSE`, `IS NOT TRUE`, and `IS NOT FALSE`.
          These are useful for nullable columns: `MergeSourceID = '<uuid>' IS NOT FALSE` matches rows where MergeSourceID equals the value OR is NULL.
          The `IN` and `NOT IN` operators accept a comma-separated list of values in parentheses,
          such as `Slug IN ('slugone', 'slugtwo')` or `Labels.environment IN ('prod', 'staging')`.
          Conjunctions are supported using the `AND` operator.
          An example conjunction is:
          `CreatedAt >= '2025-01-07' AND Slug = 'test' AND Labels.mykey = 'myvalue'`.

          Supported attributes for filtering on Invocation: BridgeWorkerID, CreatedAt, DeleteGates, DisplayName, FunctionName, Hash, InvocationID, Labels, OrganizationID, Slug, SpaceID, ToolchainType, UpdatedAt.

          The whole string must be query-encoded.
        in: query
        name: where
        required: false
        schema:
          type: string
      - description: |-
          UUID of a Filter entity to apply to the Invocation list.

          The Filter must be in the same Organization as the user credentials.

          The Filter's From field must match the entity type being filtered (Invocation).

          For Space-resident entities, if the Filter has a FromSpaceID, it must match the operation's SpaceID.

          The Filter's Where clause will be combined with any explicit 'where' parameter using AND logic.

          If both 'filter' and 'where' parameters are specified, they are combined with AND logic.
        in: query
        name: filter
        required: false
        schema:
          type: string
      - description: |-
          Free text search that approximately matches the specified string against string fields and map keys/values.

          The search is case-insensitive and uses pattern matching to find entities containing the text.

          Searchable string fields include attributes like Slug, DisplayName, and string-typed custom fields.

          For map fields (like Labels and Annotations), the search matches both map keys and values.

          The search uses OR logic across all searchable fields, so matching any field will return the entity.

          If both 'where' and 'contains' parameters are specified, they are combined with AND logic.

          Searchable fields for Invocation include string and map-type attributes from the queryable attributes list.

          The whole string must be query-encoded.
        in: query
        name: contains
        required: false
        schema:
          type: string
      - description: |-
          Include clause for expanding related entities in the response for Invocation.
          The attribute names are case-sensitive, PascalCase, and
          expected in a comma-separated list format as in the JSON encoding.

          Supported attributes for Invocation are BridgeWorkerID, OrganizationID, SpaceID.

          The whole string must be query-encoded.
        in: query
        name: include
        required: false
        schema:
          type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                items:
                  $ref: '#/components/schemas/DeleteResponse'
                type: array
          description: OK
        "207":
          content:
            application/json:
              schema:
                items:
                  $ref: '#/components/schemas/DeleteResponse'
                type: array
          description: 'Multi-Status: Mixed success and failure results'
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Invocation request is invalid (Bad Request).
        "401":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Unauthorized access.
        "403":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Forbidden access.
        "404":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Invocation not found.
        "409":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Invocation data conflict. Data has changed since last read.
        "422":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Unable to delete entity
        "500":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Something went wrong while processing Invocation.
        default:
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Unexpected error.
      summary: Bulk delete multiple invocations
      tags:
      - Invocation
    get:
      description: Retrieves a list of Invocations across spaces in the Organization
      operationId: ListAllInvocations
      parameters:
      - description: |-
          The specified string is an expression for the purpose of filtering
          the list of Invocations returned. The expression syntax was inspired by SQL.
          It supports conjunctions using `AND` of relational expressions of the form *attribute*
          *operator* *attribute_or_literal*. The attribute names are case-sensitive and PascalCase,
          as in the JSON encoding.
          Strings support the following operators: `<`, `>`, `<=`, `>=`, `=`, `!=`, `LIKE`, `NOT LIKE`, `ILIKE`, `~~`, `!~~`, `~`, `~*`, `!~`, `!~*`, `IN`, `NOT IN`.
          String pattern operators: `LIKE` and `~~` for pattern matching with `%` and `_` wildcards,
          `ILIKE` for case-insensitive pattern matching, `NOT LIKE` and `!~~` for negated pattern matching.
          String regex operators: `~` for regex matching, `~*` for case-insensitive regex,
          `!~` and `!~*` for regex not matching (case-sensitive and insensitive).
          Integers support the following operators: `<`, `>`, `<=`, `>=`, `=`, `!=`, `IN`, `NOT IN`.
          UUIDs and boolean attributes support equality and inequality only.
          UUID and time literals must be quoted as string literals.
          String literals are quoted with single quotes, such as `'string'`.
          Time literals use the same form as when serialized as JSON,
          such as: `CreatedAt > '2025-02-18T23:16:34'`.
          Integer and boolean literals are also supported for attributes of those types.
          Arrays support the `?` operator to to match any element of the array,
          as in `ApprovedBy ? '7c61626f-ddbe-41af-93f6-b69f4ab6d308'`.
          Arrays can perform LEN() to check for length, as in `LEN(ApprovedBy) > 0`.
          Map support the dot notation to specify a particular map key, as in `Labels.tier = 'Backend'`.
          Maps support `IS NULL` and `IS NOT NULL` with dot notation to check for key absence or presence,
          as in `Labels.tier IS NULL` (key doesn't exist) or `Labels.tier IS NOT NULL` (key exists).
          Comparison results can be tested with `IS TRUE`, `IS FALSE`, `IS NOT TRUE`, and `IS NOT FALSE`.
          These are useful for nullable columns: `MergeSourceID = '<uuid>' IS NOT FALSE` matches rows where MergeSourceID equals the value OR is NULL.
          The `IN` and `NOT IN` operators accept a comma-separated list of values in parentheses,
          such as `Slug IN ('slugone', 'slugtwo')` or `Labels.environment IN ('prod', 'staging')`.
          Conjunctions are supported using the `AND` operator.
          An example conjunction is:
          `CreatedAt >= '2025-01-07' AND Slug = 'test' AND Labels.mykey = 'myvalue'`.

          Supported attributes for filtering on Invocation: BridgeWorkerID, CreatedAt, DeleteGates, DisplayName, FunctionName, Hash, InvocationID, Labels, OrganizationID, Slug, SpaceID, ToolchainType, UpdatedAt.

          The whole string must be query-encoded.
        in: query
        name: where
        required: false
        schema:
          type: string
      - description: |-
          UUID of a Filter entity to apply to the Invocation list.

          The Filter must be in the same Organization as the user credentials.

          The Filter's From field must match the entity type being filtered (Invocation).

          For Space-resident entities, if the Filter has a FromSpaceID, it must match the operation's SpaceID.

          The Filter's Where clause will be combined with any explicit 'where' parameter using AND logic.

          If both 'filter' and 'where' parameters are specified, they are combined with AND logic.
        in: query
        name: filter
        required: false
        schema:
          type: string
      - description: |-
          Free text search that approximately matches the specified string against string fields and map keys/values.

          The search is case-insensitive and uses pattern matching to find entities containing the text.

          Searchable string fields include attributes like Slug, DisplayName, and string-typed custom fields.

          For map fields (like Labels and Annotations), the search matches both map keys and values.

          The search uses OR logic across all searchable fields, so matching any field will return the entity.

          If both 'where' and 'contains' parameters are specified, they are combined with AND logic.

          Searchable fields for Invocation include string and map-type attributes from the queryable attributes list.

          The whole string must be query-encoded.
        in: query
        name: contains
        required: false
        schema:
          type: string
      - description: |-
          Include clause for expanding related entities in the response for Invocation.
          The attribute names are case-sensitive, PascalCase, and
          expected in a comma-separated list format as in the JSON encoding.

          Supported attributes for Invocation are BridgeWorkerID, OrganizationID, SpaceID.

          The whole string must be query-encoded.
        in: query
        name: include
        required: false
        schema:
          type: string
      - description: |-
          Select clause for specifying which fields to include in the response for Invocation.
          The attribute names are case-sensitive, PascalCase, and
          expected in a comma-separated list format as in the JSON encoding.
          If not specified, all fields are returned.
          Entity and parent IDs (like OrganizationID, SpaceID, InvocationID) and Slug are always returned regardless of the select parameter.
          Fields used in where and contains filters are also automatically included.
          Example: 'DisplayName,CreatedAt,Labels' will return only those fields plus the required ID and Slug fields.
          The whole string must be query-encoded.
        in: query
        name: select
        required: false
        schema:
          type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                items:
                  $ref: '#/components/schemas/ExtendedInvocation'
                type: array
          description: OK
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Invocation request is invalid (Bad Request).
        "401":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Unauthorized access.
        "403":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Forbidden access.
        "404":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Invocation not found.
        "500":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Something went wrong while processing Invocation.
        default:
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Unexpected error.
      summary: List of Invocations across spaces
      tags:
      - Invocation
    patch:
      description: Apply JSON merge patch to multiple invocations selected by query
        parameters
      operationId: BulkPatchInvocations
      parameters:
      - description: |-
          The specified string is an expression for the purpose of filtering
          the list of Invocations returned. The expression syntax was inspired by SQL.
          It supports conjunctions using `AND` of relational expressions of the form *attribute*
          *operator* *attribute_or_literal*. The attribute names are case-sensitive and PascalCase,
          as in the JSON encoding.
          Strings support the following operators: `<`, `>`, `<=`, `>=`, `=`, `!=`, `LIKE`, `NOT LIKE`, `ILIKE`, `~~`, `!~~`, `~`, `~*`, `!~`, `!~*`, `IN`, `NOT IN`.
          String pattern operators: `LIKE` and `~~` for pattern matching with `%` and `_` wildcards,
          `ILIKE` for case-insensitive pattern matching, `NOT LIKE` and `!~~` for negated pattern matching.
          String regex operators: `~` for regex matching, `~*` for case-insensitive regex,
          `!~` and `!~*` for regex not matching (case-sensitive and insensitive).
          Integers support the following operators: `<`, `>`, `<=`, `>=`, `=`, `!=`, `IN`, `NOT IN`.
          UUIDs and boolean attributes support equality and inequality only.
          UUID and time literals must be quoted as string literals.
          String literals are quoted with single quotes, such as `'string'`.
          Time literals use the same form as when serialized as JSON,
          such as: `CreatedAt > '2025-02-18T23:16:34'`.
          Integer and boolean literals are also supported for attributes of those types.
          Arrays support the `?` operator to to match any element of the array,
          as in `ApprovedBy ? '7c61626f-ddbe-41af-93f6-b69f4ab6d308'`.
          Arrays can perform LEN() to check for length, as in `LEN(ApprovedBy) > 0`.
          Map support the dot notation to specify a particular map key, as in `Labels.tier = 'Backend'`.
          Maps support `IS NULL` and `IS NOT NULL` with dot notation to check for key absence or presence,
          as in `Labels.tier IS NULL` (key doesn't exist) or `Labels.tier IS NOT NULL` (key exists).
          Comparison results can be tested with `IS TRUE`, `IS FALSE`, `IS NOT TRUE`, and `IS NOT FALSE`.
          These are useful for nullable columns: `MergeSourceID = '<uuid>' IS NOT FALSE` matches rows where MergeSourceID equals the value OR is NULL.
          The `IN` and `NOT IN` operators accept a comma-separated list of values in parentheses,
          such as `Slug IN ('slugone', 'slugtwo')` or `Labels.environment IN ('prod', 'staging')`.
          Conjunctions are supported using the `AND` operator.
          An example conjunction is:
          `CreatedAt >= '2025-01-07' AND Slug = 'test' AND Labels.mykey = 'myvalue'`.

          Supported attributes for filtering on Invocation: BridgeWorkerID, CreatedAt, DeleteGates, DisplayName, FunctionName, Hash, InvocationID, Labels, OrganizationID, Slug, SpaceID, ToolchainType, UpdatedAt.

          The whole string must be query-encoded.
        in: query
        name: where
        required: false
        schema:
          type: string
      - description: |-
          UUID of a Filter entity to apply to the Invocation list.

          The Filter must be in the same Organization as the user credentials.

          The Filter's From field must match the entity type being filtered (Invocation).

          For Space-resident entities, if the Filter has a FromSpaceID, it must match the operation's SpaceID.

          The Filter's Where clause will be combined with any explicit 'where' parameter using AND logic.

          If both 'filter' and 'where' parameters are specified, they are combined with AND logic.
        in: query
        name: filter
        required: false
        schema:
          type: string
      - description: |-
          Free text search that approximately matches the specified string against string fields and map keys/values.

          The search is case-insensitive and uses pattern matching to find entities containing the text.

          Searchable string fields include attributes like Slug, DisplayName, and string-typed custom fields.

          For map fields (like Labels and Annotations), the search matches both map keys and values.

          The search uses OR logic across all searchable fields, so matching any field will return the entity.

          If both 'where' and 'contains' parameters are specified, they are combined with AND logic.

          Searchable fields for Invocation include string and map-type attributes from the queryable attributes list.

          The whole string must be query-encoded.
        in: query
        name: contains
        required: false
        schema:
          type: string
      - description: |-
          Include clause for expanding related entities in the response for Invocation.
          The attribute names are case-sensitive, PascalCase, and
          expected in a comma-separated list format as in the JSON encoding.

          Supported attributes for Invocation are BridgeWorkerID, OrganizationID, SpaceID.

          The whole string must be query-encoded.
        in: query
        name: include
        required: false
        schema:
          type: string
      requestBody:
        content:
          application/merge-patch+json:
            schema:
              description: JSON Merge Patch schema for Invocation. All fields are
                optional and nullable. Readonly fields are ignored.
              properties:
                Annotations:
                  additionalProperties:
                    nullable: true
                    type: string
                  description: An optional map of Annotation key/value pairs for tools
                    to attach information to entities.
                  nullable: true
                  type: object
                Arguments:
                  description: Function arguments
                  items:
                    nullable: true
                    type: object
                  nullable: true
                  type: array
                BridgeWorkerID:
                  description: ""
                  format: uuid
                  nullable: true
                  type: string
                DeleteGates:
                  additionalProperties:
                    nullable: true
                    type: boolean
                  description: An optional set of gates that, if any is present, will
                    block deletion
                  nullable: true
                  type: object
                DisplayName:
                  description: Friendly name for the entity.
                  nullable: true
                  type: string
                FunctionName:
                  description: Function name
                  nullable: true
                  type: string
                Labels:
                  additionalProperties:
                    nullable: true
                    type: string
                  description: An optional map of Label key/value pairs to specify
                    identifying attributes of entities for the purpose of grouping
                    and filtering them.
                  nullable: true
                  type: object
                Slug:
                  description: Unique URL-safe identifier for the entity.
                  nullable: true
                  type: string
                ToolchainType:
                  description: ""
                  nullable: true
                  type: string
                Version:
                  description: An entity-specific sequence number used for optimistic
                    concurrency control. The value read must be sent in calls to Update.
                  nullable: true
                  type: integer
              type: object
      responses:
        "200":
          content:
            application/json:
              schema:
                items:
                  $ref: '#/components/schemas/InvocationCreateOrUpdateResponse'
                type: array
          description: OK
        "207":
          content:
            application/json:
              schema:
                items:
                  $ref: '#/components/schemas/InvocationCreateOrUpdateResponse'
                type: array
          description: 'Multi-Status: Mixed success and failure results'
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Invocation request is invalid (Bad Request).
        "401":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Unauthorized access.
        "403":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Forbidden access.
        "404":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Invocation not found.
        "409":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Invocation data conflict. Data has changed since last read.
        "500":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Something went wrong while processing Invocation.
        default:
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Unexpected error.
      summary: Bulk patch multiple invocations
      tags:
      - Invocation
    post:
      description: Clone multiple invocations selected by query parameters with optional
        name prefixes and destination spaces
      operationId: BulkCreateInvocations
      parameters:
      - description: |-
          The specified string is an expression for the purpose of filtering
          the list of Invocations returned. The expression syntax was inspired by SQL.
          It supports conjunctions using `AND` of relational expressions of the form *attribute*
          *operator* *attribute_or_literal*. The attribute names are case-sensitive and PascalCase,
          as in the JSON encoding.
          Strings support the following operators: `<`, `>`, `<=`, `>=`, `=`, `!=`, `LIKE`, `NOT LIKE`, `ILIKE`, `~~`, `!~~`, `~`, `~*`, `!~`, `!~*`, `IN`, `NOT IN`.
          String pattern operators: `LIKE` and `~~` for pattern matching with `%` and `_` wildcards,
          `ILIKE` for case-insensitive pattern matching, `NOT LIKE` and `!~~` for negated pattern matching.
          String regex operators: `~` for regex matching, `~*` for case-insensitive regex,
          `!~` and `!~*` for regex not matching (case-sensitive and insensitive).
          Integers support the following operators: `<`, `>`, `<=`, `>=`, `=`, `!=`, `IN`, `NOT IN`.
          UUIDs and boolean attributes support equality and inequality only.
          UUID and time literals must be quoted as string literals.
          String literals are quoted with single quotes, such as `'string'`.
          Time literals use the same form as when serialized as JSON,
          such as: `CreatedAt > '2025-02-18T23:16:34'`.
          Integer and boolean literals are also supported for attributes of those types.
          Arrays support the `?` operator to to match any element of the array,
          as in `ApprovedBy ? '7c61626f-ddbe-41af-93f6-b69f4ab6d308'`.
          Arrays can perform LEN() to check for length, as in `LEN(ApprovedBy) > 0`.
          Map support the dot notation to specify a particular map key, as in `Labels.tier = 'Backend'`.
          Maps support `IS NULL` and `IS NOT NULL` with dot notation to check for key absence or presence,
          as in `Labels.tier IS NULL` (key doesn't exist) or `Labels.tier IS NOT NULL` (key exists).
          Comparison results can be tested with `IS TRUE`, `IS FALSE`, `IS NOT TRUE`, and `IS NOT FALSE`.
          These are useful for nullable columns: `MergeSourceID = '<uuid>' IS NOT FALSE` matches rows where MergeSourceID equals the value OR is NULL.
          The `IN` and `NOT IN` operators accept a comma-separated list of values in parentheses,
          such as `Slug IN ('slugone', 'slugtwo')` or `Labels.environment IN ('prod', 'staging')`.
          Conjunctions are supported using the `AND` operator.
          An example conjunction is:
          `CreatedAt >= '2025-01-07' AND Slug = 'test' AND Labels.mykey = 'myvalue'`.

          Supported attributes for filtering on Invocation: BridgeWorkerID, CreatedAt, DeleteGates, DisplayName, FunctionName, Hash, InvocationID, Labels, OrganizationID, Slug, SpaceID, ToolchainType, UpdatedAt.

          The whole string must be query-encoded.
        in: query
        name: where
        required: false
        schema:
          type: string
      - description: |-
          UUID of a Filter entity to apply to the Invocation list.

          The Filter must be in the same Organization as the user credentials.

          The Filter's From field must match the entity type being filtered (Invocation).

          For Space-resident entities, if the Filter has a FromSpaceID, it must match the operation's SpaceID.

          The Filter's Where clause will be combined with any explicit 'where' parameter using AND logic.

          If both 'filter' and 'where' parameters are specified, they are combined with AND logic.
        in: query
        name: filter
        required: false
        schema:
          type: string
      - description: |-
          Free text search that approximately matches the specified string against string fields and map keys/values.

          The search is case-insensitive and uses pattern matching to find entities containing the text.

          Searchable string fields include attributes like Slug, DisplayName, and string-typed custom fields.

          For map fields (like Labels and Annotations), the search matches both map keys and values.

          The search uses OR logic across all searchable fields, so matching any field will return the entity.

          If both 'where' and 'contains' parameters are specified, they are combined with AND logic.

          Searchable fields for Invocation include string and map-type attributes from the queryable attributes list.

          The whole string must be query-encoded.
        in: query
        name: contains
        required: false
        schema:
          type: string
      - description: |-
          Include clause for expanding related entities in the response for Invocation.
          The attribute names are case-sensitive, PascalCase, and
          expected in a comma-separated list format as in the JSON encoding.

          Supported attributes for Invocation are BridgeWorkerID, OrganizationID, SpaceID.

          The whole string must be query-encoded.
        in: query
        name: include
        required: false
        schema:
          type: string
      - description: Comma-separated list of prefixes to apply to cloned Invocation
          names
        in: query
        name: name_prefixes
        required: false
        schema:
          type: string
      - description: Comma-separated list of labels with multiple values for cloned
          Invocation labels, in the format of key1=value1|value2,key2=value1|value2|value3
        in: query
        name: variant_labels
        required: false
        schema:
          type: string
      - description: 'A string for clone names, use the prefix ''template:'' for a
          Go-template with .SourceEntitySlug to access the original entity''s slug
          and .Labels to access variant labels, example: ''template:{{.SourceEntitySlug}}-{{.Labels.env}}'''
        in: query
        name: name_pattern
        required: false
        schema:
          type: string
      - description: |-
          The specified string is an expression for the purpose of filtering
          the list of Spaces returned. The expression syntax was inspired by SQL.
          It supports conjunctions using `AND` of relational expressions of the form *attribute*
          *operator* *attribute_or_literal*. The attribute names are case-sensitive and PascalCase,
          as in the JSON encoding.
          Strings support the following operators: `<`, `>`, `<=`, `>=`, `=`, `!=`, `LIKE`, `NOT LIKE`, `ILIKE`, `~~`, `!~~`, `~`, `~*`, `!~`, `!~*`, `IN`, `NOT IN`.
          String pattern operators: `LIKE` and `~~` for pattern matching with `%` and `_` wildcards,
          `ILIKE` for case-insensitive pattern matching, `NOT LIKE` and `!~~` for negated pattern matching.
          String regex operators: `~` for regex matching, `~*` for case-insensitive regex,
          `!~` and `!~*` for regex not matching (case-sensitive and insensitive).
          Integers support the following operators: `<`, `>`, `<=`, `>=`, `=`, `!=`, `IN`, `NOT IN`.
          UUIDs and boolean attributes support equality and inequality only.
          UUID and time literals must be quoted as string literals.
          String literals are quoted with single quotes, such as `'string'`.
          Time literals use the same form as when serialized as JSON,
          such as: `CreatedAt > '2025-02-18T23:16:34'`.
          Integer and boolean literals are also supported for attributes of those types.
          Arrays support the `?` operator to to match any element of the array,
          as in `ApprovedBy ? '7c61626f-ddbe-41af-93f6-b69f4ab6d308'`.
          Arrays can perform LEN() to check for length, as in `LEN(ApprovedBy) > 0`.
          Map support the dot notation to specify a particular map key, as in `Labels.tier = 'Backend'`.
          Maps support `IS NULL` and `IS NOT NULL` with dot notation to check for key absence or presence,
          as in `Labels.tier IS NULL` (key doesn't exist) or `Labels.tier IS NOT NULL` (key exists).
          Comparison results can be tested with `IS TRUE`, `IS FALSE`, `IS NOT TRUE`, and `IS NOT FALSE`.
          These are useful for nullable columns: `MergeSourceID = '<uuid>' IS NOT FALSE` matches rows where MergeSourceID equals the value OR is NULL.
          The `IN` and `NOT IN` operators accept a comma-separated list of values in parentheses,
          such as `Slug IN ('slugone', 'slugtwo')` or `Labels.environment IN ('prod', 'staging')`.
          Conjunctions are supported using the `AND` operator.
          An example conjunction is:
          `CreatedAt >= '2025-01-07' AND Slug = 'test' AND Labels.mykey = 'myvalue'`.

          Supported attributes for filtering on Space: AttributeFilterID, AttributeHash, AttributeIDs, CreatedAt, DeleteGates, DisplayName, Labels, OrganizationID, Permissions, Slug, SpaceID, TriggerFilterID, TriggerHash, TriggerIDs, UpdatedAt.

          Where expression to select destination spaces for cloning invocations

          The whole string must be query-encoded.
        in: query
        name: where_space
        required: false
        schema:
          type: string
      - description: |-
          UUID of a Filter entity to apply to the Space list.

          The Filter must be in the same Organization as the user credentials.

          The Filter's From field must match the entity type being filtered (Space).

          For Space-resident entities, if the Filter has a FromSpaceID, it must match the operation's SpaceID.

          The Filter's Where clause will be combined with any explicit 'where' parameter using AND logic.

          If both 'filter' and 'where' parameters are specified, they are combined with AND logic.
        in: query
        name: filter_space
        required: false
        schema:
          type: string
      - description: Allowed values are true and false. Default is false. When true,
          reports success when an entity already exists and returns the existing entity
        in: query
        name: allow_exists
        required: false
        schema:
          type: string
      requestBody:
        content:
          application/merge-patch+json:
            schema:
              description: JSON Merge Patch schema for Invocation. All fields are
                optional and nullable. Readonly fields are ignored.
              properties:
                Annotations:
                  additionalProperties:
                    nullable: true
                    type: string
                  description: An optional map of Annotation key/value pairs for tools
                    to attach information to entities.
                  nullable: true
                  type: object
                Arguments:
                  description: Function arguments
                  items:
                    nullable: true
                    type: object
                  nullable: true
                  type: array
                BridgeWorkerID:
                  description: ""
                  format: uuid
                  nullable: true
                  type: string
                DeleteGates:
                  additionalProperties:
                    nullable: true
                    type: boolean
                  description: An optional set of gates that, if any is present, will
                    block deletion
                  nullable: true
                  type: object
                DisplayName:
                  description: Friendly name for the entity.
                  nullable: true
                  type: string
                FunctionName:
                  description: Function name
                  nullable: true
                  type: string
                Labels:
                  additionalProperties:
                    nullable: true
                    type: string
                  description: An optional map of Label key/value pairs to specify
                    identifying attributes of entities for the purpose of grouping
                    and filtering them.
                  nullable: true
                  type: object
                Slug:
                  description: Unique URL-safe identifier for the entity.
                  nullable: true
                  type: string
                ToolchainType:
                  description: ""
                  nullable: true
                  type: string
                Version:
                  description: An entity-specific sequence number used for optimistic
                    concurrency control. The value read must be sent in calls to Update.
                  nullable: true
                  type: integer
              type: object
      responses:
        "200":
          content:
            application/json:
              schema:
                items:
                  $ref: '#/components/schemas/InvocationCreateOrUpdateResponse'
                type: array
          description: OK
        "207":
          content:
            application/json:
              schema:
                items:
                  $ref: '#/components/schemas/InvocationCreateOrUpdateResponse'
                type: array
          description: Multi-Status (partial success)
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Invocation request is invalid (Bad Request).
        "401":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Unauthorized access.
        "403":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Forbidden access.
        "404":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Invocation not found.
        "409":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Invocation data conflict. Data has changed since last read.
        "500":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Something went wrong while processing Invocation.
        default:
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Unexpected error.
      summary: Bulk create (clone) multiple invocations
      tags:
      - Invocation
  /link:
    delete:
      description: Deletes multiple Links based on search criteria
      operationId: BulkDeleteLinks
      parameters:
      - description: |-
          The specified string is an expression for the purpose of filtering
          the list of Links returned. The expression syntax was inspired by SQL.
          It supports conjunctions using `AND` of relational expressions of the form *attribute*
          *operator* *attribute_or_literal*. The attribute names are case-sensitive and PascalCase,
          as in the JSON encoding.
          Strings support the following operators: `<`, `>`, `<=`, `>=`, `=`, `!=`, `LIKE`, `NOT LIKE`, `ILIKE`, `~~`, `!~~`, `~`, `~*`, `!~`, `!~*`, `IN`, `NOT IN`.
          String pattern operators: `LIKE` and `~~` for pattern matching with `%` and `_` wildcards,
          `ILIKE` for case-insensitive pattern matching, `NOT LIKE` and `!~~` for negated pattern matching.
          String regex operators: `~` for regex matching, `~*` for case-insensitive regex,
          `!~` and `!~*` for regex not matching (case-sensitive and insensitive).
          Integers support the following operators: `<`, `>`, `<=`, `>=`, `=`, `!=`, `IN`, `NOT IN`.
          UUIDs and boolean attributes support equality and inequality only.
          UUID and time literals must be quoted as string literals.
          String literals are quoted with single quotes, such as `'string'`.
          Time literals use the same form as when serialized as JSON,
          such as: `CreatedAt > '2025-02-18T23:16:34'`.
          Integer and boolean literals are also supported for attributes of those types.
          Arrays support the `?` operator to to match any element of the array,
          as in `ApprovedBy ? '7c61626f-ddbe-41af-93f6-b69f4ab6d308'`.
          Arrays can perform LEN() to check for length, as in `LEN(ApprovedBy) > 0`.
          Map support the dot notation to specify a particular map key, as in `Labels.tier = 'Backend'`.
          Maps support `IS NULL` and `IS NOT NULL` with dot notation to check for key absence or presence,
          as in `Labels.tier IS NULL` (key doesn't exist) or `Labels.tier IS NOT NULL` (key exists).
          Comparison results can be tested with `IS TRUE`, `IS FALSE`, `IS NOT TRUE`, and `IS NOT FALSE`.
          These are useful for nullable columns: `MergeSourceID = '<uuid>' IS NOT FALSE` matches rows where MergeSourceID equals the value OR is NULL.
          The `IN` and `NOT IN` operators accept a comma-separated list of values in parentheses,
          such as `Slug IN ('slugone', 'slugtwo')` or `Labels.environment IN ('prod', 'staging')`.
          Conjunctions are supported using the `AND` operator.
          An example conjunction is:
          `CreatedAt >= '2025-01-07' AND Slug = 'test' AND Labels.mykey = 'myvalue'`.

          Supported attributes for filtering on Link: AutoUpdate, CreatedAt, DeleteGates, DisplayName, DownstreamLastMergedRevisionNum, FromUnitID, Labels, LinkID, OrganizationID, Slug, SpaceID, ToSpaceID, ToUnitID, UpdateType, UpdatedAt, UpstreamLastMergedRevisionNum, UpstreamLinkID, UpstreamOrganizationID, UpstreamSpaceID, UseLiveState.

          filter

          The whole string must be query-encoded.
        in: query
        name: where
        required: false
        schema:
          type: string
      - description: |-
          UUID of a Filter entity to apply to the Link list.

          The Filter must be in the same Organization as the user credentials.

          The Filter's From field must match the entity type being filtered (Link).

          For Space-resident entities, if the Filter has a FromSpaceID, it must match the operation's SpaceID.

          The Filter's Where clause will be combined with any explicit 'where' parameter using AND logic.

          If both 'filter' and 'where' parameters are specified, they are combined with AND logic.
        in: query
        name: filter
        required: false
        schema:
          type: string
      - description: |-
          Free text search that approximately matches the specified string against string fields and map keys/values.

          The search is case-insensitive and uses pattern matching to find entities containing the text.

          Searchable string fields include attributes like Slug, DisplayName, and string-typed custom fields.

          For map fields (like Labels and Annotations), the search matches both map keys and values.

          The search uses OR logic across all searchable fields, so matching any field will return the entity.

          If both 'where' and 'contains' parameters are specified, they are combined with AND logic.

          Searchable fields for Link include string and map-type attributes from the queryable attributes list.

          The whole string must be query-encoded.
        in: query
        name: contains
        required: false
        schema:
          type: string
      - description: |-
          Include clause for expanding related entities in the response for Link.
          The attribute names are case-sensitive, PascalCase, and
          expected in a comma-separated list format as in the JSON encoding.

          Supported attributes for Link are FromUnitID, OrganizationID, SpaceID, ToSpaceID, ToUnitID.

          The whole string must be query-encoded.
        in: query
        name: include
        required: false
        schema:
          type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                items:
                  $ref: '#/components/schemas/DeleteResponse'
                type: array
          description: OK
        "207":
          content:
            application/json:
              schema:
                items:
                  $ref: '#/components/schemas/DeleteResponse'
                type: array
          description: 'Multi-Status: Mixed success and failure results'
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Link request is invalid (Bad Request).
        "401":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Unauthorized access.
        "403":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Forbidden access.
        "404":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Link not found.
        "409":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Link data conflict. Data has changed since last read.
        "422":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Unable to delete entity
        "500":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Something went wrong while processing Link.
        default:
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Unexpected error.
      summary: Delete multiple Links
      tags:
      - Link
    get:
      description: Retrieves a list of Links across spaces in the Organization
      operationId: SearchListLinks
      parameters:
      - description: |-
          The specified string is an expression for the purpose of filtering
          the list of Links returned. The expression syntax was inspired by SQL.
          It supports conjunctions using `AND` of relational expressions of the form *attribute*
          *operator* *attribute_or_literal*. The attribute names are case-sensitive and PascalCase,
          as in the JSON encoding.
          Strings support the following operators: `<`, `>`, `<=`, `>=`, `=`, `!=`, `LIKE`, `NOT LIKE`, `ILIKE`, `~~`, `!~~`, `~`, `~*`, `!~`, `!~*`, `IN`, `NOT IN`.
          String pattern operators: `LIKE` and `~~` for pattern matching with `%` and `_` wildcards,
          `ILIKE` for case-insensitive pattern matching, `NOT LIKE` and `!~~` for negated pattern matching.
          String regex operators: `~` for regex matching, `~*` for case-insensitive regex,
          `!~` and `!~*` for regex not matching (case-sensitive and insensitive).
          Integers support the following operators: `<`, `>`, `<=`, `>=`, `=`, `!=`, `IN`, `NOT IN`.
          UUIDs and boolean attributes support equality and inequality only.
          UUID and time literals must be quoted as string literals.
          String literals are quoted with single quotes, such as `'string'`.
          Time literals use the same form as when serialized as JSON,
          such as: `CreatedAt > '2025-02-18T23:16:34'`.
          Integer and boolean literals are also supported for attributes of those types.
          Arrays support the `?` operator to to match any element of the array,
          as in `ApprovedBy ? '7c61626f-ddbe-41af-93f6-b69f4ab6d308'`.
          Arrays can perform LEN() to check for length, as in `LEN(ApprovedBy) > 0`.
          Map support the dot notation to specify a particular map key, as in `Labels.tier = 'Backend'`.
          Maps support `IS NULL` and `IS NOT NULL` with dot notation to check for key absence or presence,
          as in `Labels.tier IS NULL` (key doesn't exist) or `Labels.tier IS NOT NULL` (key exists).
          Comparison results can be tested with `IS TRUE`, `IS FALSE`, `IS NOT TRUE`, and `IS NOT FALSE`.
          These are useful for nullable columns: `MergeSourceID = '<uuid>' IS NOT FALSE` matches rows where MergeSourceID equals the value OR is NULL.
          The `IN` and `NOT IN` operators accept a comma-separated list of values in parentheses,
          such as `Slug IN ('slugone', 'slugtwo')` or `Labels.environment IN ('prod', 'staging')`.
          Conjunctions are supported using the `AND` operator.
          An example conjunction is:
          `CreatedAt >= '2025-01-07' AND Slug = 'test' AND Labels.mykey = 'myvalue'`.

          Supported attributes for filtering on Link: AutoUpdate, CreatedAt, DeleteGates, DisplayName, DownstreamLastMergedRevisionNum, FromUnitID, Labels, LinkID, OrganizationID, Slug, SpaceID, ToSpaceID, ToUnitID, UpdateType, UpdatedAt, UpstreamLastMergedRevisionNum, UpstreamLinkID, UpstreamOrganizationID, UpstreamSpaceID, UseLiveState.

          The whole string must be query-encoded.
        in: query
        name: where
        required: false
        schema:
          type: string
      - description: |-
          UUID of a Filter entity to apply to the Link list.

          The Filter must be in the same Organization as the user credentials.

          The Filter's From field must match the entity type being filtered (Link).

          For Space-resident entities, if the Filter has a FromSpaceID, it must match the operation's SpaceID.

          The Filter's Where clause will be combined with any explicit 'where' parameter using AND logic.

          If both 'filter' and 'where' parameters are specified, they are combined with AND logic.
        in: query
        name: filter
        required: false
        schema:
          type: string
      - description: |-
          Free text search that approximately matches the specified string against string fields and map keys/values.

          The search is case-insensitive and uses pattern matching to find entities containing the text.

          Searchable string fields include attributes like Slug, DisplayName, and string-typed custom fields.

          For map fields (like Labels and Annotations), the search matches both map keys and values.

          The search uses OR logic across all searchable fields, so matching any field will return the entity.

          If both 'where' and 'contains' parameters are specified, they are combined with AND logic.

          Searchable fields for Link include string and map-type attributes from the queryable attributes list.

          The whole string must be query-encoded.
        in: query
        name: contains
        required: false
        schema:
          type: string
      - description: |-
          Include clause for expanding related entities in the response for Link.
          The attribute names are case-sensitive, PascalCase, and
          expected in a comma-separated list format as in the JSON encoding.

          Supported attributes for Link are FromUnitID, OrganizationID, SpaceID, ToSpaceID, ToUnitID.

          The whole string must be query-encoded.
        in: query
        name: include
        required: false
        schema:
          type: string
      - description: |-
          Select clause for specifying which fields to include in the response for Link.
          The attribute names are case-sensitive, PascalCase, and
          expected in a comma-separated list format as in the JSON encoding.
          If not specified, all fields are returned.
          Entity and parent IDs (like OrganizationID, SpaceID, LinkID) and Slug are always returned regardless of the select parameter.
          Fields used in where and contains filters are also automatically included.
          Example: 'DisplayName,CreatedAt,Labels' will return only those fields plus the required ID and Slug fields.
          The whole string must be query-encoded.
        in: query
        name: select
        required: false
        schema:
          type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                items:
                  $ref: '#/components/schemas/ExtendedLink'
                type: array
          description: OK
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Link request is invalid (Bad Request).
        "401":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Unauthorized access.
        "403":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Forbidden access.
        "404":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Link not found.
        "500":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Something went wrong while processing Link.
        default:
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Unexpected error.
      summary: List of Links across spaces
      tags:
      - Link
    patch:
      description: Updates multiple Links based on search criteria
      operationId: BulkPatchLinks
      parameters:
      - description: |-
          The specified string is an expression for the purpose of filtering
          the list of Links returned. The expression syntax was inspired by SQL.
          It supports conjunctions using `AND` of relational expressions of the form *attribute*
          *operator* *attribute_or_literal*. The attribute names are case-sensitive and PascalCase,
          as in the JSON encoding.
          Strings support the following operators: `<`, `>`, `<=`, `>=`, `=`, `!=`, `LIKE`, `NOT LIKE`, `ILIKE`, `~~`, `!~~`, `~`, `~*`, `!~`, `!~*`, `IN`, `NOT IN`.
          String pattern operators: `LIKE` and `~~` for pattern matching with `%` and `_` wildcards,
          `ILIKE` for case-insensitive pattern matching, `NOT LIKE` and `!~~` for negated pattern matching.
          String regex operators: `~` for regex matching, `~*` for case-insensitive regex,
          `!~` and `!~*` for regex not matching (case-sensitive and insensitive).
          Integers support the following operators: `<`, `>`, `<=`, `>=`, `=`, `!=`, `IN`, `NOT IN`.
          UUIDs and boolean attributes support equality and inequality only.
          UUID and time literals must be quoted as string literals.
          String literals are quoted with single quotes, such as `'string'`.
          Time literals use the same form as when serialized as JSON,
          such as: `CreatedAt > '2025-02-18T23:16:34'`.
          Integer and boolean literals are also supported for attributes of those types.
          Arrays support the `?` operator to to match any element of the array,
          as in `ApprovedBy ? '7c61626f-ddbe-41af-93f6-b69f4ab6d308'`.
          Arrays can perform LEN() to check for length, as in `LEN(ApprovedBy) > 0`.
          Map support the dot notation to specify a particular map key, as in `Labels.tier = 'Backend'`.
          Maps support `IS NULL` and `IS NOT NULL` with dot notation to check for key absence or presence,
          as in `Labels.tier IS NULL` (key doesn't exist) or `Labels.tier IS NOT NULL` (key exists).
          Comparison results can be tested with `IS TRUE`, `IS FALSE`, `IS NOT TRUE`, and `IS NOT FALSE`.
          These are useful for nullable columns: `MergeSourceID = '<uuid>' IS NOT FALSE` matches rows where MergeSourceID equals the value OR is NULL.
          The `IN` and `NOT IN` operators accept a comma-separated list of values in parentheses,
          such as `Slug IN ('slugone', 'slugtwo')` or `Labels.environment IN ('prod', 'staging')`.
          Conjunctions are supported using the `AND` operator.
          An example conjunction is:
          `CreatedAt >= '2025-01-07' AND Slug = 'test' AND Labels.mykey = 'myvalue'`.

          Supported attributes for filtering on Link: AutoUpdate, CreatedAt, DeleteGates, DisplayName, DownstreamLastMergedRevisionNum, FromUnitID, Labels, LinkID, OrganizationID, Slug, SpaceID, ToSpaceID, ToUnitID, UpdateType, UpdatedAt, UpstreamLastMergedRevisionNum, UpstreamLinkID, UpstreamOrganizationID, UpstreamSpaceID, UseLiveState.

          filter

          The whole string must be query-encoded.
        in: query
        name: where
        required: false
        schema:
          type: string
      - description: |-
          UUID of a Filter entity to apply to the Link list.

          The Filter must be in the same Organization as the user credentials.

          The Filter's From field must match the entity type being filtered (Link).

          For Space-resident entities, if the Filter has a FromSpaceID, it must match the operation's SpaceID.

          The Filter's Where clause will be combined with any explicit 'where' parameter using AND logic.

          If both 'filter' and 'where' parameters are specified, they are combined with AND logic.
        in: query
        name: filter
        required: false
        schema:
          type: string
      - description: |-
          Free text search that approximately matches the specified string against string fields and map keys/values.

          The search is case-insensitive and uses pattern matching to find entities containing the text.

          Searchable string fields include attributes like Slug, DisplayName, and string-typed custom fields.

          For map fields (like Labels and Annotations), the search matches both map keys and values.

          The search uses OR logic across all searchable fields, so matching any field will return the entity.

          If both 'where' and 'contains' parameters are specified, they are combined with AND logic.

          Searchable fields for Link include string and map-type attributes from the queryable attributes list.

          The whole string must be query-encoded.
        in: query
        name: contains
        required: false
        schema:
          type: string
      - description: |-
          Include clause for expanding related entities in the response for Link.
          The attribute names are case-sensitive, PascalCase, and
          expected in a comma-separated list format as in the JSON encoding.

          Supported attributes for Link are FromUnitID, OrganizationID, SpaceID, ToSpaceID, ToUnitID.

          The whole string must be query-encoded.
        in: query
        name: include
        required: false
        schema:
          type: string
      - description: Swap the FromUnit and ToUnit directions of the links
        in: query
        name: reverse
        required: false
        schema:
          type: boolean
      requestBody:
        content:
          application/merge-patch+json:
            schema:
              description: JSON Merge Patch schema for Link. All fields are optional
                and nullable. Readonly fields are ignored.
              properties:
                Annotations:
                  additionalProperties:
                    nullable: true
                    type: string
                  description: An optional map of Annotation key/value pairs for tools
                    to attach information to entities.
                  nullable: true
                  type: object
                AutoUpdate:
                  description: ""
                  nullable: true
                  type: boolean
                Bindings:
                  description: ""
                  items:
                    nullable: true
                    type: object
                  nullable: true
                  type: array
                DeleteGates:
                  additionalProperties:
                    nullable: true
                    type: boolean
                  description: An optional set of gates that, if any is present, will
                    block deletion
                  nullable: true
                  type: object
                DisplayName:
                  description: Friendly name for the entity.
                  nullable: true
                  type: string
                DownstreamLastMergedRevisionNum:
                  description: ""
                  nullable: true
                  type: integer
                FromUnitID:
                  description: ""
                  format: uuid
                  nullable: true
                  type: string
                Labels:
                  additionalProperties:
                    nullable: true
                    type: string
                  description: An optional map of Label key/value pairs to specify
                    identifying attributes of entities for the purpose of grouping
                    and filtering them.
                  nullable: true
                  type: object
                Slug:
                  description: Unique URL-safe identifier for the entity.
                  nullable: true
                  type: string
                ToSpaceID:
                  description: ""
                  format: uuid
                  nullable: true
                  type: string
                ToUnitID:
                  description: ""
                  format: uuid
                  nullable: true
                  type: string
                UpdateType:
                  description: ""
                  nullable: true
                  type: string
                UpstreamLastMergedRevisionNum:
                  description: ""
                  nullable: true
                  type: integer
                UseLiveState:
                  description: ""
                  nullable: true
                  type: boolean
                Version:
                  description: An entity-specific sequence number used for optimistic
                    concurrency control. The value read must be sent in calls to Update.
                  nullable: true
                  type: integer
                WhereMutation:
                  description: ""
                  nullable: true
                  type: string
                WhereResource:
                  description: ""
                  nullable: true
                  type: string
              type: object
      responses:
        "200":
          content:
            application/json:
              schema:
                items:
                  $ref: '#/components/schemas/LinkCreateOrUpdateResponse'
                type: array
          description: OK
        "207":
          content:
            application/json:
              schema:
                items:
                  $ref: '#/components/schemas/LinkCreateOrUpdateResponse'
                type: array
          description: 'Multi-Status: Mixed success and failure results'
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Link request is invalid (Bad Request).
        "401":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Unauthorized access.
        "403":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Forbidden access.
        "404":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Link not found.
        "409":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Link data conflict. Data has changed since last read.
        "500":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Something went wrong while processing Link.
        default:
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Unexpected error.
      summary: Update multiple Links
      tags:
      - Link
    post:
      description: Copy existing links with retargeted From and/or To units. Select
        source links via where/filter, then retarget using reverse, from_downstream_where,
        or to_downstream_where parameters.
      operationId: BulkCreateLinks
      parameters:
      - description: |-
          The specified string is an expression for the purpose of filtering
          the list of Links returned. The expression syntax was inspired by SQL.
          It supports conjunctions using `AND` of relational expressions of the form *attribute*
          *operator* *attribute_or_literal*. The attribute names are case-sensitive and PascalCase,
          as in the JSON encoding.
          Strings support the following operators: `<`, `>`, `<=`, `>=`, `=`, `!=`, `LIKE`, `NOT LIKE`, `ILIKE`, `~~`, `!~~`, `~`, `~*`, `!~`, `!~*`, `IN`, `NOT IN`.
          String pattern operators: `LIKE` and `~~` for pattern matching with `%` and `_` wildcards,
          `ILIKE` for case-insensitive pattern matching, `NOT LIKE` and `!~~` for negated pattern matching.
          String regex operators: `~` for regex matching, `~*` for case-insensitive regex,
          `!~` and `!~*` for regex not matching (case-sensitive and insensitive).
          Integers support the following operators: `<`, `>`, `<=`, `>=`, `=`, `!=`, `IN`, `NOT IN`.
          UUIDs and boolean attributes support equality and inequality only.
          UUID and time literals must be quoted as string literals.
          String literals are quoted with single quotes, such as `'string'`.
          Time literals use the same form as when serialized as JSON,
          such as: `CreatedAt > '2025-02-18T23:16:34'`.
          Integer and boolean literals are also supported for attributes of those types.
          Arrays support the `?` operator to to match any element of the array,
          as in `ApprovedBy ? '7c61626f-ddbe-41af-93f6-b69f4ab6d308'`.
          Arrays can perform LEN() to check for length, as in `LEN(ApprovedBy) > 0`.
          Map support the dot notation to specify a particular map key, as in `Labels.tier = 'Backend'`.
          Maps support `IS NULL` and `IS NOT NULL` with dot notation to check for key absence or presence,
          as in `Labels.tier IS NULL` (key doesn't exist) or `Labels.tier IS NOT NULL` (key exists).
          Comparison results can be tested with `IS TRUE`, `IS FALSE`, `IS NOT TRUE`, and `IS NOT FALSE`.
          These are useful for nullable columns: `MergeSourceID = '<uuid>' IS NOT FALSE` matches rows where MergeSourceID equals the value OR is NULL.
          The `IN` and `NOT IN` operators accept a comma-separated list of values in parentheses,
          such as `Slug IN ('slugone', 'slugtwo')` or `Labels.environment IN ('prod', 'staging')`.
          Conjunctions are supported using the `AND` operator.
          An example conjunction is:
          `CreatedAt >= '2025-01-07' AND Slug = 'test' AND Labels.mykey = 'myvalue'`.

          Supported attributes for filtering on Link: AutoUpdate, CreatedAt, DeleteGates, DisplayName, DownstreamLastMergedRevisionNum, FromUnitID, Labels, LinkID, OrganizationID, Slug, SpaceID, ToSpaceID, ToUnitID, UpdateType, UpdatedAt, UpstreamLastMergedRevisionNum, UpstreamLinkID, UpstreamOrganizationID, UpstreamSpaceID, UseLiveState.

          Where expression to select source links to copy

          The whole string must be query-encoded.
        in: query
        name: where
        required: false
        schema:
          type: string
      - description: |-
          UUID of a Filter entity to apply to the Link list.

          The Filter must be in the same Organization as the user credentials.

          The Filter's From field must match the entity type being filtered (Link).

          For Space-resident entities, if the Filter has a FromSpaceID, it must match the operation's SpaceID.

          The Filter's Where clause will be combined with any explicit 'where' parameter using AND logic.

          If both 'filter' and 'where' parameters are specified, they are combined with AND logic.
        in: query
        name: filter
        required: false
        schema:
          type: string
      - description: Swap the FromUnit and ToUnit directions of the copied links (for
          cross-space link reversal)
        in: query
        name: reverse
        required: false
        schema:
          type: boolean
      - description: |-
          The specified string is an expression for the purpose of filtering
          the list of Links returned. The expression syntax was inspired by SQL.
          It supports conjunctions using `AND` of relational expressions of the form *attribute*
          *operator* *attribute_or_literal*. The attribute names are case-sensitive and PascalCase,
          as in the JSON encoding.
          Strings support the following operators: `<`, `>`, `<=`, `>=`, `=`, `!=`, `LIKE`, `NOT LIKE`, `ILIKE`, `~~`, `!~~`, `~`, `~*`, `!~`, `!~*`, `IN`, `NOT IN`.
          String pattern operators: `LIKE` and `~~` for pattern matching with `%` and `_` wildcards,
          `ILIKE` for case-insensitive pattern matching, `NOT LIKE` and `!~~` for negated pattern matching.
          String regex operators: `~` for regex matching, `~*` for case-insensitive regex,
          `!~` and `!~*` for regex not matching (case-sensitive and insensitive).
          Integers support the following operators: `<`, `>`, `<=`, `>=`, `=`, `!=`, `IN`, `NOT IN`.
          UUIDs and boolean attributes support equality and inequality only.
          UUID and time literals must be quoted as string literals.
          String literals are quoted with single quotes, such as `'string'`.
          Time literals use the same form as when serialized as JSON,
          such as: `CreatedAt > '2025-02-18T23:16:34'`.
          Integer and boolean literals are also supported for attributes of those types.
          Arrays support the `?` operator to to match any element of the array,
          as in `ApprovedBy ? '7c61626f-ddbe-41af-93f6-b69f4ab6d308'`.
          Arrays can perform LEN() to check for length, as in `LEN(ApprovedBy) > 0`.
          Map support the dot notation to specify a particular map key, as in `Labels.tier = 'Backend'`.
          Maps support `IS NULL` and `IS NOT NULL` with dot notation to check for key absence or presence,
          as in `Labels.tier IS NULL` (key doesn't exist) or `Labels.tier IS NOT NULL` (key exists).
          Comparison results can be tested with `IS TRUE`, `IS FALSE`, `IS NOT TRUE`, and `IS NOT FALSE`.
          These are useful for nullable columns: `MergeSourceID = '<uuid>' IS NOT FALSE` matches rows where MergeSourceID equals the value OR is NULL.
          The `IN` and `NOT IN` operators accept a comma-separated list of values in parentheses,
          such as `Slug IN ('slugone', 'slugtwo')` or `Labels.environment IN ('prod', 'staging')`.
          Conjunctions are supported using the `AND` operator.
          An example conjunction is:
          `CreatedAt >= '2025-01-07' AND Slug = 'test' AND Labels.mykey = 'myvalue'`.

          Supported attributes for filtering on Link: AutoUpdate, CreatedAt, DeleteGates, DisplayName, DownstreamLastMergedRevisionNum, FromUnitID, Labels, LinkID, OrganizationID, Slug, SpaceID, ToSpaceID, ToUnitID, UpdateType, UpdatedAt, UpstreamLastMergedRevisionNum, UpstreamLinkID, UpstreamOrganizationID, UpstreamSpaceID, UseLiveState.

          Where expression to find downstream UpgradeUnit links from each source link's FromUnit. Creates one copy per match. Required if reverse is not specified.

          The whole string must be query-encoded.
        in: query
        name: from_downstream_where
        required: false
        schema:
          type: string
      - description: |-
          The specified string is an expression for the purpose of filtering
          the list of Links returned. The expression syntax was inspired by SQL.
          It supports conjunctions using `AND` of relational expressions of the form *attribute*
          *operator* *attribute_or_literal*. The attribute names are case-sensitive and PascalCase,
          as in the JSON encoding.
          Strings support the following operators: `<`, `>`, `<=`, `>=`, `=`, `!=`, `LIKE`, `NOT LIKE`, `ILIKE`, `~~`, `!~~`, `~`, `~*`, `!~`, `!~*`, `IN`, `NOT IN`.
          String pattern operators: `LIKE` and `~~` for pattern matching with `%` and `_` wildcards,
          `ILIKE` for case-insensitive pattern matching, `NOT LIKE` and `!~~` for negated pattern matching.
          String regex operators: `~` for regex matching, `~*` for case-insensitive regex,
          `!~` and `!~*` for regex not matching (case-sensitive and insensitive).
          Integers support the following operators: `<`, `>`, `<=`, `>=`, `=`, `!=`, `IN`, `NOT IN`.
          UUIDs and boolean attributes support equality and inequality only.
          UUID and time literals must be quoted as string literals.
          String literals are quoted with single quotes, such as `'string'`.
          Time literals use the same form as when serialized as JSON,
          such as: `CreatedAt > '2025-02-18T23:16:34'`.
          Integer and boolean literals are also supported for attributes of those types.
          Arrays support the `?` operator to to match any element of the array,
          as in `ApprovedBy ? '7c61626f-ddbe-41af-93f6-b69f4ab6d308'`.
          Arrays can perform LEN() to check for length, as in `LEN(ApprovedBy) > 0`.
          Map support the dot notation to specify a particular map key, as in `Labels.tier = 'Backend'`.
          Maps support `IS NULL` and `IS NOT NULL` with dot notation to check for key absence or presence,
          as in `Labels.tier IS NULL` (key doesn't exist) or `Labels.tier IS NOT NULL` (key exists).
          Comparison results can be tested with `IS TRUE`, `IS FALSE`, `IS NOT TRUE`, and `IS NOT FALSE`.
          These are useful for nullable columns: `MergeSourceID = '<uuid>' IS NOT FALSE` matches rows where MergeSourceID equals the value OR is NULL.
          The `IN` and `NOT IN` operators accept a comma-separated list of values in parentheses,
          such as `Slug IN ('slugone', 'slugtwo')` or `Labels.environment IN ('prod', 'staging')`.
          Conjunctions are supported using the `AND` operator.
          An example conjunction is:
          `CreatedAt >= '2025-01-07' AND Slug = 'test' AND Labels.mykey = 'myvalue'`.

          Supported attributes for filtering on Link: AutoUpdate, CreatedAt, DeleteGates, DisplayName, DownstreamLastMergedRevisionNum, FromUnitID, Labels, LinkID, OrganizationID, Slug, SpaceID, ToSpaceID, ToUnitID, UpdateType, UpdatedAt, UpstreamLastMergedRevisionNum, UpstreamLinkID, UpstreamOrganizationID, UpstreamSpaceID, UseLiveState.

          Where expression to find downstream UpgradeUnit link from each source link's ToUnit. Exactly one match required. If omitted, ToUnitID/ToSpaceID are unchanged.

          The whole string must be query-encoded.
        in: query
        name: to_downstream_where
        required: false
        schema:
          type: string
      - description: Allowed values are true and false. Default is false. When true,
          reports success when an entity already exists and returns the existing entity
        in: query
        name: allow_exists
        required: false
        schema:
          type: string
      requestBody:
        content:
          application/merge-patch+json:
            schema:
              description: JSON Merge Patch schema for Link. All fields are optional
                and nullable. Readonly fields are ignored.
              properties:
                Annotations:
                  additionalProperties:
                    nullable: true
                    type: string
                  description: An optional map of Annotation key/value pairs for tools
                    to attach information to entities.
                  nullable: true
                  type: object
                AutoUpdate:
                  description: ""
                  nullable: true
                  type: boolean
                Bindings:
                  description: ""
                  items:
                    nullable: true
                    type: object
                  nullable: true
                  type: array
                DeleteGates:
                  additionalProperties:
                    nullable: true
                    type: boolean
                  description: An optional set of gates that, if any is present, will
                    block deletion
                  nullable: true
                  type: object
                DisplayName:
                  description: Friendly name for the entity.
                  nullable: true
                  type: string
                DownstreamLastMergedRevisionNum:
                  description: ""
                  nullable: true
                  type: integer
                FromUnitID:
                  description: ""
                  format: uuid
                  nullable: true
                  type: string
                Labels:
                  additionalProperties:
                    nullable: true
                    type: string
                  description: An optional map of Label key/value pairs to specify
                    identifying attributes of entities for the purpose of grouping
                    and filtering them.
                  nullable: true
                  type: object
                Slug:
                  description: Unique URL-safe identifier for the entity.
                  nullable: true
                  type: string
                ToSpaceID:
                  description: ""
                  format: uuid
                  nullable: true
                  type: string
                ToUnitID:
                  description: ""
                  format: uuid
                  nullable: true
                  type: string
                UpdateType:
                  description: ""
                  nullable: true
                  type: string
                UpstreamLastMergedRevisionNum:
                  description: ""
                  nullable: true
                  type: integer
                UseLiveState:
                  description: ""
                  nullable: true
                  type: boolean
                Version:
                  description: An entity-specific sequence number used for optimistic
                    concurrency control. The value read must be sent in calls to Update.
                  nullable: true
                  type: integer
                WhereMutation:
                  description: ""
                  nullable: true
                  type: string
                WhereResource:
                  description: ""
                  nullable: true
                  type: string
              type: object
      responses:
        "200":
          content:
            application/json:
              schema:
                items:
                  $ref: '#/components/schemas/LinkCreateOrUpdateResponse'
                type: array
          description: OK
        "207":
          content:
            application/json:
              schema:
                items:
                  $ref: '#/components/schemas/LinkCreateOrUpdateResponse'
                type: array
          description: Multi-Status (partial success)
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Link request is invalid (Bad Request).
        "401":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Unauthorized access.
        "403":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Forbidden access.
        "404":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Link not found.
        "409":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Link data conflict. Data has changed since last read.
        "500":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Something went wrong while processing Link.
        default:
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Unexpected error.
      summary: Bulk create (copy) multiple links
      tags:
      - Link
  /me:
    get:
      description: Get the current user's information
      operationId: GetMe
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/OrganizationMember'
          description: a User given membership on the Organization
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: UserInfo request is invalid (Bad Request).
        "401":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Unauthorized access.
        "403":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Forbidden access.
        "404":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: UserInfo not found.
        "500":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Something went wrong while processing UserInfo.
        default:
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Unexpected error.
      summary: Get the current user's information
      tags:
      - UserInfo
  /organization:
    get:
      description: List Organizations
      operationId: ListOrganizations
      parameters:
      - description: |-
          The specified string is an expression for the purpose of filtering
          the list of Organizations returned. The expression syntax was inspired by SQL.
          It supports conjunctions using `AND` of relational expressions of the form *attribute*
          *operator* *attribute_or_literal*. The attribute names are case-sensitive and PascalCase,
          as in the JSON encoding.
          Strings support the following operators: `<`, `>`, `<=`, `>=`, `=`, `!=`, `LIKE`, `NOT LIKE`, `ILIKE`, `~~`, `!~~`, `~`, `~*`, `!~`, `!~*`, `IN`, `NOT IN`.
          String pattern operators: `LIKE` and `~~` for pattern matching with `%` and `_` wildcards,
          `ILIKE` for case-insensitive pattern matching, `NOT LIKE` and `!~~` for negated pattern matching.
          String regex operators: `~` for regex matching, `~*` for case-insensitive regex,
          `!~` and `!~*` for regex not matching (case-sensitive and insensitive).
          Integers support the following operators: `<`, `>`, `<=`, `>=`, `=`, `!=`, `IN`, `NOT IN`.
          UUIDs and boolean attributes support equality and inequality only.
          UUID and time literals must be quoted as string literals.
          String literals are quoted with single quotes, such as `'string'`.
          Time literals use the same form as when serialized as JSON,
          such as: `CreatedAt > '2025-02-18T23:16:34'`.
          Integer and boolean literals are also supported for attributes of those types.
          Arrays support the `?` operator to to match any element of the array,
          as in `ApprovedBy ? '7c61626f-ddbe-41af-93f6-b69f4ab6d308'`.
          Arrays can perform LEN() to check for length, as in `LEN(ApprovedBy) > 0`.
          Map support the dot notation to specify a particular map key, as in `Labels.tier = 'Backend'`.
          Maps support `IS NULL` and `IS NOT NULL` with dot notation to check for key absence or presence,
          as in `Labels.tier IS NULL` (key doesn't exist) or `Labels.tier IS NOT NULL` (key exists).
          Comparison results can be tested with `IS TRUE`, `IS FALSE`, `IS NOT TRUE`, and `IS NOT FALSE`.
          These are useful for nullable columns: `MergeSourceID = '<uuid>' IS NOT FALSE` matches rows where MergeSourceID equals the value OR is NULL.
          The `IN` and `NOT IN` operators accept a comma-separated list of values in parentheses,
          such as `Slug IN ('slugone', 'slugtwo')` or `Labels.environment IN ('prod', 'staging')`.
          Conjunctions are supported using the `AND` operator.
          An example conjunction is:
          `CreatedAt >= '2025-01-07' AND Slug = 'test' AND Labels.mykey = 'myvalue'`.

          Supported attributes for filtering on Organization: CreatedAt, DeleteGates, DisplayName, EmailDomain, ExternalID, Labels, OrganizationID, Slug, UpdatedAt.

          The whole string must be query-encoded.
        in: query
        name: where
        required: false
        schema:
          type: string
      - description: |-
          UUID of a Filter entity to apply to the Organization list.

          The Filter must be in the same Organization as the user credentials.

          The Filter's From field must match the entity type being filtered (Organization).

          For Space-resident entities, if the Filter has a FromSpaceID, it must match the operation's SpaceID.

          The Filter's Where clause will be combined with any explicit 'where' parameter using AND logic.

          If both 'filter' and 'where' parameters are specified, they are combined with AND logic.
        in: query
        name: filter
        required: false
        schema:
          type: string
      - description: |-
          Free text search that approximately matches the specified string against string fields and map keys/values.

          The search is case-insensitive and uses pattern matching to find entities containing the text.

          Searchable string fields include attributes like Slug, DisplayName, and string-typed custom fields.

          For map fields (like Labels and Annotations), the search matches both map keys and values.

          The search uses OR logic across all searchable fields, so matching any field will return the entity.

          If both 'where' and 'contains' parameters are specified, they are combined with AND logic.

          Searchable fields for Organization include string and map-type attributes from the queryable attributes list.

          The whole string must be query-encoded.
        in: query
        name: contains
        required: false
        schema:
          type: string
      - description: |-
          Include clause for expanding related entities in the response for Organization.
          The attribute names are case-sensitive, PascalCase, and
          expected in a comma-separated list format as in the JSON encoding.

          Supported attributes for Organization are .

          The whole string must be query-encoded.
        in: query
        name: include
        required: false
        schema:
          type: string
      - description: |-
          Select clause for specifying which fields to include in the response for Organization.
          The attribute names are case-sensitive, PascalCase, and
          expected in a comma-separated list format as in the JSON encoding.
          If not specified, all fields are returned.
          Entity and parent IDs (like OrganizationID, SpaceID, OrganizationID) and Slug are always returned regardless of the select parameter.
          Fields used in where and contains filters are also automatically included.
          Example: 'DisplayName,CreatedAt,Labels' will return only those fields plus the required ID and Slug fields.
          The whole string must be query-encoded.
        in: query
        name: select
        required: false
        schema:
          type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                items:
                  $ref: '#/components/schemas/Organization'
                type: array
          description: OK
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Organization request is invalid (Bad Request).
        "401":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Unauthorized access.
        "403":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Forbidden access.
        "404":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Organization not found.
        "500":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Something went wrong while processing Organization.
        default:
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Unexpected error.
      summary: List Organizations
      tags:
      - Organization
    post:
      description: Create Organization
      operationId: CreateOrganization
      parameters:
      - description: Allowed values are true and false. Default is false. When true,
          reports success when an entity already exists and returns the existing entity
        in: query
        name: allow_exists
        required: false
        schema:
          type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/Organization'
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Organization'
          description: The top-level container for an organization using ConfigHub.
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Organization request is invalid (Bad Request).
        "401":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Unauthorized access.
        "403":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Forbidden access.
        "404":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Organization not found.
        "409":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Organization data conflict. Data has changed since last read.
        "500":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Something went wrong while processing Organization.
        default:
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Unexpected error.
      summary: Create Organization
      tags:
      - Organization
  /organization/{organization_id}:
    delete:
      description: Delete Organization
      operationId: DeleteOrganization
      parameters:
      - description: Unique identifier for a organization_id
        in: path
        name: organization_id
        required: true
        schema:
          format: uuid
          type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/DeleteResponse'
          description: Response for successful delete operation
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Organization request is invalid (Bad Request).
        "401":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Unauthorized access.
        "403":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Forbidden access.
        "404":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Organization not found.
        "409":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Organization data conflict. Data has changed since last read.
        "422":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Organization could not be deleted.
        "500":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Something went wrong while processing Organization.
        default:
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Unexpected error.
      summary: Delete Organization
      tags:
      - Organization
    get:
      description: Get Organization
      operationId: GetOrganization
      parameters:
      - description: |-
          Include clause for expanding related entities in the response for Organization.
          The attribute names are case-sensitive, PascalCase, and
          expected in a comma-separated list format as in the JSON encoding.

          Supported attributes for Organization are .

          The whole string must be query-encoded.
        in: query
        name: include
        required: false
        schema:
          type: string
      - description: |-
          Select clause for specifying which fields to include in the response for Organization.
          The attribute names are case-sensitive, PascalCase, and
          expected in a comma-separated list format as in the JSON encoding.
          If not specified, all fields are returned.
          Entity and parent IDs (like OrganizationID, SpaceID, OrganizationID) and Slug are always returned regardless of the select parameter.
          Fields used in where and contains filters are also automatically included.
          Example: 'DisplayName,CreatedAt,Labels' will return only those fields plus the required ID and Slug fields.
          The whole string must be query-encoded.
        in: query
        name: select
        required: false
        schema:
          type: string
      - description: Unique identifier for a organization_id
        in: path
        name: organization_id
        required: true
        schema:
          format: uuid
          type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Organization'
          description: The top-level container for an organization using ConfigHub.
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Organization request is invalid (Bad Request).
        "401":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Unauthorized access.
        "403":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Forbidden access.
        "404":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Organization not found.
        "500":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Something went wrong while processing Organization.
        default:
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Unexpected error.
      summary: Get Organization
      tags:
      - Organization
    put:
      description: Update Organization
      operationId: UpdateOrganization
      parameters:
      - description: Unique identifier for a organization_id
        in: path
        name: organization_id
        required: true
        schema:
          format: uuid
          type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/Organization'
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Organization'
          description: The top-level container for an organization using ConfigHub.
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Organization request is invalid (Bad Request).
        "401":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Unauthorized access.
        "403":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Forbidden access.
        "404":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Organization not found.
        "409":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Organization data conflict. Data has changed since last read.
        "500":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Something went wrong while processing Organization.
        default:
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Unexpected error.
      summary: Update Organization
      tags:
      - Organization
  /organization/{organization_id}/organization_member:
    get:
      description: List OrganizationMembers
      operationId: ListOrganizationMembers
      parameters:
      - description: Unique identifier for a organization_id
        in: path
        name: organization_id
        required: true
        schema:
          format: uuid
          type: string
      - description: |-
          The specified string is an expression for the purpose of filtering
          the list of OrganizationMembers returned. The expression syntax was inspired by SQL.
          It supports conjunctions using `AND` of relational expressions of the form *attribute*
          *operator* *attribute_or_literal*. The attribute names are case-sensitive and PascalCase,
          as in the JSON encoding.
          Strings support the following operators: `<`, `>`, `<=`, `>=`, `=`, `!=`, `LIKE`, `NOT LIKE`, `ILIKE`, `~~`, `!~~`, `~`, `~*`, `!~`, `!~*`, `IN`, `NOT IN`.
          String pattern operators: `LIKE` and `~~` for pattern matching with `%` and `_` wildcards,
          `ILIKE` for case-insensitive pattern matching, `NOT LIKE` and `!~~` for negated pattern matching.
          String regex operators: `~` for regex matching, `~*` for case-insensitive regex,
          `!~` and `!~*` for regex not matching (case-sensitive and insensitive).
          Integers support the following operators: `<`, `>`, `<=`, `>=`, `=`, `!=`, `IN`, `NOT IN`.
          UUIDs and boolean attributes support equality and inequality only.
          UUID and time literals must be quoted as string literals.
          String literals are quoted with single quotes, such as `'string'`.
          Time literals use the same form as when serialized as JSON,
          such as: `CreatedAt > '2025-02-18T23:16:34'`.
          Integer and boolean literals are also supported for attributes of those types.
          Arrays support the `?` operator to to match any element of the array,
          as in `ApprovedBy ? '7c61626f-ddbe-41af-93f6-b69f4ab6d308'`.
          Arrays can perform LEN() to check for length, as in `LEN(ApprovedBy) > 0`.
          Map support the dot notation to specify a particular map key, as in `Labels.tier = 'Backend'`.
          Maps support `IS NULL` and `IS NOT NULL` with dot notation to check for key absence or presence,
          as in `Labels.tier IS NULL` (key doesn't exist) or `Labels.tier IS NOT NULL` (key exists).
          Comparison results can be tested with `IS TRUE`, `IS FALSE`, `IS NOT TRUE`, and `IS NOT FALSE`.
          These are useful for nullable columns: `MergeSourceID = '<uuid>' IS NOT FALSE` matches rows where MergeSourceID equals the value OR is NULL.
          The `IN` and `NOT IN` operators accept a comma-separated list of values in parentheses,
          such as `Slug IN ('slugone', 'slugtwo')` or `Labels.environment IN ('prod', 'staging')`.
          Conjunctions are supported using the `AND` operator.
          An example conjunction is:
          `CreatedAt >= '2025-01-07' AND Slug = 'test' AND Labels.mykey = 'myvalue'`.

          Supported attributes for filtering on OrganizationMember: DisplayName, ExternalID, Slug, UserID, Username.

          The whole string must be query-encoded.
        in: query
        name: where
        required: false
        schema:
          type: string
      - description: |-
          UUID of a Filter entity to apply to the OrganizationMember list.

          The Filter must be in the same Organization as the user credentials.

          The Filter's From field must match the entity type being filtered (OrganizationMember).

          For Space-resident entities, if the Filter has a FromSpaceID, it must match the operation's SpaceID.

          The Filter's Where clause will be combined with any explicit 'where' parameter using AND logic.

          If both 'filter' and 'where' parameters are specified, they are combined with AND logic.
        in: query
        name: filter
        required: false
        schema:
          type: string
      - description: |-
          Free text search that approximately matches the specified string against string fields and map keys/values.

          The search is case-insensitive and uses pattern matching to find entities containing the text.

          Searchable string fields include attributes like Slug, DisplayName, and string-typed custom fields.

          For map fields (like Labels and Annotations), the search matches both map keys and values.

          The search uses OR logic across all searchable fields, so matching any field will return the entity.

          If both 'where' and 'contains' parameters are specified, they are combined with AND logic.

          Searchable fields for OrganizationMember include string and map-type attributes from the queryable attributes list.

          The whole string must be query-encoded.
        in: query
        name: contains
        required: false
        schema:
          type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                items:
                  $ref: '#/components/schemas/OrganizationMember'
                type: array
          description: OK
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: OrganizationMember request is invalid (Bad Request).
        "401":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Unauthorized access.
        "403":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Forbidden access.
        "404":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: OrganizationMember not found.
        "500":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Something went wrong while processing OrganizationMember.
        default:
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Unexpected error.
      summary: List OrganizationMembers
      tags:
      - OrganizationMember
    post:
      description: Create OrganizationMember
      operationId: CreateOrganizationMember
      parameters:
      - description: Unique identifier for a organization_id
        in: path
        name: organization_id
        required: true
        schema:
          format: uuid
          type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/OrganizationMember'
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/OrganizationMember'
          description: a User given membership on the Organization
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: OrganizationMember request is invalid (Bad Request).
        "401":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Unauthorized access.
        "403":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Forbidden access.
        "404":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: OrganizationMember not found.
        "409":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: OrganizationMember data conflict. Data has changed since last
            read.
        "500":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Something went wrong while processing OrganizationMember.
        default:
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Unexpected error.
      summary: Create OrganizationMember
      tags:
      - OrganizationMember
  /organization/{organization_id}/organization_member/{organization_member_id}:
    delete:
      description: Delete OrganizationMember
      operationId: DeleteOrganizationMember
      parameters:
      - description: Unique identifier for a organization_id
        in: path
        name: organization_id
        required: true
        schema:
          format: uuid
          type: string
      - description: Unique identifier for a organization_member_id
        in: path
        name: organization_member_id
        required: true
        schema:
          format: uuid
          type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/DeleteResponse'
          description: Response for successful delete operation
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: OrganizationMember request is invalid (Bad Request).
        "401":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Unauthorized access.
        "403":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Forbidden access.
        "404":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: OrganizationMember not found.
        "409":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: OrganizationMember data conflict. Data has changed since last
            read.
        "422":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: OrganizationMember could not be deleted.
        "500":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Something went wrong while processing OrganizationMember.
        default:
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Unexpected error.
      summary: Delete OrganizationMember
      tags:
      - OrganizationMember
    get:
      description: Get OrganizationMember
      operationId: GetOrganizationMember
      parameters:
      - description: Unique identifier for a organization_id
        in: path
        name: organization_id
        required: true
        schema:
          format: uuid
          type: string
      - description: Unique identifier for a organization_member_id
        in: path
        name: organization_member_id
        required: true
        schema:
          format: uuid
          type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/OrganizationMember'
          description: a User given membership on the Organization
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: OrganizationMember request is invalid (Bad Request).
        "401":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Unauthorized access.
        "403":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Forbidden access.
        "404":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: OrganizationMember not found.
        "500":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Something went wrong while processing OrganizationMember.
        default:
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Unexpected error.
      summary: Get OrganizationMember
      tags:
      - OrganizationMember
  /revision:
    get:
      description: Retrieves a list of Revisions across spaces in the Organization,
        returning at most one revision per unit
      operationId: ListAllRevisions
      parameters:
      - description: |-
          The specified string is an expression for the purpose of filtering
          the list of Revisions returned. The expression syntax was inspired by SQL.
          It supports conjunctions using `AND` of relational expressions of the form *attribute*
          *operator* *attribute_or_literal*. The attribute names are case-sensitive and PascalCase,
          as in the JSON encoding.
          Strings support the following operators: `<`, `>`, `<=`, `>=`, `=`, `!=`, `LIKE`, `NOT LIKE`, `ILIKE`, `~~`, `!~~`, `~`, `~*`, `!~`, `!~*`, `IN`, `NOT IN`.
          String pattern operators: `LIKE` and `~~` for pattern matching with `%` and `_` wildcards,
          `ILIKE` for case-insensitive pattern matching, `NOT LIKE` and `!~~` for negated pattern matching.
          String regex operators: `~` for regex matching, `~*` for case-insensitive regex,
          `!~` and `!~*` for regex not matching (case-sensitive and insensitive).
          Integers support the following operators: `<`, `>`, `<=`, `>=`, `=`, `!=`, `IN`, `NOT IN`.
          UUIDs and boolean attributes support equality and inequality only.
          UUID and time literals must be quoted as string literals.
          String literals are quoted with single quotes, such as `'string'`.
          Time literals use the same form as when serialized as JSON,
          such as: `CreatedAt > '2025-02-18T23:16:34'`.
          Integer and boolean literals are also supported for attributes of those types.
          Arrays support the `?` operator to to match any element of the array,
          as in `ApprovedBy ? '7c61626f-ddbe-41af-93f6-b69f4ab6d308'`.
          Arrays can perform LEN() to check for length, as in `LEN(ApprovedBy) > 0`.
          Map support the dot notation to specify a particular map key, as in `Labels.tier = 'Backend'`.
          Maps support `IS NULL` and `IS NOT NULL` with dot notation to check for key absence or presence,
          as in `Labels.tier IS NULL` (key doesn't exist) or `Labels.tier IS NOT NULL` (key exists).
          Comparison results can be tested with `IS TRUE`, `IS FALSE`, `IS NOT TRUE`, and `IS NOT FALSE`.
          These are useful for nullable columns: `MergeSourceID = '<uuid>' IS NOT FALSE` matches rows where MergeSourceID equals the value OR is NULL.
          The `IN` and `NOT IN` operators accept a comma-separated list of values in parentheses,
          such as `Slug IN ('slugone', 'slugtwo')` or `Labels.environment IN ('prod', 'staging')`.
          Conjunctions are supported using the `AND` operator.
          An example conjunction is:
          `CreatedAt >= '2025-01-07' AND Slug = 'test' AND Labels.mykey = 'myvalue'`.

          Supported attributes for filtering on Revision: ApplyGates, ApplyWarnings, ApprovedBy, ChangeSetID, CreatedAt, DataHash, Description, LiveAt, OrganizationID, RevisionID, RevisionNum, Source, SpaceID, Tags, UnitID, UpdatedAt, UserAgent, UserID.

          To list tagged Revisions use `Tags ? '<tag-id>'`.

          The whole string must be query-encoded.
        in: query
        name: where
        required: false
        schema:
          type: string
      - description: |-
          UUID of a Filter entity to apply to the Revision list.

          The Filter must be in the same Organization as the user credentials.

          The Filter's From field must match the entity type being filtered (Revision).

          For Space-resident entities, if the Filter has a FromSpaceID, it must match the operation's SpaceID.

          The Filter's Where clause will be combined with any explicit 'where' parameter using AND logic.

          If both 'filter' and 'where' parameters are specified, they are combined with AND logic.
        in: query
        name: filter
        required: false
        schema:
          type: string
      - description: |-
          Free text search that approximately matches the specified string against string fields and map keys/values.

          The search is case-insensitive and uses pattern matching to find entities containing the text.

          Searchable string fields include attributes like Slug, DisplayName, and string-typed custom fields.

          For map fields (like Labels and Annotations), the search matches both map keys and values.

          The search uses OR logic across all searchable fields, so matching any field will return the entity.

          If both 'where' and 'contains' parameters are specified, they are combined with AND logic.

          Searchable fields for Revision include string and map-type attributes from the queryable attributes list.

          The whole string must be query-encoded.
        in: query
        name: contains
        required: false
        schema:
          type: string
      - description: |-
          Include clause for expanding related entities in the response for Revision.
          The attribute names are case-sensitive, PascalCase, and
          expected in a comma-separated list format as in the JSON encoding.

          Supported attributes for Revision are ChangeSetID, OrganizationID, SpaceID, Tags, UnitID, UserID.

          The whole string must be query-encoded.
        in: query
        name: include
        required: false
        schema:
          type: string
      - description: |-
          Select clause for specifying which fields to include in the response for Revision.
          The attribute names are case-sensitive, PascalCase, and
          expected in a comma-separated list format as in the JSON encoding.
          If not specified, all fields are returned.
          Entity and parent IDs (like OrganizationID, SpaceID, RevisionID) and Slug are always returned regardless of the select parameter.
          Fields used in where and contains filters are also automatically included.
          Example: 'DisplayName,CreatedAt,Labels' will return only those fields plus the required ID and Slug fields.
          The whole string must be query-encoded.
        in: query
        name: select
        required: false
        schema:
          type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                items:
                  $ref: '#/components/schemas/ExtendedRevision'
                type: array
          description: OK
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Revision request is invalid (Bad Request).
        "401":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Unauthorized access.
        "403":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Forbidden access.
        "404":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Revision not found.
        "500":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Something went wrong while processing Revision.
        default:
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Unexpected error.
      summary: List of Revisions across spaces
      tags:
      - Revision
  /space:
    get:
      description: List Spaces
      operationId: ListSpaces
      parameters:
      - description: |-
          The specified string is an expression for the purpose of filtering
          the list of Spaces returned. The expression syntax was inspired by SQL.
          It supports conjunctions using `AND` of relational expressions of the form *attribute*
          *operator* *attribute_or_literal*. The attribute names are case-sensitive and PascalCase,
          as in the JSON encoding.
          Strings support the following operators: `<`, `>`, `<=`, `>=`, `=`, `!=`, `LIKE`, `NOT LIKE`, `ILIKE`, `~~`, `!~~`, `~`, `~*`, `!~`, `!~*`, `IN`, `NOT IN`.
          String pattern operators: `LIKE` and `~~` for pattern matching with `%` and `_` wildcards,
          `ILIKE` for case-insensitive pattern matching, `NOT LIKE` and `!~~` for negated pattern matching.
          String regex operators: `~` for regex matching, `~*` for case-insensitive regex,
          `!~` and `!~*` for regex not matching (case-sensitive and insensitive).
          Integers support the following operators: `<`, `>`, `<=`, `>=`, `=`, `!=`, `IN`, `NOT IN`.
          UUIDs and boolean attributes support equality and inequality only.
          UUID and time literals must be quoted as string literals.
          String literals are quoted with single quotes, such as `'string'`.
          Time literals use the same form as when serialized as JSON,
          such as: `CreatedAt > '2025-02-18T23:16:34'`.
          Integer and boolean literals are also supported for attributes of those types.
          Arrays support the `?` operator to to match any element of the array,
          as in `ApprovedBy ? '7c61626f-ddbe-41af-93f6-b69f4ab6d308'`.
          Arrays can perform LEN() to check for length, as in `LEN(ApprovedBy) > 0`.
          Map support the dot notation to specify a particular map key, as in `Labels.tier = 'Backend'`.
          Maps support `IS NULL` and `IS NOT NULL` with dot notation to check for key absence or presence,
          as in `Labels.tier IS NULL` (key doesn't exist) or `Labels.tier IS NOT NULL` (key exists).
          Comparison results can be tested with `IS TRUE`, `IS FALSE`, `IS NOT TRUE`, and `IS NOT FALSE`.
          These are useful for nullable columns: `MergeSourceID = '<uuid>' IS NOT FALSE` matches rows where MergeSourceID equals the value OR is NULL.
          The `IN` and `NOT IN` operators accept a comma-separated list of values in parentheses,
          such as `Slug IN ('slugone', 'slugtwo')` or `Labels.environment IN ('prod', 'staging')`.
          Conjunctions are supported using the `AND` operator.
          An example conjunction is:
          `CreatedAt >= '2025-01-07' AND Slug = 'test' AND Labels.mykey = 'myvalue'`.

          Supported attributes for filtering on Space: AttributeFilterID, AttributeHash, AttributeIDs, CreatedAt, DeleteGates, DisplayName, Labels, OrganizationID, Permissions, Slug, SpaceID, TriggerFilterID, TriggerHash, TriggerIDs, UpdatedAt.

          The whole string must be query-encoded.
        in: query
        name: where
        required: false
        schema:
          type: string
      - description: |-
          UUID of a Filter entity to apply to the Space list.

          The Filter must be in the same Organization as the user credentials.

          The Filter's From field must match the entity type being filtered (Space).

          For Space-resident entities, if the Filter has a FromSpaceID, it must match the operation's SpaceID.

          The Filter's Where clause will be combined with any explicit 'where' parameter using AND logic.

          If both 'filter' and 'where' parameters are specified, they are combined with AND logic.
        in: query
        name: filter
        required: false
        schema:
          type: string
      - description: |-
          Free text search that approximately matches the specified string against string fields and map keys/values.

          The search is case-insensitive and uses pattern matching to find entities containing the text.

          Searchable string fields include attributes like Slug, DisplayName, and string-typed custom fields.

          For map fields (like Labels and Annotations), the search matches both map keys and values.

          The search uses OR logic across all searchable fields, so matching any field will return the entity.

          If both 'where' and 'contains' parameters are specified, they are combined with AND logic.

          Searchable fields for Space include string and map-type attributes from the queryable attributes list.

          The whole string must be query-encoded.
        in: query
        name: contains
        required: false
        schema:
          type: string
      - description: |-
          Include clause for expanding related entities in the response for Space.
          The attribute names are case-sensitive, PascalCase, and
          expected in a comma-separated list format as in the JSON encoding.

          Supported attributes for Space are AttributeFilterID, AttributeIDs, OrganizationID, TriggerFilterID, TriggerIDs.

          The whole string must be query-encoded.
        in: query
        name: include
        required: false
        schema:
          type: string
      - description: |-
          Select clause for specifying which fields to include in the response for Space.
          The attribute names are case-sensitive, PascalCase, and
          expected in a comma-separated list format as in the JSON encoding.
          If not specified, all fields are returned.
          Entity and parent IDs (like OrganizationID, SpaceID, SpaceID) and Slug are always returned regardless of the select parameter.
          Fields used in where and contains filters are also automatically included.
          Example: 'DisplayName,CreatedAt,Labels' will return only those fields plus the required ID and Slug fields.
          The whole string must be query-encoded.
        in: query
        name: select
        required: false
        schema:
          type: string
      - description: Return summarized entity data
        in: query
        name: summary
        required: false
        schema:
          type: boolean
      responses:
        "200":
          content:
            application/json:
              schema:
                items:
                  $ref: '#/components/schemas/ExtendedSpace'
                type: array
          description: OK
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Space request is invalid (Bad Request).
        "401":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Unauthorized access.
        "403":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Forbidden access.
        "404":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Space not found.
        "500":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Something went wrong while processing Space.
        default:
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Unexpected error.
      summary: List Spaces
      tags:
      - Space
    post:
      description: Create Space
      operationId: CreateSpace
      parameters:
      - description: Allowed values are true and false. Default is false. When true,
          reports success when an entity already exists and returns the existing entity
        in: query
        name: allow_exists
        required: false
        schema:
          type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/Space'
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Space'
          description: The logical container for most entities in ConfigHub. Namespaces
            triggers, units, targets, workers, and other entities.
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Space request is invalid (Bad Request).
        "401":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Unauthorized access.
        "403":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Forbidden access.
        "404":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Space not found.
        "409":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Space data conflict. Data has changed since last read.
        "500":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Something went wrong while processing Space.
        default:
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Unexpected error.
      summary: Create Space
      tags:
      - Space
  /space/{space_id}:
    delete:
      description: Delete Space
      operationId: DeleteSpace
      parameters:
      - description: Unique identifier for a space_id
        in: path
        name: space_id
        required: true
        schema:
          format: uuid
          type: string
      - description: Valid values are true and false. False is the default if unspecified.
          If true, recursively delete all entities within the deleted space(s) so
          long as none have delete gates.
        in: query
        name: recursive
        required: false
        schema:
          type: string
      - description: Valid values are true and false. False is the default if unspecified.
          If true, recursively delete all entities within the deleted space(s) regardless
          whether any have delete gates.
        in: query
        name: recursive_force
        required: false
        schema:
          type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/DeleteResponse'
          description: Response for successful delete operation
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Space request is invalid (Bad Request).
        "401":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Unauthorized access.
        "403":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Forbidden access.
        "404":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Space not found.
        "409":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Space data conflict. Data has changed since last read.
        "422":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Space could not be deleted.
        "500":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Something went wrong while processing Space.
        default:
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Unexpected error.
      summary: Delete Space
      tags:
      - Space
    get:
      description: Get Space
      operationId: GetSpace
      parameters:
      - description: |-
          Include clause for expanding related entities in the response for Space.
          The attribute names are case-sensitive, PascalCase, and
          expected in a comma-separated list format as in the JSON encoding.

          Supported attributes for Space are AttributeFilterID, AttributeIDs, OrganizationID, TriggerFilterID, TriggerIDs.

          The whole string must be query-encoded.
        in: query
        name: include
        required: false
        schema:
          type: string
      - description: |-
          Select clause for specifying which fields to include in the response for Space.
          The attribute names are case-sensitive, PascalCase, and
          expected in a comma-separated list format as in the JSON encoding.
          If not specified, all fields are returned.
          Entity and parent IDs (like OrganizationID, SpaceID, SpaceID) and Slug are always returned regardless of the select parameter.
          Fields used in where and contains filters are also automatically included.
          Example: 'DisplayName,CreatedAt,Labels' will return only those fields plus the required ID and Slug fields.
          The whole string must be query-encoded.
        in: query
        name: select
        required: false
        schema:
          type: string
      - description: Unique identifier for a space_id
        in: path
        name: space_id
        required: true
        schema:
          format: uuid
          type: string
      - description: Return summarized entity data
        in: query
        name: summary
        required: false
        schema:
          type: boolean
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ExtendedSpace'
          description: OK
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Space request is invalid (Bad Request).
        "401":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Unauthorized access.
        "403":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Forbidden access.
        "404":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Space not found.
        "500":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Something went wrong while processing Space.
        default:
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Unexpected error.
      summary: Get Space
      tags:
      - Space
    patch:
      description: Patch Space
      operationId: PatchSpace
      parameters:
      - description: Unique identifier for a space_id
        in: path
        name: space_id
        required: true
        schema:
          format: uuid
          type: string
      - description: If true, re-list the Triggers matching WhereTrigger and/or TriggerFilterID
          even if these fields have not changed
        in: query
        name: refresh_triggers
        required: false
        schema:
          type: boolean
      requestBody:
        content:
          application/merge-patch+json:
            schema:
              description: JSON Merge Patch schema for Space. All fields are optional
                and nullable. Readonly fields are ignored.
              properties:
                Annotations:
                  additionalProperties:
                    nullable: true
                    type: string
                  description: An optional map of Annotation key/value pairs for tools
                    to attach information to entities.
                  nullable: true
                  type: object
                AttributeFilterID:
                  description: ""
                  format: uuid
                  nullable: true
                  type: string
                DeleteGates:
                  additionalProperties:
                    nullable: true
                    type: boolean
                  description: An optional set of gates that, if any is present, will
                    block deletion
                  nullable: true
                  type: object
                DisplayName:
                  description: Friendly name for the entity.
                  nullable: true
                  type: string
                Labels:
                  additionalProperties:
                    nullable: true
                    type: string
                  description: An optional map of Label key/value pairs to specify
                    identifying attributes of entities for the purpose of grouping
                    and filtering them.
                  nullable: true
                  type: object
                Permissions:
                  additionalProperties:
                    nullable: true
                    type: object
                  description: ""
                  nullable: true
                  type: object
                Slug:
                  description: Unique URL-safe identifier for the entity.
                  nullable: true
                  type: string
                TriggerFilterID:
                  description: ""
                  format: uuid
                  nullable: true
                  type: string
                Version:
                  description: An entity-specific sequence number used for optimistic
                    concurrency control. The value read must be sent in calls to Update.
                  nullable: true
                  type: integer
                WhereAttribute:
                  description: ""
                  nullable: true
                  type: string
                WhereTrigger:
                  description: ""
                  nullable: true
                  type: string
              type: object
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Space'
          description: The logical container for most entities in ConfigHub. Namespaces
            triggers, units, targets, workers, and other entities.
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Space request is invalid (Bad Request).
        "401":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Unauthorized access.
        "403":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Forbidden access.
        "404":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Space not found.
        "409":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Space data conflict. Data has changed since last read.
        "500":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Something went wrong while processing Space.
        default:
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Unexpected error.
      summary: Patch Space
      tags:
      - Space
    put:
      description: Update Space
      operationId: UpdateSpace
      parameters:
      - description: Unique identifier for a space_id
        in: path
        name: space_id
        required: true
        schema:
          format: uuid
          type: string
      - description: If true, re-list the Triggers matching WhereTrigger and/or TriggerFilterID
          even if these fields have not changed
        in: query
        name: refresh_triggers
        required: false
        schema:
          type: boolean
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/Space'
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Space'
          description: The logical container for most entities in ConfigHub. Namespaces
            triggers, units, targets, workers, and other entities.
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Space request is invalid (Bad Request).
        "401":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Unauthorized access.
        "403":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Forbidden access.
        "404":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Space not found.
        "409":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Space data conflict. Data has changed since last read.
        "500":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Something went wrong while processing Space.
        default:
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Unexpected error.
      summary: Update Space
      tags:
      - Space
  /space/{space_id}/attribute:
    get:
      description: List Attributes
      operationId: ListAttributes
      parameters:
      - description: Unique identifier for a space_id
        in: path
        name: space_id
        required: true
        schema:
          format: uuid
          type: string
      - description: |-
          The specified string is an expression for the purpose of filtering
          the list of Attributes returned. The expression syntax was inspired by SQL.
          It supports conjunctions using `AND` of relational expressions of the form *attribute*
          *operator* *attribute_or_literal*. The attribute names are case-sensitive and PascalCase,
          as in the JSON encoding.
          Strings support the following operators: `<`, `>`, `<=`, `>=`, `=`, `!=`, `LIKE`, `NOT LIKE`, `ILIKE`, `~~`, `!~~`, `~`, `~*`, `!~`, `!~*`, `IN`, `NOT IN`.
          String pattern operators: `LIKE` and `~~` for pattern matching with `%` and `_` wildcards,
          `ILIKE` for case-insensitive pattern matching, `NOT LIKE` and `!~~` for negated pattern matching.
          String regex operators: `~` for regex matching, `~*` for case-insensitive regex,
          `!~` and `!~*` for regex not matching (case-sensitive and insensitive).
          Integers support the following operators: `<`, `>`, `<=`, `>=`, `=`, `!=`, `IN`, `NOT IN`.
          UUIDs and boolean attributes support equality and inequality only.
          UUID and time literals must be quoted as string literals.
          String literals are quoted with single quotes, such as `'string'`.
          Time literals use the same form as when serialized as JSON,
          such as: `CreatedAt > '2025-02-18T23:16:34'`.
          Integer and boolean literals are also supported for attributes of those types.
          Arrays support the `?` operator to to match any element of the array,
          as in `ApprovedBy ? '7c61626f-ddbe-41af-93f6-b69f4ab6d308'`.
          Arrays can perform LEN() to check for length, as in `LEN(ApprovedBy) > 0`.
          Map support the dot notation to specify a particular map key, as in `Labels.tier = 'Backend'`.
          Maps support `IS NULL` and `IS NOT NULL` with dot notation to check for key absence or presence,
          as in `Labels.tier IS NULL` (key doesn't exist) or `Labels.tier IS NOT NULL` (key exists).
          Comparison results can be tested with `IS TRUE`, `IS FALSE`, `IS NOT TRUE`, and `IS NOT FALSE`.
          These are useful for nullable columns: `MergeSourceID = '<uuid>' IS NOT FALSE` matches rows where MergeSourceID equals the value OR is NULL.
          The `IN` and `NOT IN` operators accept a comma-separated list of values in parentheses,
          such as `Slug IN ('slugone', 'slugtwo')` or `Labels.environment IN ('prod', 'staging')`.
          Conjunctions are supported using the `AND` operator.
          An example conjunction is:
          `CreatedAt >= '2025-01-07' AND Slug = 'test' AND Labels.mykey = 'myvalue'`.

          Supported attributes for filtering on Attribute: AttributeID, CreatedAt, DataType, DeleteGates, DisplayName, Hash, Labels, OrganizationID, Slug, SpaceID, ToolchainType, UpdatedAt.

          The whole string must be query-encoded.
        in: query
        name: where
        required: false
        schema:
          type: string
      - description: |-
          UUID of a Filter entity to apply to the Attribute list.

          The Filter must be in the same Organization as the user credentials.

          The Filter's From field must match the entity type being filtered (Attribute).

          For Space-resident entities, if the Filter has a FromSpaceID, it must match the operation's SpaceID.

          The Filter's Where clause will be combined with any explicit 'where' parameter using AND logic.

          If both 'filter' and 'where' parameters are specified, they are combined with AND logic.
        in: query
        name: filter
        required: false
        schema:
          type: string
      - description: |-
          Free text search that approximately matches the specified string against string fields and map keys/values.

          The search is case-insensitive and uses pattern matching to find entities containing the text.

          Searchable string fields include attributes like Slug, DisplayName, and string-typed custom fields.

          For map fields (like Labels and Annotations), the search matches both map keys and values.

          The search uses OR logic across all searchable fields, so matching any field will return the entity.

          If both 'where' and 'contains' parameters are specified, they are combined with AND logic.

          Searchable fields for Attribute include string and map-type attributes from the queryable attributes list.

          The whole string must be query-encoded.
        in: query
        name: contains
        required: false
        schema:
          type: string
      - description: |-
          Include clause for expanding related entities in the response for Attribute.
          The attribute names are case-sensitive, PascalCase, and
          expected in a comma-separated list format as in the JSON encoding.

          Supported attributes for Attribute are OrganizationID, SpaceID.

          The whole string must be query-encoded.
        in: query
        name: include
        required: false
        schema:
          type: string
      - description: |-
          Select clause for specifying which fields to include in the response for Attribute.
          The attribute names are case-sensitive, PascalCase, and
          expected in a comma-separated list format as in the JSON encoding.
          If not specified, all fields are returned.
          Entity and parent IDs (like OrganizationID, SpaceID, AttributeID) and Slug are always returned regardless of the select parameter.
          Fields used in where and contains filters are also automatically included.
          Example: 'DisplayName,CreatedAt,Labels' will return only those fields plus the required ID and Slug fields.
          The whole string must be query-encoded.
        in: query
        name: select
        required: false
        schema:
          type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                items:
                  $ref: '#/components/schemas/ExtendedAttribute'
                type: array
          description: OK
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Attribute request is invalid (Bad Request).
        "401":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Unauthorized access.
        "403":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Forbidden access.
        "404":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Attribute not found.
        "500":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Something went wrong while processing Attribute.
        default:
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Unexpected error.
      summary: List Attributes
      tags:
      - Attribute
    post:
      description: Create Attribute
      operationId: CreateAttribute
      parameters:
      - description: Unique identifier for a space_id
        in: path
        name: space_id
        required: true
        schema:
          format: uuid
          type: string
      - description: Allowed values are true and false. Default is false. When true,
          reports success when an entity already exists and returns the existing entity
        in: query
        name: allow_exists
        required: false
        schema:
          type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/Attribute'
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Attribute'
          description: |-
            Defines a dynamic configuration attribute that registers getter and setter functions
            and their associated paths in a Space's FunctionExecutor. Attributes enable per-Space
            customization of the function registry by specifying a set of paths within a resource
            type that can be read and written using generated get-<slug> and set-<slug> functions.
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Attribute request is invalid (Bad Request).
        "401":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Unauthorized access.
        "403":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Forbidden access.
        "404":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Attribute not found.
        "409":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Attribute data conflict. Data has changed since last read.
        "500":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Something went wrong while processing Attribute.
        default:
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Unexpected error.
      summary: Create Attribute
      tags:
      - Attribute
  /space/{space_id}/attribute/{attribute_id}:
    delete:
      description: Delete Attribute
      operationId: DeleteAttribute
      parameters:
      - description: Unique identifier for a space_id
        in: path
        name: space_id
        required: true
        schema:
          format: uuid
          type: string
      - description: Unique identifier for a attribute_id
        in: path
        name: attribute_id
        required: true
        schema:
          format: uuid
          type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/DeleteResponse'
          description: Response for successful delete operation
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Attribute request is invalid (Bad Request).
        "401":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Unauthorized access.
        "403":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Forbidden access.
        "404":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Attribute not found.
        "409":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Attribute data conflict. Data has changed since last read.
        "422":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Attribute could not be deleted.
        "500":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Something went wrong while processing Attribute.
        default:
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Unexpected error.
      summary: Delete Attribute
      tags:
      - Attribute
    get:
      description: Get Attribute
      operationId: GetAttribute
      parameters:
      - description: Unique identifier for a space_id
        in: path
        name: space_id
        required: true
        schema:
          format: uuid
          type: string
      - description: |-
          Include clause for expanding related entities in the response for Attribute.
          The attribute names are case-sensitive, PascalCase, and
          expected in a comma-separated list format as in the JSON encoding.

          Supported attributes for Attribute are OrganizationID, SpaceID.

          The whole string must be query-encoded.
        in: query
        name: include
        required: false
        schema:
          type: string
      - description: |-
          Select clause for specifying which fields to include in the response for Attribute.
          The attribute names are case-sensitive, PascalCase, and
          expected in a comma-separated list format as in the JSON encoding.
          If not specified, all fields are returned.
          Entity and parent IDs (like OrganizationID, SpaceID, AttributeID) and Slug are always returned regardless of the select parameter.
          Fields used in where and contains filters are also automatically included.
          Example: 'DisplayName,CreatedAt,Labels' will return only those fields plus the required ID and Slug fields.
          The whole string must be query-encoded.
        in: query
        name: select
        required: false
        schema:
          type: string
      - description: Unique identifier for a attribute_id
        in: path
        name: attribute_id
        required: true
        schema:
          format: uuid
          type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ExtendedAttribute'
          description: OK
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Attribute request is invalid (Bad Request).
        "401":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Unauthorized access.
        "403":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Forbidden access.
        "404":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Attribute not found.
        "500":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Something went wrong while processing Attribute.
        default:
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Unexpected error.
      summary: Get Attribute
      tags:
      - Attribute
    patch:
      description: Patch Attribute
      operationId: PatchAttribute
      parameters:
      - description: Unique identifier for a space_id
        in: path
        name: space_id
        required: true
        schema:
          format: uuid
          type: string
      - description: Unique identifier for a attribute_id
        in: path
        name: attribute_id
        required: true
        schema:
          format: uuid
          type: string
      requestBody:
        content:
          application/merge-patch+json:
            schema:
              description: JSON Merge Patch schema for Attribute. All fields are optional
                and nullable. Readonly fields are ignored.
              properties:
                Annotations:
                  additionalProperties:
                    nullable: true
                    type: string
                  description: An optional map of Annotation key/value pairs for tools
                    to attach information to entities.
                  nullable: true
                  type: object
                DataType:
                  description: ""
                  nullable: true
                  type: string
                DeleteGates:
                  additionalProperties:
                    nullable: true
                    type: boolean
                  description: An optional set of gates that, if any is present, will
                    block deletion
                  nullable: true
                  type: object
                Description:
                  description: ""
                  nullable: true
                  type: string
                DisplayName:
                  description: Friendly name for the entity.
                  nullable: true
                  type: string
                Labels:
                  additionalProperties:
                    nullable: true
                    type: string
                  description: An optional map of Label key/value pairs to specify
                    identifying attributes of entities for the purpose of grouping
                    and filtering them.
                  nullable: true
                  type: object
                Parameters:
                  description: ""
                  items:
                    nullable: true
                    type: object
                  nullable: true
                  type: array
                ResourceTypePaths:
                  description: ""
                  items:
                    nullable: true
                    type: object
                  nullable: true
                  type: array
                Slug:
                  description: Unique URL-safe identifier for the entity.
                  nullable: true
                  type: string
                ToolchainType:
                  description: ""
                  nullable: true
                  type: string
                Version:
                  description: An entity-specific sequence number used for optimistic
                    concurrency control. The value read must be sent in calls to Update.
                  nullable: true
                  type: integer
              type: object
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Attribute'
          description: |-
            Defines a dynamic configuration attribute that registers getter and setter functions
            and their associated paths in a Space's FunctionExecutor. Attributes enable per-Space
            customization of the function registry by specifying a set of paths within a resource
            type that can be read and written using generated get-<slug> and set-<slug> functions.
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Attribute request is invalid (Bad Request).
        "401":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Unauthorized access.
        "403":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Forbidden access.
        "404":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Attribute not found.
        "409":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Attribute data conflict. Data has changed since last read.
        "500":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Something went wrong while processing Attribute.
        default:
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Unexpected error.
      summary: Patch Attribute
      tags:
      - Attribute
    put:
      description: Update Attribute
      operationId: UpdateAttribute
      parameters:
      - description: Unique identifier for a space_id
        in: path
        name: space_id
        required: true
        schema:
          format: uuid
          type: string
      - description: Unique identifier for a attribute_id
        in: path
        name: attribute_id
        required: true
        schema:
          format: uuid
          type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/Attribute'
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Attribute'
          description: |-
            Defines a dynamic configuration attribute that registers getter and setter functions
            and their associated paths in a Space's FunctionExecutor. Attributes enable per-Space
            customization of the function registry by specifying a set of paths within a resource
            type that can be read and written using generated get-<slug> and set-<slug> functions.
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Attribute request is invalid (Bad Request).
        "401":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Unauthorized access.
        "403":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Forbidden access.
        "404":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Attribute not found.
        "409":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Attribute data conflict. Data has changed since last read.
        "500":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Something went wrong while processing Attribute.
        default:
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Unexpected error.
      summary: Update Attribute
      tags:
      - Attribute
  /space/{space_id}/bridge_worker:
    get:
      description: List BridgeWorkers
      operationId: ListBridgeWorkers
      parameters:
      - description: Unique identifier for a space_id
        in: path
        name: space_id
        required: true
        schema:
          format: uuid
          type: string
      - description: |-
          The specified string is an expression for the purpose of filtering
          the list of BridgeWorkers returned. The expression syntax was inspired by SQL.
          It supports conjunctions using `AND` of relational expressions of the form *attribute*
          *operator* *attribute_or_literal*. The attribute names are case-sensitive and PascalCase,
          as in the JSON encoding.
          Strings support the following operators: `<`, `>`, `<=`, `>=`, `=`, `!=`, `LIKE`, `NOT LIKE`, `ILIKE`, `~~`, `!~~`, `~`, `~*`, `!~`, `!~*`, `IN`, `NOT IN`.
          String pattern operators: `LIKE` and `~~` for pattern matching with `%` and `_` wildcards,
          `ILIKE` for case-insensitive pattern matching, `NOT LIKE` and `!~~` for negated pattern matching.
          String regex operators: `~` for regex matching, `~*` for case-insensitive regex,
          `!~` and `!~*` for regex not matching (case-sensitive and insensitive).
          Integers support the following operators: `<`, `>`, `<=`, `>=`, `=`, `!=`, `IN`, `NOT IN`.
          UUIDs and boolean attributes support equality and inequality only.
          UUID and time literals must be quoted as string literals.
          String literals are quoted with single quotes, such as `'string'`.
          Time literals use the same form as when serialized as JSON,
          such as: `CreatedAt > '2025-02-18T23:16:34'`.
          Integer and boolean literals are also supported for attributes of those types.
          Arrays support the `?` operator to to match any element of the array,
          as in `ApprovedBy ? '7c61626f-ddbe-41af-93f6-b69f4ab6d308'`.
          Arrays can perform LEN() to check for length, as in `LEN(ApprovedBy) > 0`.
          Map support the dot notation to specify a particular map key, as in `Labels.tier = 'Backend'`.
          Maps support `IS NULL` and `IS NOT NULL` with dot notation to check for key absence or presence,
          as in `Labels.tier IS NULL` (key doesn't exist) or `Labels.tier IS NOT NULL` (key exists).
          Comparison results can be tested with `IS TRUE`, `IS FALSE`, `IS NOT TRUE`, and `IS NOT FALSE`.
          These are useful for nullable columns: `MergeSourceID = '<uuid>' IS NOT FALSE` matches rows where MergeSourceID equals the value OR is NULL.
          The `IN` and `NOT IN` operators accept a comma-separated list of values in parentheses,
          such as `Slug IN ('slugone', 'slugtwo')` or `Labels.environment IN ('prod', 'staging')`.
          Conjunctions are supported using the `AND` operator.
          An example conjunction is:
          `CreatedAt >= '2025-01-07' AND Slug = 'test' AND Labels.mykey = 'myvalue'`.

          Supported attributes for filtering on BridgeWorker: BridgeWorkerID, Condition, CreatedAt, DisplayName, IPAddress, Labels, LastMessage, LastSeenAt, OrgRole, OrganizationID, Permissions, Slug, SpaceID, UpdatedAt, UserID.

          The whole string must be query-encoded.
        in: query
        name: where
        required: false
        schema:
          type: string
      - description: |-
          UUID of a Filter entity to apply to the BridgeWorker list.

          The Filter must be in the same Organization as the user credentials.

          The Filter's From field must match the entity type being filtered (BridgeWorker).

          For Space-resident entities, if the Filter has a FromSpaceID, it must match the operation's SpaceID.

          The Filter's Where clause will be combined with any explicit 'where' parameter using AND logic.

          If both 'filter' and 'where' parameters are specified, they are combined with AND logic.
        in: query
        name: filter
        required: false
        schema:
          type: string
      - description: |-
          Free text search that approximately matches the specified string against string fields and map keys/values.

          The search is case-insensitive and uses pattern matching to find entities containing the text.

          Searchable string fields include attributes like Slug, DisplayName, and string-typed custom fields.

          For map fields (like Labels and Annotations), the search matches both map keys and values.

          The search uses OR logic across all searchable fields, so matching any field will return the entity.

          If both 'where' and 'contains' parameters are specified, they are combined with AND logic.

          Searchable fields for BridgeWorker include string and map-type attributes from the queryable attributes list.

          The whole string must be query-encoded.
        in: query
        name: contains
        required: false
        schema:
          type: string
      - description: |-
          Include clause for expanding related entities in the response for BridgeWorker.
          The attribute names are case-sensitive, PascalCase, and
          expected in a comma-separated list format as in the JSON encoding.

          Supported attributes for BridgeWorker are OrganizationID, SpaceID.

          The whole string must be query-encoded.
        in: query
        name: include
        required: false
        schema:
          type: string
      - description: |-
          Select clause for specifying which fields to include in the response for BridgeWorker.
          The attribute names are case-sensitive, PascalCase, and
          expected in a comma-separated list format as in the JSON encoding.
          If not specified, all fields are returned.
          Entity and parent IDs (like OrganizationID, SpaceID, BridgeWorkerID) and Slug are always returned regardless of the select parameter.
          Fields used in where and contains filters are also automatically included.
          Example: 'DisplayName,CreatedAt,Labels' will return only those fields plus the required ID and Slug fields.
          The whole string must be query-encoded.
        in: query
        name: select
        required: false
        schema:
          type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                items:
                  $ref: '#/components/schemas/ExtendedBridgeWorker'
                type: array
          description: OK
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: BridgeWorker request is invalid (Bad Request).
        "401":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Unauthorized access.
        "403":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Forbidden access.
        "404":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: BridgeWorker not found.
        "500":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Something went wrong while processing BridgeWorker.
        default:
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Unexpected error.
      summary: List BridgeWorkers
      tags:
      - BridgeWorker
    post:
      description: Create BridgeWorker
      operationId: CreateBridgeWorker
      parameters:
      - description: Unique identifier for a space_id
        in: path
        name: space_id
        required: true
        schema:
          format: uuid
          type: string
      - description: Allowed values are true and false. Default is false. When true,
          reports success when an entity already exists and returns the existing entity
        in: query
        name: allow_exists
        required: false
        schema:
          type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/BridgeWorker'
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BridgeWorker'
          description: |-
            BridgeWorker represents a bridge worker in ConfigHub.
            A bridge worker is a worker program that connects ConfigHub to external systems and targets.
            It acts as a bridge between ConfigHub and the infrastructure where configurations need
            to be applied. Bridge workers are responsible for executing configuration changes on
            remote targets and reporting status back to ConfigHub.
            When starting a bridge worker program, both the BridgeWorkerID and Secret are
            required for authentication with the ConfigHub server. These credentials allow the
            bridge worker to establish a secure connection and receive configuration actions.
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: BridgeWorker request is invalid (Bad Request).
        "401":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Unauthorized access.
        "403":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Forbidden access.
        "404":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: BridgeWorker not found.
        "409":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: BridgeWorker data conflict. Data has changed since last read.
        "500":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Something went wrong while processing BridgeWorker.
        default:
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Unexpected error.
      summary: Create BridgeWorker
      tags:
      - BridgeWorker
  /space/{space_id}/bridge_worker/{bridge_worker_id}:
    delete:
      description: Delete BridgeWorker
      operationId: DeleteBridgeWorker
      parameters:
      - description: Unique identifier for a space_id
        in: path
        name: space_id
        required: true
        schema:
          format: uuid
          type: string
      - description: Unique identifier for a bridge_worker_id
        in: path
        name: bridge_worker_id
        required: true
        schema:
          format: uuid
          type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/DeleteResponse'
          description: Response for successful delete operation
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: BridgeWorker request is invalid (Bad Request).
        "401":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Unauthorized access.
        "403":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Forbidden access.
        "404":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: BridgeWorker not found.
        "409":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: BridgeWorker data conflict. Data has changed since last read.
        "422":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: BridgeWorker could not be deleted.
        "500":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Something went wrong while processing BridgeWorker.
        default:
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Unexpected error.
      summary: Delete BridgeWorker
      tags:
      - BridgeWorker
    get:
      description: Get BridgeWorker
      operationId: GetBridgeWorker
      parameters:
      - description: Unique identifier for a space_id
        in: path
        name: space_id
        required: true
        schema:
          format: uuid
          type: string
      - description: |-
          Include clause for expanding related entities in the response for BridgeWorker.
          The attribute names are case-sensitive, PascalCase, and
          expected in a comma-separated list format as in the JSON encoding.

          Supported attributes for BridgeWorker are OrganizationID, SpaceID.

          The whole string must be query-encoded.
        in: query
        name: include
        required: false
        schema:
          type: string
      - description: |-
          Select clause for specifying which fields to include in the response for BridgeWorker.
          The attribute names are case-sensitive, PascalCase, and
          expected in a comma-separated list format as in the JSON encoding.
          If not specified, all fields are returned.
          Entity and parent IDs (like OrganizationID, SpaceID, BridgeWorkerID) and Slug are always returned regardless of the select parameter.
          Fields used in where and contains filters are also automatically included.
          Example: 'DisplayName,CreatedAt,Labels' will return only those fields plus the required ID and Slug fields.
          The whole string must be query-encoded.
        in: query
        name: select
        required: false
        schema:
          type: string
      - description: Unique identifier for a bridge_worker_id
        in: path
        name: bridge_worker_id
        required: true
        schema:
          format: uuid
          type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ExtendedBridgeWorker'
          description: OK
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: BridgeWorker request is invalid (Bad Request).
        "401":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Unauthorized access.
        "403":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Forbidden access.
        "404":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: BridgeWorker not found.
        "500":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Something went wrong while processing BridgeWorker.
        default:
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Unexpected error.
      summary: Get BridgeWorker
      tags:
      - BridgeWorker
    patch:
      description: Patch BridgeWorker
      operationId: PatchBridgeWorker
      parameters:
      - description: Unique identifier for a space_id
        in: path
        name: space_id
        required: true
        schema:
          format: uuid
          type: string
      - description: Unique identifier for a bridge_worker_id
        in: path
        name: bridge_worker_id
        required: true
        schema:
          format: uuid
          type: string
      requestBody:
        content:
          application/merge-patch+json:
            schema:
              description: JSON Merge Patch schema for BridgeWorker. All fields are
                optional and nullable. Readonly fields are ignored.
              properties:
                Annotations:
                  additionalProperties:
                    nullable: true
                    type: string
                  description: An optional map of Annotation key/value pairs for tools
                    to attach information to entities.
                  nullable: true
                  type: object
                Condition:
                  description: ""
                  nullable: true
                  type: string
                DeleteGates:
                  additionalProperties:
                    nullable: true
                    type: boolean
                  description: An optional set of gates that, if any is present, will
                    block deletion
                  nullable: true
                  type: object
                DisplayName:
                  description: Friendly name for the entity.
                  nullable: true
                  type: string
                Labels:
                  additionalProperties:
                    nullable: true
                    type: string
                  description: An optional map of Label key/value pairs to specify
                    identifying attributes of entities for the purpose of grouping
                    and filtering them.
                  nullable: true
                  type: object
                OrgRole:
                  description: ""
                  nullable: true
                  type: string
                Permissions:
                  additionalProperties:
                    nullable: true
                    type: object
                  description: ""
                  nullable: true
                  type: object
                ProvidedInfo:
                  description: ""
                  nullable: true
                  type: object
                Slug:
                  description: Unique URL-safe identifier for the entity.
                  nullable: true
                  type: string
                Version:
                  description: An entity-specific sequence number used for optimistic
                    concurrency control. The value read must be sent in calls to Update.
                  nullable: true
                  type: integer
              type: object
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BridgeWorker'
          description: |-
            BridgeWorker represents a bridge worker in ConfigHub.
            A bridge worker is a worker program that connects ConfigHub to external systems and targets.
            It acts as a bridge between ConfigHub and the infrastructure where configurations need
            to be applied. Bridge workers are responsible for executing configuration changes on
            remote targets and reporting status back to ConfigHub.
            When starting a bridge worker program, both the BridgeWorkerID and Secret are
            required for authentication with the ConfigHub server. These credentials allow the
            bridge worker to establish a secure connection and receive configuration actions.
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: BridgeWorker request is invalid (Bad Request).
        "401":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Unauthorized access.
        "403":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Forbidden access.
        "404":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: BridgeWorker not found.
        "409":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: BridgeWorker data conflict. Data has changed since last read.
        "500":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Something went wrong while processing BridgeWorker.
        default:
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Unexpected error.
      summary: Patch BridgeWorker
      tags:
      - BridgeWorker
    put:
      description: Update BridgeWorker
      operationId: UpdateBridgeWorker
      parameters:
      - description: Unique identifier for a space_id
        in: path
        name: space_id
        required: true
        schema:
          format: uuid
          type: string
      - description: Unique identifier for a bridge_worker_id
        in: path
        name: bridge_worker_id
        required: true
        schema:
          format: uuid
          type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/BridgeWorker'
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BridgeWorker'
          description: |-
            BridgeWorker represents a bridge worker in ConfigHub.
            A bridge worker is a worker program that connects ConfigHub to external systems and targets.
            It acts as a bridge between ConfigHub and the infrastructure where configurations need
            to be applied. Bridge workers are responsible for executing configuration changes on
            remote targets and reporting status back to ConfigHub.
            When starting a bridge worker program, both the BridgeWorkerID and Secret are
            required for authentication with the ConfigHub server. These credentials allow the
            bridge worker to establish a secure connection and receive configuration actions.
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: BridgeWorker request is invalid (Bad Request).
        "401":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Unauthorized access.
        "403":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Forbidden access.
        "404":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: BridgeWorker not found.
        "409":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: BridgeWorker data conflict. Data has changed since last read.
        "500":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Something went wrong while processing BridgeWorker.
        default:
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Unexpected error.
      summary: Update BridgeWorker
      tags:
      - BridgeWorker
  /space/{space_id}/bridge_worker/{bridge_worker_id}/function:
    get:
      description: List available functions supported by a specific bridge worker
      operationId: ListBridgeWorkerFunctions
      parameters:
      - description: Unique identifier for a space_id
        in: path
        name: space_id
        required: true
        schema:
          format: uuid
          type: string
      - description: Unique identifier for a bridge_worker_id
        in: path
        name: bridge_worker_id
        required: true
        schema:
          format: uuid
          type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                additionalProperties:
                  additionalProperties:
                    $ref: '#/components/schemas/FunctionSignature'
                  type: object
                type: object
          description: OK
      summary: List available functions supported by a specific bridge worker
      tags:
      - BridgeWorker
  /space/{space_id}/bridge_worker/{bridge_worker_id}/status:
    get:
      description: List BridgeWorkerStatuses
      operationId: ListBridgeWorkerStatuses
      parameters:
      - description: Unique identifier for a space_id
        in: path
        name: space_id
        required: true
        schema:
          format: uuid
          type: string
      - description: Unique identifier for a bridge_worker_id
        in: path
        name: bridge_worker_id
        required: true
        schema:
          format: uuid
          type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                items:
                  $ref: '#/components/schemas/BridgeWorkerStatus'
                type: array
          description: OK
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: BridgeWorkerStatus request is invalid (Bad Request).
        "401":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Unauthorized access.
        "403":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Forbidden access.
        "404":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: BridgeWorkerStatus not found.
        "500":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Something went wrong while processing BridgeWorkerStatus.
        default:
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Unexpected error.
      summary: List BridgeWorkerStatuses
      tags:
      - BridgeWorkerStatus
  /space/{space_id}/bridge_worker/{bridge_worker_id}/status/{status_id}:
    get:
      description: Get BridgeWorkerStatus
      operationId: GetBridgeWorkerStatus
      parameters:
      - description: Unique identifier for a space_id
        in: path
        name: space_id
        required: true
        schema:
          format: uuid
          type: string
      - description: Unique identifier for a bridge_worker_id
        in: path
        name: bridge_worker_id
        required: true
        schema:
          format: uuid
          type: string
      - description: Unique identifier for a status_id
        in: path
        name: status_id
        required: true
        schema:
          format: uuid
          type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/BridgeWorkerStatus'
          description: BridgeWorkerStatus represents the status information of a bridge
            worker within the system.
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: BridgeWorkerStatus request is invalid (Bad Request).
        "401":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Unauthorized access.
        "403":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Forbidden access.
        "404":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: BridgeWorkerStatus not found.
        "500":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Something went wrong while processing BridgeWorkerStatus.
        default:
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Unexpected error.
      summary: Get BridgeWorkerStatus
      tags:
      - BridgeWorkerStatus
  /space/{space_id}/change_set:
    get:
      description: List ChangeSets
      operationId: ListChangeSets
      parameters:
      - description: Unique identifier for a space_id
        in: path
        name: space_id
        required: true
        schema:
          format: uuid
          type: string
      - description: |-
          The specified string is an expression for the purpose of filtering
          the list of ChangeSets returned. The expression syntax was inspired by SQL.
          It supports conjunctions using `AND` of relational expressions of the form *attribute*
          *operator* *attribute_or_literal*. The attribute names are case-sensitive and PascalCase,
          as in the JSON encoding.
          Strings support the following operators: `<`, `>`, `<=`, `>=`, `=`, `!=`, `LIKE`, `NOT LIKE`, `ILIKE`, `~~`, `!~~`, `~`, `~*`, `!~`, `!~*`, `IN`, `NOT IN`.
          String pattern operators: `LIKE` and `~~` for pattern matching with `%` and `_` wildcards,
          `ILIKE` for case-insensitive pattern matching, `NOT LIKE` and `!~~` for negated pattern matching.
          String regex operators: `~` for regex matching, `~*` for case-insensitive regex,
          `!~` and `!~*` for regex not matching (case-sensitive and insensitive).
          Integers support the following operators: `<`, `>`, `<=`, `>=`, `=`, `!=`, `IN`, `NOT IN`.
          UUIDs and boolean attributes support equality and inequality only.
          UUID and time literals must be quoted as string literals.
          String literals are quoted with single quotes, such as `'string'`.
          Time literals use the same form as when serialized as JSON,
          such as: `CreatedAt > '2025-02-18T23:16:34'`.
          Integer and boolean literals are also supported for attributes of those types.
          Arrays support the `?` operator to to match any element of the array,
          as in `ApprovedBy ? '7c61626f-ddbe-41af-93f6-b69f4ab6d308'`.
          Arrays can perform LEN() to check for length, as in `LEN(ApprovedBy) > 0`.
          Map support the dot notation to specify a particular map key, as in `Labels.tier = 'Backend'`.
          Maps support `IS NULL` and `IS NOT NULL` with dot notation to check for key absence or presence,
          as in `Labels.tier IS NULL` (key doesn't exist) or `Labels.tier IS NOT NULL` (key exists).
          Comparison results can be tested with `IS TRUE`, `IS FALSE`, `IS NOT TRUE`, and `IS NOT FALSE`.
          These are useful for nullable columns: `MergeSourceID = '<uuid>' IS NOT FALSE` matches rows where MergeSourceID equals the value OR is NULL.
          The `IN` and `NOT IN` operators accept a comma-separated list of values in parentheses,
          such as `Slug IN ('slugone', 'slugtwo')` or `Labels.environment IN ('prod', 'staging')`.
          Conjunctions are supported using the `AND` operator.
          An example conjunction is:
          `CreatedAt >= '2025-01-07' AND Slug = 'test' AND Labels.mykey = 'myvalue'`.

          Supported attributes for filtering on ChangeSet: ChangeSetID, CreatedAt, DeleteGates, Description, DisplayName, EndTagID, Labels, OrganizationID, Slug, SpaceID, StartTagID, State, UpdatedAt.

          The whole string must be query-encoded.
        in: query
        name: where
        required: false
        schema:
          type: string
      - description: |-
          UUID of a Filter entity to apply to the ChangeSet list.

          The Filter must be in the same Organization as the user credentials.

          The Filter's From field must match the entity type being filtered (ChangeSet).

          For Space-resident entities, if the Filter has a FromSpaceID, it must match the operation's SpaceID.

          The Filter's Where clause will be combined with any explicit 'where' parameter using AND logic.

          If both 'filter' and 'where' parameters are specified, they are combined with AND logic.
        in: query
        name: filter
        required: false
        schema:
          type: string
      - description: |-
          Free text search that approximately matches the specified string against string fields and map keys/values.

          The search is case-insensitive and uses pattern matching to find entities containing the text.

          Searchable string fields include attributes like Slug, DisplayName, and string-typed custom fields.

          For map fields (like Labels and Annotations), the search matches both map keys and values.

          The search uses OR logic across all searchable fields, so matching any field will return the entity.

          If both 'where' and 'contains' parameters are specified, they are combined with AND logic.

          Searchable fields for ChangeSet include string and map-type attributes from the queryable attributes list.

          The whole string must be query-encoded.
        in: query
        name: contains
        required: false
        schema:
          type: string
      - description: |-
          Include clause for expanding related entities in the response for ChangeSet.
          The attribute names are case-sensitive, PascalCase, and
          expected in a comma-separated list format as in the JSON encoding.

          Supported attributes for ChangeSet are EndTagID, OrganizationID, SpaceID, StartTagID.

          The whole string must be query-encoded.
        in: query
        name: include
        required: false
        schema:
          type: string
      - description: |-
          Select clause for specifying which fields to include in the response for ChangeSet.
          The attribute names are case-sensitive, PascalCase, and
          expected in a comma-separated list format as in the JSON encoding.
          If not specified, all fields are returned.
          Entity and parent IDs (like OrganizationID, SpaceID, ChangeSetID) and Slug are always returned regardless of the select parameter.
          Fields used in where and contains filters are also automatically included.
          Example: 'DisplayName,CreatedAt,Labels' will return only those fields plus the required ID and Slug fields.
          The whole string must be query-encoded.
        in: query
        name: select
        required: false
        schema:
          type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                items:
                  $ref: '#/components/schemas/ExtendedChangeSet'
                type: array
          description: OK
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: ChangeSet request is invalid (Bad Request).
        "401":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Unauthorized access.
        "403":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Forbidden access.
        "404":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: ChangeSet not found.
        "500":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Something went wrong while processing ChangeSet.
        default:
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Unexpected error.
      summary: List ChangeSets
      tags:
      - ChangeSet
    post:
      description: Create ChangeSet
      operationId: CreateChangeSet
      parameters:
      - description: Unique identifier for a space_id
        in: path
        name: space_id
        required: true
        schema:
          format: uuid
          type: string
      - description: Allowed values are true and false. Default is false. When true,
          reports success when an entity already exists and returns the existing entity
        in: query
        name: allow_exists
        required: false
        schema:
          type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ChangeSet'
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ChangeSet'
          description: Defines an entity changeset.
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: ChangeSet request is invalid (Bad Request).
        "401":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Unauthorized access.
        "403":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Forbidden access.
        "404":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: ChangeSet not found.
        "409":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: ChangeSet data conflict. Data has changed since last read.
        "500":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Something went wrong while processing ChangeSet.
        default:
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Unexpected error.
      summary: Create ChangeSet
      tags:
      - ChangeSet
  /space/{space_id}/change_set/{change_set_id}:
    delete:
      description: Delete ChangeSet
      operationId: DeleteChangeSet
      parameters:
      - description: Unique identifier for a space_id
        in: path
        name: space_id
        required: true
        schema:
          format: uuid
          type: string
      - description: Unique identifier for a change_set_id
        in: path
        name: change_set_id
        required: true
        schema:
          format: uuid
          type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/DeleteResponse'
          description: Response for successful delete operation
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: ChangeSet request is invalid (Bad Request).
        "401":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Unauthorized access.
        "403":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Forbidden access.
        "404":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: ChangeSet not found.
        "409":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: ChangeSet data conflict. Data has changed since last read.
        "422":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: ChangeSet could not be deleted.
        "500":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Something went wrong while processing ChangeSet.
        default:
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Unexpected error.
      summary: Delete ChangeSet
      tags:
      - ChangeSet
    get:
      description: Get ChangeSet
      operationId: GetChangeSet
      parameters:
      - description: Unique identifier for a space_id
        in: path
        name: space_id
        required: true
        schema:
          format: uuid
          type: string
      - description: |-
          Include clause for expanding related entities in the response for ChangeSet.
          The attribute names are case-sensitive, PascalCase, and
          expected in a comma-separated list format as in the JSON encoding.

          Supported attributes for ChangeSet are EndTagID, OrganizationID, SpaceID, StartTagID.

          The whole string must be query-encoded.
        in: query
        name: include
        required: false
        schema:
          type: string
      - description: |-
          Select clause for specifying which fields to include in the response for ChangeSet.
          The attribute names are case-sensitive, PascalCase, and
          expected in a comma-separated list format as in the JSON encoding.
          If not specified, all fields are returned.
          Entity and parent IDs (like OrganizationID, SpaceID, ChangeSetID) and Slug are always returned regardless of the select parameter.
          Fields used in where and contains filters are also automatically included.
          Example: 'DisplayName,CreatedAt,Labels' will return only those fields plus the required ID and Slug fields.
          The whole string must be query-encoded.
        in: query
        name: select
        required: false
        schema:
          type: string
      - description: Unique identifier for a change_set_id
        in: path
        name: change_set_id
        required: true
        schema:
          format: uuid
          type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ExtendedChangeSet'
          description: OK
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: ChangeSet request is invalid (Bad Request).
        "401":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Unauthorized access.
        "403":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Forbidden access.
        "404":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: ChangeSet not found.
        "500":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Something went wrong while processing ChangeSet.
        default:
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Unexpected error.
      summary: Get ChangeSet
      tags:
      - ChangeSet
    patch:
      description: Patch ChangeSet
      operationId: PatchChangeSet
      parameters:
      - description: Unique identifier for a space_id
        in: path
        name: space_id
        required: true
        schema:
          format: uuid
          type: string
      - description: Unique identifier for a change_set_id
        in: path
        name: change_set_id
        required: true
        schema:
          format: uuid
          type: string
      requestBody:
        content:
          application/merge-patch+json:
            schema:
              description: JSON Merge Patch schema for ChangeSet. All fields are optional
                and nullable. Readonly fields are ignored.
              properties:
                Annotations:
                  additionalProperties:
                    nullable: true
                    type: string
                  description: An optional map of Annotation key/value pairs for tools
                    to attach information to entities.
                  nullable: true
                  type: object
                DeleteGates:
                  additionalProperties:
                    nullable: true
                    type: boolean
                  description: An optional set of gates that, if any is present, will
                    block deletion
                  nullable: true
                  type: object
                Description:
                  description: ""
                  nullable: true
                  type: string
                DisplayName:
                  description: Friendly name for the entity.
                  nullable: true
                  type: string
                Labels:
                  additionalProperties:
                    nullable: true
                    type: string
                  description: An optional map of Label key/value pairs to specify
                    identifying attributes of entities for the purpose of grouping
                    and filtering them.
                  nullable: true
                  type: object
                Slug:
                  description: Unique URL-safe identifier for the entity.
                  nullable: true
                  type: string
                Version:
                  description: An entity-specific sequence number used for optimistic
                    concurrency control. The value read must be sent in calls to Update.
                  nullable: true
                  type: integer
              type: object
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ChangeSet'
          description: Defines an entity changeset.
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: ChangeSet request is invalid (Bad Request).
        "401":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Unauthorized access.
        "403":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Forbidden access.
        "404":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: ChangeSet not found.
        "409":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: ChangeSet data conflict. Data has changed since last read.
        "500":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Something went wrong while processing ChangeSet.
        default:
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Unexpected error.
      summary: Patch ChangeSet
      tags:
      - ChangeSet
    put:
      description: Update ChangeSet
      operationId: UpdateChangeSet
      parameters:
      - description: Unique identifier for a space_id
        in: path
        name: space_id
        required: true
        schema:
          format: uuid
          type: string
      - description: Unique identifier for a change_set_id
        in: path
        name: change_set_id
        required: true
        schema:
          format: uuid
          type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ChangeSet'
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ChangeSet'
          description: Defines an entity changeset.
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: ChangeSet request is invalid (Bad Request).
        "401":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Unauthorized access.
        "403":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Forbidden access.
        "404":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: ChangeSet not found.
        "409":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: ChangeSet data conflict. Data has changed since last read.
        "500":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Something went wrong while processing ChangeSet.
        default:
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Unexpected error.
      summary: Update ChangeSet
      tags:
      - ChangeSet
  /space/{space_id}/filter:
    get:
      description: List Filters
      operationId: ListFilters
      parameters:
      - description: Unique identifier for a space_id
        in: path
        name: space_id
        required: true
        schema:
          format: uuid
          type: string
      - description: |-
          The specified string is an expression for the purpose of filtering
          the list of Filters returned. The expression syntax was inspired by SQL.
          It supports conjunctions using `AND` of relational expressions of the form *attribute*
          *operator* *attribute_or_literal*. The attribute names are case-sensitive and PascalCase,
          as in the JSON encoding.
          Strings support the following operators: `<`, `>`, `<=`, `>=`, `=`, `!=`, `LIKE`, `NOT LIKE`, `ILIKE`, `~~`, `!~~`, `~`, `~*`, `!~`, `!~*`, `IN`, `NOT IN`.
          String pattern operators: `LIKE` and `~~` for pattern matching with `%` and `_` wildcards,
          `ILIKE` for case-insensitive pattern matching, `NOT LIKE` and `!~~` for negated pattern matching.
          String regex operators: `~` for regex matching, `~*` for case-insensitive regex,
          `!~` and `!~*` for regex not matching (case-sensitive and insensitive).
          Integers support the following operators: `<`, `>`, `<=`, `>=`, `=`, `!=`, `IN`, `NOT IN`.
          UUIDs and boolean attributes support equality and inequality only.
          UUID and time literals must be quoted as string literals.
          String literals are quoted with single quotes, such as `'string'`.
          Time literals use the same form as when serialized as JSON,
          such as: `CreatedAt > '2025-02-18T23:16:34'`.
          Integer and boolean literals are also supported for attributes of those types.
          Arrays support the `?` operator to to match any element of the array,
          as in `ApprovedBy ? '7c61626f-ddbe-41af-93f6-b69f4ab6d308'`.
          Arrays can perform LEN() to check for length, as in `LEN(ApprovedBy) > 0`.
          Map support the dot notation to specify a particular map key, as in `Labels.tier = 'Backend'`.
          Maps support `IS NULL` and `IS NOT NULL` with dot notation to check for key absence or presence,
          as in `Labels.tier IS NULL` (key doesn't exist) or `Labels.tier IS NOT NULL` (key exists).
          Comparison results can be tested with `IS TRUE`, `IS FALSE`, `IS NOT TRUE`, and `IS NOT FALSE`.
          These are useful for nullable columns: `MergeSourceID = '<uuid>' IS NOT FALSE` matches rows where MergeSourceID equals the value OR is NULL.
          The `IN` and `NOT IN` operators accept a comma-separated list of values in parentheses,
          such as `Slug IN ('slugone', 'slugtwo')` or `Labels.environment IN ('prod', 'staging')`.
          Conjunctions are supported using the `AND` operator.
          An example conjunction is:
          `CreatedAt >= '2025-01-07' AND Slug = 'test' AND Labels.mykey = 'myvalue'`.

          Supported attributes for filtering on Filter: CreatedAt, DeleteGates, DisplayName, FilterID, From, FromSpaceID, Hash, Labels, OrganizationID, ResourceType, Slug, SpaceID, UpdatedAt, Where, WhereData.

          The whole string must be query-encoded.
        in: query
        name: where
        required: false
        schema:
          type: string
      - description: |-
          UUID of a Filter entity to apply to the Filter list.

          The Filter must be in the same Organization as the user credentials.

          The Filter's From field must match the entity type being filtered (Filter).

          For Space-resident entities, if the Filter has a FromSpaceID, it must match the operation's SpaceID.

          The Filter's Where clause will be combined with any explicit 'where' parameter using AND logic.

          If both 'filter' and 'where' parameters are specified, they are combined with AND logic.
        in: query
        name: filter
        required: false
        schema:
          type: string
      - description: |-
          Free text search that approximately matches the specified string against string fields and map keys/values.

          The search is case-insensitive and uses pattern matching to find entities containing the text.

          Searchable string fields include attributes like Slug, DisplayName, and string-typed custom fields.

          For map fields (like Labels and Annotations), the search matches both map keys and values.

          The search uses OR logic across all searchable fields, so matching any field will return the entity.

          If both 'where' and 'contains' parameters are specified, they are combined with AND logic.

          Searchable fields for Filter include string and map-type attributes from the queryable attributes list.

          The whole string must be query-encoded.
        in: query
        name: contains
        required: false
        schema:
          type: string
      - description: |-
          Include clause for expanding related entities in the response for Filter.
          The attribute names are case-sensitive, PascalCase, and
          expected in a comma-separated list format as in the JSON encoding.

          Supported attributes for Filter are FromSpaceID, OrganizationID, SpaceID.

          The whole string must be query-encoded.
        in: query
        name: include
        required: false
        schema:
          type: string
      - description: |-
          Select clause for specifying which fields to include in the response for Filter.
          The attribute names are case-sensitive, PascalCase, and
          expected in a comma-separated list format as in the JSON encoding.
          If not specified, all fields are returned.
          Entity and parent IDs (like OrganizationID, SpaceID, FilterID) and Slug are always returned regardless of the select parameter.
          Fields used in where and contains filters are also automatically included.
          Example: 'DisplayName,CreatedAt,Labels' will return only those fields plus the required ID and Slug fields.
          The whole string must be query-encoded.
        in: query
        name: select
        required: false
        schema:
          type: string
      - description: Entity type to filter for (e.g., Unit, Space). Must be specified
          together with 'id' parameter.
        in: query
        name: entity
        required: false
        schema:
          type: string
      - description: Entity ID to filter for. Must be specified together with 'entity'
          parameter.
        in: query
        name: id
        required: false
        schema:
          type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                items:
                  $ref: '#/components/schemas/ExtendedFilter'
                type: array
          description: OK
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Filter request is invalid (Bad Request).
        "401":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Unauthorized access.
        "403":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Forbidden access.
        "404":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Filter not found.
        "500":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Something went wrong while processing Filter.
        default:
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Unexpected error.
      summary: List Filters
      tags:
      - Filter
    post:
      description: Create Filter
      operationId: CreateFilter
      parameters:
      - description: Unique identifier for a space_id
        in: path
        name: space_id
        required: true
        schema:
          format: uuid
          type: string
      - description: Allowed values are true and false. Default is false. When true,
          reports success when an entity already exists and returns the existing entity
        in: query
        name: allow_exists
        required: false
        schema:
          type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/Filter'
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Filter'
          description: Defines an entity filter.
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Filter request is invalid (Bad Request).
        "401":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Unauthorized access.
        "403":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Forbidden access.
        "404":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Filter not found.
        "409":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Filter data conflict. Data has changed since last read.
        "500":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Something went wrong while processing Filter.
        default:
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Unexpected error.
      summary: Create Filter
      tags:
      - Filter
  /space/{space_id}/filter/{filter_id}:
    delete:
      description: Delete Filter
      operationId: DeleteFilter
      parameters:
      - description: Unique identifier for a space_id
        in: path
        name: space_id
        required: true
        schema:
          format: uuid
          type: string
      - description: Unique identifier for a filter_id
        in: path
        name: filter_id
        required: true
        schema:
          format: uuid
          type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/DeleteResponse'
          description: Response for successful delete operation
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Filter request is invalid (Bad Request).
        "401":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Unauthorized access.
        "403":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Forbidden access.
        "404":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Filter not found.
        "409":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Filter data conflict. Data has changed since last read.
        "422":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Filter could not be deleted.
        "500":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Something went wrong while processing Filter.
        default:
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Unexpected error.
      summary: Delete Filter
      tags:
      - Filter
    get:
      description: Get Filter
      operationId: GetFilter
      parameters:
      - description: Unique identifier for a space_id
        in: path
        name: space_id
        required: true
        schema:
          format: uuid
          type: string
      - description: |-
          Include clause for expanding related entities in the response for Filter.
          The attribute names are case-sensitive, PascalCase, and
          expected in a comma-separated list format as in the JSON encoding.

          Supported attributes for Filter are FromSpaceID, OrganizationID, SpaceID.

          The whole string must be query-encoded.
        in: query
        name: include
        required: false
        schema:
          type: string
      - description: |-
          Select clause for specifying which fields to include in the response for Filter.
          The attribute names are case-sensitive, PascalCase, and
          expected in a comma-separated list format as in the JSON encoding.
          If not specified, all fields are returned.
          Entity and parent IDs (like OrganizationID, SpaceID, FilterID) and Slug are always returned regardless of the select parameter.
          Fields used in where and contains filters are also automatically included.
          Example: 'DisplayName,CreatedAt,Labels' will return only those fields plus the required ID and Slug fields.
          The whole string must be query-encoded.
        in: query
        name: select
        required: false
        schema:
          type: string
      - description: Unique identifier for a filter_id
        in: path
        name: filter_id
        required: true
        schema:
          format: uuid
          type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ExtendedFilter'
          description: OK
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Filter request is invalid (Bad Request).
        "401":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Unauthorized access.
        "403":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Forbidden access.
        "404":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Filter not found.
        "500":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Something went wrong while processing Filter.
        default:
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Unexpected error.
      summary: Get Filter
      tags:
      - Filter
    patch:
      description: Patch Filter
      operationId: PatchFilter
      parameters:
      - description: Unique identifier for a space_id
        in: path
        name: space_id
        required: true
        schema:
          format: uuid
          type: string
      - description: Unique identifier for a filter_id
        in: path
        name: filter_id
        required: true
        schema:
          format: uuid
          type: string
      requestBody:
        content:
          application/merge-patch+json:
            schema:
              description: JSON Merge Patch schema for Filter. All fields are optional
                and nullable. Readonly fields are ignored.
              properties:
                Annotations:
                  additionalProperties:
                    nullable: true
                    type: string
                  description: An optional map of Annotation key/value pairs for tools
                    to attach information to entities.
                  nullable: true
                  type: object
                DeleteGates:
                  additionalProperties:
                    nullable: true
                    type: boolean
                  description: An optional set of gates that, if any is present, will
                    block deletion
                  nullable: true
                  type: object
                DisplayName:
                  description: Friendly name for the entity.
                  nullable: true
                  type: string
                From:
                  description: ""
                  nullable: true
                  type: string
                FromSpaceID:
                  description: ""
                  format: uuid
                  nullable: true
                  type: string
                Labels:
                  additionalProperties:
                    nullable: true
                    type: string
                  description: An optional map of Label key/value pairs to specify
                    identifying attributes of entities for the purpose of grouping
                    and filtering them.
                  nullable: true
                  type: object
                ResourceType:
                  description: ""
                  nullable: true
                  type: string
                Slug:
                  description: Unique URL-safe identifier for the entity.
                  nullable: true
                  type: string
                Version:
                  description: An entity-specific sequence number used for optimistic
                    concurrency control. The value read must be sent in calls to Update.
                  nullable: true
                  type: integer
                Where:
                  description: ""
                  nullable: true
                  type: string
                WhereData:
                  description: ""
                  nullable: true
                  type: string
              type: object
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Filter'
          description: Defines an entity filter.
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Filter request is invalid (Bad Request).
        "401":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Unauthorized access.
        "403":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Forbidden access.
        "404":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Filter not found.
        "409":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Filter data conflict. Data has changed since last read.
        "500":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Something went wrong while processing Filter.
        default:
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Unexpected error.
      summary: Patch Filter
      tags:
      - Filter
    put:
      description: Update Filter
      operationId: UpdateFilter
      parameters:
      - description: Unique identifier for a space_id
        in: path
        name: space_id
        required: true
        schema:
          format: uuid
          type: string
      - description: Unique identifier for a filter_id
        in: path
        name: filter_id
        required: true
        schema:
          format: uuid
          type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/Filter'
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Filter'
          description: Defines an entity filter.
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Filter request is invalid (Bad Request).
        "401":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Unauthorized access.
        "403":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Forbidden access.
        "404":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Filter not found.
        "409":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Filter data conflict. Data has changed since last read.
        "500":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Something went wrong while processing Filter.
        default:
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Unexpected error.
      summary: Update Filter
      tags:
      - Filter
  /space/{space_id}/function:
    get:
      description: List retrieves a map of Functions
      operationId: ListFunctions
      parameters:
      - description: Unique identifier for a space_id
        in: path
        name: space_id
        required: true
        schema:
          format: uuid
          type: string
      - description: 'Type of entity used to identify the worker whose functions should
          be listed: unit, target, or worker'
        in: query
        name: entity
        required: false
        schema:
          type: string
      - description: ID of the entity used to identify the worker whose functions
          should be listed
        in: query
        name: id
        required: false
        schema:
          type: string
      - description: |-
          The specified string is an expression for the purpose of filtering
          the list of FunctionSignatures returned. The expression syntax was inspired by SQL.
          It supports conjunctions using `AND` of relational expressions of the form *attribute*
          *operator* *attribute_or_literal*. The attribute names are case-sensitive and PascalCase,
          as in the JSON encoding.
          Strings support the following operators: `<`, `>`, `<=`, `>=`, `=`, `!=`, `LIKE`, `NOT LIKE`, `ILIKE`, `~~`, `!~~`, `~`, `~*`, `!~`, `!~*`, `IN`, `NOT IN`.
          String pattern operators: `LIKE` and `~~` for pattern matching with `%` and `_` wildcards,
          `ILIKE` for case-insensitive pattern matching, `NOT LIKE` and `!~~` for negated pattern matching.
          String regex operators: `~` for regex matching, `~*` for case-insensitive regex,
          `!~` and `!~*` for regex not matching (case-sensitive and insensitive).
          Integers support the following operators: `<`, `>`, `<=`, `>=`, `=`, `!=`, `IN`, `NOT IN`.
          UUIDs and boolean attributes support equality and inequality only.
          UUID and time literals must be quoted as string literals.
          String literals are quoted with single quotes, such as `'string'`.
          Time literals use the same form as when serialized as JSON,
          such as: `CreatedAt > '2025-02-18T23:16:34'`.
          Integer and boolean literals are also supported for attributes of those types.
          Arrays support the `?` operator to to match any element of the array,
          as in `ApprovedBy ? '7c61626f-ddbe-41af-93f6-b69f4ab6d308'`.
          Arrays can perform LEN() to check for length, as in `LEN(ApprovedBy) > 0`.
          Map support the dot notation to specify a particular map key, as in `Labels.tier = 'Backend'`.
          Maps support `IS NULL` and `IS NOT NULL` with dot notation to check for key absence or presence,
          as in `Labels.tier IS NULL` (key doesn't exist) or `Labels.tier IS NOT NULL` (key exists).
          Comparison results can be tested with `IS TRUE`, `IS FALSE`, `IS NOT TRUE`, and `IS NOT FALSE`.
          These are useful for nullable columns: `MergeSourceID = '<uuid>' IS NOT FALSE` matches rows where MergeSourceID equals the value OR is NULL.
          The `IN` and `NOT IN` operators accept a comma-separated list of values in parentheses,
          such as `Slug IN ('slugone', 'slugtwo')` or `Labels.environment IN ('prod', 'staging')`.
          Conjunctions are supported using the `AND` operator.
          An example conjunction is:
          `CreatedAt >= '2025-01-07' AND Slug = 'test' AND Labels.mykey = 'myvalue'`.

          Supported attributes for filtering on FunctionSignature: AttributeName, Description, FunctionName, FunctionType, Hermetic, Idempotent, Mutating, OutputInfo.Description, OutputInfo.OutputType, OutputInfo.ResultName, RequiredParameters, ToolchainType, Validating, VarArgs.

          The whole string must be query-encoded.
        in: query
        name: where
        required: false
        schema:
          type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                additionalProperties:
                  additionalProperties:
                    $ref: '#/components/schemas/FunctionSignature'
                  type: object
                type: object
          description: OK
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Function request is invalid (Bad Request).
        "401":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Unauthorized access.
        "403":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Forbidden access.
        "404":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Function not found.
        "500":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Something went wrong while processing Function.
        default:
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Unexpected error.
      summary: List retrieves a map of Functions
      tags:
      - Function
  /space/{space_id}/function/invoke:
    post:
      description: Invoke Functions
      operationId: InvokeFunctions
      parameters:
      - description: Unique identifier for a space_id
        in: path
        name: space_id
        required: true
        schema:
          format: uuid
          type: string
      - description: Unit ID of the Revision to invoke functions on
        in: query
        name: unit_id
        required: false
        schema:
          format: uuid
          type: string
      - description: Revision ID to invoke functions on instead of units
        in: query
        name: revision_id
        required: false
        schema:
          format: uuid
          type: string
      - description: 'Dry run mode: when true, skip updating configuration data even
          if it changed'
        in: query
        name: dry_run
        required: false
        schema:
          type: string
      - description: Must match ChangeSetID of affected Units unless in dry run mode;
          not valid when invoked on Revisions
        in: query
        name: change_set_id
        required: false
        schema:
          format: uuid
          type: string
      - description: User-defined category for the Mutation. Must be alphanumeric,
          at most 64 characters. The prefix 'ConfigHub' is reserved.
        in: query
        name: subgroup
        required: false
        schema:
          type: string
      - description: 'Source of additional configuration data to pass to functions
          that need it (e.g., vet-immutable). Supports named revision specifiers:
          LiveRevisionNum, LastAppliedRevisionNum, PreviousLiveRevisionNum, HeadRevisionNum.
          Can be prefixed with ''Before:'' (e.g., Before:HeadRevisionNum). May be
          repeated for multiple sources.'
        in: query
        name: other_data_source
        required: false
        schema:
          type: string
      - description: |-
          The specified string is an expression for the purpose of filtering
          the list of Units returned. The expression syntax was inspired by SQL.
          It supports conjunctions using `AND` of relational expressions of the form *attribute*
          *operator* *attribute_or_literal*. The attribute names are case-sensitive and PascalCase,
          as in the JSON encoding.
          Strings support the following operators: `<`, `>`, `<=`, `>=`, `=`, `!=`, `LIKE`, `NOT LIKE`, `ILIKE`, `~~`, `!~~`, `~`, `~*`, `!~`, `!~*`, `IN`, `NOT IN`.
          String pattern operators: `LIKE` and `~~` for pattern matching with `%` and `_` wildcards,
          `ILIKE` for case-insensitive pattern matching, `NOT LIKE` and `!~~` for negated pattern matching.
          String regex operators: `~` for regex matching, `~*` for case-insensitive regex,
          `!~` and `!~*` for regex not matching (case-sensitive and insensitive).
          Integers support the following operators: `<`, `>`, `<=`, `>=`, `=`, `!=`, `IN`, `NOT IN`.
          UUIDs and boolean attributes support equality and inequality only.
          UUID and time literals must be quoted as string literals.
          String literals are quoted with single quotes, such as `'string'`.
          Time literals use the same form as when serialized as JSON,
          such as: `CreatedAt > '2025-02-18T23:16:34'`.
          Integer and boolean literals are also supported for attributes of those types.
          Arrays support the `?` operator to to match any element of the array,
          as in `ApprovedBy ? '7c61626f-ddbe-41af-93f6-b69f4ab6d308'`.
          Arrays can perform LEN() to check for length, as in `LEN(ApprovedBy) > 0`.
          Map support the dot notation to specify a particular map key, as in `Labels.tier = 'Backend'`.
          Maps support `IS NULL` and `IS NOT NULL` with dot notation to check for key absence or presence,
          as in `Labels.tier IS NULL` (key doesn't exist) or `Labels.tier IS NOT NULL` (key exists).
          Comparison results can be tested with `IS TRUE`, `IS FALSE`, `IS NOT TRUE`, and `IS NOT FALSE`.
          These are useful for nullable columns: `MergeSourceID = '<uuid>' IS NOT FALSE` matches rows where MergeSourceID equals the value OR is NULL.
          The `IN` and `NOT IN` operators accept a comma-separated list of values in parentheses,
          such as `Slug IN ('slugone', 'slugtwo')` or `Labels.environment IN ('prod', 'staging')`.
          Conjunctions are supported using the `AND` operator.
          An example conjunction is:
          `CreatedAt >= '2025-01-07' AND Slug = 'test' AND Labels.mykey = 'myvalue'`.

          Supported attributes for filtering on Unit: ApplyGates, ApplyWarnings, ApprovedBy, BridgeWorkerID, ChangeSetID, CreatedAt, DataHash, DeleteGates, DestroyGates, DisplayName, DriftReconciliationMode, FromLinkID, HeadRevisionNum, HeadUnitActionNum, HeadUnitEventNum, Labels, LastActionAt, LastAppliedRevisionNum, LastChangeDescription, LiveRevisionNum, OrganizationID, PreviousLiveRevisionNum, ProviderType, Slug, SpaceID, TargetID, TargetOptions, ToolchainType, UnitID, UpdatedAt, UpstreamOrganizationID, UpstreamRevisionNum, UpstreamSpaceID, UpstreamUnitID, Values.

          Finding all units created by cloning can be done using the expression `UpstreamRevisionNum > 0`. Clones of a specific unit can be found by additionally filtering based on `UpstreamUnitID`. Unapplied units can be found using `LiveRevisionNum = 0`. Units with unapplied changes can be found with `HeadRevisionNum > LiveRevisionNum`.

          The whole string must be query-encoded.
        in: query
        name: where
        required: false
        schema:
          type: string
      - description: |-
          UUID of a Filter entity to apply to the Unit list.

          The Filter must be in the same Organization as the user credentials.

          The Filter's From field must match the entity type being filtered (Unit).

          For Space-resident entities, if the Filter has a FromSpaceID, it must match the operation's SpaceID.

          The Filter's Where clause will be combined with any explicit 'where' parameter using AND logic.

          If both 'filter' and 'where' parameters are specified, they are combined with AND logic.
        in: query
        name: filter
        required: false
        schema:
          type: string
      - description: 'Resource type: Resource type to match for the desired ToolchainType,
          for example apps/v1/Deployment'
        in: query
        name: resource_type
        required: false
        schema:
          type: string
      - description: 'Where data: The specified string is an expression for the purpose
          of evaluating whether the configuration data matches the filter. It supports
          conjunctions using `AND` of relational expressions of the form *path* *operator*
          *literal*. The path specifications are dot-separated, for both map fields
          and array indices, as in `spec.template.spec.containers.0.image = ''ghcr.io/headlamp-k8s/headlamp:latest''
          AND spec.replicas > 1`. Path expressions support `*` for wildcard array
          or map segments and `?key=value` syntax for associative matches of array
          elements containing objects with a `key` attribute. Strings support the
          following operators: `<`, `>`, `<=`, `>=`, `=`, `!=`, `LIKE`, `ILIKE`, `~~`,
          `!~~`, `~`, `!~`, `~*`, `!~*`, `IN`, `NOT IN`. String pattern operators:
          `LIKE` and `~~` for pattern matching with `%` and `_` wildcards, `ILIKE`
          for case-insensitive pattern matching, `!~~` for NOT LIKE. String regex
          operators: `~` for regex matching, `~*` for case-insensitive regex, `!~`
          and `!~*` for regex not matching (case-sensitive and insensitive). Integers
          support the following operators: `<`, `>`, `<=`, `>=`, `=`, `!=`, `IN`,
          `NOT IN`. Boolean values support equality and inequality only. The `IN`
          and `NOT IN` operators accept a comma-separated list of values in parentheses,
          such as `spec.template.spec.containers.0.image#reference IN ('':latest'',
          '':arm64-latest'')`. The syntax `.|` requires the preceding path to exist;
          otherwise the relation `!=` will always return true regardless what it is
          compared with. String literals are quoted with single quotes, such as `''string''`.
          Integer and boolean literals are also supported for attributes of those
          types. The whole string must be query-encoded.'
        in: query
        name: where_data
        required: false
        schema:
          type: string
      - description: 'Where expression to match Triggers. Matched triggers are invoked
          on each unit to filter by validation results. Use with triggers_passed to
          control whether passing or failing units are returned (default: failing).'
        in: query
        name: where_trigger
        required: false
        schema:
          type: string
      - description: Filter UUID (with From=Trigger). The filter's matching triggers
          are invoked on units to filter by validation results. Can be combined with
          where_trigger.
        in: query
        name: trigger_filter
        required: false
        schema:
          type: string
      - description: When true, return units that pass trigger validation; when false
          (default), return units that fail. Only applies when where_trigger or trigger_filter
          is specified.
        in: query
        name: triggers_passed
        required: false
        schema:
          type: boolean
      - description: View slug or UUID. Applies the View's column definitions to extract
          values for each unit. If the View has a FilterID, its filter is ANDed with
          other filters. The View must have Of=Unit or a Filter with From=Unit.
        in: query
        name: view
        required: false
        schema:
          type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/FunctionInvocationsRequest'
      responses:
        "200":
          content:
            application/json:
              schema:
                items:
                  $ref: '#/components/schemas/FunctionInvocationsResponse'
                type: array
          description: OK
        "207":
          content:
            application/json:
              schema:
                items:
                  $ref: '#/components/schemas/FunctionInvocationsResponse'
                type: array
          description: 'Multi-Status: Mixed success and failure results'
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Function request is invalid (Bad Request).
        "401":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Unauthorized access.
        "403":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Forbidden access.
        "404":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Function not found.
        "409":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Function data conflict. Data has changed since last read.
        "422":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Unable to process configuration data
        "429":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Bridge worker is overloaded with outstanding function invocations
        "500":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Something went wrong while processing Function.
        "503":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Bridge worker is unavailable; one or more function sources
            were not executed
        "504":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Timed out waiting for the bridge worker to return a function
            result
        default:
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Unexpected error.
      summary: Invoke Functions
      tags:
      - Function
  /space/{space_id}/invocation:
    get:
      description: List Invocations
      operationId: ListInvocations
      parameters:
      - description: Unique identifier for a space_id
        in: path
        name: space_id
        required: true
        schema:
          format: uuid
          type: string
      - description: |-
          The specified string is an expression for the purpose of filtering
          the list of Invocations returned. The expression syntax was inspired by SQL.
          It supports conjunctions using `AND` of relational expressions of the form *attribute*
          *operator* *attribute_or_literal*. The attribute names are case-sensitive and PascalCase,
          as in the JSON encoding.
          Strings support the following operators: `<`, `>`, `<=`, `>=`, `=`, `!=`, `LIKE`, `NOT LIKE`, `ILIKE`, `~~`, `!~~`, `~`, `~*`, `!~`, `!~*`, `IN`, `NOT IN`.
          String pattern operators: `LIKE` and `~~` for pattern matching with `%` and `_` wildcards,
          `ILIKE` for case-insensitive pattern matching, `NOT LIKE` and `!~~` for negated pattern matching.
          String regex operators: `~` for regex matching, `~*` for case-insensitive regex,
          `!~` and `!~*` for regex not matching (case-sensitive and insensitive).
          Integers support the following operators: `<`, `>`, `<=`, `>=`, `=`, `!=`, `IN`, `NOT IN`.
          UUIDs and boolean attributes support equality and inequality only.
          UUID and time literals must be quoted as string literals.
          String literals are quoted with single quotes, such as `'string'`.
          Time literals use the same form as when serialized as JSON,
          such as: `CreatedAt > '2025-02-18T23:16:34'`.
          Integer and boolean literals are also supported for attributes of those types.
          Arrays support the `?` operator to to match any element of the array,
          as in `ApprovedBy ? '7c61626f-ddbe-41af-93f6-b69f4ab6d308'`.
          Arrays can perform LEN() to check for length, as in `LEN(ApprovedBy) > 0`.
          Map support the dot notation to specify a particular map key, as in `Labels.tier = 'Backend'`.
          Maps support `IS NULL` and `IS NOT NULL` with dot notation to check for key absence or presence,
          as in `Labels.tier IS NULL` (key doesn't exist) or `Labels.tier IS NOT NULL` (key exists).
          Comparison results can be tested with `IS TRUE`, `IS FALSE`, `IS NOT TRUE`, and `IS NOT FALSE`.
          These are useful for nullable columns: `MergeSourceID = '<uuid>' IS NOT FALSE` matches rows where MergeSourceID equals the value OR is NULL.
          The `IN` and `NOT IN` operators accept a comma-separated list of values in parentheses,
          such as `Slug IN ('slugone', 'slugtwo')` or `Labels.environment IN ('prod', 'staging')`.
          Conjunctions are supported using the `AND` operator.
          An example conjunction is:
          `CreatedAt >= '2025-01-07' AND Slug = 'test' AND Labels.mykey = 'myvalue'`.

          Supported attributes for filtering on Invocation: BridgeWorkerID, CreatedAt, DeleteGates, DisplayName, FunctionName, Hash, InvocationID, Labels, OrganizationID, Slug, SpaceID, ToolchainType, UpdatedAt.

          The whole string must be query-encoded.
        in: query
        name: where
        required: false
        schema:
          type: string
      - description: |-
          UUID of a Filter entity to apply to the Invocation list.

          The Filter must be in the same Organization as the user credentials.

          The Filter's From field must match the entity type being filtered (Invocation).

          For Space-resident entities, if the Filter has a FromSpaceID, it must match the operation's SpaceID.

          The Filter's Where clause will be combined with any explicit 'where' parameter using AND logic.

          If both 'filter' and 'where' parameters are specified, they are combined with AND logic.
        in: query
        name: filter
        required: false
        schema:
          type: string
      - description: |-
          Free text search that approximately matches the specified string against string fields and map keys/values.

          The search is case-insensitive and uses pattern matching to find entities containing the text.

          Searchable string fields include attributes like Slug, DisplayName, and string-typed custom fields.

          For map fields (like Labels and Annotations), the search matches both map keys and values.

          The search uses OR logic across all searchable fields, so matching any field will return the entity.

          If both 'where' and 'contains' parameters are specified, they are combined with AND logic.

          Searchable fields for Invocation include string and map-type attributes from the queryable attributes list.

          The whole string must be query-encoded.
        in: query
        name: contains
        required: false
        schema:
          type: string
      - description: |-
          Include clause for expanding related entities in the response for Invocation.
          The attribute names are case-sensitive, PascalCase, and
          expected in a comma-separated list format as in the JSON encoding.

          Supported attributes for Invocation are BridgeWorkerID, OrganizationID, SpaceID.

          The whole string must be query-encoded.
        in: query
        name: include
        required: false
        schema:
          type: string
      - description: |-
          Select clause for specifying which fields to include in the response for Invocation.
          The attribute names are case-sensitive, PascalCase, and
          expected in a comma-separated list format as in the JSON encoding.
          If not specified, all fields are returned.
          Entity and parent IDs (like OrganizationID, SpaceID, InvocationID) and Slug are always returned regardless of the select parameter.
          Fields used in where and contains filters are also automatically included.
          Example: 'DisplayName,CreatedAt,Labels' will return only those fields plus the required ID and Slug fields.
          The whole string must be query-encoded.
        in: query
        name: select
        required: false
        schema:
          type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                items:
                  $ref: '#/components/schemas/ExtendedInvocation'
                type: array
          description: OK
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Invocation request is invalid (Bad Request).
        "401":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Unauthorized access.
        "403":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Forbidden access.
        "404":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Invocation not found.
        "500":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Something went wrong while processing Invocation.
        default:
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Unexpected error.
      summary: List Invocations
      tags:
      - Invocation
    post:
      description: Create Invocation
      operationId: CreateInvocation
      parameters:
      - description: Unique identifier for a space_id
        in: path
        name: space_id
        required: true
        schema:
          format: uuid
          type: string
      - description: Allowed values are true and false. Default is false. When true,
          reports success when an entity already exists and returns the existing entity
        in: query
        name: allow_exists
        required: false
        schema:
          type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/Invocation'
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Invocation'
          description: Defines a function invocation.
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Invocation request is invalid (Bad Request).
        "401":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Unauthorized access.
        "403":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Forbidden access.
        "404":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Invocation not found.
        "409":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Invocation data conflict. Data has changed since last read.
        "500":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Something went wrong while processing Invocation.
        default:
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Unexpected error.
      summary: Create Invocation
      tags:
      - Invocation
  /space/{space_id}/invocation/{invocation_id}:
    delete:
      description: Delete Invocation
      operationId: DeleteInvocation
      parameters:
      - description: Unique identifier for a space_id
        in: path
        name: space_id
        required: true
        schema:
          format: uuid
          type: string
      - description: Unique identifier for a invocation_id
        in: path
        name: invocation_id
        required: true
        schema:
          format: uuid
          type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/DeleteResponse'
          description: Response for successful delete operation
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Invocation request is invalid (Bad Request).
        "401":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Unauthorized access.
        "403":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Forbidden access.
        "404":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Invocation not found.
        "409":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Invocation data conflict. Data has changed since last read.
        "422":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Invocation could not be deleted.
        "500":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Something went wrong while processing Invocation.
        default:
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Unexpected error.
      summary: Delete Invocation
      tags:
      - Invocation
    get:
      description: Get Invocation
      operationId: GetInvocation
      parameters:
      - description: Unique identifier for a space_id
        in: path
        name: space_id
        required: true
        schema:
          format: uuid
          type: string
      - description: |-
          Include clause for expanding related entities in the response for Invocation.
          The attribute names are case-sensitive, PascalCase, and
          expected in a comma-separated list format as in the JSON encoding.

          Supported attributes for Invocation are BridgeWorkerID, OrganizationID, SpaceID.

          The whole string must be query-encoded.
        in: query
        name: include
        required: false
        schema:
          type: string
      - description: |-
          Select clause for specifying which fields to include in the response for Invocation.
          The attribute names are case-sensitive, PascalCase, and
          expected in a comma-separated list format as in the JSON encoding.
          If not specified, all fields are returned.
          Entity and parent IDs (like OrganizationID, SpaceID, InvocationID) and Slug are always returned regardless of the select parameter.
          Fields used in where and contains filters are also automatically included.
          Example: 'DisplayName,CreatedAt,Labels' will return only those fields plus the required ID and Slug fields.
          The whole string must be query-encoded.
        in: query
        name: select
        required: false
        schema:
          type: string
      - description: Unique identifier for a invocation_id
        in: path
        name: invocation_id
        required: true
        schema:
          format: uuid
          type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ExtendedInvocation'
          description: OK
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Invocation request is invalid (Bad Request).
        "401":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Unauthorized access.
        "403":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Forbidden access.
        "404":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Invocation not found.
        "500":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Something went wrong while processing Invocation.
        default:
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Unexpected error.
      summary: Get Invocation
      tags:
      - Invocation
    patch:
      description: Patch Invocation
      operationId: PatchInvocation
      parameters:
      - description: Unique identifier for a space_id
        in: path
        name: space_id
        required: true
        schema:
          format: uuid
          type: string
      - description: Unique identifier for a invocation_id
        in: path
        name: invocation_id
        required: true
        schema:
          format: uuid
          type: string
      requestBody:
        content:
          application/merge-patch+json:
            schema:
              description: JSON Merge Patch schema for Invocation. All fields are
                optional and nullable. Readonly fields are ignored.
              properties:
                Annotations:
                  additionalProperties:
                    nullable: true
                    type: string
                  description: An optional map of Annotation key/value pairs for tools
                    to attach information to entities.
                  nullable: true
                  type: object
                Arguments:
                  description: Function arguments
                  items:
                    nullable: true
                    type: object
                  nullable: true
                  type: array
                BridgeWorkerID:
                  description: ""
                  format: uuid
                  nullable: true
                  type: string
                DeleteGates:
                  additionalProperties:
                    nullable: true
                    type: boolean
                  description: An optional set of gates that, if any is present, will
                    block deletion
                  nullable: true
                  type: object
                DisplayName:
                  description: Friendly name for the entity.
                  nullable: true
                  type: string
                FunctionName:
                  description: Function name
                  nullable: true
                  type: string
                Labels:
                  additionalProperties:
                    nullable: true
                    type: string
                  description: An optional map of Label key/value pairs to specify
                    identifying attributes of entities for the purpose of grouping
                    and filtering them.
                  nullable: true
                  type: object
                Slug:
                  description: Unique URL-safe identifier for the entity.
                  nullable: true
                  type: string
                ToolchainType:
                  description: ""
                  nullable: true
                  type: string
                Version:
                  description: An entity-specific sequence number used for optimistic
                    concurrency control. The value read must be sent in calls to Update.
                  nullable: true
                  type: integer
              type: object
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Invocation'
          description: Defines a function invocation.
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Invocation request is invalid (Bad Request).
        "401":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Unauthorized access.
        "403":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Forbidden access.
        "404":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Invocation not found.
        "409":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Invocation data conflict. Data has changed since last read.
        "500":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Something went wrong while processing Invocation.
        default:
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Unexpected error.
      summary: Patch Invocation
      tags:
      - Invocation
    put:
      description: Update Invocation
      operationId: UpdateInvocation
      parameters:
      - description: Unique identifier for a space_id
        in: path
        name: space_id
        required: true
        schema:
          format: uuid
          type: string
      - description: Unique identifier for a invocation_id
        in: path
        name: invocation_id
        required: true
        schema:
          format: uuid
          type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/Invocation'
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Invocation'
          description: Defines a function invocation.
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Invocation request is invalid (Bad Request).
        "401":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Unauthorized access.
        "403":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Forbidden access.
        "404":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Invocation not found.
        "409":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Invocation data conflict. Data has changed since last read.
        "500":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Something went wrong while processing Invocation.
        default:
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Unexpected error.
      summary: Update Invocation
      tags:
      - Invocation
  /space/{space_id}/link:
    get:
      description: List Links
      operationId: ListLinks
      parameters:
      - description: Unique identifier for a space_id
        in: path
        name: space_id
        required: true
        schema:
          format: uuid
          type: string
      - description: |-
          The specified string is an expression for the purpose of filtering
          the list of Links returned. The expression syntax was inspired by SQL.
          It supports conjunctions using `AND` of relational expressions of the form *attribute*
          *operator* *attribute_or_literal*. The attribute names are case-sensitive and PascalCase,
          as in the JSON encoding.
          Strings support the following operators: `<`, `>`, `<=`, `>=`, `=`, `!=`, `LIKE`, `NOT LIKE`, `ILIKE`, `~~`, `!~~`, `~`, `~*`, `!~`, `!~*`, `IN`, `NOT IN`.
          String pattern operators: `LIKE` and `~~` for pattern matching with `%` and `_` wildcards,
          `ILIKE` for case-insensitive pattern matching, `NOT LIKE` and `!~~` for negated pattern matching.
          String regex operators: `~` for regex matching, `~*` for case-insensitive regex,
          `!~` and `!~*` for regex not matching (case-sensitive and insensitive).
          Integers support the following operators: `<`, `>`, `<=`, `>=`, `=`, `!=`, `IN`, `NOT IN`.
          UUIDs and boolean attributes support equality and inequality only.
          UUID and time literals must be quoted as string literals.
          String literals are quoted with single quotes, such as `'string'`.
          Time literals use the same form as when serialized as JSON,
          such as: `CreatedAt > '2025-02-18T23:16:34'`.
          Integer and boolean literals are also supported for attributes of those types.
          Arrays support the `?` operator to to match any element of the array,
          as in `ApprovedBy ? '7c61626f-ddbe-41af-93f6-b69f4ab6d308'`.
          Arrays can perform LEN() to check for length, as in `LEN(ApprovedBy) > 0`.
          Map support the dot notation to specify a particular map key, as in `Labels.tier = 'Backend'`.
          Maps support `IS NULL` and `IS NOT NULL` with dot notation to check for key absence or presence,
          as in `Labels.tier IS NULL` (key doesn't exist) or `Labels.tier IS NOT NULL` (key exists).
          Comparison results can be tested with `IS TRUE`, `IS FALSE`, `IS NOT TRUE`, and `IS NOT FALSE`.
          These are useful for nullable columns: `MergeSourceID = '<uuid>' IS NOT FALSE` matches rows where MergeSourceID equals the value OR is NULL.
          The `IN` and `NOT IN` operators accept a comma-separated list of values in parentheses,
          such as `Slug IN ('slugone', 'slugtwo')` or `Labels.environment IN ('prod', 'staging')`.
          Conjunctions are supported using the `AND` operator.
          An example conjunction is:
          `CreatedAt >= '2025-01-07' AND Slug = 'test' AND Labels.mykey = 'myvalue'`.

          Supported attributes for filtering on Link: AutoUpdate, CreatedAt, DeleteGates, DisplayName, DownstreamLastMergedRevisionNum, FromUnitID, Labels, LinkID, OrganizationID, Slug, SpaceID, ToSpaceID, ToUnitID, UpdateType, UpdatedAt, UpstreamLastMergedRevisionNum, UpstreamLinkID, UpstreamOrganizationID, UpstreamSpaceID, UseLiveState.

          The whole string must be query-encoded.
        in: query
        name: where
        required: false
        schema:
          type: string
      - description: |-
          UUID of a Filter entity to apply to the Link list.

          The Filter must be in the same Organization as the user credentials.

          The Filter's From field must match the entity type being filtered (Link).

          For Space-resident entities, if the Filter has a FromSpaceID, it must match the operation's SpaceID.

          The Filter's Where clause will be combined with any explicit 'where' parameter using AND logic.

          If both 'filter' and 'where' parameters are specified, they are combined with AND logic.
        in: query
        name: filter
        required: false
        schema:
          type: string
      - description: |-
          Free text search that approximately matches the specified string against string fields and map keys/values.

          The search is case-insensitive and uses pattern matching to find entities containing the text.

          Searchable string fields include attributes like Slug, DisplayName, and string-typed custom fields.

          For map fields (like Labels and Annotations), the search matches both map keys and values.

          The search uses OR logic across all searchable fields, so matching any field will return the entity.

          If both 'where' and 'contains' parameters are specified, they are combined with AND logic.

          Searchable fields for Link include string and map-type attributes from the queryable attributes list.

          The whole string must be query-encoded.
        in: query
        name: contains
        required: false
        schema:
          type: string
      - description: |-
          Include clause for expanding related entities in the response for Link.
          The attribute names are case-sensitive, PascalCase, and
          expected in a comma-separated list format as in the JSON encoding.

          Supported attributes for Link are FromUnitID, OrganizationID, SpaceID, ToSpaceID, ToUnitID.

          The whole string must be query-encoded.
        in: query
        name: include
        required: false
        schema:
          type: string
      - description: |-
          Select clause for specifying which fields to include in the response for Link.
          The attribute names are case-sensitive, PascalCase, and
          expected in a comma-separated list format as in the JSON encoding.
          If not specified, all fields are returned.
          Entity and parent IDs (like OrganizationID, SpaceID, LinkID) and Slug are always returned regardless of the select parameter.
          Fields used in where and contains filters are also automatically included.
          Example: 'DisplayName,CreatedAt,Labels' will return only those fields plus the required ID and Slug fields.
          The whole string must be query-encoded.
        in: query
        name: select
        required: false
        schema:
          type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                items:
                  $ref: '#/components/schemas/ExtendedLink'
                type: array
          description: OK
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Link request is invalid (Bad Request).
        "401":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Unauthorized access.
        "403":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Forbidden access.
        "404":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Link not found.
        "500":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Something went wrong while processing Link.
        default:
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Unexpected error.
      summary: List Links
      tags:
      - Link
    post:
      description: Create Link
      operationId: CreateLink
      parameters:
      - description: Unique identifier for a space_id
        in: path
        name: space_id
        required: true
        schema:
          format: uuid
          type: string
      - description: Allowed values are true and false. Default is false. When true,
          reports success when an entity already exists and returns the existing entity
        in: query
        name: allow_exists
        required: false
        schema:
          type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/Link'
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Link'
          description: |-
            Link connects two config Units in a dependency / producer-consumer relationship.
            A Link indicates that selected config data from the upstream To Unit (the producer)
            should be propagated to the downstream From Unit (the consumer).
            Links must be created in the same Space as the From Unit.
            They also imply an ordering when Applied or Destroyed as a group.
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Link request is invalid (Bad Request).
        "401":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Unauthorized access.
        "403":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Forbidden access.
        "404":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Link not found.
        "409":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Link data conflict. Data has changed since last read.
        "500":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Something went wrong while processing Link.
        default:
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Unexpected error.
      summary: Create Link
      tags:
      - Link
  /space/{space_id}/link/{link_id}:
    delete:
      description: Delete Link
      operationId: DeleteLink
      parameters:
      - description: Unique identifier for a space_id
        in: path
        name: space_id
        required: true
        schema:
          format: uuid
          type: string
      - description: Unique identifier for a link_id
        in: path
        name: link_id
        required: true
        schema:
          format: uuid
          type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/DeleteResponse'
          description: Response for successful delete operation
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Link request is invalid (Bad Request).
        "401":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Unauthorized access.
        "403":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Forbidden access.
        "404":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Link not found.
        "409":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Link data conflict. Data has changed since last read.
        "422":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Link could not be deleted.
        "500":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Something went wrong while processing Link.
        default:
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Unexpected error.
      summary: Delete Link
      tags:
      - Link
    get:
      description: Get Link
      operationId: GetLink
      parameters:
      - description: Unique identifier for a space_id
        in: path
        name: space_id
        required: true
        schema:
          format: uuid
          type: string
      - description: |-
          Include clause for expanding related entities in the response for Link.
          The attribute names are case-sensitive, PascalCase, and
          expected in a comma-separated list format as in the JSON encoding.

          Supported attributes for Link are FromUnitID, OrganizationID, SpaceID, ToSpaceID, ToUnitID.

          The whole string must be query-encoded.
        in: query
        name: include
        required: false
        schema:
          type: string
      - description: |-
          Select clause for specifying which fields to include in the response for Link.
          The attribute names are case-sensitive, PascalCase, and
          expected in a comma-separated list format as in the JSON encoding.
          If not specified, all fields are returned.
          Entity and parent IDs (like OrganizationID, SpaceID, LinkID) and Slug are always returned regardless of the select parameter.
          Fields used in where and contains filters are also automatically included.
          Example: 'DisplayName,CreatedAt,Labels' will return only those fields plus the required ID and Slug fields.
          The whole string must be query-encoded.
        in: query
        name: select
        required: false
        schema:
          type: string
      - description: Unique identifier for a link_id
        in: path
        name: link_id
        required: true
        schema:
          format: uuid
          type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ExtendedLink'
          description: OK
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Link request is invalid (Bad Request).
        "401":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Unauthorized access.
        "403":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Forbidden access.
        "404":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Link not found.
        "500":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Something went wrong while processing Link.
        default:
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Unexpected error.
      summary: Get Link
      tags:
      - Link
    patch:
      description: Patch Link
      operationId: PatchLink
      parameters:
      - description: Unique identifier for a space_id
        in: path
        name: space_id
        required: true
        schema:
          format: uuid
          type: string
      - description: Unique identifier for a link_id
        in: path
        name: link_id
        required: true
        schema:
          format: uuid
          type: string
      - description: Swap the FromUnit and ToUnit directions of the link
        in: query
        name: reverse
        required: false
        schema:
          type: boolean
      requestBody:
        content:
          application/merge-patch+json:
            schema:
              description: JSON Merge Patch schema for Link. All fields are optional
                and nullable. Readonly fields are ignored.
              properties:
                Annotations:
                  additionalProperties:
                    nullable: true
                    type: string
                  description: An optional map of Annotation key/value pairs for tools
                    to attach information to entities.
                  nullable: true
                  type: object
                AutoUpdate:
                  description: ""
                  nullable: true
                  type: boolean
                Bindings:
                  description: ""
                  items:
                    nullable: true
                    type: object
                  nullable: true
                  type: array
                DeleteGates:
                  additionalProperties:
                    nullable: true
                    type: boolean
                  description: An optional set of gates that, if any is present, will
                    block deletion
                  nullable: true
                  type: object
                DisplayName:
                  description: Friendly name for the entity.
                  nullable: true
                  type: string
                DownstreamLastMergedRevisionNum:
                  description: ""
                  nullable: true
                  type: integer
                FromUnitID:
                  description: ""
                  format: uuid
                  nullable: true
                  type: string
                Labels:
                  additionalProperties:
                    nullable: true
                    type: string
                  description: An optional map of Label key/value pairs to specify
                    identifying attributes of entities for the purpose of grouping
                    and filtering them.
                  nullable: true
                  type: object
                Slug:
                  description: Unique URL-safe identifier for the entity.
                  nullable: true
                  type: string
                ToSpaceID:
                  description: ""
                  format: uuid
                  nullable: true
                  type: string
                ToUnitID:
                  description: ""
                  format: uuid
                  nullable: true
                  type: string
                UpdateType:
                  description: ""
                  nullable: true
                  type: string
                UpstreamLastMergedRevisionNum:
                  description: ""
                  nullable: true
                  type: integer
                UseLiveState:
                  description: ""
                  nullable: true
                  type: boolean
                Version:
                  description: An entity-specific sequence number used for optimistic
                    concurrency control. The value read must be sent in calls to Update.
                  nullable: true
                  type: integer
                WhereMutation:
                  description: ""
                  nullable: true
                  type: string
                WhereResource:
                  description: ""
                  nullable: true
                  type: string
              type: object
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Link'
          description: |-
            Link connects two config Units in a dependency / producer-consumer relationship.
            A Link indicates that selected config data from the upstream To Unit (the producer)
            should be propagated to the downstream From Unit (the consumer).
            Links must be created in the same Space as the From Unit.
            They also imply an ordering when Applied or Destroyed as a group.
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Link request is invalid (Bad Request).
        "401":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Unauthorized access.
        "403":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Forbidden access.
        "404":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Link not found.
        "409":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Link data conflict. Data has changed since last read.
        "500":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Something went wrong while processing Link.
        default:
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Unexpected error.
      summary: Patch Link
      tags:
      - Link
    put:
      description: Update Link
      operationId: UpdateLink
      parameters:
      - description: Unique identifier for a space_id
        in: path
        name: space_id
        required: true
        schema:
          format: uuid
          type: string
      - description: Unique identifier for a link_id
        in: path
        name: link_id
        required: true
        schema:
          format: uuid
          type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/Link'
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Link'
          description: |-
            Link connects two config Units in a dependency / producer-consumer relationship.
            A Link indicates that selected config data from the upstream To Unit (the producer)
            should be propagated to the downstream From Unit (the consumer).
            Links must be created in the same Space as the From Unit.
            They also imply an ordering when Applied or Destroyed as a group.
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Link request is invalid (Bad Request).
        "401":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Unauthorized access.
        "403":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Forbidden access.
        "404":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Link not found.
        "409":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Link data conflict. Data has changed since last read.
        "500":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Something went wrong while processing Link.
        default:
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Unexpected error.
      summary: Update Link
      tags:
      - Link
  /space/{space_id}/tag:
    get:
      description: List Tags
      operationId: ListTags
      parameters:
      - description: Unique identifier for a space_id
        in: path
        name: space_id
        required: true
        schema:
          format: uuid
          type: string
      - description: |-
          The specified string is an expression for the purpose of filtering
          the list of Tags returned. The expression syntax was inspired by SQL.
          It supports conjunctions using `AND` of relational expressions of the form *attribute*
          *operator* *attribute_or_literal*. The attribute names are case-sensitive and PascalCase,
          as in the JSON encoding.
          Strings support the following operators: `<`, `>`, `<=`, `>=`, `=`, `!=`, `LIKE`, `NOT LIKE`, `ILIKE`, `~~`, `!~~`, `~`, `~*`, `!~`, `!~*`, `IN`, `NOT IN`.
          String pattern operators: `LIKE` and `~~` for pattern matching with `%` and `_` wildcards,
          `ILIKE` for case-insensitive pattern matching, `NOT LIKE` and `!~~` for negated pattern matching.
          String regex operators: `~` for regex matching, `~*` for case-insensitive regex,
          `!~` and `!~*` for regex not matching (case-sensitive and insensitive).
          Integers support the following operators: `<`, `>`, `<=`, `>=`, `=`, `!=`, `IN`, `NOT IN`.
          UUIDs and boolean attributes support equality and inequality only.
          UUID and time literals must be quoted as string literals.
          String literals are quoted with single quotes, such as `'string'`.
          Time literals use the same form as when serialized as JSON,
          such as: `CreatedAt > '2025-02-18T23:16:34'`.
          Integer and boolean literals are also supported for attributes of those types.
          Arrays support the `?` operator to to match any element of the array,
          as in `ApprovedBy ? '7c61626f-ddbe-41af-93f6-b69f4ab6d308'`.
          Arrays can perform LEN() to check for length, as in `LEN(ApprovedBy) > 0`.
          Map support the dot notation to specify a particular map key, as in `Labels.tier = 'Backend'`.
          Maps support `IS NULL` and `IS NOT NULL` with dot notation to check for key absence or presence,
          as in `Labels.tier IS NULL` (key doesn't exist) or `Labels.tier IS NOT NULL` (key exists).
          Comparison results can be tested with `IS TRUE`, `IS FALSE`, `IS NOT TRUE`, and `IS NOT FALSE`.
          These are useful for nullable columns: `MergeSourceID = '<uuid>' IS NOT FALSE` matches rows where MergeSourceID equals the value OR is NULL.
          The `IN` and `NOT IN` operators accept a comma-separated list of values in parentheses,
          such as `Slug IN ('slugone', 'slugtwo')` or `Labels.environment IN ('prod', 'staging')`.
          Conjunctions are supported using the `AND` operator.
          An example conjunction is:
          `CreatedAt >= '2025-01-07' AND Slug = 'test' AND Labels.mykey = 'myvalue'`.

          Supported attributes for filtering on Tag: ChangeSetID, CreatedAt, DeleteGates, DisplayName, Labels, OrganizationID, Slug, SpaceID, TagID, UpdatedAt.

          The whole string must be query-encoded.
        in: query
        name: where
        required: false
        schema:
          type: string
      - description: |-
          UUID of a Filter entity to apply to the Tag list.

          The Filter must be in the same Organization as the user credentials.

          The Filter's From field must match the entity type being filtered (Tag).

          For Space-resident entities, if the Filter has a FromSpaceID, it must match the operation's SpaceID.

          The Filter's Where clause will be combined with any explicit 'where' parameter using AND logic.

          If both 'filter' and 'where' parameters are specified, they are combined with AND logic.
        in: query
        name: filter
        required: false
        schema:
          type: string
      - description: |-
          Free text search that approximately matches the specified string against string fields and map keys/values.

          The search is case-insensitive and uses pattern matching to find entities containing the text.

          Searchable string fields include attributes like Slug, DisplayName, and string-typed custom fields.

          For map fields (like Labels and Annotations), the search matches both map keys and values.

          The search uses OR logic across all searchable fields, so matching any field will return the entity.

          If both 'where' and 'contains' parameters are specified, they are combined with AND logic.

          Searchable fields for Tag include string and map-type attributes from the queryable attributes list.

          The whole string must be query-encoded.
        in: query
        name: contains
        required: false
        schema:
          type: string
      - description: |-
          Include clause for expanding related entities in the response for Tag.
          The attribute names are case-sensitive, PascalCase, and
          expected in a comma-separated list format as in the JSON encoding.

          Supported attributes for Tag are ChangeSetID, OrganizationID, SpaceID.

          The whole string must be query-encoded.
        in: query
        name: include
        required: false
        schema:
          type: string
      - description: |-
          Select clause for specifying which fields to include in the response for Tag.
          The attribute names are case-sensitive, PascalCase, and
          expected in a comma-separated list format as in the JSON encoding.
          If not specified, all fields are returned.
          Entity and parent IDs (like OrganizationID, SpaceID, TagID) and Slug are always returned regardless of the select parameter.
          Fields used in where and contains filters are also automatically included.
          Example: 'DisplayName,CreatedAt,Labels' will return only those fields plus the required ID and Slug fields.
          The whole string must be query-encoded.
        in: query
        name: select
        required: false
        schema:
          type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                items:
                  $ref: '#/components/schemas/ExtendedTag'
                type: array
          description: OK
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Tag request is invalid (Bad Request).
        "401":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Unauthorized access.
        "403":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Forbidden access.
        "404":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Tag not found.
        "500":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Something went wrong while processing Tag.
        default:
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Unexpected error.
      summary: List Tags
      tags:
      - Tag
    post:
      description: Create Tag
      operationId: CreateTag
      parameters:
      - description: Unique identifier for a space_id
        in: path
        name: space_id
        required: true
        schema:
          format: uuid
          type: string
      - description: Allowed values are true and false. Default is false. When true,
          reports success when an entity already exists and returns the existing entity
        in: query
        name: allow_exists
        required: false
        schema:
          type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/Tag'
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Tag'
          description: Defines a Tag that can be used to identify a set of Revisions
            across Units.
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Tag request is invalid (Bad Request).
        "401":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Unauthorized access.
        "403":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Forbidden access.
        "404":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Tag not found.
        "409":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Tag data conflict. Data has changed since last read.
        "500":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Something went wrong while processing Tag.
        default:
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Unexpected error.
      summary: Create Tag
      tags:
      - Tag
  /space/{space_id}/tag/{tag_id}:
    delete:
      description: Delete Tag
      operationId: DeleteTag
      parameters:
      - description: Unique identifier for a space_id
        in: path
        name: space_id
        required: true
        schema:
          format: uuid
          type: string
      - description: Unique identifier for a tag_id
        in: path
        name: tag_id
        required: true
        schema:
          format: uuid
          type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/DeleteResponse'
          description: Response for successful delete operation
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Tag request is invalid (Bad Request).
        "401":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Unauthorized access.
        "403":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Forbidden access.
        "404":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Tag not found.
        "409":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Tag data conflict. Data has changed since last read.
        "422":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Tag could not be deleted.
        "500":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Something went wrong while processing Tag.
        default:
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Unexpected error.
      summary: Delete Tag
      tags:
      - Tag
    get:
      description: Get Tag
      operationId: GetTag
      parameters:
      - description: Unique identifier for a space_id
        in: path
        name: space_id
        required: true
        schema:
          format: uuid
          type: string
      - description: |-
          Include clause for expanding related entities in the response for Tag.
          The attribute names are case-sensitive, PascalCase, and
          expected in a comma-separated list format as in the JSON encoding.

          Supported attributes for Tag are ChangeSetID, OrganizationID, SpaceID.

          The whole string must be query-encoded.
        in: query
        name: include
        required: false
        schema:
          type: string
      - description: |-
          Select clause for specifying which fields to include in the response for Tag.
          The attribute names are case-sensitive, PascalCase, and
          expected in a comma-separated list format as in the JSON encoding.
          If not specified, all fields are returned.
          Entity and parent IDs (like OrganizationID, SpaceID, TagID) and Slug are always returned regardless of the select parameter.
          Fields used in where and contains filters are also automatically included.
          Example: 'DisplayName,CreatedAt,Labels' will return only those fields plus the required ID and Slug fields.
          The whole string must be query-encoded.
        in: query
        name: select
        required: false
        schema:
          type: string
      - description: Unique identifier for a tag_id
        in: path
        name: tag_id
        required: true
        schema:
          format: uuid
          type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ExtendedTag'
          description: OK
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Tag request is invalid (Bad Request).
        "401":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Unauthorized access.
        "403":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Forbidden access.
        "404":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Tag not found.
        "500":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Something went wrong while processing Tag.
        default:
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Unexpected error.
      summary: Get Tag
      tags:
      - Tag
    patch:
      description: Patch Tag
      operationId: PatchTag
      parameters:
      - description: Unique identifier for a space_id
        in: path
        name: space_id
        required: true
        schema:
          format: uuid
          type: string
      - description: Unique identifier for a tag_id
        in: path
        name: tag_id
        required: true
        schema:
          format: uuid
          type: string
      requestBody:
        content:
          application/merge-patch+json:
            schema:
              description: JSON Merge Patch schema for Tag. All fields are optional
                and nullable. Readonly fields are ignored.
              properties:
                Annotations:
                  additionalProperties:
                    nullable: true
                    type: string
                  description: An optional map of Annotation key/value pairs for tools
                    to attach information to entities.
                  nullable: true
                  type: object
                DeleteGates:
                  additionalProperties:
                    nullable: true
                    type: boolean
                  description: An optional set of gates that, if any is present, will
                    block deletion
                  nullable: true
                  type: object
                DisplayName:
                  description: Friendly name for the entity.
                  nullable: true
                  type: string
                Labels:
                  additionalProperties:
                    nullable: true
                    type: string
                  description: An optional map of Label key/value pairs to specify
                    identifying attributes of entities for the purpose of grouping
                    and filtering them.
                  nullable: true
                  type: object
                Slug:
                  description: Unique URL-safe identifier for the entity.
                  nullable: true
                  type: string
                Version:
                  description: An entity-specific sequence number used for optimistic
                    concurrency control. The value read must be sent in calls to Update.
                  nullable: true
                  type: integer
              type: object
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Tag'
          description: Defines a Tag that can be used to identify a set of Revisions
            across Units.
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Tag request is invalid (Bad Request).
        "401":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Unauthorized access.
        "403":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Forbidden access.
        "404":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Tag not found.
        "409":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Tag data conflict. Data has changed since last read.
        "500":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Something went wrong while processing Tag.
        default:
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Unexpected error.
      summary: Patch Tag
      tags:
      - Tag
    put:
      description: Update Tag
      operationId: UpdateTag
      parameters:
      - description: Unique identifier for a space_id
        in: path
        name: space_id
        required: true
        schema:
          format: uuid
          type: string
      - description: Unique identifier for a tag_id
        in: path
        name: tag_id
        required: true
        schema:
          format: uuid
          type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/Tag'
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Tag'
          description: Defines a Tag that can be used to identify a set of Revisions
            across Units.
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Tag request is invalid (Bad Request).
        "401":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Unauthorized access.
        "403":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Forbidden access.
        "404":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Tag not found.
        "409":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Tag data conflict. Data has changed since last read.
        "500":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Something went wrong while processing Tag.
        default:
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Unexpected error.
      summary: Update Tag
      tags:
      - Tag
  /space/{space_id}/target:
    get:
      description: List Targets
      operationId: ListTargets
      parameters:
      - description: Unique identifier for a space_id
        in: path
        name: space_id
        required: true
        schema:
          format: uuid
          type: string
      - description: |-
          The specified string is an expression for the purpose of filtering
          the list of Targets returned. The expression syntax was inspired by SQL.
          It supports conjunctions using `AND` of relational expressions of the form *attribute*
          *operator* *attribute_or_literal*. The attribute names are case-sensitive and PascalCase,
          as in the JSON encoding.
          Strings support the following operators: `<`, `>`, `<=`, `>=`, `=`, `!=`, `LIKE`, `NOT LIKE`, `ILIKE`, `~~`, `!~~`, `~`, `~*`, `!~`, `!~*`, `IN`, `NOT IN`.
          String pattern operators: `LIKE` and `~~` for pattern matching with `%` and `_` wildcards,
          `ILIKE` for case-insensitive pattern matching, `NOT LIKE` and `!~~` for negated pattern matching.
          String regex operators: `~` for regex matching, `~*` for case-insensitive regex,
          `!~` and `!~*` for regex not matching (case-sensitive and insensitive).
          Integers support the following operators: `<`, `>`, `<=`, `>=`, `=`, `!=`, `IN`, `NOT IN`.
          UUIDs and boolean attributes support equality and inequality only.
          UUID and time literals must be quoted as string literals.
          String literals are quoted with single quotes, such as `'string'`.
          Time literals use the same form as when serialized as JSON,
          such as: `CreatedAt > '2025-02-18T23:16:34'`.
          Integer and boolean literals are also supported for attributes of those types.
          Arrays support the `?` operator to to match any element of the array,
          as in `ApprovedBy ? '7c61626f-ddbe-41af-93f6-b69f4ab6d308'`.
          Arrays can perform LEN() to check for length, as in `LEN(ApprovedBy) > 0`.
          Map support the dot notation to specify a particular map key, as in `Labels.tier = 'Backend'`.
          Maps support `IS NULL` and `IS NOT NULL` with dot notation to check for key absence or presence,
          as in `Labels.tier IS NULL` (key doesn't exist) or `Labels.tier IS NOT NULL` (key exists).
          Comparison results can be tested with `IS TRUE`, `IS FALSE`, `IS NOT TRUE`, and `IS NOT FALSE`.
          These are useful for nullable columns: `MergeSourceID = '<uuid>' IS NOT FALSE` matches rows where MergeSourceID equals the value OR is NULL.
          The `IN` and `NOT IN` operators accept a comma-separated list of values in parentheses,
          such as `Slug IN ('slugone', 'slugtwo')` or `Labels.environment IN ('prod', 'staging')`.
          Conjunctions are supported using the `AND` operator.
          An example conjunction is:
          `CreatedAt >= '2025-01-07' AND Slug = 'test' AND Labels.mykey = 'myvalue'`.

          Supported attributes for filtering on Target: BridgeHandle, BridgeWorkerID, CreatedAt, DeleteGates, DisplayName, Labels, LiveStateType, Options, OrganizationID, Permissions, ProviderType, Slug, SpaceID, TargetID, ToolchainType, TriggerFilterID, TriggerHash, TriggerIDs, UpdatedAt.

          The whole string must be query-encoded.
        in: query
        name: where
        required: false
        schema:
          type: string
      - description: |-
          UUID of a Filter entity to apply to the Target list.

          The Filter must be in the same Organization as the user credentials.

          The Filter's From field must match the entity type being filtered (Target).

          For Space-resident entities, if the Filter has a FromSpaceID, it must match the operation's SpaceID.

          The Filter's Where clause will be combined with any explicit 'where' parameter using AND logic.

          If both 'filter' and 'where' parameters are specified, they are combined with AND logic.
        in: query
        name: filter
        required: false
        schema:
          type: string
      - description: |-
          Free text search that approximately matches the specified string against string fields and map keys/values.

          The search is case-insensitive and uses pattern matching to find entities containing the text.

          Searchable string fields include attributes like Slug, DisplayName, and string-typed custom fields.

          For map fields (like Labels and Annotations), the search matches both map keys and values.

          The search uses OR logic across all searchable fields, so matching any field will return the entity.

          If both 'where' and 'contains' parameters are specified, they are combined with AND logic.

          Searchable fields for Target include string and map-type attributes from the queryable attributes list.

          The whole string must be query-encoded.
        in: query
        name: contains
        required: false
        schema:
          type: string
      - description: |-
          Include clause for expanding related entities in the response for Target.
          The attribute names are case-sensitive, PascalCase, and
          expected in a comma-separated list format as in the JSON encoding.

          Supported attributes for Target are BridgeWorkerID, OrganizationID, SpaceID, TriggerFilterID, TriggerIDs.

          The whole string must be query-encoded.
        in: query
        name: include
        required: false
        schema:
          type: string
      - description: |-
          Select clause for specifying which fields to include in the response for Target.
          The attribute names are case-sensitive, PascalCase, and
          expected in a comma-separated list format as in the JSON encoding.
          If not specified, all fields are returned.
          Entity and parent IDs (like OrganizationID, SpaceID, TargetID) and Slug are always returned regardless of the select parameter.
          Fields used in where and contains filters are also automatically included.
          Example: 'DisplayName,CreatedAt,Labels' will return only those fields plus the required ID and Slug fields.
          The whole string must be query-encoded.
        in: query
        name: select
        required: false
        schema:
          type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                items:
                  $ref: '#/components/schemas/ExtendedTarget'
                type: array
          description: OK
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Target request is invalid (Bad Request).
        "401":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Unauthorized access.
        "403":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Forbidden access.
        "404":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Target not found.
        "500":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Something went wrong while processing Target.
        default:
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Unexpected error.
      summary: List Targets
      tags:
      - Target
    post:
      description: Create Target
      operationId: CreateTarget
      parameters:
      - description: Unique identifier for a space_id
        in: path
        name: space_id
        required: true
        schema:
          format: uuid
          type: string
      - description: Allowed values are true and false. Default is false. When true,
          reports success when an entity already exists and returns the existing entity
        in: query
        name: allow_exists
        required: false
        schema:
          type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/Target'
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Target'
          description: Target represents a deployment target in ConfigHub. It defines
            where configuration should be applied, including the toolchain type (e.g.,
            Kubernetes/YAML, OpenTofu/HCL, AppConfig/Properties, AppConfig/YAML, AppConfig/TOML,
            AppConfig/INI, AppConfig/JSON, AppConfig/Env, AppConfig/Text) and provider
            (e.g., Kubernetes, OpenTofu/AWS, ConfigMap). Each Target is associated
            with a specific BridgeWorker that handles the actual deployment actions
            (e.g. Apply, Destroy).
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Target request is invalid (Bad Request).
        "401":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Unauthorized access.
        "403":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Forbidden access.
        "404":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Target not found.
        "409":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Target data conflict. Data has changed since last read.
        "500":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Something went wrong while processing Target.
        default:
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Unexpected error.
      summary: Create Target
      tags:
      - Target
  /space/{space_id}/target/{target_id}:
    delete:
      description: Delete Target
      operationId: DeleteTarget
      parameters:
      - description: Unique identifier for a space_id
        in: path
        name: space_id
        required: true
        schema:
          format: uuid
          type: string
      - description: Unique identifier for a target_id
        in: path
        name: target_id
        required: true
        schema:
          format: uuid
          type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/DeleteResponse'
          description: Response for successful delete operation
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Target request is invalid (Bad Request).
        "401":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Unauthorized access.
        "403":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Forbidden access.
        "404":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Target not found.
        "409":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Target data conflict. Data has changed since last read.
        "422":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Target could not be deleted.
        "500":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Something went wrong while processing Target.
        default:
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Unexpected error.
      summary: Delete Target
      tags:
      - Target
    get:
      description: Get Target
      operationId: GetTarget
      parameters:
      - description: Unique identifier for a space_id
        in: path
        name: space_id
        required: true
        schema:
          format: uuid
          type: string
      - description: |-
          Include clause for expanding related entities in the response for Target.
          The attribute names are case-sensitive, PascalCase, and
          expected in a comma-separated list format as in the JSON encoding.

          Supported attributes for Target are BridgeWorkerID, OrganizationID, SpaceID, TriggerFilterID, TriggerIDs.

          The whole string must be query-encoded.
        in: query
        name: include
        required: false
        schema:
          type: string
      - description: |-
          Select clause for specifying which fields to include in the response for Target.
          The attribute names are case-sensitive, PascalCase, and
          expected in a comma-separated list format as in the JSON encoding.
          If not specified, all fields are returned.
          Entity and parent IDs (like OrganizationID, SpaceID, TargetID) and Slug are always returned regardless of the select parameter.
          Fields used in where and contains filters are also automatically included.
          Example: 'DisplayName,CreatedAt,Labels' will return only those fields plus the required ID and Slug fields.
          The whole string must be query-encoded.
        in: query
        name: select
        required: false
        schema:
          type: string
      - description: Unique identifier for a target_id
        in: path
        name: target_id
        required: true
        schema:
          format: uuid
          type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ExtendedTarget'
          description: OK
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Target request is invalid (Bad Request).
        "401":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Unauthorized access.
        "403":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Forbidden access.
        "404":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Target not found.
        "500":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Something went wrong while processing Target.
        default:
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Unexpected error.
      summary: Get Target
      tags:
      - Target
    patch:
      description: Patch Target
      operationId: PatchTarget
      parameters:
      - description: Unique identifier for a space_id
        in: path
        name: space_id
        required: true
        schema:
          format: uuid
          type: string
      - description: Unique identifier for a target_id
        in: path
        name: target_id
        required: true
        schema:
          format: uuid
          type: string
      - description: Re-list the Triggers matching WhereTrigger and/or TriggerFilterID
          even if these fields have not changed
        in: query
        name: refresh_triggers
        required: false
        schema:
          type: boolean
      requestBody:
        content:
          application/merge-patch+json:
            schema:
              description: JSON Merge Patch schema for Target. All fields are optional
                and nullable. Readonly fields are ignored.
              properties:
                Annotations:
                  additionalProperties:
                    nullable: true
                    type: string
                  description: An optional map of Annotation key/value pairs for tools
                    to attach information to entities.
                  nullable: true
                  type: object
                BridgeHandle:
                  description: ""
                  nullable: true
                  type: string
                BridgeWorkerID:
                  description: ""
                  format: uuid
                  nullable: true
                  type: string
                ConfigTypes:
                  description: ""
                  items:
                    nullable: true
                    type: object
                  nullable: true
                  type: array
                DeleteGates:
                  additionalProperties:
                    nullable: true
                    type: boolean
                  description: An optional set of gates that, if any is present, will
                    block deletion
                  nullable: true
                  type: object
                DisplayName:
                  description: Friendly name for the entity.
                  nullable: true
                  type: string
                Labels:
                  additionalProperties:
                    nullable: true
                    type: string
                  description: An optional map of Label key/value pairs to specify
                    identifying attributes of entities for the purpose of grouping
                    and filtering them.
                  nullable: true
                  type: object
                LiveStateType:
                  description: ""
                  nullable: true
                  type: string
                Options:
                  additionalProperties:
                    nullable: true
                    type: string
                  description: ""
                  nullable: true
                  type: object
                Parameters:
                  description: ""
                  nullable: true
                  type: string
                Permissions:
                  additionalProperties:
                    nullable: true
                    type: object
                  description: ""
                  nullable: true
                  type: object
                ProviderType:
                  description: ""
                  nullable: true
                  type: string
                Slug:
                  description: Unique URL-safe identifier for the entity.
                  nullable: true
                  type: string
                ToolchainType:
                  description: ""
                  nullable: true
                  type: string
                TriggerFilterID:
                  description: ""
                  format: uuid
                  nullable: true
                  type: string
                Version:
                  description: An entity-specific sequence number used for optimistic
                    concurrency control. The value read must be sent in calls to Update.
                  nullable: true
                  type: integer
                WhereTrigger:
                  description: ""
                  nullable: true
                  type: string
              type: object
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Target'
          description: Target represents a deployment target in ConfigHub. It defines
            where configuration should be applied, including the toolchain type (e.g.,
            Kubernetes/YAML, OpenTofu/HCL, AppConfig/Properties, AppConfig/YAML, AppConfig/TOML,
            AppConfig/INI, AppConfig/JSON, AppConfig/Env, AppConfig/Text) and provider
            (e.g., Kubernetes, OpenTofu/AWS, ConfigMap). Each Target is associated
            with a specific BridgeWorker that handles the actual deployment actions
            (e.g. Apply, Destroy).
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Target request is invalid (Bad Request).
        "401":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Unauthorized access.
        "403":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Forbidden access.
        "404":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Target not found.
        "409":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Target data conflict. Data has changed since last read.
        "500":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Something went wrong while processing Target.
        default:
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Unexpected error.
      summary: Patch Target
      tags:
      - Target
    put:
      description: Update Target
      operationId: UpdateTarget
      parameters:
      - description: Unique identifier for a space_id
        in: path
        name: space_id
        required: true
        schema:
          format: uuid
          type: string
      - description: Unique identifier for a target_id
        in: path
        name: target_id
        required: true
        schema:
          format: uuid
          type: string
      - description: Re-list the Triggers matching WhereTrigger and/or TriggerFilterID
          even if these fields have not changed
        in: query
        name: refresh_triggers
        required: false
        schema:
          type: boolean
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/Target'
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Target'
          description: Target represents a deployment target in ConfigHub. It defines
            where configuration should be applied, including the toolchain type (e.g.,
            Kubernetes/YAML, OpenTofu/HCL, AppConfig/Properties, AppConfig/YAML, AppConfig/TOML,
            AppConfig/INI, AppConfig/JSON, AppConfig/Env, AppConfig/Text) and provider
            (e.g., Kubernetes, OpenTofu/AWS, ConfigMap). Each Target is associated
            with a specific BridgeWorker that handles the actual deployment actions
            (e.g. Apply, Destroy).
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Target request is invalid (Bad Request).
        "401":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Unauthorized access.
        "403":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Forbidden access.
        "404":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Target not found.
        "409":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Target data conflict. Data has changed since last read.
        "500":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Something went wrong while processing Target.
        default:
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Unexpected error.
      summary: Update Target
      tags:
      - Target
  /space/{space_id}/trigger:
    get:
      description: List Triggers
      operationId: ListTriggers
      parameters:
      - description: Unique identifier for a space_id
        in: path
        name: space_id
        required: true
        schema:
          format: uuid
          type: string
      - description: |-
          The specified string is an expression for the purpose of filtering
          the list of Triggers returned. The expression syntax was inspired by SQL.
          It supports conjunctions using `AND` of relational expressions of the form *attribute*
          *operator* *attribute_or_literal*. The attribute names are case-sensitive and PascalCase,
          as in the JSON encoding.
          Strings support the following operators: `<`, `>`, `<=`, `>=`, `=`, `!=`, `LIKE`, `NOT LIKE`, `ILIKE`, `~~`, `!~~`, `~`, `~*`, `!~`, `!~*`, `IN`, `NOT IN`.
          String pattern operators: `LIKE` and `~~` for pattern matching with `%` and `_` wildcards,
          `ILIKE` for case-insensitive pattern matching, `NOT LIKE` and `!~~` for negated pattern matching.
          String regex operators: `~` for regex matching, `~*` for case-insensitive regex,
          `!~` and `!~*` for regex not matching (case-sensitive and insensitive).
          Integers support the following operators: `<`, `>`, `<=`, `>=`, `=`, `!=`, `IN`, `NOT IN`.
          UUIDs and boolean attributes support equality and inequality only.
          UUID and time literals must be quoted as string literals.
          String literals are quoted with single quotes, such as `'string'`.
          Time literals use the same form as when serialized as JSON,
          such as: `CreatedAt > '2025-02-18T23:16:34'`.
          Integer and boolean literals are also supported for attributes of those types.
          Arrays support the `?` operator to to match any element of the array,
          as in `ApprovedBy ? '7c61626f-ddbe-41af-93f6-b69f4ab6d308'`.
          Arrays can perform LEN() to check for length, as in `LEN(ApprovedBy) > 0`.
          Map support the dot notation to specify a particular map key, as in `Labels.tier = 'Backend'`.
          Maps support `IS NULL` and `IS NOT NULL` with dot notation to check for key absence or presence,
          as in `Labels.tier IS NULL` (key doesn't exist) or `Labels.tier IS NOT NULL` (key exists).
          Comparison results can be tested with `IS TRUE`, `IS FALSE`, `IS NOT TRUE`, and `IS NOT FALSE`.
          These are useful for nullable columns: `MergeSourceID = '<uuid>' IS NOT FALSE` matches rows where MergeSourceID equals the value OR is NULL.
          The `IN` and `NOT IN` operators accept a comma-separated list of values in parentheses,
          such as `Slug IN ('slugone', 'slugtwo')` or `Labels.environment IN ('prod', 'staging')`.
          Conjunctions are supported using the `AND` operator.
          An example conjunction is:
          `CreatedAt >= '2025-01-07' AND Slug = 'test' AND Labels.mykey = 'myvalue'`.

          Supported attributes for filtering on Trigger: BridgeWorkerID, CreatedAt, DeleteGates, Description, Disabled, DisplayName, Event, FunctionName, Hash, InvocationID, Labels, OrganizationID, OtherDataSource, Slug, SpaceID, ToolchainType, TriggerID, UnitFilterID, UpdatedAt, Validating, Warn, WhereResource, WhereUnit.

          The whole string must be query-encoded.
        in: query
        name: where
        required: false
        schema:
          type: string
      - description: |-
          UUID of a Filter entity to apply to the Trigger list.

          The Filter must be in the same Organization as the user credentials.

          The Filter's From field must match the entity type being filtered (Trigger).

          For Space-resident entities, if the Filter has a FromSpaceID, it must match the operation's SpaceID.

          The Filter's Where clause will be combined with any explicit 'where' parameter using AND logic.

          If both 'filter' and 'where' parameters are specified, they are combined with AND logic.
        in: query
        name: filter
        required: false
        schema:
          type: string
      - description: |-
          Free text search that approximately matches the specified string against string fields and map keys/values.

          The search is case-insensitive and uses pattern matching to find entities containing the text.

          Searchable string fields include attributes like Slug, DisplayName, and string-typed custom fields.

          For map fields (like Labels and Annotations), the search matches both map keys and values.

          The search uses OR logic across all searchable fields, so matching any field will return the entity.

          If both 'where' and 'contains' parameters are specified, they are combined with AND logic.

          Searchable fields for Trigger include string and map-type attributes from the queryable attributes list.

          The whole string must be query-encoded.
        in: query
        name: contains
        required: false
        schema:
          type: string
      - description: |-
          Include clause for expanding related entities in the response for Trigger.
          The attribute names are case-sensitive, PascalCase, and
          expected in a comma-separated list format as in the JSON encoding.

          Supported attributes for Trigger are BridgeWorkerID, InvocationID, OrganizationID, SpaceID, UnitFilterID.

          The whole string must be query-encoded.
        in: query
        name: include
        required: false
        schema:
          type: string
      - description: |-
          Select clause for specifying which fields to include in the response for Trigger.
          The attribute names are case-sensitive, PascalCase, and
          expected in a comma-separated list format as in the JSON encoding.
          If not specified, all fields are returned.
          Entity and parent IDs (like OrganizationID, SpaceID, TriggerID) and Slug are always returned regardless of the select parameter.
          Fields used in where and contains filters are also automatically included.
          Example: 'DisplayName,CreatedAt,Labels' will return only those fields plus the required ID and Slug fields.
          The whole string must be query-encoded.
        in: query
        name: select
        required: false
        schema:
          type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                items:
                  $ref: '#/components/schemas/ExtendedTrigger'
                type: array
          description: OK
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Trigger request is invalid (Bad Request).
        "401":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Unauthorized access.
        "403":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Forbidden access.
        "404":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Trigger not found.
        "500":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Something went wrong while processing Trigger.
        default:
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Unexpected error.
      summary: List Triggers
      tags:
      - Trigger
    post:
      description: Create Trigger
      operationId: CreateTrigger
      parameters:
      - description: Unique identifier for a space_id
        in: path
        name: space_id
        required: true
        schema:
          format: uuid
          type: string
      - description: Allowed values are true and false. Default is false. When true,
          reports success when an entity already exists and returns the existing entity
        in: query
        name: allow_exists
        required: false
        schema:
          type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/Trigger'
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Trigger'
          description: |-
            Defines an automated function invocation that executes in response to specific
            Unit lifecycle events in ConfigHub. Triggers can be used to implement validation rules,
            automated transformations, or other custom logic that should run when configuration
            changes occur. Each Trigger is associated with a specific Space and can be configured
            to execute on events.

            Triggers can be either validating (checking configuration validity without modifying it)
            or mutating (making changes to the configuration). They can be disabled, and validating
            triggers can be set to Warn mode to produce non-blocking ApplyWarnings instead of ApplyGates.
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Trigger request is invalid (Bad Request).
        "401":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Unauthorized access.
        "403":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Forbidden access.
        "404":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Trigger not found.
        "409":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Trigger data conflict. Data has changed since last read.
        "500":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Something went wrong while processing Trigger.
        default:
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Unexpected error.
      summary: Create Trigger
      tags:
      - Trigger
  /space/{space_id}/trigger/{trigger_id}:
    delete:
      description: Delete Trigger
      operationId: DeleteTrigger
      parameters:
      - description: Unique identifier for a space_id
        in: path
        name: space_id
        required: true
        schema:
          format: uuid
          type: string
      - description: Unique identifier for a trigger_id
        in: path
        name: trigger_id
        required: true
        schema:
          format: uuid
          type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/DeleteResponse'
          description: Response for successful delete operation
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Trigger request is invalid (Bad Request).
        "401":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Unauthorized access.
        "403":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Forbidden access.
        "404":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Trigger not found.
        "409":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Trigger data conflict. Data has changed since last read.
        "422":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Trigger could not be deleted.
        "500":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Something went wrong while processing Trigger.
        default:
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Unexpected error.
      summary: Delete Trigger
      tags:
      - Trigger
    get:
      description: Get Trigger
      operationId: GetTrigger
      parameters:
      - description: Unique identifier for a space_id
        in: path
        name: space_id
        required: true
        schema:
          format: uuid
          type: string
      - description: |-
          Include clause for expanding related entities in the response for Trigger.
          The attribute names are case-sensitive, PascalCase, and
          expected in a comma-separated list format as in the JSON encoding.

          Supported attributes for Trigger are BridgeWorkerID, InvocationID, OrganizationID, SpaceID, UnitFilterID.

          The whole string must be query-encoded.
        in: query
        name: include
        required: false
        schema:
          type: string
      - description: |-
          Select clause for specifying which fields to include in the response for Trigger.
          The attribute names are case-sensitive, PascalCase, and
          expected in a comma-separated list format as in the JSON encoding.
          If not specified, all fields are returned.
          Entity and parent IDs (like OrganizationID, SpaceID, TriggerID) and Slug are always returned regardless of the select parameter.
          Fields used in where and contains filters are also automatically included.
          Example: 'DisplayName,CreatedAt,Labels' will return only those fields plus the required ID and Slug fields.
          The whole string must be query-encoded.
        in: query
        name: select
        required: false
        schema:
          type: string
      - description: Unique identifier for a trigger_id
        in: path
        name: trigger_id
        required: true
        schema:
          format: uuid
          type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ExtendedTrigger'
          description: OK
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Trigger request is invalid (Bad Request).
        "401":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Unauthorized access.
        "403":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Forbidden access.
        "404":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Trigger not found.
        "500":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Something went wrong while processing Trigger.
        default:
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Unexpected error.
      summary: Get Trigger
      tags:
      - Trigger
    patch:
      description: Patch Trigger
      operationId: PatchTrigger
      parameters:
      - description: Unique identifier for a space_id
        in: path
        name: space_id
        required: true
        schema:
          format: uuid
          type: string
      - description: Unique identifier for a trigger_id
        in: path
        name: trigger_id
        required: true
        schema:
          format: uuid
          type: string
      requestBody:
        content:
          application/merge-patch+json:
            schema:
              description: JSON Merge Patch schema for Trigger. All fields are optional
                and nullable. Readonly fields are ignored.
              properties:
                Annotations:
                  additionalProperties:
                    nullable: true
                    type: string
                  description: An optional map of Annotation key/value pairs for tools
                    to attach information to entities.
                  nullable: true
                  type: object
                Arguments:
                  description: Function arguments
                  items:
                    nullable: true
                    type: object
                  nullable: true
                  type: array
                BridgeWorkerID:
                  description: ""
                  format: uuid
                  nullable: true
                  type: string
                DeleteGates:
                  additionalProperties:
                    nullable: true
                    type: boolean
                  description: An optional set of gates that, if any is present, will
                    block deletion
                  nullable: true
                  type: object
                Description:
                  description: ""
                  nullable: true
                  type: string
                Disabled:
                  description: ""
                  nullable: true
                  type: boolean
                DisplayName:
                  description: Friendly name for the entity.
                  nullable: true
                  type: string
                Event:
                  description: ""
                  nullable: true
                  type: string
                FailOpenAfter:
                  description: ""
                  nullable: true
                  type: integer
                FunctionName:
                  description: Function name
                  nullable: true
                  type: string
                InvocationID:
                  description: ""
                  format: uuid
                  nullable: true
                  type: string
                Labels:
                  additionalProperties:
                    nullable: true
                    type: string
                  description: An optional map of Label key/value pairs to specify
                    identifying attributes of entities for the purpose of grouping
                    and filtering them.
                  nullable: true
                  type: object
                OtherDataSource:
                  description: ""
                  nullable: true
                  type: string
                Slug:
                  description: Unique URL-safe identifier for the entity.
                  nullable: true
                  type: string
                ToolchainType:
                  description: ""
                  nullable: true
                  type: string
                UnitFilterID:
                  description: ""
                  format: uuid
                  nullable: true
                  type: string
                Version:
                  description: An entity-specific sequence number used for optimistic
                    concurrency control. The value read must be sent in calls to Update.
                  nullable: true
                  type: integer
                Warn:
                  description: ""
                  nullable: true
                  type: boolean
                WhereResource:
                  description: ""
                  nullable: true
                  type: string
                WhereUnit:
                  description: ""
                  nullable: true
                  type: string
              type: object
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Trigger'
          description: |-
            Defines an automated function invocation that executes in response to specific
            Unit lifecycle events in ConfigHub. Triggers can be used to implement validation rules,
            automated transformations, or other custom logic that should run when configuration
            changes occur. Each Trigger is associated with a specific Space and can be configured
            to execute on events.

            Triggers can be either validating (checking configuration validity without modifying it)
            or mutating (making changes to the configuration). They can be disabled, and validating
            triggers can be set to Warn mode to produce non-blocking ApplyWarnings instead of ApplyGates.
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Trigger request is invalid (Bad Request).
        "401":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Unauthorized access.
        "403":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Forbidden access.
        "404":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Trigger not found.
        "409":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Trigger data conflict. Data has changed since last read.
        "500":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Something went wrong while processing Trigger.
        default:
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Unexpected error.
      summary: Patch Trigger
      tags:
      - Trigger
    put:
      description: Update Trigger
      operationId: UpdateTrigger
      parameters:
      - description: Unique identifier for a space_id
        in: path
        name: space_id
        required: true
        schema:
          format: uuid
          type: string
      - description: Unique identifier for a trigger_id
        in: path
        name: trigger_id
        required: true
        schema:
          format: uuid
          type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/Trigger'
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Trigger'
          description: |-
            Defines an automated function invocation that executes in response to specific
            Unit lifecycle events in ConfigHub. Triggers can be used to implement validation rules,
            automated transformations, or other custom logic that should run when configuration
            changes occur. Each Trigger is associated with a specific Space and can be configured
            to execute on events.

            Triggers can be either validating (checking configuration validity without modifying it)
            or mutating (making changes to the configuration). They can be disabled, and validating
            triggers can be set to Warn mode to produce non-blocking ApplyWarnings instead of ApplyGates.
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Trigger request is invalid (Bad Request).
        "401":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Unauthorized access.
        "403":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Forbidden access.
        "404":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Trigger not found.
        "409":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Trigger data conflict. Data has changed since last read.
        "500":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Something went wrong while processing Trigger.
        default:
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Unexpected error.
      summary: Update Trigger
      tags:
      - Trigger
  /space/{space_id}/unit:
    get:
      description: List Units
      operationId: ListUnits
      parameters:
      - description: Unique identifier for a space_id
        in: path
        name: space_id
        required: true
        schema:
          format: uuid
          type: string
      - description: |-
          The specified string is an expression for the purpose of filtering
          the list of Units returned. The expression syntax was inspired by SQL.
          It supports conjunctions using `AND` of relational expressions of the form *attribute*
          *operator* *attribute_or_literal*. The attribute names are case-sensitive and PascalCase,
          as in the JSON encoding.
          Strings support the following operators: `<`, `>`, `<=`, `>=`, `=`, `!=`, `LIKE`, `NOT LIKE`, `ILIKE`, `~~`, `!~~`, `~`, `~*`, `!~`, `!~*`, `IN`, `NOT IN`.
          String pattern operators: `LIKE` and `~~` for pattern matching with `%` and `_` wildcards,
          `ILIKE` for case-insensitive pattern matching, `NOT LIKE` and `!~~` for negated pattern matching.
          String regex operators: `~` for regex matching, `~*` for case-insensitive regex,
          `!~` and `!~*` for regex not matching (case-sensitive and insensitive).
          Integers support the following operators: `<`, `>`, `<=`, `>=`, `=`, `!=`, `IN`, `NOT IN`.
          UUIDs and boolean attributes support equality and inequality only.
          UUID and time literals must be quoted as string literals.
          String literals are quoted with single quotes, such as `'string'`.
          Time literals use the same form as when serialized as JSON,
          such as: `CreatedAt > '2025-02-18T23:16:34'`.
          Integer and boolean literals are also supported for attributes of those types.
          Arrays support the `?` operator to to match any element of the array,
          as in `ApprovedBy ? '7c61626f-ddbe-41af-93f6-b69f4ab6d308'`.
          Arrays can perform LEN() to check for length, as in `LEN(ApprovedBy) > 0`.
          Map support the dot notation to specify a particular map key, as in `Labels.tier = 'Backend'`.
          Maps support `IS NULL` and `IS NOT NULL` with dot notation to check for key absence or presence,
          as in `Labels.tier IS NULL` (key doesn't exist) or `Labels.tier IS NOT NULL` (key exists).
          Comparison results can be tested with `IS TRUE`, `IS FALSE`, `IS NOT TRUE`, and `IS NOT FALSE`.
          These are useful for nullable columns: `MergeSourceID = '<uuid>' IS NOT FALSE` matches rows where MergeSourceID equals the value OR is NULL.
          The `IN` and `NOT IN` operators accept a comma-separated list of values in parentheses,
          such as `Slug IN ('slugone', 'slugtwo')` or `Labels.environment IN ('prod', 'staging')`.
          Conjunctions are supported using the `AND` operator.
          An example conjunction is:
          `CreatedAt >= '2025-01-07' AND Slug = 'test' AND Labels.mykey = 'myvalue'`.

          Supported attributes for filtering on Unit: ApplyGates, ApplyWarnings, ApprovedBy, BridgeWorkerID, ChangeSetID, CreatedAt, DataHash, DeleteGates, DestroyGates, DisplayName, DriftReconciliationMode, FromLinkID, HeadRevisionNum, HeadUnitActionNum, HeadUnitEventNum, Labels, LastActionAt, LastAppliedRevisionNum, LastChangeDescription, LiveRevisionNum, OrganizationID, PreviousLiveRevisionNum, ProviderType, Slug, SpaceID, TargetID, TargetOptions, ToolchainType, UnitID, UpdatedAt, UpstreamOrganizationID, UpstreamRevisionNum, UpstreamSpaceID, UpstreamUnitID, Values.

          Finding all units created by cloning can be done using the expression `UpstreamRevisionNum > 0`. Clones of a specific unit can be found by additionally filtering based on `UpstreamUnitID`. Unapplied units can be found using `LiveRevisionNum = 0`. Units with unapplied changes can be found with `HeadRevisionNum > LiveRevisionNum`.

          The whole string must be query-encoded.
        in: query
        name: where
        required: false
        schema:
          type: string
      - description: |-
          UUID of a Filter entity to apply to the Unit list.

          The Filter must be in the same Organization as the user credentials.

          The Filter's From field must match the entity type being filtered (Unit).

          For Space-resident entities, if the Filter has a FromSpaceID, it must match the operation's SpaceID.

          The Filter's Where clause will be combined with any explicit 'where' parameter using AND logic.

          If both 'filter' and 'where' parameters are specified, they are combined with AND logic.
        in: query
        name: filter
        required: false
        schema:
          type: string
      - description: |-
          Free text search that approximately matches the specified string against string fields and map keys/values.

          The search is case-insensitive and uses pattern matching to find entities containing the text.

          Searchable string fields include attributes like Slug, DisplayName, and string-typed custom fields.

          For map fields (like Labels and Annotations), the search matches both map keys and values.

          The search uses OR logic across all searchable fields, so matching any field will return the entity.

          If both 'where' and 'contains' parameters are specified, they are combined with AND logic.

          Searchable fields for Unit include string and map-type attributes from the queryable attributes list.

          The whole string must be query-encoded.
        in: query
        name: contains
        required: false
        schema:
          type: string
      - description: |-
          Include clause for expanding related entities in the response for Unit.
          The attribute names are case-sensitive, PascalCase, and
          expected in a comma-separated list format as in the JSON encoding.

          Supported attributes for Unit are ApprovedBy, BridgeWorkerID, ChangeSetID, FromLinkID, HeadMutationNum, HeadRevisionNum, LastAppliedRevisionNum, LiveRevisionNum, OrganizationID, PreviousLiveRevisionNum, SpaceID, TargetID, UnitEventID, UpstreamSpaceID, UpstreamUnitID.

          The whole string must be query-encoded.
        in: query
        name: include
        required: false
        schema:
          type: string
      - description: |-
          Select clause for specifying which fields to include in the response for Unit.
          The attribute names are case-sensitive, PascalCase, and
          expected in a comma-separated list format as in the JSON encoding.
          If not specified, all fields are returned.
          Entity and parent IDs (like OrganizationID, SpaceID, UnitID) and Slug are always returned regardless of the select parameter.
          Fields used in where and contains filters are also automatically included.
          Example: 'DisplayName,CreatedAt,Labels' will return only those fields plus the required ID and Slug fields.
          The whole string must be query-encoded.
        in: query
        name: select
        required: false
        schema:
          type: string
      - description: 'Resource type: Resource type to match for the desired ToolchainType,
          for example apps/v1/Deployment'
        in: query
        name: resource_type
        required: false
        schema:
          type: string
      - description: 'Where data: The specified string is an expression for the purpose
          of evaluating whether the configuration data matches the filter. It supports
          conjunctions using `AND` of relational expressions of the form *path* *operator*
          *literal*. The path specifications are dot-separated, for both map fields
          and array indices, as in `spec.template.spec.containers.0.image = ''ghcr.io/headlamp-k8s/headlamp:latest''
          AND spec.replicas > 1`. Path expressions support `*` for wildcard array
          or map segments and `?key=value` syntax for associative matches of array
          elements containing objects with a `key` attribute. Strings support the
          following operators: `<`, `>`, `<=`, `>=`, `=`, `!=`, `LIKE`, `ILIKE`, `~~`,
          `!~~`, `~`, `!~`, `~*`, `!~*`, `IN`, `NOT IN`. String pattern operators:
          `LIKE` and `~~` for pattern matching with `%` and `_` wildcards, `ILIKE`
          for case-insensitive pattern matching, `!~~` for NOT LIKE. String regex
          operators: `~` for regex matching, `~*` for case-insensitive regex, `!~`
          and `!~*` for regex not matching (case-sensitive and insensitive). Integers
          support the following operators: `<`, `>`, `<=`, `>=`, `=`, `!=`, `IN`,
          `NOT IN`. Boolean values support equality and inequality only. The `IN`
          and `NOT IN` operators accept a comma-separated list of values in parentheses,
          such as `spec.template.spec.containers.0.image#reference IN ('':latest'',
          '':arm64-latest'')`. The syntax `.|` requires the preceding path to exist;
          otherwise the relation `!=` will always return true regardless what it is
          compared with. String literals are quoted with single quotes, such as `''string''`.
          Integer and boolean literals are also supported for attributes of those
          types. The whole string must be query-encoded.'
        in: query
        name: where_data
        required: false
        schema:
          type: string
      - description: 'Where expression to match Triggers. Matched triggers are invoked
          on each unit to filter by validation results. Use with triggers_passed to
          control whether passing or failing units are returned (default: failing).'
        in: query
        name: where_trigger
        required: false
        schema:
          type: string
      - description: Filter UUID (with From=Trigger). The filter's matching triggers
          are invoked on units to filter by validation results. Can be combined with
          where_trigger.
        in: query
        name: trigger_filter
        required: false
        schema:
          type: string
      - description: When true, return units that pass trigger validation; when false
          (default), return units that fail. Only applies when where_trigger or trigger_filter
          is specified.
        in: query
        name: triggers_passed
        required: false
        schema:
          type: boolean
      - description: View slug or UUID. Applies the View's column definitions to extract
          values for each unit. If the View has a FilterID, its filter is ANDed with
          other filters. The View must have Of=Unit or a Filter with From=Unit.
        in: query
        name: view
        required: false
        schema:
          type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                items:
                  $ref: '#/components/schemas/ExtendedUnit'
                type: array
          description: OK
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Unit request is invalid (Bad Request).
        "401":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Unauthorized access.
        "403":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Forbidden access.
        "404":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Unit not found.
        "500":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Something went wrong while processing Unit.
        default:
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Unexpected error.
      summary: List Units
      tags:
      - Unit
    post:
      description: Create Unit
      operationId: CreateUnit
      parameters:
      - description: Unique identifier for a space_id
        in: path
        name: space_id
        required: true
        schema:
          format: uuid
          type: string
      - description: Unique identifier for a upstream_space_id
        in: query
        name: upstream_space_id
        required: false
        schema:
          format: uuid
          type: string
      - description: Unique identifier for a upstream_unit_id
        in: query
        name: upstream_unit_id
        required: false
        schema:
          format: uuid
          type: string
      - description: Identifier of the external source. Sets the source type to MergeExternal
          and appends the source name to the change description.
        in: query
        name: merge_external_source
        required: false
        schema:
          type: string
      - description: Allowed values are true and false. Default is false. When true,
          reports success when an entity already exists and returns the existing entity
        in: query
        name: allow_exists
        required: false
        schema:
          type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/Unit'
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Unit'
          description: |-
            Unit is the core unit of operation in ConfigHub. It contains a blob of configuration Data
            of a single supported Toolchain Type (configuration format). This blob is typically a text document
            that contains a collection of Kubernetes or infrastructure resources, or an application configuration
            file. Applying / deploying or destroying the configuration happens as a single *transaction*
            from ConfigHub's perspective. In reality, it is most often a multi-step workflow performed by
            the underlying configuration / deployment tool. The resources must belong to a single
            infrastructure provider and the actuation mechanism must be able to resolve references and
            ordering dependencies among the resources within the document. For example, if one resource
            needs to be fully provisioned to provide input to another resource, then the actuation code is
            responsible for handling this. Revisions store historical copies of the configuration data.
            Configuration data can be restored from prior Revisions. Units can also be cloned to create
            new variants of a configuration.
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Unit request is invalid (Bad Request).
        "401":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Unauthorized access.
        "403":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Forbidden access.
        "404":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Unit not found.
        "409":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Unit data conflict. Data has changed since last read.
        "422":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Unable to create unit(s) due to configuration data problem
        "500":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Something went wrong while processing Unit.
        default:
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Unexpected error.
      summary: Create Unit
      tags:
      - Unit
  /space/{space_id}/unit/{unit_id}:
    delete:
      description: Delete Unit
      operationId: DeleteUnit
      parameters:
      - description: Unique identifier for a space_id
        in: path
        name: space_id
        required: true
        schema:
          format: uuid
          type: string
      - description: Unique identifier for a unit_id
        in: path
        name: unit_id
        required: true
        schema:
          format: uuid
          type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/DeleteResponse'
          description: Response for successful delete operation
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Unit request is invalid (Bad Request).
        "401":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Unauthorized access.
        "403":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Forbidden access.
        "404":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Unit not found.
        "409":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Unit data conflict. Data has changed since last read.
        "422":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Unit could not be deleted.
        "423":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Unable to delete the unit(s) due to an inflight operation on
            the unit(s)
        "500":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Something went wrong while processing Unit.
        default:
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Unexpected error.
      summary: Delete Unit
      tags:
      - Unit
    get:
      description: Get Unit
      operationId: GetUnit
      parameters:
      - description: Unique identifier for a space_id
        in: path
        name: space_id
        required: true
        schema:
          format: uuid
          type: string
      - description: |-
          Include clause for expanding related entities in the response for Unit.
          The attribute names are case-sensitive, PascalCase, and
          expected in a comma-separated list format as in the JSON encoding.

          Supported attributes for Unit are ApprovedBy, BridgeWorkerID, ChangeSetID, FromLinkID, HeadMutationNum, HeadRevisionNum, LastAppliedRevisionNum, LiveRevisionNum, OrganizationID, PreviousLiveRevisionNum, SpaceID, TargetID, UnitEventID, UpstreamSpaceID, UpstreamUnitID.

          The whole string must be query-encoded.
        in: query
        name: include
        required: false
        schema:
          type: string
      - description: |-
          Select clause for specifying which fields to include in the response for Unit.
          The attribute names are case-sensitive, PascalCase, and
          expected in a comma-separated list format as in the JSON encoding.
          If not specified, all fields are returned.
          Entity and parent IDs (like OrganizationID, SpaceID, UnitID) and Slug are always returned regardless of the select parameter.
          Fields used in where and contains filters are also automatically included.
          Example: 'DisplayName,CreatedAt,Labels' will return only those fields plus the required ID and Slug fields.
          The whole string must be query-encoded.
        in: query
        name: select
        required: false
        schema:
          type: string
      - description: Unique identifier for a unit_id
        in: path
        name: unit_id
        required: true
        schema:
          format: uuid
          type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ExtendedUnit'
          description: Unit with capability to extend additional related entities.
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Unit request is invalid (Bad Request).
        "401":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Unauthorized access.
        "403":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Forbidden access.
        "404":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Unit not found.
        "500":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Something went wrong while processing Unit.
        default:
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Unexpected error.
      summary: Get Unit
      tags:
      - Unit
    patch:
      description: Patch Unit
      operationId: PatchUnit
      parameters:
      - description: Unique identifier for a space_id
        in: path
        name: space_id
        required: true
        schema:
          format: uuid
          type: string
      - description: Unique identifier for a unit_id
        in: path
        name: unit_id
        required: true
        schema:
          format: uuid
          type: string
      - description: Unique identifier for a revision_id
        in: query
        name: revision_id
        required: false
        schema:
          format: uuid
          type: string
          x-go-name: RevisionID
      - description: 'Dry run mode: return changed unit(s) but don''t update configuration
          data'
        in: query
        name: dry_run
        required: false
        schema:
          type: boolean
      - description: Upgrade the unit to the latest version of its upstream unit
        in: query
        name: upgrade
        required: false
        schema:
          type: boolean
      - description: 'Restore revision source. Supports: Named revisions (''LiveRevisionNum'',
          ''LastAppliedRevisionNum'', ''PreviousLiveRevisionNum'', ''HeadRevisionNum''),
          direct revision number (e.g., ''42''), or entity references (''Tag:uuid'',
          ''ChangeSet:uuid'', ''Revision:uuid''). Can be prefixed with ''Before:''
          to select the revision immediately before the specified one (e.g., ''Before:LiveRevisionNum'',
          ''Before:42''). When using Tag or ChangeSet references, the latest revision
          associated with that entity is selected.'
        in: query
        name: restore
        required: false
        schema:
          type: string
      - description: Resolve specified non-automatically resolved link from this (downstream)
          Unit to another (upstream) Unit. Expects Link:uuid or Link:*.
        in: query
        name: resolve
        required: false
        schema:
          type: string
      - description: Merge source unit. Currently it must be a unit ID or 'Self'.
        in: query
        name: merge_source
        required: false
        schema:
          type: string
      - description: 'Merge base revision, which provides the base configuration data
          of the changes to merge. With merge_source, this is a revision of the merge
          source unit. With merge_external_source, this is a revision of the unit
          being updated and overrides the default selection of the latest MergeExternal
          revision. Supports: Named revisions (''LiveRevisionNum'', ''LastAppliedRevisionNum'',
          ''PreviousLiveRevisionNum'', ''HeadRevisionNum''), direct revision number
          (e.g., ''42''), or entity references (''Tag:uuid'', ''ChangeSet:uuid'',
          ''Revision:uuid''). Can be prefixed with ''Before:'' to select the revision
          immediately before the specified one (e.g., ''Before:LiveRevisionNum'',
          ''Before:42''). When using Tag or ChangeSet references, the latest revision
          associated with that entity is selected.'
        in: query
        name: merge_base
        required: false
        schema:
          type: string
      - description: 'Merge end revision of the merge source, which provides the final
          configuration of the changes to merge. Supports: Named revisions (''LiveRevisionNum'',
          ''LastAppliedRevisionNum'', ''PreviousLiveRevisionNum'', ''HeadRevisionNum''),
          direct revision number (e.g., ''42''), or entity references (''Tag:uuid'',
          ''ChangeSet:uuid'', ''Revision:uuid''). Can be prefixed with ''Before:''
          to select the revision immediately before the specified one (e.g., ''Before:LiveRevisionNum'',
          ''Before:42''). When using Tag or ChangeSet references, the latest revision
          associated with that entity is selected.'
        in: query
        name: merge_end
        required: false
        schema:
          type: string
      - description: Identifier of the external source for merge-on-update. When set,
          computes mutations between the last MergeExternal revision and the provided
          data, then patches the current unit data with those mutations.
        in: query
        name: merge_external_source
        required: false
        schema:
          type: string
      - description: |-
          The specified string is an expression for the purpose of filtering
          the list of Mutations returned. The expression syntax was inspired by SQL.
          It supports conjunctions using `AND` of relational expressions of the form *attribute*
          *operator* *attribute_or_literal*. The attribute names are case-sensitive and PascalCase,
          as in the JSON encoding.
          Strings support the following operators: `<`, `>`, `<=`, `>=`, `=`, `!=`, `LIKE`, `NOT LIKE`, `ILIKE`, `~~`, `!~~`, `~`, `~*`, `!~`, `!~*`, `IN`, `NOT IN`.
          String pattern operators: `LIKE` and `~~` for pattern matching with `%` and `_` wildcards,
          `ILIKE` for case-insensitive pattern matching, `NOT LIKE` and `!~~` for negated pattern matching.
          String regex operators: `~` for regex matching, `~*` for case-insensitive regex,
          `!~` and `!~*` for regex not matching (case-sensitive and insensitive).
          Integers support the following operators: `<`, `>`, `<=`, `>=`, `=`, `!=`, `IN`, `NOT IN`.
          UUIDs and boolean attributes support equality and inequality only.
          UUID and time literals must be quoted as string literals.
          String literals are quoted with single quotes, such as `'string'`.
          Time literals use the same form as when serialized as JSON,
          such as: `CreatedAt > '2025-02-18T23:16:34'`.
          Integer and boolean literals are also supported for attributes of those types.
          Arrays support the `?` operator to to match any element of the array,
          as in `ApprovedBy ? '7c61626f-ddbe-41af-93f6-b69f4ab6d308'`.
          Arrays can perform LEN() to check for length, as in `LEN(ApprovedBy) > 0`.
          Map support the dot notation to specify a particular map key, as in `Labels.tier = 'Backend'`.
          Maps support `IS NULL` and `IS NOT NULL` with dot notation to check for key absence or presence,
          as in `Labels.tier IS NULL` (key doesn't exist) or `Labels.tier IS NOT NULL` (key exists).
          Comparison results can be tested with `IS TRUE`, `IS FALSE`, `IS NOT TRUE`, and `IS NOT FALSE`.
          These are useful for nullable columns: `MergeSourceID = '<uuid>' IS NOT FALSE` matches rows where MergeSourceID equals the value OR is NULL.
          The `IN` and `NOT IN` operators accept a comma-separated list of values in parentheses,
          such as `Slug IN ('slugone', 'slugtwo')` or `Labels.environment IN ('prod', 'staging')`.
          Conjunctions are supported using the `AND` operator.
          An example conjunction is:
          `CreatedAt >= '2025-01-07' AND Slug = 'test' AND Labels.mykey = 'myvalue'`.

          Supported attributes for filtering on Mutation: CreatedAt, FunctionName, InvocationID, LinkID, MergeBaseRevisionNum, MergeEndRevisionNum, MergeSourceID, MutationID, MutationNum, OrganizationID, RestoredRevisionNum, RevisionID, RevisionNum, SpaceID, Subgroup, TriggerID, UnitID, UpdatedAt, UpgradedFromUpstreamRevisionNum.

          Used to filter which mutations are affected during merge operations.

          The whole string must be query-encoded.
        in: query
        name: where_mutation
        required: false
        schema:
          type: string
      - description: |-
          UUID of a Filter entity to apply to the Mutation list.

          The Filter must be in the same Organization as the user credentials.

          The Filter's From field must match the entity type being filtered (Mutation).

          For Space-resident entities, if the Filter has a FromSpaceID, it must match the operation's SpaceID.

          The Filter's Where clause will be combined with any explicit 'where' parameter using AND logic.

          If both 'filter' and 'where' parameters are specified, they are combined with AND logic.
        in: query
        name: filter_mutation
        required: false
        schema:
          type: string
      - description: Tag ID to add to the head revision
        in: query
        name: tag
        required: false
        schema:
          format: uuid
          type: string
      - description: Must match ChangeSetID of affected Units if config Data is changed
          unless in dry run mode
        in: query
        name: change_set_id
        required: false
        schema:
          format: uuid
          type: string
      - description: User-defined category for the Mutation. Must be alphanumeric,
          at most 64 characters. The prefix 'ConfigHub' is reserved.
        in: query
        name: subgroup
        required: false
        schema:
          type: string
      requestBody:
        content:
          application/merge-patch+json:
            schema:
              description: JSON Merge Patch schema for Unit. All fields are optional
                and nullable. Readonly fields are ignored.
              properties:
                Annotations:
                  additionalProperties:
                    nullable: true
                    type: string
                  description: An optional map of Annotation key/value pairs for tools
                    to attach information to entities.
                  nullable: true
                  type: object
                ChangeSetID:
                  description: Unique identifier for the ChangeSet to which the current
                    Revision belongs. Optional. Units are not required to belong to
                    ChangeSets.
                  format: uuid
                  nullable: true
                  type: string
                Data:
                  description: The full configuration data for this unit.
                  format: byte
                  nullable: true
                  type: string
                DeleteGates:
                  additionalProperties:
                    nullable: true
                    type: boolean
                  description: An optional set of gates that, if any is present, will
                    block deletion
                  nullable: true
                  type: object
                DestroyGates:
                  additionalProperties:
                    nullable: true
                    type: boolean
                  description: An optional set of gates that, if any is present, will
                    block destroy operations
                  nullable: true
                  type: object
                DisplayName:
                  description: Friendly name for the entity.
                  nullable: true
                  type: string
                Labels:
                  additionalProperties:
                    nullable: true
                    type: string
                  description: An optional map of Label key/value pairs to specify
                    identifying attributes of entities for the purpose of grouping
                    and filtering them.
                  nullable: true
                  type: object
                LastChangeDescription:
                  description: LastChangeDescription is a human-readable description
                    of the last change. This description is copied to the new Revision
                    when the Data is changed.
                  nullable: true
                  type: string
                ProviderType:
                  description: ""
                  nullable: true
                  type: string
                Slug:
                  description: Unique URL-safe identifier for the entity.
                  nullable: true
                  type: string
                TargetID:
                  description: TargetID is the identifier of the target this unit
                    is associated with. This defines where the configuration will
                    be applied. It must be set to a valid Target within the same Space
                    before the Unit can be Applied, Destroyed, Imported, or Refreshed.
                  format: uuid
                  nullable: true
                  type: string
                TargetOptions:
                  additionalProperties:
                    nullable: true
                    type: string
                  description: Bridge option values set per-Unit, merged with the
                    Target's Options when sending to the bridge worker (Target's Options
                    take precedence on overlap). The options must be predefined by
                    the ConfigType in the BridgeWorker.
                  nullable: true
                  type: object
                ToolchainType:
                  description: ToolchainType specifies the type of toolchain for this
                    unit. Possible values include "Kubernetes/YAML", "OpenTofu/HCL",
                    "AppConfig/Properties", "AppConfig/YAML", "AppConfig/TOML", "AppConfig/INI",
                    "AppConfig/JSON", "AppConfig/Env", "AppConfig/Text", "ConfigHub/YAML".
                  nullable: true
                  type: string
                Version:
                  description: An entity-specific sequence number used for optimistic
                    concurrency control. The value read must be sent in calls to Update.
                  nullable: true
                  type: integer
              type: object
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Unit'
          description: |-
            Unit is the core unit of operation in ConfigHub. It contains a blob of configuration Data
            of a single supported Toolchain Type (configuration format). This blob is typically a text document
            that contains a collection of Kubernetes or infrastructure resources, or an application configuration
            file. Applying / deploying or destroying the configuration happens as a single *transaction*
            from ConfigHub's perspective. In reality, it is most often a multi-step workflow performed by
            the underlying configuration / deployment tool. The resources must belong to a single
            infrastructure provider and the actuation mechanism must be able to resolve references and
            ordering dependencies among the resources within the document. For example, if one resource
            needs to be fully provisioned to provide input to another resource, then the actuation code is
            responsible for handling this. Revisions store historical copies of the configuration data.
            Configuration data can be restored from prior Revisions. Units can also be cloned to create
            new variants of a configuration.
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Unit request is invalid (Bad Request).
        "401":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Unauthorized access.
        "403":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Forbidden access.
        "404":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Unit not found.
        "409":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Unit data conflict. Data has changed since last read.
        "422":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Unable to patch unit(s) due to configuration data problem
        "500":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Something went wrong while processing Unit.
        default:
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Unexpected error.
      summary: Patch Unit
      tags:
      - Unit
    put:
      description: Update Unit
      operationId: UpdateUnit
      parameters:
      - description: Unique identifier for a space_id
        in: path
        name: space_id
        required: true
        schema:
          format: uuid
          type: string
      - description: Unique identifier for a unit_id
        in: path
        name: unit_id
        required: true
        schema:
          format: uuid
          type: string
      - description: Unique identifier for a revision_id
        in: query
        name: revision_id
        required: false
        schema:
          format: uuid
          type: string
          x-go-name: RevisionID
      - description: 'Dry run mode: return changed unit(s) but don''t update configuration
          data'
        in: query
        name: dry_run
        required: false
        schema:
          type: boolean
      - description: Upgrade the unit to the latest version of its upstream unit
        in: query
        name: upgrade
        required: false
        schema:
          type: boolean
      - description: 'Restore revision source. Supports: Named revisions (''LiveRevisionNum'',
          ''LastAppliedRevisionNum'', ''PreviousLiveRevisionNum'', ''HeadRevisionNum''),
          direct revision number (e.g., ''42''), or entity references (''Tag:uuid'',
          ''ChangeSet:uuid'', ''Revision:uuid''). Can be prefixed with ''Before:''
          to select the revision immediately before the specified one (e.g., ''Before:LiveRevisionNum'',
          ''Before:42''). When using Tag or ChangeSet references, the latest revision
          associated with that entity is selected.'
        in: query
        name: restore
        required: false
        schema:
          type: string
      - description: Resolve specified non-automatically resolved link from this (downstream)
          Unit to another (upstream) Unit. Expects Link:uuid or Link:*.
        in: query
        name: resolve
        required: false
        schema:
          type: string
      - description: Merge source unit. Currently it must be a unit ID or 'Self'.
        in: query
        name: merge_source
        required: false
        schema:
          type: string
      - description: 'Merge base revision, which provides the base configuration data
          of the changes to merge. With merge_source, this is a revision of the merge
          source unit. With merge_external_source, this is a revision of the unit
          being updated and overrides the default selection of the latest MergeExternal
          revision. Supports: Named revisions (''LiveRevisionNum'', ''LastAppliedRevisionNum'',
          ''PreviousLiveRevisionNum'', ''HeadRevisionNum''), direct revision number
          (e.g., ''42''), or entity references (''Tag:uuid'', ''ChangeSet:uuid'',
          ''Revision:uuid''). Can be prefixed with ''Before:'' to select the revision
          immediately before the specified one (e.g., ''Before:LiveRevisionNum'',
          ''Before:42''). When using Tag or ChangeSet references, the latest revision
          associated with that entity is selected.'
        in: query
        name: merge_base
        required: false
        schema:
          type: string
      - description: 'Merge end revision of the merge source, which provides the final
          configuration of the changes to merge. Supports: Named revisions (''LiveRevisionNum'',
          ''LastAppliedRevisionNum'', ''PreviousLiveRevisionNum'', ''HeadRevisionNum''),
          direct revision number (e.g., ''42''), or entity references (''Tag:uuid'',
          ''ChangeSet:uuid'', ''Revision:uuid''). Can be prefixed with ''Before:''
          to select the revision immediately before the specified one (e.g., ''Before:LiveRevisionNum'',
          ''Before:42''). When using Tag or ChangeSet references, the latest revision
          associated with that entity is selected.'
        in: query
        name: merge_end
        required: false
        schema:
          type: string
      - description: Identifier of the external source for merge-on-update. When set,
          computes mutations between the last MergeExternal revision and the provided
          data, then patches the current unit data with those mutations.
        in: query
        name: merge_external_source
        required: false
        schema:
          type: string
      - description: |-
          The specified string is an expression for the purpose of filtering
          the list of Mutations returned. The expression syntax was inspired by SQL.
          It supports conjunctions using `AND` of relational expressions of the form *attribute*
          *operator* *attribute_or_literal*. The attribute names are case-sensitive and PascalCase,
          as in the JSON encoding.
          Strings support the following operators: `<`, `>`, `<=`, `>=`, `=`, `!=`, `LIKE`, `NOT LIKE`, `ILIKE`, `~~`, `!~~`, `~`, `~*`, `!~`, `!~*`, `IN`, `NOT IN`.
          String pattern operators: `LIKE` and `~~` for pattern matching with `%` and `_` wildcards,
          `ILIKE` for case-insensitive pattern matching, `NOT LIKE` and `!~~` for negated pattern matching.
          String regex operators: `~` for regex matching, `~*` for case-insensitive regex,
          `!~` and `!~*` for regex not matching (case-sensitive and insensitive).
          Integers support the following operators: `<`, `>`, `<=`, `>=`, `=`, `!=`, `IN`, `NOT IN`.
          UUIDs and boolean attributes support equality and inequality only.
          UUID and time literals must be quoted as string literals.
          String literals are quoted with single quotes, such as `'string'`.
          Time literals use the same form as when serialized as JSON,
          such as: `CreatedAt > '2025-02-18T23:16:34'`.
          Integer and boolean literals are also supported for attributes of those types.
          Arrays support the `?` operator to to match any element of the array,
          as in `ApprovedBy ? '7c61626f-ddbe-41af-93f6-b69f4ab6d308'`.
          Arrays can perform LEN() to check for length, as in `LEN(ApprovedBy) > 0`.
          Map support the dot notation to specify a particular map key, as in `Labels.tier = 'Backend'`.
          Maps support `IS NULL` and `IS NOT NULL` with dot notation to check for key absence or presence,
          as in `Labels.tier IS NULL` (key doesn't exist) or `Labels.tier IS NOT NULL` (key exists).
          Comparison results can be tested with `IS TRUE`, `IS FALSE`, `IS NOT TRUE`, and `IS NOT FALSE`.
          These are useful for nullable columns: `MergeSourceID = '<uuid>' IS NOT FALSE` matches rows where MergeSourceID equals the value OR is NULL.
          The `IN` and `NOT IN` operators accept a comma-separated list of values in parentheses,
          such as `Slug IN ('slugone', 'slugtwo')` or `Labels.environment IN ('prod', 'staging')`.
          Conjunctions are supported using the `AND` operator.
          An example conjunction is:
          `CreatedAt >= '2025-01-07' AND Slug = 'test' AND Labels.mykey = 'myvalue'`.

          Supported attributes for filtering on Mutation: CreatedAt, FunctionName, InvocationID, LinkID, MergeBaseRevisionNum, MergeEndRevisionNum, MergeSourceID, MutationID, MutationNum, OrganizationID, RestoredRevisionNum, RevisionID, RevisionNum, SpaceID, Subgroup, TriggerID, UnitID, UpdatedAt, UpgradedFromUpstreamRevisionNum.

          Used to filter which mutations are affected during merge operations.

          The whole string must be query-encoded.
        in: query
        name: where_mutation
        required: false
        schema:
          type: string
      - description: |-
          UUID of a Filter entity to apply to the Mutation list.

          The Filter must be in the same Organization as the user credentials.

          The Filter's From field must match the entity type being filtered (Mutation).

          For Space-resident entities, if the Filter has a FromSpaceID, it must match the operation's SpaceID.

          The Filter's Where clause will be combined with any explicit 'where' parameter using AND logic.

          If both 'filter' and 'where' parameters are specified, they are combined with AND logic.
        in: query
        name: filter_mutation
        required: false
        schema:
          type: string
      - description: Tag ID to add to the head revision
        in: query
        name: tag
        required: false
        schema:
          format: uuid
          type: string
      - description: Must match ChangeSetID of affected Units if config Data is changed
          unless in dry run mode
        in: query
        name: change_set_id
        required: false
        schema:
          format: uuid
          type: string
      - description: User-defined category for the Mutation. Must be alphanumeric,
          at most 64 characters. The prefix 'ConfigHub' is reserved.
        in: query
        name: subgroup
        required: false
        schema:
          type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/Unit'
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Unit'
          description: |-
            Unit is the core unit of operation in ConfigHub. It contains a blob of configuration Data
            of a single supported Toolchain Type (configuration format). This blob is typically a text document
            that contains a collection of Kubernetes or infrastructure resources, or an application configuration
            file. Applying / deploying or destroying the configuration happens as a single *transaction*
            from ConfigHub's perspective. In reality, it is most often a multi-step workflow performed by
            the underlying configuration / deployment tool. The resources must belong to a single
            infrastructure provider and the actuation mechanism must be able to resolve references and
            ordering dependencies among the resources within the document. For example, if one resource
            needs to be fully provisioned to provide input to another resource, then the actuation code is
            responsible for handling this. Revisions store historical copies of the configuration data.
            Configuration data can be restored from prior Revisions. Units can also be cloned to create
            new variants of a configuration.
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Unit request is invalid (Bad Request).
        "401":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Unauthorized access.
        "403":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Forbidden access.
        "404":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Unit not found.
        "409":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Unit data conflict. Data has changed since last read.
        "422":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Unable to update unit(s) due to configuration data problem
        "500":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Something went wrong while processing Unit.
        default:
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Unexpected error.
      summary: Update Unit
      tags:
      - Unit
  /space/{space_id}/unit/{unit_id}/apply:
    post:
      description: Triggers an apply operation using the config data to a Bridge Worker
      operationId: ApplyUnit
      parameters:
      - description: Unique identifier for a space_id
        in: path
        name: space_id
        required: true
        schema:
          format: uuid
          type: string
      - description: Unique identifier for a unit_id
        in: path
        name: unit_id
        required: true
        schema:
          format: uuid
          type: string
      - description: Revision to apply (defaults to HeadRevisionNum). Can be a revision
          number, 'LiveRevisionNum', 'LastAppliedRevisionNum', 'Tag:uuid', 'ChangeSet:uuid',
          etc.
        in: query
        name: revision
        required: false
        schema:
          type: string
      - description: Dry run mode - validates which units would be applied without
          executing
        in: query
        name: dry_run
        required: false
        schema:
          type: boolean
      - description: 'Drift reconciliation mode. Valid values: OnDemand, ContinuousApply,
          ContinuousRefresh. If not specified, the current value on the Unit is used.'
        in: query
        name: drift_mode
        required: false
        schema:
          type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/QueuedOperation'
          description: "UnitAction is a record of an action to be performed by a Bridge
            Worker. They are queued and sent to the worker in creation order. \nIf
            the worker is temporarily disconnected the queued actions will be sent
            when the worker reconnects or responds.\nIf there are links between units
            applied or destroyed in a single API call, they will be sent to the appropriate\nworker(s)
            in the appropriate order (reverse or forword topological order). One or
            more UnitEvents will correspond\nto each UnitAction."
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Unit request is invalid (Bad Request).
        "401":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Unauthorized access.
        "403":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Forbidden access.
        "404":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Unit not found.
        "409":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Unit data conflict. Data has changed since last read.
        "422":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Bridge worker unavailable
        "500":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Something went wrong while processing Unit.
        default:
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Unexpected error.
      summary: Apply a unit configuration
      tags:
      - Unit
  /space/{space_id}/unit/{unit_id}/approve:
    post:
      description: Record that the user invoking this operation approves the configuration
        data
      operationId: ApproveUnit
      parameters:
      - description: Unique identifier for a space_id
        in: path
        name: space_id
        required: true
        schema:
          format: uuid
          type: string
      - description: Unique identifier for a unit_id
        in: path
        name: unit_id
        required: true
        schema:
          format: uuid
          type: string
      - description: Revision to approve (defaults to HeadRevisionNum). Can be a revision
          number, 'LiveRevisionNum', 'LastAppliedRevisionNum', 'Tag:uuid', 'ChangeSet:uuid',
          etc.
        in: query
        name: revision
        required: false
        schema:
          type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ApproveResponse'
          description: OK
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Unit request is invalid (Bad Request).
        "401":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Unauthorized access.
        "403":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Forbidden access.
        "404":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Unit not found.
        "409":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Unit data conflict. Data has changed since last read.
        "500":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Something went wrong while processing Unit.
        default:
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Unexpected error.
      summary: Approve a unit configuration
      tags:
      - Unit
  /space/{space_id}/unit/{unit_id}/data:
    get:
      description: Download the config data of a unit
      operationId: DownloadUnitData
      parameters:
      - description: Unique identifier for a space_id
        in: path
        name: space_id
        required: true
        schema:
          format: uuid
          type: string
      - description: Unique identifier for a unit_id
        in: path
        name: unit_id
        required: true
        schema:
          format: uuid
          type: string
      responses:
        "200":
          content:
            application/octet-stream:
              schema:
                format: base64
                type: string
          description: OK
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Unit request is invalid (Bad Request).
        "401":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Unauthorized access.
        "403":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Forbidden access.
        "404":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Unit not found.
        "500":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Something went wrong while processing Unit.
        default:
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Unexpected error.
      summary: Download config data
      tags:
      - Unit
  /space/{space_id}/unit/{unit_id}/destroy:
    post:
      description: Triggers a destroy operation using the config data to a Bridge
        Worker
      operationId: DestroyUnit
      parameters:
      - description: Unique identifier for a space_id
        in: path
        name: space_id
        required: true
        schema:
          format: uuid
          type: string
      - description: Unique identifier for a unit_id
        in: path
        name: unit_id
        required: true
        schema:
          format: uuid
          type: string
      - description: Dry run mode - validates which units would be destroyed without
          executing
        in: query
        name: dry_run
        required: false
        schema:
          type: boolean
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/QueuedOperation'
          description: "UnitAction is a record of an action to be performed by a Bridge
            Worker. They are queued and sent to the worker in creation order. \nIf
            the worker is temporarily disconnected the queued actions will be sent
            when the worker reconnects or responds.\nIf there are links between units
            applied or destroyed in a single API call, they will be sent to the appropriate\nworker(s)
            in the appropriate order (reverse or forword topological order). One or
            more UnitEvents will correspond\nto each UnitAction."
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Unit request is invalid (Bad Request).
        "401":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Unauthorized access.
        "403":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Forbidden access.
        "404":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Unit not found.
        "409":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Unit data conflict. Data has changed since last read.
        "422":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Bridge worker unavailable
        "500":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Something went wrong while processing Unit.
        default:
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Unexpected error.
      summary: Destroy a unit configuration
      tags:
      - Unit
  /space/{space_id}/unit/{unit_id}/extended:
    get:
      description: Get Unit details with expanded relationships
      operationId: GetUnitExtended
      parameters:
      - description: Unique identifier for a space_id
        in: path
        name: space_id
        required: true
        schema:
          format: uuid
          type: string
      - description: Unique identifier for a unit_id
        in: path
        name: unit_id
        required: true
        schema:
          format: uuid
          type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnitExtended'
          description: OK
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Unit request is invalid (Bad Request).
        "401":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Unauthorized access.
        "403":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Forbidden access.
        "404":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Unit not found.
        "500":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Something went wrong while processing Unit.
        default:
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Unexpected error.
      summary: Get Unit details with expanded relationships
      tags:
      - Unit
  /space/{space_id}/unit/{unit_id}/import:
    post:
      description: Triggers an import operation using the config data to a Bridge
        Worker
      operationId: ImportUnit
      parameters:
      - description: Unique identifier for a space_id
        in: path
        name: space_id
        required: true
        schema:
          format: uuid
          type: string
      - description: Unique identifier for a unit_id
        in: path
        name: unit_id
        required: true
        schema:
          format: uuid
          type: string
      - description: Dry run mode - returns import data in the operation/action
        in: query
        name: dry_run
        required: false
        schema:
          type: boolean
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ImportRequest'
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/QueuedOperation'
          description: "UnitAction is a record of an action to be performed by a Bridge
            Worker. They are queued and sent to the worker in creation order. \nIf
            the worker is temporarily disconnected the queued actions will be sent
            when the worker reconnects or responds.\nIf there are links between units
            applied or destroyed in a single API call, they will be sent to the appropriate\nworker(s)
            in the appropriate order (reverse or forword topological order). One or
            more UnitEvents will correspond\nto each UnitAction."
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Unit request is invalid (Bad Request).
        "401":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Unauthorized access.
        "403":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Forbidden access.
        "404":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Unit not found.
        "409":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Unit data conflict. Data has changed since last read.
        "422":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Bridge worker unavailable
        "500":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Something went wrong while processing Unit.
        default:
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Unexpected error.
      summary: Import a configuration unit
      tags:
      - Unit
  /space/{space_id}/unit/{unit_id}/live_data:
    get:
      description: Download the live data of a unit
      operationId: DownloadUnitLiveData
      parameters:
      - description: Unique identifier for a space_id
        in: path
        name: space_id
        required: true
        schema:
          format: uuid
          type: string
      - description: Unique identifier for a unit_id
        in: path
        name: unit_id
        required: true
        schema:
          format: uuid
          type: string
      responses:
        "200":
          content:
            application/octet-stream:
              schema:
                format: base64
                type: string
          description: OK
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Unit request is invalid (Bad Request).
        "401":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Unauthorized access.
        "403":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Forbidden access.
        "404":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Unit not found.
        "500":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Something went wrong while processing Unit.
        default:
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Unexpected error.
      summary: Download live data
      tags:
      - Unit
  /space/{space_id}/unit/{unit_id}/live_state:
    get:
      description: Download the live state of a unit
      operationId: DownloadUnitLiveState
      parameters:
      - description: Unique identifier for a space_id
        in: path
        name: space_id
        required: true
        schema:
          format: uuid
          type: string
      - description: Unique identifier for a unit_id
        in: path
        name: unit_id
        required: true
        schema:
          format: uuid
          type: string
      responses:
        "200":
          content:
            application/octet-stream:
              schema:
                format: base64
                type: string
          description: OK
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Unit request is invalid (Bad Request).
        "401":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Unauthorized access.
        "403":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Forbidden access.
        "404":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Unit not found.
        "500":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Something went wrong while processing Unit.
        default:
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Unexpected error.
      summary: Download live state
      tags:
      - Unit
  /space/{space_id}/unit/{unit_id}/mutation:
    get:
      description: List ExtendedMutations
      operationId: ListExtendedMutations
      parameters:
      - description: Unique identifier for a space_id
        in: path
        name: space_id
        required: true
        schema:
          format: uuid
          type: string
      - description: Unique identifier for a unit_id
        in: path
        name: unit_id
        required: true
        schema:
          format: uuid
          type: string
      - description: |-
          The specified string is an expression for the purpose of filtering
          the list of Mutations returned. The expression syntax was inspired by SQL.
          It supports conjunctions using `AND` of relational expressions of the form *attribute*
          *operator* *attribute_or_literal*. The attribute names are case-sensitive and PascalCase,
          as in the JSON encoding.
          Strings support the following operators: `<`, `>`, `<=`, `>=`, `=`, `!=`, `LIKE`, `NOT LIKE`, `ILIKE`, `~~`, `!~~`, `~`, `~*`, `!~`, `!~*`, `IN`, `NOT IN`.
          String pattern operators: `LIKE` and `~~` for pattern matching with `%` and `_` wildcards,
          `ILIKE` for case-insensitive pattern matching, `NOT LIKE` and `!~~` for negated pattern matching.
          String regex operators: `~` for regex matching, `~*` for case-insensitive regex,
          `!~` and `!~*` for regex not matching (case-sensitive and insensitive).
          Integers support the following operators: `<`, `>`, `<=`, `>=`, `=`, `!=`, `IN`, `NOT IN`.
          UUIDs and boolean attributes support equality and inequality only.
          UUID and time literals must be quoted as string literals.
          String literals are quoted with single quotes, such as `'string'`.
          Time literals use the same form as when serialized as JSON,
          such as: `CreatedAt > '2025-02-18T23:16:34'`.
          Integer and boolean literals are also supported for attributes of those types.
          Arrays support the `?` operator to to match any element of the array,
          as in `ApprovedBy ? '7c61626f-ddbe-41af-93f6-b69f4ab6d308'`.
          Arrays can perform LEN() to check for length, as in `LEN(ApprovedBy) > 0`.
          Map support the dot notation to specify a particular map key, as in `Labels.tier = 'Backend'`.
          Maps support `IS NULL` and `IS NOT NULL` with dot notation to check for key absence or presence,
          as in `Labels.tier IS NULL` (key doesn't exist) or `Labels.tier IS NOT NULL` (key exists).
          Comparison results can be tested with `IS TRUE`, `IS FALSE`, `IS NOT TRUE`, and `IS NOT FALSE`.
          These are useful for nullable columns: `MergeSourceID = '<uuid>' IS NOT FALSE` matches rows where MergeSourceID equals the value OR is NULL.
          The `IN` and `NOT IN` operators accept a comma-separated list of values in parentheses,
          such as `Slug IN ('slugone', 'slugtwo')` or `Labels.environment IN ('prod', 'staging')`.
          Conjunctions are supported using the `AND` operator.
          An example conjunction is:
          `CreatedAt >= '2025-01-07' AND Slug = 'test' AND Labels.mykey = 'myvalue'`.

          Supported attributes for filtering on Mutation: CreatedAt, FunctionName, InvocationID, LinkID, MergeBaseRevisionNum, MergeEndRevisionNum, MergeSourceID, MutationID, MutationNum, OrganizationID, RestoredRevisionNum, RevisionID, RevisionNum, SpaceID, Subgroup, TriggerID, UnitID, UpdatedAt, UpgradedFromUpstreamRevisionNum.

          The whole string must be query-encoded.
        in: query
        name: where
        required: false
        schema:
          type: string
      - description: |-
          UUID of a Filter entity to apply to the Mutation list.

          The Filter must be in the same Organization as the user credentials.

          The Filter's From field must match the entity type being filtered (Mutation).

          For Space-resident entities, if the Filter has a FromSpaceID, it must match the operation's SpaceID.

          The Filter's Where clause will be combined with any explicit 'where' parameter using AND logic.

          If both 'filter' and 'where' parameters are specified, they are combined with AND logic.
        in: query
        name: filter
        required: false
        schema:
          type: string
      - description: |-
          Free text search that approximately matches the specified string against string fields and map keys/values.

          The search is case-insensitive and uses pattern matching to find entities containing the text.

          Searchable string fields include attributes like Slug, DisplayName, and string-typed custom fields.

          For map fields (like Labels and Annotations), the search matches both map keys and values.

          The search uses OR logic across all searchable fields, so matching any field will return the entity.

          If both 'where' and 'contains' parameters are specified, they are combined with AND logic.

          Searchable fields for Mutation include string and map-type attributes from the queryable attributes list.

          The whole string must be query-encoded.
        in: query
        name: contains
        required: false
        schema:
          type: string
      - description: |-
          Include clause for expanding related entities in the response for Mutation.
          The attribute names are case-sensitive, PascalCase, and
          expected in a comma-separated list format as in the JSON encoding.

          Supported attributes for Mutation are InvocationID, LinkID, MergeSourceID, OrganizationID, RevisionID, SpaceID, TriggerID, UnitID.

          The whole string must be query-encoded.
        in: query
        name: include
        required: false
        schema:
          type: string
      - description: |-
          Select clause for specifying which fields to include in the response for Mutation.
          The attribute names are case-sensitive, PascalCase, and
          expected in a comma-separated list format as in the JSON encoding.
          If not specified, all fields are returned.
          Entity and parent IDs (like OrganizationID, SpaceID, MutationID) and Slug are always returned regardless of the select parameter.
          Fields used in where and contains filters are also automatically included.
          Example: 'DisplayName,CreatedAt,Labels' will return only those fields plus the required ID and Slug fields.
          The whole string must be query-encoded.
        in: query
        name: select
        required: false
        schema:
          type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                items:
                  $ref: '#/components/schemas/ExtendedMutation'
                type: array
          description: OK
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: ExtendedMutation request is invalid (Bad Request).
        "401":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Unauthorized access.
        "403":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Forbidden access.
        "404":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: ExtendedMutation not found.
        "500":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Something went wrong while processing ExtendedMutation.
        default:
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Unexpected error.
      summary: List ExtendedMutations
      tags:
      - Mutation
  /space/{space_id}/unit/{unit_id}/mutation/{mutation_id}:
    get:
      description: Get ExtendedMutation
      operationId: GetExtendedMutation
      parameters:
      - description: Unique identifier for a space_id
        in: path
        name: space_id
        required: true
        schema:
          format: uuid
          type: string
      - description: Unique identifier for a unit_id
        in: path
        name: unit_id
        required: true
        schema:
          format: uuid
          type: string
      - description: |-
          Include clause for expanding related entities in the response for Mutation.
          The attribute names are case-sensitive, PascalCase, and
          expected in a comma-separated list format as in the JSON encoding.

          Supported attributes for Mutation are InvocationID, LinkID, MergeSourceID, OrganizationID, RevisionID, SpaceID, TriggerID, UnitID.

          The whole string must be query-encoded.
        in: query
        name: include
        required: false
        schema:
          type: string
      - description: |-
          Select clause for specifying which fields to include in the response for Mutation.
          The attribute names are case-sensitive, PascalCase, and
          expected in a comma-separated list format as in the JSON encoding.
          If not specified, all fields are returned.
          Entity and parent IDs (like OrganizationID, SpaceID, MutationID) and Slug are always returned regardless of the select parameter.
          Fields used in where and contains filters are also automatically included.
          Example: 'DisplayName,CreatedAt,Labels' will return only those fields plus the required ID and Slug fields.
          The whole string must be query-encoded.
        in: query
        name: select
        required: false
        schema:
          type: string
      - description: Unique identifier for a mutation_id
        in: path
        name: mutation_id
        required: true
        schema:
          format: uuid
          type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ExtendedMutation'
          description: OK
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: ExtendedMutation request is invalid (Bad Request).
        "401":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Unauthorized access.
        "403":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Forbidden access.
        "404":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: ExtendedMutation not found.
        "500":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Something went wrong while processing ExtendedMutation.
        default:
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Unexpected error.
      summary: Get ExtendedMutation
      tags:
      - Mutation
  /space/{space_id}/unit/{unit_id}/refresh:
    post:
      description: Triggers a refresh operation using the config data to a Bridge
        Worker
      operationId: RefreshUnit
      parameters:
      - description: Unique identifier for a space_id
        in: path
        name: space_id
        required: true
        schema:
          format: uuid
          type: string
      - description: Unique identifier for a unit_id
        in: path
        name: unit_id
        required: true
        schema:
          format: uuid
          type: string
      - description: Dry run mode - returns refresh data in the operation/action and
          updates LiveData and LiveState in the unit
        in: query
        name: dry_run
        required: false
        schema:
          type: boolean
      - description: 'Drift reconciliation mode. Valid values: OnDemand, ContinuousApply,
          ContinuousRefresh. If not specified, the current value on the Unit is used.'
        in: query
        name: drift_mode
        required: false
        schema:
          type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/QueuedOperation'
          description: "UnitAction is a record of an action to be performed by a Bridge
            Worker. They are queued and sent to the worker in creation order. \nIf
            the worker is temporarily disconnected the queued actions will be sent
            when the worker reconnects or responds.\nIf there are links between units
            applied or destroyed in a single API call, they will be sent to the appropriate\nworker(s)
            in the appropriate order (reverse or forword topological order). One or
            more UnitEvents will correspond\nto each UnitAction."
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Unit request is invalid (Bad Request).
        "401":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Unauthorized access.
        "403":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Forbidden access.
        "404":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Unit not found.
        "409":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Unit data conflict. Data has changed since last read.
        "422":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Bridge worker unavailable
        "500":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Something went wrong while processing Unit.
        default:
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Unexpected error.
      summary: Refresh a unit configuration
      tags:
      - Unit
  /space/{space_id}/unit/{unit_id}/revision:
    get:
      description: List ExtendedRevisions
      operationId: ListExtendedRevisions
      parameters:
      - description: Unique identifier for a space_id
        in: path
        name: space_id
        required: true
        schema:
          format: uuid
          type: string
      - description: Unique identifier for a unit_id
        in: path
        name: unit_id
        required: true
        schema:
          format: uuid
          type: string
      - description: |-
          The specified string is an expression for the purpose of filtering
          the list of Revisions returned. The expression syntax was inspired by SQL.
          It supports conjunctions using `AND` of relational expressions of the form *attribute*
          *operator* *attribute_or_literal*. The attribute names are case-sensitive and PascalCase,
          as in the JSON encoding.
          Strings support the following operators: `<`, `>`, `<=`, `>=`, `=`, `!=`, `LIKE`, `NOT LIKE`, `ILIKE`, `~~`, `!~~`, `~`, `~*`, `!~`, `!~*`, `IN`, `NOT IN`.
          String pattern operators: `LIKE` and `~~` for pattern matching with `%` and `_` wildcards,
          `ILIKE` for case-insensitive pattern matching, `NOT LIKE` and `!~~` for negated pattern matching.
          String regex operators: `~` for regex matching, `~*` for case-insensitive regex,
          `!~` and `!~*` for regex not matching (case-sensitive and insensitive).
          Integers support the following operators: `<`, `>`, `<=`, `>=`, `=`, `!=`, `IN`, `NOT IN`.
          UUIDs and boolean attributes support equality and inequality only.
          UUID and time literals must be quoted as string literals.
          String literals are quoted with single quotes, such as `'string'`.
          Time literals use the same form as when serialized as JSON,
          such as: `CreatedAt > '2025-02-18T23:16:34'`.
          Integer and boolean literals are also supported for attributes of those types.
          Arrays support the `?` operator to to match any element of the array,
          as in `ApprovedBy ? '7c61626f-ddbe-41af-93f6-b69f4ab6d308'`.
          Arrays can perform LEN() to check for length, as in `LEN(ApprovedBy) > 0`.
          Map support the dot notation to specify a particular map key, as in `Labels.tier = 'Backend'`.
          Maps support `IS NULL` and `IS NOT NULL` with dot notation to check for key absence or presence,
          as in `Labels.tier IS NULL` (key doesn't exist) or `Labels.tier IS NOT NULL` (key exists).
          Comparison results can be tested with `IS TRUE`, `IS FALSE`, `IS NOT TRUE`, and `IS NOT FALSE`.
          These are useful for nullable columns: `MergeSourceID = '<uuid>' IS NOT FALSE` matches rows where MergeSourceID equals the value OR is NULL.
          The `IN` and `NOT IN` operators accept a comma-separated list of values in parentheses,
          such as `Slug IN ('slugone', 'slugtwo')` or `Labels.environment IN ('prod', 'staging')`.
          Conjunctions are supported using the `AND` operator.
          An example conjunction is:
          `CreatedAt >= '2025-01-07' AND Slug = 'test' AND Labels.mykey = 'myvalue'`.

          Supported attributes for filtering on Revision: ApplyGates, ApplyWarnings, ApprovedBy, ChangeSetID, CreatedAt, DataHash, Description, LiveAt, OrganizationID, RevisionID, RevisionNum, Source, SpaceID, Tags, UnitID, UpdatedAt, UserAgent, UserID.

          To list a tagged Revision use `Tags ? '<tag-id>'`.

          The whole string must be query-encoded.
        in: query
        name: where
        required: false
        schema:
          type: string
      - description: |-
          UUID of a Filter entity to apply to the Revision list.

          The Filter must be in the same Organization as the user credentials.

          The Filter's From field must match the entity type being filtered (Revision).

          For Space-resident entities, if the Filter has a FromSpaceID, it must match the operation's SpaceID.

          The Filter's Where clause will be combined with any explicit 'where' parameter using AND logic.

          If both 'filter' and 'where' parameters are specified, they are combined with AND logic.
        in: query
        name: filter
        required: false
        schema:
          type: string
      - description: |-
          Free text search that approximately matches the specified string against string fields and map keys/values.

          The search is case-insensitive and uses pattern matching to find entities containing the text.

          Searchable string fields include attributes like Slug, DisplayName, and string-typed custom fields.

          For map fields (like Labels and Annotations), the search matches both map keys and values.

          The search uses OR logic across all searchable fields, so matching any field will return the entity.

          If both 'where' and 'contains' parameters are specified, they are combined with AND logic.

          Searchable fields for Revision include string and map-type attributes from the queryable attributes list.

          The whole string must be query-encoded.
        in: query
        name: contains
        required: false
        schema:
          type: string
      - description: |-
          Include clause for expanding related entities in the response for Revision.
          The attribute names are case-sensitive, PascalCase, and
          expected in a comma-separated list format as in the JSON encoding.

          Supported attributes for Revision are ChangeSetID, OrganizationID, SpaceID, Tags, UnitID, UserID.

          The whole string must be query-encoded.
        in: query
        name: include
        required: false
        schema:
          type: string
      - description: |-
          Select clause for specifying which fields to include in the response for Revision.
          The attribute names are case-sensitive, PascalCase, and
          expected in a comma-separated list format as in the JSON encoding.
          If not specified, all fields are returned.
          Entity and parent IDs (like OrganizationID, SpaceID, RevisionID) and Slug are always returned regardless of the select parameter.
          Fields used in where and contains filters are also automatically included.
          Example: 'DisplayName,CreatedAt,Labels' will return only those fields plus the required ID and Slug fields.
          The whole string must be query-encoded.
        in: query
        name: select
        required: false
        schema:
          type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                items:
                  $ref: '#/components/schemas/ExtendedRevision'
                type: array
          description: OK
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: ExtendedRevision request is invalid (Bad Request).
        "401":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Unauthorized access.
        "403":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Forbidden access.
        "404":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: ExtendedRevision not found.
        "500":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Something went wrong while processing ExtendedRevision.
        default:
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Unexpected error.
      summary: List ExtendedRevisions
      tags:
      - Revision
  /space/{space_id}/unit/{unit_id}/revision/{revision_id}:
    get:
      description: Get ExtendedRevision
      operationId: GetExtendedRevision
      parameters:
      - description: Unique identifier for a space_id
        in: path
        name: space_id
        required: true
        schema:
          format: uuid
          type: string
      - description: Unique identifier for a unit_id
        in: path
        name: unit_id
        required: true
        schema:
          format: uuid
          type: string
      - description: |-
          Include clause for expanding related entities in the response for Revision.
          The attribute names are case-sensitive, PascalCase, and
          expected in a comma-separated list format as in the JSON encoding.

          Supported attributes for Revision are ChangeSetID, OrganizationID, SpaceID, Tags, UnitID, UserID.

          The whole string must be query-encoded.
        in: query
        name: include
        required: false
        schema:
          type: string
      - description: |-
          Select clause for specifying which fields to include in the response for Revision.
          The attribute names are case-sensitive, PascalCase, and
          expected in a comma-separated list format as in the JSON encoding.
          If not specified, all fields are returned.
          Entity and parent IDs (like OrganizationID, SpaceID, RevisionID) and Slug are always returned regardless of the select parameter.
          Fields used in where and contains filters are also automatically included.
          Example: 'DisplayName,CreatedAt,Labels' will return only those fields plus the required ID and Slug fields.
          The whole string must be query-encoded.
        in: query
        name: select
        required: false
        schema:
          type: string
      - description: Unique identifier for a revision_id
        in: path
        name: revision_id
        required: true
        schema:
          format: uuid
          type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ExtendedRevision'
          description: OK
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: ExtendedRevision request is invalid (Bad Request).
        "401":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Unauthorized access.
        "403":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Forbidden access.
        "404":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: ExtendedRevision not found.
        "500":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Something went wrong while processing ExtendedRevision.
        default:
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Unexpected error.
      summary: Get ExtendedRevision
      tags:
      - Revision
  /space/{space_id}/unit/{unit_id}/revision/{revision_id}/data:
    get:
      description: Download the selected Revision's data
      operationId: DownloadRevisionData
      parameters:
      - description: Unique identifier for a space_id
        in: path
        name: space_id
        required: true
        schema:
          format: uuid
          type: string
      - description: Unique identifier for a unit_id
        in: path
        name: unit_id
        required: true
        schema:
          format: uuid
          type: string
      - description: Unique identifier for a revision_id
        in: path
        name: revision_id
        required: true
        schema:
          format: uuid
          type: string
      responses:
        "200":
          content:
            application/octet-stream:
              schema:
                type: string
          description: OK
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Revision request is invalid (Bad Request).
        "401":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Unauthorized access.
        "403":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Forbidden access.
        "404":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Revision not found.
        "500":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Something went wrong while processing Revision.
        default:
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Unexpected error.
      summary: Download the selected Revision's data
      tags:
      - Revision
  /space/{space_id}/unit/{unit_id}/unit_action:
    get:
      description: List UnitActions
      operationId: ListUnitActions
      parameters:
      - description: Unique identifier for a space_id
        in: path
        name: space_id
        required: true
        schema:
          format: uuid
          type: string
      - description: Unique identifier for a unit_id
        in: path
        name: unit_id
        required: true
        schema:
          format: uuid
          type: string
      - description: |-
          The specified string is an expression for the purpose of filtering
          the list of QueuedOperations returned. The expression syntax was inspired by SQL.
          It supports conjunctions using `AND` of relational expressions of the form *attribute*
          *operator* *attribute_or_literal*. The attribute names are case-sensitive and PascalCase,
          as in the JSON encoding.
          Strings support the following operators: `<`, `>`, `<=`, `>=`, `=`, `!=`, `LIKE`, `NOT LIKE`, `ILIKE`, `~~`, `!~~`, `~`, `~*`, `!~`, `!~*`, `IN`, `NOT IN`.
          String pattern operators: `LIKE` and `~~` for pattern matching with `%` and `_` wildcards,
          `ILIKE` for case-insensitive pattern matching, `NOT LIKE` and `!~~` for negated pattern matching.
          String regex operators: `~` for regex matching, `~*` for case-insensitive regex,
          `!~` and `!~*` for regex not matching (case-sensitive and insensitive).
          Integers support the following operators: `<`, `>`, `<=`, `>=`, `=`, `!=`, `IN`, `NOT IN`.
          UUIDs and boolean attributes support equality and inequality only.
          UUID and time literals must be quoted as string literals.
          String literals are quoted with single quotes, such as `'string'`.
          Time literals use the same form as when serialized as JSON,
          such as: `CreatedAt > '2025-02-18T23:16:34'`.
          Integer and boolean literals are also supported for attributes of those types.
          Arrays support the `?` operator to to match any element of the array,
          as in `ApprovedBy ? '7c61626f-ddbe-41af-93f6-b69f4ab6d308'`.
          Arrays can perform LEN() to check for length, as in `LEN(ApprovedBy) > 0`.
          Map support the dot notation to specify a particular map key, as in `Labels.tier = 'Backend'`.
          Maps support `IS NULL` and `IS NOT NULL` with dot notation to check for key absence or presence,
          as in `Labels.tier IS NULL` (key doesn't exist) or `Labels.tier IS NOT NULL` (key exists).
          Comparison results can be tested with `IS TRUE`, `IS FALSE`, `IS NOT TRUE`, and `IS NOT FALSE`.
          These are useful for nullable columns: `MergeSourceID = '<uuid>' IS NOT FALSE` matches rows where MergeSourceID equals the value OR is NULL.
          The `IN` and `NOT IN` operators accept a comma-separated list of values in parentheses,
          such as `Slug IN ('slugone', 'slugtwo')` or `Labels.environment IN ('prod', 'staging')`.
          Conjunctions are supported using the `AND` operator.
          An example conjunction is:
          `CreatedAt >= '2025-01-07' AND Slug = 'test' AND Labels.mykey = 'myvalue'`.

          Supported attributes for filtering on QueuedOperation: Action, BridgeWorkerID, CreatedAt, DriftReconciliationMode, DryRun, OrganizationID, QueuedOperationID, RevisionNum, SpaceID, Status, TargetID, UnitActionNum, UnitID.

          The whole string must be query-encoded.
        in: query
        name: where
        required: false
        schema:
          type: string
      - description: |-
          UUID of a Filter entity to apply to the QueuedOperation list.

          The Filter must be in the same Organization as the user credentials.

          The Filter's From field must match the entity type being filtered (QueuedOperation).

          For Space-resident entities, if the Filter has a FromSpaceID, it must match the operation's SpaceID.

          The Filter's Where clause will be combined with any explicit 'where' parameter using AND logic.

          If both 'filter' and 'where' parameters are specified, they are combined with AND logic.
        in: query
        name: filter
        required: false
        schema:
          type: string
      - description: |-
          Free text search that approximately matches the specified string against string fields and map keys/values.

          The search is case-insensitive and uses pattern matching to find entities containing the text.

          Searchable string fields include attributes like Slug, DisplayName, and string-typed custom fields.

          For map fields (like Labels and Annotations), the search matches both map keys and values.

          The search uses OR logic across all searchable fields, so matching any field will return the entity.

          If both 'where' and 'contains' parameters are specified, they are combined with AND logic.

          Searchable fields for QueuedOperation include string and map-type attributes from the queryable attributes list.

          The whole string must be query-encoded.
        in: query
        name: contains
        required: false
        schema:
          type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                items:
                  $ref: '#/components/schemas/UnitAction'
                type: array
          description: OK
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: UnitAction request is invalid (Bad Request).
        "401":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Unauthorized access.
        "403":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Forbidden access.
        "404":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: UnitAction not found.
        "500":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Something went wrong while processing UnitAction.
        default:
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Unexpected error.
      summary: List UnitActions
      tags:
      - UnitAction
  /space/{space_id}/unit/{unit_id}/unit_action/{unit_action_id}:
    get:
      description: Get UnitAction
      operationId: GetUnitAction
      parameters:
      - description: Unique identifier for a space_id
        in: path
        name: space_id
        required: true
        schema:
          format: uuid
          type: string
      - description: Unique identifier for a unit_id
        in: path
        name: unit_id
        required: true
        schema:
          format: uuid
          type: string
      - description: Unique identifier for a unit_action_id
        in: path
        name: unit_action_id
        required: true
        schema:
          format: uuid
          type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnitAction'
          description: "UnitAction is a record of an action to be performed by a Bridge
            Worker. They are queued and sent to the worker in creation order. \nIf
            the worker is temporarily disconnected the queued actions will be sent
            when the worker reconnects or responds.\nIf there are links between units
            applied or destroyed in a single API call, they will be sent to the appropriate\nworker(s)
            in the appropriate order (reverse or forword topological order). One or
            more UnitEvents will correspond\nto each UnitAction."
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: UnitAction request is invalid (Bad Request).
        "401":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Unauthorized access.
        "403":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Forbidden access.
        "404":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: UnitAction not found.
        "500":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Something went wrong while processing UnitAction.
        default:
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Unexpected error.
      summary: Get UnitAction
      tags:
      - UnitAction
  /space/{space_id}/unit/{unit_id}/unit_event:
    get:
      description: List UnitEvents
      operationId: ListUnitEvents
      parameters:
      - description: Unique identifier for a space_id
        in: path
        name: space_id
        required: true
        schema:
          format: uuid
          type: string
      - description: Unique identifier for a unit_id
        in: path
        name: unit_id
        required: true
        schema:
          format: uuid
          type: string
      - description: |-
          The specified string is an expression for the purpose of filtering
          the list of UnitEvents returned. The expression syntax was inspired by SQL.
          It supports conjunctions using `AND` of relational expressions of the form *attribute*
          *operator* *attribute_or_literal*. The attribute names are case-sensitive and PascalCase,
          as in the JSON encoding.
          Strings support the following operators: `<`, `>`, `<=`, `>=`, `=`, `!=`, `LIKE`, `NOT LIKE`, `ILIKE`, `~~`, `!~~`, `~`, `~*`, `!~`, `!~*`, `IN`, `NOT IN`.
          String pattern operators: `LIKE` and `~~` for pattern matching with `%` and `_` wildcards,
          `ILIKE` for case-insensitive pattern matching, `NOT LIKE` and `!~~` for negated pattern matching.
          String regex operators: `~` for regex matching, `~*` for case-insensitive regex,
          `!~` and `!~*` for regex not matching (case-sensitive and insensitive).
          Integers support the following operators: `<`, `>`, `<=`, `>=`, `=`, `!=`, `IN`, `NOT IN`.
          UUIDs and boolean attributes support equality and inequality only.
          UUID and time literals must be quoted as string literals.
          String literals are quoted with single quotes, such as `'string'`.
          Time literals use the same form as when serialized as JSON,
          such as: `CreatedAt > '2025-02-18T23:16:34'`.
          Integer and boolean literals are also supported for attributes of those types.
          Arrays support the `?` operator to to match any element of the array,
          as in `ApprovedBy ? '7c61626f-ddbe-41af-93f6-b69f4ab6d308'`.
          Arrays can perform LEN() to check for length, as in `LEN(ApprovedBy) > 0`.
          Map support the dot notation to specify a particular map key, as in `Labels.tier = 'Backend'`.
          Maps support `IS NULL` and `IS NOT NULL` with dot notation to check for key absence or presence,
          as in `Labels.tier IS NULL` (key doesn't exist) or `Labels.tier IS NOT NULL` (key exists).
          Comparison results can be tested with `IS TRUE`, `IS FALSE`, `IS NOT TRUE`, and `IS NOT FALSE`.
          These are useful for nullable columns: `MergeSourceID = '<uuid>' IS NOT FALSE` matches rows where MergeSourceID equals the value OR is NULL.
          The `IN` and `NOT IN` operators accept a comma-separated list of values in parentheses,
          such as `Slug IN ('slugone', 'slugtwo')` or `Labels.environment IN ('prod', 'staging')`.
          Conjunctions are supported using the `AND` operator.
          An example conjunction is:
          `CreatedAt >= '2025-01-07' AND Slug = 'test' AND Labels.mykey = 'myvalue'`.

          Supported attributes for filtering on UnitEvent: Action, BridgeWorkerID, CreatedAt, OrganizationID, QueuedOperationID, Result, RevisionNum, SpaceID, StartedAt, Status, TerminatedAt, UnitEventID, UnitEventNum, UnitID, UpdatedAt.

          The whole string must be query-encoded.
        in: query
        name: where
        required: false
        schema:
          type: string
      - description: |-
          UUID of a Filter entity to apply to the UnitEvent list.

          The Filter must be in the same Organization as the user credentials.

          The Filter's From field must match the entity type being filtered (UnitEvent).

          For Space-resident entities, if the Filter has a FromSpaceID, it must match the operation's SpaceID.

          The Filter's Where clause will be combined with any explicit 'where' parameter using AND logic.

          If both 'filter' and 'where' parameters are specified, they are combined with AND logic.
        in: query
        name: filter
        required: false
        schema:
          type: string
      - description: |-
          Free text search that approximately matches the specified string against string fields and map keys/values.

          The search is case-insensitive and uses pattern matching to find entities containing the text.

          Searchable string fields include attributes like Slug, DisplayName, and string-typed custom fields.

          For map fields (like Labels and Annotations), the search matches both map keys and values.

          The search uses OR logic across all searchable fields, so matching any field will return the entity.

          If both 'where' and 'contains' parameters are specified, they are combined with AND logic.

          Searchable fields for UnitEvent include string and map-type attributes from the queryable attributes list.

          The whole string must be query-encoded.
        in: query
        name: contains
        required: false
        schema:
          type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                items:
                  $ref: '#/components/schemas/UnitEvent'
                type: array
          description: OK
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: UnitEvent request is invalid (Bad Request).
        "401":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Unauthorized access.
        "403":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Forbidden access.
        "404":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: UnitEvent not found.
        "500":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Something went wrong while processing UnitEvent.
        default:
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Unexpected error.
      summary: List UnitEvents
      tags:
      - UnitEvent
  /space/{space_id}/unit/{unit_id}/unit_event/{unit_event_id}:
    get:
      description: Get UnitEvent
      operationId: GetUnitEvent
      parameters:
      - description: Unique identifier for a space_id
        in: path
        name: space_id
        required: true
        schema:
          format: uuid
          type: string
      - description: Unique identifier for a unit_id
        in: path
        name: unit_id
        required: true
        schema:
          format: uuid
          type: string
      - description: Unique identifier for a unit_event_id
        in: path
        name: unit_event_id
        required: true
        schema:
          format: uuid
          type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/UnitEvent'
          description: |-
            UnitEvent represents an event of action performed on a Unit's configuration. Each action tracks
            the lifecycle of applying, destroying, or refreshing a Unit's configuration in the target
            live system. The event captures the current status of the operation, any configuration
            drift detected, and timing information about when the action started and completed.
            Actions are atomic from ConfigHub's perspective but may involve multiple steps
            in the connected Bridge. The status and drift detection help track the health
            and consistency of the provisioned configuration compared to what is defined in the Unit.
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: UnitEvent request is invalid (Bad Request).
        "401":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Unauthorized access.
        "403":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Forbidden access.
        "404":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: UnitEvent not found.
        "500":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Something went wrong while processing UnitEvent.
        default:
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Unexpected error.
      summary: Get UnitEvent
      tags:
      - UnitEvent
  /space/{space_id}/view:
    get:
      description: List Views
      operationId: ListViews
      parameters:
      - description: Unique identifier for a space_id
        in: path
        name: space_id
        required: true
        schema:
          format: uuid
          type: string
      - description: |-
          The specified string is an expression for the purpose of filtering
          the list of Views returned. The expression syntax was inspired by SQL.
          It supports conjunctions using `AND` of relational expressions of the form *attribute*
          *operator* *attribute_or_literal*. The attribute names are case-sensitive and PascalCase,
          as in the JSON encoding.
          Strings support the following operators: `<`, `>`, `<=`, `>=`, `=`, `!=`, `LIKE`, `NOT LIKE`, `ILIKE`, `~~`, `!~~`, `~`, `~*`, `!~`, `!~*`, `IN`, `NOT IN`.
          String pattern operators: `LIKE` and `~~` for pattern matching with `%` and `_` wildcards,
          `ILIKE` for case-insensitive pattern matching, `NOT LIKE` and `!~~` for negated pattern matching.
          String regex operators: `~` for regex matching, `~*` for case-insensitive regex,
          `!~` and `!~*` for regex not matching (case-sensitive and insensitive).
          Integers support the following operators: `<`, `>`, `<=`, `>=`, `=`, `!=`, `IN`, `NOT IN`.
          UUIDs and boolean attributes support equality and inequality only.
          UUID and time literals must be quoted as string literals.
          String literals are quoted with single quotes, such as `'string'`.
          Time literals use the same form as when serialized as JSON,
          such as: `CreatedAt > '2025-02-18T23:16:34'`.
          Integer and boolean literals are also supported for attributes of those types.
          Arrays support the `?` operator to to match any element of the array,
          as in `ApprovedBy ? '7c61626f-ddbe-41af-93f6-b69f4ab6d308'`.
          Arrays can perform LEN() to check for length, as in `LEN(ApprovedBy) > 0`.
          Map support the dot notation to specify a particular map key, as in `Labels.tier = 'Backend'`.
          Maps support `IS NULL` and `IS NOT NULL` with dot notation to check for key absence or presence,
          as in `Labels.tier IS NULL` (key doesn't exist) or `Labels.tier IS NOT NULL` (key exists).
          Comparison results can be tested with `IS TRUE`, `IS FALSE`, `IS NOT TRUE`, and `IS NOT FALSE`.
          These are useful for nullable columns: `MergeSourceID = '<uuid>' IS NOT FALSE` matches rows where MergeSourceID equals the value OR is NULL.
          The `IN` and `NOT IN` operators accept a comma-separated list of values in parentheses,
          such as `Slug IN ('slugone', 'slugtwo')` or `Labels.environment IN ('prod', 'staging')`.
          Conjunctions are supported using the `AND` operator.
          An example conjunction is:
          `CreatedAt >= '2025-01-07' AND Slug = 'test' AND Labels.mykey = 'myvalue'`.

          Supported attributes for filtering on View: CreatedAt, DisplayName, FilterID, GroupBy, Labels, Of, OrderBy, OrderByDirection, OrganizationID, Slug, SpaceID, UpdatedAt, ViewID.

          The whole string must be query-encoded.
        in: query
        name: where
        required: false
        schema:
          type: string
      - description: |-
          UUID of a Filter entity to apply to the View list.

          The Filter must be in the same Organization as the user credentials.

          The Filter's From field must match the entity type being filtered (View).

          For Space-resident entities, if the Filter has a FromSpaceID, it must match the operation's SpaceID.

          The Filter's Where clause will be combined with any explicit 'where' parameter using AND logic.

          If both 'filter' and 'where' parameters are specified, they are combined with AND logic.
        in: query
        name: filter
        required: false
        schema:
          type: string
      - description: |-
          Free text search that approximately matches the specified string against string fields and map keys/values.

          The search is case-insensitive and uses pattern matching to find entities containing the text.

          Searchable string fields include attributes like Slug, DisplayName, and string-typed custom fields.

          For map fields (like Labels and Annotations), the search matches both map keys and values.

          The search uses OR logic across all searchable fields, so matching any field will return the entity.

          If both 'where' and 'contains' parameters are specified, they are combined with AND logic.

          Searchable fields for View include string and map-type attributes from the queryable attributes list.

          The whole string must be query-encoded.
        in: query
        name: contains
        required: false
        schema:
          type: string
      - description: |-
          Include clause for expanding related entities in the response for View.
          The attribute names are case-sensitive, PascalCase, and
          expected in a comma-separated list format as in the JSON encoding.

          Supported attributes for View are FilterID, OrganizationID, SpaceID.

          The whole string must be query-encoded.
        in: query
        name: include
        required: false
        schema:
          type: string
      - description: |-
          Select clause for specifying which fields to include in the response for View.
          The attribute names are case-sensitive, PascalCase, and
          expected in a comma-separated list format as in the JSON encoding.
          If not specified, all fields are returned.
          Entity and parent IDs (like OrganizationID, SpaceID, ViewID) and Slug are always returned regardless of the select parameter.
          Fields used in where and contains filters are also automatically included.
          Example: 'DisplayName,CreatedAt,Labels' will return only those fields plus the required ID and Slug fields.
          The whole string must be query-encoded.
        in: query
        name: select
        required: false
        schema:
          type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                items:
                  $ref: '#/components/schemas/ExtendedView'
                type: array
          description: OK
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: View request is invalid (Bad Request).
        "401":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Unauthorized access.
        "403":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Forbidden access.
        "404":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: View not found.
        "500":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Something went wrong while processing View.
        default:
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Unexpected error.
      summary: List Views
      tags:
      - View
    post:
      description: Create View
      operationId: CreateView
      parameters:
      - description: Unique identifier for a space_id
        in: path
        name: space_id
        required: true
        schema:
          format: uuid
          type: string
      - description: Allowed values are true and false. Default is false. When true,
          reports success when an entity already exists and returns the existing entity
        in: query
        name: allow_exists
        required: false
        schema:
          type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/View'
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/View'
          description: Defines an entity view.
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: View request is invalid (Bad Request).
        "401":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Unauthorized access.
        "403":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Forbidden access.
        "404":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: View not found.
        "409":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: View data conflict. Data has changed since last read.
        "500":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Something went wrong while processing View.
        default:
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Unexpected error.
      summary: Create View
      tags:
      - View
  /space/{space_id}/view/{view_id}:
    delete:
      description: Delete View
      operationId: DeleteView
      parameters:
      - description: Unique identifier for a space_id
        in: path
        name: space_id
        required: true
        schema:
          format: uuid
          type: string
      - description: Unique identifier for a view_id
        in: path
        name: view_id
        required: true
        schema:
          format: uuid
          type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/DeleteResponse'
          description: Response for successful delete operation
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: View request is invalid (Bad Request).
        "401":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Unauthorized access.
        "403":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Forbidden access.
        "404":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: View not found.
        "409":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: View data conflict. Data has changed since last read.
        "422":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: View could not be deleted.
        "500":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Something went wrong while processing View.
        default:
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Unexpected error.
      summary: Delete View
      tags:
      - View
    get:
      description: Get View
      operationId: GetView
      parameters:
      - description: Unique identifier for a space_id
        in: path
        name: space_id
        required: true
        schema:
          format: uuid
          type: string
      - description: |-
          Include clause for expanding related entities in the response for View.
          The attribute names are case-sensitive, PascalCase, and
          expected in a comma-separated list format as in the JSON encoding.

          Supported attributes for View are FilterID, OrganizationID, SpaceID.

          The whole string must be query-encoded.
        in: query
        name: include
        required: false
        schema:
          type: string
      - description: |-
          Select clause for specifying which fields to include in the response for View.
          The attribute names are case-sensitive, PascalCase, and
          expected in a comma-separated list format as in the JSON encoding.
          If not specified, all fields are returned.
          Entity and parent IDs (like OrganizationID, SpaceID, ViewID) and Slug are always returned regardless of the select parameter.
          Fields used in where and contains filters are also automatically included.
          Example: 'DisplayName,CreatedAt,Labels' will return only those fields plus the required ID and Slug fields.
          The whole string must be query-encoded.
        in: query
        name: select
        required: false
        schema:
          type: string
      - description: Unique identifier for a view_id
        in: path
        name: view_id
        required: true
        schema:
          format: uuid
          type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/ExtendedView'
          description: OK
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: View request is invalid (Bad Request).
        "401":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Unauthorized access.
        "403":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Forbidden access.
        "404":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: View not found.
        "500":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Something went wrong while processing View.
        default:
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Unexpected error.
      summary: Get View
      tags:
      - View
    patch:
      description: Patch View
      operationId: PatchView
      parameters:
      - description: Unique identifier for a space_id
        in: path
        name: space_id
        required: true
        schema:
          format: uuid
          type: string
      - description: Unique identifier for a view_id
        in: path
        name: view_id
        required: true
        schema:
          format: uuid
          type: string
      requestBody:
        content:
          application/merge-patch+json:
            schema:
              description: JSON Merge Patch schema for View. All fields are optional
                and nullable. Readonly fields are ignored.
              properties:
                Annotations:
                  additionalProperties:
                    nullable: true
                    type: string
                  description: An optional map of Annotation key/value pairs for tools
                    to attach information to entities.
                  nullable: true
                  type: object
                Columns:
                  description: ""
                  items:
                    nullable: true
                    type: object
                  nullable: true
                  type: array
                DeleteGates:
                  additionalProperties:
                    nullable: true
                    type: boolean
                  description: An optional set of gates that, if any is present, will
                    block deletion
                  nullable: true
                  type: object
                DisplayName:
                  description: Friendly name for the entity.
                  nullable: true
                  type: string
                FilterID:
                  description: ""
                  format: uuid
                  nullable: true
                  type: string
                GroupBy:
                  description: ""
                  nullable: true
                  type: string
                Labels:
                  additionalProperties:
                    nullable: true
                    type: string
                  description: An optional map of Label key/value pairs to specify
                    identifying attributes of entities for the purpose of grouping
                    and filtering them.
                  nullable: true
                  type: object
                Of:
                  description: ""
                  nullable: true
                  type: string
                OrderBy:
                  description: ""
                  nullable: true
                  type: string
                OrderByDirection:
                  description: ""
                  nullable: true
                  type: string
                Slug:
                  description: Unique URL-safe identifier for the entity.
                  nullable: true
                  type: string
                Version:
                  description: An entity-specific sequence number used for optimistic
                    concurrency control. The value read must be sent in calls to Update.
                  nullable: true
                  type: integer
              type: object
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/View'
          description: Defines an entity view.
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: View request is invalid (Bad Request).
        "401":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Unauthorized access.
        "403":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Forbidden access.
        "404":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: View not found.
        "409":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: View data conflict. Data has changed since last read.
        "500":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Something went wrong while processing View.
        default:
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Unexpected error.
      summary: Patch View
      tags:
      - View
    put:
      description: Update View
      operationId: UpdateView
      parameters:
      - description: Unique identifier for a space_id
        in: path
        name: space_id
        required: true
        schema:
          format: uuid
          type: string
      - description: Unique identifier for a view_id
        in: path
        name: view_id
        required: true
        schema:
          format: uuid
          type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/View'
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/View'
          description: Defines an entity view.
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: View request is invalid (Bad Request).
        "401":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Unauthorized access.
        "403":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Forbidden access.
        "404":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: View not found.
        "409":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: View data conflict. Data has changed since last read.
        "500":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Something went wrong while processing View.
        default:
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Unexpected error.
      summary: Update View
      tags:
      - View
  /tag:
    delete:
      description: Delete multiple tags selected by query parameters
      operationId: BulkDeleteTags
      parameters:
      - description: |-
          The specified string is an expression for the purpose of filtering
          the list of Tags returned. The expression syntax was inspired by SQL.
          It supports conjunctions using `AND` of relational expressions of the form *attribute*
          *operator* *attribute_or_literal*. The attribute names are case-sensitive and PascalCase,
          as in the JSON encoding.
          Strings support the following operators: `<`, `>`, `<=`, `>=`, `=`, `!=`, `LIKE`, `NOT LIKE`, `ILIKE`, `~~`, `!~~`, `~`, `~*`, `!~`, `!~*`, `IN`, `NOT IN`.
          String pattern operators: `LIKE` and `~~` for pattern matching with `%` and `_` wildcards,
          `ILIKE` for case-insensitive pattern matching, `NOT LIKE` and `!~~` for negated pattern matching.
          String regex operators: `~` for regex matching, `~*` for case-insensitive regex,
          `!~` and `!~*` for regex not matching (case-sensitive and insensitive).
          Integers support the following operators: `<`, `>`, `<=`, `>=`, `=`, `!=`, `IN`, `NOT IN`.
          UUIDs and boolean attributes support equality and inequality only.
          UUID and time literals must be quoted as string literals.
          String literals are quoted with single quotes, such as `'string'`.
          Time literals use the same form as when serialized as JSON,
          such as: `CreatedAt > '2025-02-18T23:16:34'`.
          Integer and boolean literals are also supported for attributes of those types.
          Arrays support the `?` operator to to match any element of the array,
          as in `ApprovedBy ? '7c61626f-ddbe-41af-93f6-b69f4ab6d308'`.
          Arrays can perform LEN() to check for length, as in `LEN(ApprovedBy) > 0`.
          Map support the dot notation to specify a particular map key, as in `Labels.tier = 'Backend'`.
          Maps support `IS NULL` and `IS NOT NULL` with dot notation to check for key absence or presence,
          as in `Labels.tier IS NULL` (key doesn't exist) or `Labels.tier IS NOT NULL` (key exists).
          Comparison results can be tested with `IS TRUE`, `IS FALSE`, `IS NOT TRUE`, and `IS NOT FALSE`.
          These are useful for nullable columns: `MergeSourceID = '<uuid>' IS NOT FALSE` matches rows where MergeSourceID equals the value OR is NULL.
          The `IN` and `NOT IN` operators accept a comma-separated list of values in parentheses,
          such as `Slug IN ('slugone', 'slugtwo')` or `Labels.environment IN ('prod', 'staging')`.
          Conjunctions are supported using the `AND` operator.
          An example conjunction is:
          `CreatedAt >= '2025-01-07' AND Slug = 'test' AND Labels.mykey = 'myvalue'`.

          Supported attributes for filtering on Tag: ChangeSetID, CreatedAt, DeleteGates, DisplayName, Labels, OrganizationID, Slug, SpaceID, TagID, UpdatedAt.

          The whole string must be query-encoded.
        in: query
        name: where
        required: false
        schema:
          type: string
      - description: |-
          UUID of a Filter entity to apply to the Tag list.

          The Filter must be in the same Organization as the user credentials.

          The Filter's From field must match the entity type being filtered (Tag).

          For Space-resident entities, if the Filter has a FromSpaceID, it must match the operation's SpaceID.

          The Filter's Where clause will be combined with any explicit 'where' parameter using AND logic.

          If both 'filter' and 'where' parameters are specified, they are combined with AND logic.
        in: query
        name: filter
        required: false
        schema:
          type: string
      - description: |-
          Free text search that approximately matches the specified string against string fields and map keys/values.

          The search is case-insensitive and uses pattern matching to find entities containing the text.

          Searchable string fields include attributes like Slug, DisplayName, and string-typed custom fields.

          For map fields (like Labels and Annotations), the search matches both map keys and values.

          The search uses OR logic across all searchable fields, so matching any field will return the entity.

          If both 'where' and 'contains' parameters are specified, they are combined with AND logic.

          Searchable fields for Tag include string and map-type attributes from the queryable attributes list.

          The whole string must be query-encoded.
        in: query
        name: contains
        required: false
        schema:
          type: string
      - description: |-
          Include clause for expanding related entities in the response for Tag.
          The attribute names are case-sensitive, PascalCase, and
          expected in a comma-separated list format as in the JSON encoding.

          Supported attributes for Tag are ChangeSetID, OrganizationID, SpaceID.

          The whole string must be query-encoded.
        in: query
        name: include
        required: false
        schema:
          type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                items:
                  $ref: '#/components/schemas/DeleteResponse'
                type: array
          description: OK
        "207":
          content:
            application/json:
              schema:
                items:
                  $ref: '#/components/schemas/DeleteResponse'
                type: array
          description: 'Multi-Status: Mixed success and failure results'
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Tag request is invalid (Bad Request).
        "401":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Unauthorized access.
        "403":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Forbidden access.
        "404":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Tag not found.
        "409":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Tag data conflict. Data has changed since last read.
        "422":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Unable to delete entity
        "500":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Something went wrong while processing Tag.
        default:
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Unexpected error.
      summary: Bulk delete multiple tags
      tags:
      - Tag
    get:
      description: Retrieves a list of Tags across spaces in the Organization
      operationId: ListAllTags
      parameters:
      - description: |-
          The specified string is an expression for the purpose of filtering
          the list of Tags returned. The expression syntax was inspired by SQL.
          It supports conjunctions using `AND` of relational expressions of the form *attribute*
          *operator* *attribute_or_literal*. The attribute names are case-sensitive and PascalCase,
          as in the JSON encoding.
          Strings support the following operators: `<`, `>`, `<=`, `>=`, `=`, `!=`, `LIKE`, `NOT LIKE`, `ILIKE`, `~~`, `!~~`, `~`, `~*`, `!~`, `!~*`, `IN`, `NOT IN`.
          String pattern operators: `LIKE` and `~~` for pattern matching with `%` and `_` wildcards,
          `ILIKE` for case-insensitive pattern matching, `NOT LIKE` and `!~~` for negated pattern matching.
          String regex operators: `~` for regex matching, `~*` for case-insensitive regex,
          `!~` and `!~*` for regex not matching (case-sensitive and insensitive).
          Integers support the following operators: `<`, `>`, `<=`, `>=`, `=`, `!=`, `IN`, `NOT IN`.
          UUIDs and boolean attributes support equality and inequality only.
          UUID and time literals must be quoted as string literals.
          String literals are quoted with single quotes, such as `'string'`.
          Time literals use the same form as when serialized as JSON,
          such as: `CreatedAt > '2025-02-18T23:16:34'`.
          Integer and boolean literals are also supported for attributes of those types.
          Arrays support the `?` operator to to match any element of the array,
          as in `ApprovedBy ? '7c61626f-ddbe-41af-93f6-b69f4ab6d308'`.
          Arrays can perform LEN() to check for length, as in `LEN(ApprovedBy) > 0`.
          Map support the dot notation to specify a particular map key, as in `Labels.tier = 'Backend'`.
          Maps support `IS NULL` and `IS NOT NULL` with dot notation to check for key absence or presence,
          as in `Labels.tier IS NULL` (key doesn't exist) or `Labels.tier IS NOT NULL` (key exists).
          Comparison results can be tested with `IS TRUE`, `IS FALSE`, `IS NOT TRUE`, and `IS NOT FALSE`.
          These are useful for nullable columns: `MergeSourceID = '<uuid>' IS NOT FALSE` matches rows where MergeSourceID equals the value OR is NULL.
          The `IN` and `NOT IN` operators accept a comma-separated list of values in parentheses,
          such as `Slug IN ('slugone', 'slugtwo')` or `Labels.environment IN ('prod', 'staging')`.
          Conjunctions are supported using the `AND` operator.
          An example conjunction is:
          `CreatedAt >= '2025-01-07' AND Slug = 'test' AND Labels.mykey = 'myvalue'`.

          Supported attributes for filtering on Tag: ChangeSetID, CreatedAt, DeleteGates, DisplayName, Labels, OrganizationID, Slug, SpaceID, TagID, UpdatedAt.

          The whole string must be query-encoded.
        in: query
        name: where
        required: false
        schema:
          type: string
      - description: |-
          UUID of a Filter entity to apply to the Tag list.

          The Filter must be in the same Organization as the user credentials.

          The Filter's From field must match the entity type being filtered (Tag).

          For Space-resident entities, if the Filter has a FromSpaceID, it must match the operation's SpaceID.

          The Filter's Where clause will be combined with any explicit 'where' parameter using AND logic.

          If both 'filter' and 'where' parameters are specified, they are combined with AND logic.
        in: query
        name: filter
        required: false
        schema:
          type: string
      - description: |-
          Free text search that approximately matches the specified string against string fields and map keys/values.

          The search is case-insensitive and uses pattern matching to find entities containing the text.

          Searchable string fields include attributes like Slug, DisplayName, and string-typed custom fields.

          For map fields (like Labels and Annotations), the search matches both map keys and values.

          The search uses OR logic across all searchable fields, so matching any field will return the entity.

          If both 'where' and 'contains' parameters are specified, they are combined with AND logic.

          Searchable fields for Tag include string and map-type attributes from the queryable attributes list.

          The whole string must be query-encoded.
        in: query
        name: contains
        required: false
        schema:
          type: string
      - description: |-
          Include clause for expanding related entities in the response for Tag.
          The attribute names are case-sensitive, PascalCase, and
          expected in a comma-separated list format as in the JSON encoding.

          Supported attributes for Tag are ChangeSetID, OrganizationID, SpaceID.

          The whole string must be query-encoded.
        in: query
        name: include
        required: false
        schema:
          type: string
      - description: |-
          Select clause for specifying which fields to include in the response for Tag.
          The attribute names are case-sensitive, PascalCase, and
          expected in a comma-separated list format as in the JSON encoding.
          If not specified, all fields are returned.
          Entity and parent IDs (like OrganizationID, SpaceID, TagID) and Slug are always returned regardless of the select parameter.
          Fields used in where and contains filters are also automatically included.
          Example: 'DisplayName,CreatedAt,Labels' will return only those fields plus the required ID and Slug fields.
          The whole string must be query-encoded.
        in: query
        name: select
        required: false
        schema:
          type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                items:
                  $ref: '#/components/schemas/ExtendedTag'
                type: array
          description: OK
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Tag request is invalid (Bad Request).
        "401":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Unauthorized access.
        "403":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Forbidden access.
        "404":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Tag not found.
        "500":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Something went wrong while processing Tag.
        default:
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Unexpected error.
      summary: List of Tags across spaces
      tags:
      - Tag
    patch:
      description: Apply JSON merge patch to multiple tags selected by query parameters
      operationId: BulkPatchTags
      parameters:
      - description: |-
          The specified string is an expression for the purpose of filtering
          the list of Tags returned. The expression syntax was inspired by SQL.
          It supports conjunctions using `AND` of relational expressions of the form *attribute*
          *operator* *attribute_or_literal*. The attribute names are case-sensitive and PascalCase,
          as in the JSON encoding.
          Strings support the following operators: `<`, `>`, `<=`, `>=`, `=`, `!=`, `LIKE`, `NOT LIKE`, `ILIKE`, `~~`, `!~~`, `~`, `~*`, `!~`, `!~*`, `IN`, `NOT IN`.
          String pattern operators: `LIKE` and `~~` for pattern matching with `%` and `_` wildcards,
          `ILIKE` for case-insensitive pattern matching, `NOT LIKE` and `!~~` for negated pattern matching.
          String regex operators: `~` for regex matching, `~*` for case-insensitive regex,
          `!~` and `!~*` for regex not matching (case-sensitive and insensitive).
          Integers support the following operators: `<`, `>`, `<=`, `>=`, `=`, `!=`, `IN`, `NOT IN`.
          UUIDs and boolean attributes support equality and inequality only.
          UUID and time literals must be quoted as string literals.
          String literals are quoted with single quotes, such as `'string'`.
          Time literals use the same form as when serialized as JSON,
          such as: `CreatedAt > '2025-02-18T23:16:34'`.
          Integer and boolean literals are also supported for attributes of those types.
          Arrays support the `?` operator to to match any element of the array,
          as in `ApprovedBy ? '7c61626f-ddbe-41af-93f6-b69f4ab6d308'`.
          Arrays can perform LEN() to check for length, as in `LEN(ApprovedBy) > 0`.
          Map support the dot notation to specify a particular map key, as in `Labels.tier = 'Backend'`.
          Maps support `IS NULL` and `IS NOT NULL` with dot notation to check for key absence or presence,
          as in `Labels.tier IS NULL` (key doesn't exist) or `Labels.tier IS NOT NULL` (key exists).
          Comparison results can be tested with `IS TRUE`, `IS FALSE`, `IS NOT TRUE`, and `IS NOT FALSE`.
          These are useful for nullable columns: `MergeSourceID = '<uuid>' IS NOT FALSE` matches rows where MergeSourceID equals the value OR is NULL.
          The `IN` and `NOT IN` operators accept a comma-separated list of values in parentheses,
          such as `Slug IN ('slugone', 'slugtwo')` or `Labels.environment IN ('prod', 'staging')`.
          Conjunctions are supported using the `AND` operator.
          An example conjunction is:
          `CreatedAt >= '2025-01-07' AND Slug = 'test' AND Labels.mykey = 'myvalue'`.

          Supported attributes for filtering on Tag: ChangeSetID, CreatedAt, DeleteGates, DisplayName, Labels, OrganizationID, Slug, SpaceID, TagID, UpdatedAt.

          The whole string must be query-encoded.
        in: query
        name: where
        required: false
        schema:
          type: string
      - description: |-
          UUID of a Filter entity to apply to the Tag list.

          The Filter must be in the same Organization as the user credentials.

          The Filter's From field must match the entity type being filtered (Tag).

          For Space-resident entities, if the Filter has a FromSpaceID, it must match the operation's SpaceID.

          The Filter's Where clause will be combined with any explicit 'where' parameter using AND logic.

          If both 'filter' and 'where' parameters are specified, they are combined with AND logic.
        in: query
        name: filter
        required: false
        schema:
          type: string
      - description: |-
          Free text search that approximately matches the specified string against string fields and map keys/values.

          The search is case-insensitive and uses pattern matching to find entities containing the text.

          Searchable string fields include attributes like Slug, DisplayName, and string-typed custom fields.

          For map fields (like Labels and Annotations), the search matches both map keys and values.

          The search uses OR logic across all searchable fields, so matching any field will return the entity.

          If both 'where' and 'contains' parameters are specified, they are combined with AND logic.

          Searchable fields for Tag include string and map-type attributes from the queryable attributes list.

          The whole string must be query-encoded.
        in: query
        name: contains
        required: false
        schema:
          type: string
      - description: |-
          Include clause for expanding related entities in the response for Tag.
          The attribute names are case-sensitive, PascalCase, and
          expected in a comma-separated list format as in the JSON encoding.

          Supported attributes for Tag are ChangeSetID, OrganizationID, SpaceID.

          The whole string must be query-encoded.
        in: query
        name: include
        required: false
        schema:
          type: string
      requestBody:
        content:
          application/merge-patch+json:
            schema:
              description: JSON Merge Patch schema for Tag. All fields are optional
                and nullable. Readonly fields are ignored.
              properties:
                Annotations:
                  additionalProperties:
                    nullable: true
                    type: string
                  description: An optional map of Annotation key/value pairs for tools
                    to attach information to entities.
                  nullable: true
                  type: object
                DeleteGates:
                  additionalProperties:
                    nullable: true
                    type: boolean
                  description: An optional set of gates that, if any is present, will
                    block deletion
                  nullable: true
                  type: object
                DisplayName:
                  description: Friendly name for the entity.
                  nullable: true
                  type: string
                Labels:
                  additionalProperties:
                    nullable: true
                    type: string
                  description: An optional map of Label key/value pairs to specify
                    identifying attributes of entities for the purpose of grouping
                    and filtering them.
                  nullable: true
                  type: object
                Slug:
                  description: Unique URL-safe identifier for the entity.
                  nullable: true
                  type: string
                Version:
                  description: An entity-specific sequence number used for optimistic
                    concurrency control. The value read must be sent in calls to Update.
                  nullable: true
                  type: integer
              type: object
      responses:
        "200":
          content:
            application/json:
              schema:
                items:
                  $ref: '#/components/schemas/TagCreateOrUpdateResponse'
                type: array
          description: OK
        "207":
          content:
            application/json:
              schema:
                items:
                  $ref: '#/components/schemas/TagCreateOrUpdateResponse'
                type: array
          description: 'Multi-Status: Mixed success and failure results'
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Tag request is invalid (Bad Request).
        "401":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Unauthorized access.
        "403":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Forbidden access.
        "404":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Tag not found.
        "409":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Tag data conflict. Data has changed since last read.
        "500":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Something went wrong while processing Tag.
        default:
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Unexpected error.
      summary: Bulk patch multiple tags
      tags:
      - Tag
    post:
      description: Clone multiple tags selected by query parameters with optional
        name prefixes and destination spaces
      operationId: BulkCreateTags
      parameters:
      - description: |-
          The specified string is an expression for the purpose of filtering
          the list of Tags returned. The expression syntax was inspired by SQL.
          It supports conjunctions using `AND` of relational expressions of the form *attribute*
          *operator* *attribute_or_literal*. The attribute names are case-sensitive and PascalCase,
          as in the JSON encoding.
          Strings support the following operators: `<`, `>`, `<=`, `>=`, `=`, `!=`, `LIKE`, `NOT LIKE`, `ILIKE`, `~~`, `!~~`, `~`, `~*`, `!~`, `!~*`, `IN`, `NOT IN`.
          String pattern operators: `LIKE` and `~~` for pattern matching with `%` and `_` wildcards,
          `ILIKE` for case-insensitive pattern matching, `NOT LIKE` and `!~~` for negated pattern matching.
          String regex operators: `~` for regex matching, `~*` for case-insensitive regex,
          `!~` and `!~*` for regex not matching (case-sensitive and insensitive).
          Integers support the following operators: `<`, `>`, `<=`, `>=`, `=`, `!=`, `IN`, `NOT IN`.
          UUIDs and boolean attributes support equality and inequality only.
          UUID and time literals must be quoted as string literals.
          String literals are quoted with single quotes, such as `'string'`.
          Time literals use the same form as when serialized as JSON,
          such as: `CreatedAt > '2025-02-18T23:16:34'`.
          Integer and boolean literals are also supported for attributes of those types.
          Arrays support the `?` operator to to match any element of the array,
          as in `ApprovedBy ? '7c61626f-ddbe-41af-93f6-b69f4ab6d308'`.
          Arrays can perform LEN() to check for length, as in `LEN(ApprovedBy) > 0`.
          Map support the dot notation to specify a particular map key, as in `Labels.tier = 'Backend'`.
          Maps support `IS NULL` and `IS NOT NULL` with dot notation to check for key absence or presence,
          as in `Labels.tier IS NULL` (key doesn't exist) or `Labels.tier IS NOT NULL` (key exists).
          Comparison results can be tested with `IS TRUE`, `IS FALSE`, `IS NOT TRUE`, and `IS NOT FALSE`.
          These are useful for nullable columns: `MergeSourceID = '<uuid>' IS NOT FALSE` matches rows where MergeSourceID equals the value OR is NULL.
          The `IN` and `NOT IN` operators accept a comma-separated list of values in parentheses,
          such as `Slug IN ('slugone', 'slugtwo')` or `Labels.environment IN ('prod', 'staging')`.
          Conjunctions are supported using the `AND` operator.
          An example conjunction is:
          `CreatedAt >= '2025-01-07' AND Slug = 'test' AND Labels.mykey = 'myvalue'`.

          Supported attributes for filtering on Tag: ChangeSetID, CreatedAt, DeleteGates, DisplayName, Labels, OrganizationID, Slug, SpaceID, TagID, UpdatedAt.

          The whole string must be query-encoded.
        in: query
        name: where
        required: false
        schema:
          type: string
      - description: |-
          UUID of a Filter entity to apply to the Tag list.

          The Filter must be in the same Organization as the user credentials.

          The Filter's From field must match the entity type being filtered (Tag).

          For Space-resident entities, if the Filter has a FromSpaceID, it must match the operation's SpaceID.

          The Filter's Where clause will be combined with any explicit 'where' parameter using AND logic.

          If both 'filter' and 'where' parameters are specified, they are combined with AND logic.
        in: query
        name: filter
        required: false
        schema:
          type: string
      - description: |-
          Free text search that approximately matches the specified string against string fields and map keys/values.

          The search is case-insensitive and uses pattern matching to find entities containing the text.

          Searchable string fields include attributes like Slug, DisplayName, and string-typed custom fields.

          For map fields (like Labels and Annotations), the search matches both map keys and values.

          The search uses OR logic across all searchable fields, so matching any field will return the entity.

          If both 'where' and 'contains' parameters are specified, they are combined with AND logic.

          Searchable fields for Tag include string and map-type attributes from the queryable attributes list.

          The whole string must be query-encoded.
        in: query
        name: contains
        required: false
        schema:
          type: string
      - description: |-
          Include clause for expanding related entities in the response for Tag.
          The attribute names are case-sensitive, PascalCase, and
          expected in a comma-separated list format as in the JSON encoding.

          Supported attributes for Tag are ChangeSetID, OrganizationID, SpaceID.

          The whole string must be query-encoded.
        in: query
        name: include
        required: false
        schema:
          type: string
      - description: Comma-separated list of prefixes to apply to cloned Tag names
        in: query
        name: name_prefixes
        required: false
        schema:
          type: string
      - description: Comma-separated list of labels with multiple values for cloned
          Tag labels, in the format of key1=value1|value2,key2=value1|value2|value3
        in: query
        name: variant_labels
        required: false
        schema:
          type: string
      - description: 'A string for clone names, use the prefix ''template:'' for a
          Go-template with .SourceEntitySlug to access the original entity''s slug
          and .Labels to access variant labels, example: ''template:{{.SourceEntitySlug}}-{{.Labels.env}}'''
        in: query
        name: name_pattern
        required: false
        schema:
          type: string
      - description: |-
          The specified string is an expression for the purpose of filtering
          the list of Spaces returned. The expression syntax was inspired by SQL.
          It supports conjunctions using `AND` of relational expressions of the form *attribute*
          *operator* *attribute_or_literal*. The attribute names are case-sensitive and PascalCase,
          as in the JSON encoding.
          Strings support the following operators: `<`, `>`, `<=`, `>=`, `=`, `!=`, `LIKE`, `NOT LIKE`, `ILIKE`, `~~`, `!~~`, `~`, `~*`, `!~`, `!~*`, `IN`, `NOT IN`.
          String pattern operators: `LIKE` and `~~` for pattern matching with `%` and `_` wildcards,
          `ILIKE` for case-insensitive pattern matching, `NOT LIKE` and `!~~` for negated pattern matching.
          String regex operators: `~` for regex matching, `~*` for case-insensitive regex,
          `!~` and `!~*` for regex not matching (case-sensitive and insensitive).
          Integers support the following operators: `<`, `>`, `<=`, `>=`, `=`, `!=`, `IN`, `NOT IN`.
          UUIDs and boolean attributes support equality and inequality only.
          UUID and time literals must be quoted as string literals.
          String literals are quoted with single quotes, such as `'string'`.
          Time literals use the same form as when serialized as JSON,
          such as: `CreatedAt > '2025-02-18T23:16:34'`.
          Integer and boolean literals are also supported for attributes of those types.
          Arrays support the `?` operator to to match any element of the array,
          as in `ApprovedBy ? '7c61626f-ddbe-41af-93f6-b69f4ab6d308'`.
          Arrays can perform LEN() to check for length, as in `LEN(ApprovedBy) > 0`.
          Map support the dot notation to specify a particular map key, as in `Labels.tier = 'Backend'`.
          Maps support `IS NULL` and `IS NOT NULL` with dot notation to check for key absence or presence,
          as in `Labels.tier IS NULL` (key doesn't exist) or `Labels.tier IS NOT NULL` (key exists).
          Comparison results can be tested with `IS TRUE`, `IS FALSE`, `IS NOT TRUE`, and `IS NOT FALSE`.
          These are useful for nullable columns: `MergeSourceID = '<uuid>' IS NOT FALSE` matches rows where MergeSourceID equals the value OR is NULL.
          The `IN` and `NOT IN` operators accept a comma-separated list of values in parentheses,
          such as `Slug IN ('slugone', 'slugtwo')` or `Labels.environment IN ('prod', 'staging')`.
          Conjunctions are supported using the `AND` operator.
          An example conjunction is:
          `CreatedAt >= '2025-01-07' AND Slug = 'test' AND Labels.mykey = 'myvalue'`.

          Supported attributes for filtering on Space: AttributeFilterID, AttributeHash, AttributeIDs, CreatedAt, DeleteGates, DisplayName, Labels, OrganizationID, Permissions, Slug, SpaceID, TriggerFilterID, TriggerHash, TriggerIDs, UpdatedAt.

          Where expression to select destination spaces for cloning tags

          The whole string must be query-encoded.
        in: query
        name: where_space
        required: false
        schema:
          type: string
      - description: |-
          UUID of a Filter entity to apply to the Space list.

          The Filter must be in the same Organization as the user credentials.

          The Filter's From field must match the entity type being filtered (Space).

          For Space-resident entities, if the Filter has a FromSpaceID, it must match the operation's SpaceID.

          The Filter's Where clause will be combined with any explicit 'where' parameter using AND logic.

          If both 'filter' and 'where' parameters are specified, they are combined with AND logic.
        in: query
        name: filter_space
        required: false
        schema:
          type: string
      - description: Allowed values are true and false. Default is false. When true,
          reports success when an entity already exists and returns the existing entity
        in: query
        name: allow_exists
        required: false
        schema:
          type: string
      requestBody:
        content:
          application/merge-patch+json:
            schema:
              description: JSON Merge Patch schema for Tag. All fields are optional
                and nullable. Readonly fields are ignored.
              properties:
                Annotations:
                  additionalProperties:
                    nullable: true
                    type: string
                  description: An optional map of Annotation key/value pairs for tools
                    to attach information to entities.
                  nullable: true
                  type: object
                DeleteGates:
                  additionalProperties:
                    nullable: true
                    type: boolean
                  description: An optional set of gates that, if any is present, will
                    block deletion
                  nullable: true
                  type: object
                DisplayName:
                  description: Friendly name for the entity.
                  nullable: true
                  type: string
                Labels:
                  additionalProperties:
                    nullable: true
                    type: string
                  description: An optional map of Label key/value pairs to specify
                    identifying attributes of entities for the purpose of grouping
                    and filtering them.
                  nullable: true
                  type: object
                Slug:
                  description: Unique URL-safe identifier for the entity.
                  nullable: true
                  type: string
                Version:
                  description: An entity-specific sequence number used for optimistic
                    concurrency control. The value read must be sent in calls to Update.
                  nullable: true
                  type: integer
              type: object
      responses:
        "200":
          content:
            application/json:
              schema:
                items:
                  $ref: '#/components/schemas/TagCreateOrUpdateResponse'
                type: array
          description: OK
        "207":
          content:
            application/json:
              schema:
                items:
                  $ref: '#/components/schemas/TagCreateOrUpdateResponse'
                type: array
          description: Multi-Status (partial success)
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Tag request is invalid (Bad Request).
        "401":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Unauthorized access.
        "403":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Forbidden access.
        "404":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Tag not found.
        "409":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Tag data conflict. Data has changed since last read.
        "500":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Something went wrong while processing Tag.
        default:
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Unexpected error.
      summary: Bulk create (clone) multiple tags
      tags:
      - Tag
  /target:
    delete:
      description: Delete multiple targets selected by query parameters
      operationId: BulkDeleteTargets
      parameters:
      - description: |-
          The specified string is an expression for the purpose of filtering
          the list of Targets returned. The expression syntax was inspired by SQL.
          It supports conjunctions using `AND` of relational expressions of the form *attribute*
          *operator* *attribute_or_literal*. The attribute names are case-sensitive and PascalCase,
          as in the JSON encoding.
          Strings support the following operators: `<`, `>`, `<=`, `>=`, `=`, `!=`, `LIKE`, `NOT LIKE`, `ILIKE`, `~~`, `!~~`, `~`, `~*`, `!~`, `!~*`, `IN`, `NOT IN`.
          String pattern operators: `LIKE` and `~~` for pattern matching with `%` and `_` wildcards,
          `ILIKE` for case-insensitive pattern matching, `NOT LIKE` and `!~~` for negated pattern matching.
          String regex operators: `~` for regex matching, `~*` for case-insensitive regex,
          `!~` and `!~*` for regex not matching (case-sensitive and insensitive).
          Integers support the following operators: `<`, `>`, `<=`, `>=`, `=`, `!=`, `IN`, `NOT IN`.
          UUIDs and boolean attributes support equality and inequality only.
          UUID and time literals must be quoted as string literals.
          String literals are quoted with single quotes, such as `'string'`.
          Time literals use the same form as when serialized as JSON,
          such as: `CreatedAt > '2025-02-18T23:16:34'`.
          Integer and boolean literals are also supported for attributes of those types.
          Arrays support the `?` operator to to match any element of the array,
          as in `ApprovedBy ? '7c61626f-ddbe-41af-93f6-b69f4ab6d308'`.
          Arrays can perform LEN() to check for length, as in `LEN(ApprovedBy) > 0`.
          Map support the dot notation to specify a particular map key, as in `Labels.tier = 'Backend'`.
          Maps support `IS NULL` and `IS NOT NULL` with dot notation to check for key absence or presence,
          as in `Labels.tier IS NULL` (key doesn't exist) or `Labels.tier IS NOT NULL` (key exists).
          Comparison results can be tested with `IS TRUE`, `IS FALSE`, `IS NOT TRUE`, and `IS NOT FALSE`.
          These are useful for nullable columns: `MergeSourceID = '<uuid>' IS NOT FALSE` matches rows where MergeSourceID equals the value OR is NULL.
          The `IN` and `NOT IN` operators accept a comma-separated list of values in parentheses,
          such as `Slug IN ('slugone', 'slugtwo')` or `Labels.environment IN ('prod', 'staging')`.
          Conjunctions are supported using the `AND` operator.
          An example conjunction is:
          `CreatedAt >= '2025-01-07' AND Slug = 'test' AND Labels.mykey = 'myvalue'`.

          Supported attributes for filtering on Target: BridgeHandle, BridgeWorkerID, CreatedAt, DeleteGates, DisplayName, Labels, LiveStateType, Options, OrganizationID, Permissions, ProviderType, Slug, SpaceID, TargetID, ToolchainType, TriggerFilterID, TriggerHash, TriggerIDs, UpdatedAt.

          The whole string must be query-encoded.
        in: query
        name: where
        required: false
        schema:
          type: string
      - description: |-
          UUID of a Filter entity to apply to the Target list.

          The Filter must be in the same Organization as the user credentials.

          The Filter's From field must match the entity type being filtered (Target).

          For Space-resident entities, if the Filter has a FromSpaceID, it must match the operation's SpaceID.

          The Filter's Where clause will be combined with any explicit 'where' parameter using AND logic.

          If both 'filter' and 'where' parameters are specified, they are combined with AND logic.
        in: query
        name: filter
        required: false
        schema:
          type: string
      - description: |-
          Free text search that approximately matches the specified string against string fields and map keys/values.

          The search is case-insensitive and uses pattern matching to find entities containing the text.

          Searchable string fields include attributes like Slug, DisplayName, and string-typed custom fields.

          For map fields (like Labels and Annotations), the search matches both map keys and values.

          The search uses OR logic across all searchable fields, so matching any field will return the entity.

          If both 'where' and 'contains' parameters are specified, they are combined with AND logic.

          Searchable fields for Target include string and map-type attributes from the queryable attributes list.

          The whole string must be query-encoded.
        in: query
        name: contains
        required: false
        schema:
          type: string
      - description: |-
          Include clause for expanding related entities in the response for Target.
          The attribute names are case-sensitive, PascalCase, and
          expected in a comma-separated list format as in the JSON encoding.

          Supported attributes for Target are BridgeWorkerID, OrganizationID, SpaceID, TriggerFilterID, TriggerIDs.

          The whole string must be query-encoded.
        in: query
        name: include
        required: false
        schema:
          type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                items:
                  $ref: '#/components/schemas/DeleteResponse'
                type: array
          description: OK
        "207":
          content:
            application/json:
              schema:
                items:
                  $ref: '#/components/schemas/DeleteResponse'
                type: array
          description: 'Multi-Status: Mixed success and failure results'
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Target request is invalid (Bad Request).
        "401":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Unauthorized access.
        "403":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Forbidden access.
        "404":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Target not found.
        "409":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Target data conflict. Data has changed since last read.
        "422":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Unable to delete entity
        "500":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Something went wrong while processing Target.
        default:
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Unexpected error.
      summary: Bulk delete multiple targets
      tags:
      - Target
    get:
      description: Retrieves a list of Targets across spaces in the Organization
      operationId: ListAllTargets
      parameters:
      - description: |-
          The specified string is an expression for the purpose of filtering
          the list of Targets returned. The expression syntax was inspired by SQL.
          It supports conjunctions using `AND` of relational expressions of the form *attribute*
          *operator* *attribute_or_literal*. The attribute names are case-sensitive and PascalCase,
          as in the JSON encoding.
          Strings support the following operators: `<`, `>`, `<=`, `>=`, `=`, `!=`, `LIKE`, `NOT LIKE`, `ILIKE`, `~~`, `!~~`, `~`, `~*`, `!~`, `!~*`, `IN`, `NOT IN`.
          String pattern operators: `LIKE` and `~~` for pattern matching with `%` and `_` wildcards,
          `ILIKE` for case-insensitive pattern matching, `NOT LIKE` and `!~~` for negated pattern matching.
          String regex operators: `~` for regex matching, `~*` for case-insensitive regex,
          `!~` and `!~*` for regex not matching (case-sensitive and insensitive).
          Integers support the following operators: `<`, `>`, `<=`, `>=`, `=`, `!=`, `IN`, `NOT IN`.
          UUIDs and boolean attributes support equality and inequality only.
          UUID and time literals must be quoted as string literals.
          String literals are quoted with single quotes, such as `'string'`.
          Time literals use the same form as when serialized as JSON,
          such as: `CreatedAt > '2025-02-18T23:16:34'`.
          Integer and boolean literals are also supported for attributes of those types.
          Arrays support the `?` operator to to match any element of the array,
          as in `ApprovedBy ? '7c61626f-ddbe-41af-93f6-b69f4ab6d308'`.
          Arrays can perform LEN() to check for length, as in `LEN(ApprovedBy) > 0`.
          Map support the dot notation to specify a particular map key, as in `Labels.tier = 'Backend'`.
          Maps support `IS NULL` and `IS NOT NULL` with dot notation to check for key absence or presence,
          as in `Labels.tier IS NULL` (key doesn't exist) or `Labels.tier IS NOT NULL` (key exists).
          Comparison results can be tested with `IS TRUE`, `IS FALSE`, `IS NOT TRUE`, and `IS NOT FALSE`.
          These are useful for nullable columns: `MergeSourceID = '<uuid>' IS NOT FALSE` matches rows where MergeSourceID equals the value OR is NULL.
          The `IN` and `NOT IN` operators accept a comma-separated list of values in parentheses,
          such as `Slug IN ('slugone', 'slugtwo')` or `Labels.environment IN ('prod', 'staging')`.
          Conjunctions are supported using the `AND` operator.
          An example conjunction is:
          `CreatedAt >= '2025-01-07' AND Slug = 'test' AND Labels.mykey = 'myvalue'`.

          Supported attributes for filtering on Target: BridgeHandle, BridgeWorkerID, CreatedAt, DeleteGates, DisplayName, Labels, LiveStateType, Options, OrganizationID, Permissions, ProviderType, Slug, SpaceID, TargetID, ToolchainType, TriggerFilterID, TriggerHash, TriggerIDs, UpdatedAt.

          The whole string must be query-encoded.
        in: query
        name: where
        required: false
        schema:
          type: string
      - description: |-
          UUID of a Filter entity to apply to the Target list.

          The Filter must be in the same Organization as the user credentials.

          The Filter's From field must match the entity type being filtered (Target).

          For Space-resident entities, if the Filter has a FromSpaceID, it must match the operation's SpaceID.

          The Filter's Where clause will be combined with any explicit 'where' parameter using AND logic.

          If both 'filter' and 'where' parameters are specified, they are combined with AND logic.
        in: query
        name: filter
        required: false
        schema:
          type: string
      - description: |-
          Free text search that approximately matches the specified string against string fields and map keys/values.

          The search is case-insensitive and uses pattern matching to find entities containing the text.

          Searchable string fields include attributes like Slug, DisplayName, and string-typed custom fields.

          For map fields (like Labels and Annotations), the search matches both map keys and values.

          The search uses OR logic across all searchable fields, so matching any field will return the entity.

          If both 'where' and 'contains' parameters are specified, they are combined with AND logic.

          Searchable fields for Target include string and map-type attributes from the queryable attributes list.

          The whole string must be query-encoded.
        in: query
        name: contains
        required: false
        schema:
          type: string
      - description: |-
          Include clause for expanding related entities in the response for Target.
          The attribute names are case-sensitive, PascalCase, and
          expected in a comma-separated list format as in the JSON encoding.

          Supported attributes for Target are BridgeWorkerID, OrganizationID, SpaceID, TriggerFilterID, TriggerIDs.

          The whole string must be query-encoded.
        in: query
        name: include
        required: false
        schema:
          type: string
      - description: |-
          Select clause for specifying which fields to include in the response for Target.
          The attribute names are case-sensitive, PascalCase, and
          expected in a comma-separated list format as in the JSON encoding.
          If not specified, all fields are returned.
          Entity and parent IDs (like OrganizationID, SpaceID, TargetID) and Slug are always returned regardless of the select parameter.
          Fields used in where and contains filters are also automatically included.
          Example: 'DisplayName,CreatedAt,Labels' will return only those fields plus the required ID and Slug fields.
          The whole string must be query-encoded.
        in: query
        name: select
        required: false
        schema:
          type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                items:
                  $ref: '#/components/schemas/ExtendedTarget'
                type: array
          description: OK
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Target request is invalid (Bad Request).
        "401":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Unauthorized access.
        "403":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Forbidden access.
        "404":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Target not found.
        "500":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Something went wrong while processing Target.
        default:
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Unexpected error.
      summary: List of Targets across spaces
      tags:
      - Target
    patch:
      description: Apply JSON merge patch to multiple targets selected by query parameters
      operationId: BulkPatchTargets
      parameters:
      - description: |-
          The specified string is an expression for the purpose of filtering
          the list of Targets returned. The expression syntax was inspired by SQL.
          It supports conjunctions using `AND` of relational expressions of the form *attribute*
          *operator* *attribute_or_literal*. The attribute names are case-sensitive and PascalCase,
          as in the JSON encoding.
          Strings support the following operators: `<`, `>`, `<=`, `>=`, `=`, `!=`, `LIKE`, `NOT LIKE`, `ILIKE`, `~~`, `!~~`, `~`, `~*`, `!~`, `!~*`, `IN`, `NOT IN`.
          String pattern operators: `LIKE` and `~~` for pattern matching with `%` and `_` wildcards,
          `ILIKE` for case-insensitive pattern matching, `NOT LIKE` and `!~~` for negated pattern matching.
          String regex operators: `~` for regex matching, `~*` for case-insensitive regex,
          `!~` and `!~*` for regex not matching (case-sensitive and insensitive).
          Integers support the following operators: `<`, `>`, `<=`, `>=`, `=`, `!=`, `IN`, `NOT IN`.
          UUIDs and boolean attributes support equality and inequality only.
          UUID and time literals must be quoted as string literals.
          String literals are quoted with single quotes, such as `'string'`.
          Time literals use the same form as when serialized as JSON,
          such as: `CreatedAt > '2025-02-18T23:16:34'`.
          Integer and boolean literals are also supported for attributes of those types.
          Arrays support the `?` operator to to match any element of the array,
          as in `ApprovedBy ? '7c61626f-ddbe-41af-93f6-b69f4ab6d308'`.
          Arrays can perform LEN() to check for length, as in `LEN(ApprovedBy) > 0`.
          Map support the dot notation to specify a particular map key, as in `Labels.tier = 'Backend'`.
          Maps support `IS NULL` and `IS NOT NULL` with dot notation to check for key absence or presence,
          as in `Labels.tier IS NULL` (key doesn't exist) or `Labels.tier IS NOT NULL` (key exists).
          Comparison results can be tested with `IS TRUE`, `IS FALSE`, `IS NOT TRUE`, and `IS NOT FALSE`.
          These are useful for nullable columns: `MergeSourceID = '<uuid>' IS NOT FALSE` matches rows where MergeSourceID equals the value OR is NULL.
          The `IN` and `NOT IN` operators accept a comma-separated list of values in parentheses,
          such as `Slug IN ('slugone', 'slugtwo')` or `Labels.environment IN ('prod', 'staging')`.
          Conjunctions are supported using the `AND` operator.
          An example conjunction is:
          `CreatedAt >= '2025-01-07' AND Slug = 'test' AND Labels.mykey = 'myvalue'`.

          Supported attributes for filtering on Target: BridgeHandle, BridgeWorkerID, CreatedAt, DeleteGates, DisplayName, Labels, LiveStateType, Options, OrganizationID, Permissions, ProviderType, Slug, SpaceID, TargetID, ToolchainType, TriggerFilterID, TriggerHash, TriggerIDs, UpdatedAt.

          The whole string must be query-encoded.
        in: query
        name: where
        required: false
        schema:
          type: string
      - description: |-
          UUID of a Filter entity to apply to the Target list.

          The Filter must be in the same Organization as the user credentials.

          The Filter's From field must match the entity type being filtered (Target).

          For Space-resident entities, if the Filter has a FromSpaceID, it must match the operation's SpaceID.

          The Filter's Where clause will be combined with any explicit 'where' parameter using AND logic.

          If both 'filter' and 'where' parameters are specified, they are combined with AND logic.
        in: query
        name: filter
        required: false
        schema:
          type: string
      - description: |-
          Free text search that approximately matches the specified string against string fields and map keys/values.

          The search is case-insensitive and uses pattern matching to find entities containing the text.

          Searchable string fields include attributes like Slug, DisplayName, and string-typed custom fields.

          For map fields (like Labels and Annotations), the search matches both map keys and values.

          The search uses OR logic across all searchable fields, so matching any field will return the entity.

          If both 'where' and 'contains' parameters are specified, they are combined with AND logic.

          Searchable fields for Target include string and map-type attributes from the queryable attributes list.

          The whole string must be query-encoded.
        in: query
        name: contains
        required: false
        schema:
          type: string
      - description: |-
          Include clause for expanding related entities in the response for Target.
          The attribute names are case-sensitive, PascalCase, and
          expected in a comma-separated list format as in the JSON encoding.

          Supported attributes for Target are BridgeWorkerID, OrganizationID, SpaceID, TriggerFilterID, TriggerIDs.

          The whole string must be query-encoded.
        in: query
        name: include
        required: false
        schema:
          type: string
      - description: Re-list the Triggers matching WhereTrigger and/or TriggerFilterID
          even if these fields have not changed
        in: query
        name: refresh_triggers
        required: false
        schema:
          type: boolean
      requestBody:
        content:
          application/merge-patch+json:
            schema:
              description: JSON Merge Patch schema for Target. All fields are optional
                and nullable. Readonly fields are ignored.
              properties:
                Annotations:
                  additionalProperties:
                    nullable: true
                    type: string
                  description: An optional map of Annotation key/value pairs for tools
                    to attach information to entities.
                  nullable: true
                  type: object
                BridgeHandle:
                  description: ""
                  nullable: true
                  type: string
                BridgeWorkerID:
                  description: ""
                  format: uuid
                  nullable: true
                  type: string
                ConfigTypes:
                  description: ""
                  items:
                    nullable: true
                    type: object
                  nullable: true
                  type: array
                DeleteGates:
                  additionalProperties:
                    nullable: true
                    type: boolean
                  description: An optional set of gates that, if any is present, will
                    block deletion
                  nullable: true
                  type: object
                DisplayName:
                  description: Friendly name for the entity.
                  nullable: true
                  type: string
                Labels:
                  additionalProperties:
                    nullable: true
                    type: string
                  description: An optional map of Label key/value pairs to specify
                    identifying attributes of entities for the purpose of grouping
                    and filtering them.
                  nullable: true
                  type: object
                LiveStateType:
                  description: ""
                  nullable: true
                  type: string
                Options:
                  additionalProperties:
                    nullable: true
                    type: string
                  description: ""
                  nullable: true
                  type: object
                Parameters:
                  description: ""
                  nullable: true
                  type: string
                Permissions:
                  additionalProperties:
                    nullable: true
                    type: object
                  description: ""
                  nullable: true
                  type: object
                ProviderType:
                  description: ""
                  nullable: true
                  type: string
                Slug:
                  description: Unique URL-safe identifier for the entity.
                  nullable: true
                  type: string
                ToolchainType:
                  description: ""
                  nullable: true
                  type: string
                TriggerFilterID:
                  description: ""
                  format: uuid
                  nullable: true
                  type: string
                Version:
                  description: An entity-specific sequence number used for optimistic
                    concurrency control. The value read must be sent in calls to Update.
                  nullable: true
                  type: integer
                WhereTrigger:
                  description: ""
                  nullable: true
                  type: string
              type: object
      responses:
        "200":
          content:
            application/json:
              schema:
                items:
                  $ref: '#/components/schemas/TargetCreateOrUpdateResponse'
                type: array
          description: OK
        "207":
          content:
            application/json:
              schema:
                items:
                  $ref: '#/components/schemas/TargetCreateOrUpdateResponse'
                type: array
          description: 'Multi-Status: Mixed success and failure results'
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Target request is invalid (Bad Request).
        "401":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Unauthorized access.
        "403":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Forbidden access.
        "404":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Target not found.
        "409":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Target data conflict. Data has changed since last read.
        "500":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Something went wrong while processing Target.
        default:
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Unexpected error.
      summary: Bulk patch multiple targets
      tags:
      - Target
  /trigger:
    delete:
      description: Delete multiple triggers selected by query parameters
      operationId: BulkDeleteTriggers
      parameters:
      - description: |-
          The specified string is an expression for the purpose of filtering
          the list of Triggers returned. The expression syntax was inspired by SQL.
          It supports conjunctions using `AND` of relational expressions of the form *attribute*
          *operator* *attribute_or_literal*. The attribute names are case-sensitive and PascalCase,
          as in the JSON encoding.
          Strings support the following operators: `<`, `>`, `<=`, `>=`, `=`, `!=`, `LIKE`, `NOT LIKE`, `ILIKE`, `~~`, `!~~`, `~`, `~*`, `!~`, `!~*`, `IN`, `NOT IN`.
          String pattern operators: `LIKE` and `~~` for pattern matching with `%` and `_` wildcards,
          `ILIKE` for case-insensitive pattern matching, `NOT LIKE` and `!~~` for negated pattern matching.
          String regex operators: `~` for regex matching, `~*` for case-insensitive regex,
          `!~` and `!~*` for regex not matching (case-sensitive and insensitive).
          Integers support the following operators: `<`, `>`, `<=`, `>=`, `=`, `!=`, `IN`, `NOT IN`.
          UUIDs and boolean attributes support equality and inequality only.
          UUID and time literals must be quoted as string literals.
          String literals are quoted with single quotes, such as `'string'`.
          Time literals use the same form as when serialized as JSON,
          such as: `CreatedAt > '2025-02-18T23:16:34'`.
          Integer and boolean literals are also supported for attributes of those types.
          Arrays support the `?` operator to to match any element of the array,
          as in `ApprovedBy ? '7c61626f-ddbe-41af-93f6-b69f4ab6d308'`.
          Arrays can perform LEN() to check for length, as in `LEN(ApprovedBy) > 0`.
          Map support the dot notation to specify a particular map key, as in `Labels.tier = 'Backend'`.
          Maps support `IS NULL` and `IS NOT NULL` with dot notation to check for key absence or presence,
          as in `Labels.tier IS NULL` (key doesn't exist) or `Labels.tier IS NOT NULL` (key exists).
          Comparison results can be tested with `IS TRUE`, `IS FALSE`, `IS NOT TRUE`, and `IS NOT FALSE`.
          These are useful for nullable columns: `MergeSourceID = '<uuid>' IS NOT FALSE` matches rows where MergeSourceID equals the value OR is NULL.
          The `IN` and `NOT IN` operators accept a comma-separated list of values in parentheses,
          such as `Slug IN ('slugone', 'slugtwo')` or `Labels.environment IN ('prod', 'staging')`.
          Conjunctions are supported using the `AND` operator.
          An example conjunction is:
          `CreatedAt >= '2025-01-07' AND Slug = 'test' AND Labels.mykey = 'myvalue'`.

          Supported attributes for filtering on Trigger: BridgeWorkerID, CreatedAt, DeleteGates, Description, Disabled, DisplayName, Event, FunctionName, Hash, InvocationID, Labels, OrganizationID, OtherDataSource, Slug, SpaceID, ToolchainType, TriggerID, UnitFilterID, UpdatedAt, Validating, Warn, WhereResource, WhereUnit.

          The whole string must be query-encoded.
        in: query
        name: where
        required: false
        schema:
          type: string
      - description: |-
          UUID of a Filter entity to apply to the Trigger list.

          The Filter must be in the same Organization as the user credentials.

          The Filter's From field must match the entity type being filtered (Trigger).

          For Space-resident entities, if the Filter has a FromSpaceID, it must match the operation's SpaceID.

          The Filter's Where clause will be combined with any explicit 'where' parameter using AND logic.

          If both 'filter' and 'where' parameters are specified, they are combined with AND logic.
        in: query
        name: filter
        required: false
        schema:
          type: string
      - description: |-
          Free text search that approximately matches the specified string against string fields and map keys/values.

          The search is case-insensitive and uses pattern matching to find entities containing the text.

          Searchable string fields include attributes like Slug, DisplayName, and string-typed custom fields.

          For map fields (like Labels and Annotations), the search matches both map keys and values.

          The search uses OR logic across all searchable fields, so matching any field will return the entity.

          If both 'where' and 'contains' parameters are specified, they are combined with AND logic.

          Searchable fields for Trigger include string and map-type attributes from the queryable attributes list.

          The whole string must be query-encoded.
        in: query
        name: contains
        required: false
        schema:
          type: string
      - description: |-
          Include clause for expanding related entities in the response for Trigger.
          The attribute names are case-sensitive, PascalCase, and
          expected in a comma-separated list format as in the JSON encoding.

          Supported attributes for Trigger are BridgeWorkerID, InvocationID, OrganizationID, SpaceID, UnitFilterID.

          The whole string must be query-encoded.
        in: query
        name: include
        required: false
        schema:
          type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                items:
                  $ref: '#/components/schemas/DeleteResponse'
                type: array
          description: OK
        "207":
          content:
            application/json:
              schema:
                items:
                  $ref: '#/components/schemas/DeleteResponse'
                type: array
          description: 'Multi-Status: Mixed success and failure results'
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Trigger request is invalid (Bad Request).
        "401":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Unauthorized access.
        "403":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Forbidden access.
        "404":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Trigger not found.
        "409":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Trigger data conflict. Data has changed since last read.
        "422":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Unable to delete entity
        "500":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Something went wrong while processing Trigger.
        default:
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Unexpected error.
      summary: Bulk delete multiple triggers
      tags:
      - Trigger
    get:
      description: Retrieves a list of Triggers across spaces in the Organization
      operationId: ListAllTriggers
      parameters:
      - description: |-
          The specified string is an expression for the purpose of filtering
          the list of Triggers returned. The expression syntax was inspired by SQL.
          It supports conjunctions using `AND` of relational expressions of the form *attribute*
          *operator* *attribute_or_literal*. The attribute names are case-sensitive and PascalCase,
          as in the JSON encoding.
          Strings support the following operators: `<`, `>`, `<=`, `>=`, `=`, `!=`, `LIKE`, `NOT LIKE`, `ILIKE`, `~~`, `!~~`, `~`, `~*`, `!~`, `!~*`, `IN`, `NOT IN`.
          String pattern operators: `LIKE` and `~~` for pattern matching with `%` and `_` wildcards,
          `ILIKE` for case-insensitive pattern matching, `NOT LIKE` and `!~~` for negated pattern matching.
          String regex operators: `~` for regex matching, `~*` for case-insensitive regex,
          `!~` and `!~*` for regex not matching (case-sensitive and insensitive).
          Integers support the following operators: `<`, `>`, `<=`, `>=`, `=`, `!=`, `IN`, `NOT IN`.
          UUIDs and boolean attributes support equality and inequality only.
          UUID and time literals must be quoted as string literals.
          String literals are quoted with single quotes, such as `'string'`.
          Time literals use the same form as when serialized as JSON,
          such as: `CreatedAt > '2025-02-18T23:16:34'`.
          Integer and boolean literals are also supported for attributes of those types.
          Arrays support the `?` operator to to match any element of the array,
          as in `ApprovedBy ? '7c61626f-ddbe-41af-93f6-b69f4ab6d308'`.
          Arrays can perform LEN() to check for length, as in `LEN(ApprovedBy) > 0`.
          Map support the dot notation to specify a particular map key, as in `Labels.tier = 'Backend'`.
          Maps support `IS NULL` and `IS NOT NULL` with dot notation to check for key absence or presence,
          as in `Labels.tier IS NULL` (key doesn't exist) or `Labels.tier IS NOT NULL` (key exists).
          Comparison results can be tested with `IS TRUE`, `IS FALSE`, `IS NOT TRUE`, and `IS NOT FALSE`.
          These are useful for nullable columns: `MergeSourceID = '<uuid>' IS NOT FALSE` matches rows where MergeSourceID equals the value OR is NULL.
          The `IN` and `NOT IN` operators accept a comma-separated list of values in parentheses,
          such as `Slug IN ('slugone', 'slugtwo')` or `Labels.environment IN ('prod', 'staging')`.
          Conjunctions are supported using the `AND` operator.
          An example conjunction is:
          `CreatedAt >= '2025-01-07' AND Slug = 'test' AND Labels.mykey = 'myvalue'`.

          Supported attributes for filtering on Trigger: BridgeWorkerID, CreatedAt, DeleteGates, Description, Disabled, DisplayName, Event, FunctionName, Hash, InvocationID, Labels, OrganizationID, OtherDataSource, Slug, SpaceID, ToolchainType, TriggerID, UnitFilterID, UpdatedAt, Validating, Warn, WhereResource, WhereUnit.

          The whole string must be query-encoded.
        in: query
        name: where
        required: false
        schema:
          type: string
      - description: |-
          UUID of a Filter entity to apply to the Trigger list.

          The Filter must be in the same Organization as the user credentials.

          The Filter's From field must match the entity type being filtered (Trigger).

          For Space-resident entities, if the Filter has a FromSpaceID, it must match the operation's SpaceID.

          The Filter's Where clause will be combined with any explicit 'where' parameter using AND logic.

          If both 'filter' and 'where' parameters are specified, they are combined with AND logic.
        in: query
        name: filter
        required: false
        schema:
          type: string
      - description: |-
          Free text search that approximately matches the specified string against string fields and map keys/values.

          The search is case-insensitive and uses pattern matching to find entities containing the text.

          Searchable string fields include attributes like Slug, DisplayName, and string-typed custom fields.

          For map fields (like Labels and Annotations), the search matches both map keys and values.

          The search uses OR logic across all searchable fields, so matching any field will return the entity.

          If both 'where' and 'contains' parameters are specified, they are combined with AND logic.

          Searchable fields for Trigger include string and map-type attributes from the queryable attributes list.

          The whole string must be query-encoded.
        in: query
        name: contains
        required: false
        schema:
          type: string
      - description: |-
          Include clause for expanding related entities in the response for Trigger.
          The attribute names are case-sensitive, PascalCase, and
          expected in a comma-separated list format as in the JSON encoding.

          Supported attributes for Trigger are BridgeWorkerID, InvocationID, OrganizationID, SpaceID, UnitFilterID.

          The whole string must be query-encoded.
        in: query
        name: include
        required: false
        schema:
          type: string
      - description: |-
          Select clause for specifying which fields to include in the response for Trigger.
          The attribute names are case-sensitive, PascalCase, and
          expected in a comma-separated list format as in the JSON encoding.
          If not specified, all fields are returned.
          Entity and parent IDs (like OrganizationID, SpaceID, TriggerID) and Slug are always returned regardless of the select parameter.
          Fields used in where and contains filters are also automatically included.
          Example: 'DisplayName,CreatedAt,Labels' will return only those fields plus the required ID and Slug fields.
          The whole string must be query-encoded.
        in: query
        name: select
        required: false
        schema:
          type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                items:
                  $ref: '#/components/schemas/ExtendedTrigger'
                type: array
          description: OK
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Trigger request is invalid (Bad Request).
        "401":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Unauthorized access.
        "403":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Forbidden access.
        "404":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Trigger not found.
        "500":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Something went wrong while processing Trigger.
        default:
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Unexpected error.
      summary: List of Triggers across spaces
      tags:
      - Trigger
    patch:
      description: Apply JSON merge patch to multiple triggers selected by query parameters
      operationId: BulkPatchTriggers
      parameters:
      - description: |-
          The specified string is an expression for the purpose of filtering
          the list of Triggers returned. The expression syntax was inspired by SQL.
          It supports conjunctions using `AND` of relational expressions of the form *attribute*
          *operator* *attribute_or_literal*. The attribute names are case-sensitive and PascalCase,
          as in the JSON encoding.
          Strings support the following operators: `<`, `>`, `<=`, `>=`, `=`, `!=`, `LIKE`, `NOT LIKE`, `ILIKE`, `~~`, `!~~`, `~`, `~*`, `!~`, `!~*`, `IN`, `NOT IN`.
          String pattern operators: `LIKE` and `~~` for pattern matching with `%` and `_` wildcards,
          `ILIKE` for case-insensitive pattern matching, `NOT LIKE` and `!~~` for negated pattern matching.
          String regex operators: `~` for regex matching, `~*` for case-insensitive regex,
          `!~` and `!~*` for regex not matching (case-sensitive and insensitive).
          Integers support the following operators: `<`, `>`, `<=`, `>=`, `=`, `!=`, `IN`, `NOT IN`.
          UUIDs and boolean attributes support equality and inequality only.
          UUID and time literals must be quoted as string literals.
          String literals are quoted with single quotes, such as `'string'`.
          Time literals use the same form as when serialized as JSON,
          such as: `CreatedAt > '2025-02-18T23:16:34'`.
          Integer and boolean literals are also supported for attributes of those types.
          Arrays support the `?` operator to to match any element of the array,
          as in `ApprovedBy ? '7c61626f-ddbe-41af-93f6-b69f4ab6d308'`.
          Arrays can perform LEN() to check for length, as in `LEN(ApprovedBy) > 0`.
          Map support the dot notation to specify a particular map key, as in `Labels.tier = 'Backend'`.
          Maps support `IS NULL` and `IS NOT NULL` with dot notation to check for key absence or presence,
          as in `Labels.tier IS NULL` (key doesn't exist) or `Labels.tier IS NOT NULL` (key exists).
          Comparison results can be tested with `IS TRUE`, `IS FALSE`, `IS NOT TRUE`, and `IS NOT FALSE`.
          These are useful for nullable columns: `MergeSourceID = '<uuid>' IS NOT FALSE` matches rows where MergeSourceID equals the value OR is NULL.
          The `IN` and `NOT IN` operators accept a comma-separated list of values in parentheses,
          such as `Slug IN ('slugone', 'slugtwo')` or `Labels.environment IN ('prod', 'staging')`.
          Conjunctions are supported using the `AND` operator.
          An example conjunction is:
          `CreatedAt >= '2025-01-07' AND Slug = 'test' AND Labels.mykey = 'myvalue'`.

          Supported attributes for filtering on Trigger: BridgeWorkerID, CreatedAt, DeleteGates, Description, Disabled, DisplayName, Event, FunctionName, Hash, InvocationID, Labels, OrganizationID, OtherDataSource, Slug, SpaceID, ToolchainType, TriggerID, UnitFilterID, UpdatedAt, Validating, Warn, WhereResource, WhereUnit.

          The whole string must be query-encoded.
        in: query
        name: where
        required: false
        schema:
          type: string
      - description: |-
          UUID of a Filter entity to apply to the Trigger list.

          The Filter must be in the same Organization as the user credentials.

          The Filter's From field must match the entity type being filtered (Trigger).

          For Space-resident entities, if the Filter has a FromSpaceID, it must match the operation's SpaceID.

          The Filter's Where clause will be combined with any explicit 'where' parameter using AND logic.

          If both 'filter' and 'where' parameters are specified, they are combined with AND logic.
        in: query
        name: filter
        required: false
        schema:
          type: string
      - description: |-
          Free text search that approximately matches the specified string against string fields and map keys/values.

          The search is case-insensitive and uses pattern matching to find entities containing the text.

          Searchable string fields include attributes like Slug, DisplayName, and string-typed custom fields.

          For map fields (like Labels and Annotations), the search matches both map keys and values.

          The search uses OR logic across all searchable fields, so matching any field will return the entity.

          If both 'where' and 'contains' parameters are specified, they are combined with AND logic.

          Searchable fields for Trigger include string and map-type attributes from the queryable attributes list.

          The whole string must be query-encoded.
        in: query
        name: contains
        required: false
        schema:
          type: string
      - description: |-
          Include clause for expanding related entities in the response for Trigger.
          The attribute names are case-sensitive, PascalCase, and
          expected in a comma-separated list format as in the JSON encoding.

          Supported attributes for Trigger are BridgeWorkerID, InvocationID, OrganizationID, SpaceID, UnitFilterID.

          The whole string must be query-encoded.
        in: query
        name: include
        required: false
        schema:
          type: string
      requestBody:
        content:
          application/merge-patch+json:
            schema:
              description: JSON Merge Patch schema for Trigger. All fields are optional
                and nullable. Readonly fields are ignored.
              properties:
                Annotations:
                  additionalProperties:
                    nullable: true
                    type: string
                  description: An optional map of Annotation key/value pairs for tools
                    to attach information to entities.
                  nullable: true
                  type: object
                Arguments:
                  description: Function arguments
                  items:
                    nullable: true
                    type: object
                  nullable: true
                  type: array
                BridgeWorkerID:
                  description: ""
                  format: uuid
                  nullable: true
                  type: string
                DeleteGates:
                  additionalProperties:
                    nullable: true
                    type: boolean
                  description: An optional set of gates that, if any is present, will
                    block deletion
                  nullable: true
                  type: object
                Description:
                  description: ""
                  nullable: true
                  type: string
                Disabled:
                  description: ""
                  nullable: true
                  type: boolean
                DisplayName:
                  description: Friendly name for the entity.
                  nullable: true
                  type: string
                Event:
                  description: ""
                  nullable: true
                  type: string
                FailOpenAfter:
                  description: ""
                  nullable: true
                  type: integer
                FunctionName:
                  description: Function name
                  nullable: true
                  type: string
                InvocationID:
                  description: ""
                  format: uuid
                  nullable: true
                  type: string
                Labels:
                  additionalProperties:
                    nullable: true
                    type: string
                  description: An optional map of Label key/value pairs to specify
                    identifying attributes of entities for the purpose of grouping
                    and filtering them.
                  nullable: true
                  type: object
                OtherDataSource:
                  description: ""
                  nullable: true
                  type: string
                Slug:
                  description: Unique URL-safe identifier for the entity.
                  nullable: true
                  type: string
                ToolchainType:
                  description: ""
                  nullable: true
                  type: string
                UnitFilterID:
                  description: ""
                  format: uuid
                  nullable: true
                  type: string
                Version:
                  description: An entity-specific sequence number used for optimistic
                    concurrency control. The value read must be sent in calls to Update.
                  nullable: true
                  type: integer
                Warn:
                  description: ""
                  nullable: true
                  type: boolean
                WhereResource:
                  description: ""
                  nullable: true
                  type: string
                WhereUnit:
                  description: ""
                  nullable: true
                  type: string
              type: object
      responses:
        "200":
          content:
            application/json:
              schema:
                items:
                  $ref: '#/components/schemas/TriggerCreateOrUpdateResponse'
                type: array
          description: OK
        "207":
          content:
            application/json:
              schema:
                items:
                  $ref: '#/components/schemas/TriggerCreateOrUpdateResponse'
                type: array
          description: 'Multi-Status: Mixed success and failure results'
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Trigger request is invalid (Bad Request).
        "401":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Unauthorized access.
        "403":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Forbidden access.
        "404":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Trigger not found.
        "409":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Trigger data conflict. Data has changed since last read.
        "500":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Something went wrong while processing Trigger.
        default:
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Unexpected error.
      summary: Bulk patch multiple triggers
      tags:
      - Trigger
    post:
      description: Clone multiple triggers selected by query parameters with optional
        name prefixes and destination spaces
      operationId: BulkCreateTriggers
      parameters:
      - description: |-
          The specified string is an expression for the purpose of filtering
          the list of Triggers returned. The expression syntax was inspired by SQL.
          It supports conjunctions using `AND` of relational expressions of the form *attribute*
          *operator* *attribute_or_literal*. The attribute names are case-sensitive and PascalCase,
          as in the JSON encoding.
          Strings support the following operators: `<`, `>`, `<=`, `>=`, `=`, `!=`, `LIKE`, `NOT LIKE`, `ILIKE`, `~~`, `!~~`, `~`, `~*`, `!~`, `!~*`, `IN`, `NOT IN`.
          String pattern operators: `LIKE` and `~~` for pattern matching with `%` and `_` wildcards,
          `ILIKE` for case-insensitive pattern matching, `NOT LIKE` and `!~~` for negated pattern matching.
          String regex operators: `~` for regex matching, `~*` for case-insensitive regex,
          `!~` and `!~*` for regex not matching (case-sensitive and insensitive).
          Integers support the following operators: `<`, `>`, `<=`, `>=`, `=`, `!=`, `IN`, `NOT IN`.
          UUIDs and boolean attributes support equality and inequality only.
          UUID and time literals must be quoted as string literals.
          String literals are quoted with single quotes, such as `'string'`.
          Time literals use the same form as when serialized as JSON,
          such as: `CreatedAt > '2025-02-18T23:16:34'`.
          Integer and boolean literals are also supported for attributes of those types.
          Arrays support the `?` operator to to match any element of the array,
          as in `ApprovedBy ? '7c61626f-ddbe-41af-93f6-b69f4ab6d308'`.
          Arrays can perform LEN() to check for length, as in `LEN(ApprovedBy) > 0`.
          Map support the dot notation to specify a particular map key, as in `Labels.tier = 'Backend'`.
          Maps support `IS NULL` and `IS NOT NULL` with dot notation to check for key absence or presence,
          as in `Labels.tier IS NULL` (key doesn't exist) or `Labels.tier IS NOT NULL` (key exists).
          Comparison results can be tested with `IS TRUE`, `IS FALSE`, `IS NOT TRUE`, and `IS NOT FALSE`.
          These are useful for nullable columns: `MergeSourceID = '<uuid>' IS NOT FALSE` matches rows where MergeSourceID equals the value OR is NULL.
          The `IN` and `NOT IN` operators accept a comma-separated list of values in parentheses,
          such as `Slug IN ('slugone', 'slugtwo')` or `Labels.environment IN ('prod', 'staging')`.
          Conjunctions are supported using the `AND` operator.
          An example conjunction is:
          `CreatedAt >= '2025-01-07' AND Slug = 'test' AND Labels.mykey = 'myvalue'`.

          Supported attributes for filtering on Trigger: BridgeWorkerID, CreatedAt, DeleteGates, Description, Disabled, DisplayName, Event, FunctionName, Hash, InvocationID, Labels, OrganizationID, OtherDataSource, Slug, SpaceID, ToolchainType, TriggerID, UnitFilterID, UpdatedAt, Validating, Warn, WhereResource, WhereUnit.

          The whole string must be query-encoded.
        in: query
        name: where
        required: false
        schema:
          type: string
      - description: |-
          UUID of a Filter entity to apply to the Trigger list.

          The Filter must be in the same Organization as the user credentials.

          The Filter's From field must match the entity type being filtered (Trigger).

          For Space-resident entities, if the Filter has a FromSpaceID, it must match the operation's SpaceID.

          The Filter's Where clause will be combined with any explicit 'where' parameter using AND logic.

          If both 'filter' and 'where' parameters are specified, they are combined with AND logic.
        in: query
        name: filter
        required: false
        schema:
          type: string
      - description: |-
          Free text search that approximately matches the specified string against string fields and map keys/values.

          The search is case-insensitive and uses pattern matching to find entities containing the text.

          Searchable string fields include attributes like Slug, DisplayName, and string-typed custom fields.

          For map fields (like Labels and Annotations), the search matches both map keys and values.

          The search uses OR logic across all searchable fields, so matching any field will return the entity.

          If both 'where' and 'contains' parameters are specified, they are combined with AND logic.

          Searchable fields for Trigger include string and map-type attributes from the queryable attributes list.

          The whole string must be query-encoded.
        in: query
        name: contains
        required: false
        schema:
          type: string
      - description: |-
          Include clause for expanding related entities in the response for Trigger.
          The attribute names are case-sensitive, PascalCase, and
          expected in a comma-separated list format as in the JSON encoding.

          Supported attributes for Trigger are BridgeWorkerID, InvocationID, OrganizationID, SpaceID, UnitFilterID.

          The whole string must be query-encoded.
        in: query
        name: include
        required: false
        schema:
          type: string
      - description: Comma-separated list of prefixes to apply to cloned Trigger names
        in: query
        name: name_prefixes
        required: false
        schema:
          type: string
      - description: |-
          The specified string is an expression for the purpose of filtering
          the list of Spaces returned. The expression syntax was inspired by SQL.
          It supports conjunctions using `AND` of relational expressions of the form *attribute*
          *operator* *attribute_or_literal*. The attribute names are case-sensitive and PascalCase,
          as in the JSON encoding.
          Strings support the following operators: `<`, `>`, `<=`, `>=`, `=`, `!=`, `LIKE`, `NOT LIKE`, `ILIKE`, `~~`, `!~~`, `~`, `~*`, `!~`, `!~*`, `IN`, `NOT IN`.
          String pattern operators: `LIKE` and `~~` for pattern matching with `%` and `_` wildcards,
          `ILIKE` for case-insensitive pattern matching, `NOT LIKE` and `!~~` for negated pattern matching.
          String regex operators: `~` for regex matching, `~*` for case-insensitive regex,
          `!~` and `!~*` for regex not matching (case-sensitive and insensitive).
          Integers support the following operators: `<`, `>`, `<=`, `>=`, `=`, `!=`, `IN`, `NOT IN`.
          UUIDs and boolean attributes support equality and inequality only.
          UUID and time literals must be quoted as string literals.
          String literals are quoted with single quotes, such as `'string'`.
          Time literals use the same form as when serialized as JSON,
          such as: `CreatedAt > '2025-02-18T23:16:34'`.
          Integer and boolean literals are also supported for attributes of those types.
          Arrays support the `?` operator to to match any element of the array,
          as in `ApprovedBy ? '7c61626f-ddbe-41af-93f6-b69f4ab6d308'`.
          Arrays can perform LEN() to check for length, as in `LEN(ApprovedBy) > 0`.
          Map support the dot notation to specify a particular map key, as in `Labels.tier = 'Backend'`.
          Maps support `IS NULL` and `IS NOT NULL` with dot notation to check for key absence or presence,
          as in `Labels.tier IS NULL` (key doesn't exist) or `Labels.tier IS NOT NULL` (key exists).
          Comparison results can be tested with `IS TRUE`, `IS FALSE`, `IS NOT TRUE`, and `IS NOT FALSE`.
          These are useful for nullable columns: `MergeSourceID = '<uuid>' IS NOT FALSE` matches rows where MergeSourceID equals the value OR is NULL.
          The `IN` and `NOT IN` operators accept a comma-separated list of values in parentheses,
          such as `Slug IN ('slugone', 'slugtwo')` or `Labels.environment IN ('prod', 'staging')`.
          Conjunctions are supported using the `AND` operator.
          An example conjunction is:
          `CreatedAt >= '2025-01-07' AND Slug = 'test' AND Labels.mykey = 'myvalue'`.

          Supported attributes for filtering on Space: AttributeFilterID, AttributeHash, AttributeIDs, CreatedAt, DeleteGates, DisplayName, Labels, OrganizationID, Permissions, Slug, SpaceID, TriggerFilterID, TriggerHash, TriggerIDs, UpdatedAt.

          Where expression to select destination spaces for cloning triggers

          The whole string must be query-encoded.
        in: query
        name: where_space
        required: false
        schema:
          type: string
      - description: |-
          UUID of a Filter entity to apply to the Space list.

          The Filter must be in the same Organization as the user credentials.

          The Filter's From field must match the entity type being filtered (Space).

          For Space-resident entities, if the Filter has a FromSpaceID, it must match the operation's SpaceID.

          The Filter's Where clause will be combined with any explicit 'where' parameter using AND logic.

          If both 'filter' and 'where' parameters are specified, they are combined with AND logic.
        in: query
        name: filter_space
        required: false
        schema:
          type: string
      - description: Allowed values are true and false. Default is false. When true,
          reports success when an entity already exists and returns the existing entity
        in: query
        name: allow_exists
        required: false
        schema:
          type: string
      requestBody:
        content:
          application/merge-patch+json:
            schema:
              description: JSON Merge Patch schema for Trigger. All fields are optional
                and nullable. Readonly fields are ignored.
              properties:
                Annotations:
                  additionalProperties:
                    nullable: true
                    type: string
                  description: An optional map of Annotation key/value pairs for tools
                    to attach information to entities.
                  nullable: true
                  type: object
                Arguments:
                  description: Function arguments
                  items:
                    nullable: true
                    type: object
                  nullable: true
                  type: array
                BridgeWorkerID:
                  description: ""
                  format: uuid
                  nullable: true
                  type: string
                DeleteGates:
                  additionalProperties:
                    nullable: true
                    type: boolean
                  description: An optional set of gates that, if any is present, will
                    block deletion
                  nullable: true
                  type: object
                Description:
                  description: ""
                  nullable: true
                  type: string
                Disabled:
                  description: ""
                  nullable: true
                  type: boolean
                DisplayName:
                  description: Friendly name for the entity.
                  nullable: true
                  type: string
                Event:
                  description: ""
                  nullable: true
                  type: string
                FailOpenAfter:
                  description: ""
                  nullable: true
                  type: integer
                FunctionName:
                  description: Function name
                  nullable: true
                  type: string
                InvocationID:
                  description: ""
                  format: uuid
                  nullable: true
                  type: string
                Labels:
                  additionalProperties:
                    nullable: true
                    type: string
                  description: An optional map of Label key/value pairs to specify
                    identifying attributes of entities for the purpose of grouping
                    and filtering them.
                  nullable: true
                  type: object
                OtherDataSource:
                  description: ""
                  nullable: true
                  type: string
                Slug:
                  description: Unique URL-safe identifier for the entity.
                  nullable: true
                  type: string
                ToolchainType:
                  description: ""
                  nullable: true
                  type: string
                UnitFilterID:
                  description: ""
                  format: uuid
                  nullable: true
                  type: string
                Version:
                  description: An entity-specific sequence number used for optimistic
                    concurrency control. The value read must be sent in calls to Update.
                  nullable: true
                  type: integer
                Warn:
                  description: ""
                  nullable: true
                  type: boolean
                WhereResource:
                  description: ""
                  nullable: true
                  type: string
                WhereUnit:
                  description: ""
                  nullable: true
                  type: string
              type: object
      responses:
        "200":
          content:
            application/json:
              schema:
                items:
                  $ref: '#/components/schemas/TriggerCreateOrUpdateResponse'
                type: array
          description: OK
        "207":
          content:
            application/json:
              schema:
                items:
                  $ref: '#/components/schemas/TriggerCreateOrUpdateResponse'
                type: array
          description: Multi-Status (partial success)
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Trigger request is invalid (Bad Request).
        "401":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Unauthorized access.
        "403":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Forbidden access.
        "404":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Trigger not found.
        "409":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Trigger data conflict. Data has changed since last read.
        "500":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Something went wrong while processing Trigger.
        default:
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Unexpected error.
      summary: Bulk create (clone) multiple triggers
      tags:
      - Trigger
  /unit:
    delete:
      description: Delete multiple units selected by query parameters
      operationId: BulkDeleteUnits
      parameters:
      - description: |-
          The specified string is an expression for the purpose of filtering
          the list of Units returned. The expression syntax was inspired by SQL.
          It supports conjunctions using `AND` of relational expressions of the form *attribute*
          *operator* *attribute_or_literal*. The attribute names are case-sensitive and PascalCase,
          as in the JSON encoding.
          Strings support the following operators: `<`, `>`, `<=`, `>=`, `=`, `!=`, `LIKE`, `NOT LIKE`, `ILIKE`, `~~`, `!~~`, `~`, `~*`, `!~`, `!~*`, `IN`, `NOT IN`.
          String pattern operators: `LIKE` and `~~` for pattern matching with `%` and `_` wildcards,
          `ILIKE` for case-insensitive pattern matching, `NOT LIKE` and `!~~` for negated pattern matching.
          String regex operators: `~` for regex matching, `~*` for case-insensitive regex,
          `!~` and `!~*` for regex not matching (case-sensitive and insensitive).
          Integers support the following operators: `<`, `>`, `<=`, `>=`, `=`, `!=`, `IN`, `NOT IN`.
          UUIDs and boolean attributes support equality and inequality only.
          UUID and time literals must be quoted as string literals.
          String literals are quoted with single quotes, such as `'string'`.
          Time literals use the same form as when serialized as JSON,
          such as: `CreatedAt > '2025-02-18T23:16:34'`.
          Integer and boolean literals are also supported for attributes of those types.
          Arrays support the `?` operator to to match any element of the array,
          as in `ApprovedBy ? '7c61626f-ddbe-41af-93f6-b69f4ab6d308'`.
          Arrays can perform LEN() to check for length, as in `LEN(ApprovedBy) > 0`.
          Map support the dot notation to specify a particular map key, as in `Labels.tier = 'Backend'`.
          Maps support `IS NULL` and `IS NOT NULL` with dot notation to check for key absence or presence,
          as in `Labels.tier IS NULL` (key doesn't exist) or `Labels.tier IS NOT NULL` (key exists).
          Comparison results can be tested with `IS TRUE`, `IS FALSE`, `IS NOT TRUE`, and `IS NOT FALSE`.
          These are useful for nullable columns: `MergeSourceID = '<uuid>' IS NOT FALSE` matches rows where MergeSourceID equals the value OR is NULL.
          The `IN` and `NOT IN` operators accept a comma-separated list of values in parentheses,
          such as `Slug IN ('slugone', 'slugtwo')` or `Labels.environment IN ('prod', 'staging')`.
          Conjunctions are supported using the `AND` operator.
          An example conjunction is:
          `CreatedAt >= '2025-01-07' AND Slug = 'test' AND Labels.mykey = 'myvalue'`.

          Supported attributes for filtering on Unit: ApplyGates, ApplyWarnings, ApprovedBy, BridgeWorkerID, ChangeSetID, CreatedAt, DataHash, DeleteGates, DestroyGates, DisplayName, DriftReconciliationMode, FromLinkID, HeadRevisionNum, HeadUnitActionNum, HeadUnitEventNum, Labels, LastActionAt, LastAppliedRevisionNum, LastChangeDescription, LiveRevisionNum, OrganizationID, PreviousLiveRevisionNum, ProviderType, Slug, SpaceID, TargetID, TargetOptions, ToolchainType, UnitID, UpdatedAt, UpstreamOrganizationID, UpstreamRevisionNum, UpstreamSpaceID, UpstreamUnitID, Values.

          Finding all units created by cloning can be done using the expression `UpstreamRevisionNum > 0`. Clones of a specific unit can be found by additionally filtering based on `UpstreamUnitID`. Unapplied units can be found using `LiveRevisionNum = 0`. Units with unapplied changes can be found with `HeadRevisionNum > LiveRevisionNum`.

          The whole string must be query-encoded.
        in: query
        name: where
        required: false
        schema:
          type: string
      - description: |-
          UUID of a Filter entity to apply to the Unit list.

          The Filter must be in the same Organization as the user credentials.

          The Filter's From field must match the entity type being filtered (Unit).

          For Space-resident entities, if the Filter has a FromSpaceID, it must match the operation's SpaceID.

          The Filter's Where clause will be combined with any explicit 'where' parameter using AND logic.

          If both 'filter' and 'where' parameters are specified, they are combined with AND logic.
        in: query
        name: filter
        required: false
        schema:
          type: string
      - description: |-
          Free text search that approximately matches the specified string against string fields and map keys/values.

          The search is case-insensitive and uses pattern matching to find entities containing the text.

          Searchable string fields include attributes like Slug, DisplayName, and string-typed custom fields.

          For map fields (like Labels and Annotations), the search matches both map keys and values.

          The search uses OR logic across all searchable fields, so matching any field will return the entity.

          If both 'where' and 'contains' parameters are specified, they are combined with AND logic.

          Searchable fields for Unit include string and map-type attributes from the queryable attributes list.

          The whole string must be query-encoded.
        in: query
        name: contains
        required: false
        schema:
          type: string
      - description: |-
          Include clause for expanding related entities in the response for Unit.
          The attribute names are case-sensitive, PascalCase, and
          expected in a comma-separated list format as in the JSON encoding.

          Supported attributes for Unit are ApprovedBy, BridgeWorkerID, ChangeSetID, FromLinkID, HeadMutationNum, HeadRevisionNum, LastAppliedRevisionNum, LiveRevisionNum, OrganizationID, PreviousLiveRevisionNum, SpaceID, TargetID, UnitEventID, UpstreamSpaceID, UpstreamUnitID.

          The whole string must be query-encoded.
        in: query
        name: include
        required: false
        schema:
          type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                items:
                  $ref: '#/components/schemas/DeleteResponse'
                type: array
          description: OK
        "207":
          content:
            application/json:
              schema:
                items:
                  $ref: '#/components/schemas/DeleteResponse'
                type: array
          description: 'Multi-Status: Mixed success and failure results'
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Unit request is invalid (Bad Request).
        "401":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Unauthorized access.
        "403":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Forbidden access.
        "404":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Unit not found.
        "409":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Unit data conflict. Data has changed since last read.
        "422":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Unable to delete entities
        "423":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Unable to delete entities due to a inflight operation using
            one or more of the entities
        "500":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Something went wrong while processing Unit.
        default:
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Unexpected error.
      summary: Bulk delete multiple units
      tags:
      - Unit
    get:
      description: Retrieves a list of Units across spaces in the Organization
      operationId: ListAllUnits
      parameters:
      - description: |-
          The specified string is an expression for the purpose of filtering
          the list of Units returned. The expression syntax was inspired by SQL.
          It supports conjunctions using `AND` of relational expressions of the form *attribute*
          *operator* *attribute_or_literal*. The attribute names are case-sensitive and PascalCase,
          as in the JSON encoding.
          Strings support the following operators: `<`, `>`, `<=`, `>=`, `=`, `!=`, `LIKE`, `NOT LIKE`, `ILIKE`, `~~`, `!~~`, `~`, `~*`, `!~`, `!~*`, `IN`, `NOT IN`.
          String pattern operators: `LIKE` and `~~` for pattern matching with `%` and `_` wildcards,
          `ILIKE` for case-insensitive pattern matching, `NOT LIKE` and `!~~` for negated pattern matching.
          String regex operators: `~` for regex matching, `~*` for case-insensitive regex,
          `!~` and `!~*` for regex not matching (case-sensitive and insensitive).
          Integers support the following operators: `<`, `>`, `<=`, `>=`, `=`, `!=`, `IN`, `NOT IN`.
          UUIDs and boolean attributes support equality and inequality only.
          UUID and time literals must be quoted as string literals.
          String literals are quoted with single quotes, such as `'string'`.
          Time literals use the same form as when serialized as JSON,
          such as: `CreatedAt > '2025-02-18T23:16:34'`.
          Integer and boolean literals are also supported for attributes of those types.
          Arrays support the `?` operator to to match any element of the array,
          as in `ApprovedBy ? '7c61626f-ddbe-41af-93f6-b69f4ab6d308'`.
          Arrays can perform LEN() to check for length, as in `LEN(ApprovedBy) > 0`.
          Map support the dot notation to specify a particular map key, as in `Labels.tier = 'Backend'`.
          Maps support `IS NULL` and `IS NOT NULL` with dot notation to check for key absence or presence,
          as in `Labels.tier IS NULL` (key doesn't exist) or `Labels.tier IS NOT NULL` (key exists).
          Comparison results can be tested with `IS TRUE`, `IS FALSE`, `IS NOT TRUE`, and `IS NOT FALSE`.
          These are useful for nullable columns: `MergeSourceID = '<uuid>' IS NOT FALSE` matches rows where MergeSourceID equals the value OR is NULL.
          The `IN` and `NOT IN` operators accept a comma-separated list of values in parentheses,
          such as `Slug IN ('slugone', 'slugtwo')` or `Labels.environment IN ('prod', 'staging')`.
          Conjunctions are supported using the `AND` operator.
          An example conjunction is:
          `CreatedAt >= '2025-01-07' AND Slug = 'test' AND Labels.mykey = 'myvalue'`.

          Supported attributes for filtering on Unit: ApplyGates, ApplyWarnings, ApprovedBy, BridgeWorkerID, ChangeSetID, CreatedAt, DataHash, DeleteGates, DestroyGates, DisplayName, DriftReconciliationMode, FromLinkID, HeadRevisionNum, HeadUnitActionNum, HeadUnitEventNum, Labels, LastActionAt, LastAppliedRevisionNum, LastChangeDescription, LiveRevisionNum, OrganizationID, PreviousLiveRevisionNum, ProviderType, Slug, SpaceID, TargetID, TargetOptions, ToolchainType, UnitID, UpdatedAt, UpstreamOrganizationID, UpstreamRevisionNum, UpstreamSpaceID, UpstreamUnitID, Values.

          Finding all units created by cloning can be done using the expression `UpstreamRevisionNum > 0`. Clones of a specific unit can be found by additionally filtering based on `UpstreamUnitID`. Unapplied units can be found using `LiveRevisionNum = 0`. Units with unapplied changes can be found with `HeadRevisionNum > LiveRevisionNum`.

          The whole string must be query-encoded.
        in: query
        name: where
        required: false
        schema:
          type: string
      - description: |-
          UUID of a Filter entity to apply to the Unit list.

          The Filter must be in the same Organization as the user credentials.

          The Filter's From field must match the entity type being filtered (Unit).

          For Space-resident entities, if the Filter has a FromSpaceID, it must match the operation's SpaceID.

          The Filter's Where clause will be combined with any explicit 'where' parameter using AND logic.

          If both 'filter' and 'where' parameters are specified, they are combined with AND logic.
        in: query
        name: filter
        required: false
        schema:
          type: string
      - description: |-
          Free text search that approximately matches the specified string against string fields and map keys/values.

          The search is case-insensitive and uses pattern matching to find entities containing the text.

          Searchable string fields include attributes like Slug, DisplayName, and string-typed custom fields.

          For map fields (like Labels and Annotations), the search matches both map keys and values.

          The search uses OR logic across all searchable fields, so matching any field will return the entity.

          If both 'where' and 'contains' parameters are specified, they are combined with AND logic.

          Searchable fields for Unit include string and map-type attributes from the queryable attributes list.

          The whole string must be query-encoded.
        in: query
        name: contains
        required: false
        schema:
          type: string
      - description: |-
          Include clause for expanding related entities in the response for Unit.
          The attribute names are case-sensitive, PascalCase, and
          expected in a comma-separated list format as in the JSON encoding.

          Supported attributes for Unit are ApprovedBy, BridgeWorkerID, ChangeSetID, FromLinkID, HeadMutationNum, HeadRevisionNum, LastAppliedRevisionNum, LiveRevisionNum, OrganizationID, PreviousLiveRevisionNum, SpaceID, TargetID, UnitEventID, UpstreamSpaceID, UpstreamUnitID.

          The whole string must be query-encoded.
        in: query
        name: include
        required: false
        schema:
          type: string
      - description: |-
          Select clause for specifying which fields to include in the response for Unit.
          The attribute names are case-sensitive, PascalCase, and
          expected in a comma-separated list format as in the JSON encoding.
          If not specified, all fields are returned.
          Entity and parent IDs (like OrganizationID, SpaceID, UnitID) and Slug are always returned regardless of the select parameter.
          Fields used in where and contains filters are also automatically included.
          Example: 'DisplayName,CreatedAt,Labels' will return only those fields plus the required ID and Slug fields.
          The whole string must be query-encoded.
        in: query
        name: select
        required: false
        schema:
          type: string
      - description: 'Resource type: Resource type to match for the desired ToolchainType,
          for example apps/v1/Deployment'
        in: query
        name: resource_type
        required: false
        schema:
          type: string
      - description: 'Where data: The specified string is an expression for the purpose
          of evaluating whether the configuration data matches the filter. It supports
          conjunctions using `AND` of relational expressions of the form *path* *operator*
          *literal*. The path specifications are dot-separated, for both map fields
          and array indices, as in `spec.template.spec.containers.0.image = ''ghcr.io/headlamp-k8s/headlamp:latest''
          AND spec.replicas > 1`. Path expressions support `*` for wildcard array
          or map segments and `?key=value` syntax for associative matches of array
          elements containing objects with a `key` attribute. Strings support the
          following operators: `<`, `>`, `<=`, `>=`, `=`, `!=`, `LIKE`, `ILIKE`, `~~`,
          `!~~`, `~`, `!~`, `~*`, `!~*`, `IN`, `NOT IN`. String pattern operators:
          `LIKE` and `~~` for pattern matching with `%` and `_` wildcards, `ILIKE`
          for case-insensitive pattern matching, `!~~` for NOT LIKE. String regex
          operators: `~` for regex matching, `~*` for case-insensitive regex, `!~`
          and `!~*` for regex not matching (case-sensitive and insensitive). Integers
          support the following operators: `<`, `>`, `<=`, `>=`, `=`, `!=`, `IN`,
          `NOT IN`. Boolean values support equality and inequality only. The `IN`
          and `NOT IN` operators accept a comma-separated list of values in parentheses,
          such as `spec.template.spec.containers.0.image#reference IN ('':latest'',
          '':arm64-latest'')`. The syntax `.|` requires the preceding path to exist;
          otherwise the relation `!=` will always return true regardless what it is
          compared with. String literals are quoted with single quotes, such as `''string''`.
          Integer and boolean literals are also supported for attributes of those
          types. The whole string must be query-encoded.'
        in: query
        name: where_data
        required: false
        schema:
          type: string
      - description: 'Where expression to match Triggers. Matched triggers are invoked
          on each unit to filter by validation results. Use with triggers_passed to
          control whether passing or failing units are returned (default: failing).'
        in: query
        name: where_trigger
        required: false
        schema:
          type: string
      - description: Filter UUID (with From=Trigger). The filter's matching triggers
          are invoked on units to filter by validation results. Can be combined with
          where_trigger.
        in: query
        name: trigger_filter
        required: false
        schema:
          type: string
      - description: When true, return units that pass trigger validation; when false
          (default), return units that fail. Only applies when where_trigger or trigger_filter
          is specified.
        in: query
        name: triggers_passed
        required: false
        schema:
          type: boolean
      - description: View slug or UUID. Applies the View's column definitions to extract
          values for each unit. If the View has a FilterID, its filter is ANDed with
          other filters. The View must have Of=Unit or a Filter with From=Unit.
        in: query
        name: view
        required: false
        schema:
          type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                items:
                  $ref: '#/components/schemas/ExtendedUnit'
                type: array
          description: OK
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Unit request is invalid (Bad Request).
        "401":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Unauthorized access.
        "403":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Forbidden access.
        "404":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Unit not found.
        "500":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Something went wrong while processing Unit.
        default:
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Unexpected error.
      summary: List of Units across spaces
      tags:
      - Unit
    patch:
      description: Apply JSON merge patch to multiple units selected by query parameters
      operationId: BulkPatchUnits
      parameters:
      - description: |-
          The specified string is an expression for the purpose of filtering
          the list of Units returned. The expression syntax was inspired by SQL.
          It supports conjunctions using `AND` of relational expressions of the form *attribute*
          *operator* *attribute_or_literal*. The attribute names are case-sensitive and PascalCase,
          as in the JSON encoding.
          Strings support the following operators: `<`, `>`, `<=`, `>=`, `=`, `!=`, `LIKE`, `NOT LIKE`, `ILIKE`, `~~`, `!~~`, `~`, `~*`, `!~`, `!~*`, `IN`, `NOT IN`.
          String pattern operators: `LIKE` and `~~` for pattern matching with `%` and `_` wildcards,
          `ILIKE` for case-insensitive pattern matching, `NOT LIKE` and `!~~` for negated pattern matching.
          String regex operators: `~` for regex matching, `~*` for case-insensitive regex,
          `!~` and `!~*` for regex not matching (case-sensitive and insensitive).
          Integers support the following operators: `<`, `>`, `<=`, `>=`, `=`, `!=`, `IN`, `NOT IN`.
          UUIDs and boolean attributes support equality and inequality only.
          UUID and time literals must be quoted as string literals.
          String literals are quoted with single quotes, such as `'string'`.
          Time literals use the same form as when serialized as JSON,
          such as: `CreatedAt > '2025-02-18T23:16:34'`.
          Integer and boolean literals are also supported for attributes of those types.
          Arrays support the `?` operator to to match any element of the array,
          as in `ApprovedBy ? '7c61626f-ddbe-41af-93f6-b69f4ab6d308'`.
          Arrays can perform LEN() to check for length, as in `LEN(ApprovedBy) > 0`.
          Map support the dot notation to specify a particular map key, as in `Labels.tier = 'Backend'`.
          Maps support `IS NULL` and `IS NOT NULL` with dot notation to check for key absence or presence,
          as in `Labels.tier IS NULL` (key doesn't exist) or `Labels.tier IS NOT NULL` (key exists).
          Comparison results can be tested with `IS TRUE`, `IS FALSE`, `IS NOT TRUE`, and `IS NOT FALSE`.
          These are useful for nullable columns: `MergeSourceID = '<uuid>' IS NOT FALSE` matches rows where MergeSourceID equals the value OR is NULL.
          The `IN` and `NOT IN` operators accept a comma-separated list of values in parentheses,
          such as `Slug IN ('slugone', 'slugtwo')` or `Labels.environment IN ('prod', 'staging')`.
          Conjunctions are supported using the `AND` operator.
          An example conjunction is:
          `CreatedAt >= '2025-01-07' AND Slug = 'test' AND Labels.mykey = 'myvalue'`.

          Supported attributes for filtering on Unit: ApplyGates, ApplyWarnings, ApprovedBy, BridgeWorkerID, ChangeSetID, CreatedAt, DataHash, DeleteGates, DestroyGates, DisplayName, DriftReconciliationMode, FromLinkID, HeadRevisionNum, HeadUnitActionNum, HeadUnitEventNum, Labels, LastActionAt, LastAppliedRevisionNum, LastChangeDescription, LiveRevisionNum, OrganizationID, PreviousLiveRevisionNum, ProviderType, Slug, SpaceID, TargetID, TargetOptions, ToolchainType, UnitID, UpdatedAt, UpstreamOrganizationID, UpstreamRevisionNum, UpstreamSpaceID, UpstreamUnitID, Values.

          Finding all units created by cloning can be done using the expression `UpstreamRevisionNum > 0`. Clones of a specific unit can be found by additionally filtering based on `UpstreamUnitID`. Unapplied units can be found using `LiveRevisionNum = 0`. Units with unapplied changes can be found with `HeadRevisionNum > LiveRevisionNum`.

          The whole string must be query-encoded.
        in: query
        name: where
        required: false
        schema:
          type: string
      - description: |-
          UUID of a Filter entity to apply to the Unit list.

          The Filter must be in the same Organization as the user credentials.

          The Filter's From field must match the entity type being filtered (Unit).

          For Space-resident entities, if the Filter has a FromSpaceID, it must match the operation's SpaceID.

          The Filter's Where clause will be combined with any explicit 'where' parameter using AND logic.

          If both 'filter' and 'where' parameters are specified, they are combined with AND logic.
        in: query
        name: filter
        required: false
        schema:
          type: string
      - description: |-
          Free text search that approximately matches the specified string against string fields and map keys/values.

          The search is case-insensitive and uses pattern matching to find entities containing the text.

          Searchable string fields include attributes like Slug, DisplayName, and string-typed custom fields.

          For map fields (like Labels and Annotations), the search matches both map keys and values.

          The search uses OR logic across all searchable fields, so matching any field will return the entity.

          If both 'where' and 'contains' parameters are specified, they are combined with AND logic.

          Searchable fields for Unit include string and map-type attributes from the queryable attributes list.

          The whole string must be query-encoded.
        in: query
        name: contains
        required: false
        schema:
          type: string
      - description: |-
          Include clause for expanding related entities in the response for Unit.
          The attribute names are case-sensitive, PascalCase, and
          expected in a comma-separated list format as in the JSON encoding.

          Supported attributes for Unit are ApprovedBy, BridgeWorkerID, ChangeSetID, FromLinkID, HeadMutationNum, HeadRevisionNum, LastAppliedRevisionNum, LiveRevisionNum, OrganizationID, PreviousLiveRevisionNum, SpaceID, TargetID, UnitEventID, UpstreamSpaceID, UpstreamUnitID.

          The whole string must be query-encoded.
        in: query
        name: include
        required: false
        schema:
          type: string
      - description: 'Dry run mode: return changed unit(s) but don''t update configuration
          data'
        in: query
        name: dry_run
        required: false
        schema:
          type: boolean
      - description: Upgrade the unit to the latest version of its upstream unit
        in: query
        name: upgrade
        required: false
        schema:
          type: boolean
      - description: 'Restore revision source. Supports: Named revisions (''LiveRevisionNum'',
          ''LastAppliedRevisionNum'', ''PreviousLiveRevisionNum'', ''HeadRevisionNum''),
          direct revision number (e.g., ''42''), or entity references (''Tag:uuid'',
          ''ChangeSet:uuid'', ''Revision:uuid''). Can be prefixed with ''Before:''
          to select the revision immediately before the specified one (e.g., ''Before:LiveRevisionNum'',
          ''Before:42''). When using Tag or ChangeSet references, the latest revision
          associated with that entity is selected.'
        in: query
        name: restore
        required: false
        schema:
          type: string
      - description: Resolve specified non-automatically resolved link from this (downstream)
          Unit to another (upstream) Unit. Expects Link:uuid or Link:*.
        in: query
        name: resolve
        required: false
        schema:
          type: string
      - description: Merge source unit. Currently it must be a unit ID or 'Self'.
        in: query
        name: merge_source
        required: false
        schema:
          type: string
      - description: 'Merge base revision, which provides the base configuration data
          of the changes to merge. With merge_source, this is a revision of the merge
          source unit. With merge_external_source, this is a revision of the unit
          being updated and overrides the default selection of the latest MergeExternal
          revision. Supports: Named revisions (''LiveRevisionNum'', ''LastAppliedRevisionNum'',
          ''PreviousLiveRevisionNum'', ''HeadRevisionNum''), direct revision number
          (e.g., ''42''), or entity references (''Tag:uuid'', ''ChangeSet:uuid'',
          ''Revision:uuid''). Can be prefixed with ''Before:'' to select the revision
          immediately before the specified one (e.g., ''Before:LiveRevisionNum'',
          ''Before:42''). When using Tag or ChangeSet references, the latest revision
          associated with that entity is selected.'
        in: query
        name: merge_base
        required: false
        schema:
          type: string
      - description: 'Merge end revision of the merge source, which provides the final
          configuration of the changes to merge. Supports: Named revisions (''LiveRevisionNum'',
          ''LastAppliedRevisionNum'', ''PreviousLiveRevisionNum'', ''HeadRevisionNum''),
          direct revision number (e.g., ''42''), or entity references (''Tag:uuid'',
          ''ChangeSet:uuid'', ''Revision:uuid''). Can be prefixed with ''Before:''
          to select the revision immediately before the specified one (e.g., ''Before:LiveRevisionNum'',
          ''Before:42''). When using Tag or ChangeSet references, the latest revision
          associated with that entity is selected.'
        in: query
        name: merge_end
        required: false
        schema:
          type: string
      - description: Identifier of the external source for merge-on-update. When set,
          computes mutations between the last MergeExternal revision and the provided
          data, then patches the current unit data with those mutations.
        in: query
        name: merge_external_source
        required: false
        schema:
          type: string
      - description: |-
          The specified string is an expression for the purpose of filtering
          the list of Mutations returned. The expression syntax was inspired by SQL.
          It supports conjunctions using `AND` of relational expressions of the form *attribute*
          *operator* *attribute_or_literal*. The attribute names are case-sensitive and PascalCase,
          as in the JSON encoding.
          Strings support the following operators: `<`, `>`, `<=`, `>=`, `=`, `!=`, `LIKE`, `NOT LIKE`, `ILIKE`, `~~`, `!~~`, `~`, `~*`, `!~`, `!~*`, `IN`, `NOT IN`.
          String pattern operators: `LIKE` and `~~` for pattern matching with `%` and `_` wildcards,
          `ILIKE` for case-insensitive pattern matching, `NOT LIKE` and `!~~` for negated pattern matching.
          String regex operators: `~` for regex matching, `~*` for case-insensitive regex,
          `!~` and `!~*` for regex not matching (case-sensitive and insensitive).
          Integers support the following operators: `<`, `>`, `<=`, `>=`, `=`, `!=`, `IN`, `NOT IN`.
          UUIDs and boolean attributes support equality and inequality only.
          UUID and time literals must be quoted as string literals.
          String literals are quoted with single quotes, such as `'string'`.
          Time literals use the same form as when serialized as JSON,
          such as: `CreatedAt > '2025-02-18T23:16:34'`.
          Integer and boolean literals are also supported for attributes of those types.
          Arrays support the `?` operator to to match any element of the array,
          as in `ApprovedBy ? '7c61626f-ddbe-41af-93f6-b69f4ab6d308'`.
          Arrays can perform LEN() to check for length, as in `LEN(ApprovedBy) > 0`.
          Map support the dot notation to specify a particular map key, as in `Labels.tier = 'Backend'`.
          Maps support `IS NULL` and `IS NOT NULL` with dot notation to check for key absence or presence,
          as in `Labels.tier IS NULL` (key doesn't exist) or `Labels.tier IS NOT NULL` (key exists).
          Comparison results can be tested with `IS TRUE`, `IS FALSE`, `IS NOT TRUE`, and `IS NOT FALSE`.
          These are useful for nullable columns: `MergeSourceID = '<uuid>' IS NOT FALSE` matches rows where MergeSourceID equals the value OR is NULL.
          The `IN` and `NOT IN` operators accept a comma-separated list of values in parentheses,
          such as `Slug IN ('slugone', 'slugtwo')` or `Labels.environment IN ('prod', 'staging')`.
          Conjunctions are supported using the `AND` operator.
          An example conjunction is:
          `CreatedAt >= '2025-01-07' AND Slug = 'test' AND Labels.mykey = 'myvalue'`.

          Supported attributes for filtering on Mutation: CreatedAt, FunctionName, InvocationID, LinkID, MergeBaseRevisionNum, MergeEndRevisionNum, MergeSourceID, MutationID, MutationNum, OrganizationID, RestoredRevisionNum, RevisionID, RevisionNum, SpaceID, Subgroup, TriggerID, UnitID, UpdatedAt, UpgradedFromUpstreamRevisionNum.

          Used to filter which mutations are affected during merge operations.

          The whole string must be query-encoded.
        in: query
        name: where_mutation
        required: false
        schema:
          type: string
      - description: |-
          UUID of a Filter entity to apply to the Mutation list.

          The Filter must be in the same Organization as the user credentials.

          The Filter's From field must match the entity type being filtered (Mutation).

          For Space-resident entities, if the Filter has a FromSpaceID, it must match the operation's SpaceID.

          The Filter's Where clause will be combined with any explicit 'where' parameter using AND logic.

          If both 'filter' and 'where' parameters are specified, they are combined with AND logic.
        in: query
        name: filter_mutation
        required: false
        schema:
          type: string
      - description: Tag ID to add to the head revision
        in: query
        name: tag
        required: false
        schema:
          format: uuid
          type: string
      - description: Must match ChangeSetID of affected Units if config Data is changed
          unless in dry run mode
        in: query
        name: change_set_id
        required: false
        schema:
          format: uuid
          type: string
      - description: User-defined category for the Mutation. Must be alphanumeric,
          at most 64 characters. The prefix 'ConfigHub' is reserved.
        in: query
        name: subgroup
        required: false
        schema:
          type: string
      requestBody:
        content:
          application/merge-patch+json:
            schema:
              description: JSON Merge Patch schema for Unit. All fields are optional
                and nullable. Readonly fields are ignored.
              properties:
                Annotations:
                  additionalProperties:
                    nullable: true
                    type: string
                  description: An optional map of Annotation key/value pairs for tools
                    to attach information to entities.
                  nullable: true
                  type: object
                ChangeSetID:
                  description: Unique identifier for the ChangeSet to which the current
                    Revision belongs. Optional. Units are not required to belong to
                    ChangeSets.
                  format: uuid
                  nullable: true
                  type: string
                Data:
                  description: The full configuration data for this unit.
                  format: byte
                  nullable: true
                  type: string
                DeleteGates:
                  additionalProperties:
                    nullable: true
                    type: boolean
                  description: An optional set of gates that, if any is present, will
                    block deletion
                  nullable: true
                  type: object
                DestroyGates:
                  additionalProperties:
                    nullable: true
                    type: boolean
                  description: An optional set of gates that, if any is present, will
                    block destroy operations
                  nullable: true
                  type: object
                DisplayName:
                  description: Friendly name for the entity.
                  nullable: true
                  type: string
                Labels:
                  additionalProperties:
                    nullable: true
                    type: string
                  description: An optional map of Label key/value pairs to specify
                    identifying attributes of entities for the purpose of grouping
                    and filtering them.
                  nullable: true
                  type: object
                LastChangeDescription:
                  description: LastChangeDescription is a human-readable description
                    of the last change. This description is copied to the new Revision
                    when the Data is changed.
                  nullable: true
                  type: string
                ProviderType:
                  description: ""
                  nullable: true
                  type: string
                Slug:
                  description: Unique URL-safe identifier for the entity.
                  nullable: true
                  type: string
                TargetID:
                  description: TargetID is the identifier of the target this unit
                    is associated with. This defines where the configuration will
                    be applied. It must be set to a valid Target within the same Space
                    before the Unit can be Applied, Destroyed, Imported, or Refreshed.
                  format: uuid
                  nullable: true
                  type: string
                TargetOptions:
                  additionalProperties:
                    nullable: true
                    type: string
                  description: Bridge option values set per-Unit, merged with the
                    Target's Options when sending to the bridge worker (Target's Options
                    take precedence on overlap). The options must be predefined by
                    the ConfigType in the BridgeWorker.
                  nullable: true
                  type: object
                ToolchainType:
                  description: ToolchainType specifies the type of toolchain for this
                    unit. Possible values include "Kubernetes/YAML", "OpenTofu/HCL",
                    "AppConfig/Properties", "AppConfig/YAML", "AppConfig/TOML", "AppConfig/INI",
                    "AppConfig/JSON", "AppConfig/Env", "AppConfig/Text", "ConfigHub/YAML".
                  nullable: true
                  type: string
                Version:
                  description: An entity-specific sequence number used for optimistic
                    concurrency control. The value read must be sent in calls to Update.
                  nullable: true
                  type: integer
              type: object
      responses:
        "200":
          content:
            application/json:
              schema:
                items:
                  $ref: '#/components/schemas/UnitCreateOrUpdateResponse'
                type: array
          description: OK
        "207":
          content:
            application/json:
              schema:
                items:
                  $ref: '#/components/schemas/UnitCreateOrUpdateResponse'
                type: array
          description: 'Multi-Status: Mixed success and failure results'
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Unit request is invalid (Bad Request).
        "401":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Unauthorized access.
        "403":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Forbidden access.
        "404":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Unit not found.
        "409":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Unit data conflict. Data has changed since last read.
        "422":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Unable to patch unit(s) due to configuration data problem
        "500":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Something went wrong while processing Unit.
        default:
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Unexpected error.
      summary: Bulk patch multiple units
      tags:
      - Unit
    post:
      description: Clone multiple units selected by query parameters with optional
        name prefixes and destination spaces
      operationId: BulkCreateUnits
      parameters:
      - description: |-
          The specified string is an expression for the purpose of filtering
          the list of Units returned. The expression syntax was inspired by SQL.
          It supports conjunctions using `AND` of relational expressions of the form *attribute*
          *operator* *attribute_or_literal*. The attribute names are case-sensitive and PascalCase,
          as in the JSON encoding.
          Strings support the following operators: `<`, `>`, `<=`, `>=`, `=`, `!=`, `LIKE`, `NOT LIKE`, `ILIKE`, `~~`, `!~~`, `~`, `~*`, `!~`, `!~*`, `IN`, `NOT IN`.
          String pattern operators: `LIKE` and `~~` for pattern matching with `%` and `_` wildcards,
          `ILIKE` for case-insensitive pattern matching, `NOT LIKE` and `!~~` for negated pattern matching.
          String regex operators: `~` for regex matching, `~*` for case-insensitive regex,
          `!~` and `!~*` for regex not matching (case-sensitive and insensitive).
          Integers support the following operators: `<`, `>`, `<=`, `>=`, `=`, `!=`, `IN`, `NOT IN`.
          UUIDs and boolean attributes support equality and inequality only.
          UUID and time literals must be quoted as string literals.
          String literals are quoted with single quotes, such as `'string'`.
          Time literals use the same form as when serialized as JSON,
          such as: `CreatedAt > '2025-02-18T23:16:34'`.
          Integer and boolean literals are also supported for attributes of those types.
          Arrays support the `?` operator to to match any element of the array,
          as in `ApprovedBy ? '7c61626f-ddbe-41af-93f6-b69f4ab6d308'`.
          Arrays can perform LEN() to check for length, as in `LEN(ApprovedBy) > 0`.
          Map support the dot notation to specify a particular map key, as in `Labels.tier = 'Backend'`.
          Maps support `IS NULL` and `IS NOT NULL` with dot notation to check for key absence or presence,
          as in `Labels.tier IS NULL` (key doesn't exist) or `Labels.tier IS NOT NULL` (key exists).
          Comparison results can be tested with `IS TRUE`, `IS FALSE`, `IS NOT TRUE`, and `IS NOT FALSE`.
          These are useful for nullable columns: `MergeSourceID = '<uuid>' IS NOT FALSE` matches rows where MergeSourceID equals the value OR is NULL.
          The `IN` and `NOT IN` operators accept a comma-separated list of values in parentheses,
          such as `Slug IN ('slugone', 'slugtwo')` or `Labels.environment IN ('prod', 'staging')`.
          Conjunctions are supported using the `AND` operator.
          An example conjunction is:
          `CreatedAt >= '2025-01-07' AND Slug = 'test' AND Labels.mykey = 'myvalue'`.

          Supported attributes for filtering on Unit: ApplyGates, ApplyWarnings, ApprovedBy, BridgeWorkerID, ChangeSetID, CreatedAt, DataHash, DeleteGates, DestroyGates, DisplayName, DriftReconciliationMode, FromLinkID, HeadRevisionNum, HeadUnitActionNum, HeadUnitEventNum, Labels, LastActionAt, LastAppliedRevisionNum, LastChangeDescription, LiveRevisionNum, OrganizationID, PreviousLiveRevisionNum, ProviderType, Slug, SpaceID, TargetID, TargetOptions, ToolchainType, UnitID, UpdatedAt, UpstreamOrganizationID, UpstreamRevisionNum, UpstreamSpaceID, UpstreamUnitID, Values.

          Finding all units created by cloning can be done using the expression `UpstreamRevisionNum > 0`. Clones of a specific unit can be found by additionally filtering based on `UpstreamUnitID`. Unapplied units can be found using `LiveRevisionNum = 0`. Units with unapplied changes can be found with `HeadRevisionNum > LiveRevisionNum`.

          The whole string must be query-encoded.
        in: query
        name: where
        required: false
        schema:
          type: string
      - description: |-
          UUID of a Filter entity to apply to the Unit list.

          The Filter must be in the same Organization as the user credentials.

          The Filter's From field must match the entity type being filtered (Unit).

          For Space-resident entities, if the Filter has a FromSpaceID, it must match the operation's SpaceID.

          The Filter's Where clause will be combined with any explicit 'where' parameter using AND logic.

          If both 'filter' and 'where' parameters are specified, they are combined with AND logic.
        in: query
        name: filter
        required: false
        schema:
          type: string
      - description: |-
          Free text search that approximately matches the specified string against string fields and map keys/values.

          The search is case-insensitive and uses pattern matching to find entities containing the text.

          Searchable string fields include attributes like Slug, DisplayName, and string-typed custom fields.

          For map fields (like Labels and Annotations), the search matches both map keys and values.

          The search uses OR logic across all searchable fields, so matching any field will return the entity.

          If both 'where' and 'contains' parameters are specified, they are combined with AND logic.

          Searchable fields for Unit include string and map-type attributes from the queryable attributes list.

          The whole string must be query-encoded.
        in: query
        name: contains
        required: false
        schema:
          type: string
      - description: |-
          Include clause for expanding related entities in the response for Unit.
          The attribute names are case-sensitive, PascalCase, and
          expected in a comma-separated list format as in the JSON encoding.

          Supported attributes for Unit are ApprovedBy, BridgeWorkerID, ChangeSetID, FromLinkID, HeadMutationNum, HeadRevisionNum, LastAppliedRevisionNum, LiveRevisionNum, OrganizationID, PreviousLiveRevisionNum, SpaceID, TargetID, UnitEventID, UpstreamSpaceID, UpstreamUnitID.

          The whole string must be query-encoded.
        in: query
        name: include
        required: false
        schema:
          type: string
      - description: Comma-separated list of prefixes to apply to cloned Unit names
        in: query
        name: name_prefixes
        required: false
        schema:
          type: string
      - description: Comma-separated list of labels with multiple values for cloned
          Unit labels, in the format of key1=value1|value2,key2=value1|value2|value3
        in: query
        name: variant_labels
        required: false
        schema:
          type: string
      - description: 'A string for clone names, use the prefix ''template:'' for a
          Go-template with .SourceEntitySlug to access the original entity''s slug
          and .Labels to access variant labels, example: ''template:{{.SourceEntitySlug}}-{{.Labels.env}}'''
        in: query
        name: name_pattern
        required: false
        schema:
          type: string
      - description: |-
          The specified string is an expression for the purpose of filtering
          the list of Spaces returned. The expression syntax was inspired by SQL.
          It supports conjunctions using `AND` of relational expressions of the form *attribute*
          *operator* *attribute_or_literal*. The attribute names are case-sensitive and PascalCase,
          as in the JSON encoding.
          Strings support the following operators: `<`, `>`, `<=`, `>=`, `=`, `!=`, `LIKE`, `NOT LIKE`, `ILIKE`, `~~`, `!~~`, `~`, `~*`, `!~`, `!~*`, `IN`, `NOT IN`.
          String pattern operators: `LIKE` and `~~` for pattern matching with `%` and `_` wildcards,
          `ILIKE` for case-insensitive pattern matching, `NOT LIKE` and `!~~` for negated pattern matching.
          String regex operators: `~` for regex matching, `~*` for case-insensitive regex,
          `!~` and `!~*` for regex not matching (case-sensitive and insensitive).
          Integers support the following operators: `<`, `>`, `<=`, `>=`, `=`, `!=`, `IN`, `NOT IN`.
          UUIDs and boolean attributes support equality and inequality only.
          UUID and time literals must be quoted as string literals.
          String literals are quoted with single quotes, such as `'string'`.
          Time literals use the same form as when serialized as JSON,
          such as: `CreatedAt > '2025-02-18T23:16:34'`.
          Integer and boolean literals are also supported for attributes of those types.
          Arrays support the `?` operator to to match any element of the array,
          as in `ApprovedBy ? '7c61626f-ddbe-41af-93f6-b69f4ab6d308'`.
          Arrays can perform LEN() to check for length, as in `LEN(ApprovedBy) > 0`.
          Map support the dot notation to specify a particular map key, as in `Labels.tier = 'Backend'`.
          Maps support `IS NULL` and `IS NOT NULL` with dot notation to check for key absence or presence,
          as in `Labels.tier IS NULL` (key doesn't exist) or `Labels.tier IS NOT NULL` (key exists).
          Comparison results can be tested with `IS TRUE`, `IS FALSE`, `IS NOT TRUE`, and `IS NOT FALSE`.
          These are useful for nullable columns: `MergeSourceID = '<uuid>' IS NOT FALSE` matches rows where MergeSourceID equals the value OR is NULL.
          The `IN` and `NOT IN` operators accept a comma-separated list of values in parentheses,
          such as `Slug IN ('slugone', 'slugtwo')` or `Labels.environment IN ('prod', 'staging')`.
          Conjunctions are supported using the `AND` operator.
          An example conjunction is:
          `CreatedAt >= '2025-01-07' AND Slug = 'test' AND Labels.mykey = 'myvalue'`.

          Supported attributes for filtering on Space: AttributeFilterID, AttributeHash, AttributeIDs, CreatedAt, DeleteGates, DisplayName, Labels, OrganizationID, Permissions, Slug, SpaceID, TriggerFilterID, TriggerHash, TriggerIDs, UpdatedAt.

          Where expression to select destination spaces for cloning units

          The whole string must be query-encoded.
        in: query
        name: where_space
        required: false
        schema:
          type: string
      - description: |-
          UUID of a Filter entity to apply to the Space list.

          The Filter must be in the same Organization as the user credentials.

          The Filter's From field must match the entity type being filtered (Space).

          For Space-resident entities, if the Filter has a FromSpaceID, it must match the operation's SpaceID.

          The Filter's Where clause will be combined with any explicit 'where' parameter using AND logic.

          If both 'filter' and 'where' parameters are specified, they are combined with AND logic.
        in: query
        name: filter_space
        required: false
        schema:
          type: string
      - description: Allowed values are true and false. Default is false. When true,
          reports success when an entity already exists and returns the existing entity
        in: query
        name: allow_exists
        required: false
        schema:
          type: string
      - description: |-
          The specified string is an expression for the purpose of filtering
          the list of Links returned. The expression syntax was inspired by SQL.
          It supports conjunctions using `AND` of relational expressions of the form *attribute*
          *operator* *attribute_or_literal*. The attribute names are case-sensitive and PascalCase,
          as in the JSON encoding.
          Strings support the following operators: `<`, `>`, `<=`, `>=`, `=`, `!=`, `LIKE`, `NOT LIKE`, `ILIKE`, `~~`, `!~~`, `~`, `~*`, `!~`, `!~*`, `IN`, `NOT IN`.
          String pattern operators: `LIKE` and `~~` for pattern matching with `%` and `_` wildcards,
          `ILIKE` for case-insensitive pattern matching, `NOT LIKE` and `!~~` for negated pattern matching.
          String regex operators: `~` for regex matching, `~*` for case-insensitive regex,
          `!~` and `!~*` for regex not matching (case-sensitive and insensitive).
          Integers support the following operators: `<`, `>`, `<=`, `>=`, `=`, `!=`, `IN`, `NOT IN`.
          UUIDs and boolean attributes support equality and inequality only.
          UUID and time literals must be quoted as string literals.
          String literals are quoted with single quotes, such as `'string'`.
          Time literals use the same form as when serialized as JSON,
          such as: `CreatedAt > '2025-02-18T23:16:34'`.
          Integer and boolean literals are also supported for attributes of those types.
          Arrays support the `?` operator to to match any element of the array,
          as in `ApprovedBy ? '7c61626f-ddbe-41af-93f6-b69f4ab6d308'`.
          Arrays can perform LEN() to check for length, as in `LEN(ApprovedBy) > 0`.
          Map support the dot notation to specify a particular map key, as in `Labels.tier = 'Backend'`.
          Maps support `IS NULL` and `IS NOT NULL` with dot notation to check for key absence or presence,
          as in `Labels.tier IS NULL` (key doesn't exist) or `Labels.tier IS NOT NULL` (key exists).
          Comparison results can be tested with `IS TRUE`, `IS FALSE`, `IS NOT TRUE`, and `IS NOT FALSE`.
          These are useful for nullable columns: `MergeSourceID = '<uuid>' IS NOT FALSE` matches rows where MergeSourceID equals the value OR is NULL.
          The `IN` and `NOT IN` operators accept a comma-separated list of values in parentheses,
          such as `Slug IN ('slugone', 'slugtwo')` or `Labels.environment IN ('prod', 'staging')`.
          Conjunctions are supported using the `AND` operator.
          An example conjunction is:
          `CreatedAt >= '2025-01-07' AND Slug = 'test' AND Labels.mykey = 'myvalue'`.

          Supported attributes for filtering on Link: AutoUpdate, CreatedAt, DeleteGates, DisplayName, DownstreamLastMergedRevisionNum, FromUnitID, Labels, LinkID, OrganizationID, Slug, SpaceID, ToSpaceID, ToUnitID, UpdateType, UpdatedAt, UpstreamLastMergedRevisionNum, UpstreamLinkID, UpstreamOrganizationID, UpstreamSpaceID, UseLiveState.

          Where expression to filter outgoing links (links to units outside the cloned set) for copying. If non-empty, matching outgoing links are also copied with FromUnitID retargeted to the cloned unit.

          The whole string must be query-encoded.
        in: query
        name: include_outgoing_links_where
        required: false
        schema:
          type: string
      requestBody:
        content:
          application/merge-patch+json:
            schema:
              description: JSON Merge Patch schema for Unit. All fields are optional
                and nullable. Readonly fields are ignored.
              properties:
                Annotations:
                  additionalProperties:
                    nullable: true
                    type: string
                  description: An optional map of Annotation key/value pairs for tools
                    to attach information to entities.
                  nullable: true
                  type: object
                ChangeSetID:
                  description: Unique identifier for the ChangeSet to which the current
                    Revision belongs. Optional. Units are not required to belong to
                    ChangeSets.
                  format: uuid
                  nullable: true
                  type: string
                Data:
                  description: The full configuration data for this unit.
                  format: byte
                  nullable: true
                  type: string
                DeleteGates:
                  additionalProperties:
                    nullable: true
                    type: boolean
                  description: An optional set of gates that, if any is present, will
                    block deletion
                  nullable: true
                  type: object
                DestroyGates:
                  additionalProperties:
                    nullable: true
                    type: boolean
                  description: An optional set of gates that, if any is present, will
                    block destroy operations
                  nullable: true
                  type: object
                DisplayName:
                  description: Friendly name for the entity.
                  nullable: true
                  type: string
                Labels:
                  additionalProperties:
                    nullable: true
                    type: string
                  description: An optional map of Label key/value pairs to specify
                    identifying attributes of entities for the purpose of grouping
                    and filtering them.
                  nullable: true
                  type: object
                LastChangeDescription:
                  description: LastChangeDescription is a human-readable description
                    of the last change. This description is copied to the new Revision
                    when the Data is changed.
                  nullable: true
                  type: string
                ProviderType:
                  description: ""
                  nullable: true
                  type: string
                Slug:
                  description: Unique URL-safe identifier for the entity.
                  nullable: true
                  type: string
                TargetID:
                  description: TargetID is the identifier of the target this unit
                    is associated with. This defines where the configuration will
                    be applied. It must be set to a valid Target within the same Space
                    before the Unit can be Applied, Destroyed, Imported, or Refreshed.
                  format: uuid
                  nullable: true
                  type: string
                TargetOptions:
                  additionalProperties:
                    nullable: true
                    type: string
                  description: Bridge option values set per-Unit, merged with the
                    Target's Options when sending to the bridge worker (Target's Options
                    take precedence on overlap). The options must be predefined by
                    the ConfigType in the BridgeWorker.
                  nullable: true
                  type: object
                ToolchainType:
                  description: ToolchainType specifies the type of toolchain for this
                    unit. Possible values include "Kubernetes/YAML", "OpenTofu/HCL",
                    "AppConfig/Properties", "AppConfig/YAML", "AppConfig/TOML", "AppConfig/INI",
                    "AppConfig/JSON", "AppConfig/Env", "AppConfig/Text", "ConfigHub/YAML".
                  nullable: true
                  type: string
                Version:
                  description: An entity-specific sequence number used for optimistic
                    concurrency control. The value read must be sent in calls to Update.
                  nullable: true
                  type: integer
              type: object
      responses:
        "200":
          content:
            application/json:
              schema:
                items:
                  $ref: '#/components/schemas/UnitCreateOrUpdateResponse'
                type: array
          description: OK
        "207":
          content:
            application/json:
              schema:
                items:
                  $ref: '#/components/schemas/UnitCreateOrUpdateResponse'
                type: array
          description: Multi-Status (partial success)
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Unit request is invalid (Bad Request).
        "401":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Unauthorized access.
        "403":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Forbidden access.
        "404":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Unit not found.
        "409":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Unit data conflict. Data has changed since last read.
        "422":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Unable to create unit(s) due to configuration data problem
        "500":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Something went wrong while processing Unit.
        default:
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Unexpected error.
      summary: Bulk create (clone) multiple units
      tags:
      - Unit
  /unit/apply:
    post:
      description: Executes apply operations for multiple units based on filter criteria
        across spaces
      operationId: BulkApplyUnits
      parameters:
      - description: |-
          The specified string is an expression for the purpose of filtering
          the list of Units returned. The expression syntax was inspired by SQL.
          It supports conjunctions using `AND` of relational expressions of the form *attribute*
          *operator* *attribute_or_literal*. The attribute names are case-sensitive and PascalCase,
          as in the JSON encoding.
          Strings support the following operators: `<`, `>`, `<=`, `>=`, `=`, `!=`, `LIKE`, `NOT LIKE`, `ILIKE`, `~~`, `!~~`, `~`, `~*`, `!~`, `!~*`, `IN`, `NOT IN`.
          String pattern operators: `LIKE` and `~~` for pattern matching with `%` and `_` wildcards,
          `ILIKE` for case-insensitive pattern matching, `NOT LIKE` and `!~~` for negated pattern matching.
          String regex operators: `~` for regex matching, `~*` for case-insensitive regex,
          `!~` and `!~*` for regex not matching (case-sensitive and insensitive).
          Integers support the following operators: `<`, `>`, `<=`, `>=`, `=`, `!=`, `IN`, `NOT IN`.
          UUIDs and boolean attributes support equality and inequality only.
          UUID and time literals must be quoted as string literals.
          String literals are quoted with single quotes, such as `'string'`.
          Time literals use the same form as when serialized as JSON,
          such as: `CreatedAt > '2025-02-18T23:16:34'`.
          Integer and boolean literals are also supported for attributes of those types.
          Arrays support the `?` operator to to match any element of the array,
          as in `ApprovedBy ? '7c61626f-ddbe-41af-93f6-b69f4ab6d308'`.
          Arrays can perform LEN() to check for length, as in `LEN(ApprovedBy) > 0`.
          Map support the dot notation to specify a particular map key, as in `Labels.tier = 'Backend'`.
          Maps support `IS NULL` and `IS NOT NULL` with dot notation to check for key absence or presence,
          as in `Labels.tier IS NULL` (key doesn't exist) or `Labels.tier IS NOT NULL` (key exists).
          Comparison results can be tested with `IS TRUE`, `IS FALSE`, `IS NOT TRUE`, and `IS NOT FALSE`.
          These are useful for nullable columns: `MergeSourceID = '<uuid>' IS NOT FALSE` matches rows where MergeSourceID equals the value OR is NULL.
          The `IN` and `NOT IN` operators accept a comma-separated list of values in parentheses,
          such as `Slug IN ('slugone', 'slugtwo')` or `Labels.environment IN ('prod', 'staging')`.
          Conjunctions are supported using the `AND` operator.
          An example conjunction is:
          `CreatedAt >= '2025-01-07' AND Slug = 'test' AND Labels.mykey = 'myvalue'`.

          Supported attributes for filtering on Unit: ApplyGates, ApplyWarnings, ApprovedBy, BridgeWorkerID, ChangeSetID, CreatedAt, DataHash, DeleteGates, DestroyGates, DisplayName, DriftReconciliationMode, FromLinkID, HeadRevisionNum, HeadUnitActionNum, HeadUnitEventNum, Labels, LastActionAt, LastAppliedRevisionNum, LastChangeDescription, LiveRevisionNum, OrganizationID, PreviousLiveRevisionNum, ProviderType, Slug, SpaceID, TargetID, TargetOptions, ToolchainType, UnitID, UpdatedAt, UpstreamOrganizationID, UpstreamRevisionNum, UpstreamSpaceID, UpstreamUnitID, Values.

          Finding all units created by cloning can be done using the expression `UpstreamRevisionNum > 0`. Clones of a specific unit can be found by additionally filtering based on `UpstreamUnitID`. Unapplied units can be found using `LiveRevisionNum = 0`. Units with unapplied changes can be found with `HeadRevisionNum > LiveRevisionNum`.

          The whole string must be query-encoded.
        in: query
        name: where
        required: true
        schema:
          type: string
      - description: |-
          UUID of a Filter entity to apply to the Unit list.

          The Filter must be in the same Organization as the user credentials.

          The Filter's From field must match the entity type being filtered (Unit).

          For Space-resident entities, if the Filter has a FromSpaceID, it must match the operation's SpaceID.

          The Filter's Where clause will be combined with any explicit 'where' parameter using AND logic.

          If both 'filter' and 'where' parameters are specified, they are combined with AND logic.
        in: query
        name: filter
        required: false
        schema:
          type: string
      - description: |-
          Free text search that approximately matches the specified string against string fields and map keys/values.

          The search is case-insensitive and uses pattern matching to find entities containing the text.

          Searchable string fields include attributes like Slug, DisplayName, and string-typed custom fields.

          For map fields (like Labels and Annotations), the search matches both map keys and values.

          The search uses OR logic across all searchable fields, so matching any field will return the entity.

          If both 'where' and 'contains' parameters are specified, they are combined with AND logic.

          Searchable fields for Unit include string and map-type attributes from the queryable attributes list.

          The whole string must be query-encoded.
        in: query
        name: contains
        required: false
        schema:
          type: string
      - description: |-
          Include clause for expanding related entities in the response for Unit.
          The attribute names are case-sensitive, PascalCase, and
          expected in a comma-separated list format as in the JSON encoding.

          Supported attributes for Unit are ApprovedBy, BridgeWorkerID, ChangeSetID, FromLinkID, HeadMutationNum, HeadRevisionNum, LastAppliedRevisionNum, LiveRevisionNum, OrganizationID, PreviousLiveRevisionNum, SpaceID, TargetID, UnitEventID, UpstreamSpaceID, UpstreamUnitID.

          The whole string must be query-encoded.
        in: query
        name: include
        required: false
        schema:
          type: string
      - description: Dry run mode - validates which units would be applied without
          executing
        in: query
        name: dry_run
        required: false
        schema:
          type: boolean
      - description: Revision to apply (defaults to HeadRevisionNum). Can be a revision
          number, 'LiveRevisionNum', 'LastAppliedRevisionNum', 'Tag:uuid', 'ChangeSet:uuid',
          etc.
        in: query
        name: revision
        required: false
        schema:
          type: string
      - description: 'Drift reconciliation mode. Valid values: OnDemand, ContinuousApply,
          ContinuousRefresh. If not specified, the current value on the Unit is used.'
        in: query
        name: drift_mode
        required: false
        schema:
          type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                items:
                  $ref: '#/components/schemas/UnitActionResponse'
                type: array
          description: OK
        "207":
          content:
            application/json:
              schema:
                items:
                  $ref: '#/components/schemas/UnitActionResponse'
                type: array
          description: 'Multi-Status: Mixed success and failure results'
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Unit request is invalid (Bad Request).
        "401":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Unauthorized access.
        "403":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Forbidden access.
        "404":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Unit not found.
        "409":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Unit data conflict. Data has changed since last read.
        "422":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Unable to apply unit(s)
        "500":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Something went wrong while processing Unit.
        default:
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Unexpected error.
      summary: Bulk apply units
      tags:
      - Unit
  /unit/approve:
    post:
      description: Record that the user invoking this operation approves multiple
        unit configurations using where and contains filters
      operationId: BulkApproveUnits
      parameters:
      - description: |-
          The specified string is an expression for the purpose of filtering
          the list of Units returned. The expression syntax was inspired by SQL.
          It supports conjunctions using `AND` of relational expressions of the form *attribute*
          *operator* *attribute_or_literal*. The attribute names are case-sensitive and PascalCase,
          as in the JSON encoding.
          Strings support the following operators: `<`, `>`, `<=`, `>=`, `=`, `!=`, `LIKE`, `NOT LIKE`, `ILIKE`, `~~`, `!~~`, `~`, `~*`, `!~`, `!~*`, `IN`, `NOT IN`.
          String pattern operators: `LIKE` and `~~` for pattern matching with `%` and `_` wildcards,
          `ILIKE` for case-insensitive pattern matching, `NOT LIKE` and `!~~` for negated pattern matching.
          String regex operators: `~` for regex matching, `~*` for case-insensitive regex,
          `!~` and `!~*` for regex not matching (case-sensitive and insensitive).
          Integers support the following operators: `<`, `>`, `<=`, `>=`, `=`, `!=`, `IN`, `NOT IN`.
          UUIDs and boolean attributes support equality and inequality only.
          UUID and time literals must be quoted as string literals.
          String literals are quoted with single quotes, such as `'string'`.
          Time literals use the same form as when serialized as JSON,
          such as: `CreatedAt > '2025-02-18T23:16:34'`.
          Integer and boolean literals are also supported for attributes of those types.
          Arrays support the `?` operator to to match any element of the array,
          as in `ApprovedBy ? '7c61626f-ddbe-41af-93f6-b69f4ab6d308'`.
          Arrays can perform LEN() to check for length, as in `LEN(ApprovedBy) > 0`.
          Map support the dot notation to specify a particular map key, as in `Labels.tier = 'Backend'`.
          Maps support `IS NULL` and `IS NOT NULL` with dot notation to check for key absence or presence,
          as in `Labels.tier IS NULL` (key doesn't exist) or `Labels.tier IS NOT NULL` (key exists).
          Comparison results can be tested with `IS TRUE`, `IS FALSE`, `IS NOT TRUE`, and `IS NOT FALSE`.
          These are useful for nullable columns: `MergeSourceID = '<uuid>' IS NOT FALSE` matches rows where MergeSourceID equals the value OR is NULL.
          The `IN` and `NOT IN` operators accept a comma-separated list of values in parentheses,
          such as `Slug IN ('slugone', 'slugtwo')` or `Labels.environment IN ('prod', 'staging')`.
          Conjunctions are supported using the `AND` operator.
          An example conjunction is:
          `CreatedAt >= '2025-01-07' AND Slug = 'test' AND Labels.mykey = 'myvalue'`.

          Supported attributes for filtering on Unit: ApplyGates, ApplyWarnings, ApprovedBy, BridgeWorkerID, ChangeSetID, CreatedAt, DataHash, DeleteGates, DestroyGates, DisplayName, DriftReconciliationMode, FromLinkID, HeadRevisionNum, HeadUnitActionNum, HeadUnitEventNum, Labels, LastActionAt, LastAppliedRevisionNum, LastChangeDescription, LiveRevisionNum, OrganizationID, PreviousLiveRevisionNum, ProviderType, Slug, SpaceID, TargetID, TargetOptions, ToolchainType, UnitID, UpdatedAt, UpstreamOrganizationID, UpstreamRevisionNum, UpstreamSpaceID, UpstreamUnitID, Values.

          Finding all units created by cloning can be done using the expression `UpstreamRevisionNum > 0`. Clones of a specific unit can be found by additionally filtering based on `UpstreamUnitID`. Unapplied units can be found using `LiveRevisionNum = 0`. Units with unapplied changes can be found with `HeadRevisionNum > LiveRevisionNum`.

          The whole string must be query-encoded.
        in: query
        name: where
        required: false
        schema:
          type: string
      - description: |-
          UUID of a Filter entity to apply to the Unit list.

          The Filter must be in the same Organization as the user credentials.

          The Filter's From field must match the entity type being filtered (Unit).

          For Space-resident entities, if the Filter has a FromSpaceID, it must match the operation's SpaceID.

          The Filter's Where clause will be combined with any explicit 'where' parameter using AND logic.

          If both 'filter' and 'where' parameters are specified, they are combined with AND logic.
        in: query
        name: filter
        required: false
        schema:
          type: string
      - description: |-
          Free text search that approximately matches the specified string against string fields and map keys/values.

          The search is case-insensitive and uses pattern matching to find entities containing the text.

          Searchable string fields include attributes like Slug, DisplayName, and string-typed custom fields.

          For map fields (like Labels and Annotations), the search matches both map keys and values.

          The search uses OR logic across all searchable fields, so matching any field will return the entity.

          If both 'where' and 'contains' parameters are specified, they are combined with AND logic.

          Searchable fields for Unit include string and map-type attributes from the queryable attributes list.

          The whole string must be query-encoded.
        in: query
        name: contains
        required: false
        schema:
          type: string
      - description: |-
          Include clause for expanding related entities in the response for Unit.
          The attribute names are case-sensitive, PascalCase, and
          expected in a comma-separated list format as in the JSON encoding.

          Supported attributes for Unit are ApprovedBy, BridgeWorkerID, ChangeSetID, FromLinkID, HeadMutationNum, HeadRevisionNum, LastAppliedRevisionNum, LiveRevisionNum, OrganizationID, PreviousLiveRevisionNum, SpaceID, TargetID, UnitEventID, UpstreamSpaceID, UpstreamUnitID.

          The whole string must be query-encoded.
        in: query
        name: include
        required: false
        schema:
          type: string
      - description: Revision to approve (defaults to HeadRevisionNum). Can be a revision
          number, 'LiveRevisionNum', 'LastAppliedRevisionNum', 'Tag:uuid', 'ChangeSet:uuid',
          etc.
        in: query
        name: revision
        required: false
        schema:
          type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                items:
                  $ref: '#/components/schemas/ApproveResponse'
                type: array
          description: OK
        "207":
          content:
            application/json:
              schema:
                items:
                  $ref: '#/components/schemas/ApproveResponse'
                type: array
          description: 'Multi-Status: Mixed success and failure results'
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Unit request is invalid (Bad Request).
        "401":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Unauthorized access.
        "403":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Forbidden access.
        "404":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Unit not found.
        "409":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Unit data conflict. Data has changed since last read.
        "500":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Something went wrong while processing Unit.
        default:
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Unexpected error.
      summary: Bulk approve multiple units
      tags:
      - Unit
  /unit/cancel:
    post:
      description: Cancels pending or in-progress operations for multiple units based
        on filter criteria
      operationId: BulkCancelUnits
      parameters:
      - description: |-
          The specified string is an expression for the purpose of filtering
          the list of Units returned. The expression syntax was inspired by SQL.
          It supports conjunctions using `AND` of relational expressions of the form *attribute*
          *operator* *attribute_or_literal*. The attribute names are case-sensitive and PascalCase,
          as in the JSON encoding.
          Strings support the following operators: `<`, `>`, `<=`, `>=`, `=`, `!=`, `LIKE`, `NOT LIKE`, `ILIKE`, `~~`, `!~~`, `~`, `~*`, `!~`, `!~*`, `IN`, `NOT IN`.
          String pattern operators: `LIKE` and `~~` for pattern matching with `%` and `_` wildcards,
          `ILIKE` for case-insensitive pattern matching, `NOT LIKE` and `!~~` for negated pattern matching.
          String regex operators: `~` for regex matching, `~*` for case-insensitive regex,
          `!~` and `!~*` for regex not matching (case-sensitive and insensitive).
          Integers support the following operators: `<`, `>`, `<=`, `>=`, `=`, `!=`, `IN`, `NOT IN`.
          UUIDs and boolean attributes support equality and inequality only.
          UUID and time literals must be quoted as string literals.
          String literals are quoted with single quotes, such as `'string'`.
          Time literals use the same form as when serialized as JSON,
          such as: `CreatedAt > '2025-02-18T23:16:34'`.
          Integer and boolean literals are also supported for attributes of those types.
          Arrays support the `?` operator to to match any element of the array,
          as in `ApprovedBy ? '7c61626f-ddbe-41af-93f6-b69f4ab6d308'`.
          Arrays can perform LEN() to check for length, as in `LEN(ApprovedBy) > 0`.
          Map support the dot notation to specify a particular map key, as in `Labels.tier = 'Backend'`.
          Maps support `IS NULL` and `IS NOT NULL` with dot notation to check for key absence or presence,
          as in `Labels.tier IS NULL` (key doesn't exist) or `Labels.tier IS NOT NULL` (key exists).
          Comparison results can be tested with `IS TRUE`, `IS FALSE`, `IS NOT TRUE`, and `IS NOT FALSE`.
          These are useful for nullable columns: `MergeSourceID = '<uuid>' IS NOT FALSE` matches rows where MergeSourceID equals the value OR is NULL.
          The `IN` and `NOT IN` operators accept a comma-separated list of values in parentheses,
          such as `Slug IN ('slugone', 'slugtwo')` or `Labels.environment IN ('prod', 'staging')`.
          Conjunctions are supported using the `AND` operator.
          An example conjunction is:
          `CreatedAt >= '2025-01-07' AND Slug = 'test' AND Labels.mykey = 'myvalue'`.

          Supported attributes for filtering on Unit: ApplyGates, ApplyWarnings, ApprovedBy, BridgeWorkerID, ChangeSetID, CreatedAt, DataHash, DeleteGates, DestroyGates, DisplayName, DriftReconciliationMode, FromLinkID, HeadRevisionNum, HeadUnitActionNum, HeadUnitEventNum, Labels, LastActionAt, LastAppliedRevisionNum, LastChangeDescription, LiveRevisionNum, OrganizationID, PreviousLiveRevisionNum, ProviderType, Slug, SpaceID, TargetID, TargetOptions, ToolchainType, UnitID, UpdatedAt, UpstreamOrganizationID, UpstreamRevisionNum, UpstreamSpaceID, UpstreamUnitID, Values.

          Finding all units created by cloning can be done using the expression `UpstreamRevisionNum > 0`. Clones of a specific unit can be found by additionally filtering based on `UpstreamUnitID`. Unapplied units can be found using `LiveRevisionNum = 0`. Units with unapplied changes can be found with `HeadRevisionNum > LiveRevisionNum`.

          The whole string must be query-encoded.
        in: query
        name: where
        required: false
        schema:
          type: string
      - description: |-
          UUID of a Filter entity to apply to the Unit list.

          The Filter must be in the same Organization as the user credentials.

          The Filter's From field must match the entity type being filtered (Unit).

          For Space-resident entities, if the Filter has a FromSpaceID, it must match the operation's SpaceID.

          The Filter's Where clause will be combined with any explicit 'where' parameter using AND logic.

          If both 'filter' and 'where' parameters are specified, they are combined with AND logic.
        in: query
        name: filter
        required: false
        schema:
          type: string
      - description: |-
          Free text search that approximately matches the specified string against string fields and map keys/values.

          The search is case-insensitive and uses pattern matching to find entities containing the text.

          Searchable string fields include attributes like Slug, DisplayName, and string-typed custom fields.

          For map fields (like Labels and Annotations), the search matches both map keys and values.

          The search uses OR logic across all searchable fields, so matching any field will return the entity.

          If both 'where' and 'contains' parameters are specified, they are combined with AND logic.

          Searchable fields for Unit include string and map-type attributes from the queryable attributes list.

          The whole string must be query-encoded.
        in: query
        name: contains
        required: false
        schema:
          type: string
      - description: |-
          Include clause for expanding related entities in the response for Unit.
          The attribute names are case-sensitive, PascalCase, and
          expected in a comma-separated list format as in the JSON encoding.

          Supported attributes for Unit are ApprovedBy, BridgeWorkerID, ChangeSetID, FromLinkID, HeadMutationNum, HeadRevisionNum, LastAppliedRevisionNum, LiveRevisionNum, OrganizationID, PreviousLiveRevisionNum, SpaceID, TargetID, UnitEventID, UpstreamSpaceID, UpstreamUnitID.

          The whole string must be query-encoded.
        in: query
        name: include
        required: false
        schema:
          type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                items:
                  $ref: '#/components/schemas/UnitActionResponse'
                type: array
          description: OK
        "207":
          content:
            application/json:
              schema:
                items:
                  $ref: '#/components/schemas/UnitActionResponse'
                type: array
          description: 'Multi-Status: Mixed success and failure results'
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Unit request is invalid (Bad Request).
        "401":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Unauthorized access.
        "403":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Forbidden access.
        "404":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Unit not found.
        "409":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Unit data conflict. Data has changed since last read.
        "422":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Unable to cancel unit operation(s)
        "500":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Something went wrong while processing Unit.
        default:
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Unexpected error.
      summary: Bulk cancel unit operations
      tags:
      - Unit
  /unit/destroy:
    post:
      description: Executes destroy operations for multiple units based on filter
        criteria across spaces
      operationId: BulkDestroyUnits
      parameters:
      - description: |-
          The specified string is an expression for the purpose of filtering
          the list of Units returned. The expression syntax was inspired by SQL.
          It supports conjunctions using `AND` of relational expressions of the form *attribute*
          *operator* *attribute_or_literal*. The attribute names are case-sensitive and PascalCase,
          as in the JSON encoding.
          Strings support the following operators: `<`, `>`, `<=`, `>=`, `=`, `!=`, `LIKE`, `NOT LIKE`, `ILIKE`, `~~`, `!~~`, `~`, `~*`, `!~`, `!~*`, `IN`, `NOT IN`.
          String pattern operators: `LIKE` and `~~` for pattern matching with `%` and `_` wildcards,
          `ILIKE` for case-insensitive pattern matching, `NOT LIKE` and `!~~` for negated pattern matching.
          String regex operators: `~` for regex matching, `~*` for case-insensitive regex,
          `!~` and `!~*` for regex not matching (case-sensitive and insensitive).
          Integers support the following operators: `<`, `>`, `<=`, `>=`, `=`, `!=`, `IN`, `NOT IN`.
          UUIDs and boolean attributes support equality and inequality only.
          UUID and time literals must be quoted as string literals.
          String literals are quoted with single quotes, such as `'string'`.
          Time literals use the same form as when serialized as JSON,
          such as: `CreatedAt > '2025-02-18T23:16:34'`.
          Integer and boolean literals are also supported for attributes of those types.
          Arrays support the `?` operator to to match any element of the array,
          as in `ApprovedBy ? '7c61626f-ddbe-41af-93f6-b69f4ab6d308'`.
          Arrays can perform LEN() to check for length, as in `LEN(ApprovedBy) > 0`.
          Map support the dot notation to specify a particular map key, as in `Labels.tier = 'Backend'`.
          Maps support `IS NULL` and `IS NOT NULL` with dot notation to check for key absence or presence,
          as in `Labels.tier IS NULL` (key doesn't exist) or `Labels.tier IS NOT NULL` (key exists).
          Comparison results can be tested with `IS TRUE`, `IS FALSE`, `IS NOT TRUE`, and `IS NOT FALSE`.
          These are useful for nullable columns: `MergeSourceID = '<uuid>' IS NOT FALSE` matches rows where MergeSourceID equals the value OR is NULL.
          The `IN` and `NOT IN` operators accept a comma-separated list of values in parentheses,
          such as `Slug IN ('slugone', 'slugtwo')` or `Labels.environment IN ('prod', 'staging')`.
          Conjunctions are supported using the `AND` operator.
          An example conjunction is:
          `CreatedAt >= '2025-01-07' AND Slug = 'test' AND Labels.mykey = 'myvalue'`.

          Supported attributes for filtering on Unit: ApplyGates, ApplyWarnings, ApprovedBy, BridgeWorkerID, ChangeSetID, CreatedAt, DataHash, DeleteGates, DestroyGates, DisplayName, DriftReconciliationMode, FromLinkID, HeadRevisionNum, HeadUnitActionNum, HeadUnitEventNum, Labels, LastActionAt, LastAppliedRevisionNum, LastChangeDescription, LiveRevisionNum, OrganizationID, PreviousLiveRevisionNum, ProviderType, Slug, SpaceID, TargetID, TargetOptions, ToolchainType, UnitID, UpdatedAt, UpstreamOrganizationID, UpstreamRevisionNum, UpstreamSpaceID, UpstreamUnitID, Values.

          Finding all units created by cloning can be done using the expression `UpstreamRevisionNum > 0`. Clones of a specific unit can be found by additionally filtering based on `UpstreamUnitID`. Unapplied units can be found using `LiveRevisionNum = 0`. Units with unapplied changes can be found with `HeadRevisionNum > LiveRevisionNum`.

          The whole string must be query-encoded.
        in: query
        name: where
        required: true
        schema:
          type: string
      - description: |-
          UUID of a Filter entity to apply to the Unit list.

          The Filter must be in the same Organization as the user credentials.

          The Filter's From field must match the entity type being filtered (Unit).

          For Space-resident entities, if the Filter has a FromSpaceID, it must match the operation's SpaceID.

          The Filter's Where clause will be combined with any explicit 'where' parameter using AND logic.

          If both 'filter' and 'where' parameters are specified, they are combined with AND logic.
        in: query
        name: filter
        required: false
        schema:
          type: string
      - description: |-
          Free text search that approximately matches the specified string against string fields and map keys/values.

          The search is case-insensitive and uses pattern matching to find entities containing the text.

          Searchable string fields include attributes like Slug, DisplayName, and string-typed custom fields.

          For map fields (like Labels and Annotations), the search matches both map keys and values.

          The search uses OR logic across all searchable fields, so matching any field will return the entity.

          If both 'where' and 'contains' parameters are specified, they are combined with AND logic.

          Searchable fields for Unit include string and map-type attributes from the queryable attributes list.

          The whole string must be query-encoded.
        in: query
        name: contains
        required: false
        schema:
          type: string
      - description: |-
          Include clause for expanding related entities in the response for Unit.
          The attribute names are case-sensitive, PascalCase, and
          expected in a comma-separated list format as in the JSON encoding.

          Supported attributes for Unit are ApprovedBy, BridgeWorkerID, ChangeSetID, FromLinkID, HeadMutationNum, HeadRevisionNum, LastAppliedRevisionNum, LiveRevisionNum, OrganizationID, PreviousLiveRevisionNum, SpaceID, TargetID, UnitEventID, UpstreamSpaceID, UpstreamUnitID.

          The whole string must be query-encoded.
        in: query
        name: include
        required: false
        schema:
          type: string
      - description: Dry run mode - validates which units would be destroyed without
          executing
        in: query
        name: dry_run
        required: false
        schema:
          type: boolean
      responses:
        "200":
          content:
            application/json:
              schema:
                items:
                  $ref: '#/components/schemas/UnitActionResponse'
                type: array
          description: OK
        "207":
          content:
            application/json:
              schema:
                items:
                  $ref: '#/components/schemas/UnitActionResponse'
                type: array
          description: 'Multi-Status: Mixed success and failure results'
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Unit request is invalid (Bad Request).
        "401":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Unauthorized access.
        "403":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Forbidden access.
        "404":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Unit not found.
        "409":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Unit data conflict. Data has changed since last read.
        "422":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Unable to destroy unit(s)
        "500":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Something went wrong while processing Unit.
        default:
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Unexpected error.
      summary: Bulk destroy units
      tags:
      - Unit
  /unit/refresh:
    post:
      description: Executes refresh operations for multiple units based on filter
        criteria across spaces
      operationId: BulkRefreshUnits
      parameters:
      - description: |-
          The specified string is an expression for the purpose of filtering
          the list of Units returned. The expression syntax was inspired by SQL.
          It supports conjunctions using `AND` of relational expressions of the form *attribute*
          *operator* *attribute_or_literal*. The attribute names are case-sensitive and PascalCase,
          as in the JSON encoding.
          Strings support the following operators: `<`, `>`, `<=`, `>=`, `=`, `!=`, `LIKE`, `NOT LIKE`, `ILIKE`, `~~`, `!~~`, `~`, `~*`, `!~`, `!~*`, `IN`, `NOT IN`.
          String pattern operators: `LIKE` and `~~` for pattern matching with `%` and `_` wildcards,
          `ILIKE` for case-insensitive pattern matching, `NOT LIKE` and `!~~` for negated pattern matching.
          String regex operators: `~` for regex matching, `~*` for case-insensitive regex,
          `!~` and `!~*` for regex not matching (case-sensitive and insensitive).
          Integers support the following operators: `<`, `>`, `<=`, `>=`, `=`, `!=`, `IN`, `NOT IN`.
          UUIDs and boolean attributes support equality and inequality only.
          UUID and time literals must be quoted as string literals.
          String literals are quoted with single quotes, such as `'string'`.
          Time literals use the same form as when serialized as JSON,
          such as: `CreatedAt > '2025-02-18T23:16:34'`.
          Integer and boolean literals are also supported for attributes of those types.
          Arrays support the `?` operator to to match any element of the array,
          as in `ApprovedBy ? '7c61626f-ddbe-41af-93f6-b69f4ab6d308'`.
          Arrays can perform LEN() to check for length, as in `LEN(ApprovedBy) > 0`.
          Map support the dot notation to specify a particular map key, as in `Labels.tier = 'Backend'`.
          Maps support `IS NULL` and `IS NOT NULL` with dot notation to check for key absence or presence,
          as in `Labels.tier IS NULL` (key doesn't exist) or `Labels.tier IS NOT NULL` (key exists).
          Comparison results can be tested with `IS TRUE`, `IS FALSE`, `IS NOT TRUE`, and `IS NOT FALSE`.
          These are useful for nullable columns: `MergeSourceID = '<uuid>' IS NOT FALSE` matches rows where MergeSourceID equals the value OR is NULL.
          The `IN` and `NOT IN` operators accept a comma-separated list of values in parentheses,
          such as `Slug IN ('slugone', 'slugtwo')` or `Labels.environment IN ('prod', 'staging')`.
          Conjunctions are supported using the `AND` operator.
          An example conjunction is:
          `CreatedAt >= '2025-01-07' AND Slug = 'test' AND Labels.mykey = 'myvalue'`.

          Supported attributes for filtering on Unit: ApplyGates, ApplyWarnings, ApprovedBy, BridgeWorkerID, ChangeSetID, CreatedAt, DataHash, DeleteGates, DestroyGates, DisplayName, DriftReconciliationMode, FromLinkID, HeadRevisionNum, HeadUnitActionNum, HeadUnitEventNum, Labels, LastActionAt, LastAppliedRevisionNum, LastChangeDescription, LiveRevisionNum, OrganizationID, PreviousLiveRevisionNum, ProviderType, Slug, SpaceID, TargetID, TargetOptions, ToolchainType, UnitID, UpdatedAt, UpstreamOrganizationID, UpstreamRevisionNum, UpstreamSpaceID, UpstreamUnitID, Values.

          Finding all units created by cloning can be done using the expression `UpstreamRevisionNum > 0`. Clones of a specific unit can be found by additionally filtering based on `UpstreamUnitID`. Unapplied units can be found using `LiveRevisionNum = 0`. Units with unapplied changes can be found with `HeadRevisionNum > LiveRevisionNum`.

          The whole string must be query-encoded.
        in: query
        name: where
        required: true
        schema:
          type: string
      - description: |-
          UUID of a Filter entity to apply to the Unit list.

          The Filter must be in the same Organization as the user credentials.

          The Filter's From field must match the entity type being filtered (Unit).

          For Space-resident entities, if the Filter has a FromSpaceID, it must match the operation's SpaceID.

          The Filter's Where clause will be combined with any explicit 'where' parameter using AND logic.

          If both 'filter' and 'where' parameters are specified, they are combined with AND logic.
        in: query
        name: filter
        required: false
        schema:
          type: string
      - description: |-
          Free text search that approximately matches the specified string against string fields and map keys/values.

          The search is case-insensitive and uses pattern matching to find entities containing the text.

          Searchable string fields include attributes like Slug, DisplayName, and string-typed custom fields.

          For map fields (like Labels and Annotations), the search matches both map keys and values.

          The search uses OR logic across all searchable fields, so matching any field will return the entity.

          If both 'where' and 'contains' parameters are specified, they are combined with AND logic.

          Searchable fields for Unit include string and map-type attributes from the queryable attributes list.

          The whole string must be query-encoded.
        in: query
        name: contains
        required: false
        schema:
          type: string
      - description: |-
          Include clause for expanding related entities in the response for Unit.
          The attribute names are case-sensitive, PascalCase, and
          expected in a comma-separated list format as in the JSON encoding.

          Supported attributes for Unit are ApprovedBy, BridgeWorkerID, ChangeSetID, FromLinkID, HeadMutationNum, HeadRevisionNum, LastAppliedRevisionNum, LiveRevisionNum, OrganizationID, PreviousLiveRevisionNum, SpaceID, TargetID, UnitEventID, UpstreamSpaceID, UpstreamUnitID.

          The whole string must be query-encoded.
        in: query
        name: include
        required: false
        schema:
          type: string
      - description: Dry run mode - returns refresh data in the operation/action and
          updates LiveData and LiveState in the unit
        in: query
        name: dry_run
        required: false
        schema:
          type: boolean
      - description: 'Drift reconciliation mode. Valid values: OnDemand, ContinuousApply,
          ContinuousRefresh. If not specified, the current value on the Unit is used.'
        in: query
        name: drift_mode
        required: false
        schema:
          type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                items:
                  $ref: '#/components/schemas/UnitActionResponse'
                type: array
          description: OK
        "207":
          content:
            application/json:
              schema:
                items:
                  $ref: '#/components/schemas/UnitActionResponse'
                type: array
          description: 'Multi-Status: Mixed success and failure results'
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Unit request is invalid (Bad Request).
        "401":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Unauthorized access.
        "403":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Forbidden access.
        "404":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Unit not found.
        "409":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Unit data conflict. Data has changed since last read.
        "422":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Unable to refresh unit(s)
        "500":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Something went wrong while processing Unit.
        default:
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Unexpected error.
      summary: Bulk refresh units
      tags:
      - Unit
  /unit/tag:
    post:
      description: Attach tags to specified unit revisions
      operationId: BulkTagUnits
      parameters:
      - description: |-
          The specified string is an expression for the purpose of filtering
          the list of Units returned. The expression syntax was inspired by SQL.
          It supports conjunctions using `AND` of relational expressions of the form *attribute*
          *operator* *attribute_or_literal*. The attribute names are case-sensitive and PascalCase,
          as in the JSON encoding.
          Strings support the following operators: `<`, `>`, `<=`, `>=`, `=`, `!=`, `LIKE`, `NOT LIKE`, `ILIKE`, `~~`, `!~~`, `~`, `~*`, `!~`, `!~*`, `IN`, `NOT IN`.
          String pattern operators: `LIKE` and `~~` for pattern matching with `%` and `_` wildcards,
          `ILIKE` for case-insensitive pattern matching, `NOT LIKE` and `!~~` for negated pattern matching.
          String regex operators: `~` for regex matching, `~*` for case-insensitive regex,
          `!~` and `!~*` for regex not matching (case-sensitive and insensitive).
          Integers support the following operators: `<`, `>`, `<=`, `>=`, `=`, `!=`, `IN`, `NOT IN`.
          UUIDs and boolean attributes support equality and inequality only.
          UUID and time literals must be quoted as string literals.
          String literals are quoted with single quotes, such as `'string'`.
          Time literals use the same form as when serialized as JSON,
          such as: `CreatedAt > '2025-02-18T23:16:34'`.
          Integer and boolean literals are also supported for attributes of those types.
          Arrays support the `?` operator to to match any element of the array,
          as in `ApprovedBy ? '7c61626f-ddbe-41af-93f6-b69f4ab6d308'`.
          Arrays can perform LEN() to check for length, as in `LEN(ApprovedBy) > 0`.
          Map support the dot notation to specify a particular map key, as in `Labels.tier = 'Backend'`.
          Maps support `IS NULL` and `IS NOT NULL` with dot notation to check for key absence or presence,
          as in `Labels.tier IS NULL` (key doesn't exist) or `Labels.tier IS NOT NULL` (key exists).
          Comparison results can be tested with `IS TRUE`, `IS FALSE`, `IS NOT TRUE`, and `IS NOT FALSE`.
          These are useful for nullable columns: `MergeSourceID = '<uuid>' IS NOT FALSE` matches rows where MergeSourceID equals the value OR is NULL.
          The `IN` and `NOT IN` operators accept a comma-separated list of values in parentheses,
          such as `Slug IN ('slugone', 'slugtwo')` or `Labels.environment IN ('prod', 'staging')`.
          Conjunctions are supported using the `AND` operator.
          An example conjunction is:
          `CreatedAt >= '2025-01-07' AND Slug = 'test' AND Labels.mykey = 'myvalue'`.

          Supported attributes for filtering on Unit: ApplyGates, ApplyWarnings, ApprovedBy, BridgeWorkerID, ChangeSetID, CreatedAt, DataHash, DeleteGates, DestroyGates, DisplayName, DriftReconciliationMode, FromLinkID, HeadRevisionNum, HeadUnitActionNum, HeadUnitEventNum, Labels, LastActionAt, LastAppliedRevisionNum, LastChangeDescription, LiveRevisionNum, OrganizationID, PreviousLiveRevisionNum, ProviderType, Slug, SpaceID, TargetID, TargetOptions, ToolchainType, UnitID, UpdatedAt, UpstreamOrganizationID, UpstreamRevisionNum, UpstreamSpaceID, UpstreamUnitID, Values.

          Finding all units created by cloning can be done using the expression `UpstreamRevisionNum > 0`. Clones of a specific unit can be found by additionally filtering based on `UpstreamUnitID`. Unapplied units can be found using `LiveRevisionNum = 0`. Units with unapplied changes can be found with `HeadRevisionNum > LiveRevisionNum`.

          The whole string must be query-encoded.
        in: query
        name: where
        required: false
        schema:
          type: string
      - description: |-
          UUID of a Filter entity to apply to the Unit list.

          The Filter must be in the same Organization as the user credentials.

          The Filter's From field must match the entity type being filtered (Unit).

          For Space-resident entities, if the Filter has a FromSpaceID, it must match the operation's SpaceID.

          The Filter's Where clause will be combined with any explicit 'where' parameter using AND logic.

          If both 'filter' and 'where' parameters are specified, they are combined with AND logic.
        in: query
        name: filter
        required: false
        schema:
          type: string
      - description: |-
          Free text search that approximately matches the specified string against string fields and map keys/values.

          The search is case-insensitive and uses pattern matching to find entities containing the text.

          Searchable string fields include attributes like Slug, DisplayName, and string-typed custom fields.

          For map fields (like Labels and Annotations), the search matches both map keys and values.

          The search uses OR logic across all searchable fields, so matching any field will return the entity.

          If both 'where' and 'contains' parameters are specified, they are combined with AND logic.

          Searchable fields for Unit include string and map-type attributes from the queryable attributes list.

          The whole string must be query-encoded.
        in: query
        name: contains
        required: false
        schema:
          type: string
      - description: |-
          Include clause for expanding related entities in the response for Unit.
          The attribute names are case-sensitive, PascalCase, and
          expected in a comma-separated list format as in the JSON encoding.

          Supported attributes for Unit are ApprovedBy, BridgeWorkerID, ChangeSetID, FromLinkID, HeadMutationNum, HeadRevisionNum, LastAppliedRevisionNum, LiveRevisionNum, OrganizationID, PreviousLiveRevisionNum, SpaceID, TargetID, UnitEventID, UpstreamSpaceID, UpstreamUnitID.

          The whole string must be query-encoded.
        in: query
        name: include
        required: false
        schema:
          type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/UnitTagRequest'
      responses:
        "200":
          content:
            application/json:
              schema:
                items:
                  $ref: '#/components/schemas/UnitTagResponse'
                type: array
          description: OK
        "207":
          content:
            application/json:
              schema:
                items:
                  $ref: '#/components/schemas/UnitTagResponse'
                type: array
          description: 'Multi-Status: Mixed success and failure results'
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Unit request is invalid (Bad Request).
        "401":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Unauthorized access.
        "403":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Forbidden access.
        "404":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Unit not found.
        "409":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Unit data conflict. Data has changed since last read.
        "500":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Something went wrong while processing Unit.
        default:
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Unexpected error.
      summary: Bulk tag multiple units
      tags:
      - Unit
  /unit_action:
    get:
      description: Retrieves a list of UnitActions across units in the Organization,
        returning at most one UnitAction per unit
      operationId: ListAllUnitActions
      parameters:
      - description: |-
          The specified string is an expression for the purpose of filtering
          the list of QueuedOperations returned. The expression syntax was inspired by SQL.
          It supports conjunctions using `AND` of relational expressions of the form *attribute*
          *operator* *attribute_or_literal*. The attribute names are case-sensitive and PascalCase,
          as in the JSON encoding.
          Strings support the following operators: `<`, `>`, `<=`, `>=`, `=`, `!=`, `LIKE`, `NOT LIKE`, `ILIKE`, `~~`, `!~~`, `~`, `~*`, `!~`, `!~*`, `IN`, `NOT IN`.
          String pattern operators: `LIKE` and `~~` for pattern matching with `%` and `_` wildcards,
          `ILIKE` for case-insensitive pattern matching, `NOT LIKE` and `!~~` for negated pattern matching.
          String regex operators: `~` for regex matching, `~*` for case-insensitive regex,
          `!~` and `!~*` for regex not matching (case-sensitive and insensitive).
          Integers support the following operators: `<`, `>`, `<=`, `>=`, `=`, `!=`, `IN`, `NOT IN`.
          UUIDs and boolean attributes support equality and inequality only.
          UUID and time literals must be quoted as string literals.
          String literals are quoted with single quotes, such as `'string'`.
          Time literals use the same form as when serialized as JSON,
          such as: `CreatedAt > '2025-02-18T23:16:34'`.
          Integer and boolean literals are also supported for attributes of those types.
          Arrays support the `?` operator to to match any element of the array,
          as in `ApprovedBy ? '7c61626f-ddbe-41af-93f6-b69f4ab6d308'`.
          Arrays can perform LEN() to check for length, as in `LEN(ApprovedBy) > 0`.
          Map support the dot notation to specify a particular map key, as in `Labels.tier = 'Backend'`.
          Maps support `IS NULL` and `IS NOT NULL` with dot notation to check for key absence or presence,
          as in `Labels.tier IS NULL` (key doesn't exist) or `Labels.tier IS NOT NULL` (key exists).
          Comparison results can be tested with `IS TRUE`, `IS FALSE`, `IS NOT TRUE`, and `IS NOT FALSE`.
          These are useful for nullable columns: `MergeSourceID = '<uuid>' IS NOT FALSE` matches rows where MergeSourceID equals the value OR is NULL.
          The `IN` and `NOT IN` operators accept a comma-separated list of values in parentheses,
          such as `Slug IN ('slugone', 'slugtwo')` or `Labels.environment IN ('prod', 'staging')`.
          Conjunctions are supported using the `AND` operator.
          An example conjunction is:
          `CreatedAt >= '2025-01-07' AND Slug = 'test' AND Labels.mykey = 'myvalue'`.

          Supported attributes for filtering on QueuedOperation: Action, BridgeWorkerID, CreatedAt, DriftReconciliationMode, DryRun, OrganizationID, QueuedOperationID, RevisionNum, SpaceID, Status, TargetID, UnitActionNum, UnitID.

          The whole string must be query-encoded.
        in: query
        name: where
        required: false
        schema:
          type: string
      - description: |-
          UUID of a Filter entity to apply to the QueuedOperation list.

          The Filter must be in the same Organization as the user credentials.

          The Filter's From field must match the entity type being filtered (QueuedOperation).

          For Space-resident entities, if the Filter has a FromSpaceID, it must match the operation's SpaceID.

          The Filter's Where clause will be combined with any explicit 'where' parameter using AND logic.

          If both 'filter' and 'where' parameters are specified, they are combined with AND logic.
        in: query
        name: filter
        required: false
        schema:
          type: string
      - description: |-
          Free text search that approximately matches the specified string against string fields and map keys/values.

          The search is case-insensitive and uses pattern matching to find entities containing the text.

          Searchable string fields include attributes like Slug, DisplayName, and string-typed custom fields.

          For map fields (like Labels and Annotations), the search matches both map keys and values.

          The search uses OR logic across all searchable fields, so matching any field will return the entity.

          If both 'where' and 'contains' parameters are specified, they are combined with AND logic.

          Searchable fields for QueuedOperation include string and map-type attributes from the queryable attributes list.

          The whole string must be query-encoded.
        in: query
        name: contains
        required: false
        schema:
          type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                items:
                  $ref: '#/components/schemas/UnitAction'
                type: array
          description: OK
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: QueuedOperation request is invalid (Bad Request).
        "401":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Unauthorized access.
        "403":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Forbidden access.
        "404":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: QueuedOperation not found.
        "500":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Something went wrong while processing QueuedOperation.
        default:
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Unexpected error.
      summary: List of UnitActions across units
      tags:
      - QueuedOperation
  /unit_event:
    get:
      description: Retrieves a list of UnitEvents across units in the Organization,
        returning at most one UnitEvent per unit
      operationId: ListAllUnitEvents
      parameters:
      - description: |-
          The specified string is an expression for the purpose of filtering
          the list of UnitEvents returned. The expression syntax was inspired by SQL.
          It supports conjunctions using `AND` of relational expressions of the form *attribute*
          *operator* *attribute_or_literal*. The attribute names are case-sensitive and PascalCase,
          as in the JSON encoding.
          Strings support the following operators: `<`, `>`, `<=`, `>=`, `=`, `!=`, `LIKE`, `NOT LIKE`, `ILIKE`, `~~`, `!~~`, `~`, `~*`, `!~`, `!~*`, `IN`, `NOT IN`.
          String pattern operators: `LIKE` and `~~` for pattern matching with `%` and `_` wildcards,
          `ILIKE` for case-insensitive pattern matching, `NOT LIKE` and `!~~` for negated pattern matching.
          String regex operators: `~` for regex matching, `~*` for case-insensitive regex,
          `!~` and `!~*` for regex not matching (case-sensitive and insensitive).
          Integers support the following operators: `<`, `>`, `<=`, `>=`, `=`, `!=`, `IN`, `NOT IN`.
          UUIDs and boolean attributes support equality and inequality only.
          UUID and time literals must be quoted as string literals.
          String literals are quoted with single quotes, such as `'string'`.
          Time literals use the same form as when serialized as JSON,
          such as: `CreatedAt > '2025-02-18T23:16:34'`.
          Integer and boolean literals are also supported for attributes of those types.
          Arrays support the `?` operator to to match any element of the array,
          as in `ApprovedBy ? '7c61626f-ddbe-41af-93f6-b69f4ab6d308'`.
          Arrays can perform LEN() to check for length, as in `LEN(ApprovedBy) > 0`.
          Map support the dot notation to specify a particular map key, as in `Labels.tier = 'Backend'`.
          Maps support `IS NULL` and `IS NOT NULL` with dot notation to check for key absence or presence,
          as in `Labels.tier IS NULL` (key doesn't exist) or `Labels.tier IS NOT NULL` (key exists).
          Comparison results can be tested with `IS TRUE`, `IS FALSE`, `IS NOT TRUE`, and `IS NOT FALSE`.
          These are useful for nullable columns: `MergeSourceID = '<uuid>' IS NOT FALSE` matches rows where MergeSourceID equals the value OR is NULL.
          The `IN` and `NOT IN` operators accept a comma-separated list of values in parentheses,
          such as `Slug IN ('slugone', 'slugtwo')` or `Labels.environment IN ('prod', 'staging')`.
          Conjunctions are supported using the `AND` operator.
          An example conjunction is:
          `CreatedAt >= '2025-01-07' AND Slug = 'test' AND Labels.mykey = 'myvalue'`.

          Supported attributes for filtering on UnitEvent: Action, BridgeWorkerID, CreatedAt, OrganizationID, QueuedOperationID, Result, RevisionNum, SpaceID, StartedAt, Status, TerminatedAt, UnitEventID, UnitEventNum, UnitID, UpdatedAt.

          The whole string must be query-encoded.
        in: query
        name: where
        required: false
        schema:
          type: string
      - description: |-
          UUID of a Filter entity to apply to the UnitEvent list.

          The Filter must be in the same Organization as the user credentials.

          The Filter's From field must match the entity type being filtered (UnitEvent).

          For Space-resident entities, if the Filter has a FromSpaceID, it must match the operation's SpaceID.

          The Filter's Where clause will be combined with any explicit 'where' parameter using AND logic.

          If both 'filter' and 'where' parameters are specified, they are combined with AND logic.
        in: query
        name: filter
        required: false
        schema:
          type: string
      - description: |-
          Free text search that approximately matches the specified string against string fields and map keys/values.

          The search is case-insensitive and uses pattern matching to find entities containing the text.

          Searchable string fields include attributes like Slug, DisplayName, and string-typed custom fields.

          For map fields (like Labels and Annotations), the search matches both map keys and values.

          The search uses OR logic across all searchable fields, so matching any field will return the entity.

          If both 'where' and 'contains' parameters are specified, they are combined with AND logic.

          Searchable fields for UnitEvent include string and map-type attributes from the queryable attributes list.

          The whole string must be query-encoded.
        in: query
        name: contains
        required: false
        schema:
          type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                items:
                  $ref: '#/components/schemas/UnitEvent'
                type: array
          description: OK
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: UnitEvent request is invalid (Bad Request).
        "401":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Unauthorized access.
        "403":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Forbidden access.
        "404":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: UnitEvent not found.
        "500":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Something went wrong while processing UnitEvent.
        default:
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Unexpected error.
      summary: List of UnitEvents across units
      tags:
      - UnitEvent
  /user:
    get:
      description: List Users
      operationId: ListUsers
      parameters:
      - description: |-
          The specified string is an expression for the purpose of filtering
          the list of Users returned. The expression syntax was inspired by SQL.
          It supports conjunctions using `AND` of relational expressions of the form *attribute*
          *operator* *attribute_or_literal*. The attribute names are case-sensitive and PascalCase,
          as in the JSON encoding.
          Strings support the following operators: `<`, `>`, `<=`, `>=`, `=`, `!=`, `LIKE`, `NOT LIKE`, `ILIKE`, `~~`, `!~~`, `~`, `~*`, `!~`, `!~*`, `IN`, `NOT IN`.
          String pattern operators: `LIKE` and `~~` for pattern matching with `%` and `_` wildcards,
          `ILIKE` for case-insensitive pattern matching, `NOT LIKE` and `!~~` for negated pattern matching.
          String regex operators: `~` for regex matching, `~*` for case-insensitive regex,
          `!~` and `!~*` for regex not matching (case-sensitive and insensitive).
          Integers support the following operators: `<`, `>`, `<=`, `>=`, `=`, `!=`, `IN`, `NOT IN`.
          UUIDs and boolean attributes support equality and inequality only.
          UUID and time literals must be quoted as string literals.
          String literals are quoted with single quotes, such as `'string'`.
          Time literals use the same form as when serialized as JSON,
          such as: `CreatedAt > '2025-02-18T23:16:34'`.
          Integer and boolean literals are also supported for attributes of those types.
          Arrays support the `?` operator to to match any element of the array,
          as in `ApprovedBy ? '7c61626f-ddbe-41af-93f6-b69f4ab6d308'`.
          Arrays can perform LEN() to check for length, as in `LEN(ApprovedBy) > 0`.
          Map support the dot notation to specify a particular map key, as in `Labels.tier = 'Backend'`.
          Maps support `IS NULL` and `IS NOT NULL` with dot notation to check for key absence or presence,
          as in `Labels.tier IS NULL` (key doesn't exist) or `Labels.tier IS NOT NULL` (key exists).
          Comparison results can be tested with `IS TRUE`, `IS FALSE`, `IS NOT TRUE`, and `IS NOT FALSE`.
          These are useful for nullable columns: `MergeSourceID = '<uuid>' IS NOT FALSE` matches rows where MergeSourceID equals the value OR is NULL.
          The `IN` and `NOT IN` operators accept a comma-separated list of values in parentheses,
          such as `Slug IN ('slugone', 'slugtwo')` or `Labels.environment IN ('prod', 'staging')`.
          Conjunctions are supported using the `AND` operator.
          An example conjunction is:
          `CreatedAt >= '2025-01-07' AND Slug = 'test' AND Labels.mykey = 'myvalue'`.

          Supported attributes for filtering on User: CreatedAt, DisplayName, ExternalID, Slug, UpdatedAt, UserID, Username.

          The whole string must be query-encoded.
        in: query
        name: where
        required: false
        schema:
          type: string
      - description: |-
          UUID of a Filter entity to apply to the User list.

          The Filter must be in the same Organization as the user credentials.

          The Filter's From field must match the entity type being filtered (User).

          For Space-resident entities, if the Filter has a FromSpaceID, it must match the operation's SpaceID.

          The Filter's Where clause will be combined with any explicit 'where' parameter using AND logic.

          If both 'filter' and 'where' parameters are specified, they are combined with AND logic.
        in: query
        name: filter
        required: false
        schema:
          type: string
      - description: |-
          Free text search that approximately matches the specified string against string fields and map keys/values.

          The search is case-insensitive and uses pattern matching to find entities containing the text.

          Searchable string fields include attributes like Slug, DisplayName, and string-typed custom fields.

          For map fields (like Labels and Annotations), the search matches both map keys and values.

          The search uses OR logic across all searchable fields, so matching any field will return the entity.

          If both 'where' and 'contains' parameters are specified, they are combined with AND logic.

          Searchable fields for User include string and map-type attributes from the queryable attributes list.

          The whole string must be query-encoded.
        in: query
        name: contains
        required: false
        schema:
          type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                items:
                  $ref: '#/components/schemas/User'
                type: array
          description: OK
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: User request is invalid (Bad Request).
        "401":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Unauthorized access.
        "403":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Forbidden access.
        "404":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: User not found.
        "500":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Something went wrong while processing User.
        default:
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Unexpected error.
      summary: List Users
      tags:
      - User
  /user/{user_id}:
    get:
      description: Get User
      operationId: GetUser
      parameters:
      - description: Unique identifier for a user_id
        in: path
        name: user_id
        required: true
        schema:
          format: uuid
          type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/User'
          description: a User in Confighub.
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: User request is invalid (Bad Request).
        "401":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Unauthorized access.
        "403":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Forbidden access.
        "404":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: User not found.
        "500":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Something went wrong while processing User.
        default:
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Unexpected error.
      summary: Get User
      tags:
      - User
  /view:
    delete:
      description: Delete multiple views selected by query parameters
      operationId: BulkDeleteViews
      parameters:
      - description: |-
          The specified string is an expression for the purpose of filtering
          the list of Views returned. The expression syntax was inspired by SQL.
          It supports conjunctions using `AND` of relational expressions of the form *attribute*
          *operator* *attribute_or_literal*. The attribute names are case-sensitive and PascalCase,
          as in the JSON encoding.
          Strings support the following operators: `<`, `>`, `<=`, `>=`, `=`, `!=`, `LIKE`, `NOT LIKE`, `ILIKE`, `~~`, `!~~`, `~`, `~*`, `!~`, `!~*`, `IN`, `NOT IN`.
          String pattern operators: `LIKE` and `~~` for pattern matching with `%` and `_` wildcards,
          `ILIKE` for case-insensitive pattern matching, `NOT LIKE` and `!~~` for negated pattern matching.
          String regex operators: `~` for regex matching, `~*` for case-insensitive regex,
          `!~` and `!~*` for regex not matching (case-sensitive and insensitive).
          Integers support the following operators: `<`, `>`, `<=`, `>=`, `=`, `!=`, `IN`, `NOT IN`.
          UUIDs and boolean attributes support equality and inequality only.
          UUID and time literals must be quoted as string literals.
          String literals are quoted with single quotes, such as `'string'`.
          Time literals use the same form as when serialized as JSON,
          such as: `CreatedAt > '2025-02-18T23:16:34'`.
          Integer and boolean literals are also supported for attributes of those types.
          Arrays support the `?` operator to to match any element of the array,
          as in `ApprovedBy ? '7c61626f-ddbe-41af-93f6-b69f4ab6d308'`.
          Arrays can perform LEN() to check for length, as in `LEN(ApprovedBy) > 0`.
          Map support the dot notation to specify a particular map key, as in `Labels.tier = 'Backend'`.
          Maps support `IS NULL` and `IS NOT NULL` with dot notation to check for key absence or presence,
          as in `Labels.tier IS NULL` (key doesn't exist) or `Labels.tier IS NOT NULL` (key exists).
          Comparison results can be tested with `IS TRUE`, `IS FALSE`, `IS NOT TRUE`, and `IS NOT FALSE`.
          These are useful for nullable columns: `MergeSourceID = '<uuid>' IS NOT FALSE` matches rows where MergeSourceID equals the value OR is NULL.
          The `IN` and `NOT IN` operators accept a comma-separated list of values in parentheses,
          such as `Slug IN ('slugone', 'slugtwo')` or `Labels.environment IN ('prod', 'staging')`.
          Conjunctions are supported using the `AND` operator.
          An example conjunction is:
          `CreatedAt >= '2025-01-07' AND Slug = 'test' AND Labels.mykey = 'myvalue'`.

          Supported attributes for filtering on View: CreatedAt, DisplayName, FilterID, GroupBy, Labels, Of, OrderBy, OrderByDirection, OrganizationID, Slug, SpaceID, UpdatedAt, ViewID.

          The whole string must be query-encoded.
        in: query
        name: where
        required: false
        schema:
          type: string
      - description: |-
          UUID of a Filter entity to apply to the View list.

          The Filter must be in the same Organization as the user credentials.

          The Filter's From field must match the entity type being filtered (View).

          For Space-resident entities, if the Filter has a FromSpaceID, it must match the operation's SpaceID.

          The Filter's Where clause will be combined with any explicit 'where' parameter using AND logic.

          If both 'filter' and 'where' parameters are specified, they are combined with AND logic.
        in: query
        name: filter
        required: false
        schema:
          type: string
      - description: |-
          Free text search that approximately matches the specified string against string fields and map keys/values.

          The search is case-insensitive and uses pattern matching to find entities containing the text.

          Searchable string fields include attributes like Slug, DisplayName, and string-typed custom fields.

          For map fields (like Labels and Annotations), the search matches both map keys and values.

          The search uses OR logic across all searchable fields, so matching any field will return the entity.

          If both 'where' and 'contains' parameters are specified, they are combined with AND logic.

          Searchable fields for View include string and map-type attributes from the queryable attributes list.

          The whole string must be query-encoded.
        in: query
        name: contains
        required: false
        schema:
          type: string
      - description: |-
          Include clause for expanding related entities in the response for View.
          The attribute names are case-sensitive, PascalCase, and
          expected in a comma-separated list format as in the JSON encoding.

          Supported attributes for View are FilterID, OrganizationID, SpaceID.

          The whole string must be query-encoded.
        in: query
        name: include
        required: false
        schema:
          type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                items:
                  $ref: '#/components/schemas/DeleteResponse'
                type: array
          description: OK
        "207":
          content:
            application/json:
              schema:
                items:
                  $ref: '#/components/schemas/DeleteResponse'
                type: array
          description: 'Multi-Status: Mixed success and failure results'
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: View request is invalid (Bad Request).
        "401":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Unauthorized access.
        "403":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Forbidden access.
        "404":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: View not found.
        "409":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: View data conflict. Data has changed since last read.
        "422":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Unable to delete entity
        "500":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Something went wrong while processing View.
        default:
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Unexpected error.
      summary: Bulk delete multiple views
      tags:
      - View
    get:
      description: Retrieves a list of Views across spaces in the Organization
      operationId: ListAllViews
      parameters:
      - description: |-
          The specified string is an expression for the purpose of filtering
          the list of Views returned. The expression syntax was inspired by SQL.
          It supports conjunctions using `AND` of relational expressions of the form *attribute*
          *operator* *attribute_or_literal*. The attribute names are case-sensitive and PascalCase,
          as in the JSON encoding.
          Strings support the following operators: `<`, `>`, `<=`, `>=`, `=`, `!=`, `LIKE`, `NOT LIKE`, `ILIKE`, `~~`, `!~~`, `~`, `~*`, `!~`, `!~*`, `IN`, `NOT IN`.
          String pattern operators: `LIKE` and `~~` for pattern matching with `%` and `_` wildcards,
          `ILIKE` for case-insensitive pattern matching, `NOT LIKE` and `!~~` for negated pattern matching.
          String regex operators: `~` for regex matching, `~*` for case-insensitive regex,
          `!~` and `!~*` for regex not matching (case-sensitive and insensitive).
          Integers support the following operators: `<`, `>`, `<=`, `>=`, `=`, `!=`, `IN`, `NOT IN`.
          UUIDs and boolean attributes support equality and inequality only.
          UUID and time literals must be quoted as string literals.
          String literals are quoted with single quotes, such as `'string'`.
          Time literals use the same form as when serialized as JSON,
          such as: `CreatedAt > '2025-02-18T23:16:34'`.
          Integer and boolean literals are also supported for attributes of those types.
          Arrays support the `?` operator to to match any element of the array,
          as in `ApprovedBy ? '7c61626f-ddbe-41af-93f6-b69f4ab6d308'`.
          Arrays can perform LEN() to check for length, as in `LEN(ApprovedBy) > 0`.
          Map support the dot notation to specify a particular map key, as in `Labels.tier = 'Backend'`.
          Maps support `IS NULL` and `IS NOT NULL` with dot notation to check for key absence or presence,
          as in `Labels.tier IS NULL` (key doesn't exist) or `Labels.tier IS NOT NULL` (key exists).
          Comparison results can be tested with `IS TRUE`, `IS FALSE`, `IS NOT TRUE`, and `IS NOT FALSE`.
          These are useful for nullable columns: `MergeSourceID = '<uuid>' IS NOT FALSE` matches rows where MergeSourceID equals the value OR is NULL.
          The `IN` and `NOT IN` operators accept a comma-separated list of values in parentheses,
          such as `Slug IN ('slugone', 'slugtwo')` or `Labels.environment IN ('prod', 'staging')`.
          Conjunctions are supported using the `AND` operator.
          An example conjunction is:
          `CreatedAt >= '2025-01-07' AND Slug = 'test' AND Labels.mykey = 'myvalue'`.

          Supported attributes for filtering on View: CreatedAt, DisplayName, FilterID, GroupBy, Labels, Of, OrderBy, OrderByDirection, OrganizationID, Slug, SpaceID, UpdatedAt, ViewID.

          The whole string must be query-encoded.
        in: query
        name: where
        required: false
        schema:
          type: string
      - description: |-
          UUID of a Filter entity to apply to the View list.

          The Filter must be in the same Organization as the user credentials.

          The Filter's From field must match the entity type being filtered (View).

          For Space-resident entities, if the Filter has a FromSpaceID, it must match the operation's SpaceID.

          The Filter's Where clause will be combined with any explicit 'where' parameter using AND logic.

          If both 'filter' and 'where' parameters are specified, they are combined with AND logic.
        in: query
        name: filter
        required: false
        schema:
          type: string
      - description: |-
          Free text search that approximately matches the specified string against string fields and map keys/values.

          The search is case-insensitive and uses pattern matching to find entities containing the text.

          Searchable string fields include attributes like Slug, DisplayName, and string-typed custom fields.

          For map fields (like Labels and Annotations), the search matches both map keys and values.

          The search uses OR logic across all searchable fields, so matching any field will return the entity.

          If both 'where' and 'contains' parameters are specified, they are combined with AND logic.

          Searchable fields for View include string and map-type attributes from the queryable attributes list.

          The whole string must be query-encoded.
        in: query
        name: contains
        required: false
        schema:
          type: string
      - description: |-
          Include clause for expanding related entities in the response for View.
          The attribute names are case-sensitive, PascalCase, and
          expected in a comma-separated list format as in the JSON encoding.

          Supported attributes for View are FilterID, OrganizationID, SpaceID.

          The whole string must be query-encoded.
        in: query
        name: include
        required: false
        schema:
          type: string
      - description: |-
          Select clause for specifying which fields to include in the response for View.
          The attribute names are case-sensitive, PascalCase, and
          expected in a comma-separated list format as in the JSON encoding.
          If not specified, all fields are returned.
          Entity and parent IDs (like OrganizationID, SpaceID, ViewID) and Slug are always returned regardless of the select parameter.
          Fields used in where and contains filters are also automatically included.
          Example: 'DisplayName,CreatedAt,Labels' will return only those fields plus the required ID and Slug fields.
          The whole string must be query-encoded.
        in: query
        name: select
        required: false
        schema:
          type: string
      responses:
        "200":
          content:
            application/json:
              schema:
                items:
                  $ref: '#/components/schemas/ExtendedView'
                type: array
          description: OK
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: View request is invalid (Bad Request).
        "401":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Unauthorized access.
        "403":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Forbidden access.
        "404":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: View not found.
        "500":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Something went wrong while processing View.
        default:
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Unexpected error.
      summary: List of Views across spaces
      tags:
      - View
    patch:
      description: Apply JSON merge patch to multiple views selected by query parameters
      operationId: BulkPatchViews
      parameters:
      - description: |-
          The specified string is an expression for the purpose of filtering
          the list of Views returned. The expression syntax was inspired by SQL.
          It supports conjunctions using `AND` of relational expressions of the form *attribute*
          *operator* *attribute_or_literal*. The attribute names are case-sensitive and PascalCase,
          as in the JSON encoding.
          Strings support the following operators: `<`, `>`, `<=`, `>=`, `=`, `!=`, `LIKE`, `NOT LIKE`, `ILIKE`, `~~`, `!~~`, `~`, `~*`, `!~`, `!~*`, `IN`, `NOT IN`.
          String pattern operators: `LIKE` and `~~` for pattern matching with `%` and `_` wildcards,
          `ILIKE` for case-insensitive pattern matching, `NOT LIKE` and `!~~` for negated pattern matching.
          String regex operators: `~` for regex matching, `~*` for case-insensitive regex,
          `!~` and `!~*` for regex not matching (case-sensitive and insensitive).
          Integers support the following operators: `<`, `>`, `<=`, `>=`, `=`, `!=`, `IN`, `NOT IN`.
          UUIDs and boolean attributes support equality and inequality only.
          UUID and time literals must be quoted as string literals.
          String literals are quoted with single quotes, such as `'string'`.
          Time literals use the same form as when serialized as JSON,
          such as: `CreatedAt > '2025-02-18T23:16:34'`.
          Integer and boolean literals are also supported for attributes of those types.
          Arrays support the `?` operator to to match any element of the array,
          as in `ApprovedBy ? '7c61626f-ddbe-41af-93f6-b69f4ab6d308'`.
          Arrays can perform LEN() to check for length, as in `LEN(ApprovedBy) > 0`.
          Map support the dot notation to specify a particular map key, as in `Labels.tier = 'Backend'`.
          Maps support `IS NULL` and `IS NOT NULL` with dot notation to check for key absence or presence,
          as in `Labels.tier IS NULL` (key doesn't exist) or `Labels.tier IS NOT NULL` (key exists).
          Comparison results can be tested with `IS TRUE`, `IS FALSE`, `IS NOT TRUE`, and `IS NOT FALSE`.
          These are useful for nullable columns: `MergeSourceID = '<uuid>' IS NOT FALSE` matches rows where MergeSourceID equals the value OR is NULL.
          The `IN` and `NOT IN` operators accept a comma-separated list of values in parentheses,
          such as `Slug IN ('slugone', 'slugtwo')` or `Labels.environment IN ('prod', 'staging')`.
          Conjunctions are supported using the `AND` operator.
          An example conjunction is:
          `CreatedAt >= '2025-01-07' AND Slug = 'test' AND Labels.mykey = 'myvalue'`.

          Supported attributes for filtering on View: CreatedAt, DisplayName, FilterID, GroupBy, Labels, Of, OrderBy, OrderByDirection, OrganizationID, Slug, SpaceID, UpdatedAt, ViewID.

          The whole string must be query-encoded.
        in: query
        name: where
        required: false
        schema:
          type: string
      - description: |-
          UUID of a Filter entity to apply to the View list.

          The Filter must be in the same Organization as the user credentials.

          The Filter's From field must match the entity type being filtered (View).

          For Space-resident entities, if the Filter has a FromSpaceID, it must match the operation's SpaceID.

          The Filter's Where clause will be combined with any explicit 'where' parameter using AND logic.

          If both 'filter' and 'where' parameters are specified, they are combined with AND logic.
        in: query
        name: filter
        required: false
        schema:
          type: string
      - description: |-
          Free text search that approximately matches the specified string against string fields and map keys/values.

          The search is case-insensitive and uses pattern matching to find entities containing the text.

          Searchable string fields include attributes like Slug, DisplayName, and string-typed custom fields.

          For map fields (like Labels and Annotations), the search matches both map keys and values.

          The search uses OR logic across all searchable fields, so matching any field will return the entity.

          If both 'where' and 'contains' parameters are specified, they are combined with AND logic.

          Searchable fields for View include string and map-type attributes from the queryable attributes list.

          The whole string must be query-encoded.
        in: query
        name: contains
        required: false
        schema:
          type: string
      - description: |-
          Include clause for expanding related entities in the response for View.
          The attribute names are case-sensitive, PascalCase, and
          expected in a comma-separated list format as in the JSON encoding.

          Supported attributes for View are FilterID, OrganizationID, SpaceID.

          The whole string must be query-encoded.
        in: query
        name: include
        required: false
        schema:
          type: string
      requestBody:
        content:
          application/merge-patch+json:
            schema:
              description: JSON Merge Patch schema for View. All fields are optional
                and nullable. Readonly fields are ignored.
              properties:
                Annotations:
                  additionalProperties:
                    nullable: true
                    type: string
                  description: An optional map of Annotation key/value pairs for tools
                    to attach information to entities.
                  nullable: true
                  type: object
                Columns:
                  description: ""
                  items:
                    nullable: true
                    type: object
                  nullable: true
                  type: array
                DeleteGates:
                  additionalProperties:
                    nullable: true
                    type: boolean
                  description: An optional set of gates that, if any is present, will
                    block deletion
                  nullable: true
                  type: object
                DisplayName:
                  description: Friendly name for the entity.
                  nullable: true
                  type: string
                FilterID:
                  description: ""
                  format: uuid
                  nullable: true
                  type: string
                GroupBy:
                  description: ""
                  nullable: true
                  type: string
                Labels:
                  additionalProperties:
                    nullable: true
                    type: string
                  description: An optional map of Label key/value pairs to specify
                    identifying attributes of entities for the purpose of grouping
                    and filtering them.
                  nullable: true
                  type: object
                Of:
                  description: ""
                  nullable: true
                  type: string
                OrderBy:
                  description: ""
                  nullable: true
                  type: string
                OrderByDirection:
                  description: ""
                  nullable: true
                  type: string
                Slug:
                  description: Unique URL-safe identifier for the entity.
                  nullable: true
                  type: string
                Version:
                  description: An entity-specific sequence number used for optimistic
                    concurrency control. The value read must be sent in calls to Update.
                  nullable: true
                  type: integer
              type: object
      responses:
        "200":
          content:
            application/json:
              schema:
                items:
                  $ref: '#/components/schemas/ViewCreateOrUpdateResponse'
                type: array
          description: OK
        "207":
          content:
            application/json:
              schema:
                items:
                  $ref: '#/components/schemas/ViewCreateOrUpdateResponse'
                type: array
          description: 'Multi-Status: Mixed success and failure results'
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: View request is invalid (Bad Request).
        "401":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Unauthorized access.
        "403":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Forbidden access.
        "404":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: View not found.
        "409":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: View data conflict. Data has changed since last read.
        "500":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Something went wrong while processing View.
        default:
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Unexpected error.
      summary: Bulk patch multiple views
      tags:
      - View
    post:
      description: Clone multiple views selected by query parameters with optional
        name prefixes and destination spaces
      operationId: BulkCreateViews
      parameters:
      - description: |-
          The specified string is an expression for the purpose of filtering
          the list of Views returned. The expression syntax was inspired by SQL.
          It supports conjunctions using `AND` of relational expressions of the form *attribute*
          *operator* *attribute_or_literal*. The attribute names are case-sensitive and PascalCase,
          as in the JSON encoding.
          Strings support the following operators: `<`, `>`, `<=`, `>=`, `=`, `!=`, `LIKE`, `NOT LIKE`, `ILIKE`, `~~`, `!~~`, `~`, `~*`, `!~`, `!~*`, `IN`, `NOT IN`.
          String pattern operators: `LIKE` and `~~` for pattern matching with `%` and `_` wildcards,
          `ILIKE` for case-insensitive pattern matching, `NOT LIKE` and `!~~` for negated pattern matching.
          String regex operators: `~` for regex matching, `~*` for case-insensitive regex,
          `!~` and `!~*` for regex not matching (case-sensitive and insensitive).
          Integers support the following operators: `<`, `>`, `<=`, `>=`, `=`, `!=`, `IN`, `NOT IN`.
          UUIDs and boolean attributes support equality and inequality only.
          UUID and time literals must be quoted as string literals.
          String literals are quoted with single quotes, such as `'string'`.
          Time literals use the same form as when serialized as JSON,
          such as: `CreatedAt > '2025-02-18T23:16:34'`.
          Integer and boolean literals are also supported for attributes of those types.
          Arrays support the `?` operator to to match any element of the array,
          as in `ApprovedBy ? '7c61626f-ddbe-41af-93f6-b69f4ab6d308'`.
          Arrays can perform LEN() to check for length, as in `LEN(ApprovedBy) > 0`.
          Map support the dot notation to specify a particular map key, as in `Labels.tier = 'Backend'`.
          Maps support `IS NULL` and `IS NOT NULL` with dot notation to check for key absence or presence,
          as in `Labels.tier IS NULL` (key doesn't exist) or `Labels.tier IS NOT NULL` (key exists).
          Comparison results can be tested with `IS TRUE`, `IS FALSE`, `IS NOT TRUE`, and `IS NOT FALSE`.
          These are useful for nullable columns: `MergeSourceID = '<uuid>' IS NOT FALSE` matches rows where MergeSourceID equals the value OR is NULL.
          The `IN` and `NOT IN` operators accept a comma-separated list of values in parentheses,
          such as `Slug IN ('slugone', 'slugtwo')` or `Labels.environment IN ('prod', 'staging')`.
          Conjunctions are supported using the `AND` operator.
          An example conjunction is:
          `CreatedAt >= '2025-01-07' AND Slug = 'test' AND Labels.mykey = 'myvalue'`.

          Supported attributes for filtering on View: CreatedAt, DisplayName, FilterID, GroupBy, Labels, Of, OrderBy, OrderByDirection, OrganizationID, Slug, SpaceID, UpdatedAt, ViewID.

          The whole string must be query-encoded.
        in: query
        name: where
        required: false
        schema:
          type: string
      - description: |-
          UUID of a Filter entity to apply to the View list.

          The Filter must be in the same Organization as the user credentials.

          The Filter's From field must match the entity type being filtered (View).

          For Space-resident entities, if the Filter has a FromSpaceID, it must match the operation's SpaceID.

          The Filter's Where clause will be combined with any explicit 'where' parameter using AND logic.

          If both 'filter' and 'where' parameters are specified, they are combined with AND logic.
        in: query
        name: filter
        required: false
        schema:
          type: string
      - description: |-
          Free text search that approximately matches the specified string against string fields and map keys/values.

          The search is case-insensitive and uses pattern matching to find entities containing the text.

          Searchable string fields include attributes like Slug, DisplayName, and string-typed custom fields.

          For map fields (like Labels and Annotations), the search matches both map keys and values.

          The search uses OR logic across all searchable fields, so matching any field will return the entity.

          If both 'where' and 'contains' parameters are specified, they are combined with AND logic.

          Searchable fields for View include string and map-type attributes from the queryable attributes list.

          The whole string must be query-encoded.
        in: query
        name: contains
        required: false
        schema:
          type: string
      - description: |-
          Include clause for expanding related entities in the response for View.
          The attribute names are case-sensitive, PascalCase, and
          expected in a comma-separated list format as in the JSON encoding.

          Supported attributes for View are FilterID, OrganizationID, SpaceID.

          The whole string must be query-encoded.
        in: query
        name: include
        required: false
        schema:
          type: string
      - description: Comma-separated list of prefixes to apply to cloned View names
        in: query
        name: name_prefixes
        required: false
        schema:
          type: string
      - description: Comma-separated list of labels with multiple values fro cloned
          View labels, in the format of key1=value1|value2,key2=value1|value2|value3
        in: query
        name: variant_labels
        required: false
        schema:
          type: string
      - description: 'A string for clone names, use the prefix ''template:'' for a
          Go-template with .SourceEntitySlug to access the original entity''s slug
          and .Labels to access variant labels, example: ''template:{{.SourceEntitySlug}}-{{.Labels.env}}'''
        in: query
        name: name_pattern
        required: false
        schema:
          type: string
      - description: |-
          The specified string is an expression for the purpose of filtering
          the list of Spaces returned. The expression syntax was inspired by SQL.
          It supports conjunctions using `AND` of relational expressions of the form *attribute*
          *operator* *attribute_or_literal*. The attribute names are case-sensitive and PascalCase,
          as in the JSON encoding.
          Strings support the following operators: `<`, `>`, `<=`, `>=`, `=`, `!=`, `LIKE`, `NOT LIKE`, `ILIKE`, `~~`, `!~~`, `~`, `~*`, `!~`, `!~*`, `IN`, `NOT IN`.
          String pattern operators: `LIKE` and `~~` for pattern matching with `%` and `_` wildcards,
          `ILIKE` for case-insensitive pattern matching, `NOT LIKE` and `!~~` for negated pattern matching.
          String regex operators: `~` for regex matching, `~*` for case-insensitive regex,
          `!~` and `!~*` for regex not matching (case-sensitive and insensitive).
          Integers support the following operators: `<`, `>`, `<=`, `>=`, `=`, `!=`, `IN`, `NOT IN`.
          UUIDs and boolean attributes support equality and inequality only.
          UUID and time literals must be quoted as string literals.
          String literals are quoted with single quotes, such as `'string'`.
          Time literals use the same form as when serialized as JSON,
          such as: `CreatedAt > '2025-02-18T23:16:34'`.
          Integer and boolean literals are also supported for attributes of those types.
          Arrays support the `?` operator to to match any element of the array,
          as in `ApprovedBy ? '7c61626f-ddbe-41af-93f6-b69f4ab6d308'`.
          Arrays can perform LEN() to check for length, as in `LEN(ApprovedBy) > 0`.
          Map support the dot notation to specify a particular map key, as in `Labels.tier = 'Backend'`.
          Maps support `IS NULL` and `IS NOT NULL` with dot notation to check for key absence or presence,
          as in `Labels.tier IS NULL` (key doesn't exist) or `Labels.tier IS NOT NULL` (key exists).
          Comparison results can be tested with `IS TRUE`, `IS FALSE`, `IS NOT TRUE`, and `IS NOT FALSE`.
          These are useful for nullable columns: `MergeSourceID = '<uuid>' IS NOT FALSE` matches rows where MergeSourceID equals the value OR is NULL.
          The `IN` and `NOT IN` operators accept a comma-separated list of values in parentheses,
          such as `Slug IN ('slugone', 'slugtwo')` or `Labels.environment IN ('prod', 'staging')`.
          Conjunctions are supported using the `AND` operator.
          An example conjunction is:
          `CreatedAt >= '2025-01-07' AND Slug = 'test' AND Labels.mykey = 'myvalue'`.

          Supported attributes for filtering on Space: AttributeFilterID, AttributeHash, AttributeIDs, CreatedAt, DeleteGates, DisplayName, Labels, OrganizationID, Permissions, Slug, SpaceID, TriggerFilterID, TriggerHash, TriggerIDs, UpdatedAt.

          Where expression to select destination spaces for cloning views

          The whole string must be query-encoded.
        in: query
        name: where_space
        required: false
        schema:
          type: string
      - description: |-
          UUID of a Filter entity to apply to the Space list.

          The Filter must be in the same Organization as the user credentials.

          The Filter's From field must match the entity type being filtered (Space).

          For Space-resident entities, if the Filter has a FromSpaceID, it must match the operation's SpaceID.

          The Filter's Where clause will be combined with any explicit 'where' parameter using AND logic.

          If both 'filter' and 'where' parameters are specified, they are combined with AND logic.
        in: query
        name: filter_space
        required: false
        schema:
          type: string
      - description: Allowed values are true and false. Default is false. When true,
          reports success when an entity already exists and returns the existing entity
        in: query
        name: allow_exists
        required: false
        schema:
          type: string
      requestBody:
        content:
          application/merge-patch+json:
            schema:
              description: JSON Merge Patch schema for View. All fields are optional
                and nullable. Readonly fields are ignored.
              properties:
                Annotations:
                  additionalProperties:
                    nullable: true
                    type: string
                  description: An optional map of Annotation key/value pairs for tools
                    to attach information to entities.
                  nullable: true
                  type: object
                Columns:
                  description: ""
                  items:
                    nullable: true
                    type: object
                  nullable: true
                  type: array
                DeleteGates:
                  additionalProperties:
                    nullable: true
                    type: boolean
                  description: An optional set of gates that, if any is present, will
                    block deletion
                  nullable: true
                  type: object
                DisplayName:
                  description: Friendly name for the entity.
                  nullable: true
                  type: string
                FilterID:
                  description: ""
                  format: uuid
                  nullable: true
                  type: string
                GroupBy:
                  description: ""
                  nullable: true
                  type: string
                Labels:
                  additionalProperties:
                    nullable: true
                    type: string
                  description: An optional map of Label key/value pairs to specify
                    identifying attributes of entities for the purpose of grouping
                    and filtering them.
                  nullable: true
                  type: object
                Of:
                  description: ""
                  nullable: true
                  type: string
                OrderBy:
                  description: ""
                  nullable: true
                  type: string
                OrderByDirection:
                  description: ""
                  nullable: true
                  type: string
                Slug:
                  description: Unique URL-safe identifier for the entity.
                  nullable: true
                  type: string
                Version:
                  description: An entity-specific sequence number used for optimistic
                    concurrency control. The value read must be sent in calls to Update.
                  nullable: true
                  type: integer
              type: object
      responses:
        "200":
          content:
            application/json:
              schema:
                items:
                  $ref: '#/components/schemas/ViewCreateOrUpdateResponse'
                type: array
          description: OK
        "207":
          content:
            application/json:
              schema:
                items:
                  $ref: '#/components/schemas/ViewCreateOrUpdateResponse'
                type: array
          description: Multi-Status (partial success)
        "400":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: View request is invalid (Bad Request).
        "401":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Unauthorized access.
        "403":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Forbidden access.
        "404":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: View not found.
        "409":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: View data conflict. Data has changed since last read.
        "500":
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Something went wrong while processing View.
        default:
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/StandardErrorResponse'
          description: Unexpected error.
      summary: Bulk create (clone) multiple views
      tags:
      - View
components:
  schemas:
    ActionResult:
      properties:
        Action:
          $ref: '#/components/schemas/ActionType'
        BridgeState:
          description: Additional state used by the Bridge
          format: base64
          type: string
          x-go-type-skip-optional-pointer: true
        Data:
          description: Updated configuration Data of the Unit (for refresh and import)
          format: base64
          type: string
          x-go-type-skip-optional-pointer: true
        ErrorMessages:
          description: Warning or error messages to surface to the user
          items:
            type: string
          type: array
          x-go-type-skip-optional-pointer: true
        LiveData:
          description: Live Data corresponding to the Unit (for inventory and drift
            detection)
          format: base64
          type: string
          x-go-type-skip-optional-pointer: true
        LiveState:
          description: Live State corresponding to the Unit (for status determination)
          format: base64
          type: string
          x-go-type-skip-optional-pointer: true
        Message:
          type: string
          x-go-type-skip-optional-pointer: true
        QueuedOperationID:
          description: UUID of the operation corresponding to the action request
          example: 248df4b7-aa70-47b8-a036-33ac447e668d
          format: uuid
          type: string
          x-go-type-skip-optional-pointer: true
        ResourceStatuses:
          $ref: '#/components/schemas/ResourceStatusMap'
        Result:
          $ref: '#/components/schemas/ActionResultType'
        RevisionNum:
          format: int64
          type: integer
          x-go-type-skip-optional-pointer: true
        SpaceID:
          description: UUID of the Space of the Unit on which the action is performed
          example: 248df4b7-aa70-47b8-a036-33ac447e668d
          format: uuid
          type: string
          x-go-type-skip-optional-pointer: true
        StartedAt:
          example: 2006-01-02T15:04:05Z07:00
          format: date-time
          type: string
          x-go-type-skip-optional-pointer: true
        Status:
          $ref: '#/components/schemas/ActionStatusType'
        TerminatedAt:
          format: date-time
          nullable: true
          type: string
          x-go-type-skip-optional-pointer: true
        UnitID:
          description: UUID of the Unit on which the action is performed
          example: 248df4b7-aa70-47b8-a036-33ac447e668d
          format: uuid
          type: string
          x-go-type-skip-optional-pointer: true
      type: object
    ActionResultType:
      enum:
      - ApplyFailed
      - ApplyWaitFailed
      - ApplyCompleted
      - DestroyCompleted
      - DestroyWaitFailed
      - DestroyFailed
      - ImportCompleted
      - ImportFailed
      - RefreshAndDrifted
      - RefreshAndNoDrift
      - RefreshFailed
      - None
      type: string
    ActionStatusType:
      enum:
      - None
      - Pending
      - Submitted
      - Progressing
      - Completed
      - Failed
      - Canceled
      - Aborted
      type: string
    ActionType:
      enum:
      - Apply
      - Destroy
      - Finalize
      - Heartbeat
      - Import
      - N/A
      - Refresh
      type: string
    ApiInfo:
      description: Information provided to clients by the server.
      properties:
        AuthServer:
          readOnly: true
          type: string
          x-go-type-skip-optional-pointer: true
        Build:
          description: Build identifier for support cases.
          readOnly: true
          type: string
          x-go-type-skip-optional-pointer: true
        BuiltAt:
          description: The timestamp when ConfigHub was built in "2023-01-01T12:00:00Z"
            format for support cases.
          example: "2025-04-04T11:50:02.95102-07:00"
          readOnly: true
          type: string
          x-go-type-skip-optional-pointer: true
        ClientID:
          description: ClientID for identity provider service.
          readOnly: true
          type: string
          x-go-type-skip-optional-pointer: true
        DeviceAuthURL:
          readOnly: true
          type: string
          x-go-type-skip-optional-pointer: true
        DeviceTokenURL:
          readOnly: true
          type: string
          x-go-type-skip-optional-pointer: true
        OCIHost:
          description: OCI registry host for pulling configuration artifacts.
          readOnly: true
          type: string
          x-go-type-skip-optional-pointer: true
        OCIPort:
          description: OCI registry port for pulling configuration artifacts.
          readOnly: true
          type: string
          x-go-type-skip-optional-pointer: true
        RedirectURI:
          readOnly: true
          type: string
          x-go-type-skip-optional-pointer: true
        Version:
          description: Semantic version of the server (e.g. v1.2.3), or 'dev' for
            development builds.
          readOnly: true
          type: string
          x-go-type-skip-optional-pointer: true
        WorkerPort:
          description: Port number for the worker to connect to the server.
          readOnly: true
          type: string
          x-go-type-skip-optional-pointer: true
      type: object
    ApproveResponse:
      properties:
        Error:
          $ref: '#/components/schemas/ResponseError'
        Message:
          type: string
          x-go-type-skip-optional-pointer: true
        Unit:
          $ref: '#/components/schemas/Unit'
      type: object
    ArrayElementAliasMap:
      additionalProperties:
        additionalProperties:
          type: string
        type: object
      type: object
    ArrayOrderMap:
      additionalProperties:
        items:
          type: string
        type: array
      type: object
    Attribute:
      description: |-
        Defines a dynamic configuration attribute that registers getter and setter functions
        and their associated paths in a Space's FunctionExecutor. Attributes enable per-Space
        customization of the function registry by specifying a set of paths within a resource
        type that can be read and written using generated get-<slug> and set-<slug> functions.
      properties:
        Annotations:
          additionalProperties:
            maxLength: 1024
            pattern: ^(.|\n)*$
            type: string
          description: An optional map of Annotation key/value pairs for tools to
            attach information to entities.
          type: object
          x-go-type-skip-optional-pointer: true
        AttributeID:
          description: AttributeID uniquely identifies an attribute within the system.
          example: 248df4b7-aa70-47b8-a036-33ac447e668d
          format: uuid
          type: string
          x-go-type-skip-optional-pointer: true
        CreatedAt:
          description: The timestamp when the entity was created in "2023-01-01T12:00:00Z"
            format.
          example: "2025-04-04T11:50:02.95102-07:00"
          format: date-time
          readOnly: true
          type: string
          x-go-type-skip-optional-pointer: true
        CursorID:
          description: An auto-incrementing sequence number used for pagination.
          format: int64
          readOnly: true
          type: integer
          x-go-type-skip-optional-pointer: true
        DataType:
          description: 'DataType specifies the data type of the attribute value. Must
            be one of: string, int, bool.'
          type: string
          x-go-type-skip-optional-pointer: true
        DeleteGates:
          additionalProperties:
            type: boolean
          description: An optional set of gates that, if any is present, will block
            deletion.
          type: object
          x-go-type-skip-optional-pointer: true
        Description:
          description: Description describes the attribute and its generated functions.
          type: string
          x-go-type-skip-optional-pointer: true
        DisplayName:
          description: Friendly name for the entity.
          type: string
          x-go-type-skip-optional-pointer: true
        EntityType:
          description: The type of entity.
          readOnly: true
          type: string
          x-go-type-skip-optional-pointer: true
        Hash:
          description: Hash is a SHA256 hash of the attribute's defining properties.
            (readonly)
          readOnly: true
          type: string
          x-go-type-skip-optional-pointer: true
        Labels:
          additionalProperties:
            maxLength: 128
            pattern: ^[\-_@/#$%&~+=!?.,:;(){}\[\]<>|A-Za-z0-9](([\-_@/#$%&~+=!?.,:;(){}\[\]<>|A-Za-z0-9]|
              )*[\-_@/#$%&~+=!?.,:;(){}\[\]<>|A-Za-z0-9])?$
            type: string
          description: An optional map of Label key/value pairs to specify identifying
            attributes of entities for the purpose of grouping and filtering them.
          type: object
          x-go-type-skip-optional-pointer: true
        OrganizationID:
          description: Unique identifier for an organization.
          example: 248df4b7-aa70-47b8-a036-33ac447e668d
          format: uuid
          type: string
          x-go-type-skip-optional-pointer: true
        Parameters:
          description: Parameters specifies the function parameters for the getter
            and setter functions.
          items:
            $ref: '#/components/schemas/FunctionParameter'
          nullable: true
          type: array
          x-go-type-skip-optional-pointer: true
        ResourceTypePaths:
          description: ResourceTypePaths maps resource types to their path-to-visitor-info
            mappings.
          items:
            $ref: '#/components/schemas/ResourceTypePathsEntry'
          nullable: true
          type: array
          x-go-type-skip-optional-pointer: true
        Slug:
          description: Unique URL-safe identifier for the entity.
          maxLength: 128
          pattern: ^[A-Za-z0-9]([\-_.A-Za-z0-9]*[A-Za-z0-9])?$
          type: string
          x-go-type-skip-optional-pointer: true
        SpaceID:
          description: Unique identifier for a space.
          example: 248df4b7-aa70-47b8-a036-33ac447e668d
          format: uuid
          type: string
          x-go-type-skip-optional-pointer: true
        SpaceSlug:
          description: Slug of the Space this entity belongs to. (readonly)
          readOnly: true
          type: string
          x-go-type-skip-optional-pointer: true
        ToolchainType:
          description: ToolchainType specifies the type of toolchain this attribute
            works with.
          example: Kubernetes/YAML
          type: string
          x-go-type-skip-optional-pointer: true
        UpdatedAt:
          description: The timestamp when the entity was last updated in "2023-01-01T12:00:00Z"
            format.
          example: "2025-04-04T11:50:02.95102-07:00"
          format: date-time
          readOnly: true
          type: string
          x-go-type-skip-optional-pointer: true
        Version:
          description: An entity-specific sequence number used for optimistic concurrency
            control. The value read must be sent in calls to Update.
          format: int64
          readOnly: false
          type: integer
          x-go-type-skip-optional-pointer: true
      required:
      - Slug
      - ToolchainType
      - DataType
      type: object
    AttributeCreateOrUpdateResponse:
      properties:
        Attribute:
          $ref: '#/components/schemas/Attribute'
        Error:
          $ref: '#/components/schemas/ResponseError'
      type: object
    AttributeDetails:
      properties:
        BoundLinkID:
          description: ID of the Link that bound this needed path to a provided value
          example: 248df4b7-aa70-47b8-a036-33ac447e668d
          format: uuid
          type: string
          x-go-type-skip-optional-pointer: true
        BoundProvidedProperties:
          additionalProperties:
            type: string
          description: ProvidedProperties of the provided path that was bound to this
            needed path, for cross-link score comparison
          type: object
          x-go-type-skip-optional-pointer: true
        Description:
          description: Description of the attribute
          type: string
          x-go-type-skip-optional-pointer: true
        GetterInvocation:
          $ref: '#/components/schemas/FunctionInvocation'
        IsNeeded:
          description: Whether this attribute is a needed value
          type: boolean
          x-go-type-skip-optional-pointer: true
        IsProvided:
          description: Whether this attribute is a provided value
          type: boolean
          x-go-type-skip-optional-pointer: true
        NeededPreferred:
          additionalProperties:
            type: string
          description: Preferred properties for matching; more matches produce a stronger
            match preference
          type: object
          x-go-type-skip-optional-pointer: true
        NeededRequired:
          additionalProperties:
            type: string
          description: Required properties that a provided value must have in order
            to match
          type: object
          x-go-type-skip-optional-pointer: true
        ProvidedProperties:
          additionalProperties:
            type: string
          description: Key/value properties describing what this provided value offers,
            for matching
          type: object
          x-go-type-skip-optional-pointer: true
        SetterInvocations:
          description: Function invocation used to set the attribute (except for the
            value), if any
          items:
            $ref: '#/components/schemas/FunctionInvocation'
          type: array
          x-go-type-skip-optional-pointer: true
      type: object
    AttributeInfo:
      properties:
        AttributeName:
          description: Name of the registered attribute
          type: string
          x-go-type-skip-optional-pointer: true
        DataType:
          description: Data type if the attribute value.
          type: string
          x-go-type-skip-optional-pointer: true
        Details:
          $ref: '#/components/schemas/AttributeDetails'
        InLiveState:
          description: True if a path in the live state, false if a path in the configuration
            data
          type: boolean
          x-go-type-skip-optional-pointer: true
        Path:
          description: Path of the attribute
          type: string
          x-go-type-skip-optional-pointer: true
        ResourceCategory:
          description: Category of configuration element represented in the configuration
            data; Kubernetes and OpenTofu resources are of category Resource, and
            application configuration files are of category AppConfig
          type: string
          x-go-type-skip-optional-pointer: true
        ResourceMergeID:
          description: Stable identifier (UUID) for a resource stored with the resource
            data that is intended to remain consistent across resource name and scope
            changes and across variants, used to match resources between config data
            documents when computing and patching mutations
          type: string
          x-go-type-skip-optional-pointer: true
        ResourceName:
          description: Name of a resource in the system under management represented
            in the configuration data; Kubernetes resources are represented in the
            form <metadata.namespace>/<metadata.name>; not all ToolchainTypes necessarily
            use '/' as a separator between any scope(s) and name or other client-chosen
            ID
          type: string
          x-go-type-skip-optional-pointer: true
        ResourceNameStableCore:
          description: Name of a resource in the system under management represented
            in the configuration data with generated prefixes and suffixes stripped;
            empty if nothing to strip
          type: string
          x-go-type-skip-optional-pointer: true
        ResourceNameWithoutScope:
          description: Name of a resource in the system under management represented
            in the configuration data, without any uniquifying scope, such as Namespace,
            Project, Account, Region, etc.; Kubernetes resources are represented in
            the form <metadata.name>
          type: string
          x-go-type-skip-optional-pointer: true
        ResourceType:
          description: Type of a resource in the system under management represented
            in the configuration data; Kubernetes resources are represented in the
            form <apiVersion>/<kind> (aka group-version-kind)
          type: string
          x-go-type-skip-optional-pointer: true
      type: object
    AttributeSelector:
      properties:
        Path:
          type: string
          x-go-type-skip-optional-pointer: true
        WhereResource:
          type: string
          x-go-type-skip-optional-pointer: true
      type: object
    AttributeValue:
      properties:
        AttributeName:
          description: Name of the registered attribute
          type: string
          x-go-type-skip-optional-pointer: true
        Comment:
          description: Line comment on the attribute at the specified Path
          type: string
          x-go-type-skip-optional-pointer: true
        DataType:
          description: Data type if the attribute value.
          type: string
          x-go-type-skip-optional-pointer: true
        Details:
          $ref: '#/components/schemas/AttributeDetails'
        FunctionName:
          description: Name of the function invocation corresponding to the output
          type: string
          x-go-type-skip-optional-pointer: true
        InLiveState:
          description: True if a path in the live state, false if a path in the configuration
            data
          type: boolean
          x-go-type-skip-optional-pointer: true
        Index:
          description: Index of the function invocation corresponding to the output.
            Useful in the case that multiple function invocations in the same executor
            call return AttributeValueList output.
          type: integer
          x-go-type-skip-optional-pointer: true
        Issues:
          description: Issues found with the attribute
          items:
            $ref: '#/components/schemas/Issue'
          type: array
          x-go-type-skip-optional-pointer: true
        Path:
          description: Path of the attribute
          type: string
          x-go-type-skip-optional-pointer: true
        ResourceCategory:
          description: Category of configuration element represented in the configuration
            data; Kubernetes and OpenTofu resources are of category Resource, and
            application configuration files are of category AppConfig
          type: string
          x-go-type-skip-optional-pointer: true
        ResourceMergeID:
          description: Stable identifier (UUID) for a resource stored with the resource
            data that is intended to remain consistent across resource name and scope
            changes and across variants, used to match resources between config data
            documents when computing and patching mutations
          type: string
          x-go-type-skip-optional-pointer: true
        ResourceName:
          description: Name of a resource in the system under management represented
            in the configuration data; Kubernetes resources are represented in the
            form <metadata.namespace>/<metadata.name>; not all ToolchainTypes necessarily
            use '/' as a separator between any scope(s) and name or other client-chosen
            ID
          type: string
          x-go-type-skip-optional-pointer: true
        ResourceNameStableCore:
          description: Name of a resource in the system under management represented
            in the configuration data with generated prefixes and suffixes stripped;
            empty if nothing to strip
          type: string
          x-go-type-skip-optional-pointer: true
        ResourceNameWithoutScope:
          description: Name of a resource in the system under management represented
            in the configuration data, without any uniquifying scope, such as Namespace,
            Project, Account, Region, etc.; Kubernetes resources are represented in
            the form <metadata.name>
          type: string
          x-go-type-skip-optional-pointer: true
        ResourceType:
          description: Type of a resource in the system under management represented
            in the configuration data; Kubernetes resources are represented in the
            form <apiVersion>/<kind> (aka group-version-kind)
          type: string
          x-go-type-skip-optional-pointer: true
        Score:
          description: Score of finding attributed to this Path
          type: string
          x-go-type-skip-optional-pointer: true
        Value:
          description: Value of the attribute at the specified Path
          x-go-type-skip-optional-pointer: true
      type: object
    AttributeValueList:
      items:
        $ref: '#/components/schemas/AttributeValue'
      type: array
    Binding:
      properties:
        AttributeName:
          type: string
          x-go-type-skip-optional-pointer: true
        AutoUpdate:
          type: boolean
          x-go-type-skip-optional-pointer: true
        DataType:
          type: string
          x-go-type-skip-optional-pointer: true
        Expression:
          type: string
          x-go-type-skip-optional-pointer: true
        InLiveState:
          type: boolean
          x-go-type-skip-optional-pointer: true
        NeededPath:
          type: string
          x-go-type-skip-optional-pointer: true
        NeededResource:
          $ref: '#/components/schemas/ResourceInfo'
        ProvidedPath:
          type: string
          x-go-type-skip-optional-pointer: true
        ProvidedResource:
          $ref: '#/components/schemas/ResourceInfo'
      type: object
    BindingList:
      items:
        $ref: '#/components/schemas/Binding'
      type: array
    BridgeOption:
      properties:
        DataType:
          description: Data type of the option
          type: string
          x-go-type-skip-optional-pointer: true
        Description:
          description: Description of the option
          type: string
          x-go-type-skip-optional-pointer: true
        Example:
          description: Example value
          type: string
          x-go-type-skip-optional-pointer: true
        Name:
          description: Name of the option in PascalCase
          type: string
          x-go-type-skip-optional-pointer: true
        Required:
          description: Whether the option is required
          type: boolean
          x-go-type-skip-optional-pointer: true
      type: object
    BridgeWorker:
      description: |-
        BridgeWorker represents a bridge worker in ConfigHub.
        A bridge worker is a worker program that connects ConfigHub to external systems and targets.
        It acts as a bridge between ConfigHub and the infrastructure where configurations need
        to be applied. Bridge workers are responsible for executing configuration changes on
        remote targets and reporting status back to ConfigHub.
        When starting a bridge worker program, both the BridgeWorkerID and Secret are
        required for authentication with the ConfigHub server. These credentials allow the
        bridge worker to establish a secure connection and receive configuration actions.
      properties:
        Annotations:
          additionalProperties:
            maxLength: 1024
            pattern: ^(.|\n)*$
            type: string
          description: An optional map of Annotation key/value pairs for tools to
            attach information to entities.
          type: object
          x-go-type-skip-optional-pointer: true
        BridgeWorkerID:
          description: Unique identifier for a Bridge Worker.
          example: 248df4b7-aa70-47b8-a036-33ac447e668d
          format: uuid
          type: string
          x-go-type-skip-optional-pointer: true
        Condition:
          description: Condition represents the worker's readiness state (Ready, NotReady,
            Unresponsive, Disconnected).
          type: string
          x-go-type-skip-optional-pointer: true
        CreatedAt:
          description: The timestamp when the entity was created in "2023-01-01T12:00:00Z"
            format.
          example: "2025-04-04T11:50:02.95102-07:00"
          format: date-time
          readOnly: true
          type: string
          x-go-type-skip-optional-pointer: true
        CursorID:
          description: An auto-incrementing sequence number used for pagination.
          format: int64
          readOnly: true
          type: integer
          x-go-type-skip-optional-pointer: true
        DeleteGates:
          additionalProperties:
            type: boolean
          description: An optional set of gates that, if any is present, will block
            deletion.
          type: object
          x-go-type-skip-optional-pointer: true
        DisplayName:
          description: Friendly name for the entity.
          type: string
          x-go-type-skip-optional-pointer: true
        EntityType:
          description: The type of entity.
          readOnly: true
          type: string
          x-go-type-skip-optional-pointer: true
        IPAddress:
          description: IPAddress is the IP address from which the worker last connected.
          readOnly: true
          type: string
          x-go-type-skip-optional-pointer: true
        Labels:
          additionalProperties:
            maxLength: 128
            pattern: ^[\-_@/#$%&~+=!?.,:;(){}\[\]<>|A-Za-z0-9](([\-_@/#$%&~+=!?.,:;(){}\[\]<>|A-Za-z0-9]|
              )*[\-_@/#$%&~+=!?.,:;(){}\[\]<>|A-Za-z0-9])?$
            type: string
          description: An optional map of Label key/value pairs to specify identifying
            attributes of entities for the purpose of grouping and filtering them.
          type: object
          x-go-type-skip-optional-pointer: true
        LastMessage:
          description: LastMessage contains the last message from the worker (heartbeat
            message or other status).
          readOnly: true
          type: string
          x-go-type-skip-optional-pointer: true
        LastSeenAt:
          description: LastSeenAt is the time the worker was last seen (heartbeat,
            connection, or any event).
          example: 2006-01-02T15:04:05Z07:00
          format: date-time
          readOnly: true
          type: string
          x-go-type-skip-optional-pointer: true
        OrgRole:
          description: Organization-level permission for the BridgeWorker User.
          type: string
          x-go-type-skip-optional-pointer: true
        OrganizationID:
          description: Unique identifier for an organization.
          example: 248df4b7-aa70-47b8-a036-33ac447e668d
          format: uuid
          type: string
          x-go-type-skip-optional-pointer: true
        Permissions:
          $ref: '#/components/schemas/Permissions'
        ProvidedInfo:
          $ref: '#/components/schemas/WorkerInfo'
        Secret:
          description: |-
            Secret is a unique secret token for the bridge worker.
            It's auto-generated when the BridgeWorker entity is created and cannot be modified.
            This field is output-only and used for authentication.
            This secret is required when starting the bridge worker program.
          readOnly: true
          type: string
          x-go-type-skip-optional-pointer: true
        Slug:
          description: Unique URL-safe identifier for the entity.
          maxLength: 128
          pattern: ^[A-Za-z0-9]([\-_.A-Za-z0-9]*[A-Za-z0-9])?$
          type: string
          x-go-type-skip-optional-pointer: true
        SpaceID:
          description: Unique identifier for a space.
          example: 248df4b7-aa70-47b8-a036-33ac447e668d
          format: uuid
          type: string
          x-go-type-skip-optional-pointer: true
        SpaceSlug:
          description: Slug of the Space this entity belongs to. (readonly)
          readOnly: true
          type: string
          x-go-type-skip-optional-pointer: true
        UpdatedAt:
          description: The timestamp when the entity was last updated in "2023-01-01T12:00:00Z"
            format.
          example: "2025-04-04T11:50:02.95102-07:00"
          format: date-time
          readOnly: true
          type: string
          x-go-type-skip-optional-pointer: true
        UserID:
          description: |-
            UserID is the ID of the bot user associated with this bridge worker.
            This user is created when the bridge worker is created and is used for
            audit trails and permissions.
            For legacy workers, this field may be nil (zero UUID).
          example: 248df4b7-aa70-47b8-a036-33ac447e668d
          format: uuid
          readOnly: true
          type: string
        Version:
          description: An entity-specific sequence number used for optimistic concurrency
            control. The value read must be sent in calls to Update.
          format: int64
          readOnly: false
          type: integer
          x-go-type-skip-optional-pointer: true
      required:
      - Slug
      type: object
    BridgeWorkerCreateOrUpdateResponse:
      properties:
        BridgeWorker:
          $ref: '#/components/schemas/BridgeWorker'
        Error:
          $ref: '#/components/schemas/ResponseError'
      type: object
    BridgeWorkerInfo:
      properties:
        SupportedConfigTypes:
          description: Configuration types of the bridges supported by the worker
          items:
            $ref: '#/components/schemas/SupportedConfigType'
          type: array
          x-go-type-skip-optional-pointer: true
      type: object
    BridgeWorkerStatus:
      description: BridgeWorkerStatus represents the status information of a bridge
        worker within the system.
      properties:
        BridgeWorkerID:
          description: Unique identifier for the Bridge Worker.
          example: 248df4b7-aa70-47b8-a036-33ac447e668d
          format: uuid
          type: string
          x-go-type-skip-optional-pointer: true
        BridgeWorkerSlug:
          description: Slug for the Bridge Worker.
          type: string
          x-go-type-skip-optional-pointer: true
        BridgeWorkerStatusID:
          description: BridgeWorkerStatusID is the unique identifier for the bridge
            worker status entry.
          example: 248df4b7-aa70-47b8-a036-33ac447e668d
          format: uuid
          type: string
          x-go-type-skip-optional-pointer: true
        IPAddress:
          description: IPAddress is the IP address from which the bridge worker is
            connecting.
          type: string
          x-go-type-skip-optional-pointer: true
        OrganizationID:
          description: OrganizationID is the unique identifier of the organization
            the bridge worker belongs to.
          example: 248df4b7-aa70-47b8-a036-33ac447e668d
          format: uuid
          type: string
          x-go-type-skip-optional-pointer: true
        SeenAt:
          description: The timestamp when the bridge worker last responded in "2023-01-01T12:00:00Z"
            format.
          example: 2006-01-02T15:04:05Z07:00
          format: date-time
          type: string
          x-go-type-skip-optional-pointer: true
        SpaceID:
          description: SpaceID is the unique identifier of the space the bridge worker
            belongs to.
          example: 248df4b7-aa70-47b8-a036-33ac447e668d
          format: uuid
          type: string
          x-go-type-skip-optional-pointer: true
        Status:
          description: Status indicates the current status of the bridge worker. Possible
            values include Connected, Disconnected, ActionSent, ActionResultReceived.
          type: string
          x-go-type-skip-optional-pointer: true
      type: object
    ChangeSet:
      description: Defines an entity changeset.
      properties:
        Annotations:
          additionalProperties:
            maxLength: 1024
            pattern: ^(.|\n)*$
            type: string
          description: An optional map of Annotation key/value pairs for tools to
            attach information to entities.
          type: object
          x-go-type-skip-optional-pointer: true
        ChangeSetID:
          description: ChangeSetID uniquely identifies a changeset within the system.
          example: 248df4b7-aa70-47b8-a036-33ac447e668d
          format: uuid
          type: string
          x-go-type-skip-optional-pointer: true
        CreatedAt:
          description: The timestamp when the entity was created in "2023-01-01T12:00:00Z"
            format.
          example: "2025-04-04T11:50:02.95102-07:00"
          format: date-time
          readOnly: true
          type: string
          x-go-type-skip-optional-pointer: true
        CursorID:
          description: An auto-incrementing sequence number used for pagination.
          format: int64
          readOnly: true
          type: integer
          x-go-type-skip-optional-pointer: true
        DeleteGates:
          additionalProperties:
            type: boolean
          description: An optional set of gates that, if any is present, will block
            deletion.
          type: object
          x-go-type-skip-optional-pointer: true
        Description:
          description: Description is a human-readable description of the change.
          type: string
          x-go-type-skip-optional-pointer: true
        DisplayName:
          description: Friendly name for the entity.
          type: string
          x-go-type-skip-optional-pointer: true
        EndTagID:
          description: EndTagID is the identifier of the set of revisions that end
            the ChangeSet.
          example: 248df4b7-aa70-47b8-a036-33ac447e668d
          format: uuid
          readOnly: true
          type: string
          x-go-type-skip-optional-pointer: true
        EntityType:
          description: The type of entity.
          readOnly: true
          type: string
          x-go-type-skip-optional-pointer: true
        Labels:
          additionalProperties:
            maxLength: 128
            pattern: ^[\-_@/#$%&~+=!?.,:;(){}\[\]<>|A-Za-z0-9](([\-_@/#$%&~+=!?.,:;(){}\[\]<>|A-Za-z0-9]|
              )*[\-_@/#$%&~+=!?.,:;(){}\[\]<>|A-Za-z0-9])?$
            type: string
          description: An optional map of Label key/value pairs to specify identifying
            attributes of entities for the purpose of grouping and filtering them.
          type: object
          x-go-type-skip-optional-pointer: true
        OrganizationID:
          description: Unique identifier for an organization.
          example: 248df4b7-aa70-47b8-a036-33ac447e668d
          format: uuid
          type: string
          x-go-type-skip-optional-pointer: true
        Slug:
          description: Unique URL-safe identifier for the entity.
          maxLength: 128
          pattern: ^[A-Za-z0-9]([\-_.A-Za-z0-9]*[A-Za-z0-9])?$
          type: string
          x-go-type-skip-optional-pointer: true
        SpaceID:
          description: Unique identifier for a space.
          example: 248df4b7-aa70-47b8-a036-33ac447e668d
          format: uuid
          type: string
          x-go-type-skip-optional-pointer: true
        SpaceSlug:
          description: Slug of the Space this entity belongs to. (readonly)
          readOnly: true
          type: string
          x-go-type-skip-optional-pointer: true
        StartTagID:
          description: StartTagID is the identifier of the set of revisions that begin
            the ChangeSet.
          example: 248df4b7-aa70-47b8-a036-33ac447e668d
          format: uuid
          readOnly: true
          type: string
          x-go-type-skip-optional-pointer: true
        State:
          description: State represents the current state of the ChangeSet.
          readOnly: true
          type: string
          x-go-type-skip-optional-pointer: true
        UpdatedAt:
          description: The timestamp when the entity was last updated in "2023-01-01T12:00:00Z"
            format.
          example: "2025-04-04T11:50:02.95102-07:00"
          format: date-time
          readOnly: true
          type: string
          x-go-type-skip-optional-pointer: true
        Version:
          description: An entity-specific sequence number used for optimistic concurrency
            control. The value read must be sent in calls to Update.
          format: int64
          readOnly: false
          type: integer
          x-go-type-skip-optional-pointer: true
      required:
      - Slug
      type: object
    ChangeSetCreateOrUpdateResponse:
      properties:
        ChangeSet:
          $ref: '#/components/schemas/ChangeSet'
        Error:
          $ref: '#/components/schemas/ResponseError'
      type: object
    Column:
      properties:
        ColumnSource:
          $ref: '#/components/schemas/ColumnSource'
        ColumnType:
          type: string
          x-go-type-skip-optional-pointer: true
        DataType:
          type: string
          x-go-type-skip-optional-pointer: true
        GroupBy:
          type: boolean
          x-go-type-skip-optional-pointer: true
        Name:
          type: string
          x-go-type-skip-optional-pointer: true
        OrderByDirection:
          type: string
          x-go-type-skip-optional-pointer: true
      required:
      - Name
      type: object
    ColumnSource:
      properties:
        DataExpression:
          type: string
          x-go-type-skip-optional-pointer: true
        DataPath:
          $ref: '#/components/schemas/AttributeSelector'
        MetadataAttribute:
          type: string
          x-go-type-skip-optional-pointer: true
        MetadataExpression:
          type: string
          x-go-type-skip-optional-pointer: true
      type: object
    DeleteResponse:
      description: Response for successful delete operation
      properties:
        Error:
          $ref: '#/components/schemas/ResponseError'
        Message:
          description: Response message.
          type: string
          x-go-type-skip-optional-pointer: true
      type: object
    ErrorItem:
      properties:
        Description:
          description: A clear explanation of what went wrong
          type: string
          x-go-type-skip-optional-pointer: true
        Item:
          description: The name of the field, resource, or entity that the error relates
            to
          type: string
          x-go-type-skip-optional-pointer: true
      type: object
    ErrorMetadata:
      properties:
        EntityID:
          description: Optional ID of the entity this error relates to
          type: string
          x-go-type-skip-optional-pointer: true
        EntitySlug:
          description: Optional slug of the entity this error relates to
          type: string
          x-go-type-skip-optional-pointer: true
        EntityType:
          description: Optional type of the entity this error relates to
          type: string
          x-go-type-skip-optional-pointer: true
        Items:
          description: Collection of error details
          items:
            $ref: '#/components/schemas/ErrorItem'
          type: array
          x-go-type-skip-optional-pointer: true
      type: object
    EventMessage:
      properties:
        Data:
          format: base64
          type: string
        Event:
          type: string
      type: object
    ExtendedAttribute:
      properties:
        Attribute:
          $ref: '#/components/schemas/Attribute'
        Error:
          $ref: '#/components/schemas/ResponseError'
        Organization:
          $ref: '#/components/schemas/Organization'
        Space:
          $ref: '#/components/schemas/Space'
      type: object
    ExtendedBridgeWorker:
      properties:
        BridgeWorker:
          $ref: '#/components/schemas/BridgeWorker'
        Error:
          $ref: '#/components/schemas/ResponseError'
        Organization:
          $ref: '#/components/schemas/Organization'
        Space:
          $ref: '#/components/schemas/Space'
        TargetCount:
          format: int64
          type: integer
          x-go-type-skip-optional-pointer: true
      type: object
    ExtendedChangeSet:
      properties:
        ChangeSet:
          $ref: '#/components/schemas/ChangeSet'
        EndTag:
          $ref: '#/components/schemas/Tag'
        Error:
          $ref: '#/components/schemas/ResponseError'
        Organization:
          $ref: '#/components/schemas/Organization'
        Space:
          $ref: '#/components/schemas/Space'
        StartTag:
          $ref: '#/components/schemas/Tag'
      type: object
    ExtendedFilter:
      properties:
        Error:
          $ref: '#/components/schemas/ResponseError'
        Filter:
          $ref: '#/components/schemas/Filter'
        FromSpace:
          $ref: '#/components/schemas/Space'
        Organization:
          $ref: '#/components/schemas/Organization'
        Space:
          $ref: '#/components/schemas/Space'
      type: object
    ExtendedInvocation:
      properties:
        BridgeWorker:
          $ref: '#/components/schemas/BridgeWorker'
        Error:
          $ref: '#/components/schemas/ResponseError'
        Invocation:
          $ref: '#/components/schemas/Invocation'
        Organization:
          $ref: '#/components/schemas/Organization'
        Space:
          $ref: '#/components/schemas/Space'
      type: object
    ExtendedLink:
      properties:
        Error:
          $ref: '#/components/schemas/ResponseError'
        FromUnit:
          $ref: '#/components/schemas/Unit'
        Link:
          $ref: '#/components/schemas/Link'
        Organization:
          $ref: '#/components/schemas/Organization'
        Space:
          $ref: '#/components/schemas/Space'
        ToSpace:
          $ref: '#/components/schemas/Space'
        ToUnit:
          $ref: '#/components/schemas/Unit'
      type: object
    ExtendedMutation:
      properties:
        Error:
          $ref: '#/components/schemas/ResponseError'
        Invocation:
          $ref: '#/components/schemas/Invocation'
        Link:
          $ref: '#/components/schemas/Link'
        MergeSource:
          $ref: '#/components/schemas/Unit'
        Mutation:
          $ref: '#/components/schemas/Mutation'
        Organization:
          $ref: '#/components/schemas/Organization'
        Revision:
          $ref: '#/components/schemas/Revision'
        Space:
          $ref: '#/components/schemas/Space'
        Trigger:
          $ref: '#/components/schemas/Trigger'
        Unit:
          $ref: '#/components/schemas/Unit'
      type: object
    ExtendedRevision:
      properties:
        ChangeSet:
          $ref: '#/components/schemas/ChangeSet'
        Error:
          $ref: '#/components/schemas/ResponseError'
        Organization:
          $ref: '#/components/schemas/Organization'
        Revision:
          $ref: '#/components/schemas/Revision'
        Space:
          $ref: '#/components/schemas/Space'
        Tags:
          items:
            $ref: '#/components/schemas/Tag'
          type: array
          x-go-type-skip-optional-pointer: true
        Unit:
          $ref: '#/components/schemas/Unit'
        User:
          $ref: '#/components/schemas/User'
      type: object
    ExtendedSpace:
      properties:
        AttributeFilter:
          $ref: '#/components/schemas/Filter'
        Attributes:
          items:
            $ref: '#/components/schemas/Attribute'
          type: array
          x-go-type-skip-optional-pointer: true
        Error:
          $ref: '#/components/schemas/ResponseError'
        GatedUnitCount:
          format: int64
          type: integer
          x-go-type-skip-optional-pointer: true
        IncompleteApplyUnitCount:
          format: int64
          type: integer
          x-go-type-skip-optional-pointer: true
        Organization:
          $ref: '#/components/schemas/Organization'
        Space:
          $ref: '#/components/schemas/Space'
        TargetCountByToolchainType:
          additionalProperties:
            type: integer
          nullable: true
          type: object
          x-go-type-skip-optional-pointer: true
        TotalAttributeCount:
          format: int64
          type: integer
          x-go-type-skip-optional-pointer: true
        TotalBridgeWorkerCount:
          format: int64
          type: integer
          x-go-type-skip-optional-pointer: true
        TotalChangeSetCount:
          format: int64
          type: integer
          x-go-type-skip-optional-pointer: true
        TotalFilterCount:
          format: int64
          type: integer
          x-go-type-skip-optional-pointer: true
        TotalInvocationCount:
          format: int64
          type: integer
          x-go-type-skip-optional-pointer: true
        TotalLinkCount:
          format: int64
          type: integer
          x-go-type-skip-optional-pointer: true
        TotalTagCount:
          format: int64
          type: integer
          x-go-type-skip-optional-pointer: true
        TotalUnitCount:
          format: int64
          type: integer
          x-go-type-skip-optional-pointer: true
        TotalViewCount:
          format: int64
          type: integer
          x-go-type-skip-optional-pointer: true
        TriggerCountByEventType:
          additionalProperties:
            type: integer
          nullable: true
          type: object
          x-go-type-skip-optional-pointer: true
        TriggerFilter:
          $ref: '#/components/schemas/Filter'
        Triggers:
          items:
            $ref: '#/components/schemas/Trigger'
          type: array
          x-go-type-skip-optional-pointer: true
        UnappliedUnitCount:
          format: int64
          type: integer
          x-go-type-skip-optional-pointer: true
        UnapprovedUnitCount:
          format: int64
          type: integer
          x-go-type-skip-optional-pointer: true
        UnlinkedUnitCount:
          format: int64
          type: integer
          x-go-type-skip-optional-pointer: true
        UpgradableUnitCount:
          format: int64
          type: integer
          x-go-type-skip-optional-pointer: true
      type: object
    ExtendedTag:
      properties:
        ChangeSet:
          $ref: '#/components/schemas/ChangeSet'
        Error:
          $ref: '#/components/schemas/ResponseError'
        Organization:
          $ref: '#/components/schemas/Organization'
        Space:
          $ref: '#/components/schemas/Space'
        Tag:
          $ref: '#/components/schemas/Tag'
      type: object
    ExtendedTarget:
      properties:
        BridgeWorker:
          $ref: '#/components/schemas/BridgeWorker'
        Error:
          $ref: '#/components/schemas/ResponseError'
        Organization:
          $ref: '#/components/schemas/Organization'
        Space:
          $ref: '#/components/schemas/Space'
        Target:
          $ref: '#/components/schemas/Target'
        TriggerFilter:
          $ref: '#/components/schemas/Filter'
        Triggers:
          items:
            $ref: '#/components/schemas/Trigger'
          type: array
          x-go-type-skip-optional-pointer: true
      type: object
    ExtendedTrigger:
      properties:
        BridgeWorker:
          $ref: '#/components/schemas/BridgeWorker'
        Error:
          $ref: '#/components/schemas/ResponseError'
        Invocation:
          $ref: '#/components/schemas/Invocation'
        Organization:
          $ref: '#/components/schemas/Organization'
        Space:
          $ref: '#/components/schemas/Space'
        Trigger:
          $ref: '#/components/schemas/Trigger'
        UnitFilter:
          $ref: '#/components/schemas/Filter'
      type: object
    ExtendedUnit:
      description: Unit with capability to extend additional related entities.
      properties:
        ApprovedBy:
          description: the users that have approved the latest revision of the config
            data.
          items:
            $ref: '#/components/schemas/User'
          type: array
          x-go-type-skip-optional-pointer: true
        BridgeWorker:
          $ref: '#/components/schemas/BridgeWorker'
        ChangeSet:
          $ref: '#/components/schemas/ChangeSet'
        Error:
          $ref: '#/components/schemas/ResponseError'
        FromLink:
          items:
            $ref: '#/components/schemas/Link'
          type: array
          x-go-type-skip-optional-pointer: true
        HeadMutation:
          $ref: '#/components/schemas/Mutation'
        HeadRevision:
          $ref: '#/components/schemas/Revision'
        LastAppliedRevision:
          $ref: '#/components/schemas/Revision'
        LatestUnitEvent:
          $ref: '#/components/schemas/UnitEvent'
        LiveRevision:
          $ref: '#/components/schemas/Revision'
        Organization:
          $ref: '#/components/schemas/Organization'
        PreviousLiveRevision:
          $ref: '#/components/schemas/Revision'
        Space:
          $ref: '#/components/schemas/Space'
        Target:
          $ref: '#/components/schemas/Target'
        Unit:
          $ref: '#/components/schemas/Unit'
        UnitStatus:
          $ref: '#/components/schemas/UnitStatus'
        UpstreamSpace:
          $ref: '#/components/schemas/Space'
        UpstreamUnit:
          $ref: '#/components/schemas/Unit'
        View:
          $ref: '#/components/schemas/View'
        ViewColumns:
          items:
            $ref: '#/components/schemas/ViewColumn'
          type: array
          x-go-type-skip-optional-pointer: true
      type: object
    ExtendedView:
      properties:
        Error:
          $ref: '#/components/schemas/ResponseError'
        Filter:
          $ref: '#/components/schemas/Filter'
        Organization:
          $ref: '#/components/schemas/Organization'
        Space:
          $ref: '#/components/schemas/Space'
        View:
          $ref: '#/components/schemas/View'
      type: object
    Filter:
      description: Defines an entity filter.
      properties:
        Annotations:
          additionalProperties:
            maxLength: 1024
            pattern: ^(.|\n)*$
            type: string
          description: An optional map of Annotation key/value pairs for tools to
            attach information to entities.
          type: object
          x-go-type-skip-optional-pointer: true
        CreatedAt:
          description: The timestamp when the entity was created in "2023-01-01T12:00:00Z"
            format.
          example: "2025-04-04T11:50:02.95102-07:00"
          format: date-time
          readOnly: true
          type: string
          x-go-type-skip-optional-pointer: true
        CursorID:
          description: An auto-incrementing sequence number used for pagination.
          format: int64
          readOnly: true
          type: integer
          x-go-type-skip-optional-pointer: true
        DeleteGates:
          additionalProperties:
            type: boolean
          description: An optional set of gates that, if any is present, will block
            deletion.
          type: object
          x-go-type-skip-optional-pointer: true
        DisplayName:
          description: Friendly name for the entity.
          type: string
          x-go-type-skip-optional-pointer: true
        EntityType:
          description: The type of entity.
          readOnly: true
          type: string
          x-go-type-skip-optional-pointer: true
        FilterID:
          description: FilterID uniquely identifies a filter within the system.
          example: 248df4b7-aa70-47b8-a036-33ac447e668d
          format: uuid
          type: string
          x-go-type-skip-optional-pointer: true
        From:
          description: From specifies the type of entity (Unit, Space, etc.) to filter,
            in PascalCase.
          type: string
          x-go-type-skip-optional-pointer: true
        FromSpaceID:
          description: FromSpaceID optionally specifies a Space to filter within.
            Only relevant for spaced entity spaces. (optional)
          example: 248df4b7-aa70-47b8-a036-33ac447e668d
          format: uuid
          type: string
        Hash:
          description: SHA256 hash of the filter parameters encoded as hexadecimal.
            (readonly)
          readOnly: true
          type: string
          x-go-type-skip-optional-pointer: true
        Labels:
          additionalProperties:
            maxLength: 128
            pattern: ^[\-_@/#$%&~+=!?.,:;(){}\[\]<>|A-Za-z0-9](([\-_@/#$%&~+=!?.,:;(){}\[\]<>|A-Za-z0-9]|
              )*[\-_@/#$%&~+=!?.,:;(){}\[\]<>|A-Za-z0-9])?$
            type: string
          description: An optional map of Label key/value pairs to specify identifying
            attributes of entities for the purpose of grouping and filtering them.
          type: object
          x-go-type-skip-optional-pointer: true
        OrganizationID:
          description: Unique identifier for an organization.
          example: 248df4b7-aa70-47b8-a036-33ac447e668d
          format: uuid
          type: string
          x-go-type-skip-optional-pointer: true
        ResourceType:
          description: Resource type to match for the desired ToolchainType, for example
            apps/v1/Deployment. Valid only for Units. (optional)
          type: string
          x-go-type-skip-optional-pointer: true
        Slug:
          description: Unique URL-safe identifier for the entity.
          maxLength: 128
          pattern: ^[A-Za-z0-9]([\-_.A-Za-z0-9]*[A-Za-z0-9])?$
          type: string
          x-go-type-skip-optional-pointer: true
        SpaceID:
          description: Unique identifier for a space.
          example: 248df4b7-aa70-47b8-a036-33ac447e668d
          format: uuid
          type: string
          x-go-type-skip-optional-pointer: true
        SpaceSlug:
          description: Slug of the Space this entity belongs to. (readonly)
          readOnly: true
          type: string
          x-go-type-skip-optional-pointer: true
        UpdatedAt:
          description: The timestamp when the entity was last updated in "2023-01-01T12:00:00Z"
            format.
          example: "2025-04-04T11:50:02.95102-07:00"
          format: date-time
          readOnly: true
          type: string
          x-go-type-skip-optional-pointer: true
        Version:
          description: An entity-specific sequence number used for optimistic concurrency
            control. The value read must be sent in calls to Update.
          format: int64
          readOnly: false
          type: integer
          x-go-type-skip-optional-pointer: true
        Where:
          description: Where specifices the where filter expression in the syntax
            used in list and search API query parameters. (optional)
          type: string
          x-go-type-skip-optional-pointer: true
        WhereData:
          description: WhereData specifies a filter expression for configuration data.
            Valid only for Units. (optional)
          type: string
          x-go-type-skip-optional-pointer: true
      required:
      - Slug
      - From
      type: object
    FilterCreateOrUpdateResponse:
      properties:
        Error:
          $ref: '#/components/schemas/ResponseError'
        Filter:
          $ref: '#/components/schemas/Filter'
      type: object
    FunctionArgument:
      properties:
        Evaluator:
          type: string
        ParameterName:
          type: string
        Value:
          anyOf:
          - type: string
          - format: int64
            type: integer
          - type: boolean
      type: object
    FunctionInvocation:
      properties:
        Arguments:
          description: Function arguments
          items:
            $ref: '#/components/schemas/FunctionArgument'
          nullable: true
          type: array
          x-go-type-skip-optional-pointer: true
        FunctionName:
          description: Function name
          type: string
          x-go-type-skip-optional-pointer: true
      type: object
    FunctionInvocationList:
      items:
        $ref: '#/components/schemas/FunctionInvocation'
      nullable: true
      type: array
    FunctionInvocationsRequest:
      description: FunctionInvocationsRequest represents a request to invoke a list
        of functions on the configuration data of the matching Units or Revision.
      properties:
        BridgeWorkerID:
          description: BridgeWorkerID is the identifier of the associated worker that
            will execute these functions.
          example: 248df4b7-aa70-47b8-a036-33ac447e668d
          format: uuid
          type: string
        ChangeDescription:
          description: ChangeDescription is a description of the change being made,
            if any.
          type: string
          x-go-type-skip-optional-pointer: true
        FunctionInvocations:
          $ref: '#/components/schemas/FunctionInvocationList'
        Invocations:
          description: 'Invocations is a list of Invocation IDs to execute. The invocations
            must be within the same Organization. Invocations will be executed after
            the FunctionInvocations list. Functions are grouped by executor (built-in
            vs bridge worker) and executed in phases: general mutating functions first,
            then final mutating functions (like ensure-context), then validating functions.
            Functions that don''t match the unit''s toolchain type are ignored.'
          items:
            $ref: '#/components/schemas/UUID'
          type: array
          x-go-type-skip-optional-pointer: true
        NumFilters:
          description: NumFilters is the number of validating functions from the FunctionInvocations
            to treat as filters for the remaining functions in the list. In the case
            that the validation function does not pass, stop and don't execute the
            remaining functions, but don't report an error.
          type: integer
          x-go-type-skip-optional-pointer: true
        OnLiveState:
          description: OnLiveState indicates that the functions should be invoked
            on the LiveState rather than the Data.
          type: boolean
          x-go-type-skip-optional-pointer: true
        StopOnError:
          description: StopOnError indicates whether to stop executing functions from
            the FunctionInvocations list on the first error, or to execute all of
            the functions and return all of the errors. Note that this applies to
            each Unit or Revision individually rather than all of the entities on
            which the functions are being invoked.
          type: boolean
          x-go-type-skip-optional-pointer: true
        ToolchainType:
          description: ToolchainType specifies the type of toolchain for these function
            invocations. This determines which configuration formats the functions
            can process. If OnLiveState is false, it must match the ToolchainType
            of the Units. If OnLiveState is true, it must match the LiveStateType
            of the Targets of the Units.
          type: string
          x-go-type-skip-optional-pointer: true
        Triggers:
          description: 'Triggers is a list of Trigger IDs to execute. The triggers
            must be within the same Organization. Triggers will be executed after
            the FunctionInvocations list. Functions are grouped by executor (built-in
            vs bridge worker) and executed in phases: general mutating functions first,
            then final mutating functions (like ensure-context), then validating functions.
            Functions that don''t match the unit''s toolchain type are ignored.'
          items:
            $ref: '#/components/schemas/UUID'
          type: array
          x-go-type-skip-optional-pointer: true
        UpdateApplyGates:
          type: boolean
          x-go-type-skip-optional-pointer: true
        WhereResource:
          description: WhereResource restricts which resources functions operate on
            using ConfigHub metadata path expressions (ConfigHub.ResourceName, ConfigHub.ResourceNameWithoutScope,
            ConfigHub.ResourceType, ConfigHub.ResourceCategory).
          type: string
          x-go-type-skip-optional-pointer: true
      type: object
    FunctionInvocationsResponse:
      properties:
        ConfigData:
          description: The resulting configuration data, potentially mutated
          format: base64
          type: string
          x-go-type-skip-optional-pointer: true
        Error:
          $ref: '#/components/schemas/ResponseError'
        HasNewMutations:
          description: Functions produced new mutations (of type other than None)
          type: boolean
          x-go-type-skip-optional-pointer: true
        Mutations:
          $ref: '#/components/schemas/ResourceMutationList'
        Mutators:
          description: List of function invocation indices that resulted in mutations
          items:
            type: integer
          nullable: true
          type: array
          x-go-type-skip-optional-pointer: true
        OrganizationID:
          description: ID of the Unit's Organization
          example: 248df4b7-aa70-47b8-a036-33ac447e668d
          format: uuid
          type: string
          x-go-type-skip-optional-pointer: true
        Outputs:
          additionalProperties:
            format: base64
            type: string
          description: Map of output types to their corresponding output data as embedded
            JSON
          nullable: true
          type: object
          x-go-type-skip-optional-pointer: true
        RevisionID:
          description: ID of the Revision the configuration data is associated with
          example: 248df4b7-aa70-47b8-a036-33ac447e668d
          format: uuid
          type: string
          x-go-type-skip-optional-pointer: true
        SpaceID:
          description: ID of the Unit's Space
          example: 248df4b7-aa70-47b8-a036-33ac447e668d
          format: uuid
          type: string
          x-go-type-skip-optional-pointer: true
        SpaceSlug:
          description: Slug of the Unit's Space
          type: string
          x-go-type-skip-optional-pointer: true
        Success:
          description: True if all functions executed successfully
          type: boolean
          x-go-type-skip-optional-pointer: true
        UnitID:
          description: ID of the Unit the configuration data is associated with
          example: 248df4b7-aa70-47b8-a036-33ac447e668d
          format: uuid
          type: string
          x-go-type-skip-optional-pointer: true
        UnitSlug:
          description: Slug of the Unit
          type: string
          x-go-type-skip-optional-pointer: true
      type: object
    FunctionOutput:
      properties:
        Description:
          description: Description of the result
          type: string
          x-go-type-skip-optional-pointer: true
        OutputType:
          description: Data type of the JSON embedded in the output
          type: string
          x-go-type-skip-optional-pointer: true
        ResultName:
          description: Name of the result in kabob-case
          type: string
          x-go-type-skip-optional-pointer: true
        Schema:
          $ref: '#/components/schemas/Schema'
      type: object
    FunctionParameter:
      properties:
        DataType:
          description: Data type of the parameter
          type: string
          x-go-type-skip-optional-pointer: true
        Description:
          description: Description of the parameter
          type: string
          x-go-type-skip-optional-pointer: true
        EnumValues:
          description: List of valid enum values; applies to enum parameters
          items:
            type: string
          type: array
          x-go-type-skip-optional-pointer: true
        Example:
          description: Example value
          type: string
          x-go-type-skip-optional-pointer: true
        Max:
          description: Maximum allowed value; applies to int parameters
          nullable: true
          type: integer
          x-go-type-skip-optional-pointer: true
        Min:
          description: Minimum allowed value; applies to int parameters
          nullable: true
          type: integer
          x-go-type-skip-optional-pointer: true
        ParameterName:
          description: Name of the parameter in kabob-case
          type: string
          x-go-type-skip-optional-pointer: true
        Regexp:
          description: Regular expression matching valid values; applies to string
            parameters
          type: string
          x-go-type-skip-optional-pointer: true
        Required:
          description: Whether the parameter is required
          type: boolean
          x-go-type-skip-optional-pointer: true
        Schema:
          $ref: '#/components/schemas/Schema'
      type: object
    FunctionSignature:
      properties:
        AffectedResourceTypes:
          description: Resource types the function applies to; * if all
          items:
            type: string
          type: array
          x-go-type-skip-optional-pointer: true
        AttributeName:
          description: Attribute corresponding to registered paths, if a path visitor;
            optional
          type: string
          x-go-type-skip-optional-pointer: true
        Description:
          description: Description of the function
          type: string
          x-go-type-skip-optional-pointer: true
        FunctionName:
          description: Name of the function in kabob-case
          type: string
          x-go-type-skip-optional-pointer: true
        FunctionType:
          description: 'Implementation pattern of the function: PathVisitor or Custom'
          type: string
          x-go-type-skip-optional-pointer: true
        Hermetic:
          description: Does not call other systems
          type: boolean
          x-go-type-skip-optional-pointer: true
        Idempotent:
          description: Will return the same result if invoked again
          type: boolean
          x-go-type-skip-optional-pointer: true
        Mutating:
          description: May change the configuration data
          type: boolean
          x-go-type-skip-optional-pointer: true
        OtherDataExpected:
          description: If non-empty, specification of what source(s) are expected
            in OtherData; if empty, OtherData is not used
          items:
            type: string
          type: array
          x-go-type-skip-optional-pointer: true
        OutputInfo:
          $ref: '#/components/schemas/FunctionOutput'
        Parameters:
          description: Function parameters, in order
          items:
            $ref: '#/components/schemas/FunctionParameter'
          nullable: true
          type: array
          x-go-type-skip-optional-pointer: true
        RequiredParameters:
          description: Number of required parameters
          type: integer
          x-go-type-skip-optional-pointer: true
        ToolchainType:
          description: Toolchain under which the function is registered
          type: string
          x-go-type-skip-optional-pointer: true
        Validating:
          description: Returns ValidationResult
          type: boolean
          x-go-type-skip-optional-pointer: true
        VarArgs:
          description: Last parameter may be repeated
          type: boolean
          x-go-type-skip-optional-pointer: true
      type: object
    FunctionWorkerInfo:
      properties:
        SupportedFunctions:
          additionalProperties:
            additionalProperties:
              $ref: '#/components/schemas/FunctionSignature'
            type: object
          description: Signatures of supported functions by ToolchainType
          nullable: true
          type: object
          x-go-type-skip-optional-pointer: true
        ToolchainTypes:
          description: Supported ToolchainTypes
          items:
            type: string
          nullable: true
          type: array
          x-go-type-skip-optional-pointer: true
      type: object
    ImportFilter:
      properties:
        Operator:
          description: Operator specifies how to apply the filter (include, exclude,
            equals, contains, matches)
          type: string
          x-go-type-skip-optional-pointer: true
        Type:
          description: Type specifies the filter type (namespace, label, resource_type,
            etc.)
          type: string
          x-go-type-skip-optional-pointer: true
        Values:
          description: Values specifies the filter values
          items:
            type: string
          type: array
          x-go-type-skip-optional-pointer: true
      type: object
    ImportOptions:
      additionalProperties: {}
      type: object
    ImportRequest:
      properties:
        Filters:
          description: List of ImportFilter expression clauses. Mutually exclusive
            with Where.
          items:
            $ref: '#/components/schemas/ImportFilter'
          type: array
          x-go-type-skip-optional-pointer: true
        Options:
          $ref: '#/components/schemas/ImportOptions'
        ResourceInfoList:
          $ref: '#/components/schemas/ResourceInfoList'
        Where:
          description: 'Where specifies a unified resource filter expression for import
            resources and options. It uses SQL-inspired syntax, similar to the where-filter
            function. Supports conjunctions with AND. String operators: =, !=, <,
            >, <=, >=, LIKE, ILIKE, ~~, !~~, ~, ~*, !~, !~*. Pattern matching with
            LIKE/ILIKE uses % and _ wildcards. Regex operators (~, ~*, !~, !~*) support
            POSIX regular expressions. Kubernetes-specific filters include import.include_system
            for system namespaces like kube-system, import.include_cluster for cluster-scoped
            resources like ClusterRole, and import.include_custom for custom resource
            types.'
          type: string
          x-go-type-skip-optional-pointer: true
      type: object
    Invocation:
      description: Defines a function invocation.
      properties:
        Annotations:
          additionalProperties:
            maxLength: 1024
            pattern: ^(.|\n)*$
            type: string
          description: An optional map of Annotation key/value pairs for tools to
            attach information to entities.
          type: object
          x-go-type-skip-optional-pointer: true
        Arguments:
          description: Function arguments
          items:
            $ref: '#/components/schemas/FunctionArgument'
          nullable: true
          type: array
          x-go-type-skip-optional-pointer: true
        BridgeWorkerID:
          description: Unique identifier for a Bridge Worker to execute the function
            specified by the Invocation. If unspecified, use the builtin function
            executor.
          example: 248df4b7-aa70-47b8-a036-33ac447e668d
          format: uuid
          type: string
        CreatedAt:
          description: The timestamp when the entity was created in "2023-01-01T12:00:00Z"
            format.
          example: "2025-04-04T11:50:02.95102-07:00"
          format: date-time
          readOnly: true
          type: string
          x-go-type-skip-optional-pointer: true
        CursorID:
          description: An auto-incrementing sequence number used for pagination.
          format: int64
          readOnly: true
          type: integer
          x-go-type-skip-optional-pointer: true
        DeleteGates:
          additionalProperties:
            type: boolean
          description: An optional set of gates that, if any is present, will block
            deletion.
          type: object
          x-go-type-skip-optional-pointer: true
        DisplayName:
          description: Friendly name for the entity.
          type: string
          x-go-type-skip-optional-pointer: true
        EntityType:
          description: The type of entity.
          readOnly: true
          type: string
          x-go-type-skip-optional-pointer: true
        FunctionName:
          description: Function name
          type: string
          x-go-type-skip-optional-pointer: true
        Hash:
          description: SHA256 hash of the function name and arguments encoded as hexadecimal.
          readOnly: true
          type: string
          x-go-type-skip-optional-pointer: true
        InvocationID:
          description: InvocationID uniquely identifies a invocation within the system.
          example: 248df4b7-aa70-47b8-a036-33ac447e668d
          format: uuid
          type: string
          x-go-type-skip-optional-pointer: true
        Labels:
          additionalProperties:
            maxLength: 128
            pattern: ^[\-_@/#$%&~+=!?.,:;(){}\[\]<>|A-Za-z0-9](([\-_@/#$%&~+=!?.,:;(){}\[\]<>|A-Za-z0-9]|
              )*[\-_@/#$%&~+=!?.,:;(){}\[\]<>|A-Za-z0-9])?$
            type: string
          description: An optional map of Label key/value pairs to specify identifying
            attributes of entities for the purpose of grouping and filtering them.
          type: object
          x-go-type-skip-optional-pointer: true
        OrganizationID:
          description: Unique identifier for an organization.
          example: 248df4b7-aa70-47b8-a036-33ac447e668d
          format: uuid
          type: string
          x-go-type-skip-optional-pointer: true
        Slug:
          description: Unique URL-safe identifier for the entity.
          maxLength: 128
          pattern: ^[A-Za-z0-9]([\-_.A-Za-z0-9]*[A-Za-z0-9])?$
          type: string
          x-go-type-skip-optional-pointer: true
        SpaceID:
          description: Unique identifier for a space.
          example: 248df4b7-aa70-47b8-a036-33ac447e668d
          format: uuid
          type: string
          x-go-type-skip-optional-pointer: true
        SpaceSlug:
          description: Slug of the Space this entity belongs to. (readonly)
          readOnly: true
          type: string
          x-go-type-skip-optional-pointer: true
        ToolchainType:
          description: "ToolchainType specifies the type of toolchain this invocation
            works with.\n\t\tThis determines which configuration formats the invocation
            can process."
          example: Kubernetes/YAML
          required:
          - "true"
          type: string
          x-go-type-skip-optional-pointer: true
        UpdatedAt:
          description: The timestamp when the entity was last updated in "2023-01-01T12:00:00Z"
            format.
          example: "2025-04-04T11:50:02.95102-07:00"
          format: date-time
          readOnly: true
          type: string
          x-go-type-skip-optional-pointer: true
        Version:
          description: An entity-specific sequence number used for optimistic concurrency
            control. The value read must be sent in calls to Update.
          format: int64
          readOnly: false
          type: integer
          x-go-type-skip-optional-pointer: true
      required:
      - Slug
      - ToolchainType
      type: object
    InvocationCreateOrUpdateResponse:
      properties:
        Error:
          $ref: '#/components/schemas/ResponseError'
        Invocation:
          $ref: '#/components/schemas/Invocation'
      type: object
    Issue:
      properties:
        Identifier:
          description: Identifier for the kind of issue found, such as a number, alphanumeric
            code, or policy/rule name. Use especially with validation functions that
            check multiple policies/rules.
          type: string
          x-go-type-skip-optional-pointer: true
        Message:
          description: A more detailed and specific explanation of the issue found
          type: string
          x-go-type-skip-optional-pointer: true
      type: object
    Link:
      description: |-
        Link connects two config Units in a dependency / producer-consumer relationship.
        A Link indicates that selected config data from the upstream To Unit (the producer)
        should be propagated to the downstream From Unit (the consumer).
        Links must be created in the same Space as the From Unit.
        They also imply an ordering when Applied or Destroyed as a group.
      properties:
        Annotations:
          additionalProperties:
            maxLength: 1024
            pattern: ^(.|\n)*$
            type: string
          description: An optional map of Annotation key/value pairs for tools to
            attach information to entities.
          type: object
          x-go-type-skip-optional-pointer: true
        AutoUpdate:
          description: Automatically update the downstream Unit when the upstream
            Unit changes. Always treated as true for links with no UpdateType, for
            backward compatibility.
          type: boolean
          x-go-type-skip-optional-pointer: true
        Bindings:
          $ref: '#/components/schemas/BindingList'
        CreatedAt:
          description: The timestamp when the entity was created in "2023-01-01T12:00:00Z"
            format.
          example: "2025-04-04T11:50:02.95102-07:00"
          format: date-time
          readOnly: true
          type: string
          x-go-type-skip-optional-pointer: true
        CursorID:
          description: An auto-incrementing sequence number used for pagination.
          format: int64
          readOnly: true
          type: integer
          x-go-type-skip-optional-pointer: true
        DeleteGates:
          additionalProperties:
            type: boolean
          description: An optional set of gates that, if any is present, will block
            deletion.
          type: object
          x-go-type-skip-optional-pointer: true
        DisplayName:
          description: Friendly name for the entity.
          type: string
          x-go-type-skip-optional-pointer: true
        DownstreamLastMergedRevisionNum:
          description: The sequence number of the revision of the downstream unit
            created by the last merge.
          format: int64
          type: integer
          x-go-type-skip-optional-pointer: true
        EntityType:
          description: The type of entity.
          readOnly: true
          type: string
          x-go-type-skip-optional-pointer: true
        FromUnitID:
          description: Unique identifier of the downstream (consumer) Unit. Links
            must be in the same space as the source unit.
          example: 248df4b7-aa70-47b8-a036-33ac447e668d
          format: uuid
          type: string
          x-go-type-skip-optional-pointer: true
        Labels:
          additionalProperties:
            maxLength: 128
            pattern: ^[\-_@/#$%&~+=!?.,:;(){}\[\]<>|A-Za-z0-9](([\-_@/#$%&~+=!?.,:;(){}\[\]<>|A-Za-z0-9]|
              )*[\-_@/#$%&~+=!?.,:;(){}\[\]<>|A-Za-z0-9])?$
            type: string
          description: An optional map of Label key/value pairs to specify identifying
            attributes of entities for the purpose of grouping and filtering them.
          type: object
          x-go-type-skip-optional-pointer: true
        LinkID:
          description: Unique identifier for a Link.
          example: 248df4b7-aa70-47b8-a036-33ac447e668d
          format: uuid
          type: string
          x-go-type-skip-optional-pointer: true
        OrganizationID:
          description: Unique identifier for an organization.
          example: 248df4b7-aa70-47b8-a036-33ac447e668d
          format: uuid
          type: string
          x-go-type-skip-optional-pointer: true
        Slug:
          description: Unique URL-safe identifier for the entity.
          maxLength: 128
          pattern: ^[A-Za-z0-9]([\-_.A-Za-z0-9]*[A-Za-z0-9])?$
          type: string
          x-go-type-skip-optional-pointer: true
        SpaceID:
          description: Unique identifier for a space.
          example: 248df4b7-aa70-47b8-a036-33ac447e668d
          format: uuid
          type: string
          x-go-type-skip-optional-pointer: true
        SpaceSlug:
          description: Slug of the Space this entity belongs to. (readonly)
          readOnly: true
          type: string
          x-go-type-skip-optional-pointer: true
        ToSpaceID:
          description: Unique identifier of the Space of the upstream Unit.
          example: 248df4b7-aa70-47b8-a036-33ac447e668d
          format: uuid
          type: string
          x-go-type-skip-optional-pointer: true
        ToUnitID:
          description: Unique identifier of the upstream (producer) Unit.
          example: 248df4b7-aa70-47b8-a036-33ac447e668d
          format: uuid
          type: string
          x-go-type-skip-optional-pointer: true
        UpdateType:
          description: The ConfigHub operation performed using this Link. Valid values
            are NeedsProvides, MergeUnits, and UpgradeUnit. If empty, then assumed
            to be NeedsProvides. UpgradeUnit is like MergeUnits but also keeps the
            downstream unit's UpstreamRevision fields in sync.
          type: string
          x-go-type-skip-optional-pointer: true
        UpdatedAt:
          description: The timestamp when the entity was last updated in "2023-01-01T12:00:00Z"
            format.
          example: "2025-04-04T11:50:02.95102-07:00"
          format: date-time
          readOnly: true
          type: string
          x-go-type-skip-optional-pointer: true
        UpstreamLastMergedRevisionNum:
          description: The sequence number of the last merged upstream change. When
            UseLiveState is false, this is the RevisionNum of the last merged revision.
            When UseLiveState is true, this is the UnitActionNum of the last merged
            Apply action, since applying the same revision multiple times can produce
            different LiveState.
          format: int64
          type: integer
          x-go-type-skip-optional-pointer: true
        UpstreamLinkID:
          description: Link ID of the link this link was cloned from (if any).
          example: 248df4b7-aa70-47b8-a036-33ac447e668d
          format: uuid
          readOnly: true
          type: string
        UpstreamOrganizationID:
          description: Organization ID of the link this link was cloned from (if any).
          example: 248df4b7-aa70-47b8-a036-33ac447e668d
          format: uuid
          readOnly: true
          type: string
        UpstreamSpaceID:
          description: Space ID of the link this link was cloned from (if any).
          example: 248df4b7-aa70-47b8-a036-33ac447e668d
          format: uuid
          readOnly: true
          type: string
        UseLiveState:
          description: Take data from the LiveState of the upstream Unit rather than
            from Data.
          type: boolean
          x-go-type-skip-optional-pointer: true
        Version:
          description: An entity-specific sequence number used for optimistic concurrency
            control. The value read must be sent in calls to Update.
          format: int64
          readOnly: false
          type: integer
          x-go-type-skip-optional-pointer: true
        WhereMutation:
          description: Where expression used to filter which Mutations of the downstream
            Unit can be affected during merge operations.
          type: string
          x-go-type-skip-optional-pointer: true
        WhereResource:
          description: Where expression used to select which resources of the upstream
            Unit should be eligible for propagation to the downstream Unit.
          type: string
          x-go-type-skip-optional-pointer: true
      required:
      - Slug
      - FromUnitID
      - ToUnitID
      type: object
    LinkCreateOrUpdateResponse:
      properties:
        Error:
          $ref: '#/components/schemas/ResponseError'
        Link:
          $ref: '#/components/schemas/Link'
      type: object
    Mutation:
      description: Mutation is a single source of mutation for a Revision.
      properties:
        CreatedAt:
          description: The timestamp when the entity was created in "2023-01-01T12:00:00Z"
            format.
          example: "2025-04-04T11:50:02.95102-07:00"
          format: date-time
          readOnly: true
          type: string
          x-go-type-skip-optional-pointer: true
        CursorID:
          description: An auto-incrementing sequence number used for pagination.
          format: int64
          readOnly: true
          type: integer
          x-go-type-skip-optional-pointer: true
        EntityType:
          description: The type of entity.
          readOnly: true
          type: string
          x-go-type-skip-optional-pointer: true
        FunctionInvocation:
          $ref: '#/components/schemas/FunctionInvocation'
        InvocationID:
          description: InvocationID is the identifier of the function invoked, if
            there is a corresponding Invocation.
          example: 248df4b7-aa70-47b8-a036-33ac447e668d
          format: uuid
          type: string
        LinkID:
          description: LinkID is the unique identifier of the link if the change was
            made due to resolving a link.
          example: 248df4b7-aa70-47b8-a036-33ac447e668d
          format: uuid
          type: string
        MergeBaseRevisionNum:
          description: MergeBaseRevisionNum is the sequence number of the revision
            preceding merged changes, if the change was due to a merge operation.
          format: int64
          type: integer
          x-go-type-skip-optional-pointer: true
        MergeEndRevisionNum:
          description: MergeEndRevisionNum is the sequence number of the revision
            ending merged changes, if the change was due to a merge operation.
          format: int64
          type: integer
          x-go-type-skip-optional-pointer: true
        MergeSourceID:
          description: MergeSourceID is the unique identifier of the unit if the change
            was made due to merging from another unit, including for clone and upgrade.
          example: 248df4b7-aa70-47b8-a036-33ac447e668d
          format: uuid
          type: string
        MutationID:
          description: Unique identifier for a Mutation.
          example: 248df4b7-aa70-47b8-a036-33ac447e668d
          format: uuid
          type: string
          x-go-type-skip-optional-pointer: true
        MutationNum:
          description: Sequence number for the Mutation.
          format: int64
          type: integer
          x-go-type-skip-optional-pointer: true
        OrganizationID:
          description: Unique identifier for an Organization.
          example: 248df4b7-aa70-47b8-a036-33ac447e668d
          format: uuid
          type: string
          x-go-type-skip-optional-pointer: true
        ProvidedPath:
          description: ProvidedPath is the path of the provided value used to satisfy
            a needed value if the change was made due to resolving a link.
          type: string
          x-go-type-skip-optional-pointer: true
        ProvidedResource:
          $ref: '#/components/schemas/ResourceInfoType2'
        RestoredRevisionNum:
          description: Sequence number of the restored revision, if the change was
            due to a restore operation.
          format: int64
          type: integer
          x-go-type-skip-optional-pointer: true
        RevisionID:
          description: Unique identifier of the corresponding Revision.
          example: 248df4b7-aa70-47b8-a036-33ac447e668d
          format: uuid
          type: string
          x-go-type-skip-optional-pointer: true
        RevisionNum:
          description: Sequence number of the corresponding Revision.
          format: int64
          type: integer
          x-go-type-skip-optional-pointer: true
        SpaceID:
          description: Unique identifier for a space.
          example: 248df4b7-aa70-47b8-a036-33ac447e668d
          format: uuid
          type: string
          x-go-type-skip-optional-pointer: true
        SpaceSlug:
          description: Slug of the Space this entity belongs to. (readonly)
          readOnly: true
          type: string
          x-go-type-skip-optional-pointer: true
        Subgroup:
          description: User-defined category for the Mutation. The prefix 'ConfigHub'
            is reserved.
          type: string
          x-go-type-skip-optional-pointer: true
        TriggerID:
          description: TriggerID is the unique identifier of the trigger if the change
            was made by a trigger.
          example: 248df4b7-aa70-47b8-a036-33ac447e668d
          format: uuid
          type: string
        UnitID:
          description: Unique identifier for a Unit.
          example: 248df4b7-aa70-47b8-a036-33ac447e668d
          format: uuid
          type: string
          x-go-type-skip-optional-pointer: true
        UpdatedAt:
          description: The timestamp when the entity was last updated in "2023-01-01T12:00:00Z"
            format.
          example: "2025-04-04T11:50:02.95102-07:00"
          format: date-time
          readOnly: true
          type: string
          x-go-type-skip-optional-pointer: true
        UpgradedFromUpstreamRevisionNum:
          description: Sequence number of the upstream revision the unit was upgraded
            from, if the change was due to an upgrade operation.
          format: int64
          type: integer
          x-go-type-skip-optional-pointer: true
        Version:
          description: An entity-specific sequence number used for optimistic concurrency
            control. The value read must be sent in calls to Update.
          format: int64
          readOnly: false
          type: integer
          x-go-type-skip-optional-pointer: true
      type: object
    MutationConflict:
      properties:
        Path:
          description: Path of the mutation; empty for resource-level conflicts
          type: string
          x-go-type-skip-optional-pointer: true
        Reason:
          description: Why the mutation was dropped
          type: string
          x-go-type-skip-optional-pointer: true
        Resource:
          $ref: '#/components/schemas/ResourceInfo'
        Source:
          $ref: '#/components/schemas/MutationInfo'
        Target:
          $ref: '#/components/schemas/MutationInfo'
        UnitID:
          description: ID of the other unit involved in the conflict (upstream for
            upgrade/merge, link target for resolve)
          example: 248df4b7-aa70-47b8-a036-33ac447e668d
          format: uuid
          type: string
          x-go-type-skip-optional-pointer: true
      type: object
    MutationConflictList:
      items:
        $ref: '#/components/schemas/MutationConflict'
      type: array
    MutationInfo:
      properties:
        Index:
          description: Function index or sequence number corresponding to the change
          format: int64
          type: integer
          x-go-type-skip-optional-pointer: true
        MutationType:
          $ref: '#/components/schemas/MutationType'
        Patch:
          description: Line-level patch for multi-line string updates, in unified
            diff format. When present on an Update, PatchMutations applies this to
            the target value instead of replacing with Value. Falls back to Value
            if the patch cannot be applied cleanly.
          type: string
          x-go-type-skip-optional-pointer: true
        Predicate:
          description: Used to decide how to use the mututation
          type: boolean
          x-go-type-skip-optional-pointer: true
        Value:
          description: Removed configuration data if MutationType is Delete and otherwise
            the new data
          type: string
          x-go-type-skip-optional-pointer: true
      type: object
    MutationMap:
      additionalProperties:
        $ref: '#/components/schemas/MutationInfo'
      type: object
    MutationType:
      enum:
      - Add
      - Delete
      - Update
      - Replace
      - None
      type: string
    Organization:
      description: The top-level container for an organization using ConfigHub.
      properties:
        Annotations:
          additionalProperties:
            maxLength: 1024
            pattern: ^(.|\n)*$
            type: string
          description: An optional map of Annotation key/value pairs for tools to
            attach information to entities.
          type: object
          x-go-type-skip-optional-pointer: true
        CreatedAt:
          description: The timestamp when the entity was created in "2023-01-01T12:00:00Z"
            format.
          example: "2025-04-04T11:50:02.95102-07:00"
          format: date-time
          readOnly: true
          type: string
          x-go-type-skip-optional-pointer: true
        CursorID:
          description: An auto-incrementing sequence number used for pagination.
          format: int64
          readOnly: true
          type: integer
          x-go-type-skip-optional-pointer: true
        DeleteGates:
          additionalProperties:
            type: boolean
          description: An optional set of gates that, if any is present, will block
            deletion.
          type: object
          x-go-type-skip-optional-pointer: true
        DisplayName:
          description: Friendly name for the entity.
          type: string
          x-go-type-skip-optional-pointer: true
        EmailDomain:
          description: Unique email domain name for the External Identity Provider
            record matching this organization
          type: string
          x-go-type-skip-optional-pointer: true
        EntityType:
          description: The type of entity.
          readOnly: true
          type: string
          x-go-type-skip-optional-pointer: true
        ExternalID:
          description: Unique identifier for the External Identity Provider record
            matching this organization.
          readOnly: true
          type: string
          x-go-type-skip-optional-pointer: true
        Labels:
          additionalProperties:
            maxLength: 128
            pattern: ^[\-_@/#$%&~+=!?.,:;(){}\[\]<>|A-Za-z0-9](([\-_@/#$%&~+=!?.,:;(){}\[\]<>|A-Za-z0-9]|
              )*[\-_@/#$%&~+=!?.,:;(){}\[\]<>|A-Za-z0-9])?$
            type: string
          description: An optional map of Label key/value pairs to specify identifying
            attributes of entities for the purpose of grouping and filtering them.
          type: object
          x-go-type-skip-optional-pointer: true
        OrganizationID:
          description: Unique identifier for an organization.
          example: 248df4b7-aa70-47b8-a036-33ac447e668d
          format: uuid
          type: string
          x-go-type-skip-optional-pointer: true
        Slug:
          description: Unique URL-safe identifier for the entity.
          maxLength: 128
          pattern: ^[A-Za-z0-9]([\-_.A-Za-z0-9]*[A-Za-z0-9])?$
          type: string
          x-go-type-skip-optional-pointer: true
        UpdatedAt:
          description: The timestamp when the entity was last updated in "2023-01-01T12:00:00Z"
            format.
          example: "2025-04-04T11:50:02.95102-07:00"
          format: date-time
          readOnly: true
          type: string
          x-go-type-skip-optional-pointer: true
        Version:
          description: An entity-specific sequence number used for optimistic concurrency
            control. The value read must be sent in calls to Update.
          format: int64
          readOnly: false
          type: integer
          x-go-type-skip-optional-pointer: true
      required:
      - Slug
      type: object
    OrganizationMember:
      description: a User given membership on the Organization
      properties:
        DisplayName:
          description: Friendly name for the organization member User.
          type: string
          x-go-type-skip-optional-pointer: true
        ExternalID:
          description: Unique identifier for the External Identity Provider record
            matching this User.
          type: string
          x-go-type-skip-optional-pointer: true
        ExternalOrganizationID:
          description: Unique identifier for the External Identity Provider record
            matching this organization.
          type: string
          x-go-type-skip-optional-pointer: true
        OrganizationID:
          description: Unique identifier for an organization.
          example: 248df4b7-aa70-47b8-a036-33ac447e668d
          format: uuid
          type: string
          x-go-type-skip-optional-pointer: true
        ProfilePictureURL:
          description: The URL to get the profile avatar picture of the User.
          type: string
          x-go-type-skip-optional-pointer: true
        Slug:
          description: Unique URL-safe identifier for the organization member User.
          type: string
          x-go-type-skip-optional-pointer: true
        UserID:
          description: Unique identifier for the organization member User.
          example: 248df4b7-aa70-47b8-a036-33ac447e668d
          format: uuid
          type: string
          x-go-type-skip-optional-pointer: true
        Username:
          description: Unique username for a User. Must be unique for all of ConfigHub.
          type: string
          x-go-type-skip-optional-pointer: true
      type: object
    PathToVisitorInfoType:
      additionalProperties:
        $ref: '#/components/schemas/PathVisitorInfo'
      type: object
    PathVisitorInfo:
      properties:
        AttributeName:
          description: AttributeName for the path
          type: string
          x-go-type-skip-optional-pointer: true
        DataType:
          description: DataType of the attribute at the path
          type: string
          x-go-type-skip-optional-pointer: true
        Details:
          $ref: '#/components/schemas/AttributeDetails'
        EmbeddedAccessorConfig:
          description: Configuration of the embedded accessor, if any
          type: string
          x-go-type-skip-optional-pointer: true
        EmbeddedAccessorType:
          description: Embedded accessor to use, if any
          type: string
          x-go-type-skip-optional-pointer: true
        Path:
          description: Unresolved path pattern
          type: string
          x-go-type-skip-optional-pointer: true
        ResolvedPath:
          description: Specific resolved path
          type: string
          x-go-type-skip-optional-pointer: true
        TypeExceptions:
          additionalProperties:
            type: object
          description: Resource types to skip
          type: object
          x-go-type-skip-optional-pointer: true
      type: object
    Permissions:
      additionalProperties:
        $ref: '#/components/schemas/Subjects'
      type: object
    QueuedOperation:
      description: "UnitAction is a record of an action to be performed by a Bridge
        Worker. They are queued and sent to the worker in creation order. \nIf the
        worker is temporarily disconnected the queued actions will be sent when the
        worker reconnects or responds.\nIf there are links between units applied or
        destroyed in a single API call, they will be sent to the appropriate\nworker(s)
        in the appropriate order (reverse or forword topological order). One or more
        UnitEvents will correspond\nto each UnitAction."
      properties:
        Action:
          $ref: '#/components/schemas/ActionType'
        BridgeState:
          format: base64
          type: string
          x-go-type-skip-optional-pointer: true
        BridgeWorkerID:
          description: BridgeWorkerID is the unique identifier of the bridge worker
            that will process this operation.
          example: 248df4b7-aa70-47b8-a036-33ac447e668d
          format: uuid
          type: string
          x-go-type-skip-optional-pointer: true
        CreatedAt:
          description: The timestamp when the entity was created in "2023-01-01T12:00:00Z"
            format.
          example: 2006-01-02T15:04:05Z07:00
          format: date-time
          type: string
          x-go-type-skip-optional-pointer: true
        Data:
          description: The result of a dry-run Data-changing action like refresh and
            import, where the data is not stored in the Unit.
          format: base64
          type: string
          x-go-type-skip-optional-pointer: true
        Dependencies:
          description: Dependencies contains the list of operation IDs that this operation
            depends on. Operations will not be delivered until all dependencies are
            completed.
          items:
            $ref: '#/components/schemas/UUID'
          nullable: true
          type: array
          x-go-type-skip-optional-pointer: true
        DriftReconciliationMode:
          description: The drift reconciliation mode for the unit at the time of the
            operation.
          type: string
          x-go-type-skip-optional-pointer: true
        DryRun:
          description: DryRun indicates whether the action is a dry run.
          type: boolean
          x-go-type-skip-optional-pointer: true
        ErrorDetails:
          description: Error details returned by the worker.
          items:
            $ref: '#/components/schemas/ErrorItem'
          type: array
          x-go-type-skip-optional-pointer: true
        ExtraParams:
          description: ExtraParams contains additional parameters for the operation
            in string format.
          type: string
          x-go-type-skip-optional-pointer: true
        LiveData:
          format: base64
          type: string
          x-go-type-skip-optional-pointer: true
        LiveState:
          format: base64
          type: string
          x-go-type-skip-optional-pointer: true
        OrganizationID:
          description: OrganizationID is the unique identifier of the organization
            this operation belongs to.
          example: 248df4b7-aa70-47b8-a036-33ac447e668d
          format: uuid
          type: string
          x-go-type-skip-optional-pointer: true
        QueuedOperationID:
          description: QueuedOperationID is the unique identifier for the queued unit
            action.
          example: 248df4b7-aa70-47b8-a036-33ac447e668d
          format: uuid
          type: string
          x-go-type-skip-optional-pointer: true
        RevisionNum:
          description: RevisionNum is the revision number this operation was performed
            on.
          format: int64
          type: integer
          x-go-type-skip-optional-pointer: true
        SpaceID:
          description: SpaceID is the unique identifier of the space of the unit this
            operation is performed on.
          example: 248df4b7-aa70-47b8-a036-33ac447e668d
          format: uuid
          type: string
          x-go-type-skip-optional-pointer: true
        Status:
          description: 'Status indicates the current status of the unit action. v2
            statuses: Initializing (being set up), Pending (waiting), Delivered (sent
            to worker), Progressing (being processed), Completed (success), Failed
            (error). v1 compatibility: ''pending'' = Pending, ''delivered'' = Completed
            (legacy ''delivered'' meant work done).'
          enum:
          - Initializing
          - Pending
          - Delivered
          - Progressing
          - Completed
          - Failed
          - Aborted
          - Canceled
          - pending
          - delivered
          type: string
          x-go-type-skip-optional-pointer: true
        TargetID:
          description: TargetID is the unique identifier of the target this operation
            is directed to.
          example: 248df4b7-aa70-47b8-a036-33ac447e668d
          format: uuid
          type: string
          x-go-type-skip-optional-pointer: true
        UnitActionNum:
          description: UnitActionNum is the sequence number of this unit action.
          format: int64
          type: integer
          x-go-type-skip-optional-pointer: true
        UnitID:
          description: UnitID is the unique identifier of the unit this operation
            is performed on.
          example: 248df4b7-aa70-47b8-a036-33ac447e668d
          format: uuid
          type: string
          x-go-type-skip-optional-pointer: true
        UserAgent:
          description: User-Agent string of the API call.
          type: string
          x-go-type-skip-optional-pointer: true
        UserID:
          description: UserID of the user the action was performed by.
          example: 248df4b7-aa70-47b8-a036-33ac447e668d
          format: uuid
          type: string
          x-go-type-skip-optional-pointer: true
        UserIsBot:
          description: Whether the user who requested the action is a bot user.
          type: boolean
          x-go-type-skip-optional-pointer: true
        UserRole:
          description: Organization-level role of the user who requested the action.
          type: string
          x-go-type-skip-optional-pointer: true
        Version:
          description: |-
            An entity-specific sequence number used for optimistic concurrency control.
            The value read must be sent in calls to Update.
          format: int64
          type: integer
          x-go-type-skip-optional-pointer: true
      type: object
    ResourceInfo:
      properties:
        ResourceCategory:
          description: Category of configuration element represented in the configuration
            data; Kubernetes and OpenTofu resources are of category Resource, and
            application configuration files are of category AppConfig
          type: string
          x-go-type-skip-optional-pointer: true
        ResourceMergeID:
          description: Stable identifier (UUID) for a resource stored with the resource
            data that is intended to remain consistent across resource name and scope
            changes and across variants, used to match resources between config data
            documents when computing and patching mutations
          type: string
          x-go-type-skip-optional-pointer: true
        ResourceName:
          description: Name of a resource in the system under management represented
            in the configuration data; Kubernetes resources are represented in the
            form <metadata.namespace>/<metadata.name>; not all ToolchainTypes necessarily
            use '/' as a separator between any scope(s) and name or other client-chosen
            ID
          type: string
          x-go-type-skip-optional-pointer: true
        ResourceNameStableCore:
          description: Name of a resource in the system under management represented
            in the configuration data with generated prefixes and suffixes stripped;
            empty if nothing to strip
          type: string
          x-go-type-skip-optional-pointer: true
        ResourceNameWithoutScope:
          description: Name of a resource in the system under management represented
            in the configuration data, without any uniquifying scope, such as Namespace,
            Project, Account, Region, etc.; Kubernetes resources are represented in
            the form <metadata.name>
          type: string
          x-go-type-skip-optional-pointer: true
        ResourceType:
          description: Type of a resource in the system under management represented
            in the configuration data; Kubernetes resources are represented in the
            form <apiVersion>/<kind> (aka group-version-kind)
          type: string
          x-go-type-skip-optional-pointer: true
      type: object
    ResourceInfoList:
      items:
        $ref: '#/components/schemas/ResourceInfo'
      type: array
    ResourceInfoType2:
      properties:
        ResourceCategory:
          description: Category of configuration element represented in the configuration
            data; Kubernetes and OpenTofu resources are of category Resource, and
            application configuration files are of category AppConfig
          type: string
          x-go-type-skip-optional-pointer: true
        ResourceMergeID:
          description: Stable identifier (UUID) for a resource stored with the resource
            data that is intended to remain consistent across resource name and scope
            changes and across variants, used to match resources between config data
            documents when computing and patching mutations
          type: string
          x-go-type-skip-optional-pointer: true
        ResourceName:
          description: Name of a resource in the system under management represented
            in the configuration data; Kubernetes resources are represented in the
            form <metadata.namespace>/<metadata.name>; not all ToolchainTypes necessarily
            use '/' as a separator between any scope(s) and name or other client-chosen
            ID
          type: string
          x-go-type-skip-optional-pointer: true
        ResourceNameStableCore:
          description: Name of a resource in the system under management represented
            in the configuration data with generated prefixes and suffixes stripped;
            empty if nothing to strip
          type: string
          x-go-type-skip-optional-pointer: true
        ResourceType:
          description: Type of a resource in the system under management represented
            in the configuration data; Kubernetes resources are represented in the
            form <apiVersion>/<kind> (aka group-version-kind)
          type: string
          x-go-type-skip-optional-pointer: true
      type: object
    ResourceMutation:
      properties:
        Aliases:
          additionalProperties:
            type: object
          description: Names (with scopes, if any) used in current and prior revisions
            of this resource
          type: object
          x-go-type-skip-optional-pointer: true
        AliasesWithoutScopes:
          additionalProperties:
            type: object
          description: Names without scopes used in current and prior revisions of
            this resource
          type: object
          x-go-type-skip-optional-pointer: true
        ArrayElementAliases:
          $ref: '#/components/schemas/ArrayElementAliasMap'
        ArrayOrders:
          $ref: '#/components/schemas/ArrayOrderMap'
        PathMutationMap:
          $ref: '#/components/schemas/MutationMap'
        Resource:
          $ref: '#/components/schemas/ResourceInfo'
        ResourceMutationInfo:
          $ref: '#/components/schemas/MutationInfo'
      type: object
    ResourceMutationList:
      items:
        $ref: '#/components/schemas/ResourceMutation'
      nullable: true
      type: array
    ResourceStatus:
      properties:
        Message:
          description: Human-readable status details or error message
          type: string
          x-go-type-skip-optional-pointer: true
        Readiness:
          description: Health state from kstatus (Ready, InProgress, Failed, Unknown)
          type: string
          x-go-type-skip-optional-pointer: true
        SyncStatus:
          description: Whether config was pushed to the target (Synced or NotSynced)
          type: string
          x-go-type-skip-optional-pointer: true
        UpdatedAt:
          description: Timestamp when this resource status was last updated
          example: 2006-01-02T15:04:05Z07:00
          format: date-time
          type: string
          x-go-type-skip-optional-pointer: true
      type: object
    ResourceStatusMap:
      additionalProperties:
        $ref: '#/components/schemas/ResourceStatus'
      type: object
    ResourceStatusSummary:
      properties:
        Failed:
          description: Number of resources with Readiness=Failed
          type: integer
          x-go-type-skip-optional-pointer: true
        FirstUpdatedAt:
          description: Earliest UpdatedAt timestamp across all resources
          format: date-time
          nullable: true
          type: string
          x-go-type-skip-optional-pointer: true
        LastUpdatedAt:
          description: Most recent UpdatedAt timestamp across all resources
          format: date-time
          nullable: true
          type: string
          x-go-type-skip-optional-pointer: true
        Progressing:
          description: Number of resources with Readiness=InProgress
          type: integer
          x-go-type-skip-optional-pointer: true
        Ready:
          description: Number of resources with Readiness=Ready
          type: integer
          x-go-type-skip-optional-pointer: true
        Synced:
          description: Number of resources with SyncStatus=Synced
          type: integer
          x-go-type-skip-optional-pointer: true
        Total:
          description: Total number of resources in the unit
          type: integer
          x-go-type-skip-optional-pointer: true
      type: object
    ResourceTypePathsEntry:
      properties:
        BoundLinkID:
          description: ID of the Link that bound this needed path to a provided value
          example: 248df4b7-aa70-47b8-a036-33ac447e668d
          format: uuid
          type: string
          x-go-type-skip-optional-pointer: true
        BoundProvidedProperties:
          additionalProperties:
            type: string
          description: ProvidedProperties of the provided path that was bound to this
            needed path, for cross-link score comparison
          type: object
          x-go-type-skip-optional-pointer: true
        GetterInvocation:
          $ref: '#/components/schemas/FunctionInvocation'
        IsNeeded:
          description: Whether this attribute is a needed value
          type: boolean
          x-go-type-skip-optional-pointer: true
        IsProvided:
          description: Whether this attribute is a provided value
          type: boolean
          x-go-type-skip-optional-pointer: true
        NeededPreferred:
          additionalProperties:
            type: string
          description: Preferred properties for matching; more matches produce a stronger
            match preference
          type: object
          x-go-type-skip-optional-pointer: true
        NeededRequired:
          additionalProperties:
            type: string
          description: Required properties that a provided value must have in order
            to match
          type: object
          x-go-type-skip-optional-pointer: true
        Paths:
          $ref: '#/components/schemas/PathToVisitorInfoType'
        ProvidedProperties:
          additionalProperties:
            type: string
          description: Key/value properties describing what this provided value offers,
            for matching
          type: object
          x-go-type-skip-optional-pointer: true
        ResourceType:
          type: string
          x-go-type-skip-optional-pointer: true
        SetterInvocation:
          $ref: '#/components/schemas/FunctionInvocation'
      type: object
    ResponseError:
      properties:
        Details:
          description: Additional context messages
          items:
            type: string
          type: array
          x-go-type-skip-optional-pointer: true
        ErrorCategory:
          description: The type of error (e.g., validation, not-found)
          type: string
          x-go-type-skip-optional-pointer: true
        ErrorMetadata:
          $ref: '#/components/schemas/ErrorMetadata'
        Message:
          description: The primary error message
          type: string
          x-go-type-skip-optional-pointer: true
        Status:
          description: HTTP status code
          type: integer
          x-go-type-skip-optional-pointer: true
        Type:
          description: The type of error (e.g., validation, not-found)
          type: string
          x-go-type-skip-optional-pointer: true
      type: object
    Revision:
      description: Revision is a historial view of a Config Unit.
      properties:
        ApplyGates:
          additionalProperties:
            type: boolean
          description: A map of "<space slug>/<trigger slug>/<function name>" to true
            of Triggers invoking validating functions that did not pass on the configuration
            data at this Revision. These block Apply operations.
          type: object
          x-go-type-skip-optional-pointer: true
        ApplyWarnings:
          additionalProperties:
            type: boolean
          description: A map of "<space slug>/<trigger slug>/<function name>" to true
            of Triggers with Warn=true invoking validating functions that did not
            pass on the configuration data at this Revision. These do not block Apply
            operations.
          type: object
          x-go-type-skip-optional-pointer: true
        ApprovedBy:
          description: the users that have approved the latest version of the config
            data for the Unit.
          items:
            $ref: '#/components/schemas/UUID'
          type: array
          x-go-type-skip-optional-pointer: true
        ChangeSetID:
          description: Unique identifier for the ChangeSet to which this Revision
            belongs. Optional. Revisions are not required to belong to ChangeSets.
          example: 248df4b7-aa70-47b8-a036-33ac447e668d
          format: uuid
          type: string
        ContentHash:
          description: 'Deprecated: Use DataHash instead. The CRC32 hash of this revision''s
            data.'
          type: integer
          x-go-type-skip-optional-pointer: true
        CreatedAt:
          description: The timestamp when the entity was created in "2023-01-01T12:00:00Z"
            format.
          example: "2025-04-04T11:50:02.95102-07:00"
          format: date-time
          readOnly: true
          type: string
          x-go-type-skip-optional-pointer: true
        CursorID:
          description: An auto-incrementing sequence number used for pagination.
          format: int64
          readOnly: true
          type: integer
          x-go-type-skip-optional-pointer: true
        Data:
          description: The full configuration data for this unit at this revision.
          format: base64
          type: string
          x-go-type-skip-optional-pointer: true
        DataHash:
          description: The SHA256 hash of this revision's data, encoded as hexadecimal.
          type: string
          x-go-type-skip-optional-pointer: true
        Description:
          description: User description of the change. It is copied from the LastChangeDescription
            field of the Unit at the time the change was made that created the Revision.
          type: string
          x-go-type-skip-optional-pointer: true
        EntityType:
          description: The type of entity.
          readOnly: true
          type: string
          x-go-type-skip-optional-pointer: true
        LiveAt:
          description: Time at which the revision was applied, if it was applied.
            If not applied, the value is "0001-01-01T00:00:00Z".
          example: 2006-01-02T15:04:05Z07:00
          format: date-time
          type: string
          x-go-type-skip-optional-pointer: true
        MutationSources:
          $ref: '#/components/schemas/ResourceMutationList'
        OrganizationID:
          description: Unique identifier for an Organization.
          example: 248df4b7-aa70-47b8-a036-33ac447e668d
          format: uuid
          type: string
          x-go-type-skip-optional-pointer: true
        RevisionID:
          description: Unique identifier for a Revision.
          example: 248df4b7-aa70-47b8-a036-33ac447e668d
          format: uuid
          type: string
          x-go-type-skip-optional-pointer: true
        RevisionNum:
          description: Sequence number for a Revision.
          format: int64
          type: integer
          x-go-type-skip-optional-pointer: true
        Source:
          description: ConfigHub operation that created this revision.
          type: string
          x-go-type-skip-optional-pointer: true
        SpaceID:
          description: Unique identifier for a space.
          example: 248df4b7-aa70-47b8-a036-33ac447e668d
          format: uuid
          type: string
          x-go-type-skip-optional-pointer: true
        SpaceSlug:
          description: Slug of the Space this entity belongs to. (readonly)
          readOnly: true
          type: string
          x-go-type-skip-optional-pointer: true
        Tags:
          additionalProperties:
            type: string
          description: A set (map) of TagIDs of any Tags applied to this Revision.
            The string values have no particular meaning for now.
          type: object
          x-go-type-skip-optional-pointer: true
        UnitID:
          description: Unique identifier for a Unit.
          example: 248df4b7-aa70-47b8-a036-33ac447e668d
          format: uuid
          type: string
          x-go-type-skip-optional-pointer: true
        UpdatedAt:
          description: The timestamp when the entity was last updated in "2023-01-01T12:00:00Z"
            format.
          example: "2025-04-04T11:50:02.95102-07:00"
          format: date-time
          readOnly: true
          type: string
          x-go-type-skip-optional-pointer: true
        UserAgent:
          description: User-Agent string if created by an API call. Optional.
          type: string
          x-go-type-skip-optional-pointer: true
        UserID:
          description: UserID if change was made by a user. Automated changes, such
            as by triggers and resolve, are currently made with the UserID "00000000-0000-0000-0000-000000000000".
          example: 248df4b7-aa70-47b8-a036-33ac447e668d
          format: uuid
          type: string
          x-go-type-skip-optional-pointer: true
        Version:
          description: An entity-specific sequence number used for optimistic concurrency
            control. The value read must be sent in calls to Update.
          format: int64
          readOnly: false
          type: integer
          x-go-type-skip-optional-pointer: true
      type: object
    Schema: {}
    Space:
      description: The logical container for most entities in ConfigHub. Namespaces
        triggers, units, targets, workers, and other entities.
      properties:
        Annotations:
          additionalProperties:
            maxLength: 1024
            pattern: ^(.|\n)*$
            type: string
          description: An optional map of Annotation key/value pairs for tools to
            attach information to entities.
          type: object
          x-go-type-skip-optional-pointer: true
        AttributeFilterID:
          description: Reference to a Filter entity used to identify Attributes for
            the Space's FunctionExecutor. The Filter's From field must be set to 'Attribute'.
          example: 248df4b7-aa70-47b8-a036-33ac447e668d
          format: uuid
          type: string
        AttributeHash:
          description: Hash of all registered Attribute configurations for the Space.
            (readonly)
          readOnly: true
          type: string
          x-go-type-skip-optional-pointer: true
        AttributeIDs:
          description: List of Attribute IDs that match the WhereAttribute and/or
            AttributeFilterID criteria. (readonly)
          items:
            $ref: '#/components/schemas/UUID'
          readOnly: true
          type: array
          x-go-type-skip-optional-pointer: true
        CreatedAt:
          description: The timestamp when the entity was created in "2023-01-01T12:00:00Z"
            format.
          example: "2025-04-04T11:50:02.95102-07:00"
          format: date-time
          readOnly: true
          type: string
          x-go-type-skip-optional-pointer: true
        CursorID:
          description: An auto-incrementing sequence number used for pagination.
          format: int64
          readOnly: true
          type: integer
          x-go-type-skip-optional-pointer: true
        DeleteGates:
          additionalProperties:
            type: boolean
          description: An optional set of gates that, if any is present, will block
            deletion.
          type: object
          x-go-type-skip-optional-pointer: true
        DisplayName:
          description: Friendly name for the entity.
          type: string
          x-go-type-skip-optional-pointer: true
        EntityType:
          description: The type of entity.
          readOnly: true
          type: string
          x-go-type-skip-optional-pointer: true
        Labels:
          additionalProperties:
            maxLength: 128
            pattern: ^[\-_@/#$%&~+=!?.,:;(){}\[\]<>|A-Za-z0-9](([\-_@/#$%&~+=!?.,:;(){}\[\]<>|A-Za-z0-9]|
              )*[\-_@/#$%&~+=!?.,:;(){}\[\]<>|A-Za-z0-9])?$
            type: string
          description: An optional map of Label key/value pairs to specify identifying
            attributes of entities for the purpose of grouping and filtering them.
          type: object
          x-go-type-skip-optional-pointer: true
        OrganizationID:
          description: Unique identifier for an organization.
          example: 248df4b7-aa70-47b8-a036-33ac447e668d
          format: uuid
          type: string
          x-go-type-skip-optional-pointer: true
        Permissions:
          $ref: '#/components/schemas/Permissions'
        Slug:
          description: Unique URL-safe identifier for the entity.
          maxLength: 128
          pattern: ^[A-Za-z0-9]([\-_.A-Za-z0-9]*[A-Za-z0-9])?$
          type: string
          x-go-type-skip-optional-pointer: true
        SpaceID:
          description: Unique identifier for a space.
          example: 248df4b7-aa70-47b8-a036-33ac447e668d
          format: uuid
          type: string
          x-go-type-skip-optional-pointer: true
        TriggerFilterID:
          description: Reference to a Filter entity used to identify Triggers that
            should be invoked on Units within this Space. The Filter's From field
            must be set to 'Trigger'.
          example: 248df4b7-aa70-47b8-a036-33ac447e668d
          format: uuid
          type: string
        TriggerHash:
          readOnly: true
          type: string
          x-go-type-skip-optional-pointer: true
        TriggerIDs:
          description: List of Trigger IDs that match the WhereTrigger and/or TriggerFilterID
            criteria. (readonly)
          items:
            $ref: '#/components/schemas/UUID'
          readOnly: true
          type: array
          x-go-type-skip-optional-pointer: true
        UpdatedAt:
          description: The timestamp when the entity was last updated in "2023-01-01T12:00:00Z"
            format.
          example: "2025-04-04T11:50:02.95102-07:00"
          format: date-time
          readOnly: true
          type: string
          x-go-type-skip-optional-pointer: true
        Version:
          description: An entity-specific sequence number used for optimistic concurrency
            control. The value read must be sent in calls to Update.
          format: int64
          readOnly: false
          type: integer
          x-go-type-skip-optional-pointer: true
        WhereAttribute:
          description: |-
            Filter expression to identify Attributes that should be registered in the Space's FunctionExecutor. The specified string is an expression for the purpose of filtering
            the list of Attributes returned. The expression syntax was inspired by SQL.
            It supports conjunctions using `AND` of relational expressions of the form *attribute*
            *operator* *attribute_or_literal*. The attribute names are case-sensitive and PascalCase,
            as in the JSON encoding.
            Strings support the following operators: `<`, `>`, `<=`, `>=`, `=`, `!=`, `LIKE`, `NOT LIKE`, `ILIKE`, `~~`, `!~~`, `~`, `~*`, `!~`, `!~*`, `IN`, `NOT IN`.
            String pattern operators: `LIKE` and `~~` for pattern matching with `%` and `_` wildcards,
            `ILIKE` for case-insensitive pattern matching, `NOT LIKE` and `!~~` for negated pattern matching.
            String regex operators: `~` for regex matching, `~*` for case-insensitive regex,
            `!~` and `!~*` for regex not matching (case-sensitive and insensitive).
            Integers support the following operators: `<`, `>`, `<=`, `>=`, `=`, `!=`, `IN`, `NOT IN`.
            UUIDs and boolean attributes support equality and inequality only.
            UUID and time literals must be quoted as string literals.
            String literals are quoted with single quotes, such as `'string'`.
            Time literals use the same form as when serialized as JSON,
            such as: `CreatedAt > '2025-02-18T23:16:34'`.
            Integer and boolean literals are also supported for attributes of those types.
            Arrays support the `?` operator to to match any element of the array,
            as in `ApprovedBy ? '7c61626f-ddbe-41af-93f6-b69f4ab6d308'`.
            Arrays can perform LEN() to check for length, as in `LEN(ApprovedBy) > 0`.
            Map support the dot notation to specify a particular map key, as in `Labels.tier = 'Backend'`.
            Maps support `IS NULL` and `IS NOT NULL` with dot notation to check for key absence or presence,
            as in `Labels.tier IS NULL` (key doesn't exist) or `Labels.tier IS NOT NULL` (key exists).
            Comparison results can be tested with `IS TRUE`, `IS FALSE`, `IS NOT TRUE`, and `IS NOT FALSE`.
            These are useful for nullable columns: `MergeSourceID = '<uuid>' IS NOT FALSE` matches rows where MergeSourceID equals the value OR is NULL.
            The `IN` and `NOT IN` operators accept a comma-separated list of values in parentheses,
            such as `Slug IN ('slugone', 'slugtwo')` or `Labels.environment IN ('prod', 'staging')`.
            Conjunctions are supported using the `AND` operator.
            An example conjunction is:
            `CreatedAt >= '2025-01-07' AND Slug = 'test' AND Labels.mykey = 'myvalue'`.

            Supported attributes for filtering on Attribute: AttributeID, CreatedAt, DataType, DeleteGates, DisplayName, Hash, Labels, OrganizationID, Slug, SpaceID, ToolchainType, UpdatedAt.

            The whole string must be query-encoded.
          type: string
          x-go-type-skip-optional-pointer: true
        WhereTrigger:
          description: |-
            Filter expression to identify Triggers that should be invoked on Units within this Space. The specified string is an expression for the purpose of filtering
            the list of Triggers returned. The expression syntax was inspired by SQL.
            It supports conjunctions using `AND` of relational expressions of the form *attribute*
            *operator* *attribute_or_literal*. The attribute names are case-sensitive and PascalCase,
            as in the JSON encoding.
            Strings support the following operators: `<`, `>`, `<=`, `>=`, `=`, `!=`, `LIKE`, `NOT LIKE`, `ILIKE`, `~~`, `!~~`, `~`, `~*`, `!~`, `!~*`, `IN`, `NOT IN`.
            String pattern operators: `LIKE` and `~~` for pattern matching with `%` and `_` wildcards,
            `ILIKE` for case-insensitive pattern matching, `NOT LIKE` and `!~~` for negated pattern matching.
            String regex operators: `~` for regex matching, `~*` for case-insensitive regex,
            `!~` and `!~*` for regex not matching (case-sensitive and insensitive).
            Integers support the following operators: `<`, `>`, `<=`, `>=`, `=`, `!=`, `IN`, `NOT IN`.
            UUIDs and boolean attributes support equality and inequality only.
            UUID and time literals must be quoted as string literals.
            String literals are quoted with single quotes, such as `'string'`.
            Time literals use the same form as when serialized as JSON,
            such as: `CreatedAt > '2025-02-18T23:16:34'`.
            Integer and boolean literals are also supported for attributes of those types.
            Arrays support the `?` operator to to match any element of the array,
            as in `ApprovedBy ? '7c61626f-ddbe-41af-93f6-b69f4ab6d308'`.
            Arrays can perform LEN() to check for length, as in `LEN(ApprovedBy) > 0`.
            Map support the dot notation to specify a particular map key, as in `Labels.tier = 'Backend'`.
            Maps support `IS NULL` and `IS NOT NULL` with dot notation to check for key absence or presence,
            as in `Labels.tier IS NULL` (key doesn't exist) or `Labels.tier IS NOT NULL` (key exists).
            Comparison results can be tested with `IS TRUE`, `IS FALSE`, `IS NOT TRUE`, and `IS NOT FALSE`.
            These are useful for nullable columns: `MergeSourceID = '<uuid>' IS NOT FALSE` matches rows where MergeSourceID equals the value OR is NULL.
            The `IN` and `NOT IN` operators accept a comma-separated list of values in parentheses,
            such as `Slug IN ('slugone', 'slugtwo')` or `Labels.environment IN ('prod', 'staging')`.
            Conjunctions are supported using the `AND` operator.
            An example conjunction is:
            `CreatedAt >= '2025-01-07' AND Slug = 'test' AND Labels.mykey = 'myvalue'`.

            Supported attributes for filtering on Trigger: BridgeWorkerID, CreatedAt, DeleteGates, Description, Disabled, DisplayName, Event, FunctionName, Hash, InvocationID, Labels, OrganizationID, OtherDataSource, Slug, SpaceID, ToolchainType, TriggerID, UnitFilterID, UpdatedAt, Validating, Warn, WhereResource, WhereUnit.

            The whole string must be query-encoded.
          type: string
          x-go-type-skip-optional-pointer: true
      required:
      - Slug
      type: object
    SpaceCreateOrUpdateResponse:
      properties:
        Error:
          $ref: '#/components/schemas/ResponseError'
        Space:
          $ref: '#/components/schemas/Space'
      type: object
    StandardErrorResponse:
      description: Error response details.
      properties:
        Code:
          description: HTTP status code of the response.
          type: string
          x-go-type-skip-optional-pointer: true
        Message:
          description: Message returned with the response.
          type: string
          x-go-type-skip-optional-pointer: true
      type: object
    Subjects:
      properties:
        UserIDs:
          additionalProperties:
            type: boolean
          type: object
          x-go-type-skip-optional-pointer: true
      type: object
    SupportedConfigType:
      properties:
        AvailableTargets:
          description: Targets known by the BridgeWorker. Optional.
          items:
            $ref: '#/components/schemas/TargetType2'
          type: array
          x-go-type-skip-optional-pointer: true
        CompatibleBridge:
          description: Bridge with compatible BridgeHandles
          type: string
          x-go-type-skip-optional-pointer: true
        LiveStateType:
          description: Configuration toolchain and format of the LiveState for this
            bridge; required in order to invoke functions on LiveState
          type: string
          x-go-type-skip-optional-pointer: true
        Options:
          description: Supported bridge options
          items:
            $ref: '#/components/schemas/BridgeOption'
          type: array
          x-go-type-skip-optional-pointer: true
        ProviderType:
          description: Type identifying a bridge implementation supported by the worker
          type: string
          x-go-type-skip-optional-pointer: true
        ToolchainType:
          description: Configuration toolchain and format implemented by this bridge
            of the worker
          type: string
          x-go-type-skip-optional-pointer: true
      type: object
    Tag:
      description: Defines a Tag that can be used to identify a set of Revisions across
        Units.
      properties:
        Annotations:
          additionalProperties:
            maxLength: 1024
            pattern: ^(.|\n)*$
            type: string
          description: An optional map of Annotation key/value pairs for tools to
            attach information to entities.
          type: object
          x-go-type-skip-optional-pointer: true
        ChangeSetID:
          description: ChangeSetID is the optional ID of the ChangeSet this Tag is
            associated with.
          example: 248df4b7-aa70-47b8-a036-33ac447e668d
          format: uuid
          readOnly: true
          type: string
        CreatedAt:
          description: The timestamp when the entity was created in "2023-01-01T12:00:00Z"
            format.
          example: "2025-04-04T11:50:02.95102-07:00"
          format: date-time
          readOnly: true
          type: string
          x-go-type-skip-optional-pointer: true
        CursorID:
          description: An auto-incrementing sequence number used for pagination.
          format: int64
          readOnly: true
          type: integer
          x-go-type-skip-optional-pointer: true
        DeleteGates:
          additionalProperties:
            type: boolean
          description: An optional set of gates that, if any is present, will block
            deletion.
          type: object
          x-go-type-skip-optional-pointer: true
        DisplayName:
          description: Friendly name for the entity.
          type: string
          x-go-type-skip-optional-pointer: true
        EntityType:
          description: The type of entity.
          readOnly: true
          type: string
          x-go-type-skip-optional-pointer: true
        Labels:
          additionalProperties:
            maxLength: 128
            pattern: ^[\-_@/#$%&~+=!?.,:;(){}\[\]<>|A-Za-z0-9](([\-_@/#$%&~+=!?.,:;(){}\[\]<>|A-Za-z0-9]|
              )*[\-_@/#$%&~+=!?.,:;(){}\[\]<>|A-Za-z0-9])?$
            type: string
          description: An optional map of Label key/value pairs to specify identifying
            attributes of entities for the purpose of grouping and filtering them.
          type: object
          x-go-type-skip-optional-pointer: true
        OrganizationID:
          description: Unique identifier for an organization.
          example: 248df4b7-aa70-47b8-a036-33ac447e668d
          format: uuid
          type: string
          x-go-type-skip-optional-pointer: true
        Slug:
          description: Unique URL-safe identifier for the entity.
          maxLength: 128
          pattern: ^[A-Za-z0-9]([\-_.A-Za-z0-9]*[A-Za-z0-9])?$
          type: string
          x-go-type-skip-optional-pointer: true
        SpaceID:
          description: Unique identifier for a space.
          example: 248df4b7-aa70-47b8-a036-33ac447e668d
          format: uuid
          type: string
          x-go-type-skip-optional-pointer: true
        SpaceSlug:
          description: Slug of the Space this entity belongs to. (readonly)
          readOnly: true
          type: string
          x-go-type-skip-optional-pointer: true
        TagID:
          description: TagID uniquely identifies a tag within the system.
          example: 248df4b7-aa70-47b8-a036-33ac447e668d
          format: uuid
          type: string
          x-go-type-skip-optional-pointer: true
        UpdatedAt:
          description: The timestamp when the entity was last updated in "2023-01-01T12:00:00Z"
            format.
          example: "2025-04-04T11:50:02.95102-07:00"
          format: date-time
          readOnly: true
          type: string
          x-go-type-skip-optional-pointer: true
        Version:
          description: An entity-specific sequence number used for optimistic concurrency
            control. The value read must be sent in calls to Update.
          format: int64
          readOnly: false
          type: integer
          x-go-type-skip-optional-pointer: true
      required:
      - Slug
      type: object
    TagCreateOrUpdateResponse:
      properties:
        Error:
          $ref: '#/components/schemas/ResponseError'
        Tag:
          $ref: '#/components/schemas/Tag'
      type: object
    Target:
      description: Target represents a deployment target in ConfigHub. It defines
        where configuration should be applied, including the toolchain type (e.g.,
        Kubernetes/YAML, OpenTofu/HCL, AppConfig/Properties, AppConfig/YAML, AppConfig/TOML,
        AppConfig/INI, AppConfig/JSON, AppConfig/Env, AppConfig/Text) and provider
        (e.g., Kubernetes, OpenTofu/AWS, ConfigMap). Each Target is associated with
        a specific BridgeWorker that handles the actual deployment actions (e.g. Apply,
        Destroy).
      properties:
        Annotations:
          additionalProperties:
            maxLength: 1024
            pattern: ^(.|\n)*$
            type: string
          description: An optional map of Annotation key/value pairs for tools to
            attach information to entities.
          type: object
          x-go-type-skip-optional-pointer: true
        BridgeHandle:
          description: Identifier used by the Bridge to refer to discovered/enabled
            Target credentials and coordinates.
          type: string
          x-go-type-skip-optional-pointer: true
        BridgeWorkerID:
          description: Unique identifier for a Bridge Worker associated with the Target.
          example: 248df4b7-aa70-47b8-a036-33ac447e668d
          format: uuid
          type: string
          x-go-type-skip-optional-pointer: true
        ConfigTypes:
          description: ConfigTypes (ToolchainType, ProviderType, LiveStateType tuples)
            supported by this Target.
          items:
            $ref: '#/components/schemas/TargetConfigType'
          type: array
          x-go-type-skip-optional-pointer: true
        CreatedAt:
          description: The timestamp when the entity was created in "2023-01-01T12:00:00Z"
            format.
          example: "2025-04-04T11:50:02.95102-07:00"
          format: date-time
          readOnly: true
          type: string
          x-go-type-skip-optional-pointer: true
        CursorID:
          description: An auto-incrementing sequence number used for pagination.
          format: int64
          readOnly: true
          type: integer
          x-go-type-skip-optional-pointer: true
        DeleteGates:
          additionalProperties:
            type: boolean
          description: An optional set of gates that, if any is present, will block
            deletion.
          type: object
          x-go-type-skip-optional-pointer: true
        DisplayName:
          description: Friendly name for the entity.
          type: string
          x-go-type-skip-optional-pointer: true
        EntityType:
          description: The type of entity.
          readOnly: true
          type: string
          x-go-type-skip-optional-pointer: true
        Labels:
          additionalProperties:
            maxLength: 128
            pattern: ^[\-_@/#$%&~+=!?.,:;(){}\[\]<>|A-Za-z0-9](([\-_@/#$%&~+=!?.,:;(){}\[\]<>|A-Za-z0-9]|
              )*[\-_@/#$%&~+=!?.,:;(){}\[\]<>|A-Za-z0-9])?$
            type: string
          description: An optional map of Label key/value pairs to specify identifying
            attributes of entities for the purpose of grouping and filtering them.
          type: object
          x-go-type-skip-optional-pointer: true
        LiveStateType:
          description: LiveStateType specifies the first/default configuration toolchain
            and format of the LiveState for the bridge corresponding to this Target.
            Possible values include "Kubernetes/YAML" and "ConfigHub/YAML".
          type: string
          x-go-type-skip-optional-pointer: true
        Options:
          additionalProperties:
            type: string
          description: Bridge option values for the first ProviderType. The options
            must be predefined by the ConfigType in the BridgeWorker.
          type: object
          x-go-type-skip-optional-pointer: true
        OrganizationID:
          description: Unique identifier for an organization.
          example: 248df4b7-aa70-47b8-a036-33ac447e668d
          format: uuid
          type: string
          x-go-type-skip-optional-pointer: true
        Parameters:
          description: |
            Deprecated. Parameters contains toolchain-type and/or provider-type-specific parameters in JSON format.

            For ProviderType: Kubernetes (ToolchainType: Kubernetes/YAML)
            The Parameters object may contain the following fields:
            - "KubeContext" (string): The name of the Kubernetes context (from "~/.kube/config") to use. (Not typically needed if running in-cluster).
          type: string
          x-go-type-skip-optional-pointer: true
        Permissions:
          $ref: '#/components/schemas/Permissions'
        ProviderType:
          description: ProviderType specifies the first/default cloud or infrastructure
            provider for this target, such as "Kubernetes".
          type: string
          x-go-type-skip-optional-pointer: true
        Slug:
          description: Unique URL-safe identifier for the entity.
          maxLength: 128
          pattern: ^[A-Za-z0-9]([\-_.A-Za-z0-9]*[A-Za-z0-9])?$
          type: string
          x-go-type-skip-optional-pointer: true
        SpaceID:
          description: Unique identifier for a space.
          example: 248df4b7-aa70-47b8-a036-33ac447e668d
          format: uuid
          type: string
          x-go-type-skip-optional-pointer: true
        SpaceSlug:
          description: Slug of the Space this entity belongs to. (readonly)
          readOnly: true
          type: string
          x-go-type-skip-optional-pointer: true
        TargetID:
          description: Unique identifier for a Target.
          example: 248df4b7-aa70-47b8-a036-33ac447e668d
          format: uuid
          type: string
          x-go-type-skip-optional-pointer: true
        ToolchainType:
          description: ToolchainType specifies the type of the first/default toolchain
            supported by this Target. Possible values include "Kubernetes/YAML", "ConfigHub/YAML",
            "OpenTofu/HCL", "AppConfig/Properties", "AppConfig/YAML", "AppConfig/TOML",
            "AppConfig/INI", "AppConfig/JSON", "AppConfig/Env", "AppConfig/Text".
          type: string
          x-go-type-skip-optional-pointer: true
        TriggerFilterID:
          description: Reference to a Filter entity used to identify Triggers that
            should be invoked on Units this Target is attached to. The Filter's From
            field must be set to 'Trigger'.
          example: 248df4b7-aa70-47b8-a036-33ac447e668d
          format: uuid
          type: string
        TriggerHash:
          readOnly: true
          type: string
          x-go-type-skip-optional-pointer: true
        TriggerIDs:
          description: List of Trigger IDs that match the WhereTrigger and/or TriggerFilterID
            criteria. (readonly)
          items:
            $ref: '#/components/schemas/UUID'
          readOnly: true
          type: array
          x-go-type-skip-optional-pointer: true
        UpdatedAt:
          description: The timestamp when the entity was last updated in "2023-01-01T12:00:00Z"
            format.
          example: "2025-04-04T11:50:02.95102-07:00"
          format: date-time
          readOnly: true
          type: string
          x-go-type-skip-optional-pointer: true
        Version:
          description: An entity-specific sequence number used for optimistic concurrency
            control. The value read must be sent in calls to Update.
          format: int64
          readOnly: false
          type: integer
          x-go-type-skip-optional-pointer: true
        WhereTrigger:
          description: |-
            Filter expression to identify Triggers that should be invoked on Units this Target is attached to. The specified string is an expression for the purpose of filtering
            the list of Triggers returned. The expression syntax was inspired by SQL.
            It supports conjunctions using `AND` of relational expressions of the form *attribute*
            *operator* *attribute_or_literal*. The attribute names are case-sensitive and PascalCase,
            as in the JSON encoding.
            Strings support the following operators: `<`, `>`, `<=`, `>=`, `=`, `!=`, `LIKE`, `NOT LIKE`, `ILIKE`, `~~`, `!~~`, `~`, `~*`, `!~`, `!~*`, `IN`, `NOT IN`.
            String pattern operators: `LIKE` and `~~` for pattern matching with `%` and `_` wildcards,
            `ILIKE` for case-insensitive pattern matching, `NOT LIKE` and `!~~` for negated pattern matching.
            String regex operators: `~` for regex matching, `~*` for case-insensitive regex,
            `!~` and `!~*` for regex not matching (case-sensitive and insensitive).
            Integers support the following operators: `<`, `>`, `<=`, `>=`, `=`, `!=`, `IN`, `NOT IN`.
            UUIDs and boolean attributes support equality and inequality only.
            UUID and time literals must be quoted as string literals.
            String literals are quoted with single quotes, such as `'string'`.
            Time literals use the same form as when serialized as JSON,
            such as: `CreatedAt > '2025-02-18T23:16:34'`.
            Integer and boolean literals are also supported for attributes of those types.
            Arrays support the `?` operator to to match any element of the array,
            as in `ApprovedBy ? '7c61626f-ddbe-41af-93f6-b69f4ab6d308'`.
            Arrays can perform LEN() to check for length, as in `LEN(ApprovedBy) > 0`.
            Map support the dot notation to specify a particular map key, as in `Labels.tier = 'Backend'`.
            Maps support `IS NULL` and `IS NOT NULL` with dot notation to check for key absence or presence,
            as in `Labels.tier IS NULL` (key doesn't exist) or `Labels.tier IS NOT NULL` (key exists).
            Comparison results can be tested with `IS TRUE`, `IS FALSE`, `IS NOT TRUE`, and `IS NOT FALSE`.
            These are useful for nullable columns: `MergeSourceID = '<uuid>' IS NOT FALSE` matches rows where MergeSourceID equals the value OR is NULL.
            The `IN` and `NOT IN` operators accept a comma-separated list of values in parentheses,
            such as `Slug IN ('slugone', 'slugtwo')` or `Labels.environment IN ('prod', 'staging')`.
            Conjunctions are supported using the `AND` operator.
            An example conjunction is:
            `CreatedAt >= '2025-01-07' AND Slug = 'test' AND Labels.mykey = 'myvalue'`.

            Supported attributes for filtering on Trigger: BridgeWorkerID, CreatedAt, DeleteGates, Description, Disabled, DisplayName, Event, FunctionName, Hash, InvocationID, Labels, OrganizationID, OtherDataSource, Slug, SpaceID, ToolchainType, TriggerID, UnitFilterID, UpdatedAt, Validating, Warn, WhereResource, WhereUnit.

            The whole string must be query-encoded.
          type: string
          x-go-type-skip-optional-pointer: true
      required:
      - Slug
      - BridgeWorkerID
      - ToolchainType
      - ProviderType
      type: object
    TargetConfigType:
      properties:
        LiveStateType:
          description: Configuration toolchain and format of the LiveState for this
            bridge; required in order to invoke functions on LiveState
          type: string
          x-go-type-skip-optional-pointer: true
        Options:
          additionalProperties:
            type: string
          type: object
          x-go-type-skip-optional-pointer: true
        ProviderType:
          description: Type identifying a bridge implementation supported by the worker
          type: string
          x-go-type-skip-optional-pointer: true
        ToolchainType:
          description: Configuration toolchain and format implemented by this bridge
            of the worker
          type: string
          x-go-type-skip-optional-pointer: true
      type: object
    TargetCreateOrUpdateResponse:
      properties:
        Error:
          $ref: '#/components/schemas/ResponseError'
        Target:
          $ref: '#/components/schemas/Target'
      type: object
    TargetType2:
      properties:
        BridgeHandle:
          description: Identifier used by the Bridge to refer to discovered/enabled
            Target credentials and coordinates
          type: string
          x-go-type-skip-optional-pointer: true
        Name:
          description: Used to set the Slug and DisplayName of the Target created
            in ConfigHub. Optional.
          type: string
          x-go-type-skip-optional-pointer: true
        Params:
          additionalProperties: {}
          description: Deprecated. Used to set the Parameters of the Target created
            in ConfigHub
          type: object
          x-go-type-skip-optional-pointer: true
      type: object
    Trigger:
      description: |-
        Defines an automated function invocation that executes in response to specific
        Unit lifecycle events in ConfigHub. Triggers can be used to implement validation rules,
        automated transformations, or other custom logic that should run when configuration
        changes occur. Each Trigger is associated with a specific Space and can be configured
        to execute on events.

        Triggers can be either validating (checking configuration validity without modifying it)
        or mutating (making changes to the configuration). They can be disabled, and validating
        triggers can be set to Warn mode to produce non-blocking ApplyWarnings instead of ApplyGates.
      properties:
        Annotations:
          additionalProperties:
            maxLength: 1024
            pattern: ^(.|\n)*$
            type: string
          description: An optional map of Annotation key/value pairs for tools to
            attach information to entities.
          type: object
          x-go-type-skip-optional-pointer: true
        Arguments:
          description: Function arguments
          items:
            $ref: '#/components/schemas/FunctionArgument'
          nullable: true
          type: array
          x-go-type-skip-optional-pointer: true
        BridgeWorkerID:
          description: Unique identifier for a Bridge Worker to execute the function
            specified by the Trigger. If unspecified, use the builtin function executor.
          example: 248df4b7-aa70-47b8-a036-33ac447e668d
          format: uuid
          type: string
        CreatedAt:
          description: The timestamp when the entity was created in "2023-01-01T12:00:00Z"
            format.
          example: "2025-04-04T11:50:02.95102-07:00"
          format: date-time
          readOnly: true
          type: string
          x-go-type-skip-optional-pointer: true
        CursorID:
          description: An auto-incrementing sequence number used for pagination.
          format: int64
          readOnly: true
          type: integer
          x-go-type-skip-optional-pointer: true
        DeleteGates:
          additionalProperties:
            type: boolean
          description: An optional set of gates that, if any is present, will block
            deletion.
          type: object
          x-go-type-skip-optional-pointer: true
        Description:
          description: A longer description which explains what the trigger checks
            and how to fix validation failures. Shown as a pop-up when hovering over
            an ApplyGate in the UI.
          type: string
          x-go-type-skip-optional-pointer: true
        Disabled:
          description: "Disabled indicates whether this trigger is currently disabled.\n\t\tWhen
            disabled, the trigger will not be executed even when matching events occur."
          example: "false"
          type: boolean
          x-go-type-skip-optional-pointer: true
        DisplayName:
          description: Friendly name for the entity.
          type: string
          x-go-type-skip-optional-pointer: true
        EntityType:
          description: The type of entity.
          readOnly: true
          type: string
          x-go-type-skip-optional-pointer: true
        Event:
          description: Event specifies the type of event that will activate this trigger.
            Valid values are Mutation and PostClone
          type: string
          x-go-type-skip-optional-pointer: true
        FailOpenAfter:
          description: Duration after which a disconnected BridgeWorker's triggers
            are treated as fail-open. Can only be set when BridgeWorkerID is set.
          nullable: true
          type: integer
          x-go-type-skip-optional-pointer: true
        FunctionName:
          description: Function name
          type: string
          x-go-type-skip-optional-pointer: true
        Hash:
          description: SHA256 hash of the trigger's specification fields, used to
            detect changes.
          readOnly: true
          type: string
          x-go-type-skip-optional-pointer: true
        InvocationID:
          description: InvocationID is the identifier of the function to be invoked,
            if there is a corresponding Invocation.
          example: 248df4b7-aa70-47b8-a036-33ac447e668d
          format: uuid
          type: string
        Labels:
          additionalProperties:
            maxLength: 128
            pattern: ^[\-_@/#$%&~+=!?.,:;(){}\[\]<>|A-Za-z0-9](([\-_@/#$%&~+=!?.,:;(){}\[\]<>|A-Za-z0-9]|
              )*[\-_@/#$%&~+=!?.,:;(){}\[\]<>|A-Za-z0-9])?$
            type: string
          description: An optional map of Label key/value pairs to specify identifying
            attributes of entities for the purpose of grouping and filtering them.
          type: object
          x-go-type-skip-optional-pointer: true
        OrganizationID:
          description: Unique identifier for an organization.
          example: 248df4b7-aa70-47b8-a036-33ac447e668d
          format: uuid
          type: string
          x-go-type-skip-optional-pointer: true
        OtherDataSource:
          description: Specifies the source of additional configuration data to pass
            to functions that need it (e.g., vet-immutable needs LiveRevisionNum data).
            Uses revision specifier format such as LiveRevisionNum or Before:HeadRevisionNum.
          type: string
          x-go-type-skip-optional-pointer: true
        Slug:
          description: Unique URL-safe identifier for the entity.
          maxLength: 128
          pattern: ^[A-Za-z0-9]([\-_.A-Za-z0-9]*[A-Za-z0-9])?$
          type: string
          x-go-type-skip-optional-pointer: true
        SpaceID:
          description: Unique identifier for a space.
          example: 248df4b7-aa70-47b8-a036-33ac447e668d
          format: uuid
          type: string
          x-go-type-skip-optional-pointer: true
        SpaceSlug:
          description: Slug of the Space this entity belongs to. (readonly)
          readOnly: true
          type: string
          x-go-type-skip-optional-pointer: true
        ToolchainType:
          description: "ToolchainType specifies the type of toolchain this trigger
            works with.\n\t\tThis determines which configuration formats the trigger
            can process."
          example: Kubernetes/YAML
          required:
          - "true"
          type: string
          x-go-type-skip-optional-pointer: true
        TriggerID:
          description: TriggerID uniquely identifies a trigger within the system.
          example: 248df4b7-aa70-47b8-a036-33ac447e668d
          format: uuid
          type: string
          x-go-type-skip-optional-pointer: true
        UnitFilterID:
          description: References a Filter entity (with From=Unit) to restrict which
            Units this Trigger applies to.
          example: 248df4b7-aa70-47b8-a036-33ac447e668d
          format: uuid
          type: string
        UpdatedAt:
          description: The timestamp when the entity was last updated in "2023-01-01T12:00:00Z"
            format.
          example: "2025-04-04T11:50:02.95102-07:00"
          format: date-time
          readOnly: true
          type: string
          x-go-type-skip-optional-pointer: true
        Validating:
          description: "Validating indicates whether this is a validating function
            (true) or not (false).\n\t\tWhen false, the function can be either mutating
            (modifying configuration) or readonly returning an AttributeValueList
            (extracting values without modification).\n\t\tValidating functions check
            configuration validity without modifying it.\n\t\tThis value is returned
            by ConfigHub based on the corresponding property of the specified function."
          example: "true"
          readOnly: true
          type: boolean
          x-go-type-skip-optional-pointer: true
        Version:
          description: An entity-specific sequence number used for optimistic concurrency
            control. The value read must be sent in calls to Update.
          format: int64
          readOnly: false
          type: integer
          x-go-type-skip-optional-pointer: true
        Warn:
          description: Warn indicates whether this trigger produces ApplyWarnings
            instead of ApplyGates when its validating function fails. ApplyWarnings
            are non-blocking.
          example: "false"
          type: boolean
          x-go-type-skip-optional-pointer: true
        WhereResource:
          description: Restricts which resources within a Unit's configuration data
            the Trigger's function operates on, using ConfigHub metadata path expressions.
          type: string
          x-go-type-skip-optional-pointer: true
        WhereUnit:
          description: A filter expression to restrict which Units this Trigger applies
            to.
          type: string
          x-go-type-skip-optional-pointer: true
      required:
      - Slug
      - Event
      - ToolchainType
      type: object
    TriggerCreateOrUpdateResponse:
      properties:
        Error:
          $ref: '#/components/schemas/ResponseError'
        Trigger:
          $ref: '#/components/schemas/Trigger'
      type: object
    UUID:
      example: 248df4b7-aa70-47b8-a036-33ac447e668d
      format: uuid
      type: string
    Unit:
      description: |-
        Unit is the core unit of operation in ConfigHub. It contains a blob of configuration Data
        of a single supported Toolchain Type (configuration format). This blob is typically a text document
        that contains a collection of Kubernetes or infrastructure resources, or an application configuration
        file. Applying / deploying or destroying the configuration happens as a single *transaction*
        from ConfigHub's perspective. In reality, it is most often a multi-step workflow performed by
        the underlying configuration / deployment tool. The resources must belong to a single
        infrastructure provider and the actuation mechanism must be able to resolve references and
        ordering dependencies among the resources within the document. For example, if one resource
        needs to be fully provisioned to provide input to another resource, then the actuation code is
        responsible for handling this. Revisions store historical copies of the configuration data.
        Configuration data can be restored from prior Revisions. Units can also be cloned to create
        new variants of a configuration.
      properties:
        Annotations:
          additionalProperties:
            maxLength: 1024
            pattern: ^(.|\n)*$
            type: string
          description: An optional map of Annotation key/value pairs for tools to
            attach information to entities.
          type: object
          x-go-type-skip-optional-pointer: true
        ApplyGates:
          additionalProperties:
            type: boolean
          description: A map of "<space slug>/<trigger slug>/<function name>" to true
            of Triggers invoking validating functions that did not pass on the latest
            configuration data. These block Apply operations.
          readOnly: true
          type: object
          x-go-type-skip-optional-pointer: true
        ApplyWarnings:
          additionalProperties:
            type: boolean
          description: A map of "<space slug>/<trigger slug>/<function name>" to true
            of Triggers with Warn=true invoking validating functions that did not
            pass on the latest configuration data. These do not block Apply operations.
          readOnly: true
          type: object
          x-go-type-skip-optional-pointer: true
        ApprovedBy:
          description: The users that have approved the latest revision of the config
            data for the Unit.
          items:
            $ref: '#/components/schemas/UUID'
          nullable: true
          readOnly: true
          type: array
          x-go-type-skip-optional-pointer: true
        BridgeState:
          description: Additional state used by the Bridge; content is ProviderType-specific.
          format: base64
          readOnly: true
          type: string
          x-go-type-skip-optional-pointer: true
        BridgeWorkerID:
          description: ID of the BridgeWorker from the Target assigned to this Unit.
          example: 248df4b7-aa70-47b8-a036-33ac447e668d
          format: uuid
          readOnly: true
          type: string
        ChangeSetID:
          description: Unique identifier for the ChangeSet to which the current Revision
            belongs. Optional. Units are not required to belong to ChangeSets.
          example: 248df4b7-aa70-47b8-a036-33ac447e668d
          format: uuid
          type: string
        ContentHash:
          description: 'Deprecated: Use DataHash instead. The CRC32 hash of the configuration
            data.'
          readOnly: true
          type: integer
          x-go-type-skip-optional-pointer: true
        CreatedAt:
          description: The timestamp when the entity was created in "2023-01-01T12:00:00Z"
            format.
          example: "2025-04-04T11:50:02.95102-07:00"
          format: date-time
          readOnly: true
          type: string
          x-go-type-skip-optional-pointer: true
        CursorID:
          description: An auto-incrementing sequence number used for pagination.
          format: int64
          readOnly: true
          type: integer
          x-go-type-skip-optional-pointer: true
        Data:
          description: The full configuration data for this unit. The maximum size
            is 67108864 bytes.
          format: base64
          type: string
          x-go-type-skip-optional-pointer: true
        DataHash:
          description: The SHA256 hash of the configuration data, encoded as hexadecimal.
          readOnly: true
          type: string
          x-go-type-skip-optional-pointer: true
        DeleteGates:
          additionalProperties:
            type: boolean
          description: An optional set of gates that, if any is present, will block
            deletion.
          type: object
          x-go-type-skip-optional-pointer: true
        DestroyGates:
          additionalProperties:
            type: boolean
          description: An optional set of gates that, if any is present, will block
            destroy operations.
          type: object
          x-go-type-skip-optional-pointer: true
        DisplayName:
          description: Friendly name for the entity.
          type: string
          x-go-type-skip-optional-pointer: true
        DriftReconciliationMode:
          description: When the drift reconciliation mode is OnDemand, then the live
            state of the Target is updated only on Apply actions and the unit Data
            is updated only on Refresh actions. When the mode is ContinuousApply the
            live state is updated to match the last applied state when it has drifted
            from that state. When the mode is ContinuousRefresh, the unit Data is
            updated when it has drifted from the live state. The mode can be changed
            via the drift_mode parameter on Apply and Refresh operations. If the drift
            reconciliation mode is set in the opposing direction on the Unit (i.e.,
            ContinuousApply when Refresh is invoked or ContinuousRefresh when Apply
            is invoked) and is not changed to a compatible value, then the operation
            will fail.
          readOnly: true
          type: string
          x-go-type-skip-optional-pointer: true
        EntityType:
          description: The type of entity.
          readOnly: true
          type: string
          x-go-type-skip-optional-pointer: true
        FromLinkID:
          description: IDs of Links originating from this Unit.
          items:
            $ref: '#/components/schemas/UUID'
          nullable: true
          readOnly: true
          type: array
          x-go-type-skip-optional-pointer: true
        HeadMutationNum:
          description: Sequence number the head Mutation.
          format: int64
          readOnly: true
          type: integer
          x-go-type-skip-optional-pointer: true
        HeadRevisionNum:
          description: Sequence number the head Revision.
          format: int64
          readOnly: true
          type: integer
          x-go-type-skip-optional-pointer: true
        HeadUnitActionNum:
          description: Sequence number of the head unit action (queued operation).
          format: int64
          readOnly: true
          type: integer
          x-go-type-skip-optional-pointer: true
        HeadUnitEventNum:
          description: Sequence number of the head unit event.
          format: int64
          readOnly: true
          type: integer
          x-go-type-skip-optional-pointer: true
        Labels:
          additionalProperties:
            maxLength: 128
            pattern: ^[\-_@/#$%&~+=!?.,:;(){}\[\]<>|A-Za-z0-9](([\-_@/#$%&~+=!?.,:;(){}\[\]<>|A-Za-z0-9]|
              )*[\-_@/#$%&~+=!?.,:;(){}\[\]<>|A-Za-z0-9])?$
            type: string
          description: An optional map of Label key/value pairs to specify identifying
            attributes of entities for the purpose of grouping and filtering them.
          type: object
          x-go-type-skip-optional-pointer: true
        LastAppliedRevisionNum:
          description: Sequence number the last Revision applied. 0 if no live revision.
          format: int64
          readOnly: true
          type: integer
          x-go-type-skip-optional-pointer: true
        LastChangeDescription:
          description: LastChangeDescription is a human-readable description of the
            last change. This description is copied to the new Revision when the Data
            is changed.
          type: string
          x-go-type-skip-optional-pointer: true
        LiveData:
          description: The live resources as of the most recent non-dry-run action
            in the same representation as Data.
          format: base64
          readOnly: true
          type: string
          x-go-type-skip-optional-pointer: true
        LiveRevisionNum:
          description: Sequence number the last Revision applied once apply has completed.
            0 if no live revision.
          format: int64
          readOnly: true
          type: integer
          x-go-type-skip-optional-pointer: true
        LiveState:
          description: The live state as of the most recent non-dry-run action; content
            is ProviderType-specific.
          format: base64
          readOnly: true
          type: string
          x-go-type-skip-optional-pointer: true
        MutationSources:
          $ref: '#/components/schemas/ResourceMutationList'
        NeededPaths:
          description: Attribute paths that this Unit needs from upstream Units via
            NeedsProvides Links. Computed from get-needed and stored on data updates.
          items:
            $ref: '#/components/schemas/AttributeValue'
          readOnly: true
          type: array
          x-go-type-skip-optional-pointer: true
        OrganizationID:
          description: Unique identifier for an organization.
          example: 248df4b7-aa70-47b8-a036-33ac447e668d
          format: uuid
          type: string
          x-go-type-skip-optional-pointer: true
        PreviousLiveRevisionNum:
          description: Sequence number the previous Revision applied. 0 if no live
            revision.
          format: int64
          readOnly: true
          type: integer
          x-go-type-skip-optional-pointer: true
        ProvidedPaths:
          description: Attribute paths that this Unit provides to downstream Units
            via NeedsProvides Links. Computed from get-provided and stored on data
            updates.
          items:
            $ref: '#/components/schemas/AttributeInfo'
          readOnly: true
          type: array
          x-go-type-skip-optional-pointer: true
        ProviderType:
          description: ProviderType identifies which bridge to use in the case that
            the Target supports multiple ProviderTypes.
          type: string
          x-go-type-skip-optional-pointer: true
        Slug:
          description: Unique URL-safe identifier for the entity.
          maxLength: 128
          pattern: ^[A-Za-z0-9]([\-_.A-Za-z0-9]*[A-Za-z0-9])?$
          type: string
          x-go-type-skip-optional-pointer: true
        SpaceID:
          description: Unique identifier for a space.
          example: 248df4b7-aa70-47b8-a036-33ac447e668d
          format: uuid
          type: string
          x-go-type-skip-optional-pointer: true
        SpaceSlug:
          description: Slug of the Space this entity belongs to. (readonly)
          readOnly: true
          type: string
          x-go-type-skip-optional-pointer: true
        TargetID:
          description: TargetID is the identifier of the target this unit is associated
            with. This defines where the configuration will be applied. It must be
            set to a valid Target within the same Space before the Unit can be Applied,
            Destroyed, Imported, or Refreshed.
          example: 248df4b7-aa70-47b8-a036-33ac447e668d
          format: uuid
          type: string
        TargetOptions:
          additionalProperties:
            type: string
          description: Bridge option values set per-Unit, merged with the Target's
            Options when sending to the bridge worker (Target's Options take precedence
            on overlap). The options must be predefined by the ConfigType in the BridgeWorker.
          type: object
          x-go-type-skip-optional-pointer: true
        ToolchainType:
          description: ToolchainType specifies the type of toolchain for this unit.
            Possible values include "Kubernetes/YAML", "OpenTofu/HCL", "AppConfig/Properties",
            "AppConfig/YAML", "AppConfig/TOML", "AppConfig/INI", "AppConfig/JSON",
            "AppConfig/Env", "AppConfig/Text", "ConfigHub/YAML".
          type: string
          x-go-type-skip-optional-pointer: true
        UnitID:
          description: Unique identifier for a Unit.
          example: 248df4b7-aa70-47b8-a036-33ac447e668d
          format: uuid
          type: string
          x-go-type-skip-optional-pointer: true
        UpdatedAt:
          description: The timestamp when the entity was last updated in "2023-01-01T12:00:00Z"
            format.
          example: "2025-04-04T11:50:02.95102-07:00"
          format: date-time
          readOnly: true
          type: string
          x-go-type-skip-optional-pointer: true
        UpstreamOrganizationID:
          description: Unique identifier for the Organization of the Unit this unit
            was cloned from, if any.
          example: 248df4b7-aa70-47b8-a036-33ac447e668d
          format: uuid
          readOnly: true
          type: string
        UpstreamRevisionNum:
          description: Sequence number for the Revision of the Unit this unit was
            cloned from, or 0. This is updated to the upstream Unit's head revision
            number when the Unit is upgraded. To change this revision number, change
            the UpstreamLastMergedRevisionNum of the corresponding Link of UpdateType
            UpgradeUnit from this Unit to the upstream Unit.
          format: int64
          readOnly: true
          type: integer
          x-go-type-skip-optional-pointer: true
        UpstreamSpaceID:
          description: Unique identifier for the Space of the Unit this unit was cloned
            from, if any.
          example: 248df4b7-aa70-47b8-a036-33ac447e668d
          format: uuid
          readOnly: true
          type: string
        UpstreamUnitID:
          description: Unique identifier for Unit this unit was cloned from, if any.
            To change or remove the upstream unit, change or delete the corresponding
            Link of UpdateType UpgradeUnit from this Unit to the upstream Unit.
          example: 248df4b7-aa70-47b8-a036-33ac447e668d
          format: uuid
          readOnly: true
          type: string
        ValidationResults:
          additionalProperties:
            $ref: '#/components/schemas/ValidationResultList'
          description: A map from gate/warning name to the list of validation results
            that caused the gate or warning.
          readOnly: true
          type: object
          x-go-type-skip-optional-pointer: true
        Values:
          additionalProperties:
            type: string
          description: Map from "<trigger slug>/<attribute name>" to the first output
            Value with that attribute name of the function invocation specified by
            the Trigger.
          readOnly: true
          type: object
          x-go-type-skip-optional-pointer: true
        Version:
          description: An entity-specific sequence number used for optimistic concurrency
            control. The value read must be sent in calls to Update.
          format: int64
          readOnly: false
          type: integer
          x-go-type-skip-optional-pointer: true
      required:
      - Slug
      - ToolchainType
      type: object
    UnitAction:
      description: "UnitAction is a record of an action to be performed by a Bridge
        Worker. They are queued and sent to the worker in creation order. \nIf the
        worker is temporarily disconnected the queued actions will be sent when the
        worker reconnects or responds.\nIf there are links between units applied or
        destroyed in a single API call, they will be sent to the appropriate\nworker(s)
        in the appropriate order (reverse or forword topological order). One or more
        UnitEvents will correspond\nto each UnitAction."
      properties:
        Action:
          $ref: '#/components/schemas/ActionType'
        BridgeState:
          format: base64
          type: string
          x-go-type-skip-optional-pointer: true
        BridgeWorkerID:
          description: BridgeWorkerID is the unique identifier of the bridge worker
            that will process this operation.
          example: 248df4b7-aa70-47b8-a036-33ac447e668d
          format: uuid
          type: string
          x-go-type-skip-optional-pointer: true
        CreatedAt:
          description: The timestamp when the entity was created in "2023-01-01T12:00:00Z"
            format.
          example: 2006-01-02T15:04:05Z07:00
          format: date-time
          type: string
          x-go-type-skip-optional-pointer: true
        Data:
          description: The result of a dry-run Data-changing action like refresh and
            import, where the data is not stored in the Unit.
          format: base64
          type: string
          x-go-type-skip-optional-pointer: true
        Dependencies:
          description: Dependencies contains the list of operation IDs that this operation
            depends on. Operations will not be delivered until all dependencies are
            completed.
          items:
            $ref: '#/components/schemas/UUID'
          nullable: true
          type: array
          x-go-type-skip-optional-pointer: true
        DriftReconciliationMode:
          description: The drift reconciliation mode for the unit at the time of the
            operation.
          type: string
          x-go-type-skip-optional-pointer: true
        DryRun:
          description: DryRun indicates whether the action is a dry run.
          type: boolean
          x-go-type-skip-optional-pointer: true
        ErrorDetails:
          description: Error details returned by the worker.
          items:
            $ref: '#/components/schemas/ErrorItem'
          type: array
          x-go-type-skip-optional-pointer: true
        ExtraParams:
          description: ExtraParams contains additional parameters for the operation
            in string format.
          type: string
          x-go-type-skip-optional-pointer: true
        LiveData:
          format: base64
          type: string
          x-go-type-skip-optional-pointer: true
        LiveState:
          format: base64
          type: string
          x-go-type-skip-optional-pointer: true
        OrganizationID:
          description: OrganizationID is the unique identifier of the organization
            this operation belongs to.
          example: 248df4b7-aa70-47b8-a036-33ac447e668d
          format: uuid
          type: string
          x-go-type-skip-optional-pointer: true
        QueuedOperationID:
          description: QueuedOperationID is the unique identifier for the queued unit
            action.
          example: 248df4b7-aa70-47b8-a036-33ac447e668d
          format: uuid
          type: string
          x-go-type-skip-optional-pointer: true
        RevisionNum:
          description: RevisionNum is the revision number this operation was performed
            on.
          format: int64
          type: integer
          x-go-type-skip-optional-pointer: true
        SpaceID:
          description: SpaceID is the unique identifier of the space of the unit this
            operation is performed on.
          example: 248df4b7-aa70-47b8-a036-33ac447e668d
          format: uuid
          type: string
          x-go-type-skip-optional-pointer: true
        Status:
          description: 'Status indicates the current status of the unit action. v2
            statuses: Initializing (being set up), Pending (waiting), Delivered (sent
            to worker), Progressing (being processed), Completed (success), Failed
            (error). v1 compatibility: ''pending'' = Pending, ''delivered'' = Completed
            (legacy ''delivered'' meant work done).'
          enum:
          - Initializing
          - Pending
          - Delivered
          - Progressing
          - Completed
          - Failed
          - Aborted
          - Canceled
          - pending
          - delivered
          type: string
          x-go-type-skip-optional-pointer: true
        TargetID:
          description: TargetID is the unique identifier of the target this operation
            is directed to.
          example: 248df4b7-aa70-47b8-a036-33ac447e668d
          format: uuid
          type: string
          x-go-type-skip-optional-pointer: true
        UnitActionNum:
          description: UnitActionNum is the sequence number of this unit action.
          format: int64
          type: integer
          x-go-type-skip-optional-pointer: true
        UnitID:
          description: UnitID is the unique identifier of the unit this operation
            is performed on.
          example: 248df4b7-aa70-47b8-a036-33ac447e668d
          format: uuid
          type: string
          x-go-type-skip-optional-pointer: true
        UserAgent:
          description: User-Agent string of the API call.
          type: string
          x-go-type-skip-optional-pointer: true
        UserID:
          description: UserID of the user the action was performed by.
          example: 248df4b7-aa70-47b8-a036-33ac447e668d
          format: uuid
          type: string
          x-go-type-skip-optional-pointer: true
        UserIsBot:
          description: Whether the user who requested the action is a bot user.
          type: boolean
          x-go-type-skip-optional-pointer: true
        UserRole:
          description: Organization-level role of the user who requested the action.
          type: string
          x-go-type-skip-optional-pointer: true
        Version:
          description: |-
            An entity-specific sequence number used for optimistic concurrency control.
            The value read must be sent in calls to Update.
          format: int64
          type: integer
          x-go-type-skip-optional-pointer: true
      type: object
    UnitActionResponse:
      properties:
        Action:
          $ref: '#/components/schemas/QueuedOperation'
        Error:
          $ref: '#/components/schemas/ResponseError'
      type: object
    UnitCreateOrUpdateResponse:
      properties:
        Conflicts:
          $ref: '#/components/schemas/MutationConflictList'
        Error:
          $ref: '#/components/schemas/ResponseError'
        Links:
          items:
            $ref: '#/components/schemas/LinkCreateOrUpdateResponse'
          type: array
          x-go-type-skip-optional-pointer: true
        Unit:
          $ref: '#/components/schemas/Unit'
      type: object
    UnitEvent:
      description: |-
        UnitEvent represents an event of action performed on a Unit's configuration. Each action tracks
        the lifecycle of applying, destroying, or refreshing a Unit's configuration in the target
        live system. The event captures the current status of the operation, any configuration
        drift detected, and timing information about when the action started and completed.
        Actions are atomic from ConfigHub's perspective but may involve multiple steps
        in the connected Bridge. The status and drift detection help track the health
        and consistency of the provisioned configuration compared to what is defined in the Unit.
      properties:
        Action:
          $ref: '#/components/schemas/ActionType'
        BridgeWorkerID:
          description: BridgeWorkerID is the ID of the bridge worker that performed
            this action. This field is populated from the Target's BridgeWorkerID
            when the event is created.
          example: 248df4b7-aa70-47b8-a036-33ac447e668d
          format: uuid
          type: string
        CreatedAt:
          description: The timestamp when the entity was created in "2023-01-01T12:00:00Z"
            format.
          example: "2025-04-04T11:50:02.95102-07:00"
          format: date-time
          readOnly: true
          type: string
          x-go-type-skip-optional-pointer: true
        CursorID:
          description: An auto-incrementing sequence number used for pagination.
          format: int64
          readOnly: true
          type: integer
          x-go-type-skip-optional-pointer: true
        EntityType:
          description: The type of entity.
          readOnly: true
          type: string
          x-go-type-skip-optional-pointer: true
        Message:
          type: string
          x-go-type-skip-optional-pointer: true
        OrganizationID:
          description: Unique identifier for an Organization.
          example: 248df4b7-aa70-47b8-a036-33ac447e668d
          format: uuid
          type: string
          x-go-type-skip-optional-pointer: true
        QueuedOperationID:
          description: QueuedOperationID is the unique identifier for the corresponding
            queued operation.
          example: 248df4b7-aa70-47b8-a036-33ac447e668d
          format: uuid
          type: string
          x-go-type-skip-optional-pointer: true
        ResourceStatuses:
          $ref: '#/components/schemas/ResourceStatusMap'
        Result:
          $ref: '#/components/schemas/ActionResultType'
        RevisionNum:
          format: int64
          type: integer
          x-go-type-skip-optional-pointer: true
        SpaceID:
          description: Unique identifier for a space.
          example: 248df4b7-aa70-47b8-a036-33ac447e668d
          format: uuid
          type: string
          x-go-type-skip-optional-pointer: true
        SpaceSlug:
          description: Slug of the Space this entity belongs to. (readonly)
          readOnly: true
          type: string
          x-go-type-skip-optional-pointer: true
        StartedAt:
          example: 2006-01-02T15:04:05Z07:00
          format: date-time
          type: string
          x-go-type-skip-optional-pointer: true
        Status:
          $ref: '#/components/schemas/ActionStatusType'
        TerminatedAt:
          format: date-time
          nullable: true
          type: string
          x-go-type-skip-optional-pointer: true
        UnitEventID:
          example: 248df4b7-aa70-47b8-a036-33ac447e668d
          format: uuid
          type: string
          x-go-type-skip-optional-pointer: true
        UnitEventNum:
          description: Sequence number for this unit event.
          format: int64
          type: integer
          x-go-type-skip-optional-pointer: true
        UnitID:
          description: Unique identifier for a Unit.
          example: 248df4b7-aa70-47b8-a036-33ac447e668d
          format: uuid
          type: string
          x-go-type-skip-optional-pointer: true
        UpdatedAt:
          description: The timestamp when the entity was last updated in "2023-01-01T12:00:00Z"
            format.
          example: "2025-04-04T11:50:02.95102-07:00"
          format: date-time
          readOnly: true
          type: string
          x-go-type-skip-optional-pointer: true
        Version:
          description: An entity-specific sequence number used for optimistic concurrency
            control. The value read must be sent in calls to Update.
          format: int64
          readOnly: false
          type: integer
          x-go-type-skip-optional-pointer: true
      type: object
    UnitExtended:
      properties:
        Action:
          $ref: '#/components/schemas/ActionType'
        ActionResult:
          $ref: '#/components/schemas/ActionResultType'
        ActionStartedAt:
          format: date-time
          nullable: true
          type: string
          x-go-type-skip-optional-pointer: true
        ActionTerminatedAt:
          format: date-time
          nullable: true
          type: string
          x-go-type-skip-optional-pointer: true
        ApprovedByUsers:
          items:
            type: string
          nullable: true
          type: array
          x-go-type-skip-optional-pointer: true
        Drift:
          type: string
          x-go-type-skip-optional-pointer: true
        FromLinks:
          items:
            $ref: '#/components/schemas/Link'
          nullable: true
          type: array
          x-go-type-skip-optional-pointer: true
        ResourceStatusSummary:
          $ref: '#/components/schemas/ResourceStatusSummary'
        Status:
          type: string
          x-go-type-skip-optional-pointer: true
        SyncStatus:
          type: string
          x-go-type-skip-optional-pointer: true
        ToLinks:
          items:
            $ref: '#/components/schemas/Link'
          nullable: true
          type: array
          x-go-type-skip-optional-pointer: true
        Unit:
          $ref: '#/components/schemas/Unit'
      type: object
    UnitStatus:
      properties:
        Action:
          $ref: '#/components/schemas/ActionType'
        ActionResult:
          $ref: '#/components/schemas/ActionResultType'
        ActionStartedAt:
          format: date-time
          nullable: true
          type: string
          x-go-type-skip-optional-pointer: true
        ActionTerminatedAt:
          format: date-time
          nullable: true
          type: string
          x-go-type-skip-optional-pointer: true
        Drift:
          type: string
          x-go-type-skip-optional-pointer: true
        ResourceStatusSummary:
          $ref: '#/components/schemas/ResourceStatusSummary'
        Status:
          type: string
          x-go-type-skip-optional-pointer: true
        SyncStatus:
          type: string
          x-go-type-skip-optional-pointer: true
      type: object
    UnitTagRequest:
      properties:
        Revision:
          description: 'Which Unit revision to tag: ''HeadRevisionNum'', ''LiveRevisionNum'',
            ''LastAppliedRevisionNum'', ''PreviousLiveRevisionNum'', or ''Remove''
            to remove the tag from the unit'
          type: string
          x-go-type-skip-optional-pointer: true
        TagID:
          example: 248df4b7-aa70-47b8-a036-33ac447e668d
          format: uuid
          type: string
          x-go-type-skip-optional-pointer: true
      type: object
    UnitTagResponse:
      properties:
        Error:
          $ref: '#/components/schemas/ResponseError'
        Message:
          type: string
          x-go-type-skip-optional-pointer: true
      type: object
    User:
      description: a User in Confighub.
      properties:
        CreatedAt:
          description: The timestamp when the entity was created in "2023-01-01T12:00:00Z"
            format.
          example: "2025-04-04T11:50:02.95102-07:00"
          format: date-time
          readOnly: true
          type: string
          x-go-type-skip-optional-pointer: true
        CursorID:
          description: An auto-incrementing sequence number used for pagination.
          format: int64
          readOnly: true
          type: integer
          x-go-type-skip-optional-pointer: true
        DisplayName:
          description: Friendly name for the entity.
          type: string
          x-go-type-skip-optional-pointer: true
        EntityType:
          description: The type of entity.
          readOnly: true
          type: string
          x-go-type-skip-optional-pointer: true
        ExternalID:
          description: Unique identifier for the External Identity Provider record
            matching this User.
          type: string
          x-go-type-skip-optional-pointer: true
        ProfilePictureURL:
          description: The URL to get the profile avatar picture of the User.
          type: string
          x-go-type-skip-optional-pointer: true
        Slug:
          description: Unique URL-safe identifier for the entity.
          maxLength: 128
          pattern: ^[A-Za-z0-9]([\-_.A-Za-z0-9]*[A-Za-z0-9])?$
          type: string
          x-go-type-skip-optional-pointer: true
        UpdatedAt:
          description: The timestamp when the entity was last updated in "2023-01-01T12:00:00Z"
            format.
          example: "2025-04-04T11:50:02.95102-07:00"
          format: date-time
          readOnly: true
          type: string
          x-go-type-skip-optional-pointer: true
        UserID:
          description: Unique identifier for a User.
          example: 248df4b7-aa70-47b8-a036-33ac447e668d
          format: uuid
          type: string
          x-go-type-skip-optional-pointer: true
        Username:
          description: Unique username for a User. Must be unique for all of Confighub.
          type: string
          x-go-type-skip-optional-pointer: true
        Version:
          description: An entity-specific sequence number used for optimistic concurrency
            control. The value read must be sent in calls to Update.
          format: int64
          readOnly: false
          type: integer
          x-go-type-skip-optional-pointer: true
      required:
      - Slug
      type: object
    ValidationResult:
      properties:
        Details:
          description: Deprecated. Use Issues or FailedAttributes instead. Optional
            list of failure details when not associated with specific attributes/paths.
          items:
            type: string
          type: array
          x-go-type-skip-optional-pointer: true
        FailedAttributes:
          $ref: '#/components/schemas/AttributeValueList'
        FunctionName:
          description: Name of the function invocation corresponding to the result
          type: string
          x-go-type-skip-optional-pointer: true
        Index:
          description: Index of the function invocation corresponding to the result.
            Useful in the case that multiple function invocations in the same executor
            call return ValidationResultList output.
          type: integer
          x-go-type-skip-optional-pointer: true
        Issues:
          description: Issues found with the configuration unit that are not associated
            with specific attributes/paths. Use FailedAttributes where possible.
          items:
            $ref: '#/components/schemas/Issue'
          type: array
          x-go-type-skip-optional-pointer: true
        MaxScore:
          description: Maximum score of all findings
          type: string
          x-go-type-skip-optional-pointer: true
        Passed:
          description: True if valid, false otherwise
          type: boolean
          x-go-type-skip-optional-pointer: true
      type: object
    ValidationResultList:
      items:
        $ref: '#/components/schemas/ValidationResult'
      type: array
    View:
      description: Defines an entity view.
      properties:
        Annotations:
          additionalProperties:
            maxLength: 1024
            pattern: ^(.|\n)*$
            type: string
          description: An optional map of Annotation key/value pairs for tools to
            attach information to entities.
          type: object
          x-go-type-skip-optional-pointer: true
        Columns:
          description: Columns to display, in order. (optional)
          items:
            $ref: '#/components/schemas/Column'
          type: array
          x-go-type-skip-optional-pointer: true
        CreatedAt:
          description: The timestamp when the entity was created in "2023-01-01T12:00:00Z"
            format.
          example: "2025-04-04T11:50:02.95102-07:00"
          format: date-time
          readOnly: true
          type: string
          x-go-type-skip-optional-pointer: true
        CursorID:
          description: An auto-incrementing sequence number used for pagination.
          format: int64
          readOnly: true
          type: integer
          x-go-type-skip-optional-pointer: true
        DeleteGates:
          additionalProperties:
            type: boolean
          description: An optional set of gates that, if any is present, will block
            deletion.
          type: object
          x-go-type-skip-optional-pointer: true
        DisplayName:
          description: Friendly name for the entity.
          type: string
          x-go-type-skip-optional-pointer: true
        EntityType:
          description: The type of entity.
          readOnly: true
          type: string
          x-go-type-skip-optional-pointer: true
        FilterID:
          description: FilterID identifies a filter. At least one of FilterID or Of
            must be specified. (optional)
          example: 248df4b7-aa70-47b8-a036-33ac447e668d
          format: uuid
          type: string
        GroupBy:
          description: Column to group by (optional).
          type: string
          x-go-type-skip-optional-pointer: true
        Labels:
          additionalProperties:
            maxLength: 128
            pattern: ^[\-_@/#$%&~+=!?.,:;(){}\[\]<>|A-Za-z0-9](([\-_@/#$%&~+=!?.,:;(){}\[\]<>|A-Za-z0-9]|
              )*[\-_@/#$%&~+=!?.,:;(){}\[\]<>|A-Za-z0-9])?$
            type: string
          description: An optional map of Label key/value pairs to specify identifying
            attributes of entities for the purpose of grouping and filtering them.
          type: object
          x-go-type-skip-optional-pointer: true
        Of:
          description: Entity type to view (e.g., Unit, Space). At least one of FilterID
            or Of must be specified. If both are specified, Of must match Filter.From.
            (optional)
          type: string
          x-go-type-skip-optional-pointer: true
        OrderBy:
          description: Column to sort by. (optional)
          type: string
          x-go-type-skip-optional-pointer: true
        OrderByDirection:
          description: Columnn sort order, ASC or DESC. Default is ASC. Only should
            be specified if OrderBy is specified. (optional)
          type: string
          x-go-type-skip-optional-pointer: true
        OrganizationID:
          description: Unique identifier for an organization.
          example: 248df4b7-aa70-47b8-a036-33ac447e668d
          format: uuid
          type: string
          x-go-type-skip-optional-pointer: true
        Slug:
          description: Unique URL-safe identifier for the entity.
          maxLength: 128
          pattern: ^[A-Za-z0-9]([\-_.A-Za-z0-9]*[A-Za-z0-9])?$
          type: string
          x-go-type-skip-optional-pointer: true
        SpaceID:
          description: Unique identifier for a space.
          example: 248df4b7-aa70-47b8-a036-33ac447e668d
          format: uuid
          type: string
          x-go-type-skip-optional-pointer: true
        SpaceSlug:
          description: Slug of the Space this entity belongs to. (readonly)
          readOnly: true
          type: string
          x-go-type-skip-optional-pointer: true
        UpdatedAt:
          description: The timestamp when the entity was last updated in "2023-01-01T12:00:00Z"
            format.
          example: "2025-04-04T11:50:02.95102-07:00"
          format: date-time
          readOnly: true
          type: string
          x-go-type-skip-optional-pointer: true
        Version:
          description: An entity-specific sequence number used for optimistic concurrency
            control. The value read must be sent in calls to Update.
          format: int64
          readOnly: false
          type: integer
          x-go-type-skip-optional-pointer: true
        ViewID:
          description: ViewID uniquely identifies a view within the system.
          example: 248df4b7-aa70-47b8-a036-33ac447e668d
          format: uuid
          type: string
          x-go-type-skip-optional-pointer: true
      required:
      - Slug
      type: object
    ViewColumn:
      properties:
        Name:
          type: string
          x-go-type-skip-optional-pointer: true
        Value:
          type: string
          x-go-type-skip-optional-pointer: true
      type: object
    ViewCreateOrUpdateResponse:
      properties:
        Error:
          $ref: '#/components/schemas/ResponseError'
        View:
          $ref: '#/components/schemas/View'
      type: object
    WorkerInfo:
      properties:
        BridgeWorkerInfo:
          $ref: '#/components/schemas/BridgeWorkerInfo'
        FunctionWorkerInfo:
          $ref: '#/components/schemas/FunctionWorkerInfo'
        IsServerWorker:
          description: If true, this is a server-hosted worker.
          type: boolean
          x-go-type-skip-optional-pointer: true
        UseUserIdentity:
          description: If true, the server worker operates using the requesting user's
            identity rather than the worker's bot identity. Requires IsServerWorker
            to be true.
          type: boolean
          x-go-type-skip-optional-pointer: true
      type: object
  securitySchemes:
    bearerAuth:
      bearerFormat: JWT
      description: Bearer authentication
      scheme: bearer
      type: http
